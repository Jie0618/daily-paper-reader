---
title: On Surprising Effectiveness of Masking Updates in Adaptive Optimizers
title_zh: 论自适应优化器中掩码更新的惊人有效性
authors: "Taejong Joo, Wenhan Xia, Cheolmin Kim, Ming Zhang, Eugene Ie"
date: 2026-02-17
pdf: "https://arxiv.org/pdf/2602.15322v1"
tags: ["query:opt-lm"]
score: 9.0
evidence: 用于大语言模型预训练的自适应优化器
tldr: 本研究挑战了大语言模型训练中对密集自适应优化器的依赖，发现随机掩码参数更新具有惊人效果。通过分析掩码带来的曲率相关几何正则化，作者提出了Magma（动量对齐梯度掩码）优化器。Magma利用动量与梯度的对齐程度来调制更新，在1B规模模型预训练中表现优异，显著降低了困惑度，且计算开销极低，为大模型优化提供了新思路。
selection_source: fresh_fetch
motivation: 旨在探索在自适应优化器中引入随机掩码更新是否能提升大语言模型的训练效率和性能。
method: 提出Magma优化器，通过动量-梯度对齐机制动态调制掩码更新，实现几何正则化。
result: "实验显示Magma在1B模型上比Adam和Muon分别降低了19%和9%的困惑度。"
conclusion: 随机掩码更新能有效平滑优化轨迹，是提升自适应优化器性能的一种简单且高效的手段。
---

## 摘要
大语言模型（LLMs）的训练几乎完全依赖于具有日益复杂的预条件矩阵（preconditioners）的稠密自适应优化器。我们对此提出了挑战，证明了随机掩码参数更新可以非常有效，其中 RMSProp 的掩码变体在性能上持续优于近期最先进的优化器。我们的分析表明，随机掩码会诱导一种依赖于曲率的几何正则化，从而平滑优化轨迹。基于这一发现，我们引入了动量对齐梯度掩码（Momentum-aligned gradient masking，简称 Magma），它利用动量-梯度对齐来调节掩码更新。大规模 LLM 预训练实验表明，Magma 是自适应优化器的一种简单即插即用替代方案，具有持续的增益且计算开销可以忽略不计。值得注意的是，在 1B 模型规模下，与 Adam 和 Muon 相比，Magma 分别将困惑度（perplexity）降低了 19% 以上和 9% 以上。

## Abstract
Training large language models (LLMs) relies almost exclusively on dense adaptive optimizers with increasingly sophisticated preconditioners. We challenge this by showing that randomly masking parameter updates can be highly effective, with a masked variant of RMSProp consistently outperforming recent state-of-the-art optimizers. Our analysis reveals that the random masking induces a curvature-dependent geometric regularization that smooths the optimization trajectory. Motivated by this finding, we introduce Momentum-aligned gradient masking (Magma), which modulates the masked updates using momentum-gradient alignment. Extensive LLM pre-training experiments show that Magma is a simple drop-in replacement for adaptive optimizers with consistent gains and negligible computational overhead. Notably, for the 1B model size, Magma reduces perplexity by over 19\% and 9\% compared to Adam and Muon, respectively.

---

## 论文详细总结（自动生成）

这是一份关于论文《On Surprising Effectiveness of Masking Updates in Adaptive Optimizers》的结构化总结：

### 1. 核心问题与整体含义
*   **研究背景**：当前大语言模型（LLM）的训练几乎完全依赖于稠密的自适应优化器（如 Adam、RMSProp）。这些优化器在每一步都会更新所有参数。
*   **核心问题**：论文挑战了“稠密更新必然优于稀疏更新”的传统假设。作者发现，在训练过程中随机“掩码”（即跳过）部分参数块的更新，反而能显著提升模型的性能和泛化能力。
*   **研究动机**：探索随机掩码更新背后的数学机理，并利用这一发现改进现有的自适应优化器，以应对 Transformer 模型中复杂且不平滑的优化景观。

### 2. 方法论
*   **核心思想**：通过随机丢弃部分参数更新，引入一种隐式的、与曲率相关的“几何正则化”，促使模型向损失平面更平坦的区域收敛。
*   **关键技术细节**：
    *   **SkipUpdate**：一种基础变体。在每一步迭代中，以 Bernoulli 分布随机决定是否更新某个参数块。关键点在于：**参数更新被掩码，但动量状态（Momentum）依然保持稠密更新**，以维持搜索方向的准确性。
    *   **Magma (Momentum-aligned gradient masking)**：在 SkipUpdate 基础上引入了“动量-梯度对齐”机制。
        *   **对齐评分**：计算当前随机梯度与一阶矩（动量）估计之间的余弦相似度。
        *   **动态调制**：利用该相似度通过 Sigmoid 函数生成缩放因子，并结合 EMA（指数移动平均）平滑处理。
        *   **更新规则**：只有当当前梯度方向与长期动量方向一致时，才赋予较大的更新权重；否则抑制更新。
*   **算法流程**：Magma 作为一个“包装器”（Wrapper），可以套用在 Adam、RMSProp 或 Muon 等任何基础优化器上，通过简单的乘法操作调制最终的更新步长。

### 3. 实验设计
*   **数据集与场景**：
    *   **Llama 2 预训练**：使用 C4 数据集，模型规模涵盖 60M、130M、350M 和 1B。
    *   **Nano MoE 预训练**：在 OpenWebText 数据集上训练混合专家模型（MoE），验证其在复杂架构下的稳定性。
    *   **受控合成实验**：包括重尾噪声分布实验、异构二次函数优化实验，以及 ResNet-50 在 CIFAR-10 上的对比。
*   **Benchmark（对比方法）**：
    *   基础优化器：Adam, RMSProp, Adafactor, LaProp。
    *   先进/矩阵优化器：Muon (近期 SOTA), SOAP, APOLLO。
    *   增强插件：Cautious Adam (C-Adam), SGG (Gradient Grouping)。

### 4. 资源与算力
*   **明确提及的算力**：在 Nano MoE 实验中，明确使用了 **8x NVIDIA A100 GPU**。
*   **训练时长/规模**：Llama 预训练根据模型大小设定了不同的迭代步数，例如 60M 模型训练 10K 步，1B 模型训练 100K 步。
*   **计算开销**：论文强调 Magma 的计算和内存开销**几乎可以忽略不计**，因为它不涉及复杂的矩阵分解或二阶导数显式计算。

### 5. 实验数量与充分性
*   **实验组数**：论文进行了多维度的大规模实验，包括 4 种模型规模的横向对比、MoE 架构的纵向验证，以及针对掩码粒度（元素级 vs 块级）、采样率、温度参数等进行的详尽消融实验。
*   **充分性与客观性**：实验设计较为全面，不仅在标准 Benchmark 上刷榜，还通过合成实验深入探讨了 Magma 在处理“重尾噪声”和“异构曲率”时的优势。对比对象包含了 2024-2025 年间最新的优化器技术，具有较强的时效性和说服力。

### 6. 主要结论与发现
*   **性能飞跃**：Magma 显著优于所有对比的稠密优化器。在 1B 模型规模下，相比 Adam 困惑度降低了 19%，相比 Muon 降低了 9%。
*   **几何正则化**：理论证明随机掩码等同于在期望上增加了一个 Hessian 惩罚项，能够自动抑制损失平面上“尖锐”方向的更新。
*   **扩展性**：Magma 的优势随模型规模增大而增强，这表明大型模型更需要这种几何正则化来稳定训练。
*   **架构特异性**：Magma 在 Transformer 上表现极佳，但在 CNN（如 ResNet）上几乎没有增益，这反映了 Transformer 独特的损失平面特征（高度异构的块对角 Hessian 结构）。

### 7. 优点
*   **极简实现**：仅需几行代码即可集成到现有训练框架中，无需改变模型结构。
*   **高效性**：在不增加显存和计算量的前提下，达到了甚至超过了二阶优化器（如 Muon, SOAP）的效果。
*   **鲁棒性**：实验显示 Magma 显著扩大了学习率的可选范围，使训练对超参数不那么敏感。

### 8. 不足与局限
*   **应用限制**：目前仅在 Transformer 架构上验证了有效性，对于其他类型的神经网络（如 CNN）缺乏普适性。
*   **理论偏差**：Magma 的阻尼机制（Damping）虽然在实践中很稳定，但在数学上引入了更新偏差（Biased updates），未来仍需探索更完美的无偏方案。
*   **验证规模**：虽然涵盖了 1B 参数，但尚未在 70B 或 400B 等超大规模工业级模型上进行验证。

（完）
