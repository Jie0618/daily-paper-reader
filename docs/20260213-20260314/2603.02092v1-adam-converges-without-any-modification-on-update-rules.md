---
title: Adam Converges Without Any Modification On Update Rules
title_zh: Adam 在不修改更新规则的情况下收敛
authors: "Yushun Zhang, Bingran Li, Congliang Chen, Zhi-Quan Luo, Ruoyu Sun"
date: 2026-03-02
pdf: "https://arxiv.org/pdf/2603.02092v1"
tags: ["query:opt-lm"]
score: 10.0
evidence: LLM训练中Adam算法的收敛理论与超参数条件
tldr: 本研究针对Adam优化器在特定条件下可能发散的理论担忧，指出以往研究中超参数选取顺序与实际不符。通过理论证明，Adam在无需修改更新规则的情况下，只要选取合适的超参数（如较大的beta2且beta1 < sqrt(beta2)）即可收敛。研究揭示了超参数平面上的相变现象，并提出根据批量大小调整参数的实用建议，为大模型训练提供了重要的理论保障。
selection_source: fresh_fetch
motivation: 旨在解决Adam优化器在理论上可能不收敛的问题，并消除理论分析与实际调参实践之间的不一致性。
method: 通过数学证明分析了Adam在不同超参数组合下的收敛行为，并探讨了超参数与批量大小等问题特征的关联。
result: 证明了在beta2较大且beta1 < sqrt(beta2)时Adam必然收敛，并首次发现了超参数空间中存在从发散到收敛的相变边界。
conclusion: Adam无需修改即可收敛，建议根据批量大小反向调大beta2并确保beta1满足特定条件以优化训练性能。
---

## 摘要
Adam 是训练神经网络（包括大语言模型 LLMs）的默认算法。然而，Reddi 等人 (2019) 提供了一个 Adam 发散的例子，引发了对其在 AI 模型训练中应用的担忧。我们识别出该发散示例与实践之间存在一个关键的不匹配：Reddi 等人 (2019) 是在选定 Adam 的超参数（即 $\beta_1, \beta_2$）之后再选择问题的；而实际应用中通常是先固定问题，然后再调整 $\beta_1, \beta_2

## Abstract
Adam is the default algorithm for training neural networks, including large language models (LLMs). However, \citet{reddi2019convergence} provided an example that Adam diverges, raising concerns for its deployment in AI model training. We identify a key mismatch between the divergence example and practice: \citet{reddi2019convergence} pick the problem after picking the hyperparameters of Adam, i.e., $(β_1,β_2)$; while practical applications often fix the problem first and then tune $(β_1,β_2)$. In this work, we prove that Adam converges with proper problem-dependent hyperparameters. First, we prove that Adam converges when $β_2$ is large and $β_1 < \sqrt{β_2}$. Second, when $β_2$ is small, we point out a region of $(β_1,β_2)$ combinations where Adam can diverge to infinity. Our results indicate a phase transition for Adam from divergence to convergence when changing the $(β_1, β_2)$ combination. To our knowledge, this is the first phase transition in $(β_1,β_2)$ 2D-plane reported in the literature, providing rigorous theoretical guarantees for Adam optimizer. We further point out that the critical boundary $(β_1^*, β_2^*)$ is problem-dependent, and particularly, dependent on batch size. This provides suggestions on how to tune $β_1$ and $β_2$: when Adam does not work well, we suggest tuning up $β_2$ inversely with batch size to surpass the threshold $β_2^*$, and then trying $β_1< \sqrt{β_2}$. Our suggestions are supported by reports from several empirical studies, which observe improved LLM training performance when applying them.

---

## 论文详细总结（自动生成）

这是一份关于论文 **《Adam Converges Without Any Modification On Update Rules》** 的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心矛盾**：Adam 是深度学习（尤其是大语言模型 LLM）中最常用的优化器。然而，Reddi 等人（2018）曾证明 Adam 在某些超参数设置下会发散。这导致理论界普遍认为必须修改 Adam 的更新规则（如 AMSGrad）才能保证收敛。
*   **研究动机**：作者发现理论反例与实际应用之间存在“顺序错位”。理论反例是先固定超参数再构造特定问题使其发散；而实践中是先固定问题再调参。
*   **整体含义**：本论文旨在为“原生 Adam”（Vanilla Adam）正名，证明只要超参数选择得当，无需对算法做任何修改，Adam 就能在非凸优化中收敛。

### 2. 论文提出的方法论
*   **核心思想**：通过数学证明揭示 Adam 在 $(\beta_1, \beta_2)$ 超参数平面上存在从“发散”到“收敛”的**相变（Phase Transition）**。
*   **关键技术细节**：
    *   **收敛域证明**：证明当 $\beta_2$ 足够大（接近 1）且满足 $\beta_1 < \sqrt{\beta_2}$ 时，Adam 能够收敛。
    *   **集中效应分析**：分析了 $1/\sqrt{v_k}$ 的随机动力学，证明在 $\beta_2$ 较大时，二阶动量 $v_k$ 变化缓慢且可预测，从而使更新方向保持在梯度的对偶锥内。
    *   **势函数构造**：引入辅助序列 $z_k$ 构造势函数 $f(z_k)$，用以抵消一阶动量 $m_k$ 带来的历史信号干扰。
    *   **无梯度有界假设**：与以往研究不同，本证明不依赖“梯度先验有界”这一强假设，这使得理论能够捕捉到梯度真正发散到无穷大的情况。

### 3. 实验设计
*   **数据集/场景**：
    *   **合成函数**：构造了一个一维非凸函数类，用于验证 Adam 在特定参数下确实会发散到无穷大。
    *   **标准视觉任务**：在 **MNIST** 和 **CIFAR-10** 上进行大规模参数扫描。
*   **Benchmark 与对比**：
    *   **对比方法**：主要对比了不同 $(\beta_1, \beta_2)$ 组合下的原生 Adam 表现。
    *   **实验目标**：验证理论预测的收敛/发散边界是否与实际训练损失曲线吻合。

### 4. 资源与算力
*   **算力说明**：论文主要侧重于理论证明。在实验部分，作者提到在 MNIST 和 CIFAR-10 上分别进行了 $50 \times 50 = 2500$ 组网格搜索试验。
*   **具体细节**：文中**未明确说明**使用的具体 GPU 型号、数量或总训练时长。

### 5. 实验数量与充分性
*   **实验组数**：总计超过 5000 组网格搜索实验，以及针对合成函数的发散轨迹追踪。
*   **充分性与公平性**：
    *   实验设计非常充分，通过密集的网格搜索完整刻画了超参数平面的性能图谱。
    *   实验结果客观地展示了理论预测的边界（如 $\beta_1 < \sqrt{\beta_2}$ 曲线）与实际低损失区域的高度一致性。
    *   通过引用 LLM 预训练（如 Llama, GPT-3, DeepSeek）的实际参数设置，进一步增强了理论的现实说服力。

### 6. 论文的主要结论与发现
*   **相变边界**：首次在理论上确定了 Adam 的相变边界 $(\beta_1^*, \beta_2^*)$。
*   **问题依赖性**：收敛所需的 $\beta_2$ 阈值与**批量大小（Batch Size）**密切相关。Batch Size 越小（即 $n$ 越大），需要的 $\beta_2$ 就越接近 1。
*   **调参指南**：如果 Adam 训练不稳定，建议根据 Batch Size 的倒数调大 $\beta_2$，然后再尝试满足 $\beta_1 < \sqrt{\beta_2}$ 的 $\beta_1$。
*   **理论平反**：原生 Adam 是安全的，AMSGrad 等变体并非保证收敛的唯一途径。

### 7. 优点
*   **理论深度**：解决了困扰优化领域多年的 Adam 收敛性争议，弥合了理论与实践的鸿沟。
*   **假设贴切**：移除了“梯度有界”假设，使其理论分析对深度学习实际场景（如梯度爆炸风险）更具解释力。
*   **实践指导性强**：直接解释了为什么 LLM 训练中常用的参数（如 0.9, 0.95）在小 Batch 下可能出问题，并给出了具体的修复建议。

### 8. 不足与局限
*   **阈值保守性**：理论给出的 $\beta_2$ 充分条件（如 $1 - O(1/n^5)$）在数学上为了严谨可能过于保守，实际收敛区域可能比理论预测的更大。
*   **泛化性探讨不足**：论文主要关注“优化收敛”（训练损失减小），但未深入探讨超参数对“泛化性能”（测试集表现）的影响。
*   **最优参数定位**：虽然指出了“安全区”，但未能在安全区内进一步确定哪种参数组合能带来最快的收敛速度。

（完）
