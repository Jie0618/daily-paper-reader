---
title: A successive difference-of-convex method for a class of two-stage nonconvex nonsmooth stochastic conic program via SVI
title_zh: 一种求解一类两阶段非凸非光滑随机锥规划问题的逐次凸差方法：基于随机变分不等式
authors: "Chao Zhang, Di Wang"
date: 2026-03-04
pdf: "https://arxiv.org/pdf/2603.03787v1"
tags: ["query:opt-lm"]
score: 9.0
evidence: 随机锥规划的KKT点作为必要最优性条件
tldr: 针对一类具有非凸非光滑目标函数和锥约束的两阶段随机规划问题，本文提出了一种基于逐次凸差（SDC）的方法。研究首先定义了KKT点并将其转化为等价的随机变分不等式（SVI），随后利用Moreau包络构造SDC算法，并结合逐步对冲法求解子问题。该方法在理论上保证了收敛性，并在扩展的马科维茨均值-方差模型实验中验证了其有效性。
selection_source: fresh_fetch
motivation: 旨在解决包含非凸、非光滑且可能非利普希茨项的两阶段随机锥规划问题，这类问题在实际应用中建模能力强但求解极具挑战。
method: 将KKT条件转化为随机变分不等式，并提出一种利用Moreau包络和逐步对冲法的逐次凸差（SDC）算法。
result: 理论上证明了算法在适当假设下的严格收敛性，并在扩展的投资组合模型上通过数值实验验证了算法的有效性。
conclusion: 该研究为复杂两阶段非凸非光滑随机优化问题提供了一个有效的理论框架和数值求解方案。
---

## 摘要
我们考虑一类两阶段非凸非光滑随机锥规划问题，其中两个阶段的目标函数均可包含非光滑项，这些项是具有易于计算的近端映射的函数，并进一步与仿射映射复合。此类问题能够对多种应用进行建模。然而，由于具有可能包含大量场景的两阶段结构、非凸、非光滑甚至非 Lipschitz 间断项以及锥约束，解决这些问题具有挑战性。在本文中，我们定义了该问题的 KKT 点，证明了其在温和条件下是必要的最优性条件，并将其转化为等价的非单调非光滑两阶段随机变分不等式 (SVI)。随后，我们利用 Moreau 包络提出了一种逐次凸差 (SDC) 方法来求解该问题，其子问题通过求解极大单调两阶段 SVI 的累进对冲法近似求解。我们证明了该方法在适当假设下的严格收敛性。我们提供了一个 Markowitz 均值-方差模型的扩展作为应用，数值结果证明了该模型和 SDC 方法的有效性。

## Abstract
We consider a class of two-stage nonconvex nonsmooth stochastic conic program, where the objective functions in both stages can contain nonsmooth terms that are functions with easily computed proximal mappings, further composed with affine mappings. This kind of problem is capable of modeling various applications. Solving these problems, however, can be challenging due to the two-stage structure with possibly large number of scenarios, the nonconvex, nonsmooth and even non-Lipschitz discontinuous terms, as well as the conic constraints. In this paper, we define a KKT point of the problem, show that it is a necessary optimality condition under mild conditions, and transform it to an equivalent nonmonotone nonsmooth two-stage stochastic variational inequality (SVI). We then propose a successive difference-of-convex (SDC) method by making use of Moreau envelope to solve it, the subproblems of which are approximately solved by the progressive hedging method for solving maximal monotone two-stage SVI. We show the rigorous convergence of our method under suitable assumptions. An extension of Markowitz's mean-variance model is provided as an application and numerical results on it demonstrate the effectiveness of the model and the SDC method.

---

## 论文详细总结（自动生成）

这篇论文提出了一种求解两阶段非凸非光滑随机锥规划（T-NNS-SCP）的新型数值方法。以下是对该论文的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **研究对象**：两阶段随机锥规划问题，其特点是第一阶段和第二阶段的目标函数均包含非凸、非光滑、甚至非 Lipschitz 连续（如 $\ell_0$ 范数）的项，且约束条件涉及对称锥（如二阶锥、半正定锥）。
*   **研究动机**：
    *   **建模能力强**：该模型能处理不确定性下的复杂决策，如稀疏投资组合优化、医疗物资分配等。
    *   **求解困难**：传统的逐步对冲法（PHM）主要适用于凸问题。当引入非凸非光滑项和复杂锥约束时，现有的分解算法难以保证收敛性或处理非光滑性诱导的计算挑战。
*   **核心目标**：建立一套理论框架，将此类复杂优化问题的最优性条件转化为随机变分不等式（SVI），并设计一种具备收敛保证的分解算法。

### 2. 论文提出的方法论
该论文的核心是 **SDC-PHM 算法**，其主要步骤如下：
*   **KKT 条件转化**：首先定义了问题的 KKT 点，并证明在 Robinson 约束限定（RCQ）下，它是局部最优的必要条件。随后将 KKT 条件等价转化为一个非单调、非光滑的两阶段随机变分不等式（SVI）。
*   **Moreau 包络近似**：利用 Moreau 包络（Moreau Envelope）对目标函数中的非光滑项进行平滑近似。Moreau 包络可以自然地分解为两个凸函数的差（DC 分解）。
*   **逐次凸差（SDC）框架**：
    *   **线性化与正则化**：在每次迭代中，对 DC 分解中的第二个凸项进行线性化，并加入二次正则化项。
    *   **转化为凸子问题**：通过上述处理，将原非凸问题转化为一系列凸的两阶段随机规划子问题。
*   **子问题求解（PHM）**：每个 SDC 子问题等价于一个极大单调（Maximal Monotone）的平滑两阶段 SVI。论文采用逐步对冲法（PHM）进行场景分解并行求解，从而应对大规模随机场景。
*   **收敛性保证**：论文在数学上严格证明了该算法生成的序列点，其任何聚点都是原问题的 KKT 点。

### 3. 实验设计
*   **应用场景**：扩展的 **Markowitz 均值-方差投资组合模型**。
*   **模型特性**：
    *   引入 $\ell_0$ 范数正则项以诱导投资组合的稀疏性（非凸非光滑）。
    *   引入二阶锥（SOC）约束限制第一阶段与第二阶段决策间的偏差。
*   **对比实验（Benchmark）**：设计了四个模型变体进行对比：
    *   **Model A**：完整模型（含 $\ell_0$ 和 SOC 约束）。
    *   **Model B**：无 SOC 约束。
    *   **Model C**：无 $\ell_0$ 正则项（凸模型）。
    *   **Model D**：既无 $\ell_0$ 也无 SOC 约束（纯凸模型）。
*   **场景规模**：测试了 $K = 1000, 3000, 5000$ 个随机场景。

### 4. 资源与算力
*   **硬件环境**：Windows 10 系统，Intel Core i7-10700 CPU @ 3.70 GHz，64 GB RAM。
*   **软件环境**：MATLAB R2021a。
*   **算力说明**：论文未提及使用 GPU，主要依赖 CPU 进行计算。对于 5000 个场景（涉及约 21.5 万个约束和 20 万个变量）的问题，Model A 的平均求解时间约为 56 秒。

### 5. 实验数量与充分性
*   **实验规模**：针对每种场景规模（1000/3000/5000），独立生成了 20 组随机重复实验，并报告了平均值和标准差。
*   **充分性评价**：实验设计较为充分。通过 Model A-D 的消融对比，清晰地展示了 $\ell_0$ 项对稀疏性的贡献以及 SOC 约束对决策稳定性的影响。数值指标涵盖了 KKT 残差、可行性误差、稀疏度（nnz）和计算时间，评估维度全面。

### 6. 论文的主要结论与发现
*   **算法有效性**：SDC-PHM 能够稳定收敛至非凸非光滑问题的 KKT 点，且可行性误差极低（$10^{-4}$ 量级）。
*   **反直觉的效率**：实验发现，求解非凸非光滑模型（Model A/B）的速度竟然比求解纯凸模型（Model C/D）**更快**。
    *   **原因分析**：$\ell_0$ 正则项迅速诱导了决策变量的稀疏性，减少了有效变量的数量，从而加速了内层 PHM 子问题的收敛。
*   **模型价值**：扩展的投资组合模型在保持稀疏性的同时，通过 SOC 约束有效控制了跨阶段的决策漂移。

### 7. 优点
*   **理论严谨**：不仅提出了算法，还给出了详尽的 KKT 必要性证明和全局收敛性分析。
*   **处理能力强**：能够处理非 Lipschitz 甚至不连续的函数，这在两阶段随机规划领域属于较为前沿且困难的工作。
*   **分解高效**：结合了 SVI 视角和 PHM 分解，使得算法具备处理大规模场景的潜力。

### 8. 不足与局限
*   **应用领域单一**：数值实验仅限于投资组合优化，虽然该模型具有代表性，但未在其他领域（如电力系统或供应链）验证算法的普适性。
*   **参数敏感性**：算法依赖于多个超参数（如 $\rho_t$ 的更新率、正则化参数 $\tau$ 等），论文未深入讨论这些参数对不同类型问题的敏感程度。
*   **二阶信息依赖**：算法要求平滑部分是二次连续可微的，对于某些更复杂的非光滑结构可能仍需进一步扩展。

（完）
