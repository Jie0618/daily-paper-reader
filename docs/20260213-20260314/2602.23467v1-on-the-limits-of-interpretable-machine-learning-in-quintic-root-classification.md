---
title: On the Limits of Interpretable Machine Learning in Quintic Root Classification
title_zh: 论五次方程根分类中可解释机器学习的局限性
authors: "Rohan Thomas, Majid Bani-Yaghoub"
date: 2026-02-26
pdf: "https://arxiv.org/pdf/2602.23467v1"
tags: ["query:sr-alg"]
score: 10.0
evidence: 符号回归与深度学习在数学结构恢复方面的比较
tldr: 本研究探讨机器学习能否从原始数值数据中自动提取可解释的数学结构，以五次多项式实根分类为基准。通过测试神经网络、决策树等多种模型，发现神经网络虽能达到较高准确率，但其学习的是几何近似而非符号规则。当引入关键点符号变化等显式特征时，决策树性能大幅提升。结果表明，在结构化数学领域，实现可解释性往往需要显式的结构归纳偏置，而非单纯的数据驱动。
selection_source: fresh_fetch
motivation: 探究机器学习模型是否能从原始数值数据中自主发现可解释的数学规律和符号结构。
method: 以五次多项式实根配置分类为基准，对比分析了神经网络、决策树及符号回归等多种模型在原始系数和显式特征下的表现。
result: 神经网络在原始系数上表现优异但缺乏符号不变性，而决策树在引入特定数学特征后能达到同等精度并生成可解释规则。
conclusion: 机器学习模型倾向于学习连续的几何近似而非离散的符号规则，因此在数学领域实现可解释性需要引入显式的结构归纳偏置。
---

## 摘要
机器学习（ML）能否从原始数值数据中自主恢复可解释的数学结构？我们旨在利用最高五次多项式的实根配置分类作为结构化基准来回答这一问题。我们测试了广泛的机器学习模型，包括决策树、逻辑回归、支持向量机、随机森林、梯度提升、XGBoost、符号回归和神经网络。仅使用原始系数，神经网络在五次方程分类的分布内表现上取得了强劲结果（均衡准确率为 84.3% ± 0.9%），而决策树的表现则明显较差（59.9% ± 0.9%）。然而，当提供捕捉临界点符号变化的显式特征时，决策树的表现与神经网络相当（84.2% ± 1.2%），并能产生显式的分类规则。知识蒸馏表明，这一单一不变性占提取决策结构的 97.5%。分布外、数据效率和噪声鲁棒性分析表明，神经网络学习的是决策边界的连续、依赖于数据的几何近似，而非恢复尺度不变的符号规则。这种几何近似与符号不变性之间的区别，解释了在不同模型中观察到的预测性能与可解释性之间的差距。尽管可以获得较高的预测准确率，但我们没有发现证据表明所评估的机器学习模型能从原始系数中自主恢复离散的、人类可理解的数学规则。这些结果表明，在结构化数学领域，可解释性可能需要显式的结构归纳偏置，而非纯粹的数据驱动近似。

## 速览
**TLDR**：本研究探讨了机器学习能否从原始数值数据中自动恢复可解释的数学结构，以五次多项式实根分类为基准。通过测试决策树、神经网络等多种模型，发现神经网络在原始系数上表现良好，但并未学习到符号规则，而是学习了几何近似。当引入关键点的符号变化特征时，决策树性能大幅提升并产生可解释规则。研究表明，在结构化数学领域，实现可解释性可能需要显式的结构归纳偏置，而非纯数据驱动的逼近。 \
**Motivation**：探究机器学习模型是否能从原始数值数据中自主发现并提取人类可理解的数学结构和符号规则。 \
**Method**：以五次多项式实根分类为基准，对比分析了从决策树到神经网络等多种模型在原始系数及特征工程下的表现。 \
**Result**：神经网络虽在原始数据上准确率较高，但本质是几何近似而非符号规则，而决策树在引入特定数学特征后能达到同等性能并提供可解释规则。 \
**Conclusion**：在结构化数学任务中，单纯的数据驱动模型难以自动恢复离散的符号规则，可解释性往往依赖于显式的结构归纳偏置。

---

## Abstract
Can Machine Learning (ML) autonomously recover interpretable mathematical structure from raw numerical data? We aim to answer this question using the classification of real-root configurations of polynomials up to degree five as a structured benchmark. We tested an extensive set of ML models, including decision trees, logistic regression, support vector machines, random forest, gradient boosting, XGBoost, symbolic regression, and neural networks. Neural networks achieved strong in-distribution performance on quintic classification using raw coefficients alone (84.3% + or - 0.9% balanced accuracy), whereas decision trees perform substantially worse (59.9% + or - 0.9\%). However, when provided with an explicit feature capturing sign changes at critical points, decision trees match neural performance (84.2% + or - 1.2%) and yield explicit classification rules. Knowledge distillation reveals that this single invariant accounts for 97.5% of the extracted decision structure. Out-of-distribution, data-efficiency, and noise robustness analyses indicate that neural networks learn continuous, data-dependent geometric approximations of the decision boundary rather than recovering scale-invariant symbolic rules. This distinction between geometric approximation and symbolic invariance explains the gap between predictive performance and interpretability observed across models. Although high predictive accuracy is attainable, we find no evidence that the evaluated ML models autonomously recover discrete, human-interpretable mathematical rules from raw coefficients. These results suggest that, in structured mathematical domains, interpretability may require explicit structural inductive bias rather than purely data-driven approximation.

---

## 论文详细总结（自动生成）

这是一份关于论文《On the Limits of Interpretable Machine Learning in Quintic Root Classification》（论五次方程根分类中可解释机器学习的局限性）的结构化深入总结：

### 1. 核心问题与研究动机
*   **核心问题**：机器学习（ML）能否仅从原始数值数据（如多项式系数）中，自主发现并提取出人类可理解的、具有泛化能力的数学结构或符号规则？
*   **研究背景**：
    *   **数学基准**：研究选择了多项式实根个数分类作为任务。2至4次多项式存在代数判别式，而5次多项式（根据阿贝尔-鲁菲尼定理）不存在一般的根式解，但其根的个数可以通过解析方法（如施图姆序列）确定。
    *   **动机**：这提供了一个受控环境，用于评估ML模型是仅仅在做复杂的几何边界近似（黑盒），还是真正“重新发现”了数学上的符号不变性（可解释性）。

### 2. 方法论
*   **核心思想**：对比“黑盒”模型（神经网络）与“可解释”模型（决策树、符号回归）在原始系数和人工特征下的表现差异，并通过知识蒸馏技术尝试提取模型内部逻辑。
*   **关键技术细节**：
    *   **特征工程**：设计了63个数学特征，分为六大类：
        1.  **施图姆序列（Sturm Sequences）**：通过多项式除法序列的符号变化统计根数。
        2.  **笛卡尔符号法则**：基于系数序列符号变化估计正负根上限。
        3.  **牛顿恒等式（Newton’s Sums）**：计算根的幂次和。
        4.  **临界点特征（Crit8）**：计算导数根（临界点）处的多项式值符号变化，这是本文发现的最关键特征。
        5.  **混合符号方法**：包括Tschirnhaus不变量和系数比例。
        6.  **分解法**：检测系数的稀疏性和因式分解模式。
    *   **知识蒸馏协议**：先训练高性能神经网络，再用其预测结果训练决策树，通过SHAP值分析特征贡献，试图将神经网络的隐性知识转化为显性规则。

### 3. 实验设计
*   **数据集**：生成了40,000个五次多项式，系数在 $[-10, 10]$ 之间均匀采样。
*   **分类目标**：将多项式分为三类：1个实根、3个实根、5个实根。
*   **Benchmark与对比方法**：
    *   **验证基准**：先在2-4次多项式上验证，确认模型能否找回已知的代数判别式。
    *   **对比模型**：决策树 (CART)、逻辑回归、SVM (RBF核)、随机森林、梯度提升机 (GBM)、XGBoost、符号回归 (PySR) 以及多层感知器神经网络 (NN)。
*   **压力测试**：包括分布外 (OOD) 外推（系数范围扩大10倍）、数据效率测试（减少训练样本）和噪声鲁棒性测试。

### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的硬件型号（如GPU/CPU数量）或具体的训练时长。
*   **软件环境**：提到了使用 NumPy 的特征值方法计算根作为基准真相，使用 PySR 进行符号回归搜索。

### 5. 实验数量与充分性
*   **实验规模**：
    *   采用了 **20个独立随机种子** 进行 5折分层交叉验证，确保了统计结果的可靠性。
    *   针对不同阶数（2-5阶）分别进行了实验。
    *   进行了消融实验，逐一测试六大类数学特征对模型性能的影响。
*   **充分性评价**：实验设计非常严谨。通过对比原始系数与工程特征、分布内与分布外表现，多维度地审视了模型的“学习”本质。实验覆盖了从线性模型到深度学习的多种主流架构，具有较好的代表性和客观性。

### 6. 主要结论与发现
*   **性能鸿沟**：在仅使用原始系数时，神经网络表现优异（84.3% 准确率），而决策树表现极差（59.9%）。这表明神经网络学到了某种复杂的模式，但这种模式对人类不可见。
*   **关键特征的威力**：一旦引入 **Crit8（临界点符号变化）** 特征，决策树的准确率飙升至 84.2%，且生成的规则极其简洁（如：若 Crit8 > 1.5 则为5个实根）。
*   **几何近似 vs. 符号不变性**：OOD测试显示，神经网络在超出训练范围后性能大幅下降，而基于数学特征的决策树保持完美表现。这证明神经网络学习的是**局部几何边界的连续近似**，而非**尺度不变的符号规则**。
*   **自主发现的失败**：没有任何模型（包括符号回归）能仅从原始系数中自主推导出类似 Crit8 的高级数学概念。

### 7. 优点
*   **跨学科结合**：将经典的代数理论（阿贝尔-鲁菲尼定理、施图姆序列）与现代机器学习的可解释性研究完美结合。
*   **深度洞察**：通过知识蒸馏和SHAP分析，成功定位了影响分类的核心数学算子（Crit8），为“模型到底学到了什么”提供了量化证据。
*   **严谨的鲁棒性分析**：通过数据效率和外推实验，清晰地界定了深度学习在处理结构化数学问题时的局限性。

### 8. 不足与局限
*   **模型覆盖范围**：虽然测试了多种模型，但未涉及当前流行的 Transformer 架构或专门的大型数学模型，这些模型可能在符号推理上有不同表现。
*   **采样偏差风险**：实验仅使用了 $[-10, 10]$ 的均匀分布采样，不同的系数分布可能会影响根的几何分布，进而影响模型的泛化结论。
*   **符号回归限制**：符号回归在搜索深度和计算预算受限时表现不佳，不能完全排除在无限算力下其发现规则的可能性。
*   **应用限制**：结论主要集中在纯数学领域，对于非结构化的现实世界数据，几何近似可能依然是唯一可行的路径。

（完）
