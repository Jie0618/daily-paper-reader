---
title: Combining Adam and its Inverse Counterpart to Enhance Generalization of Deep Learning Optimizers
title_zh: 结合 Adam 及其逆对应物以增强深度学习优化器的泛化能力
authors: "Tao Shi, Liangming Chen, Long Jin, Mengchu Zhou"
date: 2026-03-07
pdf: "https://arxiv.org/pdf/2603.07122v1"
tags: ["query:opt-lm"]
score: 9.0
evidence: 增强泛化能力的Adam优化器变体
tldr: 针对Adam优化器易陷入尖锐极小值导致泛化性能不足的问题，本文提出InvAdam，通过将一阶与二阶矩的除法改为乘法，增大梯度大时的步长以逃离尖锐极小值。为兼顾收敛性，进一步提出DualAdam，融合了Adam与InvAdam的更新机制。实验证明DualAdam在图像分类和LLM微调中表现优异，显著提升了泛化能力。
selection_source: fresh_fetch
motivation: 旨在解决Adam优化器因倾向于收敛至尖锐极小值而导致的泛化性能欠佳问题。
method: 提出InvAdam利用矩的乘法更新机制逃离尖锐极小值，并结合Adam构建DualAdam以平衡收敛与泛化。
result: 在图像分类和LLM微调任务中，DualAdam的泛化性能均优于Adam及其多种最先进的变体。
conclusion: DualAdam通过整合正反两种矩更新机制，成功实现了在保证模型收敛的同时显著增强其泛化性能。
---

## 摘要
在神经网络训练中，自适应矩估计（Adam）通常收敛较快，但泛化性能欠佳。对其泛化缺陷的一个广泛接受的解释是，它往往倾向于收敛到尖锐极小值。为了增强其寻找平坦极小值的能力，我们提出了一种名为逆 Adam（InvAdam）的新变体。InvAdam 的关键改进在于其参数更新机制，该机制与 Adam 相反。具体而言，它计算一阶矩和二阶矩的逐元素乘法，而 Adam 计算这两个矩的逐元素除法。这种修改旨在当二阶矩中的元素较大时增加参数更新的步长，反之亦然，这有助于参数逃离尖锐极小值并停留在平坦极小值处。然而，InvAdam 的更新机制可能面临收敛方面的挑战。为了应对这一挑战，我们提出了双 Adam（DualAdam），它集成了 Adam 和 InvAdam 的更新机制，在确保收敛的同时增强了泛化性能。此外，我们引入了扩散理论，从数学上证明了 InvAdam 逃离尖锐极小值的能力。在图像分类任务和大型语言模型（LLM）微调上进行了广泛的实验。结果验证了 DualAdam 在泛化性能方面优于 Adam 及其最先进的变体。代码可在 https://github.com/LongJin-lab/DualAdam 公开获取。

## Abstract
In the training of neural networks, adaptive moment estimation (Adam) typically converges fast but exhibits suboptimal generalization performance. A widely accepted explanation for its defect in generalization is that it often tends to converge to sharp minima. To enhance its ability to find flat minima, we propose its new variant named inverse Adam (InvAdam). The key improvement of InvAdam lies in its parameter update mechanism, which is opposite to that of Adam. Specifically, it computes element-wise multiplication of the first-order and second-order moments, while Adam computes the element-wise division of these two moments. This modification aims to increase the step size of the parameter update when the elements in the second-order moments are large and vice versa, which helps the parameter escape sharp minima and stay at flat ones. However, InvAdam's update mechanism may face challenges in convergence. To address this challenge, we propose dual Adam (DualAdam), which integrates the update mechanisms of both Adam and InvAdam, ensuring convergence while enhancing generalization performance. Additionally, we introduce the diffusion theory to mathematically demonstrate InvAdam's ability to escape sharp minima. Extensive experiments are conducted on image classification tasks and large language model (LLM) fine-tuning. The results validate that DualAdam outperforms Adam and its state-of-the-art variants in terms of generalization performance. The code is publicly available at https://github.com/LongJin-lab/DualAdam.

---

## 论文详细总结（自动生成）

这是一份关于论文《Combining Adam and its Inverse Counterpart to Enhance Generalization of Deep Learning Optimizers》的结构化深入分析总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：自适应矩估计（Adam）优化器虽然收敛速度快，但其泛化性能往往不如 SGD。
*   **研究背景**：学术界普遍认为，泛化性能与损失函数地形的“平坦度”正相关。Adam 的更新机制（步长与二阶矩成反比）在梯度较大的区域（通常对应尖锐极小值）会减小步长，导致模型容易被困在这些泛化差的“尖锐极小值”中。
*   **研究动机**：探索一种能够主动逃离尖锐极小值并寻找平坦极小值的优化机制，同时保留 Adam 快速收敛的优势。

### 2. 论文提出的方法论
*   **核心思想**：提出一种与 Adam 更新逻辑相反的机制——**InvAdam**，并将其与 Adam 结合形成 **DualAdam**。
*   **关键技术细节**：
    *   **InvAdam (Inverse Adam)**：其参数更新公式为 $\Delta \theta_i = \hat{m}_i \cdot \sqrt{\hat{v}_i}$（而 Adam 是 $\hat{m}_i / \sqrt{\hat{v}_i}$）。当二阶矩 $\hat{v}_i$ 较大时，InvAdam 会增大步长，从而产生更强的扰动，帮助参数跳出尖锐区域。
    *   **DualAdam**：为了解决 InvAdam 难以收敛的问题，DualAdam 采用线性切换机制：$\bar{u}_t = \alpha \tilde{u}_{InvAdam} + (1-\alpha) u_{Adam}$。
    *   **切换机制**：引入比例系数 $\alpha = \max(0, 1 - \xi t)$，其中 $\xi$ 是切换率。训练初期以 InvAdam 为主进行全局探索，寻找平坦盆地；随着迭代次数 $t$ 增加，线性过渡到 Adam 以确保最终稳定收敛。
*   **理论支撑**：引入**扩散理论（Diffusion Theory）**，通过 Kramers 逃逸问题模型证明了 InvAdam 的平均逃逸时间对 Hessian 矩阵特征值（代表尖锐度）的敏感度远高于 Adam，从数学上验证了其逃离尖锐极小值的能力。

### 3. 实验设计
*   **数据集/场景**：
    *   **数值模拟**：2参数自定义函数、Eggholder 函数（可视化优化轨迹）。
    *   **图像分类**：CIFAR-10、CIFAR-100、Tiny ImageNet、ImageNet-1k。
    *   **大语言模型微调**：OpenPangu-Embedded-1B 模型，使用 Alpaca-GPT4-CN 数据集。
*   **Benchmark 与对比方法**：
    *   **对比方法**：Adam, AdamW, RAdam, SWATS（Adam转SGD）, NAdam, Adan, MIAdam。
    *   **评估指标**：Top-1 测试准确率、训练时间、Hessian 特征值分布、损失平面可视化、验证困惑度（PPL）。

### 4. 资源与算力
*   **算力说明**：论文**未明确列出具体的 GPU 型号和数量**。
*   **训练时长**：文中提供了详细的训练时间对比。例如，在 CIFAR-100 上训练 ResNet-18，Adam 耗时约 36.6 分钟，DualAdam 耗时约 36.9 分钟。
*   **计算开销分析**：作者通过 FLOPs 分析指出，DualAdam 每参数每迭代仅比 Adam 多出约 4p 的计算量，且仅在训练前期的切换阶段存在，对总训练成本的影响微乎其微（约 1% 左右）。

### 5. 实验数量与充分性
*   **实验规模**：
    *   涵盖了从小型数据集（CIFAR）到大规模数据集（ImageNet-1k）的跨度。
    *   模型架构包括 CNN（ResNet, VGG）、Transformer（ViT）以及 LLM（1B参数）。
    *   **消融实验**：针对切换率 $\xi$、不同的切换机制（线性、指数、固定步长）以及学习率调度器进行了详细测试。
*   **充分性与公平性**：实验设置了多次运行取均值和标准差，对比了近年来多种主流的 Adam 变体，并使用了 Hessian 矩阵分析和损失平面可视化等深度分析手段，实验设计较为客观、全面。

### 6. 主要结论与发现
*   **泛化性能显著提升**：DualAdam 在所有测试的视觉任务中均优于 Adam 及其变体，在 ImageNet-1k 上比 Adam 提升了约 3% 的准确率。
*   **LLM 微调优势**：在 1B 参数模型的微调中，DualAdam 虽然训练损失略高于 AdamW，但验证集困惑度（PPL）更低且更稳定，有效缓解了过拟合。
*   **地形平坦度验证**：通过 Py-Hessian 工具分析发现，DualAdam 找到的解其 Hessian 最大特征值和迹（Trace）显著小于 Adam，证明其确实收敛到了更平坦的区域。

### 7. 优点
*   **创新性强**：提出了“逆向思维”的 InvAdam 机制，打破了传统自适应学习率必须与梯度反向缩放的思维定式。
*   **理论与实践结合**：不仅有直观的算法改进，还利用扩散理论提供了严谨的数学证明。
*   **易用性与低成本**：算法实现简单，几乎不增加额外的计算负担和内存开销，具有很强的工程实用价值。

### 8. 不足与局限
*   **超参数敏感性**：切换率 $\xi$ 是一个关键超参数，虽然消融实验给出了建议值，但在不同任务或不同总 Epoch 数下，可能需要重新调整以获得最佳平衡。
*   **硬件信息缺失**：未提供具体的硬件测试环境，难以评估在不同架构 GPU 上的实际吞吐量差异。
*   **应用范围限制**：目前主要验证了 CV 和 NLP 任务，对于强化学习或生成模型（如 Diffusion Models）等其他领域的适用性尚待验证。

（完）
