---
title: "Extending $μ$P: Spectral Conditions for Feature Learning Across Optimizers"
title_zh: 扩展 $μ$P：跨优化器特征学习的谱条件
authors: "Akshita Gupta, Marieme Ngom, Sam Foreman, Venkatram Vishwanath"
date: 2026-02-24
pdf: "https://arxiv.org/pdf/2602.20937v1"
tags: ["query:opt-lm"]
score: 9.0
evidence: 大语言模型中自适应优化方法的缩放规则
tldr: 针对大模型超参数调优成本高的问题，本文扩展了最大更新参数化（μP）框架。通过引入光谱条件替代复杂的张量程序，为 AdamW、Sophia、Shampoo 等多种优化器推导了统一的 μP 规则。实验证明该方法能实现跨模型宽度的零样本学习率迁移，显著降低了大模型训练的调优开销，并提供了深度缩放的实证见解。
selection_source: fresh_fetch
motivation: 现有的 μP 推导依赖复杂的张量程序，难以扩展到 SGD 和 Adam 之外的多种现代优化器。
method: 利用光谱条件构建新框架，为 AdamW、ADOPT、Sophia、Shampoo 和 Muon 等优化器推导了 μP 规则。
result: 在多个基准模型上成功实现了跨模型宽度的零样本学习率迁移，验证了不同优化器下的参数化效果。
conclusion: 该研究简化了 μP 的推导并扩展了其适用范围，为大规模模型的高效超参数调优提供了有力工具。
---

## 摘要
为了加速和扩展大语言模型的训练，研究者们提出了多种自适应一阶和二阶优化方法的变体。这些优化程序的性能对超参数（HPs）的选择高度敏感，而对于大规模模型，调优这些超参数的计算成本极其昂贵。最大更新参数化（$μ$P）是一套缩放规则，旨在使最优超参数独立于模型大小，从而允许将在较小（计算成本较低）模型上调优的超参数迁移到训练更大的目标模型中。尽管在 SGD 和 Adam 上取得了显著成果，但为其他优化器推导 $μ$P 仍具挑战性，因为其底层的张量程序（tensor programming）方法难以掌握。基于近期引入谱条件作为张量程序替代方案的研究工作，我们提出了一个新颖的框架，用于为更广泛的优化器类别推导 $μ$P，包括 AdamW、ADOPT、LAMB、Sophia、Shampoo 和 Muon。我们在多个基准模型上实现了 $μ$P 推导，并证明了上述优化器在模型宽度增加时实现了零样本学习率迁移。此外，我们还提供了关于这些优化器深度缩放参数化的经验性见解。

## Abstract
Several variations of adaptive first-order and second-order optimization methods have been proposed to accelerate and scale the training of large language models. The performance of these optimization routines is highly sensitive to the choice of hyperparameters (HPs), which are computationally expensive to tune for large-scale models. Maximal update parameterization $(μ$P$)$ is a set of scaling rules which aims to make the optimal HPs independent of the model size, thereby allowing the HPs tuned on a smaller (computationally cheaper) model to be transferred to train a larger, target model. Despite promising results for SGD and Adam, deriving $μ$P for other optimizers is challenging because the underlying tensor programming approach is difficult to grasp. Building on recent work that introduced spectral conditions as an alternative to tensor programs, we propose a novel framework to derive $μ$P for a broader class of optimizers, including AdamW, ADOPT, LAMB, Sophia, Shampoo and Muon. We implement our $μ$P derivations on multiple benchmark models and demonstrate zero-shot learning rate transfer across increasing model width for the above optimizers. Further, we provide empirical insights into depth-scaling parameterization for these optimizers.

---

## 论文详细总结（自动生成）

这篇论文《Extending $\mu$P: Spectral Conditions for Feature Learning Across Optimizers》深入探讨了大语言模型（LLM）训练中的超参数调优难题，并提出了一种更具普适性的缩放框架。以下是对该论文的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **研究动机**：训练大规模模型（如 LLM）时，超参数（如学习率）的调优成本极高。**最大更新参数化（$\mu$P）** 允许在小模型上寻找最优超参数并直接迁移到大模型，但传统的 $\mu$P 推导依赖复杂的“张量程序（Tensor Programming）”理论，导致其难以扩展到除 SGD 和 Adam 之外的现代优化器。
*   **核心问题**：如何简化 $\mu$P 的推导过程，并将其扩展到包括二阶优化器在内的多种先进优化算法中，以实现跨模型宽度的零样本超参数迁移。

### 2. 方法论：核心思想与技术细节
*   **核心思想**：利用**谱条件（Spectral Conditions）**替代张量程序。研究者认为，只要保证权重矩阵及其更新量的谱范数（Spectral Norm）随模型宽度按特定比例缩放，就能确保特征学习的稳定性。
*   **关键技术细节**：
    *   **谱条件 C.2**：要求权重矩阵 $W_l$ 的谱范数 $\|W_l\|_* = \Theta(\sqrt{n_l/n_{l-1}})$，且其更新量 $\|\Delta W_l\|_*$ 也遵循相同的缩放比例。
    *   **通用框架**：
        1.  **权重初始化缩放**：仅取决于架构，通常令权重乘数 $\sigma_l \approx 1/\sqrt{n_{l-1}}$。
        2.  **学习率（LR）缩放**：根据不同优化器的更新规则 $\Psi(\cdot)$，推导使 $\|\Delta W_l\|_*$ 满足谱条件的缩放因子 $n^{-c}$。
*   **推导结果**：
    *   **AdamW / ADOPT / Sophia**：学习率应按 $1/n_{l-1}$ 缩放。
    *   **LAMB / Muon**：学习率保持不变（$\Theta(1)$），因为这些优化器内部已有自适应缩放机制。
    *   **Shampoo**：学习率应按 $\sqrt{n_l/n_{l-1}}$ 缩放。

### 3. 实验设计
*   **基准模型**：使用了 **NanoGPT**（约 1.59M 至 403M 参数）和 **Llama2**（约 154M 至 1.38B 参数）。
*   **数据集**：Tiny Shakespeare（用于 NanoGPT）和 Wikitext-103（用于 Llama2）。
*   **对比方法**：在标准参数化（SP）与 $\mu$P 下，对比了 AdamW、ADOPT、LAMB、Sophia、Shampoo 和 Muon 六种优化器。
*   **实验场景**：
    *   **宽度缩放**：模型宽度从 128 扩展至 2048，观察最优学习率是否保持一致。
    *   **深度缩放**：初步探索了模型深度从 2 层增加到 64 层时的参数化表现。

### 4. 资源与算力
*   **NanoGPT 实验**：使用了 Argonne 领导力计算设施（ALCF）的 **Polaris 超级计算机**，配置为 **4 台 NVIDIA A100 GPU**。
*   **Llama2 实验**：使用了 **Aurora 超级计算机**，配置为 **12 个 Intel Data Center GPU Max Series**。
*   **训练时长**：文中未明确给出总训练时长，但提到了 Llama2 训练了 1026 个步骤。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 6 种优化器、2 种主流架构、多个宽度等级（128, 256, 512, 1024, 2048）以及深度等级。
*   **充分性与公平性**：实验设计较为充分，通过“坐标检查图（Coordinate Check Plots）”验证了特征学习的稳定性，并展示了“越宽模型损失越低”的性质。对比实验在相同的训练设置下进行，具有较好的客观性。

### 6. 主要结论与发现
*   **零样本迁移成功**：通过谱条件推导的 $\mu$P 规则，成功实现了所有测试优化器在不同宽度下的学习率稳定迁移。
*   **优化器特性**：发现 LAMB 和 Muon 等优化器在设计上天然具有一定的宽度独立性，而 Sophia 和 Shampoo 则需要特定的 $\mu$P 修正。
*   **深度缩放挑战**：实验表明，目前的深度缩放参数化在不同优化器上的表现不如宽度缩放稳定，最优学习率仍会随深度增加而波动，暗示需要更深入的理论研究。

### 7. 优点
*   **理论简化**：将晦涩的张量程序简化为直观的线性代数谱条件，降低了 $\mu$P 的应用门槛。
*   **扩展性强**：首次系统性地为 Sophia、Shampoo、Muon 等二阶或准二阶优化器提供了 $\mu$P 推导。
*   **实证支持**：在 NVIDIA 和 Intel 两种不同的硬件平台上均验证了方案的有效性。

### 8. 不足与局限
*   **批大小限制**：$\mu$P 假设批大小（Batch Size）固定，但在实际生产中，大模型通常需要更大的批大小，这可能导致迁移后的性能并非绝对最优。
*   **深度缩放不成熟**：论文虽然尝试了深度缩放，但结果显示其稳定性较差，未能像宽度缩放那样实现完美的零样本迁移。
*   **模型复杂度**：实验主要集中在 MLP 和 Transformer 架构，对于更复杂的混合专家模型（MoE）或其他新兴架构的适用性尚待验证。

（完）
