---
title: "SymTorch: A Framework for Symbolic Distillation of Deep Neural Networks"
title_zh: SymTorch：深度神经网络符号蒸馏框架
authors: "Elizabeth S. Z. Tan, Adil Soubki, Miles Cranmer"
date: 2026-02-24
pdf: "https://arxiv.org/pdf/2602.21307v1"
tags: ["query:sr-alg"]
score: 10.0
evidence: 通过 PySR 自动将神经网络蒸馏为闭式数学方程
tldr: "符号蒸馏旨在将神经网络转换为可解释的数学公式，但其工程集成难度限制了广泛应用。本文推出 SymTorch 框架，通过封装神经网络组件并利用 PySR 自动进行符号回归，解决了数据传输、缓存和模型切换等工程挑战。该框架支持 GNN、PINN 和 Transformer 等多种架构，并成功在 LLM 推理加速中实现 8.3% 的吞吐量提升，为深度学习模型的可解释性和效率优化提供了新工具。"
selection_source: fresh_fetch
motivation: 符号蒸馏在发现物理规律方面极具潜力，但将符号回归集成到深度学习工作流中存在巨大的工程障碍。
method: 开发了 SymTorch 库，通过自动化数据收集、符号近似及无缝的前向传播切换，简化了神经网络组件的符号化过程。
result: "在多种架构上验证了有效性，并在 LLM 推理中通过替换 MLP 层实现了 8.3% 的吞吐量提升。"
conclusion: SymTorch 降低了符号蒸馏的技术门槛，为构建更具可解释性和更高效率的深度学习模型提供了有力支持。
---

## 摘要
符号蒸馏利用可解释的闭式数学表达式替换神经网络或其组件。这种方法在直接从训练好的深度学习模型中发现物理定律和数学关系方面展现出巨大潜力，但由于将符号回归集成到深度学习工作流中的工程障碍，其应用仍然受限。我们推出了 SymTorch，这是一个通过封装神经网络组件、收集其输入输出行为并利用 PySR 将其近似为人类可读方程来自动化此蒸馏过程的库。SymTorch 解决了阻碍应用的工程挑战：GPU-CPU 数据传输、输入输出缓存、模型序列化，以及神经前向传播与符号前向传播之间的无缝切换。我们在包括图神经网络 (GNN)、物理信息神经网络 (PINN) 和 Transformer 模型在内的多种架构上演示了 SymTorch。最后，我们提出了一个通过用符号代理替换 MLP 层来加速大语言模型 (LLM) 推理的概念验证，在性能适度下降的情况下实现了 8.3% 的吞吐量提升。

## 速览
**TLDR**：符号蒸馏旨在将神经网络转换为可解释的数学公式，但其工程集成难度限制了广泛应用。本文推出 SymTorch 框架，通过封装神经网络组件并利用 PySR 自动进行符号回归，解决了数据传输、缓存和模型切换等工程挑战。该框架支持 GNN、PINN 和 Transformer 等多种架构，并成功在 LLM 推理加速中实现 8.3% 的吞吐量提升，为深度学习模型的可解释性和效率优化提供了新工具。 \
**Motivation**：符号蒸馏在发现物理规律方面极具潜力，但将符号回归集成到深度学习工作流中存在巨大的工程障碍。 \
**Method**：开发了 SymTorch 库，通过自动化数据收集、符号近似及无缝的前向传播切换，简化了神经网络组件的符号化过程。 \
**Result**：在多种架构上验证了有效性，并在 LLM 推理中通过替换 MLP 层实现了 8.3% 的吞吐量提升。 \
**Conclusion**：SymTorch 降低了符号蒸馏的技术门槛，为构建更具可解释性和更高效率的深度学习模型提供了有力支持。

---

## Abstract
Symbolic distillation replaces neural networks, or components thereof, with interpretable, closed-form mathematical expressions. This approach has shown promise in discovering physical laws and mathematical relationships directly from trained deep learning models, yet adoption remains limited due to the engineering barrier of integrating symbolic regression into deep learning workflows. We introduce SymTorch, a library that automates this distillation by wrapping neural network components, collecting their input-output behavior, and approximating them with human-readable equations via PySR. SymTorch handles the engineering challenges that have hindered adoption: GPU-CPU data transfer, input-output caching, model serialization, and seamless switching between neural and symbolic forward passes. We demonstrate SymTorch across diverse architectures including GNNs, PINNs and transformer models. Finally, we present a proof-of-concept for accelerating LLM inference by replacing MLP layers with symbolic surrogates, achieving an 8.3\% throughput improvement with moderate performance degradation.

---

## 论文详细总结（自动生成）

这是一份关于论文《SymTorch: A Framework for Symbolic Distillation of Deep Neural Networks》的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：深度学习模型（如大语言模型、物理模拟网络）虽然性能强大，但其内部逻辑是“黑盒”，难以解释。符号蒸馏（Symbolic Distillation）旨在将神经网络组件替换为简洁、可读的数学公式，但目前面临巨大的**工程障碍**：包括 GPU-CPU 数据传输、激活值缓存、模型序列化以及神经/符号前向传播的无缝切换。
*   **研究动机**：在科学发现领域，研究者需要的是像 $F=ma$ 这样简洁的物理定律。作者希望通过开发 SymTorch 框架，降低符号回归（SR）在深度学习工作流中的应用门槛，实现模型的可解释性与推理加速。

### 2. 方法论
*   **核心思想**：SymTorch 是一个基于 PyTorch 的库，它通过包装（Wrapping）神经网络组件，自动收集输入输出（I/O）数据，并调用 **PySR**（一种基于遗传算法的符号回归引擎）来寻找最优的闭式数学表达式。
*   **关键技术细节**：
    *   **SymbolicModel 包装器**：继承自 `nn.Module`，可包裹任意 PyTorch 模块或函数。
    *   **自动化数据流**：利用前向钩子（Forward Hooks）自动记录激活值，处理 GPU 到 CPU 的数据转移和缓存。
    *   **无缝切换机制**：支持 `switch_to_symbolic` 和 `switch_to_block`，允许模型在原始神经计算和符号代理计算之间自由切换。
    *   **SLIME 实现**：内置了“超局部可解释模型无关解释”（SLIME），通过在特定点周围采样并拟合符号方程来解释局部行为。
    *   **维度缩减**：针对高维输入（如 LLM 的 MLP 层），引入 PCA（主成分分析）进行降维，使符号回归在计算上可行。

### 3. 实验设计
论文通过四个多样化的案例研究验证了框架：
*   **LLM 推理加速**：在 **Qwen2.5-1.5B** 模型上，用符号方程替换部分 MLP 层。
    *   **数据集**：Wikitext-2-v1。
    *   **Benchmark**：对比了原始模型、PCA+MLP 压缩模型以及恒等函数替换（Control）。
*   **物理规律发现**：使用 **图神经网络 (GNN)** 训练粒子动力学。
    *   **场景**：引力、弹力、$1/r$ 和 $1/r^2$ 力场。
    *   **对比**：标准 GNN、瓶颈 GNN、L1 正则化、KL 正则化及剪枝（Pruning）变体。
*   **PDE 解提取**：使用 **物理信息神经网络 (PINN)** 解决一维热传导方程。
*   **LLM 算术分析**：分析 **Llama-3.2-1B** 在加法、乘法、计数和温度转换任务中的内部逻辑。

### 4. 资源与算力
*   **GPU 设备**：推理速度基准测试在 **Nvidia A100-SXM4-80GB** 上运行。
*   **计算耗时（Wall Clock Time）**：
    *   **LLM MLP 蒸馏**：在 Apple M4 Max (14核 CPU) 上耗时约 **7-8 小时**。
    *   **GNN 物理发现**：约 **10 分钟**。
    *   **PINN 蒸馏**：少于 **3 分钟**。
    *   **LLM 算术操作分析**：每个操作约 **5-15 分钟**。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了从基础物理模拟（GNN/PINN）到前沿大模型（Qwen/Llama）的多种架构，实验组设置包括了消融实验（如 PCA 敏感性分析）和对比实验（不同正则化方法）。
*   **充分性与客观性**：实验设计较为全面，不仅关注准确率（困惑度），还关注推理吞吐量。作者诚实地展示了符号替换带来的性能损失（Perplexity 上升），并将其置于帕累托前沿（Pareto Front）进行客观评价。

### 6. 主要结论与发现
*   **推理加速**：通过替换 LLM 的 3 层 MLP，实现了 **8.3% 的吞吐量提升**，且模型仍保持在性能-速度的帕累托前沿。
*   **物理发现**：SymTorch 成功从 GNN 的隐藏层中恢复了正确的物理力学公式（如平方反比定律）。
*   **PINN 优势**：相比普通神经网络，PINN 蒸馏出的符号方程更接近真实 PDE 解。
*   **LLM 偏见**：发现 LLM 在执行简单算术时，内部实际上使用了带有系统性误差的复杂启发式公式，而非精确的数学定义。

### 7. 优点
*   **工程自动化**：极大地简化了符号回归与深度学习结合的繁琐过程（如数据采集、设备管理）。
*   **架构无关性**：适用于 GNN、Transformer、PINN 等多种主流架构。
*   **可解释性增强**：提供了一种将“黑盒”权重转化为人类可读公式的直接路径。
*   **开源贡献**：提供了完整的库、文档和可复现的 Jupyter Notebook。

### 8. 不足与局限
*   **计算开销**：符号回归本质上是暴力搜索，随着变量增加，搜索空间呈指数级增长，处理极高维输入仍需依赖 PCA 等降维手段。
*   **性能权衡**：符号方程的表达能力弱于稠密神经层，在 LLM 实验中导致了不可忽视的性能下降（困惑度增加）。
*   **泛化风险**：符号代理是在特定分布上训练的，其在分布外（OOD）任务上的鲁棒性仍有待进一步验证。
*   **复杂度度量**：目前的复杂度衡量（节点数）可能无法完全等同于人类的可理解性（例如 `sin(sin(x))` 与 `x+2` 复杂度相同但理解难度不同）。

（完）
