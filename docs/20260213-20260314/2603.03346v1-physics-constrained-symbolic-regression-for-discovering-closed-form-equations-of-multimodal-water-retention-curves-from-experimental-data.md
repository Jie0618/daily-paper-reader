---
title: Physics-constrained symbolic regression for discovering closed-form equations of multimodal water retention curves from experimental data
title_zh: 物理约束符号回归用于从实验数据中发现多峰持水曲线的闭式方程
authors: "Yejin Kim, Hyoung Suk Suh"
date: 2026-02-24
pdf: "https://arxiv.org/pdf/2603.03346v1"
tags: ["query:sr-alg"]
score: 10.0
evidence: 物理约束符号回归用于发现闭式方程
tldr: 本研究针对具有多峰孔径分布的多孔材料，提出了一种物理约束的符号回归机器学习框架，旨在从实验数据中自动发现水分入渗曲线的闭式数学表达式。该方法通过遗传算法演化数学表达式树，并将物理约束集成到损失函数中，克服了传统模型在处理复杂多尺度特征时解释性差和泛化能力弱的问题。实验结果证明了该框架在发现物理一致且数学鲁棒的方程方面的有效性，为多孔介质水力建模提供了新的自动化工具。
selection_source: fresh_fetch
motivation: 传统水力模型难以捕捉多孔材料复杂的多峰孔径分布特征，且现有的叠加单峰函数方法在数据稀疏时缺乏解释性和泛化能力。
method: 提出一种物理约束的符号回归框架，利用遗传算法演化二叉树形式的数学表达式，并将物理约束嵌入损失函数以引导模型发现物理一致的解。
result: 该框架成功从实验数据中自动发现了能够有效描述不同孔隙结构多峰水分入渗特性的闭式数学方程。
conclusion: 物理约束符号回归为多孔介质水力特性的元建模提供了鲁棒且具解释性的新途径，且相关代码已开源以供进一步研究。
---

## 摘要
对具有多峰孔径分布的多孔材料的非饱和行为进行建模面临重大挑战，因为标准水力模型通常无法捕捉其复杂的、多尺度特征。一种常见的解决方法是叠加单峰持水函数，每个函数针对特定的孔径范围；然而，这种方法需要为每个模态单独进行参数识别，这限制了可解释性和泛化能力，特别是在数据稀疏的情况下。在这项工作中，我们介绍了一种根本不同的方法：一种专为元建模设计的物理约束机器学习框架，能够直接从实验数据中自动发现多峰持水曲线的闭式数学表达式。数学表达式被表示为二叉树，并通过遗传规划进行演化，同时将物理约束嵌入到损失函数中，以引导符号回归器寻找物理一致且数学稳健的解。我们的结果表明，所提出的框架可以发现能够有效表征具有不同孔隙结构的多孔材料持水特性的闭式方程。为了支持第三方验证、应用和扩展，我们将完整的实现代码在开源仓库中公开。

## 速览
**TLDR**：本研究针对具有多峰孔隙分布的多孔材料水分特征曲线建模难题，提出了一种物理约束的符号回归机器学习框架。该框架利用遗传算法演化数学表达式，并将物理约束融入损失函数，从而直接从实验数据中自动发现具有闭式解的数学方程。该方法克服了传统叠加法参数多、解释性差的缺点，为多尺度多孔介质的水力特性建模提供了物理一致且鲁式的新工具，并已开源。 \
**Motivation**：传统水力模型难以捕捉多峰孔隙分布材料的复杂多尺度特性，且现有的叠加法存在参数识别难和泛化性差的问题。 \
**Method**：提出一种基于遗传编程的物理约束符号回归框架，通过将数学表达式表示为二叉树并在损失函数中嵌入物理约束来自动发现闭式方程。 \
**Result**：实验结果表明，该框架能够发现有效表征不同孔隙结构材料水分特性的闭式方程，且具有良好的物理一致性。 \
**Conclusion**：该研究为多峰水分特征曲线的自动建模提供了新途径，其开源实现有助于该领域进一步的验证与应用。

---

## Abstract
Modeling the unsaturated behavior of porous materials with multimodal pore size distributions presents significant challenges, as standard hydraulic models often fail to capture their complex, multi-scale characteristics. A common workaround involves superposing unimodal retention functions, each tailored to a specific pore size range; however, this approach requires separate parameter identification for each mode, which limits interpretability and generalizability, especially in data-sparse scenarios. In this work, we introduce a fundamentally different approach: a physics-constrained machine learning framework designed for meta-modeling, enabling the automatic discovery of closed-form mathematical expressions for multimodal water retention curves directly from experimental data. Mathematical expressions are represented as binary trees and evolved via genetic programming, while physical constraints are embedded into the loss function to guide the symbolic regressor toward solutions that are physically consistent and mathematically robust. Our results demonstrate that the proposed framework can discover closed-form equations that effectively represent the water retention characteristics of porous materials with varying pore structures. To support third-party validation, application, and extension, we make the full implementation publicly available in an open-source repository.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种结合物理约束与符号回归（Symbolic Regression, SR）的机器学习框架，旨在从实验数据中自动发现描述多孔介质（如土壤、岩石）多峰持水曲线（Water Retention Curves, WRC）的闭式数学方程。

以下是对该论文的深度结构化总结：

### 1. 论文的核心问题与整体含义（研究动机和背景）
*   **核心问题**：如何准确且具解释性地建模具有复杂孔隙结构（多峰孔径分布）材料的非饱和持水特性。
*   **研究背景**：
    *   传统的半经验模型（如 van Genuchten 模型）通常假设孔隙分布是单峰的，难以捕捉多尺度孔隙结构的复杂行为。
    *   现有的解决方法（如 Durner 模型）通过叠加多个单峰函数来逼近，但参数识别困难，且在数据稀疏时泛化性差。
    *   深度学习模型虽然拟合能力强，但属于“黑盒”，缺乏物理可解释性，难以集成到现有的水力模拟软件中。
*   **整体含义**：论文提出了一种“元建模”方法，利用符号回归自动搜索数学表达式空间，既保证了拟合精度，又通过物理约束确保了方程的物理一致性。

### 2. 论文提出的方法论
*   **核心思想**：将持水曲线的发现过程建模为一个**多目标优化问题**。利用遗传规划（Genetic Programming）在二叉树形式的数学表达式空间中搜索，同时在损失函数中嵌入物理定律。
*   **关键技术细节**：
    *   **表达式表示**：数学方程被表示为二叉树，节点包含变量（吸力 $s$）、常数和算子（$+$, $\times$, $\sin$, $\exp$, $\ln$ 等）。
    *   **数据预处理**：对吸力进行对数变换并归一化到 $[0, 1]$ 空间，确保训练的稳定性。
    *   **多目标损失函数**：$L = L_{data} + L_{phys} + L_{mode}$。
        *   $L_{data}$：均方误差（MSE），确保拟合实验点。
        *   $L_{phys}$：物理约束项，包括**单调性**（含水率随吸力增加而降低）、**极限条件**（最大饱和度与残余饱和度处的导数为零）和**有界性**。
        *   $L_{mode}$：形状引导约束，通过计算二阶导数的符号变化次数来强制模型符合预设的模态数量（如单峰、双峰）。
*   **算法流程**：初始化随机方程种群 $\rightarrow$ 评估损失（含物理约束） $\rightarrow$ 遗传操作（选择、交叉、变异） $\rightarrow$ 迭代演化直至发现最优闭式方程。

### 3. 实验设计
*   **数据集与场景**：
    *   **单峰曲线**：使用了级配不良砂土（Song, 2014）和粘壤土（Tuller and Or, 2001）的实验数据。
    *   **双峰曲线**：使用了含砾石粉砂、级配良好砂砾（Li et al., 2014）、水稻土（Zhai et al., 2017）及砂土-高岭土混合物（Zhou et al., 2017）的实验数据。
    *   **高阶多峰（合成数据）**：利用 Durner 模型生成了三峰和四峰的合成数据集，测试框架的扩展性。
*   **Benchmark（基准方法）**：
    *   传统的 van Genuchten (1980) 模型（针对单峰）。
    *   Durner (1994) 叠加模型（针对多峰）。
    *   Vanilla SR（无物理约束的标准符号回归）。

### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的硬件型号（如 GPU/CPU 核心数）。
*   **训练参数**：提到了训练迭代次数为 100 次，种群大小为 50，表达式最大复杂度限制为 150 个节点。
*   **软件环境**：基于 Julia 语言的 `SymbolicRegression.jl` 库实现。

### 5. 实验数量与充分性
*   **实验规模**：共进行了约 8 组主要实验（2 组单峰、4 组双峰、2 组高阶合成数据），并在附录中针对单调性、极限值和有界性约束进行了专门的消融对比实验。
*   **充分性评价**：实验覆盖了从简单到复杂的多种土壤类型，对比了传统经验模型和纯数据驱动模型。通过合成数据验证了模型处理极复杂情况的能力，实验设计较为系统且客观。

### 6. 论文的主要结论与发现
*   **物理约束的重要性**：纯符号回归（Vanilla SR）虽然能拟合数据点，但经常出现非物理的波动（如非单调、超出 0-1 范围）。加入物理约束后，发现的方程在物理上是稳健的。
*   **模态引导的有效性**：通过 $L_{mode}$ 约束，模型能够准确捕捉多孔介质的多尺度特征，避免在稀疏数据下产生虚假的震荡。
*   **闭式方程的优势**：发现的方程是显式的数学表达式，可以直接求导或集成到有限元模拟代码中，且比神经网络更具解释性。

### 7. 优点
*   **白盒建模**：解决了机器学习在土木/地学领域应用时的“黑盒”信任危机。
*   **自动发现**：无需预设复杂的函数形式（如叠加几个单峰），由算法根据数据自动组合算子。
*   **鲁棒性**：在数据稀疏或含有噪声的情况下，物理约束能有效防止过拟合。
*   **开源支持**：提供了完整的 Julia 实现代码，具有良好的可重复性。

### 8. 不足与局限
*   **计算复杂度**：符号回归的搜索空间随算子增加呈指数级增长，对于极高维度的输入可能存在扩展性问题。
*   **滞后效应缺失**：目前仅针对主干燥曲线（Primary Drying Path）建模，未考虑持水特性的滞后效应（Hysteresis）。
*   **方程复杂度**：虽然是闭式方程，但演化出的某些表达式可能非常冗长（如嵌套多层 $\sin$ 和 $\exp$），物理意义的直观解释仍有难度。
*   **随机性**：遗传算法具有随机性，多次运行可能得到形式不同但效果相似的方程。

（完）
