---
title: "Taming Momentum: Rethinking Optimizer States Through Low-Rank Approximation"
title_zh: 驯服动量：通过低秩近似重新思考优化器状态
authors: "Zhengbo Wang, Jian Liang, Ran He, Zilei Wang, Tieniu Tan"
date: 2026-02-27
pdf: "https://arxiv.org/pdf/2602.24283v1"
tags: ["query:opt-lm"]
score: 10.0
evidence: Adam及现代大模型优化器的低秩近似
tldr: 本研究针对Adam等优化器中动量状态带来的巨大内存开销问题，提出了一种名为LoRA-Pre的新型低秩优化器。通过将指数移动平均（EMA）重新表述为在线线性回归过程，LoRA-Pre利用低秩分解显著降低了优化器的内存占用。实验表明，LoRA-Pre在60M至1B参数规模的Llama模型预训练中表现优异，且在微调任务中显著超越了标准LoRA等基准方法，实现了极高的秩效率和性能提升。
selection_source: fresh_fetch
motivation: 现代优化器依赖的一阶和二阶动量产生了巨大的内存开销，限制了大语言模型训练的可扩展性和效率。
method: 将动量的指数移动平均等效为在线线性学习过程，并引入低秩子空间分解来压缩完整的动量矩阵。
result: LoRA-Pre在预训练中仅需基准方法1/8的秩即可达到同等性能，且在微调Llama-3.1-8B时比标准LoRA提升了3.14分。
conclusion: LoRA-Pre通过低秩近似有效平衡了优化性能与内存效率，是适用于预训练和微调的高效通用优化方案。
---

## 摘要
Adam 和 Muon 等现代优化器是大语言模型训练的核心，但它们对一阶和二阶动量的依赖引入了显著的内存开销，限制了可扩展性和计算效率。在这项工作中，我们将这些动量中使用的指数移动平均（EMA）重新定义为通过在线梯度流训练线性回归器。基于这种等效性，我们引入了 LoRA-Pre，这是一种专为高效预训练设计的新型低秩优化器。具体而言，LoRA-Pre 通过在在线线性学习器中将完整的动量矩阵分解为紧凑的低秩子空间，从而减少了优化器的内存占用，在提高内存效率的同时保持了优化性能。我们通过预训练 Llama 架构系列的语言模型（参数规模从 60M 到 1B）实证验证了 LoRA-Pre 的有效性。LoRA-Pre 在所有模型规模上都实现了最高的性能。值得注意的是，LoRA-Pre 展示了卓越的秩效率，仅使用基准方法 1/8 的秩就达到了相当或更优的结果。除了预训练，我们还评估了 LoRA-Pre 在微调场景中的有效性。在相同秩的情况下，LoRA-Pre 始终优于所有高效微调基准。具体而言，与标准 LoRA 相比，LoRA-Pre 在 Llama-3.1-8B 上实现了 3.14 个点的显著提升，在 Llama-2-7B 上实现了 6.17 个点的提升，验证了我们方法在预训练和微调范式中的有效性。我们的代码可在 https://github.com/mrflogs/LoRA-Pre 公开获取。

## Abstract
Modern optimizers like Adam and Muon are central to training large language models, but their reliance on first- and second-order momenta introduces significant memory overhead, which constrains scalability and computational efficiency. In this work, we reframe the exponential moving average (EMA) used in these momenta as the training of a linear regressor via online gradient flow. Building on this equivalence, we introduce LoRA-Pre, a novel low-rank optimizer designed for efficient pre-training. Specifically, LoRA-Pre reduces the optimizer's memory footprint by decomposing the full momentum matrix into a compact low-rank subspace within the online linear learner, thereby maintaining optimization performance while improving memory efficiency. We empirically validate LoRA-Pre's efficacy by pre-training models from the Llama architecture family, scaling from 60M to 1B parameters. LoRA-Pre achieves the highest performance across all model sizes. Notably, LoRA-Pre demonstrates remarkable rank efficiency, achieving comparable or superior results using only 1/8 the rank of baseline methods. Beyond pre-training, we evaluate LoRA-Pre's effectiveness in fine-tuning scenarios. With the same rank, LoRA-Pre consistently outperforms all efficient fine-tuning baselines. Specifically, compared to standard LoRA, LoRA-Pre achieves substantial improvements of 3.14 points on Llama-3.1-8B and 6.17 points on Llama-2-7B, validating our approach's effectiveness across both pre-training and fine-tuning paradigms. Our code is publicly available at https://github.com/mrflogs/LoRA-Pre.

---

## 论文详细总结（自动生成）

### 论文总结：Taming Momentum: Rethinking Optimizer States Through Low-Rank Approximation

#### 1. 核心问题与整体含义（研究动机和背景）
大语言模型（LLM）的训练和微调面临巨大的内存开销，其中**优化器状态（Optimizer States）**是主要瓶颈。以 Adam 优化器为例，它需要维护与模型参数量等大的一阶动量（$m$）和二阶动量（$v$），这使得内存需求通常是模型权重的三倍。
现有的低秩优化方法（如 GaLore）通常采用投影梯度下降，依赖周期性的奇异值分解（SVD）或随机投影。这种“非连续”的子空间更新会导致优化过程中的误差累积和不稳定性。本文的研究动机在于寻找一种更动态、更连续的动量压缩方法，以在降低内存的同时保持甚至提升优化性能。

#### 2. 方法论：核心思想与技术细节
*   **核心洞察：** 论文揭示了一个重要的数学等效性——优化器中动量的**指数移动平均（EMA）更新，本质上等同于通过在线梯度流训练一个线性回归器**。即 $m \leftarrow \beta m + (1-\beta)g$ 实际上是在最小化损失函数 $L(m; g) = \frac{1}{2}\|m - g\|_F^2$。
*   **低秩分解（LoRA-Pre）：** 基于上述发现，作者将全秩动量矩阵 $m$ 分解为两个低秩矩阵的乘积 $m = m_B m_A$。
*   **更新规则：** 为了高效更新 $m_B$ 和 $m_A$，作者利用**牛顿法（Newton's Method）**推导出了闭式更新公式（Theorem 3.1）。这种更新方式不需要反向传播，且在形式上保留了 EMA 的结构，实现了子空间的步步自适应（Continuous Subspace Adaptation）。
*   **二阶动量处理：** 针对二阶动量必须为正的约束，作者将其参数化为 $v = (v_B v_A)^{\circ 2}$（哈达玛积的平方），确保了数值稳定性。
*   **通用性：** 该框架具有通用性，作者展示了如何将其应用于 AdamW 和新型优化器 Muon。

#### 3. 实验设计
*   **预训练场景：**
    *   **数据集：** C4 (Colossal Clean Crawled Corpus)。
    *   **模型：** Llama 架构，规模涵盖 60M、130M、350M 和 1B 参数。
    *   **基准方法：** Adam、Muon（全秩）、GaLore、LoRA、ReLoRA、SLTrain、LORO、Fira 等。
*   **微调场景：**
    *   **数据集：** MetaMath100k，在 GSM8K 和 MATH-500 上评估。
    *   **模型：** Llama-2-7B 和 Llama-3.1-8B。
    *   **基准方法：** LoRA、rsLoRA、DoRA、GaLore。

#### 4. 资源与算力
论文在正文和附录中**未明确列出具体的 GPU 型号、数量或总训练时长**。但提到实验设置遵循了 GaLore 的标准，且在致谢中感谢了提供计算资源的个人。实验涵盖了从 1.1B 到 13.1B tokens 的训练量，这暗示了其消耗了相当规模的计算集群资源。

#### 5. 实验数量与充分性
*   **实验规模：** 涵盖了从 60M 到 8B 参数的不同模型，跨越了预训练和微调两个范式。
*   **消融实验：** 详细探讨了不同“秩”（Rank）对性能的影响，以及在不同优化器（Adam vs Muon）下的表现。
*   **充分性与公平性：** 实验对比了近两年几乎所有主流的低秩优化算法。作者在对比时保持了相同的超参数（如缩放因子 0.25）和学习率搜索范围，确保了对比的客观性和公平性。

#### 6. 主要结论与发现
*   **卓越的性能：** LoRA-Pre 在所有预训练模型规模上均取得了最低的验证困惑度（Perplexity），显著优于 GaLore 和 Fira。
*   **极高的秩效率（Rank Efficiency）：** LoRA-Pre 表现出惊人的鲁棒性，仅需基准方法（如 GaLore）**1/8 甚至 1/16 的秩**即可达到相同甚至更好的效果。
*   **微调优势：** 在数学推理任务的微调中，LoRA-Pre 显著超越了标准 LoRA（在 8B 模型上提升 3.14 分，7B 上提升 6.17 分）。
*   **子空间自适应：** 实验证明，每一步都更新子空间（而非周期性更新）能有效避免优化中断，是性能提升的关键。

#### 7. 优点
*   **理论优雅：** 将 EMA 巧妙地联系到在线线性回归，为优化器压缩提供了坚实的数学基础。
*   **内存友好：** 显著降低了显存占用，使得在消费级显卡上训练更大模型成为可能。
*   **闭式解：** 推导出的更新公式计算高效，避免了复杂的二阶导数计算。
*   **高兼容性：** 能够无缝集成到 Adam 和 Muon 等多种现代优化器中。

#### 8. 不足与局限
*   **超参数敏感性：** 实验发现当动量系数 $\beta$ 趋于极值（如 0.999）时，由于耦合关系会导致更新率 $\gamma$ 过小，可能引发训练不稳定。
*   **计算开销：** 虽然避免了 SVD，但更新公式中涉及小规模矩阵的逆运算（$r \times r$ 维度），虽然 $r$ 很小，但在极大规模并行时可能存在微小的计算延迟。
*   **预训练规模上限：** 论文预训练实验最高只做到 1B 参数，虽然微调涉及了 8B，但超大规模（如 70B+）预训练下的表现仍需进一步验证。

（完）
