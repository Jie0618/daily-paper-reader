---
title: Towards a data-scale independent regulariser for robust sparse identification of non-linear dynamics
title_zh: 迈向一种用于非线性动力学鲁棒稀疏识别的数据尺度无关正则化项
authors: "Jay Raut, Daniel N. Wilke, Stephan Schmidt"
date: 2026-03-05
pdf: "https://arxiv.org/pdf/2603.05201v1"
tags: ["query:sr-alg"]
score: 9.0
evidence: 非线性动力学和控制方程的鲁棒稀疏识别
tldr: 针对数据归一化导致非线性动力学稀疏识别（SINDy）失效的问题，本文提出了一种对数据缩放具有鲁棒性的新算法STCV。该方法通过引入无量纲统计指标“系数存在度（CP）”替代传统的幅值阈值，有效解决了噪声和缩放干扰下的模型失真问题。实验证明，STCV在处理归一化噪声数据时显著优于现有方法，提升了物理规律识别的准确性和可靠性。
selection_source: fresh_fetch
motivation: 传统SINDy算法依赖幅值阈值，在数据归一化和噪声干扰下容易产生不准确且难以解释的稠密模型。
method: 提出变异系数顺序阈值法（STCV），利用无量纲统计指标评估候选项的统计有效性，实现对数据缩放的不变性。
result: 在多个动力学系统及物理实验中，STCV在归一化噪声数据集上的表现均显著优于STLSQ和E-SINDy。
conclusion: STCV通过消除归一化的扭曲效应，增强了稀疏系统识别在实际工程应用中的自动化程度、可解释性与可信度。
---

## 摘要
数据归一化是工程和科学应用中常见且通常必要的预处理步骤，但它会严重扭曲基于幅值的稀疏回归方法对控制方程的发现。这一问题在非线性动力学稀疏识别（SINDy）框架中尤为突出，其中稀疏性的核心假设受到数据缩放与测量噪声之间相互作用的破坏。由此发现的模型可能是稠密的、不可解释的，且在物理上是错误的。为了解决这一关键漏洞，我们引入了变异系数顺序阈值法（STCV），这是一种新型、计算高效的稀疏回归算法，对数据缩放具有内在的鲁棒性。STCV 用一种无量纲的统计指标——系数存在度（Coefficient Presence, CP）取代了传统的基于幅值的阈值处理，该指标用于评估模型库中候选术语的统计有效性和一致性。这种从幅值到统计显著性的转变使得发现过程对任意数据缩放具有不变性。通过对典型动力系统和实际工程问题（包括物理质量-弹簧-阻尼器实验）的全面基准测试，我们证明了 STCV 在归一化且含噪声的数据集上始终显著优于标准的顺序阈值最小二乘法（STLSQ）和集成 SINDy（E-SINDy）。结果表明，即使在其他方法失效的情况下，基于 STCV 的方法也能成功识别出正确的、稀疏的物理定律。通过减轻归一化的扭曲效应，STCV 使稀疏系统识别成为现实应用中更可靠、更自动化的工具，从而增强了模型的可解释性和可信度。

## 速览
**TLDR**：针对数据归一化导致非线性动力学稀疏识别（SINDy）失效的问题，本文提出了一种对数据缩放具有鲁棒性的新算法STCV。该方法通过引入无量纲统计指标“系数存在度（CP）”替代传统的幅值阈值，有效解决了噪声和缩放干扰下的模型失真问题。实验证明，STCV在处理归一化噪声数据时显著优于现有方法，提升了物理规律识别的准确性和可靠性。 \
**Motivation**：传统SINDy算法依赖幅值阈值，在数据归一化和噪声干扰下容易产生不准确且难以解释的稠密模型。 \
**Method**：提出变异系数顺序阈值法（STCV），利用无量纲统计指标评估候选项的统计有效性，实现对数据缩放的不变性。 \
**Result**：在多个动力学系统及物理实验中，STCV在归一化噪声数据集上的表现均显著优于STLSQ和E-SINDy。 \
**Conclusion**：STCV通过消除归一化的扭曲效应，增强了稀疏系统识别在实际工程应用中的自动化程度、可解释性与可信度。

---

## Abstract
Data normalisation, a common and often necessary preprocessing step in engineering and scientific applications, can severely distort the discovery of governing equations by magnitudebased sparse regression methods. This issue is particularly acute for the Sparse Identification of Nonlinear Dynamics (SINDy) framework, where the core assumption of sparsity is undermined by the interaction between data scaling and measurement noise. The resulting discovered models can be dense, uninterpretable, and physically incorrect. To address this critical vulnerability, we introduce the Sequential Thresholding of Coefficient of Variation (STCV), a novel, computationally efficient sparse regression algorithm that is inherently robust to data scaling. STCV replaces conventional magnitude-based thresholding with a dimensionless statistical metric, the Coefficient Presence (CP), which assesses the statistical validity and consistency of candidate terms in the model library. This shift from magnitude to statistical significance makes the discovery process invariant to arbitrary data scaling. Through comprehensive benchmarking on canonical dynamical systems and practical engineering problems, including a physical mass-spring-damper experiment, we demonstrate that STCV consistently and significantly outperforms standard Sequential Thresholding Least Squares (STLSQ) and Ensemble-SINDy (E-SINDy) on normalised, noisy datasets. The results show that STCV-based methods can successfully identify the correct, sparse physical laws even when other methods fail. By mitigating the distorting effects of normalisation, STCV makes sparse system identification a more reliable and automated tool for real-world applications, thereby enhancing model interpretability and trustworthiness.

---

## 论文详细总结（自动生成）

这是一份关于论文《Towards a data-scale independent regulariser for robust sparse identification of non-linear dynamics》（迈向一种用于非线性动力学鲁棒稀疏识别的数据尺度无关正则化项）的结构化分析报告：

### 1. 核心问题与研究背景
*   **核心问题**：在非线性动力学稀疏识别（SINDy）框架中，传统的稀疏回归算法（如 STLSQ）严重依赖系数的**绝对幅值**进行项的筛选。然而，在工程实践中，为了数值稳定性，通常需要对不同量级的状态变量进行**归一化**。
*   **研究背景**：当数据同时存在**测量噪声**和**归一化缩放**时，噪声引起的虚假项系数幅值可能会超过真实物理项的系数，导致传统方法识别出稠密、错误且不可解释的模型。论文旨在开发一种对数据缩放不敏感（尺度无关）的鲁棒稀疏识别算法。

### 2. 方法论：STCV 算法
*   **核心思想**：将筛选标准从“系数大小”转变为“统计一致性”。物理上真实的项在不同噪声实现下应具有稳定的估计值，而虚假项则表现出剧烈的波动。
*   **关键技术细节**：
    *   **变异系数 (CV)**：定义为系数标准差与均值的比值（$\sigma/\mu$），是一个无量纲指标。
    *   **系数存在度 (CP)**：论文定义 $CP = \sqrt{m} \cdot \mu / \sigma$（$m$ 为样本数）。CP 值越高，代表该项在统计上越显著。
    *   **贝叶斯线性回归 (BLR)**：为了避免昂贵的蒙特卡洛采样（如 E-SINDy 所用的方法），STCV 使用具有弱先验的 BLR。这允许通过闭式解（Closed-form）快速计算系数的后验均值和协方差，从而高效获取 CP 值。
*   **算法流程**：
    1.  **初始化**：使用带岭回归的最小二乘法获取初始系数。
    2.  **迭代细化**：采用类似模拟退火的策略，逐步减小岭惩罚（Ridge Penalty）并增加 CP 阈值。
    3.  **顺序阈值处理**：在每一步迭代中，剔除 CP 值低于阈值的项，直到模型结构收敛。
    4.  **级联模式 (STCV-STLSQ)**：可选方案，先用 STCV 预筛选库，再用 STLSQ 精细化。

### 3. 实验设计
*   **数据集与场景**：
    *   **典型动力学系统**：Lorenz 系统、Rössler 系统、Van der Pol 振子、Duffing 振子。
    *   **工程仿真系统**：受损轴承座模型（高刚性、尺度差异极大的线性系统）、半车模型（8 状态的高维线性及非线性系统）。
    *   **物理实验验证**：搭建了真实的质量-弹簧-阻尼物理装置，通过 IMU 采集加速度数据。
*   **Benchmark（对比方法）**：
    *   **STLSQ**：标准顺序阈值最小二乘法。
    *   **E-SINDy**：集成 SINDy（基于自助法/Bagging 的改进版）。
*   **评估指标**：识别成功率（即在不同噪声水平下正确识别出物理方程稀疏结构的概率）。

### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的 GPU 型号或训练时长。
*   **算法效率评价**：作者强调了 STCV 的计算效率，因为它利用了 BLR 的解析解，避免了 E-SINDy 中需要多次重复拟合的计算开销，因此在普通 CPU 环境下即可快速运行。

### 5. 实验数量与充分性
*   **实验规模**：
    *   针对每个数值案例，进行了 **1000 次**独立的噪声实现实验，以计算统计成功率。
    *   覆盖了从 0% 到 5% 的多个噪声梯度。
    *   对所有算法进行了广泛的超参数网格搜索（Grid Search），确保对比的公平性。
*   **充分性评价**：实验设计非常充分。不仅涵盖了低维和高维系统，还对比了原始数据与归一化数据下的表现，并最终通过物理实验闭环验证，客观地展示了 STCV 在极端尺度差异下的优势。

### 6. 主要结论与发现
*   **归一化的破坏性**：实验证实，在含噪归一化数据上，STLSQ 和 E-SINDy 的成功率会迅速降至 0%，因为它们无法区分被放大的噪声项和被缩小的物理项。
*   **STCV 的鲁棒性**：STCV 在归一化数据上的表现几乎与原始数据一致，且在多个案例中显著优于 E-SINDy。
*   **物理实验结果**：在真实物理装置的数据处理中，只有 STCV 成功识别出了正确的线性模型形式，而其他方法均引入了大量无物理意义的非线性项。

### 7. 优点
*   **尺度不变性**：解决了 SINDy 在实际工程应用中必须面对的归一化难题。
*   **计算高效**：相比于基于 MCMC 或大量 Bagging 的统计方法，STCV 的解析计算速度极快。
*   **易于集成**：可以作为现有 SINDy 工具箱的插件或预处理步骤。

### 8. 不足与局限
*   **无噪声陷阱**：在完全无噪声的理想数据下，STCV 可能会因为数值积分误差产生的微小一致性而误选项（需人为加入微量噪声来“打破”这种虚假一致性）。
*   **超参数依赖**：虽然 CP 阈值比幅值阈值更具通用性，但仍需要针对不同复杂度的系统进行一定程度的手动调优或网格搜索。
*   **偏差风险**：在系数极小的边缘物理项识别上，STCV 存在一定的漏选风险（即“捕捉阈值”效应）。

（完）
