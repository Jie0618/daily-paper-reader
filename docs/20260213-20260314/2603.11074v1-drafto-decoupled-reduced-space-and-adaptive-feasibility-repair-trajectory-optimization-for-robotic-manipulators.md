---
title: "DRAFTO: Decoupled Reduced-space and Adaptive Feasibility-repair Trajectory Optimization for Robotic Manipulators"
title_zh: DRAFTO：一种用于机器人机械臂的解耦缩减空间与自适应可行性修复轨迹优化算法
authors: "Yichang Feng, Xiao Liang, Minghui Zheng"
date: 2026-03-10
pdf: "https://arxiv.org/pdf/2603.11074v1"
tags: ["query:opt-lm"]
score: 9.0
evidence: 轨迹优化的约束目标函数
tldr: 本文提出DRAFTO算法，旨在解决机械臂轨迹优化中的效率与可行性平衡问题。该方法通过基函数参数化轨迹，并将优化过程解耦为用于主迭代的约简空间高斯-牛顿下降和用于可行性修复的约束二次规划。结合非单调策略的二阶段接受规则，DRAFTO在保证平滑性、安全性和关节限制的同时，显著提升了复杂任务下的求解效率和可靠性。
selection_source: fresh_fetch
motivation: 针对机械臂轨迹优化中重复约束优化计算量大且难以兼顾关节限制可行性的挑战。
method: 将优化解耦为约简空间高斯-牛顿下降与约束二次规划修复，并采用非单调策略的二阶段接受规则。
result: "在与CHOMP、TrajOpt及RRT*等多种主流规划器的对比测试中，该算法展现出更高的执行效率和成功率。"
conclusion: DRAFTO为复杂环境下的机械臂操作提供了一种高效且可靠的轨迹优化方案，具有很强的实际应用潜力。
---

## 摘要
本文介绍了一种新的轨迹优化算法，即解耦缩减空间与自适应可行性修复轨迹优化（DRAFTO）。该算法首先构建了一个考虑平滑度、安全性、关节限制和任务要求的约束目标函数。然后，它对系数进行优化，这些系数是用于轨迹参数化的一组基函数的坐标。为了在处理关节限制可行性的同时减少重复约束优化的次数，该优化过程被解耦为用于主迭代的缩减空间高斯-牛顿（GN）下降，以及用于初始化和末端可行性修复的约束二次规划。针对使用平方铰链惩罚项处理不等式约束的 GN 模型，应用了带有非单调策略的两阶段接受规则，以确保全局收敛性。与基于优化的规划器（如 CHOMP、TrajOpt、GPMP2 和 FACTO）以及基于采样的规划器（如 RRT-Connect、RRT* 和 PRM）进行的基准测试结果验证了该算法在不同场景和任务中的高效性和可靠性。从抽屉中抓取物体的实验进一步展示了其在复杂操作任务中的应用潜力。补充视频可在 https://youtu.be/XisFI37YyTQ 查看。

## Abstract
This paper introduces a new algorithm for trajectory optimization, Decoupled Reduced-space and Adaptive Feasibility-repair Trajectory Optimization (DRAFTO). It first constructs a constrained objective that accounts for smoothness, safety, joint limits, and task requirements. Then, it optimizes the coefficients, which are the coordinates of a set of basis functions for trajectory parameterization. To reduce the number of repeated constrained optimizations while handling joint-limit feasibility, the optimization is decoupled into a reduced-space Gauss-Newton (GN) descent for the main iterations and constrained quadratic programming for initialization and terminal feasibility repair. The two-phase acceptance rule with a non-monotone policy is applied to the GN model, which uses a hinge-squared penalty for inequality constraints, to ensure globalizability. The results of our benchmark tests against optimization-based planners, such as CHOMP, TrajOpt, GPMP2, and FACTO, and sampling-based planners, such as RRT-Connect, RRT*, and PRM, validate the high efficiency and reliability across diverse scenarios and tasks. The experiment involving grabbing an object from a drawer further demonstrates the potential for implementation in complex manipulation tasks. The supplemental video is available at https://youtu.be/XisFI37YyTQ.

---

## 论文详细总结（自动生成）

### DRAFTO：解耦缩减空间与自适应可行性修复轨迹优化算法总结

#### 1. 核心问题与整体含义（研究动机和背景）
在复杂且动态的环境中，机器人机械臂的运动规划需要同时满足高效率、高可靠性以及多种约束（如平滑度、避障安全、关节限位和特定任务约束）。
*   **研究动机**：传统的**基于优化的规划器**（如 CHOMP、TrajOpt）虽高效且轨迹平滑，但易陷入不可行的局部最优；**基于采样的规划器**（如 RRT*）具备概率完备性，但计算效率低且轨迹抖动剧烈。
*   **核心挑战**：如何在保证轨迹严格满足关节限位等硬约束的同时，显著降低大规模不等式约束带来的计算开销。

#### 2. 方法论：核心思想与技术细节
DRAFTO 提出了一种**解耦优化框架**，将轨迹优化过程分为主迭代和可行性修复两个阶段：
*   **轨迹参数化**：采用函数空间（Function-space）表示法，利用有限数量的正交基函数系数来描述连续轨迹，大幅减少了优化变量的数量。
*   **解耦优化流程**：
    1.  **初始化**：通过求解一个简单的约束二次规划（QP）问题，确保初始轨迹满足边界条件（起点/终点）。
    2.  **主迭代（缩减空间 GN 下降）**：使用高斯-牛顿法进行主要搜索。通过**零空间（Null-space）方法**处理等式约束（如任务约束），并使用**平方铰链惩罚项（Hinge-squared penalty）**将关节限位等不等式约束转化为软约束，从而避免在每次迭代中求解复杂的约束 QP。
    3.  **两阶段接受规则**：
        *   **第一阶段（探索）**：采用激进的更新策略，允许暂时的非下降步以跳出局部极值。
        *   **第二阶段（稳定）**：引入非单调接受准则，确保在接近可行解时稳定收敛。
    4.  **末端可行性修复**：在优化结束前，若轨迹仍微幅违反关节限位，则执行一次（或极少数次）**硬约束 QP 修复**，确保最终输出绝对可行。

#### 3. 实验设计
*   **实验场景**：涵盖了单臂（10 个场景，如厨房、笼子、书架、装箱等）和双臂（2 个场景，涉及协同操作和任务约束）。
*   **Benchmark 对比方法**：
    *   **优化类**：CHOMP, TrajOpt, GPMP2, FACTO（基准算法）。
    *   **采样类**：RRT-Connect, RRT*, PRM。
*   **评估指标**：成功率（Success Rate）、计算时间（Computation Time）、轨迹粗糙度（Roughness）。
*   **物理实验**：在 Franka Research 3 (FR3) 机器人上进行了从抽屉中抓取物体的复杂任务验证。

#### 4. 资源与算力
*   **硬件环境**：实验在一台配备 **2.3 GHz Intel Core i9 处理器**和 **4GB 内存**的笔记本电脑上运行。
*   **软件实现**：算法采用单线程执行，使用 Pinocchio 动力学库和 FCL 碰撞检测库。
*   **算力说明**：论文未涉及深度学习训练，因此未提及 GPU 算力。其核心优势在于 CPU 上的实时/准实时规划能力（平均计算时间在 0.03s - 0.1s 级别）。

#### 5. 实验数量与充分性
*   **实验规模**：总计进行了超过 **1000 组**规划任务测试。
*   **消融实验**：设计了 **DRAFTO-GN**（去掉非单调尾部策略的纯 GN 版本）作为对比，验证了两阶段全球化策略对提高成功率和减少最坏情况计算时间的有效性。
*   **充分性评价**：实验覆盖了从简单避障到带任务约束（如末端姿态保持）的多种难度级别，对比了当前主流的各类规划器，数据统计包含平均值和最大值，实验设计较为客观、全面。

#### 6. 主要结论与发现
*   **高效性**：DRAFTO 的计算速度比 FACTO 快 **40%-75%**，比 GPMP2 快 **2-6 倍**。
*   **可靠性**：在复杂场景（如“笼子”场景）中，DRAFTO 的成功率显著高于其他优化类规划器，且远快于采样类规划器。
*   **约束满足**：通过末端 QP 修复，DRAFTO 能够以极低的额外开销确保轨迹 100% 满足关节限位。
*   **任务适应性**：在带约束的任务（如双臂协同）中，DRAFTO 表现出比传统方法更强的鲁棒性。

#### 7. 优点（亮点）
*   **解耦策略巧妙**：将昂贵的硬约束处理留在初始化和终点，中间过程采用高效的惩罚项方法，极大地提升了搜索效率。
*   **两阶段规则**：解决了优化算法容易在可行域边界“振荡”或陷入局部最优的问题，增强了全局搜索能力。
*   **函数空间优势**：连续时间的碰撞检测和轨迹表示使得生成的运动非常平滑，无需复杂的后处理。

#### 8. 不足与局限
*   **局部优化本质**：尽管引入了两阶段规则增强探索，但 DRAFTO 本质上仍是基于梯度的局部优化算法，在极度复杂、狭窄且非凸的“迷宫式”环境中，仍可能无法找到全局最优解。
*   **基函数依赖**：轨迹的表达能力受限于基函数的数量和类型，对于极长或极复杂的轨迹，可能需要增加基函数维度，从而增加计算量。
*   **参数敏感性**：如惩罚项系数 $\sigma_{jnt}$ 和正则化参数 $\lambda$ 的设置可能需要针对不同机器人模型进行微调。

（完）
