---
title: Target-Rate Least-Squares Power Allocation over Parallel Channels
title_zh: 并行信道上的目标速率最小二乘功率分配
authors: Bhaskar Krishnamachari
date: 2026-03-06
pdf: "https://arxiv.org/pdf/2603.06893v1"
tags: ["query:opt-lm"]
score: 9.0
evidence: 使用 KKT 条件解决受限功率分配问题
tldr: 本研究针对并行高斯信道（如OFDM）中的功率分配问题，提出了一种最小化目标速率平方偏差的最小二乘分配方案。与传统的注水算法不同，该方法在满足总功率约束的前提下，通过KKT条件推导出基于Lambert W函数的闭式解，并利用一维二分法高效求解对偶变量。实验表明，该算法在目标跟踪性能上优于注水法和比例公平算法，且计算速度比通用求解器快三个数量级，具有极高的实用价值。
selection_source: fresh_fetch
motivation: 旨在解决并行信道中如何分配功率以使各信道实际速率尽可能接近预设目标速率，而非单纯追求容量最大化的问题。
method: 通过分析KKT条件，推导出基于Lambert W函数的闭式解，并将计算简化为对偶变量的一维单调二分搜索。
result: 该算法在1024个信道下实现了比通用数值求解器高达1890倍的加速，且在瑞利衰落信道下表现出优异的目标跟踪性能。
conclusion: 提出的最小二乘功率分配方案为具有特定速率需求的通信系统提供了一种计算高效且数学精确的优化手段。
---

## 摘要
我们研究了 $N$ 个并行高斯信道（如 OFDM 子载波）上的功率分配问题，其中每个信道都具有预期的目标频谱效率。给定信道增益噪声比系数 $a_i>0$ 和各信道目标 $T_i\ge 0$，我们在总功率约束 $\sum_i P_i \le P_{\mathrm{tot}}$ 和非负性约束 $P_i \ge 0$ 下，最小化总速率偏差平方和 $\sum_{i=1}^{N}(\log_2(1+a_iP_i)-T_i)^2$。我们证明了最优分配永远不会超过任何目标，并且当所有目标共同可行时可能会留有未使用的功率，这种结构与经典的注水算法（waterfilling）有着本质的不同。利用 KKT 条件，我们在激活集上推导出了基于 Lambert W 函数的各信道解析解，并将剩余计算简化为对对偶变量的一维单调二分查找。所得算法的运行时间为 $O(N\log(1/\varepsilon))$，在 $N=1024$ 个信道时，相比通用数值求解器实现了高达 1,890 倍的加速。在瑞利衰落信道上的数值实验证实，该解析解在机器精度范围内与数值优化结果一致，并且在各种运行条件下，与注水算法、均匀分配和比例公平算法相比，展现出了更优越的目标追踪性能。

## Abstract
We study power allocation over $N$ parallel Gaussian channels, such as OFDM subcarriers, when each channel has a desired target spectral efficiency. Given channel gain-to-noise coefficients $a_i>0$ and per-channel targets $T_i\ge 0$, we minimize the total squared rate deviation $\sum_{i=1}^{N}(\log_2(1+a_iP_i)-T_i)^2$ subject to a sum-power constraint $\sum_i P_i \le P_{\mathrm{tot}}$ and nonnegativity $P_i \ge 0$. We prove that the optimal allocation never overshoots any target and may leave power unused when all targets are jointly feasible, a structure fundamentally different from classical waterfilling. Using the KKT conditions, we derive a per-channel closed-form solution in terms of the Lambert~W function on the active set and reduce the remaining computation to a one-dimensional monotone bisection for the dual variable. The resulting algorithm runs in $O(N\log(1/\varepsilon))$ time and achieves up to 1{,}890$\times$ speedup over general-purpose numerical solvers at $N=1024$ channels. Numerical experiments over Rayleigh fading channels confirm that the closed-form solution matches numerical optimization to machine precision and demonstrate superior target-tracking performance compared to waterfilling, uniform allocation, and proportional fairness across a range of operating conditions.

---

## 论文详细总结（自动生成）

这篇论文由南加州大学的 Bhaskar Krishnamachari 撰写，提出了一种针对并行高斯信道（如 OFDM 系统）的新型功率分配方案。以下是对该论文的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：在无线通信（如 OFDM）中，传统的“注水算法”（Waterfilling）旨在最大化总吞吐量，但在实际应用中，系统往往需要满足特定的服务质量（QoS）目标、离散的调制编码方案（MCS）等级或调度器分配的特定速率。
*   **研究动机**：现有的目标速率分配方法通常采用“硬约束”（如必须达到最低速率），这在资源不足时会导致无解。本文提出了一种“软目标”追踪公式，通过最小化实际速率与目标速率之间的**最小二乘偏差（Least-Squares Deviation）**，在功率预算内实现最接近目标的分配，且在功率充足时避免浪费。

### 2. 论文提出的方法论
*   **核心思想**：将功率分配建模为一个凸优化问题，目标函数是各信道实际速率 $r_i = \log_2(1 + a_i P_i)$ 与目标速率 $T_i$ 之差的平方和。
*   **关键技术细节**：
    *   **无超调性质（No-Overshoot）**：证明了最优分配下，任何信道的实际速率都不会超过其目标速率 $T_i$。
    *   **凸性证明**：虽然该目标函数在全局并非凸函数，但论文证明了在 $[0, \bar{P}_i]$（即不超过目标的功率区间）内，该问题是严格凸的。
    *   **Lambert W 闭式解**：利用 KKT 条件，推导出每个激活信道的功率分配解析表达式：
        $$P_i(\lambda) = \frac{2}{\lambda c^2} W_0\left( \frac{\lambda c^2}{2 a_i} 2^{T_i} \right) - \frac{1}{a_i}$$
        其中 $W_0$ 是 Lambert W 函数的第 0 分支，$c = \ln 2$。
*   **算法流程**：
    1.  计算每个信道达到目标所需的功率上限 $\bar{P}_i$。
    2.  若总功率预算充足，直接按上限分配。
    3.  若预算不足，利用 $P_i(\lambda)$ 对对偶变量 $\lambda$ 进行一维单调二分搜索（Bisection），复杂度仅为 $O(N \log(1/\epsilon))$。

### 3. 实验设计
*   **实验场景**：
    *   **确定性场景**：8 个具有不同增益的并行信道，验证算法逻辑。
    *   **统计场景**：1000 次瑞利衰落（Rayleigh Fading）信道实现，评估平均性能。
*   **Benchmark（基准）**：通用数值优化求解器 SLSQP（用于验证解析解的精确度）。
*   **对比方法**：
    *   **注水算法（Waterfilling）**：最大化总速率。
    *   **均匀分配（Uniform Allocation）**：平分功率。
    *   **比例公平（Proportional Fairness）**：平衡用户间的速率比例。

### 4. 资源与算力
*   **硬件环境**：单核 AMD Ryzen 9 5900X 处理器。
*   **软件环境**：Python 3.11，使用 SciPy 库中的 `lambertw` 函数和 `SLSQP` 优化器。
*   **算力消耗**：未涉及 GPU 或大规模集群。论文强调了算法的轻量级特性，在 $N=1024$ 个信道时，计算耗时仅约 0.011 秒。

### 5. 实验数量与充分性
*   **实验规模**：
    *   验证了不同功率预算（$P_{tot}$）下的目标函数演变。
    *   对比了异构目标速率（Heterogeneous Targets）下的分配行为。
    *   进行了 1000 次蒙特卡洛模拟以获取速率偏差的累积分布函数（CDF）。
    *   测试了从 $N=4$ 到 $N=1024$ 的计算扩展性。
*   **充分性评价**：实验设计非常充分且客观。它不仅验证了数学推导的正确性（与数值解对比），还通过多维度（速率偏差、功率利用率、计算时间）证明了该方法在特定场景下优于传统通信算法。

### 6. 论文的主要结论与发现
*   **性能优越**：在目标追踪任务中，该方法的速率偏差显著低于注水法和比例公平法。
*   **计算极速**：相比通用数值求解器，该算法在 1024 个信道下实现了 **1890 倍的加速**。
*   **功率节省**：与注水法始终耗尽功率不同，该算法在达到所有目标后会停止增加功率（Slack Power），具有更好的能效潜力。
*   **机制差异**：注水法倾向于增强强信道，而本方法在预算有限时会优先照顾那些能最有效减小平方偏差的信道。

### 7. 优点
*   **数学优雅**：成功将复杂的非线性优化问题转化为基于 Lambert W 函数的闭式解。
*   **实用性强**：$O(N)$ 级别的复杂度使其非常适合实时性要求极高的 5G/6G 物理层资源调度。
*   **鲁棒性**：作为“软目标”方案，它避免了硬约束导致的不可行解问题，提供了平滑的性能降级。

### 8. 不足与局限
*   **CSI 假设**：假设发射端拥有完美的信道状态信息（CSI），未讨论信道估计误差的影响。
*   **模型简化**：使用了理想的香农容量公式，未考虑实际系统中有限码长、非理想编码带来的速率损失。
*   **单用户限制**：目前仅针对单用户多并行信道，虽提及可扩展至多用户 OFDMA，但未给出具体的子载波分配与功率耦合的联合优化方案。
*   **目标设定**：未讨论如何科学地设定目标速率 $T_i$，这在实际网络中通常由更高层的调度策略决定。

（完）
