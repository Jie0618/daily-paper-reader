---
title: "Learning Gradient Flow: Using Equation Discovery to Accelerate Engineering Optimization"
title_zh: 学习梯度流：利用方程发现加速工程优化
authors: "Grant Norman, Conor Rowan, Kurt Maute, Alireza Doostan"
date: 2026-02-13
pdf: "https://arxiv.org/pdf/2602.13513v2"
tags: ["query:sr-alg"]
score: 9.0
evidence: 动力系统的驱动方程发现和可变多项式阶数模型
tldr: 本研究提出“学习梯度流（LGF）”优化器，利用方程发现技术从轨迹数据中学习优化算法（如梯度下降、ADAM）的连续时间动力学。通过构建全维或降维空间的代理模型替代昂贵的目标函数评估，LGF能有效捕捉优化轨迹特征。在逆问题、拓扑优化等工程案例中，该方法显著加快了收敛速度，为降低复杂工程优化计算成本提供了高效方案。
selection_source: fresh_fetch
motivation: 针对工程优化中目标函数及其梯度评估成本过高、导致计算效率低下的问题。
method: 提出LGF优化器，通过方程发现技术学习优化轨迹的动力学，并构建多项式代理模型进行求解。
result: 在逆问题、拓扑优化及前向求解等多个工程力学案例中，该方法显著提升了收敛效率。
conclusion: 学习梯度流通过捕捉动力学特征，在减少高昂计算开销的同时实现了高效的工程优化。
---

## 摘要
在这项工作中，我们研究了利用动力系统的数据驱动方程发现技术，来建模和预测无约束优化问题的连续时间动力学。为了避免对目标函数及其梯度进行昂贵的评估，我们利用优化变量的轨迹数据来学习与梯度下降、牛顿法和 ADAM 优化相关的连续时间动力学。随后，将发现的梯度流作为原始优化问题的代理模型进行求解。为此，我们引入了学习梯度流（LGF）优化器，该优化器能够在优化过程中以用户定义的时间间隔，在全维或降维空间中构建变多项式阶数的代理模型。我们在工程力学和科学机器学习的几个标准问题上证明了该方法的有效性，包括两个反问题、结构拓扑优化以及两个具有不同离散化的正向求解问题。我们的结果表明，学习到的梯度流通过捕捉优化轨迹的关键特征，同时避免对目标及其梯度进行昂贵的评估，可以显著加速收敛。

## 速览
**TLDR**：本研究提出了一种名为“学习梯度流（LGF）”的优化器，旨在通过数据驱动的方程发现技术来加速工程优化。针对目标函数及其梯度评估成本高昂的问题，该方法利用优化变量的轨迹数据，学习梯度下降、牛顿法或ADAM等算法的连续时间动力学。通过构建全维或降维空间的代理模型，LGF能够捕捉优化轨迹的关键特征，从而在减少昂贵计算的同时显著提高收敛速度，适用于逆问题和结构拓扑优化等多种工程场景。 \
**Motivation**：传统的工程优化方法在处理复杂问题时，频繁评估目标函数及其梯度会导致极高的计算成本。 \
**Method**：提出LGF优化器，利用方程发现技术从优化轨迹中学习连续时间动力学，并将其作为原始优化问题的代理模型进行求解。 \
**Result**：在逆问题、结构拓扑优化和科学机器学习等多个标准工程问题上的实验表明，该方法能有效捕捉轨迹特征并显著加速收敛。 \
**Conclusion**：通过学习梯度流构建代理模型是一种加速工程优化的有效策略，能在保证精度的前提下大幅降低计算开销。

---

## Abstract
In this work, we investigate the use of data-driven equation discovery for dynamical systems to model and forecast continuous-time dynamics of unconstrained optimization problems. To avoid expensive evaluations of the objective function and its gradient, we leverage trajectory data on the optimization variables to learn the continuous-time dynamics associated with gradient descent, Newton's method, and ADAM optimization. The discovered gradient flows are then solved as a surrogate for the original optimization problem. To this end, we introduce the Learned Gradient Flow (LGF) optimizer, which is equipped to build surrogate models of variable polynomial order in full- or reduced-dimensional spaces at user-defined intervals in the optimization process. We demonstrate the efficacy of this approach on several standard problems from engineering mechanics and scientific machine learning, including two inverse problems, structural topology optimization, and two forward solves with different discretizations. Our results suggest that the learned gradient flows can significantly expedite convergence by capturing critical features of the optimization trajectory while avoiding expensive evaluations of the objective and its gradient.

---

## 论文详细总结（自动生成）

### 论文总结：学习梯度流（Learning Gradient Flow）

#### 1. 核心问题与整体含义（研究动机和背景）
在工程设计、逆问题求解和科学机器学习中，优化问题通常涉及复杂的偏微分方程（PDE）约束。传统的基于梯度的优化方法（如梯度下降、ADAM）需要频繁评估目标函数及其梯度，而每一次评估往往意味着一次昂贵的数值仿真（如有限元分析）。
**研究动机**：为了降低计算成本，论文提出将优化迭代过程视为一个连续时间的动力系统（即“梯度流”）。通过数据驱动的“方程发现”技术，从少量的初始优化轨迹中学习出该动力系统的近似解析方程，从而利用这个廉价的代理模型来预测后续的优化路径，跳过高昂的物理仿真步骤。

#### 2. 方法论：核心思想与关键技术
论文提出了 **Learned Gradient Flow (LGF)** 框架，其核心流程如下：
*   **核心思想**：利用 **SINDy（稀疏动力系统识别）** 算法，将优化变量 $a$ 的演化建模为 $\frac{da}{dt} = \hat{f}(a)$。其中 $\hat{f}$ 是由预定义函数库（如多项式）组成的线性组合。
*   **算法流程（定期重训练模式）**：
    1.  **数据生成阶段**：运行 $K$ 步真实的优化算法（如标准梯度下降），记录优化变量的轨迹数据。
    2.  **模型识别阶段**：利用轨迹数据，通过最小二乘法或稀疏回归求解 SINDy 系数矩阵 $\Xi$，构建代理动力学方程。
    3.  **代理演化阶段**：不再调用真实的梯度计算，而是数值积分求解学习到的 ODE，直接外推至第 $M$ 步（$M > K$）。
    4.  **循环往复**：达到 $M$ 步后，重新回到步骤 1 进行校准和重训练。
*   **针对不同优化器的扩展**：
    *   **牛顿法**：学习包含黑塞矩阵逆信息的连续动力学。
    *   **ADAM**：将动量项和二阶矩项纳入 ODE 系统，仅对其中最昂贵的梯度项进行 SINDy 建模。
*   **高维问题处理**：引入 **SVD（奇异值分解）** 进行降维，在低维潜空间（Latent Space）中学习动力学，随后映射回原空间，解决了参数量过大导致的计算负担。

#### 3. 实验设计
论文在五个具有代表性的工程力学和科学机器学习场景中验证了 LGF：
*   **场景 1：1D 动态逆问题**。识别加热棒的材料导热系数。
*   **场景 2：3D 结构拓扑优化**。高维优化问题（13,824 个变量），目标是最小化结构合规性。
*   **场景 3：非线性热传导前向求解**。使用谱方法离散化，测试对牛顿法的加速效果。
*   **场景 4：全波形反演 (FWI)**。地球物理中的非凸逆问题，测试 ADAM 版本的 LGF。
*   **场景 5：Deep Ritz 方法**。利用神经网络求解 3D 弹性力学问题，测试在随机梯度/小批量（Mini-batch）噪声环境下的表现。
*   **Benchmark 与对比方法**：所有实验均以标准的梯度下降（GD）、牛顿法（Newton）或 ADAM 优化器作为基准对比。

#### 4. 资源与算力
*   **软件框架**：基于 PyTorch 实现，使用 `torchdiffeq` 库进行 ODE 数值积分。
*   **算力说明**：论文**未明确指出**具体的 GPU 型号、数量或总训练时长。
*   **效率衡量**：主要通过“加速比”来衡量，即减少的真实梯度评估次数。例如，在拓扑优化中实现了 250% 的加速（即每 7 步中有 5 步是靠廉价代理模型预测的）。

#### 5. 实验数量与充分性
*   **实验组数**：共进行了 5 组大型实验，涵盖了线性/非线性物理、前向/逆问题、确定性/随机优化。
*   **消融与参数研究**：针对加速比（$M/K$ 比例）进行了对比实验，展示了过度加速（外推过远）会导致模型失稳或误差累积。
*   **充分性评价**：实验设计较为全面，特别是 3D 拓扑优化实验证明了该方法在极高维度下的可行性。实验对比客观，不仅展示了成功案例，也诚实地记录了模型在过度外推时失效的情况。

#### 6. 主要结论与发现
*   **显著加速**：LGF 在保持与原优化算法相似精度的前提下，可减少 33% 至 250% 的昂贵梯度评估。
*   **内在低维性**：即使是上万维的拓扑优化问题，其优化轨迹往往可以被极低维（如 2 维）的潜空间动力学捕捉。
*   **通用性**：该方法不仅适用于简单的梯度下降，也能有效加速复杂的 ADAM 和牛顿法。
*   **稳定性风险**：代理模型的有效性具有时效性，必须配合定期重训练（Scheduled Retraining）来修正轨迹偏差。

#### 7. 优点
*   **非侵入性**：不需要修改底层的物理求解器，只需记录优化变量的输出即可建模。
*   **计算高效**：SINDy 模型的训练和推理成本远低于 PDE 求解，尤其在降维后。
*   **理论联系紧密**：将优化理论与动力系统发现相结合，为理解优化算法的收敛行为提供了新视角。

#### 8. 不足与局限
*   **超参数敏感性**：历史窗口大小 $K$ 和外推步数 $M$ 的选择缺乏自动化指导，目前依赖经验调节。
*   **约束处理能力有限**：目前主要针对无约束优化，对于带有复杂不等式约束或投影步骤的优化问题，轨迹可能不连续，导致 ODE 建模困难。
*   **外推风险**：如果优化景观（Landscape）发生剧烈变化（如进入高度非凸区域），基于过去轨迹学习的模型会迅速失效。
*   **内存开销**：对于超大规模问题，存储优化历史轨迹矩阵 $A$ 可能会带来显著的内存压力。

（完）
