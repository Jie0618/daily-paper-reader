---
title: "Adam Improves Muon: Adaptive Moment Estimation with Orthogonalized Momentum"
title_zh: Adam 改进 Muon：结合正交化动量的自适应矩估计
authors: "Minxin Zhang, Yuxuan Liu, Hayden Schaeffer"
date: 2026-02-19
pdf: "https://arxiv.org/pdf/2602.17080v2"
tags: ["query:opt-lm"]
score: 10.0
evidence: 针对大语言模型训练的 Adam 类噪声自适应优化
tldr: 本研究针对大语言模型训练，提出了一种结合正交化动量与Adam式自适应噪声调节的新型优化器。Muon优化器虽利用矩阵结构表现优异，但缺乏对随机扰动的自适应性。为此，作者提出了NAMO及其对角扩展版本NAMO-D。NAMO通过单一自适应步长保持正交性，而NAMO-D通过对角矩阵实现神经元级的噪声自适应。实验证明，这两种方法在GPT-2预训练中均优于AdamW和Muon，实现了更优的收敛性能。
selection_source: fresh_fetch
motivation: 旨在解决Muon优化器在处理随机梯度噪声时缺乏自适应机制的问题，通过结合Adam的优势提升大模型训练的稳定性与效率。
method: 提出了NAMO和NAMO-D算法，前者利用全局自适应步长保持更新的正交性，后者通过带约束的对角矩阵实现细粒度的神经元级噪声调节。
result: 在GPT-2预训练实验中，NAMO和NAMO-D的性能均显著优于AdamW和Muon基准，且NAMO-D凭借更精细的自适应能力表现更佳。
conclusion: 该研究成功将正交化动量与范数自适应技术相结合，为大规模随机优化提供了具有理论保证且高效的新型优化方案。
---

## 摘要
高效的随机优化通常将确定性机制下表现良好的更新方向与适应随机扰动的机制相结合。Adam 使用自适应矩估计来提升稳定性，而 Muon 通过正交化动量利用权重层的矩阵结构，在大语言模型训练中展现出优越的性能。我们提出了一种新的优化器及其对角扩展，即 NAMO 和 NAMO-D，首次实现了正交化动量与基于范数的 Adam 式噪声自适应的原则性集成。NAMO 使用单一自适应步长缩放正交化动量，在保持正交性的同时，以微不足道的额外成本改进了 Muon。NAMO-D 则将正交化动量右乘一个带有截断项的对角矩阵。这种设计实现了神经元级的噪声自适应，并与常见的近块对角 Hessian 结构相契合。在标准假设下，我们建立了两种算法在确定性设置下的最优收敛率，并证明在随机设置下，它们的收敛保证能够适应随机梯度的噪声水平。在 GPT-2 模型预训练上的实验表明，与 AdamW 和 Muon 基准相比，NAMO 和 NAMO-D 均表现出更好的性能，其中 NAMO-D 通过一个额外的截断超参数，在维持良好条件的更新方向与利用细粒度噪声自适应这两个竞争目标之间取得平衡，从而在 NAMO 的基础上实现了进一步的提升。

## Abstract
Efficient stochastic optimization typically integrates an update direction that performs well in the deterministic regime with a mechanism adapting to stochastic perturbations. While Adam uses adaptive moment estimates to promote stability, Muon utilizes the weight layers' matrix structure via orthogonalized momentum, showing superior performance in large language model training. We propose a new optimizer and a diagonal extension, NAMO and NAMO-D, providing the first principled integration of orthogonalized momentum with norm-based Adam-type noise adaptation. NAMO scales orthogonalized momentum using a single adaptive stepsize, preserving orthogonality while improving upon Muon at negligible additional cost. NAMO-D instead right-multiplies orthogonalized momentum by a diagonal matrix with clamped entries. This design enables neuron-wise noise adaptation and aligns with the common near block-diagonal Hessian structure. Under standard assumptions, we establish optimal convergence rates for both algorithms in the deterministic setting and show that, in the stochastic setting, their convergence guarantees adapt to the noise level of stochastic gradients. Experiments on pretraining GPT-2 models demonstrate improved performance of both NAMO and NAMO-D compared to the AdamW and Muon baselines, with NAMO-D achieving further gains over NAMO via an additional clamping hyperparameter that balances the competing goals of maintaining a well-conditioned update direction and leveraging fine-grained noise adaptation.

---

## 论文详细总结（自动生成）

这篇论文由加州大学洛杉矶分校（UCLA）的研究团队提出，旨在结合 Adam 的噪声自适应能力与 Muon 的矩阵正交化优势，提升大语言模型（LLM）的训练效率。以下是对该论文的深度结构化总结：

### 1. 核心问题与研究动机
*   **研究背景**：在随机优化中，平衡“有效的更新方向”与“对随机噪声的自适应”至关重要。
*   **核心矛盾**：
    *   **Adam/AdamW**：通过坐标维度的自适应步长处理梯度噪声，但将参数视为扁平向量，忽略了神经网络权重的矩阵结构。
    *   **Muon**：利用正交化动量（Orthogonalized Momentum）在矩阵空间寻找最优方向，在 LLM 训练中表现优异，但缺乏显式的噪声自适应机制，在随机梯度波动大时可能不稳定。
*   **研究目标**：提出一种具有理论支撑的方法，将 Adam 式的自适应矩估计（SNR 缩放）集成到正交化更新中。

### 2. 论文提出的方法论
论文提出了两种算法：**NAMO** 及其对角扩展版本 **NAMO-D**。

*   **NAMO (Norm-Based Adaptive Moment Estimation with Orthogonalized Momentum)**：
    *   **核心思想**：计算动量矩阵的正交化版本 $O_t$，然后使用基于 Frobenius 范数的单一标量 $\alpha_t$ 进行缩放。
    *   **技术细节**：$\alpha_t$ 类似于 Adam 的信噪比（SNR）项，即“动量范数 / 二阶矩平方根”。这种设计在保持更新方向严格正交的同时，根据梯度噪声水平动态调整步长。
*   **NAMO-D (NAMO-Diagonal)**：
    *   **核心思想**：为了实现更细粒度的控制，将正交化动量右乘一个对角矩阵 $D_t$。
    *   **技术细节**：对角线元素基于矩阵的列范数（对应神经元维度）计算。为了防止更新方向过于畸变，引入了**截断（Clamping）机制**，将自适应步长限制在平均值的一定比例内（由超参数 $c$ 控制），在“神经元级自适应”与“维持良好条件数”之间取得平衡。
*   **理论保证**：论文证明了两种算法在确定性设置下达到最优的 $O(T^{-1/2})$ 收敛率，在随机设置下达到 $O(T^{-1/4})$，且收敛界能随噪声水平自适应。

### 3. 实验设计
*   **实验场景**：GPT-2 模型预训练。
*   **模型规模**：GPT-2 Small (124M) 和 GPT-2 Medium (355M)。
*   **数据集**：OpenWebText（约 90 亿 token）。
*   **Benchmark（基准）**：
    *   **AdamW**：工业界最常用的优化器。
    *   **Muon**：目前最先进的针对矩阵结构的优化器。
*   **对比指标**：训练损失（Training Loss）、验证损失（Validation Loss）以及对学习率（LR）的鲁棒性。

### 4. 资源与算力
*   **硬件设备**：使用了 **4 × NVIDIA H100 GPU**。
*   **训练细节**：
    *   GPT-2 (124M) 训练了 50,000 步。
    *   GPT-2 (355M) 训练了 10,000 步。
    *   使用了线性预热（2000步）加常数学习率的调度策略。

### 5. 实验数量与充分性
*   **实验规模**：
    *   针对 124M 模型进行了详尽的学习率网格搜索（Grid Search），验证了优化器在不同 LR 下的稳定性。
    *   针对 355M 模型进行了超参数 $c$ 的消融实验。
*   **充分性评价**：实验设计较为标准且客观。通过在不同规模的模型上复现改进效果，证明了算法的有效性。对比实验均在相同的 Token 数和 Batch Size 下进行，确保了公平性。

### 6. 主要结论与发现
*   **性能超越**：NAMO 和 NAMO-D 在训练和验证损失上均显著优于 AdamW 和 Muon。
*   **NAMO-D 的优势**：NAMO-D 凭借神经元级的细粒度自适应，在长时训练中表现优于 NAMO。
*   **鲁棒性**：NAMO 系列算法对学习率的选择表现出更强的鲁棒性（即在较宽的 LR 范围内都能获得较好的结果）。
*   **结构契合**：NAMO-D 的列缩放设计与神经网络中常见的“近块对角 Hessian 结构”理论相吻合。

### 7. 优点
*   **理论与实践结合**：不仅提供了严谨的收敛性证明，还在主流的 LLM 预训练任务上验证了效果。
*   **低额外开销**：NAMO 仅增加极小的标量计算开销，NAMO-D 的对角矩阵乘法也远低于二阶优化器的复杂度。
*   **即插即用**：可以作为 Muon 的直接升级版，且在代码实现上非常简洁。

### 8. 不足与局限
*   **模型规模覆盖**：实验主要集中在 GPT-2 级别（最高 355M），尚未在百亿（10B+）或千亿参数规模的超大模型上验证其扩展性。
*   **引入新超参数**：NAMO-D 引入了截断参数 $c$，虽然论文给出了推荐值，但在不同架构上可能仍需微调。
*   **任务单一**：目前仅验证了语言模型预训练，在视觉、强化学习等其他领域的泛化性有待考察。

（完）
