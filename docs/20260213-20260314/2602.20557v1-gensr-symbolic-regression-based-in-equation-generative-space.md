---
title: "GENSR: Symbolic Regression Based in Equation Generative Space"
title_zh: GenSR：基于方程生成空间的符号回归
authors: "Qian Li, Yuxiao Hu, Juncheng Liu, Yuntian Chen"
date: 2026-02-24
pdf: "https://arxiv.org/pdf/2602.20557v1"
tags: ["query:sr-alg"]
score: 10.0
evidence: 基于生成潜空间的符号回归框架
tldr: 符号回归（SR）常在离散空间搜索，导致方程结构修改与数值表现不一致，反馈信号嘈杂。本文提出GenSR框架，通过预训练双分支条件变分自编码器（CVAE），将方程重参数化为具有符号连续性和局部数值平滑性的生成潜空间。该方法遵循“地图构建、粗略定位、精细搜索”范式，利用CVAE定位候选区域并结合CMA-ES进行精细优化。GenSR在预测精度、表达式简洁度和计算效率上表现优异，且具备较强的抗噪性。
selection_source: fresh_fetch
motivation: 针对传统符号回归在离散空间搜索时，方程结构变化与数值行为不匹配导致搜索效率低下的问题。
method: 提出一种基于生成潜空间的框架，利用CVAE构建平滑的方程“地图”，并结合CMA-ES在潜空间内进行高效搜索。
result: 实验表明，GenSR在保持表达式简洁的同时，显著提升了预测精度和计算效率，并展现出良好的鲁棒性。
conclusion: 该研究通过贝叶斯视角重新定义了符号回归任务，为在连续潜空间中进行符号搜索提供了理论保证和有效路径。
---

## 摘要
符号回归 (SR) 旨在揭示观测数据背后的隐藏方程。然而，大多数方法在离散方程空间中进行搜索，其中方程的结构修改很少与其数值行为一致，导致拟合误差反馈过于嘈杂，难以引导探索。为了应对这一挑战，我们提出了 GenSR，这是一个基于生成潜空间的符号回归框架，遵循“地图构建 -> 粗略定位 -> 精细搜索”的范式。具体而言，GenSR 首先预训练一个双分支条件变分自编码器 (CVAE)，将符号方程重新参数化为具有符号连续性和局部数值平滑性的生成潜空间。该空间可以被视为方程空间中结构良好的“地图”，为搜索提供方向信号。在推理时，CVAE 将输入数据粗略定位到潜空间中有潜力的区域。随后，改进的 CMA-ES 利用平滑的潜梯度对候选区域进行精细化搜索。从贝叶斯角度来看，GenSR 将符号回归任务重新定义为最大化条件分布 $p(\mathrm{Equ.} \mid \mathrm{Num.})$，而 CVAE 的训练通过证据下界 (ELBO) 实现了这一目标。这一新视角为 GenSR 的有效性提供了理论保证。大量实验表明，GenSR 在保持对噪声鲁棒性的同时，共同优化了预测准确性、表达式简洁性和计算效率。

## 速览
**TLDR**：符号回归（SR）常在离散空间搜索，导致方程结构修改与数值表现不一致，反馈信号嘈杂。本文提出GenSR框架，通过预训练双分支条件变分自编码器（CVAE），将方程重参数化为具有符号连续性和局部数值平滑性的生成潜空间。该方法遵循“地图构建、粗略定位、精细搜索”范式，利用CVAE定位候选区域并结合CMA-ES进行精细优化。GenSR在预测精度、表达式简洁度和计算效率上表现优异，且具备较强的抗噪性。 \
**Motivation**：针对传统符号回归在离散空间搜索时，方程结构变化与数值行为不匹配导致搜索效率低下的问题。 \
**Method**：提出一种基于生成潜空间的框架，利用CVAE构建平滑的方程“地图”，并结合CMA-ES在潜空间内进行高效搜索。 \
**Result**：实验表明，GenSR在保持表达式简洁的同时，显著提升了预测精度和计算效率，并展现出良好的鲁棒性。 \
**Conclusion**：该研究通过贝叶斯视角重新定义了符号回归任务，为在连续潜空间中进行符号搜索提供了理论保证和有效路径。

---

## Abstract
Symbolic Regression (SR) tries to reveal the hidden equations behind observed data. However, most methods search within a discrete equation space, where the structural modifications of equations rarely align with their numerical behavior, leaving fitting error feedback too noisy to guide exploration. To address this challenge, we propose GenSR, a generative latent space-based SR framework following the `map construction -> coarse localization -> fine search'' paradigm. Specifically, GenSR first pretrains a dual-branch Conditional Variational Autoencoder (CVAE) to reparameterize symbolic equations into a generative latent space with symbolic continuity and local numerical smoothness. This space can be regarded as a well-structured `map'' of the equation space, providing directional signals for search. At inference, the CVAE coarsely localizes the input data to promising regions in the latent space. Then, a modified CMA-ES refines the candidate region, leveraging smooth latent gradients. From a Bayesian perspective, GenSR reframes the SR task as maximizing the conditional distribution $p(\mathrm{Equ.} \mid \mathrm{Num.})$, with CVAE training achieving this objective through the Evidence Lower Bound (ELBO). This new perspective provides a theoretical guarantee for the effectiveness of GenSR. Extensive experiments show that GenSR jointly optimizes predictive accuracy, expression simplicity, and computational efficiency, while remaining robust under noise.

---

## 论文详细总结（自动生成）

这是一份关于论文《GENSR: Symbolic Regression Based in Equation Generative Space》的结构化深入分析报告：

### 1. 核心问题与整体含义
符号回归（Symbolic Regression, SR）的核心挑战在于其搜索空间是**离散且非结构化**的。传统的搜索方法（如遗传算法、树搜索）在修改方程结构（编辑距离）时，方程的数值行为往往会发生剧烈跳变，导致拟合误差的反馈信号极其嘈杂，难以引导搜索算法走向最优解。

本研究的动机是构建一个**连续且生成式的方程潜空间**。通过将离散的方程重参数化为连续向量，将 SR 任务从“在迷宫中随机碰撞”转变为“在地图上进行定位与导航”，从而解决结构修改与数值表现不一致的问题。

### 2. 方法论
GenSR 提出了一个“地图构建 → 粗略定位 → 精细搜索”的范式：

*   **核心思想**：利用双分支条件变分自编码器（CVAE）构建一个兼具“符号连续性”和“局部数值平滑性”的潜空间。
*   **关键技术细节**：
    *   **双分支 CVAE**：
        *   **后验分支**：输入方程结构和数值样本，负责学习方程的符号表示，确保潜空间能解码出合法的数学表达式。
        *   **先验分支**：仅输入数值样本，通过 KL 散度与后验分支对齐，学习如何从数据特征推断方程位置。
    *   **潜空间特性**：通过联合优化重构损失和 KL 散度，使得潜空间中距离相近的点不仅结构相似，且数值行为也相近。
    *   **搜索算法（改进的 CMA-ES）**：在推理阶段，先由先验分支给出初始分布（粗略定位），再利用改进的协方差矩阵自适应进化策略（CMA-ES）在潜空间内收缩分布。
    *   **优化技巧**：为了应对高维潜空间，采用了对角协方差假设和 Top-k 维度更新策略，显著提升了搜索效率。
*   **理论支撑**：论文从贝叶斯视角证明，CVAE 的训练过程本质上是在优化证据下界（ELBO），从而最大化条件概率 $p(\text{Equation} | \text{Data})$。

### 3. 实验设计
*   **数据集与场景**：
    *   **SRBench**：目前符号回归领域最权威的基准测试集。
    *   **Feynman 数据集**：119 个物理方程，用于测试物理规律发现能力。
    *   **Strogatz 数据集**：用于非线性动力系统建模。
    *   **Black-box 数据集**：57 个真实世界的黑盒回归任务，目标方程未知。
*   **对比方法**：涵盖了 18 种主流算法，包括：
    *   **启发式/遗传算法**：GPlearn, Operon, GP-GOMEA。
    *   **强化学习/神经网络**：DSR, RSRM, E2ESR, TPSR。
    *   **预训练/判别式模型**：SNIP（主要竞争对手）。
    *   **物理启发式**：AI Feynman 2.0。

### 4. 资源与算力
*   **硬件环境**：使用了 2 台 **NVIDIA H800 GPU** (80GB 显存)。
*   **训练细节**：
    *   预训练数据集包含约 500 万个合成方程-样本对。
    *   训练时长：共 200 个 epoch，每个 epoch 耗时约 1 小时，总计约 **200 小时**。
    *   模型参数量：约 **1.621 亿** (162.1M)。

### 5. 实验数量与充分性
实验设计非常详尽且具有高度的客观性：
*   **多维度评估**：不仅对比了拟合精度（$R^2$），还通过 Pareto 前沿分析了方程复杂度和时间复杂度的权衡。
*   **抗噪性测试**：在 0 到 0.1 的四个噪声等级下进行了鲁棒性实验。
*   **消融实验**：深入探讨了潜空间维度、CMA-ES 参数（种群大小、步长、Top-k）对结果的影响。
*   **可视化分析**：通过 t-SNE 可视化证明了潜空间的结构化特性，并通过插值实验展示了空间的生成能力。
*   **迁移能力**：额外设计了方程分类任务，证明潜空间特征具有很强的泛化性。

### 6. 主要结论与发现
*   **Pareto 最优**：GenSR 在精度、简洁度和搜索速度三个维度上均表现优异，处于 Pareto 前沿的领先位置。
*   **生成空间优于判别空间**：相比于 SNIP 等判别式模型，GenSR 的生成式空间减少了“无效区域”，使得连续搜索成为可能。
*   **鲁棒性强**：在噪声环境下，GenSR 的性能下降比传统方法更缓慢，且生成的方程更不容易过拟合。
*   **搜索效率高**：得益于良好的“地图”引导，GenSR 避免了离散空间中的盲目随机变异。

### 7. 优点
*   **范式突破**：将符号回归从纯粹的组合优化问题转化为“定位+连续优化”问题，思路新颖。
*   **理论与实践结合**：不仅有严谨的贝叶斯理论推导，还在大规模标准 Benchmark 上证明了实用性。
*   **空间质量高**：成功解决了符号连续性与数值平滑性之间的矛盾，构建了一个高质量的方程“地图”。

### 8. 不足与局限
*   **预训练依赖**：模型的性能高度依赖于预训练合成数据的质量和覆盖范围，对于极其罕见的数学结构可能失效。
*   **算力门槛**：虽然推理很快，但预训练阶段需要昂贵的 H800 算力和大量时间，普通研究者难以复现预训练过程。
*   **常数优化**：虽然使用了 BFGS 优化常数，但在处理具有大量高精度浮点常数的复杂方程时，可能仍面临挑战。

（完）
