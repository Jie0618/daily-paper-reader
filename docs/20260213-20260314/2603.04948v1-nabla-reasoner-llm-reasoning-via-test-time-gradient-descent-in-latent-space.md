---
title: "$\\nabla$-Reasoner: LLM Reasoning via Test-Time Gradient Descent in Latent Space"
title_zh: $\nabla$-Reasoner：通过潜在空间中的测试时梯度下降实现大语言模型推理
authors: "Peihao Wang, Ruisi Cai, Zhen Wang, Hongyuan Mei, Qiang Liu, Pan Li, Zhangyang Wang"
date: 2026-03-05
pdf: "https://arxiv.org/pdf/2603.04948v1"
tags: ["query:opt-lm"]
score: 9.0
evidence: 大语言模型Token Logit的微分优化
tldr: 针对大语言模型推理时计算扩展中离散搜索效率低下的问题，本文提出 $\nabla$-Reasoner 框架。该框架引入可微文本优化（DTO），在解码过程中利用 LLM 似然和奖励模型的梯度信号实时优化 Token 概率分布。通过将零阶搜索转变为一阶优化，$\nabla$-Reasoner 在数学推理任务上显著提升了准确率并降低了计算开销，为增强 LLM 推理能力提供了高效的新路径。
selection_source: fresh_fetch
motivation: 现有的推理时扩展方法主要依赖低效的离散搜索或试错提示，难以实现最优的在线策略改进。
method: 提出 $\nabla$-Reasoner 框架，通过可微文本优化（DTO）在解码循环中利用梯度下降实时精炼文本表示。
result: "在挑战性数学推理基准上实现了超过 20% 的准确率提升，同时比强基准减少了 10-40% 的模型调用次数。"
conclusion: 该研究实现了从推理时零阶搜索到一阶优化的范式转变，为提升大语言模型推理能力提供了一种高性价比的优化方案。
---

## 摘要
扩展大语言模型（LLMs）的推理时计算已解锁了前所未有的推理能力。然而，现有的推理时扩展方法通常依赖于低效且次优的离散搜索算法或试错提示来改进在线策略。在本文中，我们提出了 $\nabla$-Reasoner，这是一个迭代生成框架，它将针对 token logit 的可微优化集成到解码循环中，以实时优化策略。我们的核心组件——可微文本优化（DTO），利用来自 LLM 似然度和奖励模型的梯度信号来精炼文本表示。$\nabla$-Reasoner 进一步结合了拒绝采样和加速设计，以增强解码的鲁棒性并提高速度。在理论上，我们证明了在样本空间中执行推理时梯度下降以最大化奖励，与通过 KL 正则化强化学习对齐 LLM 策略具有对偶性。在实证方面，$\nabla$-Reasoner 在具有挑战性的数学推理基准测试中实现了超过 20% 的准确率提升，同时与强基准相比，模型调用次数减少了约 10-40%。总之，我们的工作引入了从测试时零阶搜索到一阶优化的范式转变，为增强 LLM 推理能力提供了一条具有成本效益的路径。

## Abstract
Scaling inference-time compute for Large Language Models (LLMs) has unlocked unprecedented reasoning capabilities. However, existing inference-time scaling methods typically rely on inefficient and suboptimal discrete search algorithms or trial-and-error prompting to improve the online policy. In this paper, we propose $\nabla$-Reasoner, an iterative generation framework that integrates differentiable optimization over token logits into the decoding loop to refine the policy on the fly. Our core component, Differentiable Textual Optimization (DTO), leverages gradient signals from both the LLM's likelihood and a reward model to refine textual representations. $\nabla$-Reasoner further incorporates rejection sampling and acceleration design to robustify and speed up decoding. Theoretically, we show that performing inference-time gradient descent in the sample space to maximize reward is dual to aligning an LLM policy via KL-regularized reinforcement learning. Empirically, $\nabla$-Reasoner achieves over 20% accuracy improvement on a challenging mathematical reasoning benchmark, while reducing number of model calls by approximately 10-40% compared to strong baselines. Overall, our work introduces a paradigm shift from zeroth-order search to first-order optimization at test time, offering a cost-effective path to amplify LLM reasoning.

---

## 论文详细总结（自动生成）

这是一份关于论文《$\nabla$-Reasoner: LLM Reasoning via Test-Time Gradient Descent in Latent Space》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
论文的核心问题在于如何更高效地实现大语言模型（LLM）的**推理时计算扩展（Inference-time Scaling）**。
*   **背景**：目前的推理扩展方法（如 OpenAI o1 采用的思路）主要依赖于**零阶搜索算法**（如 Best-of-N 采样、蒙特卡洛树搜索 MCTS、Tree-of-Thought 等）。
*   **动机**：零阶搜索在处理长推理链时效率低下，且由于奖励信号稀疏，搜索空间呈指数级增长，导致性能容易饱和。
*   **核心思想**：将推理过程从“离散搜索”转变为“连续优化”。利用 LLM 和奖励模型（RM）本身是可微的特性，在 Token 的 Logit 空间（潜在空间）进行**一阶梯度下降**，从而直接引导模型生成高奖励的响应。

### 2. 方法论：核心思想与关键技术
论文提出了 **$\nabla$-Reasoner** 框架，其核心组件是**可微文本优化（DTO）**：
*   **目标函数**：构建了一个结合奖励最大化和语言模型似然度的损失函数：$L(y) = -\lambda r(y|x) - \log \pi_{LLM}(y|x)$。其中奖励项引导逻辑正确性，似然项（正则项）确保生成的文本保持流畅且不偏离预训练分布（防止奖励作弊）。
*   **参数化与优化**：
    *   将离散 Token 映射到连续的 Logit 向量空间。
    *   使用**直通估计器（Straight-Through Estimator, STE）**解决离散采样不可微的问题，允许梯度从奖励模型反向传播回 Token Logits。
*   **迭代解码流程**：
    1.  **初始生成**：模型先生成一个完整的推理链。
    2.  **策略精炼（DTO）**：对该推理链的 Logits 进行 $T$ 步梯度下降优化。
    3.  **拒绝采样**：从优化后的首个 Token 分布中采样，若新 Token 产生的完整路径奖励更高则接受，否则回退。
*   **加速策略**：
    *   **梯度缓存**：仅在 Token 类别发生变化时重新计算重型梯度。
    *   **Token 选择**：仅对低置信度（高熵）或高梯度的 Token 执行优化，跳过确定性高的 Token。
    *   **KV Cache 复用**：在迭代中最大化复用计算结果。

### 3. 实验设计
*   **数据集**：专注于数学推理，包括 **MATH-500**、**AIME24**、**AIME25** 和 **AMC**。
*   **基座模型**：Qwen-2.5-7B（Base & Instruct）、Llama-3.1-8B-Instruct。
*   **奖励模型**：Skywork-V2 系列（4B 和 8B 版本）。
*   **对比方法（Baselines）**：
    *   **测试时方法**：Greedy Decoding、Self-Consistency (SC)、Best-of-N (BoN)、Tree-of-Thought (ToT)、Reasoning-as-Planning (RAP)、TPO。
    *   **训练时方法**：SFT（监督微调）、GRPO（强化学习训练）。

### 4. 资源与算力
*   **硬件环境**：论文在性能测试部分提到了使用 **8x NVIDIA H200 (140GB)** GPU 集群。
*   **算力细节**：
    *   在 AMC 数据集上对 83 个 Prompt 进行测试，$\nabla$-Reasoner 的总墙钟时间（152.1s）略高于 BoN (136.1s)，但其 FLOPs 消耗显著更高（约高出两个数量级）。
    *   这表明该方法通过牺牲计算密度（利用 GPU 并行处理梯度的能力）换取了更少的模型调用次数和更高的推理质量。

### 5. 实验数量与充分性
*   **实验规模**：在 4 个主流数学基准上进行了测试，涵盖了不同参数规模和微调阶段的模型，实验设置较为全面。
*   **消融实验**：
    *   对比了不同奖励模型（4B vs 8B）的影响，证明了方法对 RM 规模的鲁棒性。
    *   分析了拒绝采样率，证明 DTO 确实显著提高了采样成功率（拒绝率从 66% 降至约 30%）。
    *   绘制了测试时扩展曲线（Scaling Law），展示了在相同模型调用次数下，该方法优于 BoN 和 SC。
*   **客观性**：通过引入“模型调用次数”作为统一度量衡，公平地对比了不同搜索算法的效率。

### 6. 主要结论与发现
*   **性能飞跃**：在 Qwen-2.5-7B-Instruct 上，MATH-500 准确率达到 80.4%，显著超过所有测试时基准。
*   **效率优势**：相比 BoN 等方法，在达到相同或更高精度时，模型调用次数减少了 **10-40%**。
*   **理论关联**：论文证明了在样本空间进行梯度下降在数学上等价于执行 KL 正则化的强化学习（如 PPO），这为“测试时对齐”提供了坚实的理论基础。
*   **一阶优化的优越性**：梯度提供的方向性信息比单纯的标量奖励值（零阶）更能有效引导模型找到最优解。

### 7. 优点（亮点）
*   **范式创新**：将 LLM 推理从低效的“试错采样”提升为高效的“梯度引导优化”。
*   **计算效率**：巧妙利用 Transformer 的并行计算特性处理梯度，虽然 FLOPs 高，但在实际推理延迟上具有竞争力。
*   **即插即用**：不需要对原模型进行任何参数微调，即可在测试时显著增强性能。
*   **理论深度**：建立了测试时优化与强化学习训练之间的对偶关系。

### 8. 不足与局限
*   **白盒依赖**：该方法要求能够获取模型的 Logits 和梯度，因此**无法直接应用于闭源 API 模型**（如 GPT-4o）。
*   **词表一致性**：要求奖励模型和语言模型共享相同的词表，否则难以进行端到端的梯度传播。
*   **计算开销**：尽管模型调用次数减少，但反向传播带来的内存占用和计算量（FLOPs）远高于纯前向推理。
*   **任务局限**：目前主要在数学推理（结果易于验证且奖励模型较成熟）上验证，在开放式创作或多模态任务上的效果尚待观察。

（完）
