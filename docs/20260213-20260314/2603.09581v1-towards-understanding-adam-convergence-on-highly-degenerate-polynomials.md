---
title: Towards Understanding Adam Convergence on Highly Degenerate Polynomials
title_zh: 深入理解 Adam 在高度退化多项式上的收敛性
authors: "Zhiwei Bai, Jiajie Zhao, Zhangchen Zhou, Zhi-Qin John Xu, Yaoyu Zhang"
date: 2026-03-10
pdf: "https://arxiv.org/pdf/2603.09581v1"
tags: ["query:opt-lm"]
score: 9.0
evidence: Adam算法的收敛特性与理论条件
tldr: 本研究探讨了Adam优化器在高度退化多项式上的自动收敛特性，填补了其在特定目标函数下优势研究的空白。通过理论推导，证明了Adam在无需外部调度器的情况下可实现局部线性收敛，显著优于梯度下降的次线性收敛。研究揭示了二阶动量与梯度平方解耦带来的加速机制，并刻画了超参数相位图，为理解Adam的优越性提供了新视角。
selection_source: fresh_fetch
motivation: 旨在探索Adam在无需外部调度器的情况下，在何种特定目标函数类上具有天然的收敛优势。
method: 针对高度退化多项式，推导了Adam局部渐近稳定性的理论条件，并分析了二阶动量与梯度平方之间的解耦机制。
result: 证明了Adam在退化函数上能实现局部线性收敛，速度远超梯度下降和动量法，并识别出三种不同的超参数行为状态。
conclusion: Adam通过指数级放大有效学习率实现加速，其在退化景观中的表现证明了其相对于传统一阶方法的内在优越性。
---

## 摘要
Adam 是深度学习中广泛使用的优化算法，但其表现出固有优势的具体目标函数类别仍有待探索。与以往需要外部调度器且 $β_2$ 接近 1 才能收敛的研究不同，本工作研究了 Adam 的“自然”自动收敛特性。我们识别出一类高度退化的多项式，Adam 在这些多项式上无需额外调度器即可自动收敛。具体而言，我们推导了退化多项式上局部渐近稳定性的理论条件，并证明了理论界限与实验结果之间的高度一致性。我们证明了 Adam 在这些退化函数上实现了局部线性收敛，显著优于梯度下降（Gradient Descent）和动量法（Momentum）的次线性收敛。这种加速源于二阶矩 $v_t$ 与梯度平方 $g_t^2$ 之间的解耦机制，该机制以指数方式放大了有效学习率。最后，我们刻画了 Adam 的超参数相图，识别出三种不同的行为模式：稳定收敛、尖峰以及类 SignGD 振荡。

## Abstract
Adam is a widely used optimization algorithm in deep learning, yet the specific class of objective functions where it exhibits inherent advantages remains underexplored. Unlike prior studies requiring external schedulers and $β_2$ near 1 for convergence, this work investigates the "natural" auto-convergence properties of Adam. We identify a class of highly degenerate polynomials where Adam converges automatically without additional schedulers. Specifically, we derive theoretical conditions for local asymptotic stability on degenerate polynomials and demonstrate strong alignment between theoretical bounds and experimental results. We prove that Adam achieves local linear convergence on these degenerate functions, significantly outperforming the sub-linear convergence of Gradient Descent and Momentum. This acceleration stems from a decoupling mechanism between the second moment $v_t$ and squared gradient $g_t^2$, which exponentially amplifies the effective learning rate. Finally, we characterize Adam's hyperparameter phase diagram, identifying three distinct behavioral regimes: stable convergence, spikes, and SignGD-like oscillation.

---

## 论文详细总结（自动生成）

这是一份关于论文《Towards Understanding Adam Convergence on Highly Degenerate Polynomials》的结构化深入分析报告：

### 1. 核心问题与整体含义（研究动机和背景）
*   **研究动机**：尽管 Adam 优化器在深度学习中被广泛应用，但其相对于梯度下降（GD）和动量法（Momentum）表现出显著优势的理论根源仍不明确。
*   **核心问题**：在不需要外部学习率调度器（Scheduler）的情况下，Adam 在何种函数类上具有“天然”的收敛优势？
*   **背景**：传统理论多关注强凸函数，但深度学习的损失景观（Loss Landscape）包含大量**高度退化（Highly Degenerate）**的方向（即 Hessian 矩阵特征值接近 0）。该论文旨在填补 Adam 在此类退化景观下收敛特性的理论空白。

### 2. 方法论：核心思想与技术细节
*   **模型问题**：研究原型函数 $L(x) = \frac{1}{k}x^k$（$k \ge 4$ 且为偶数），其在原点处的 Hessian 矩阵消失，代表了高度退化的景观。
*   **状态空间动力学**：引入两个关键的归一化状态变量来解耦迭代尺度与优化器动力学：
    *   $\omega_t := m_t / x_t^{k-1}$（归一化一阶矩）。
    *   $\lambda_t := x_t^{k-2} / \sqrt{v_t}$（有效曲率）。
*   **局部稳定性分析**：通过推导离散动力系统的雅可比矩阵（Jacobian），确定了非平凡固定点的局部渐近稳定性条件。
*   **加速机制（解耦机制）**：
    *   **核心思想**：在退化函数上，梯度 $g_t$ 消失的速度快于二阶矩 $v_t$ 的记忆衰减速度。
    *   **技术细节**：这导致 $v_t$ 与 $g_t^2$ 发生**解耦**，使得 $v_t$ 呈现自主的几何级数衰减（$v_t \approx \beta_2 v_{t-1}$）。这种衰减在分母上起作用，等效于为算法提供了一个**指数级增长的有效学习率**，从而将 GD 的次线性收敛（Power-law）加速为 Adam 的线性收敛（Exponential）。

### 3. 实验设计
*   **合成实验（Synthetic）**：
    *   **场景**：一维及多维退化多项式（$x^4, x^6, x^{20}$）以及混合曲率函数（二次项与四次项耦合）。
    *   **Benchmark**：以 GD、带动量的 GD（Momentum）、SignGD 为基准。
    *   **对比维度**：收敛速度（Loss 随 Epoch 的变化）、超参数 $\beta_1, \beta_2$ 对稳定性的影响。
*   **深度学习实验**：
    *   **场景**：MNIST 上的多层感知机（MLP）、CIFAR-10 上的卷积神经网络（CNN）、SlimPajama 数据集上的 Transformer（GPT-2）。
    *   **目的**：验证不同架构的损失景观退化程度（通过 Hessian 谱密度分析）与 Adam 优势之间的相关性。

### 4. 资源与算力
*   **算力说明**：论文中**未明确说明**具体的 GPU 型号、数量或总训练时长。
*   **实验性质**：由于核心部分是针对一维或低维多项式的数值模拟，这类实验通常在普通 CPU 或单块 GPU 上即可快速完成。深度学习部分的实验（如 GPT-2 训练）涉及真实数据集，但未给出具体的硬件配置信息。

### 5. 实验数量与充分性
*   **实验规模**：
    *   进行了详尽的超参数网格搜索（$50 \times 50$ 的 $\beta_1, \beta_2$ 采样），生成了精细的经验相图。
    *   针对不同阶数 $k$ 的多项式进行了重复验证。
    *   包含了从合成函数到真实复杂模型（Transformer vs CNN）的跨度。
*   **充分性与客观性**：实验设计较为充分，理论推导的稳定性边界与实验观测到的收敛/发散区域高度吻合（见论文 Fig 3 和 Fig 12），证明了理论的预测力。通过对比不同激活函数（ReLU vs Softmax）对退化程度的影响，进一步增强了结论的客观性。

### 6. 主要结论与发现
*   **线性收敛证明**：证明了 Adam 在高度退化多项式上能实现局部线性收敛，而 GD 和 Momentum 仅能达到次线性收敛。
*   **相图刻画**：识别出 Adam 的三个超参数行为状态：
    1.  **区域 I（稳定收敛）**：$\beta_1$ 较小且满足稳定性条件，实现稳定的指数加速。
    2.  **区域 II（尖峰/Spikes）**：存在不稳定的非平凡固定点，初始加速后会发生剧烈的 Loss 尖峰。
    3.  **区域 III（类 SignGD 振荡）**：$\beta_1$ 过大导致无法形成指数加速阶段，表现为在最小值附近的振荡。
*   **架构洞察**：发现 Transformer 的损失景观比 CNN 具有更高的退化程度（Hessian 特征值更集中于 0），这解释了为什么 Adam 在 Transformer 上比在 CNN 上具有更显著的优势。

### 7. 优点
*   **理论突破**：在无需学习率衰减（Constant LR）的假设下，给出了 Adam 在退化函数上的全超参数空间稳定性分析，推广了前人仅限于 $\beta_2 \approx 1$ 的结论。
*   **机制解释**：清晰地揭示了“二阶矩解耦”这一物理直觉，为理解自适应优化器的加速本质提供了新视角。
*   **理论与实践结合**：通过 Hessian 谱分析将抽象的多项式理论与现实中的 Transformer 优化难题联系起来。

### 8. 不足与局限
*   **确定性假设**：主要分析是在确定性（Full-batch）设置下进行的，未充分探讨随机梯度（Stochastic noise）对解耦机制和稳定性边界的干扰。
*   **局部性限制**：稳定性分析基于局部固定点，对于 Adam 如何从远离目标的区域进入该局部稳定区域（全局收敛性）讨论较少。
*   **函数类限制**：虽然退化多项式具有代表性，但真实神经网络的非凸性远比单项式复杂，理论模型在多维非凸耦合情况下的适用性仍有待进一步验证。

（完）
