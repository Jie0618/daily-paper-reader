---
title: An information-based model selection criterion for data-driven model discovery
title_zh: 一种用于数据驱动模型发现的基于信息的模型选择准则
authors: "Michael C Chung, Alen Zacharia, Juan Guan"
date: 2026-02-24
pdf: "https://arxiv.org/pdf/2602.21393v1"
tags: ["query:sr-alg"]
score: 10.0
evidence: 平衡符号模型的拟合优度与稀疏性
tldr: 数据驱动模型发现（DDMD）在平衡拟合度与稀疏性时常面临调参困难和过拟合问题。本文提出一种稀疏回归算法，能自动生成候选模型，并引入一种新型的样本长度缩放对数信息准则（SLIC）来识别最优模型。SLIC在处理非线性常微分和偏微分方程时表现优于传统准则，并能从流体力学和纳米技术实验数据中提取具有预测能力的解释性模型。
selection_source: fresh_fetch
motivation: 现有的数据驱动模型发现方法在平衡模型复杂度和拟合精度时高度依赖人工调参，且容易出现过拟合。
method: 提出一种自动生成候选模型的稀疏回归算法，并结合新型的样本长度缩放对数信息准则（SLIC）进行模型选择。
result: 实验证明 SLIC 在多种非线性方程及流体力学、纳米技术实验数据上的表现显著优于其他流行信息准则。
conclusion: SLIC 为数据驱动的模型发现提供了一种稳健且自动化的选择机制，增强了从复杂实验数据中提取可解释物理模型的能力。
---

## 摘要
数据驱动的模型发现 (DDMD) 算法是从数据中提取可解释符号模型的强大工具。然而，识别出最佳平衡拟合优度与稀疏性的模型通常是一个费力的过程，需要用户进行微调，且容易出现过拟合，结果可能会因模型初始化和特定训练过程的不同而产生显著差异。在这里，我们提出了一种稀疏回归算法，该算法能够自动且自适应地生成候选模型，并使用一种新型的样本长度缩放对数信息准则 (SLIC) 从这些候选模型中识别出最佳模型。我们证明了在从多个非线性常微分方程和偏微分方程的数据中提取正确模型方面，SLIC 的表现大大优于其他流行的信息准则。随后，我们展示了 SLIC 从流体动力学和纳米技术的实验数据集中发现可解释模型的能力，这些模型产生了新的可测试预测。

## 速览
**TLDR**：数据驱动模型发现（DDMD）在平衡拟合度与稀疏性时常面临调参困难和过拟合问题。本文提出一种稀疏回归算法，能自动生成候选模型，并引入创新的样本长度缩放对数信息准则（SLIC）进行模型选择。实验证明 SLIC 在处理非线性微分方程及流体力学、纳米技术等实验数据时，其准确性和解释性显著优于传统信息准则，为科学发现提供了强有力的自动化工具。 \
**Motivation**：现有的数据驱动模型发现算法在平衡模型复杂度和拟合精度时高度依赖人工调参，且容易出现过拟合。 \
**Method**：提出一种自适应生成候选模型的稀疏回归算法，并结合一种新型的样本长度缩放对数信息准则（SLIC）来自动识别最优模型。 \
**Result**：在多种非线性常微分和偏微分方程以及流体力学、纳米技术的实验数据中，SLIC 的模型提取表现显著优于主流信息准则。 \
**Conclusion**：SLIC 为从复杂实验数据中自动发现具有可解释性和预测能力的物理模型提供了一种稳健且高效的解决方案。

---

## Abstract
Data-driven model discovery (DDMD) algorithms are powerful tools for extracting interpretable symbolic models from data. However, identifying the model that best balances goodness-of-fit and sparsity is often a laborious process requiring user fine-tuning, is prone to overfitting, and results may significantly vary depending on model initialization and specific training procedure. Here, we present a sparse regression algorithm that automatically and adaptively generates candidate models, and uses a novel sample-length-scaling logarithmic information criterion (SLIC) to identify the best model from these candidates. We demonstrate that SLIC greatly outperforms other popular information criteria in extracting the correct model from the data of several nonlinear ordinary and partial differential equations. Then, we demonstrate SLIC's ability to discover interpretable models from experimental datasets in fluid dynamics and nanotechnology that generate new testable predictions.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **SLIC**（Sample-Length-scaling Logarithmic Information Criterion，样本长度缩放对数信息准则）的新型模型选择方法，旨在解决数据驱动模型发现（DDMD）中的过拟合与调参难题。

以下是对该论文的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：在从数据中自动提取数学方程（如微分方程）时，如何平衡**拟合精度**与**模型稀疏性**（简洁性）。
*   **研究背景**：现有的数据驱动模型发现方法（如 SINDy）依赖于稀疏惩罚参数的微调。传统的模型选择准则（如 AIC、BIC）在处理大规模数据集时，往往会过度偏向拟合精度而忽略稀疏性，导致模型包含过多的虚假项（过拟合），且无法在不同数据量下保持一致性。
*   **整体含义**：论文旨在开发一种无需人工干预、对数据量鲁棒、且能自动识别最简物理规律的自动化算法。

### 2. 论文提出的方法论
*   **核心思想**：基于缩放和对称性原则，设计一个使拟合误差和模型复杂度（参数数量）具有同等权重的评分函数。
*   **关键技术细节**：
    *   **SLIC 公式**：$SLIC(\hat{\epsilon}, k) = n \log(\hat{\epsilon}k)$。其中 $n$ 是样本量，$\hat{\epsilon}$ 是均方误差（MSE），$k$ 是模型参数数量。
    *   **理论关联**：论文证明了最小化 SLIC 等价于在统计学中的“偏差-方差权衡”中最小化模型方差，这使得选出的模型具有更强的泛化能力。
    *   **自动剪枝算法**：
        1.  使用伪逆获得初始模型估计。
        2.  自适应地从系数大小中生成一系列候选阈值 $\lambda$。
        3.  利用 SLIC 对不同阈值产生的候选模型进行评分。
        4.  迭代优化，自动锁定 SLIC 分数最低（即最优）的模型。
    *   **弱形式表述**：采用 Galerkin 投影（积分形式）处理噪声数据，避免直接对噪声数据求导。

### 3. 实验设计
*   **数据集与场景**：
    *   **仿真 ODE（常微分方程）**：包括 Lorenz 系统、Rössler 吸引子、Lotka-Volterra 模型、Brusselator、Van der Pol 振子和非线性摆。
    *   **仿真 PDE（偏微分方程）**：包括 Burger's 方程、KdV 方程、Kuramoto-Sivashinsky (KS) 方程、非线性薛定谔方程 (NLS) 和 Sine-Gordon 方程。
    *   **真实实验数据**：矩形水箱中的流体晃动质心动力学、RNA-脂质体纳米颗粒的团聚动力学（Smoluchowski 理论）。
*   **Benchmark 与对比方法**：
    *   对比了 6 种主流信息准则：**AIC** (Akaike)、**AICc** (修正 AIC)、**BIC** (Bayesian)、**HQIC** (Hannan-Quinn)、**KIC** (Kashyap) 和 **BC** (Bridge Criterion)。

### 4. 资源与算力
*   **算力说明**：论文**未明确提及**具体的 GPU 型号、数量或具体的训练时长。
*   **软件环境**：实验使用了 Julia 语言及其生态系统（如 `OrdinaryDiffEq.jl` 用于仿真，`Optimization.jl` 用于参数优化，`Trapz.jl` 用于数值积分）。由于稀疏回归和信息准则计算通常涉及矩阵运算和优化，其算力需求远低于深度学习模型，通常在普通工作站即可完成。

### 5. 实验数量与充分性
*   **实验规模**：
    *   针对每个仿真系统，在不同的噪声水平（0% 到 40%）下进行了 **25 次独立随机实验**。
    *   测试了不同采样频率、轨迹长度和系统参数下的鲁棒性。
*   **充分性与客观性**：实验设计非常充分。作者不仅在标准基准数据集上进行了测试，还引入了具有挑战性的高噪声环境和跨领域的真实实验数据。对比指标包括准确率（Accuracy）、虚假发现率（FPR）和系数误差（SMAE），评估维度全面且客观。

### 6. 论文的主要结论与发现
*   **卓越的鲁棒性**：SLIC 在所有测试的 ODE 和 PDE 系统中，识别正确方程项的准确率显著高于 AIC、BIC 等传统准则。
*   **极低的过拟合率**：在噪声增加时，传统准则的虚假发现率（FPR）迅速上升，而 SLIC 几乎不引入多余的错误项。
*   **强大的预测能力**：
    *   在流体晃动实验中，SLIC 仅凭无驱动数据就准确预测了有驱动条件下的滞后和不稳定行为。
    *   在纳米技术实验中，SLIC 成功识别出常数核（Constant Kernel），并预测了不同稀释浓度下的颗粒生长速率。

### 7. 优点
*   **无需调参**：SLIC 内部没有超参数，实现了模型发现的完全自动化。
*   **数据量不变性**：解决了传统准则随数据量增大而倾向于复杂模型的缺陷。
*   **物理可解释性**：强制执行稀疏性，确保提取的模型符合物理直觉，而非仅仅是曲线拟合。

### 8. 不足与局限
*   **框架限制**：目前主要在稀疏线性回归框架（Library-based methods）下验证，尚未探讨其在神经网络符号回归或非线性参数优化中的普适性。
*   **先验依赖**：算法仍依赖于用户预定义的候选函数库（Library），如果真实的物理项不在库中，SLIC 无法凭空发现。
*   **计算复杂度**：虽然单次评分很快，但在处理具有海量候选项的超大型库时，迭代搜索最优阈值的过程可能会变慢。

（完）
