---
title: "OptEMA: Adaptive Exponential Moving Average for Stochastic Optimization with Zero-Noise Optimality"
title_zh: OptEMA：具有零噪声最优性的随机优化自适应指数移动平均
authors: Ganzhao Yuan
date: 2026-03-10
pdf: "https://arxiv.org/pdf/2603.09923v1"
tags: ["query:opt-lm"]
score: 10.0
evidence: 随机优化的自适应EMA与Adam风格方法
tldr: 针对Adam类优化器在零噪声环境下表现欠佳及依赖超参数等局限，本文提出OptEMA算法。该算法通过引入自适应指数移动平均（EMA）系数，实现了闭环且无需Lipschitz常数的优化过程。OptEMA包含两种变体，分别对一阶和二阶动量进行自适应调整，在随机梯度下降标准假设下，实现了噪声自适应的收敛率，且在零噪声时达到近乎最优的确定性收敛速度。
selection_source: fresh_fetch
motivation: 现有Adam类优化器在零噪声环境下收敛性非最优，且往往依赖有界性假设或预先已知的Lipschitz常数。
method: 提出OptEMA算法及其两种变体，通过轨迹相关的自适应EMA系数实现闭环且无需Lipschitz常数的优化。
result: 算法实现了噪声自适应的收敛率，并在零噪声情况下自动达到近乎最优的确定性收敛速度。
conclusion: OptEMA为随机优化提供了一种更稳健、自适应且理论完备的动量调整方案，有效解决了传统方法在不同噪声水平下的性能瓶颈。
---

## 摘要
指数移动平均（EMA）是 Adam 等广泛使用的优化器的基石。然而，现有的 Adam 类方法的理论分析存在显著局限性：其保证在零噪声状态下可能仍非最优，依赖于限制性的有界性条件（例如有界梯度或目标函数间隙），使用常数或开环步长，或者需要 Lipschitz 常数的先验知识。为了克服这些瓶颈，我们引入了 OptEMA 并分析了两个新变体：OptEMA-M 将自适应递减的 EMA 系数应用于一阶矩，并保持固定的二阶衰减；而 OptEMA-V 则交换了这两个角色。至关重要的是，OptEMA 是闭环且无需 Lipschitz 常数的（Lipschitz-free），即其有效步长依赖于轨迹，且不需要 Lipschitz 常数进行参数化。在标准的随机梯度下降（SGD）假设下，即平滑性、目标函数下有界以及具有有界方差的无偏梯度，我们建立了严格的收敛保证。两个变体在平均梯度范数上均实现了噪声自适应的收敛速率 $\widetilde{\mathcal{O}}(T^{-1/2}+σ^{1/2} T^{-1/4})$，其中 $σ$ 是噪声水平。特别地，在 $σ=0$ 的零噪声状态下，我们的界限在无需手动调整超参数的情况下，简化为近乎最优的确定性速率 $\widetilde{\mathcal{O}}(T^{-1/2})$。

## Abstract
The Exponential Moving Average (EMA) is a cornerstone of widely used optimizers such as Adam. However, existing theoretical analyses of Adam-style methods have notable limitations: their guarantees can remain suboptimal in the zero-noise regime, rely on restrictive boundedness conditions (e.g., bounded gradients or objective gaps), use constant or open-loop stepsizes, or require prior knowledge of Lipschitz constants. To overcome these bottlenecks, we introduce OptEMA and analyze two novel variants: OptEMA-M, which applies an adaptive, decreasing EMA coefficient to the first-order moment with a fixed second-order decay, and OptEMA-V, which swaps these roles. Crucially, OptEMA is closed-loop and Lipschitz-free in the sense that its effective stepsizes are trajectory-dependent and do not require the Lipschitz constant for parameterization. Under standard stochastic gradient descent (SGD) assumptions, namely smoothness, a lower-bounded objective, and unbiased gradients with bounded variance, we establish rigorous convergence guarantees. Both variants achieve a noise-adaptive convergence rate of $\widetilde{\mathcal{O}}(T^{-1/2}+σ^{1/2} T^{-1/4})$ for the average gradient norm, where $σ$ is the noise level. In particular, in the zero-noise regime where $σ=0$, our bounds reduce to the nearly optimal deterministic rate $\widetilde{\mathcal{O}}(T^{-1/2})$ without manual hyperparameter retuning.

---

## 论文详细总结（自动生成）

这是一份关于论文《OptEMA: Adaptive Exponential Moving Average for Stochastic Optimization with Zero-Noise Optimality》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：现有的 Adam 类优化器虽然在实践中非常成功，但在理论分析上存在四大瓶颈：
    1.  **零噪声非最优性**：在确定性（无噪声）环境下，其收敛速率通常只能达到 $O(T^{-1/4})$，而非理想的 $O(T^{-1/2})$。
    2.  **依赖严苛假设**：现有证明往往依赖于“全局有界梯度”或“有界目标函数间隙”，这在现代深度学习模型中难以成立。
    3.  **开环步长**：步长通常是预设的（开环），无法根据优化轨迹的局部几何特征动态调整。
    4.  **参数依赖**：需要预先知道 Lipschitz 平滑常数 $L$ 来进行参数化。
*   **研究动机**：设计一种既保留 Adam 风格的 EMA（指数移动平均）结构，又能实现闭环自适应、无需 Lipschitz 常数（Lipschitz-free），且在零噪声下具有最优收敛性的优化算法。

### 2. 论文提出的方法论
*   **核心思想**：将 EMA 机制重新设计为一种**闭环反馈控制器**。算法通过观察优化轨迹中的梯度信息，动态调整 EMA 系数和有效步长。
*   **关键技术细节**：
    *   **轨迹统计量**：引入 $\rho_t$（累积梯度平方和）和 $\tau_t$（历史最大梯度范数）作为反馈信号。
    *   **OptEMA-M 变体**：对一阶矩（动量）应用自适应递减的 EMA 系数 $\alpha_t$，而二阶矩系数固定。其步长包含一个稳定性项和一个能量控制项。
    *   **OptEMA-V 变体**：固定一阶矩系数，对二阶矩应用自适应 EMA 系数 $\beta_t$。
    *   **闭环步长 $\gamma_t$**：步长完全依赖于已观察到的梯度轨迹，不依赖于未知的 Lipschitz 常数 $L$。
*   **算法流程**：在每一步迭代中，计算随机梯度 $g_t$，更新轨迹统计量，根据统计量计算自适应的 EMA 权重（$\alpha_t$ 或 $\beta_t$），更新一阶和二阶矩估计，最后根据轨迹相关的闭环规则计算步长并更新参数。

### 3. 实验设计
*   **研究性质**：本论文是一篇**纯理论分析论文**，重点在于数学证明和收敛速率的推导。
*   **Benchmark 与对比**：
    *   **对比对象**：在理论层面对比了 STORM、STORM+、SuperADAM、AdaGrad-Norm 以及各种版本的 Adam。
    *   **对比维度**：平滑性假设（平均平滑 vs 个体平滑）、是否无需 Lipschitz 常数、是否需要有界梯度假设、收敛速率等。
*   **数据集/场景**：文中未展示具体的数值实验或真实数据集测试，其“实验”体现为对非凸优化问题的严格数学推演。

### 4. 资源与算力
*   **算力说明**：由于本文属于理论证明类论文，**文中未提及任何关于 GPU 型号、数量或训练时长的信息**。其成果主要体现为纸面上的逻辑证明和定理推导。

### 5. 实验数量与充分性
*   **充分性评价**：
    *   **理论充分性**：非常充分。作者在标准的 SGD 假设（下有界目标函数、无偏梯度、有界方差、平均平滑性）下，为两种算法变体提供了详尽的收敛性证明（见附录 20 页的证明过程）。
    *   **实证充分性**：不充分。论文完全缺乏经验性的数值实验来验证理论结果在实际深度学习任务（如图像分类或语言建模）中的表现。

### 6. 论文的主要结论与发现
*   **收敛速率**：证明了 OptEMA 在平均梯度范数上达到了 $\widetilde{\mathcal{O}}(T^{-1/2} + \sigma^{1/2}T^{-1/4})$ 的噪声自适应速率（其中 $\sigma$ 为噪声水平）。
*   **零噪声最优性**：当 $\sigma = 0$ 时，算法自动恢复到 $\widetilde{\mathcal{O}}(T^{-1/2})$ 的近乎最优确定性速率，无需手动调整超参数。
*   **理论突破**：在无需“有界梯度”假设的前提下，实现了 Adam 风格算法的闭环自适应性，并证明了其在非凸设定下的稳健性。

### 7. 优点
*   **理论完备性**：成功移除了 Adam 理论分析中长期存在的“有界梯度”假设，使其更符合实际。
*   **高度自适应**：实现了 Lipschitz-free 特性，降低了对超参数调优的依赖。
*   **统一框架**：通过 OptEMA-M 和 OptEMA-V 两个变体，对称地探讨了一阶和二阶矩自适应性的作用。

### 8. 不足与局限
*   **缺乏实证验证**：作为一种旨在改进 Adam 的算法，没有在任何实际的大规模机器学习任务上进行测试，难以评估其在真实场景（如 Transformer 训练）中的收敛速度和泛化能力。
*   **计算开销**：虽然 EMA 更新是高效的，但维护轨迹统计量（如 $\rho_t$ 和 $\tau_t$）以及计算复杂的闭环步长规则会带来微小的额外计算负担。
*   **收敛阶数限制**：在极高噪声环境下，其 $\sigma^{1/2}T^{-1/4}$ 的项在阶数上略逊于某些 STORM 类方法（$T^{-1/3}$），尽管后者通常需要更强的假设。

（完）
