---
title: "ConstraintBench: Benchmarking LLM Constraint Reasoning on Direct Optimization"
title_zh: ConstraintBench：评估大语言模型在直接优化中的约束推理能力
authors: "Joseph Tso, Preston Schmittou, Quan Huynh, Jibran Hutchins"
date: 2026-02-25
pdf: "https://arxiv.org/pdf/2602.22465v2"
tags: ["query:opt-lm"]
score: 9.0
evidence: 评估大模型在直接约束优化上的推理能力
tldr: 本研究提出了 ConstraintBench，这是一个评估大语言模型（LLM）直接解决约束优化问题能力的基准测试。该基准涵盖 10 个运筹学领域，通过自然语言描述场景并由 Gurobi 求解器验证。实验评估了六个前沿模型，发现 LLM 的主要瓶颈在于满足约束的可行性而非寻找最优解，且在不同领域表现差异巨大，揭示了模型在处理复杂逻辑约束时的局限性。
selection_source: fresh_fetch
motivation: 旨在探索大语言模型在不借助外部求解器的情况下，能否直接理解并解决复杂的约束优化决策问题。
method: 构建了包含 10 个运筹学领域的 ConstraintBench 基准，利用自然语言描述任务并使用 Gurobi 求解器作为验证基准。
result: "实验发现模型的可行性达成率最高仅为 65%，且在同时满足可行性与最优性方面的表现普遍低于 30.5%。"
conclusion: 可行性而非最优性是 LLM 直接解决优化问题的主要障碍，模型在处理特定约束和避免幻觉方面仍存在显著缺陷。
---

## 摘要
大语言模型正越来越多地应用于底层结构为约束优化的运营决策。现有的基准测试主要评估 LLM 是否能将优化问题建模为求解器代码，但留下了一个互补的问题：在不使用求解器的情况下，LLM 能否直接为完全指定的约束优化问题生成正确的解？我们推出了 ConstraintBench，这是一个用于评估 LLM 在 10 个运筹学领域直接约束优化能力的基准测试，所有标准答案均由 Gurobi 求解器验证。每个任务都提供了一个包含实体、约束和优化目标的自然语言场景；模型必须返回一个结构化解，由确定性验证器根据每个约束和求解器证明的最优解进行检查。我们在 200 个任务上评估了六个前沿模型，发现可行性（feasibility）而非最优性（optimality）是主要的瓶颈。表现最好的模型仅达到了 65.0% 的可行性，但可行解的平均目标值达到了 Gurobi 最优解的 89% 到 96%。在求解器参考值 0.1% 误差范围内的联合可行性和最优性方面，没有模型超过 30.5%。逐领域分析显示难度差异巨大，平均可行性从设施选址领域的 85.0% 到机组排班领域的 0.8% 不等。此外，系统性失败模式包括对持续时间约束的误解、实体幻觉，以及在设施选址和车辆路径问题中出现的可行性与最优性脱钩现象，即模型实现了高可行性但最优性为 0%。ConstraintBench 及其所有评估基础设施将公开发布。

## Abstract
Large language models are increasingly applied to operational decision-making where the underlying structure is constrained optimization. Existing benchmarks evaluate whether LLMs can formulate optimization problems as solver code, but leave open a complementary question. Can LLMs directly produce correct solutions to fully specified constrained optimization problems without access to a solver? We introduce ConstraintBench, a benchmark for evaluating LLMs on direct constrained optimization across 10 operations research domains, with all ground-truth solutions verified by the Gurobi solver. Each task presents a natural-language scenario with entities, constraints, and an optimization objective; the model must return a structured solution that a deterministic verifier checks against every constraint and the solver-proven optimum. We evaluate six frontier models on 200 tasks and find that feasibility, not optimality, is the primary bottleneck. The best model achieves only 65.0% feasibility, yet feasible solutions average 89 to 96% of the Gurobi-optimal objective. No model exceeds 30.5% on joint feasibility and optimality within 0.1% of the solver reference. Per-domain analysis shows large variation in difficulty, with average feasibility spanning from 85.0% in the facility location domain to 0.8% in the crew assignment domain. Further, systematic failure modes include duration constraint misunderstanding, entity hallucination, and a feasibility-optimality decoupling in facility location and vehicle routing where models achieve high feasibility but 0% optimality. ConstraintBench and all evaluation infrastructure will be publicly released.

---

## 论文详细总结（自动生成）

### 论文总结：ConstraintBench - 评估大语言模型在直接优化中的约束推理能力

#### 1. 论文的核心问题与整体含义（研究动机和背景）
论文探讨的核心问题是：**大语言模型（LLM）能否在不借助外部求解器（如 Gurobi）编写代码的情况下，直接通过推理解决复杂的约束优化问题？**
*   **背景：** 现有的基准测试（如 NL4Opt, IndustryOR）主要关注“建模能力”，即 LLM 将自然语言转化为求解器代码的能力。
*   **动机：** 这种“直接求解”设置测试的是模型底层的约束推理和组合搜索能力，而非代码生成能力。在无法访问求解器、低延迟需求或需要模型提供初始启发式解的场景中，这种能力至关重要。

#### 2. 论文提出的方法论：核心思想与关键技术
论文提出了 **ConstraintBench**，一个包含 10 个运筹学（OR）领域的基准测试。
*   **核心思想：** 利用 Gurobi 求解器生成并验证具有数学严谨性的“问题-答案”对，要求模型直接输出结构化的 JSON 决策方案。
*   **任务生成流程：**
    1.  **种子生成：** 基于行业背景、规模、紧急程度等 5 个维度生成数万个确定性种子。
    2.  **场景创建：** 使用 Agent 调用 Python 和 Gurobi 迭代生成具体的自然语言描述场景，确保问题既符合逻辑又是可解的。
    3.  **求解器验证：** 使用 Gurobi 求解并证明最优性，剔除无法在规定时间内找到最优解的任务。
*   **评估指标：**
    *   **可行性（Feasibility）：** 方案是否满足所有硬性约束。
    *   **目标质量（Objective Quality）：** 可行解相对于最优解的接近程度。
    *   **联合最优率（Joint Optimality）：** 同时满足可行性且目标值误差在 0.1% 以内的比例。

#### 3. 实验设计
*   **数据集/场景：** 涵盖 10 个领域（设施选址、生产组合、订单履行、车间调度、排班、投资组合、车辆路径、装箱、项目规划、机组分配），共 200 个验证过的任务。
*   **对比方法（评估模型）：** 选取了 6 个前沿模型：GPT-5.2-pro、GPT-5.2、Claude Opus 4.6、Claude Opus 4.5、o4-mini、Gemini 3 Pro Preview。
*   **实验设置：** 标准化 Prompt，单轮对话，要求结构化 JSON 输出，使用确定性验证器重新计算目标值（不信任模型自报的数值）。

#### 4. 资源与算力
*   **训练算力：** 论文未提及模型训练的算力，因为其目标是评估现有的闭源前沿模型。
*   **推理成本：** 论文提到评估成本较高，每个任务的推理费用在 0.50 美元到 3.00 美元之间（取决于模型的推理预算和 Token 消耗）。
*   **验证工具：** 使用 Gurobi Optimizer 11.0 进行基准答案生成和验证。

#### 5. 实验数量与充分性
*   **实验规模：** 总计 1,200 次模型评估（6 模型 × 200 任务）。
*   **充分性：** 实验在领域覆盖上较为广泛，且对 4,314 个约束违规案例进行了深入的分类分析。
*   **客观性：** 采用 Gurobi 提供的数学证明作为 Ground Truth，消除了人工标注可能存在的错误，评估过程高度自动化且客观。

#### 6. 主要结论与发现
*   **可行性是主要瓶颈：** 即使是最好的模型（GPT-5.2-pro），可行性也仅为 65.0%。模型往往能找到不错的解（目标质量 89-96%），但很难完全不违反任何约束。
*   **领域难度差异巨大：** 设施选址的平均可行性达 85.0%，而机组分配仅为 0.8%。
*   **可行性与最优性脱钩：** 在设施选址和车辆路径领域，模型虽然能生成可行方案，但最优性几乎为 0%，说明模型依赖简单启发式而非深度搜索。
*   **自报虚假：** 模型经常错误报告自己的目标值（Self-report discrepancy），甚至出现数万倍的单位换算错误。
*   **系统性失败模式：** 包括结构性误解（如低估任务时长）、实体幻觉（虚构不存在的箱子 ID）和特定约束推理失败。

#### 7. 优点
*   **高标准验证：** 采用商业级求解器 Gurobi 验证，确保了 Benchmark 的数据质量，避免了以往基准测试中常见的标注错误。
*   **细粒度诊断：** 不仅给出对错，还提供了约束级别的违规诊断，揭示了模型失败的具体原因。
*   **领域广泛：** 覆盖了从连续变量到离散组合优化的多种数学结构。

#### 8. 不足与局限
*   **样本量较小：** 每个领域仅 20 个任务，总计 200 个，对于细粒度的模型能力排名可能存在统计波动。
*   **单轮限制：** 实验仅测试了单轮推理，未探索多轮对话或 Agent 迭代修正对性能的提升。
*   **部署建议：** 论文承认直接求解目前并非生产环境的最优选（建模+求解器仍更可靠），该基准更多是科学探索。
*   **阈值敏感性：** 0.1% 的最优性阈值非常严格，虽然反映了数学严谨性，但也导致部分领域的得分极低。

（完）
