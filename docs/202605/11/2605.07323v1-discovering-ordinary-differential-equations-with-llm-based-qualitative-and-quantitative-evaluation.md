---
title: Discovering Ordinary Differential Equations with LLM-Based Qualitative and Quantitative Evaluation
title_zh: 基于大语言模型定性与定量评估的常微分方程发现
authors: "Sum Kyun Song, Bong Gyun Shin, Jae Yong Lee"
date: 2026-05-08
pdf: "https://arxiv.org/pdf/2605.07323v1"
tags: ["query:sr-alg"]
score: 10.0
evidence: 从观测数据中发现控制微分方程
tldr: 本研究针对从观测数据中发现控制微分方程的挑战，指出传统符号回归方法过度依赖定量指标而忽视物理合理性的问题。为此，提出了DoLQ方法，利用大语言模型（LLM）进行定性和定量双重评估。该方法采用多智能体架构，通过采样、参数优化及科学家智能体协同工作，利用LLM的领域知识引导搜索过程。实验证明，DoLQ在多维常微分方程基准测试中表现卓越，不仅提高了发现成功率，还能更准确地还原真实方程的符号项。
selection_source: fresh_fetch
motivation: 现有的符号回归方法主要依赖定量指标，缺乏对物理合理性等领域知识的定性评估。
method: 提出DoLQ框架，通过采样代理、参数优化代理和科学家代理（LLM）协同进行定性与定量评估，迭代引导方程搜索。
result: 在多维常微分方程基准测试中，DoLQ在成功率和符号项恢复准确度上均优于现有方法。
conclusion: 结合大语言模型的定性分析能力能显著提升科学发现中微分方程建模的准确性和物理一致性。
---

## 摘要
从观测数据中发现控制微分方程是科学机器学习中的一个基本挑战。现有的符号回归方法主要依赖于定量指标；然而，现实世界的微分方程建模还需要结合领域知识以确保物理上的合理性。为了填补这一空白，我们提出了 DoLQ，一种基于大语言模型（LLM）定性与定量评估的常微分方程发现方法。DoLQ 采用多智能体架构：采样智能体（Sampler Agent）提出动力系统候选方案，参数优化器（Parameter Optimizer）精细化方程以提高准确性，科学家智能体（Scientist Agent）则利用 LLM 进行定性与定量评估，并综合评估结果以迭代地引导搜索。在多维常微分方程基准测试上的实验表明，与现有方法相比，DoLQ 取得了更优越的性能，不仅获得了更高的成功率，而且能更准确地恢复出真实方程的正确符号项。我们的代码可在 https://github.com/Bon99yun/DoLQ 获取。

## Abstract
Discovering governing differential equations from observational data is a fundamental challenge in scientific machine learning. Existing symbolic regression approaches rely primarily on quantitative metrics; however, real-world differential equation modeling also requires incorporating domain knowledge to ensure physical plausibility. To address this gap, we propose DoLQ, a method for discovering ordinary differential equations with LLM-based qualitative and quantitative evaluation. DoLQ employs a multi-agent architecture: a Sampler Agent proposes dynamic system candidates, a Parameter Optimizer refines equations for accuracy, and a Scientist Agent leverages an LLM to conduct both qualitative and quantitative evaluations and synthesize their results to iteratively guide the search. Experiments on multi-dimensional ordinary differential equation benchmarks demonstrate that DoLQ achieves superior performance compared to existing methods, not only attaining higher success rates but also more accurately recovering the correct symbolic terms of ground truth equations. Our code is available at https://github.com/Bon99yun/DoLQ.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **DoLQ** 的新框架，旨在利用大语言模型（LLM）的语义推理能力来改进从观测数据中发现控制微分方程（ODE）的过程。以下是对该论文的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：传统的符号回归（SR）方法在发现动力系统方程时，主要依赖于数值拟合指标（如均方误差 MSE）。然而，仅凭低误差并不意味着发现了真实的物理规律（即“同差不同义”现象），往往会导致生成的方程缺乏物理可解释性或包含冗余项。
*   **研究动机**：在科学发现中，领域知识（如“空气阻力”、“饱和效应”）对于确保方程的物理合理性至关重要。作者希望将 LLM 的定性推理能力与传统的定量数值优化相结合，以更准确地识别复杂动力系统的控制方程。

### 2. 方法论：DoLQ 框架
DoLQ 采用多智能体协作架构，通过迭代循环不断精炼候选方程：
*   **采样智能体 (Sampler Agent)**：利用 LLM 根据系统描述（领域知识）和科学家智能体的反馈，提出候选的数学项（Term Skeletons）。它不仅生成公式，还提供物理辩护。
*   **参数优化器 (Parameter Optimizer)**：
    *   **混合优化策略**：首先使用**差分进化算法 (Differential Evolution)** 进行全局搜索以确定参数的大致范围，随后使用 **BFGS 算法** 进行局部精细化。
    *   **目标函数**：优化残差 MSE（Residual MSE），即预测导数与数值估计导数之间的差异，以降低计算成本并提高多维系统的稳定性。
*   **科学家智能体 (Scientist Agent)**：
    *   **定量评估**：通过**消融测试 (Ablation Test)** 评估每个项对误差降低的贡献，将其分类为 Good（显著降低误差）、Neutral 或 Bad（增加误差）。
    *   **定性评估**：利用 LLM 判断该项是否符合物理直觉和系统描述。
    *   **反馈合成**：综合两项评估结果，决定对每个项采取 `keep`（保留）、`hold`（观察）或 `remove`（移除）的操作，并更新“知识库”以引导下一轮采样。

### 3. 实验设计
*   **数据集/场景**：使用了 8 个常微分方程基准问题，涵盖生物（SIR 模型）、化学（CDIMA 反应、Gray-Scott 模型）、物理（滑翔机动力学、磁体相互作用）等领域。
*   **评估指标**：
    *   **NMSE (Normalized MSE)**：包括残差 NMSE 和积分 NMSE（轨迹预测准确度）。
    *   **ID-Ext (Extended In-Domain)**：测试模型在时间轴延伸后的外推预测能力。
    *   **Term Test**：验证发现的方程结构是否与真实方程（Ground Truth）的符号项一致。
*   **对比方法**：对比了四种主流的基于 LLM 的符号回归方法：**ICSR、LASR、LLM-SR** 和 **EDL**。

### 4. 资源与算力
*   **LLM 模型**：主要实验使用 **Gemini 2.5 Flash Lite**。在鲁棒性分析中也测试了 GPT-4o mini、DeepSeek-V3.2 和 Grok 4.1 Fast。
*   **硬件环境**：Intel Core i7-12700K CPU，94 GiB RAM。
*   **算力说明**：论文未提及使用 GPU 进行训练，因为核心逻辑依赖于 LLM API 调用和 CPU 端的数值优化。DoLQ 在总 Token 消耗和运行时间上通常低于 LLM-SR 等基准方法。

### 5. 实验数量与充分性
*   **实验规模**：在 8 个不同维度的系统上进行了测试，每个实验运行 100 次迭代。
*   **消融实验**：专门设计了“无科学家智能体”的对比实验，证明了定性评估能显著加快收敛速度。
*   **鲁棒性测试**：进行了噪声鲁棒性（不同 $\sigma$ 噪声水平）、初始条件敏感性以及描述信息准确性（错误描述对结果的影响）的多组实验。
*   **评价**：实验设计较为全面，不仅关注数值精度，还深入探讨了优化器选择、Token 效率和模型泛化性，具有较高的客观性和公平性。

### 6. 主要结论与发现
*   **性能领先**：DoLQ 在所有基准测试中的成功率（NMSE Test 和 Term Test）均显著高于对比方法。
*   **结构准确性**：相比于 LLM-SR 容易产生冗余项（Bloat），DoLQ 发现的方程更加简洁且更接近真实物理结构。
*   **定性评估的价值**：LLM 的物理推理能有效过滤掉虽然数值拟合好但物理上无意义的“伪项”，从而引导搜索过程避开局部最优。
*   **优化器协同**：混合优化（DE + BFGS）对于处理符号回归中复杂的损失景观至关重要。

### 7. 优点与亮点
*   **语义与数值融合**：成功将 LLM 从简单的“公式生成器”提升为具有“评审能力”的科学家角色，实现了定性与定量的闭环。
*   **多智能体架构**：分工明确，采样、优化与评估的解耦使得系统具有良好的扩展性和可解释性。
*   **高效性**：通过消融测试和反馈机制，减少了无效搜索，降低了 Token 消耗。

### 8. 不足与局限
*   **噪声敏感性**：由于依赖数值微分来计算导数，在处理高噪声数据时，性能会受到显著影响。
*   **推理偏见**：如果初始的系统描述（领域知识）存在严重错误，DoLQ 可能会过度信任该描述而无法收敛到正确的数值解。
*   **偏微分方程 (PDE) 扩展**：目前仅限于 ODE，由于 PDE 缺乏通用的数值求解器，将其扩展到时空动力系统仍具挑战。

（完）
