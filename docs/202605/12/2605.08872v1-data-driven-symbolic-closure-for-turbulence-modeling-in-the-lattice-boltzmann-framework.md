---
title: Data-driven Symbolic Closure for Turbulence Modeling in the Lattice Boltzmann Framework
title_zh: 格子玻尔兹曼框架下湍流建模的数据驱动符号闭合
authors: "Yujie Fu, Yihan Zhang, Wanru Deng, Yuanjun Dai"
date: 2026-05-09
pdf: "https://arxiv.org/pdf/2605.08872v1"
tags: ["query:sr-alg"]
score: 10.0
evidence: 利用物理符号优化 (Phi-SO) 从 DNS 数据集中发现显式解析闭合式
tldr: 本研究针对格子玻尔兹曼方法（LBM）中传统亚格子模型过度耗散及近壁面表现不佳的问题，提出了一种数据驱动的符号闭合建模方法。通过物理符号优化（Phi-SO）并结合量纲分析与张量不变量，从高保真DNS数据中自动发现显式解析表达式。该模型在泰勒-格林涡和顶盖驱动流中表现优异，且无需壁面修正即可泛化至湍流槽道流，显著提升了湍流模拟的精度与可解释性。
selection_source: fresh_fetch
motivation: 传统LBM亚格子模型在处理复杂边界时存在过度耗散和空间选择性不足的缺陷。
method: 采用物理符号优化（Phi-SO）技术，结合虚拟量纲分析和非线性张量不变量，从高保真DNS数据中提取显式解析闭合模型。
result: 新模型在捕捉动能耗散峰值和解析次级角涡方面优于标准Smagorinsky模型，并能直接泛化至壁面湍流。
conclusion: 符号回归为发现具有鲁棒性和可解释性的物理规律提供了有效途径，助力开发下一代智能流体动力学求解器。
---

## 摘要
格子玻尔兹曼方法（LBM）框架内的湍流建模长期以来依赖于传统的代数亚格子尺度（SGS）模型，这些模型通常存在过度耗散以及在固体边界附近缺乏空间选择性的问题。在这项工作中，我们利用物理符号优化（Phi-SO），从泰勒-格林涡（TGV）和顶盖驱动流（LDC）的高保真直接数值模拟（DNS）数据集中发现显式解析闭合式。我们方法的核心是整合虚拟量纲分析和非线性张量不变量，这一策略在符号搜索过程中直接强制执行物理标度律。所得模型对应变率和旋转率不变量均表现出高度的非线性依赖性。数值验证证实，该符号闭合在捕捉动能耗散率峰值和解析精细的次级角涡方面优于标准的 Smagorinsky 方法。此外，该模型在无需任何辅助壁面阻尼修正的情况下，对壁面约束湍流槽道流（Re_tau = 180）表现出鲁棒的零样本泛化能力。这项工作突显了符号回归在为下一代智能计算流体力学求解器揭示鲁棒且可解释的物理规律方面的潜力。

## Abstract
Turbulence modeling within the Lattice Boltzmann Method (LBM) framework has long relied on traditional algebraic sub-grid scale (SGS) models, which often suffer from over-dissipation and lack of spatial selectivity near solid boundaries. In this work, we utilize Physical Symbolic Optimization (Phi-SO) to discover explicit analytical closures from high-fidelity DNS datasets of Taylor-Green Vortex (TGV) and Lid-Driven Cavity (LDC) flows. Central to our methodology is the integration of virtual dimensional analysis and non-linear tensor invariants, a strategy that enforces physical scaling laws directly within the symbolic search process. The resulting model exhibits a highly non-linear dependency on both strain-rate and rotation-rate invariants. Numerical validations confirm that this symbolic closure outperforms the standard Smagorinsky approach in capturing kinetic energy dissipation rate peaks and resolving delicate secondary corner vortices. Furthermore, the model exhibits robust zero-shot generalization to wall-bounded turbulent channel flow (Re_tau = 180) without the aid of any supplemental wall-damping corrections. This work highlights the potential of symbolic regression to uncover robust, interpretable physical laws for the next generation of intelligent computational fluid dynamics solvers.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种在格子玻尔兹曼方法（LBM）框架下，利用数据驱动的符号回归技术构建湍流闭合模型的新方法。以下是对该论文的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：传统的格子玻尔兹曼大涡模拟（LBM-LES）主要依赖代数亚格子尺度（SGS）模型（如 Smagorinsky 模型）。这些模型在处理复杂边界时往往表现出**过度耗散**，且在近壁面区域缺乏空间选择性。
*   **研究背景**：虽然深度学习（神经网络）在湍流建模中取得了进展，但其“黑盒”特性导致缺乏物理可解释性，且难以集成到现有求解器中。
*   **研究目标**：利用符号回归（Symbolic Regression）从高保真数据中自动发现显式的、具有物理意义且易于实现的代数闭合公式，以替代传统的经验模型。

### 2. 方法论
*   **核心思想**：结合高保真直接数值模拟（DNS）数据与物理符号优化（Phi-SO）算法，搜索最优的有效松弛时间（$\tau_{eff}$）表达式。
*   **关键技术细节**：
    *   **数据生成与过滤**：通过 DNS 模拟泰勒-格林涡（TGV）和顶盖驱动流（LDC），利用高斯滤波器对分布函数进行空间过滤，提取目标变量——湍流松弛时间增量（$\tau_{turb}$）。
    *   **特征工程**：构造了一系列框架无关的张量不变量，包括应变率张量（$S$）和旋转率张量（$\Omega$）的基本及高阶不变量。
    *   **物理约束（虚拟量纲分析）**：在符号搜索过程中引入“虚拟量纲”，强制要求生成的公式符合物理标度律（如 $\tau \propto |S|$），从而大幅缩小搜索空间并提高物理一致性。
    *   **Phi-SO 算法**：使用循环神经网络（RNN）作为代理生成数学表达式树，通过强化学习（风险寻求策略梯度）优化表达式的准确性和简洁度。

### 3. 实验设计
*   **数据集/场景**：
    *   **训练场景**：泰勒-格林涡（TGV，$Re=800, 1600, 3000$）和三维顶盖驱动流（LDC，$Re=3200, 10000$）。
    *   **泛化测试场景**：壁面约束湍流槽道流（Turbulent Channel Flow，$Re_\tau = 180$）。
*   **Benchmark（基准）**：DNS 高保真数据（Ground Truth）。
*   **对比方法**：标准的 Smagorinsky 模型（$C_s = 0.1$）。

### 4. 资源与算力
*   **软件平台**：使用开源 LBM 代码 **OpenLB** 进行模拟。
*   **算力说明**：论文中**未明确说明**具体的 GPU 型号、数量或训练所需的总时长。但提到 LBM 本身具有高度的局部性，适合 GPU 加速计算。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了衰减同性湍流（TGV）、非均匀剪切流（LDC）和壁面约束流（Channel Flow）三种截然不同的流态。
*   **充分性评价**：实验设计较为充分。研究不仅在训练集内进行了验证（A posteriori validation），还进行了**零样本（Zero-shot）泛化测试**，这在数据驱动建模中是衡量模型鲁棒性的关键指标。对比了动能耗散率、平均速度剖面、脉动速度等多个统计量，结果具有客观性。

### 6. 主要结论与发现
*   **发现新模型**：通过符号回归得到了一个高度非线性的代数公式，该公式同时依赖于应变率和旋转率的不变量。
*   **性能优异**：在 TGV 模拟中，新模型能更准确地捕捉动能耗散率的峰值；在 LDC 模拟中，成功解析出了 Smagorinsky 模型无法捕捉到的微小次级角涡（Moffatt eddies）。
*   **强泛化性**：在未参与训练的槽道流测试中，新模型在无需任何壁面阻尼函数的情况下，准确还原了对数律剖面，表现优于传统模型。

### 7. 优点
*   **可解释性强**：生成的模型是显式的代数方程，物理意义明确，且易于直接写入现有 CFD 代码。
*   **物理启发式搜索**：通过虚拟量纲分析和张量不变量约束，确保了数学形式的物理合理性。
*   **无需经验修正**：模型表现出内在的近壁面选择性，避免了传统模型对壁面函数或经验常数的依赖。

### 8. 不足与局限
*   **理论推导缺失**：虽然公式在经验上表现良好，但其中某些分数阶指数和系数的严格物理来源尚不清楚。
*   **数值稳定性风险**：公式中存在项的比例关系（如 $II_\Omega/II_S$），在应变率极低（分母趋于零）的区域可能存在数值不稳定性，需进一步正则化。
*   **对比范围有限**：主要对比了 Smagorinsky 模型，若能与 WALE 或 Vreman 等更先进的代数模型进行对比，将更具说服力。

（完）
