---
title: Discovery of Nonlinear Dynamics with Automated Basis Function Generation
title_zh: 结合自动基函数生成的非线性动力学发现
authors: "Mohammad Amin Basiri, Charles Nicholson"
date: 2026-05-10
pdf: "https://arxiv.org/pdf/2605.09696v1"
tags: ["query:sr-alg"]
score: 10.0
evidence: 结合符号回归与稀疏识别的混合框架
tldr: "本研究提出AutoSINDy框架，解决传统SINDy依赖预设基函数及符号回归易受噪声影响的问题。该方法通过符号回归自动生成候选函数，经去共线性处理构建精简库，再利用SINDy识别稀疏方程。实验显示其真值恢复率达92.8%，在抗噪性、预测精度和模型简洁度上均优于现有方法，实现了动力系统建模的自动化。"
selection_source: fresh_fetch
motivation: 传统SINDy依赖预设基函数且符号回归易受噪声干扰，限制了对未知非线性动力系统的建模能力。
method: 提出AutoSINDy框架，通过符号回归自动生成候选函数，经去冗余处理后构建定制化库，再利用SINDy进行稀疏识别。
result: "在多个经典非线性系统中实现了92.8%的真值恢复率，且在预测精度和模型简洁度上优于现有方法。"
conclusion: 该方法有效结合了符号探索与稀疏优化的优势，为自动化发现复杂动力系统方程提供了稳健的解决方案。
---

## 摘要
从观测数据中发现控制方程仍然是科学建模中的一个基本挑战，特别是当底层数学结构未知时。传统的稀疏识别方法（如 SINDy）在发现简洁模型方面表现出色，但需要研究人员预先指定候选基函数，这一限制在关键项被遗漏或系统表现出非常规动力学时往往会导致模型失败。纯符号回归方法提供了无限的灵活性，但在噪声敏感性方面存在困难，且经常产生过于复杂、不稳定的方程。我们提出了 AutoSINDy，这是一种混合的“先发现后求解”框架，它结合了符号回归的探索能力与 SINDy 稳健的稀疏促进能力。我们的方法分为三个阶段：（1）基于 PySR 的符号回归从自助法（bootstrapped）数据块中发现候选函数形式；（2）策展流水线利用共线性分析对这些表达式进行分解、扩展和过滤，以构建一个极简且全面的库；（3）SINDy 从这个定制库中识别稀疏控制方程。在典型非线性系统上的广泛实验表明，即使在高观测噪声下，AutoSINDy 也能一致地恢复真实方程，在所有试验中实现了 92.8% 的真实方程恢复率。与使用丰富库的标准 SINDy 和独立符号回归相比，AutoSINDy 实现了更高的预测精度、对未见轨迹的卓越泛化能力以及显著更低的符号复杂度。

## Abstract
Discovering governing equations from observational data remains a fundamental challenge in scientific modeling, particularly when the underlying mathematical structure is unknown. Traditional sparse identification methods like SINDy excel at discovering parsimonious models but require researchers to specify candidate basis functions a priori, a limitation that often leads to model failure when critical terms are omitted or when systems exhibit unconventional dynamics. Purely symbolic regression approaches offer unlimited flexibility but struggle with noise sensitivity and frequently produce overly complex, unstable equations. We present AutoSINDy, a hybrid Discovery-then-Solve framework that combines the exploratory power of symbolic regression with the robust sparsity-promoting capabilities of SINDy. Our method operates in three stages: (1) PySR-based symbolic regression discovers candidate functional forms from bootstrapped data chunks; (2) a curation pipeline decomposes, expands, and filters these expressions using collinearity analysis to construct a minimal yet comprehensive library; and (3) SINDy identifies sparse governing equations from this custom-tailored library. Extensive experiments across canonical nonlinear systems demonstrate that AutoSINDy consistently recovers ground-truth equations even under high observational noise, achieving a ground-truth recovery rate of 92.8% across all trials. Compared with standard SINDy using enriched libraries and standalone symbolic regression, AutoSINDy achieves higher predictive accuracy, superior generalization to unseen trajectories, and substantially lower symbolic complexity.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **AutoSINDy** 的新型框架，旨在从噪声观测数据中自动发现非线性动力系统的控制方程。以下是对该论文的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：如何在**缺乏先验领域知识**（即不知道方程的具体数学形式）且存在**高观测噪声**的情况下，从时间序列数据中准确、简洁地提取控制方程。
*   **研究背景**：
    *   **SINDy（稀疏识别）**：虽然能生成简洁模型，但极度依赖预设的候选函数库。如果库中遗漏了关键项，模型就会失效；如果库太大，则会导致多重共线性和不稳定性。
    *   **符号回归（SR）**：虽然灵活，但计算开销大，对噪声极其敏感，且容易生成过于复杂、在数值积分时不稳定的“黑盒”表达式。
*   **整体含义**：AutoSINDy 通过结合符号回归的“探索能力”和 SINDy 的“稀疏优化能力”，解决了自动化建模中的特征工程瓶颈。

### 2. 方法论：AutoSINDy 三阶段框架
该方法采用“先发现后求解”（Discovery-then-Solve）的策略：
1.  **阶段 1：符号挖掘（Discover）**
    *   利用 **PySR** 引擎，在随机采样的短数据块（Chunks）上独立运行符号回归。
    *   **核心思想**：局部数据块能暴露特定的动力学特征，通过收集所有数据块生成的帕累托前沿（Pareto front）表达式，构建一个原始候选池。
2.  **阶段 2：库策展（Curate）**
    *   **符号分解与扩展**：使用 SymPy 将复杂的表达式分解为原子项（如 $x^2 \sin(y)$），并根据策略进行代数展开。
    *   **共线性剪枝**：引入“简洁性偏好”（Simplicity Bias），按复杂度从小到大排序，利用皮尔逊相关系数或方差膨胀因子（VIF）剔除冗余项，构建一个正交性好、紧凑的定制基函数库。
3.  **阶段 3：稀疏识别（Identify）**
    *   使用 **Ensemble-SINDy**。在多个自助法（Bootstrap）样本上运行稀疏回归（如 STLSQ 或 SR3）。
    *   **硬截断过滤**：只有在超过 80%（阈值 $\kappa$）的子模型中出现的项才会被保留，从而极大地增强了抗噪性。

### 3. 实验设计
*   **基准系统**：涵盖 6 个经典非线性系统，包括谐波振荡器、带阻尼摆、范德波尔振荡器、杜芬振荡器以及复杂的 3D 混沌系统（Complex Lorenz）。
*   **对比方法（Benchmarks）**：
    *   **Standard SINDy**：使用预设的丰富库（多项式 + 傅里叶项）。
    *   **Standard PySR**：直接对全量数据运行符号回归。
*   **评估指标**：导数预测 $R^2$、长时仿真稳定性（Simulation $R^2$）、方程恢复率（Recovery Rate）、符号复杂度（算子计数）及计算耗时。

### 4. 资源与算力
*   **算力说明**：文中**未明确提及**具体的 GPU 型号或 CPU 核心数。
*   **时间开销**：
    *   **发现阶段**：每个状态变量的平均耗时约为 10-30 秒（中位数约 24 秒）。
    *   **仿真阶段**：由于生成的方程极其简洁，其数值积分速度显著快于基准方法（通常在 2 秒以内）。

### 5. 实验数量与充分性
*   **实验规模**：总计进行了 **540 组独立实验**（6 个系统 × 6 种噪声水平 [0% 到 5%] × 5 个随机种子）。
*   **充分性与公平性**：
    *   实验涵盖了从线性到混沌、从多项式到超越函数的多种动力学行为。
    *   引入了“未见轨迹”验证，确保模型不是在拟合噪声而是捕捉物理本质。
    *   对比实验使用了相同的优化器参数，确保了比较的客观性。

### 6. 主要结论与发现
*   **高恢复率**：AutoSINDy 在所有试验中的真实方程恢复率达到 **92.8%**，远超 Standard SINDy（58.9%）和 PySR（28.3%）。
*   **极强的稳定性**：在长时仿真中，AutoSINDy 实现了 **0% 的崩溃率**（即没有出现数值发散），而基准方法在高噪声下频繁崩溃。
*   **模型简洁度**：生成的方程复杂度几乎与真实物理方程一致，有效避免了 Standard SINDy 常见的“库膨胀”问题（例如在范德波尔系统中，SINDy 生成了 175 个算子，而 AutoSINDy 仅用 12 个）。

### 7. 优点
*   **自动化特征工程**：无需手动设计基函数库，真正实现了“数据驱动”的科学发现。
*   **鲁棒性**：通过“局部挖掘 + 全局策展 + 集成过滤”的多重机制，在 5% 的高噪声下依然表现稳健。
*   **物理可解释性**：输出的是紧凑的闭式数学表达式，便于后续的解析分析和控制设计。

### 8. 不足与局限
*   **计算扩展性**：虽然在 2D/3D 系统上很快，但随着系统维度增加，符号回归阶段的计算量呈线性增长，处理超高维系统可能存在压力。
*   **混沌系统的长时预测**：对于混沌系统（如 Lorenz），虽然方程找对了，但由于混沌敏感性，长时轨迹模拟仍会偏离真值（这是动力系统固有的性质，而非方法缺陷）。
*   **偏微分方程（PDE）**：目前主要针对常微分方程（ODE），扩展到 PDE 还需要进一步开发空间局部挖掘机制。

（完）
