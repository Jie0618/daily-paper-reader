---
title: The finite expression method for turbulent dynamics with high-order moment recovery
title_zh: 具有高阶矩恢复能力的湍流动力学有限表达式方法
authors: "Xingjian Xu, Di Qi, Chunmei Wang"
date: 2026-05-11
pdf: "https://arxiv.org/pdf/2605.10687v1"
tags: ["query:sr-alg"]
score: 10.0
evidence: 符号回归用于发现湍流的主导动力学方程
tldr: 针对湍流动力系统中非线性交互和随机效应导致的高阶矩难以捕捉的问题，本文提出了一种结合符号回归与生成模型的两阶段数据驱动建模框架。第一阶段利用有限表达式方法（FEX）自动发现确定性动力学的闭式表达式；第二阶段引入生成模型学习残差随机分量，以修正模型误差并准确表征高阶统计特性。该方法在随机三元组模型上验证了其在恢复交互项及预测高达五阶统计矩方面的卓越性能。
selection_source: fresh_fetch
motivation: 湍流系统中的非线性交互和随机效应使得从数据中准确捕捉高阶统计矩变得极具挑战性。
method: 提出一个结合有限表达式方法（FEX）发现确定性动力学和生成模型学习残差随机性的两阶段建模框架。
result: 数值实验表明该框架能成功恢复动力学表达式，并精确预测高达五阶的统计矩。
conclusion: 该研究展示了将可解释的符号发现与数据驱动的随机建模相结合在处理复杂湍流系统方面的巨大潜力。
---

## 摘要
湍流动力系统的特征在于非线性相互作用和随机效应，这些效应产生了耦合的统计量（如非零高阶矩），而这些统计量很难从数据中准确捕捉。我们提出了一个两阶段数据驱动建模框架，该框架结合了符号回归与生成模型，以共同识别控制动力学并预测其关键统计量。在框架的第一阶段，采用有限表达式方法（FEX）来发现确定性动力学的闭式表达式，在无需预定义库的情况下恢复非线性相互作用项和外部强迫。在第二阶段，引入生成模型来学习残差随机分量，作为对第一阶段近似模型误差的精细修正，从而实现对高阶统计特性的准确表征。理论分析确立了符号估计量的一致性，并根据数据规模和数值离散化量化了估计误差。通过在多种状态下的随机三元组模型上进行的详细数值实验验证了模型性能，结果表明该框架成功恢复了相互作用项和强迫表达式，并准确预测了高达五阶的统计矩。这些结果突显了将可解释的符号发现与数据驱动的随机建模相结合，在处理复杂湍流系统方面的潜力。

## Abstract
Turbulent dynamical systems are characterized by nonlinear interactions and stochastic effects that generate coupled statistical quantities, such as non-zero higher-order moments, which are difficult to capture from data with accuracy. We propose a two-stage data-driven modeling framework that combines symbolic regression with generative models to jointly identify the governing dynamics and predict their key statistical quantities. In Stage I of the framework, the Finite Expression Method (FEX) is adopted to discover closed-form expressions of the deterministic dynamics, recovering nonlinear interaction terms and external forcing without predefined libraries. In Stage II, generative models are introduced to learn the residual stochastic components as a refined correction to the model error from the Stage I approximation, enabling accurate characterization of higher-order statistics. Theoretical analysis establishes the consistency of the symbolic estimator and quantifies the estimation error in terms of data size and numerical discretization. The model performance is verified through detailed numerical experiments on the stochastic triad models across multiple regimes, demonstrating that the framework successfully recovers interaction terms and forcing expressions, and accurately predicts statistical moments up to order five. These results highlight the potential of integrating interpretable symbolic discovery with data-driven stochastic modeling for complex turbulent systems.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种结合符号回归与生成模型的两阶段数据驱动框架，旨在解决湍流动力系统中高阶统计矩（High-order Moments）难以准确捕捉的难题。以下是对该论文的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：湍流系统具有强非线性、不稳定性及高维特征，其统计特性往往呈现非高斯分布。现有的数据驱动方法（如 SINDy）通常依赖预定义的函数库，且往往只能捕捉低阶统计量（均值、方差），难以准确恢复决定系统长期行为的高阶矩（如偏度、峰度等）。
*   **研究背景**：湍流中的非线性交互和外部随机强迫共同决定了能量的演化。准确建模这些系统不仅需要发现其确定性的控制方程，还需要精确刻画残差中的随机效应。

### 2. 方法论：两阶段建模框架
该论文提出了一个名为 **FEX-Generative** 的两阶段框架：
*   **第一阶段：有限表达式方法 (FEX)**
    *   **核心思想**：将控制方程的每一维分量表示为二叉树结构，通过强化学习（策略梯度）在算子库（如 $\sin, \exp, +, \times$ 等）中搜索最优的闭式表达式。
    *   **技术细节**：无需预定义基函数库，能够发现嵌套的复杂函数形式。通过最小化经验损失函数来识别确定性动力学（漂移项），并利用一阶优化算法微调系数，确保符合物理约束（如能量守恒）。
*   **第二阶段：随机残差修正（生成模型）**
    *   **核心思想**：利用生成模型学习第一阶段模型与观测数据之间的残差分布。
    *   **关键技术**：对比了三种生成模型：
        1.  **TFDM (无训练扩散模型)**：利用分数匹配（Score-matching）和随机微分方程（SDE）框架，无需显式训练即可采样。
        2.  **SRAN (随机残差激活网络)**：轻量级网络，通过正则化协方差匹配高斯噪声到残差的映射。
        3.  **VAE (变分自编码器)**：通过潜在变量框架捕捉随机结构。
*   **理论支撑**：论文证明了符号估计量的一致性，并给出了系数估计误差关于数据量和时间步长的收敛界限。

### 3. 实验设计
*   **实验场景**：采用**随机三元组模型 (Stochastic Triad Model)**，这是研究湍流能量交换的经典简化模型。
*   **测试状态 (Regimes)**：
    1.  能量均分状态 (Equipartition)
    2.  前向能量级联 (Forward Cascade)
    3.  双向能量级联 (Dual Cascade)
    4.  周期强迫级联 (Periodic Forcing)
    5.  随机强迫级联 (Random Forcing)
*   **Benchmark 与对比方法**：
    *   **基准方法**：Weak SINDy（弱形式稀疏识别）。
    *   **对比维度**：方程恢复准确度、系数误差、1-7 阶统计矩的预测精度、概率密度函数 (PDF) 匹配度。

### 4. 资源与算力
*   **算力说明**：论文提到在 Stage II 中使用了基于 GPU 的 **FAISS** 库进行近邻搜索以加速 TFDM 的局部分布表征。
*   **具体细节**：文中未明确给出具体的 GPU 型号（如 A100 或 RTX 4090）及总训练时长。但提到了超参数设置：如 FEX 候选池大小为 100，微调迭代 50,000 次；TFDM 迭代 2,000 次。

### 5. 实验数量与充分性
*   **实验规模**：
    *   涵盖了 5 种不同的物理状态，每种状态均进行了长时预测验证（训练 T=10，测试 T=20）。
    *   进行了**消融实验**：测试了不同噪声水平（0 到 2 倍标准差）和不同样本量（1,000 到 10,000 条轨迹）对系数恢复的影响。
*   **充分性评价**：实验设计较为充分且客观。通过对比不同生成模型（TFDM vs SRAN vs VAE）以及传统符号回归方法，验证了该框架在处理强随机性系统时的鲁棒性。

### 6. 主要结论与发现
*   **符号恢复精度高**：FEX 在所有状态下均能准确识别非线性交互项的结构，且系数误差保持在 $10^{-2}$ 到 $10^{-5}$ 之间。
*   **高阶矩恢复能力强**：相比于仅有第一阶段的模型，引入生成模型后，系统能够精确预测高达 **5 阶甚至 7 阶**的统计矩，准确刻画了非高斯分布的尾部特征。
*   **泛化性**：模型在训练域外（更长的时间跨度）仍能保持稳定的统计特性预测。
*   **模型选择**：SRAN 在某些状态下表现最接近真值，而 TFDM 在处理复杂的时变随机强迫时更具潜力。

### 7. 优点与亮点
*   **摆脱库依赖**：FEX 克服了 SINDy 必须预设基函数的局限，能够自动组合出复杂的非线性项。
*   **物理一致性**：通过微调过程，发现的系数能够很好地遵守能量守恒等物理约束。
*   **两阶段解耦**：将确定性结构发现与随机误差修正分离，既保证了模型的可解释性，又兼顾了随机模拟的准确性。

### 8. 不足与局限
*   **计算成本**：TFDM 的计算量随时间步长和数据量显著增加，在大规模高维系统中的扩展性可能受限。
*   **维度挑战**：虽然论文讨论了分量分解策略，但在极高维（如全尺度 3D Navier-Stokes）系统中的表现尚待验证。
*   **超参数敏感性**：Stage II 中生成模型的正则化参数（如 VAE 的 KL 散度权重）对最终高阶矩的恢复质量有较大影响，需要精细调优。

（完）
