---
title: "EnergyLens: Interpretable Closed-Form Energy Models for Multimodal LLM Inference Serving"
title_zh: EnergyLens：面向多模态大语言模型推理服务的可解释闭式能耗模型
authors: "Vittorio Palladino, Gianluca Palermo, Michael E. Papka, Zhiling Lan"
date: 2026-05-11
pdf: "https://arxiv.org/pdf/2605.10556v1"
tags: ["query:sr-alg"]
score: 9.0
evidence: 使用符号回归作为闭式模型的结构发现工具
tldr: EnergyLens 针对多模态大模型推理能耗优化问题，提出了一种基于符号回归的可解释闭式能耗模型。该模型仅含12个参数，能有效解耦并行策略及预填充与解码阶段的能耗贡献。相比于将延迟作为代理或依赖黑盒模型的方法，EnergyLens 在极少样本（50个）下即可实现高精度预测，并在多种硬件和模型架构上展现出极强的泛化与外推能力，为实现能耗最优的 LLM 部署提供了实用且透明的工具。
selection_source: fresh_fetch
motivation: 现有的推理优化方法将延迟误认为能耗代理或依赖数据饥渴的黑盒模型，难以在复杂的并行策略和异构硬件下提供准确且可解释的能耗预测。
method: 采用符号回归技术从少量分析数据中提取结构，构建了一个包含12个参数的闭式模型，实现了张量/流水线并行及预填充/解码阶段能耗的物理级解耦。
result: "在仅需50个样本的情况下，该模型实现了88.2%的配置选择准确率，显著优于传统分析基准，并能可靠地外推至未见的批处理大小和硬件平台。"
conclusion: EnergyLens 证明了简洁的可解释数学模型在处理复杂大模型推理能耗预测时的有效性，是实现跨平台能耗最优部署的高效方案。
---

## 摘要
随着大语言模型涵盖了稠密、混合专家和状态空间架构，并被部署在日益多样化的多模态工作负载下的异构加速器上，优化推理能耗已变得与优化延迟和吞吐量同样关键。现有方法要么将延迟视为能耗的代理指标，要么依赖于对数据需求量大的黑盒代理模型。在不同的并行策略下，这两种方法均告失败：在我们测试的超过 20% 的配置中，延迟最优值与能耗最优值发生了偏离，且黑盒代理模型需要数百个性能分析样本才能在不同模型家族和硬件之间进行泛化。我们提出了 EnergyLens，它利用符号回归作为性能分析数据的结构发现工具，推导出一个包含 12 个参数的单一闭式能耗模型，该模型以并行度、批处理大小和序列长度等系统属性表示。与黑盒代理模型不同，EnergyLens 解耦了张量并行和流水线并行的贡献，并分离了预填充与解码能耗，使其预测在物理上具有可解释性和可操作性。EnergyLens 仅需 50 次性能分析测量即可完成拟合，在多种评估场景中实现了 88.2% 的 Top-1 配置选择准确率，而最接近的先前分析基准仅为 60.9%；它以少 10 倍的性能分析样本达到了集成机器学习方法的预测精度，并能在无需结构修改的情况下可靠地外推至未见的批处理大小和硬件平台，使其成为实现能耗最优 LLM 部署的实用且可解释的工具。

## Abstract
As large language models span dense, mixture-of-experts, and state-space architectures and are deployed on heterogeneous accelerators under increasingly diverse multimodal workloads, optimising inference energy has become as critical as optimizing latency and throughput. Existing approaches either treat latency as an energy proxy or rely on data-hungry black-box surrogates. Both fail under varying parallelism strategies: latency and energy optima diverge in over 20% of configurations we tested, and black-box surrogates require hundreds of profiling samples to generalize across model families and hardware. We present EnergyLens, which uses symbolic regression as a structure-discovery tool over profiling data to derive a single twelve-parameter closed-form energy model expressed in terms of system properties such as degree of parallelism, batch size, and sequence length. Unlike black-box surrogates, EnergyLens decouples tensor and pipeline parallelism contributions and separates prefill from decode energy, making its predictions physically interpretable and actionable. Fitted from as few as 50 profiling measurements, EnergyLens achieves 88.2% Top-1 configuration selection accuracy across many evaluation scenarios compared to 60.9% for the closest prior analytical baseline, matches the predictive accuracy of ensemble ML methods with 10x fewer profiling samples, and extrapolates reliably to unseen batch sizes and hardware platforms without structural modification, making it a practical, interpretable tool for energy-optimal LLM deployment.

---

## 论文详细总结（自动生成）

这篇论文介绍了一个名为 **EnergyLens** 的框架，旨在为多模态大语言模型（LLM）推理服务提供可解释的、闭式（Closed-Form）能耗预测模型。以下是对该论文的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：在异构硬件和多模态负载下，如何准确且可解释地预测 LLM 推理的能耗。
*   **研究背景**：
    *   **延迟不等于能耗**：在超过 20% 的配置中，延迟最优和能耗最优的配置是偏离的。简单将延迟作为能耗代理会导致高达 79.8% 的能耗惩罚。
    *   **黑盒模型缺陷**：现有的机器学习代理模型（如随机森林）虽然准确，但属于“黑盒”，需要大量（数百个）性能分析样本，且难以跨硬件和模型泛化。
    *   **并行策略复杂性**：张量并行（TP）和流水线并行（PP）对 GPU 利用率和空闲功耗的影响截然不同，现有模型往往将其混为一谈。

### 2. 方法论
EnergyLens 的核心思想是利用**符号回归（Symbolic Regression）**从少量分析数据中发现能耗的数学结构，并将其固化为一个包含 **12 个参数** 的闭式公式。
*   **核心公式结构**：能耗 $E$ 被分解为三个部分：
    1.  **预填充阶段能耗（Prefill）**：与输入 Token 数、批处理大小（Batch Size）以及 TP/PP 程度相关。
    2.  **解码阶段能耗（Decode）**：与最大输出 Token 数、批处理大小以及 TP/PP 程度相关。
    3.  **基准开销（Baseline Overhead）**：捕捉硬件层面的静态功耗和并行引入的固定开销。
*   **关键技术细节**：
    *   **解耦并行贡献**：通过独立的指数参数分别捕捉 TP 和 PP 对能耗的非线性影响。
    *   **批处理饱和建模**：使用子线性项描述批处理如何摊销固定成本，避免了线性假设在饱和区的失效。
    *   **参数拟合**：使用 L-BFGS-B 优化算法，通过最小化平均绝对百分比误差（MAPE）来拟合这 12 个具有物理含义的标量参数。

### 3. 实验设计
*   **数据集与负载**：
    *   **文本**：WikiText-2。
    *   **多模态**：COCO-2017（图像+文本）、Video-MME（视频+文本）、MSR-VTT（纯视频）。
*   **模型覆盖**：
    *   稠密 Transformer（Mistral-7B, Nemotron-v2-9B）。
    *   混合专家模型（Qwen1.5-MoE）。
    *   状态空间模型（Mamba-130M）。
    *   多模态模型（Qwen2-VL, LLaVA-1.5）。
*   **对比基准（Benchmarks）**：
    *   **线性回归**：最简单的参数化基准。
    *   **随机森林/梯度提升树**：非参数化黑盒 ML 方法。
    *   **MAVER IQ**：最先进的基于延迟推导能耗的分析模型。

### 4. 资源与算力
论文在三种主流 GPU 供应商的平台上进行了广泛测试：
*   **NVIDIA A100**：4×40GB 节点，NVLink 3.0 互联。
*   **Intel Data Center GPU Max 1550 (Ponte Vecchio)**：6×128GB 节点，Xe Link 互联。
*   **AMD Instinct MI100**：2×192GB 节点，Infinity Fabric 互联。
*   **推理引擎**：统一使用 vLLM 框架进行性能分析。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 940 个总场景，涉及不同的并行度（TP/PP 1, 2, 4）、批处理大小（最高 16384）和量化方案（BF16, 4-bit, 8-bit）。
*   **充分性**：实验不仅评估了预测精度（MAPE），还评估了排序质量（Pairwise Accuracy）和 Top-1 配置选择准确率。
*   **客观性**：通过跨硬件、跨模型家族、跨模态的交叉验证，证明了模型的泛化能力。

### 6. 主要结论与发现
*   **高精度与高效率**：EnergyLens 仅需 **50 个样本** 即可达到 13.0% 的 MAPE，而随机森林需要 500 个样本才能达到类似精度（样本效率提升 10 倍）。
*   **配置选择优异**：Top-1 配置选择准确率达到 **88.2%**，远高于 MAVER IQ 的 60.9%。
*   **量化陷阱**：发现 4-bit 量化在低并行度下反而可能增加能耗，原因是反量化开销超过了存储节省。
*   **外推能力**：在训练集之外的大批处理场景下，EnergyLens 依然能保持 93% 以上的排序准确率。

### 7. 优点
*   **可解释性**：12 个参数均有明确物理含义（如功耗基数、并行效率指数），方便运维人员理解。
*   **跨平台通用性**：同一套公式结构适用于 NVIDIA、Intel 和 AMD，只需重新拟合少量参数。
*   **物理级解耦**：首次在闭式模型中成功分离了 TP 和 PP 对能耗的不同物理影响。

### 8. 不足与局限
*   **静态假设**：目前主要针对静态配置选择，尚未完全集成到 vLLM 的动态连续批处理（Continuous Batching）调度器中。
*   **交互项简化**：公式目前采用加法结构处理预填充和解码，对于某些极长上下文或 MoE 路由频率极高的极端情况，可能需要更复杂的乘法交互项。
*   **硬件特性覆盖**：虽然覆盖了三大厂商，但未涉及移动端 SoC 或专门的 AI 推理芯片（如 Groq 或 AWS Inferentia）。

（完）
