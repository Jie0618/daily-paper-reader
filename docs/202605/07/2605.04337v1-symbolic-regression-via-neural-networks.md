---
title: Symbolic Regression via Neural Networks
title_zh: 基于神经网络的符号回归
authors: "Nibodh Boddupalli, Timothy Matchen, Jeff Moehlis"
date: 2026-05-05
pdf: "https://arxiv.org/pdf/2605.04337v1"
tags: ["query:sr-alg"]
score: 10.0
evidence: 通过神经网络进行符号回归以识别控制方程的新方法
tldr: 针对动力系统控制方程识别中深度学习模型缺乏可解释性以及传统符号回归方法依赖先验知识的问题，本文提出了一种结合深度学习灵活性与符号解实用性的新方法。该方法利用深度神经网络直接从数据中生成控制方程的符号表达式。通过在多种经典动力系统上的实验，证明了该架构在保持高精度的同时，能够提供具有物理洞察力的解析表达式，克服了传统黑盒模型的局限性。
selection_source: fresh_fetch
motivation: 传统的深度学习模型在动力系统建模中缺乏可解释性，而现有的符号识别方法往往依赖预定义的基函数字典或易于过拟合。
method: 提出一种新型深度神经网络架构，能够直接从观测数据中学习并生成动力系统的符号化控制方程。
result: 该算法在多种经典动力系统上展现了极高的准确性，成功提取出了简洁且具有物理意义的数学表达式。
conclusion: 该研究成功将深度学习的拟合能力与符号回归的可解释性相结合，为复杂动力系统的自动化建模提供了有效工具。
---

## 摘要
识别动力系统的控制方程是数学、工程、生物等多个学科中备受关注的课题。机器学习——特别是深度学习——技术已经展示了其从数据中逼近动力学特性的能力，但传统深度学习的一个缺点是，除了给定输入的数值输出外，几乎无法洞察其底层的映射关系。这限制了它们在简单预测之外的分析效用。与此同时，存在许多基于固定基函数字典来识别模型的策略，但大多数策略要么需要对系统有一定的直觉或洞察力，要么容易出现过拟合或缺乏简洁性。在这里，我们提出了一种结合了深度学习方法的灵活性和准确性以及符号解实用性的新方法：一种能够生成控制方程符号表达式的深度神经网络。我们首先描述了模型的架构，然后展示了我们的算法在一系列经典动力系统中的准确性。

## Abstract
Identifying governing equations for a dynamical system is a topic of critical interest across an array of disciplines, from mathematics to engineering to biology. Machine learning -- specifically deep learning -- techniques have shown their capabilities in approximating dynamics from data, but a shortcoming of traditional deep learning is that there is little insight into the underlying mapping beyond its numerical output for a given input. This limits their utility in analysis beyond simple prediction. Simultaneously, a number of strategies exist which identify models based on a fixed dictionary of basis functions, but most either require some intuition or insight about the system, or are susceptible to overfitting or a lack of parsimony. Here we present a novel approach that combines the flexibility and accuracy of deep learning approaches with the utility of symbolic solutions: a deep neural network that generates a symbolic expression for the governing equations. We first describe the architecture for our model, then show the accuracy of our algorithm across a range of classical dynamical systems.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **SymANNTEx**（Symbolic, Artificial Neural Network-Trained Expressions）的新型符号回归方法，旨在通过神经网络架构从观测数据中自动发现动力系统的控制方程。

### 1. 核心问题与整体含义
*   **研究背景**：识别动力系统的控制方程（通常为常微分方程 ODEs）是科学和工程的核心。
*   **核心痛点**：
    *   **深度学习（Black Box）**：虽然预测精度高，但缺乏可解释性，无法给出解析表达式。
    *   **固定字典方法（如 SINDy）**：依赖于预先定义的基函数库，如果真实项不在字典中则失效。
    *   **遗传算法符号回归**：容易产生“表达式膨胀”（Bloat），导致结果过于复杂且难以优化。
*   **研究动机**：开发一种既具备神经网络的灵活性（生成式字典），又能产出简洁、可解释符号解的方法。

### 2. 方法论
*   **核心思想**：设计一种特殊的神经网络架构，其权重直接对应数学表达式的系数和指数，通过网络的深度实现函数的复合，通过宽度实现项的叠加。
*   **关键技术细节**：
    *   **层级结构**：由多个“堆栈（Stacks）”组成，每个堆栈包含多个“操作层（Operational Layers）”。
    *   **操作层子层**：
        1.  **线性组合**：实现变量的加权求和。
        2.  **多项式组合**：通过对输入取绝对值并以权重为幂次，实现非整数幂次项。
        3.  **简单乘积**：利用 Sigmoid 激活函数作为“开关”，选择性地将变量相乘。
        4.  **常用算子**：内置 `sin`、`exp`、`sgn` 等函数。
    *   **损失函数与正则化**：采用 **MAE（平均绝对误差）** 而非 MSE，并结合 **$L_{1/2}$ 正则化**（比 $L_1$ 更能诱导稀疏性）以及针对多项式阶数的自定义正则项。
    *   **模型筛选（AIC）**：训练后，使用不同容差对权重进行舍入，利用 **赤池信息量准则（AIC）** 在模型准确度与简洁度（参数数量）之间取得平衡，选出最优的解析表达式。

### 3. 实验设计
*   **实验场景**：涵盖了 7 种具有代表性的动力系统：
    *   **Takens-Bogdanov**（固定点与分岔特性）。
    *   **单摆（Simple Pendulum）**（包含三角函数项）。
    *   **Rössler & Lorenz 系统**（经典混沌系统）。
    *   **FitzHugh-Nagumo**（神经动力学，快慢时间尺度）。
    *   **化学动力学**（包含 Arrhenius 指数项）。
    *   **Chua 双涡卷**（包含非光滑绝对值项）。
*   **Benchmark 与对比**：
    *   **基准**：各系统的真实物理方程（Ground Truth）。
    *   **对比方法**：对比了使用传统 $L_1$ 正则化 + MSE 损失函数的训练效果，证明了本文提出的 MAE + $L_{1/2}$ 组合在稀疏性上的优越性。
*   **噪声设置**：在状态数据和导数数据中均加入了 1% 的相对噪声。

### 4. 资源与算力
*   **硬件设备**：实验在一台标准笔记本电脑上完成，配置为 **AMD Ryzen 4900HS 处理器** 和 **16GB RAM**。
*   **训练时长**：根据数据集大小和网络深度，从生成数据到获得最终方程的时间在 **几分钟到一小时** 之间。
*   **参数规模**：网络极其轻量化，可训练参数量仅在 **$O(10^2)$** 级别（例如 68 到 322 个参数）。

### 5. 实验数量与充分性
*   **实验规模**：针对 7 个不同类型的物理模型进行了详细测试。
*   **充分性**：
    *   涵盖了线性、多项式、三角函数、指数及非光滑函数。
    *   测试了不同噪声水平下的鲁棒性。
    *   进行了超参数（如学习率、堆栈数）的影响分析。
*   **客观性**：实验结果不仅给出了识别出的方程，还通过相图（Phase Portrait）和时间序列对比了识别模型与真实模型的长期动力学行为，评估标准较为全面。

### 6. 主要结论与发现
*   **准确性**：SymANNTEx 能够从噪声数据中精确识别出大多数系统的控制方程及其系数。
*   **简洁性**：通过 AIC 筛选，模型能自动剔除神经网络中的冗余项，回归到最简物理形式。
*   **泛化能力**：即使在算子库不包含目标函数（如 Chua 系统的分段线性项）时，算法也能利用现有算子（如 `sin`）给出高质量的近似。
*   **优化发现**：较高的学习率（如 0.01）和 $L_{1/2}$ 正则化对于跳出局部最优、获得稀疏解至关重要。

### 7. 优点
*   **生成式字典**：无需像 SINDy 那样预设庞大的字典，通过层级复合可以生成极其复杂的函数形式。
*   **计算高效**：参数量极小，对算力要求极低，适合在普通硬件上运行。
*   **端到端**：从原始数据直接输出人类可读的数学公式，弥合了深度学习与物理建模的鸿沟。

### 8. 不足与局限
*   **局部最优风险**：对于某些系统（如单摆），如果学习率设置不当，优化过程可能会陷入线性近似的局部极小值。
*   **算子依赖**：虽然是生成式，但最终结果仍受限于基础算子（sin, exp 等）的选择，若系统包含极特殊的超越函数，可能难以完美还原。
*   **高维扩展性**：文中实验主要集中在 2-3 维系统，对于更高维度的复杂耦合系统，其搜索空间的爆炸是否会影响收敛性尚待验证。

（完）
