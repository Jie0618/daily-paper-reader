Title: Average Attention Transformers and Arithmetic Circuits

URL Source: https://arxiv.org/pdf/2605.04683v1

Published Time: Thu, 07 May 2026 00:44:10 GMT

Number of Pages: 12

Markdown Content:
# Average Attention Transformers and Arithmetic Circuits 

Lena Ehrmuth 1 Laura Strieker 1

## Abstract 

We analyse the computational power of trans-former encoders as sequence-to-sequence func-tions on vectors. We show that average hard atten-tion can be used to simulate arithmetic circuits if they are given as an input to an encoder. The cir-cuit families that can be simulated this way have constant depth while using unbounded addition, binary multiplication and sign gates. The trans-formers we use have arithmetic circuits instead of feed-forward networks. With typical average attention the functions they compute are also com-puted by the same class of circuit families. Our results hold for transformers over the reals, ratio-nals and any ring in between the two. 

## 1. Introduction 

Transformers (Vaswani et al., 2017) have recently gained popularity in machine learning applications as well as the-oretical research. The analysis of their expressivity has mostly been focused on what decision problems can be solved by transformers. In that regard, Boolean circuits have been a promising model for comparison. For example, Hao et al. (2022) showed that leftmost hard attention trans-formers over Q can only recognize languages in AC 0, the class of functions computed by Boolean circuit families of constant depth and polynomial size with unbounded fan-in. Strobl (2023) showed that average hard attention transform-ers using floats can only recognize languages in logspace-uniform TC 0 and Merrill & Sabharwal (2023) showed the same result for soft attention transformers with logarithmic precision. There are also a number of descriptive charac-terizations comparing transformers to logics. Chiang et al. (2023) defined an extension of first-order logic with modular predicates on positions and counting quantifiers and showed that its definable languages are between the languages recog-nized by soft attention transformers with fixed and unlimited precision. Barcel ´o et al. (2024) showed that all languages      

> 1Institute for Theoretical Computer Science, Leibniz Uni-versity Hanover, Hanover, Germany . Correspondence to: Lena Ehrmuth <ehrmuth@thi.uni-hannover.de >, Laura Strieker
> <strieker@thi.uni-hannover.de >.
> Preprint. May 7, 2026.

definable in first-order logic with arbitrary unary predicates can be recognized by leftmost hard attention transformers and even when extending this logic with counting terms, the definable languages are recognized by average hard atten-tion transformers. Whereas Yang et al. (2024) showed that transformers using left- and rightmost attention with strict masking and no positional embedding recognize exactly the languages definable in linear temporal logic. All of those results are limited to decision problems over finite alphabets. We want to analyse the expressivity of transformers as functions from vector sequences to vector sequences and compare them to arithmetic circuits. These circuits take numbers as inputs and their gates compute functions like addition, multiplication and the sign func-tion. This allows us to reason precisely about the theoretical transformer model, which uses real numbers and also about practical implementations using rational numbers for ap-proximation. We adapt a model of generalized transformers by Hao et al. (2022) and use it to define a class of aver-age dot-product attention transformers which use arithmetic circuits instead of feed-forward networks. 

Main Results The main contribution of this paper is a cor-respondence between average attention circuit transformers and the function class FSAC 0

> R

[sign] . All functions com-puted by the transformers are in FSAC 0

> R

[sign] and all cir-cuits from an FSAC 0

> R

[sign] family can be simulated by a single such transformer. Our results give a new perspective on the expressivity of transformers and we think that the field of machine learning would benefit from advances in the theory of algebraic com-putation and vice versa. We would also like to emphasize that theoretical results have the potential to inspire practical development. 

Organization In the next section, we define arithmetic circuits and generalized transformers over semirings. In Section 3, we briefly describe an upper bound for the circuit complexity of transformers. In Section 4, we show how average attention circuit transformers can be used to sim-ulate circuits. This requires a rather intricate construction presented incrementally in multiple subsections. The full proofs of most of the theorems are deferred to the Appendix. 1

> arXiv:2605.04683v1 [cs.CC] 6 May 2026 Average Attention Transformers and Arithmetic Circuits

## 2. Preliminaries 

We define N = {0, 1, 2, . . . }. For a set S, we use S∗ for the Kleene closure of S. We use lowercase letters to denote numbers, overlines for vectors and uppercase letters for sequences of vectors. The length of a vector or sequence is denoted with |x| or |X|. For d ∈ N, the components of a vector x ∈ Sd are x1, . . . , x d and the vectors of a sequence 

X ∈ (Sd)∗ are X1, . . . , X |X|.

2.1. Algebra 

We will discuss arithmetic circuits and transformers using values from arbitrary semirings. 

Definition 2.1. A semiring is a set R equipped with binary functions + and × such that • (R, +) is a commutative monoid with identity 0,• (R, ×) is a monoid with identity 1,• 0 is absorbing with respect to × and • × distributes over +.A semiring is called commutative if (R, ×) is a commutative monoid. A ring is a semiring such that (R, +) is an abelian group. A field is a ring such that (R, ×) is an abelian group. We can perform addition and multiplication over any semir-ing. A ring also allows subtraction and over a field we can additionally use division. For example, N is a semiring but not a ring, Z is a ring but not a field and Q, R are fields. If we want to simulate the computation of one semiring within another, we need what we call an extension. 

Definition 2.2. Let R and T be semirings with functions 

+R, ×R and +T , ×T having identities 0R, 1R and 0T , 1T .Then a function f : R → T is a homomorphism if we get 

f (x +R y) = f (x) + T f (y),f (x ×R y) = f (x) ×T f (y),

and f (1 R) = 1 T

for all x, y ∈ R. We call T a semiring extension of R if there is an injective homomorphism from R to T . If T is also a ring/field, we call T a ring/field extension of R.For example, N is a semiring extension of N, Z is a ring extension of N and Q is a field extension of Z.

Definition 2.3. An ordered semiring is a semiring R with a partial order ≤ ⊆ R2 such that for all x, y, z ∈ R we get 

x ≤ y ⇒ x + z ≤ y + z and 

x ≤ y and 0 ≤ z ⇒ x × z ≤ y × z and z × x ≤ z × y. 

We call a ring extension of Z Z -ordered, if it preserves the usual order of Z, which means that for all x, y ∈ Z

f (x) ≤ f (y) ⇔ x ≤ y. 

We will need an order on our values to define hard attention. 

2.2. Arithmetic Circuits 

We compare transformers to the well established theoretical model of circuits. For a more thorough introduction to this topic, see Savage (1997) or Vollmer (1999). A circuit is defined by an acyclic graph labeled with functions over some set. In this paper, this will be a semiring with its operations + and ×.

Definition 2.4. A circuit basis is a pair (S, B ) where S is a set or domain and B is a set of functions f : Sk → S with 

k ∈ N and functions on sequences f : S∗ → S.

Definition 2.5. A circuit C over a basis (S, B ) with m

inputs and n outputs is a tuple C = ( V, E, α, β ) where • (V, E ) is a finite directed acyclic graph, the nodes of which are called the gates of C,• β : V → S ∪ B ∪ { x1, . . . , x m, y 1, . . . , y n} is a labeling function that assigns each element of 

{x1, . . . , x m, y 1, . . . , y n} exactly once and • α : E → N is a function such that for each node v ∈ V ,

α(Nin (v)) = {1, . . . , |Nin (v)|} where Nin (v) = {u |

(u, v ) ∈ E} is the in-neighbourhood of v.We call the in- and out-degree of gates their fan-in and fan-out. The function β determines the type of each gate and thereby imposes some conditions on the graph: • A gate v with β(v) ∈ S is called a constant gate and has fan-in 0.• A gate v with β(v) ∈ { x1, . . . , x m} is called an input gate and has fan-in 0.• A gate v with β(v) ∈ { y1, . . . , y n} is called an output gate, has fan-in 1 and fan-out 0.• A gate v with β(v) ∈ B is called a function gate and has no restrictions. The size of a circuit is the number of gates it has and the 

depth of a circuit is the length of the longest path from an input to an output gate. 

Definition 2.6. A circuit C = ( V, E, α, β ) over (S, B ) with 

m inputs and n outputs computes a function fC : Sm → Sn

defined as follows. Each gate v ∈ V computes a function 

fC,v : Sm → S inductively defined as 

x 7 →



c if β(v) = c ∈ Sxi if β(v) = xi

fC,u (x) if β(v) ∈ { y1, . . . , y n}

and Nin (v) = {u}

b(fC,u 1 (x), . . . . . . , f C,u k (x)) 

if β(v) = b ∈ B and 

Nin (v) = {u1, . . . , u k}

s.t. α(u1) < · · · < α (uk)

and fC (x) = ( fC,β −1(y1), . . . , f C,β −1(yn)).A single circuit takes an input vector whose dimension is equal to the number of input gates. To handle inputs se-quences of any length, we need a family of circuits. 2Average Attention Transformers and Arithmetic Circuits 

Definition 2.7. A circuit family C = ( Cn)n∈N over (S, B )

is a sequence of circuits over (S, B ) such that each circuit 

Cn has exactly n inputs. It computes a function 

fC : S∗ → S∗, x 7 → fC|x| (x).

We define FSizeDepth (S,B )(fs, f d) as the set of func-tions computed by circuit families over (S, B ) that have 

size( Cn) ∈ O (fs(n)) and depth( Cn) ∈ O (fd(n)) .If the basis is a semiring with addition and multiplication functions, the corresponding circuits are called arithmetic. We define three different complexity classes based on the fan-in of addition and multiplication gates. 

Definition 2.8. Let R be any semiring with operations 

+ and ×. We define +∗ : R∗ → R, x 7 → P|x| 

> i=1

xi and 

×∗ : R∗ → R, x 7 → Q|x| 

> i=1

xi. Then a bounded fan-in (arith-metic) circuit over R is a circuit over the basis (R, {+, ×} ),a semi-unbounded fan-in circuit over R uses the basis 

(R, {+∗, ×} ) and an unbounded fan-in circuit over R uses the basis (R, {+∗, ×∗}). We use FC IRC R for functions computed by an unbounded fan-in circuit over R. These always have fixed size input and output vectors. To analyse functions of sequences, we define complexity classes for the three kinds of circuit families. 

FNC 0 

> R

= FSizeDepth (R, {+,×} )(nO(1) , 1) 

FSAC 0 

> R

= FSizeDepth (R, {+∗,×} )(nO(1) , 1) 

FAC 0 

> R

= FSizeDepth (R, {+∗,×∗})(nO(1) , 1) 

All three classes restrict circuit families to polynomial size and constant depth, but we get FNC 0 

> R

⊊ FSAC 0 

> R

⊊ FAC 0

> R

.For example, (Pni=1 xi)n∈N ∈ FSAC 0 

> R

\ FNC 0 

> R

and 

(Qni=1 xi)n∈N ∈ FAC 0 

> R

\ FSAC 0

> R

, because using only bi-nary gates they require circuit families of logarithmic depth. Because these circuits can only compute functions where each component is defined by a polynomial, which are con-tinuous, while transformers can compute uncontinuous func-tions, we need to extend the circuit bases with other func-tions. For this purpose we use the following notation. 

Definition 2.9. Let (S, B ) and (S, A ) be circuit bases. Then 

FSizeDepth (S,B )(fs,f d)[ A] = FSizeDepth (S,A ∪B)(fs,f d).For a semiring R, we use FC IRC R[A] for functions com-puted by circuits over (R, {+∗, ×∗} ∪ A).When explicitly listing the contents of the set A, the braces will be omitted, e.g. FNC 0

> R

[f, g ] instead of FNC 0

> R

[{f, g }].

2.3. Characteristic Functions 

It will later be useful to distinguish elements of a fixed finite set. This requires the following kind of function. 

Definition 2.10. Let A be a set and a ∈ A. Then the function χAa : A → { 0, 1} is defined as 

χAa (x) = 

(

1 if x = a and 

0 otherwise .

Such a function is not generally computable by an arithmetic circuit unless A = {0, 1}. One way to mitigate this fact is adding the zero function to a ring. It is defined as 

zero( x) = 

(

1 if x = 0 0 otherwise .

Lemma 2.11. Let R be a ring and r ∈ R. Then χRr ∈

FC IRC R[zero] .Proof. The following formula describes such a circuit: 

χRr (x) = zero( x − r) = zero( x + ( −1) × r)

If R is a field, we can instead use Lagrange polynomials to interpolate any finite set of values. 

Lemma 2.12. Let R be a field, A ⊆ R be finite and a ∈ A.Then there is a polynomial pAa : R → R in FC IRC R such that pAa (x) = χAa (x) for all x ∈ A.

The proof can be found in the Appendix. 

2.4. Generalized Transformers 

We adapt a definition by Hao et al. (2022) to describe transformer encoders using values from arbitrary semirings. Also, instead of using them for decision problems, we look at the functions computed by encoders with no output layer. This allows us to directly output real values and compare them to the values computed by circuit families. A transformer can have multiple layers with multiple atten-tion heads. Each head uses an attention function to compute attention scores between pairs of vectors and a pooling func-tion to combine all vectors based on those. The activation function of a layer takes in the results of its attention heads. Usually, the activation functions are computed by feedfor-ward networks, the attention functions are dot-products and the pooling functions apply softmax to the attention scores and compute a weighted sum of all vectors. 

Definition 2.13. A generalized transformer with K layers and H attention heads is a tuple (R, d, Σ, f in , f pos , f att 

> 1,1

,f att 

> 1,2

, . . . , f att  

> K,H

, f pool  

> 1,1

, . . . , f pool  

> K,H

, f act  

> 1

, . . . , f act  

> K

) where • R is the underlying semiring, • d ∈ N is the dimension, • Σ is the (possibly infinite) input alphabet, • f in : Σ → Rd is the input embedding, 3Average Attention Transformers and Arithmetic Circuits 

• f pos : N × N → Rd is the positional embedding, • f att  

> k,h

: Rd × Rd → R is the attention function for atten-tion head h in layer k,• f pool  

> k,h

: ( Rd)∗ × R∗ → Rd is the pooling function for attention head h in layer k and • f act  

> k

: ( Rd)H+1 → Rd is the activation function for layer k.Such a transformer computes a function T : Σ ∗ → (Rd)∗

defined by induction over k. First, the input sequence X =(x1, . . . , x n) ∈ Σ∗ is turned into a sequence of Rd-vectors using input and positional embedding: 

Y 0 =  f in (x1) + f pos (1 , n ), . . . , f in (xn) + f pos (n, n )

Then each layer k transforms the previous sequence Y k−1

into a sequence Y k of the same length in the following way. The attention function of each attention head h produces an 

n × n matrix of attention values: 

ak,h i,j = f att 

> k,h

(Y k−1 

> i

, Y k−1 

> j

) for 1 ≤ i, j ≤ n

The pooling function of each attention head h turnes it back into a vector sequence: 

Zk,h i = f pool 

> k,h

 Y k−1, (ak,h i, 1 , . . . , a k,h i,n ) for 1 ≤ i ≤ n

Finally, the activation function of the layer is applied to each vector in those sequences: 

Y ki = f act 

> k

 Y k−1 

> i

, Z k, 1 

> i

, . . . , Z k,H i

 for 1 ≤ i ≤ n

The output of the transformer is just the output of the last layer: T (X) = Y K .

2.5. Restricted Transformers 

We can define classes of transformers by restricting the choices for the functions they use. 

Definition 2.14. Let R be a semiring and A, B, C, D, E 

be sets of functions. Then GT R

 A, B, C, D, E  is the set of transformers using R as the underlying semiring, input embeddings from A, positional embeddings from B, acti-vation functions from C, attention functions from D and pooling functions from E and FT R

 A, B, C, D, E  is the set of functions computed by them. To describe the original transformer model by Vaswani et al. (2017), we define three sets of functions. DPA R is the set of attention functions computable via dot-products. WS R(f )

describes a weighted sum using the resulting values of a function f on the attention scores. And WP R(f ) is a varia-tion of WS R(f ) that uses multiplication instead of addition while also excluding zero values and will be used later. 

Definition 2.15. Let R be a semiring and f : R∗ → R∗.

DPA R =

(g : Rd × Rd → R, 

(x, y ) 7 → Ax · By 

d ∈ N and 

A, B ∈ Rd×d

)

WS R(f ) = 



g : ( Rd)∗ × R∗ → Rd,

(X, a ) 7 →

> |X|

X

> i=1

f (a)i × Xi

d ∈ N



WP R(f ) = 



g : ( Rd)∗ × R∗ → Rd,

(X, a ) 7 →

> |X|

K  

> i=1
> f(a)i̸=0

f (a)i × Xi d ∈ N



Here ⊙ is used for element-wise multiplication of vectors. The original model uses R = R and pooling functions from 

WS R(softmax) where 

softmax( a)i = eai

P|a| 

> j=1

eaj

.

This scheme is called soft attention. In this paper, we are only concerned with hard attention, because it makes it possible to attend only to specific vectors. There are three variants of hard attention. Leftmost (or unique) and right-most attention are defined by the functions 

left( a) = 

(

1 if i = min M

0 otherwise and right( a) = 

(

1 if i = max M

0 otherwise with M = {i | ai is maximal in {a1, . . . , a |a|}} .

Here we say that each vector attends to only one vector. Average attention is defined by the function 

avg( a)i =

( 1  

> |M|

if i ∈ M

0 otherwise .

Here each vector attends to |M | vectors. These attention schemes correspond to pooling functions from WS R(left) , WS R(right) and WS R(avg) respectively. They require a (partial) order on R to define maximal ele-ments while avg also relies on the existence of multiplicative inverses of N \ { 0}.

## 3. Simulating Transformers with Circuits 

The equations describing the computation of a transformer can be seen as a description of a circuit family. Their length is only dependent on the length n of the input sequence. If all functions used by a generalized transformer T are computable by circuits and we fix n, we can connect those together to construct a circuit for T . The circuits for all 

n form a circuit family. This requires the input alphabet 

Σ to be a subset of Rk for some k, but if it is finite, the complexity of a mapping f : Σ → Rk is essentially trivial. 4Average Attention Transformers and Arithmetic Circuits 

Because transformers have a fixed number of layers, the depth of the resulting circuits grows linearly with the depth of the circuits for the functions they use. The pooling func-tions are the only ones that require a circuit family, as all others are defined on fixed-size vectors. Therefore, if those families have constant depth, so will the family for the trans-former. The size also grows linearly with the size of the pooling function circuits, but it is at least quadratic in n, be-cause of the n × n matrix of attention values. For a detailed explanation of this construction, see Ehrmuth (2025). 

Theorem 3.1. Let R be any semiring, (R, B ) a circuit basis and P ⊆ { f : N2 → R}. Then FT R

 FC IRC R[B], P, 

FC IRC R[B], FC IRC R[B], FNC 0

> R

[B] ⊆ FNC 0

> R

[B]. This also holds if we replace FNC with FSAC or FAC .

There is no restriction on the positional embedding, because it does not actually need to be computed by the circuit family. Since it is only dependent on the number of inputs, its values can be stored in constant gates. 

3.1. Simulating Restricted Transformers 

Dot-products over any semiring R can be computed by arithmetic circuits, so DPA R ⊆ FC IRC R. For the hard attention functions, we get left , right , avg ∈ FSAC 0

> R

[sign] 

where 

sign( x) = 



1 if x > 00 if x = 0 

−1 if x < 0

.

So WS R(left) , WS R(right) , WS R(avg) ⊆ FSAC 0

> R

[sign] .The most common type of activation function is a feed-forward network using the rectified linear unit function which are also part of FC IRC R[sign] . All together, these restricted transformers compute functions in FSAC 0

> R

[sign] .

## 4. Simulating Circuits with Transformers 

Because transformers are length-preserving, they cannot directly compute functions that are not. To work around this difference, the input to the transformer must be long enough to fit the output of the function. We choose a similar approach to Barlag et al. (2024). They used graph neural networks on graphs of circuits to compute their output val-ues. Since transformers only work on vectors of numbers, we need some way to encode the circuits as such. We use one vector for each edge in the circuit, so the length of the encoding is quadratic with respect to the size of the circuit. We assume that the gates are numbered with indices from N. The types of the gates will be encoded with their incoming edges. Since the input and constant gates do not have any, we also add one vector for each of those. 

Definition 4.1. Let R be a semiring extension of N, C =(V, E, α, β ) an R-circuit with n inputs and u ∈ Rn. We fix 

x1 x2 x3 x4

+

×

y1       

> 14
> 1234
> 56
> 7
> 8
> Figure 1. An unbounded fan-in circuit with a numbering for the gates. The edges are numbered from left to right.
> Table 1. Encoding of the circuit in Figure 1 with input vector u.

xs xp xi xt xv

x1 1 0 0 tin u1

x2 2 0 0 tin u2

x3 3 0 0 tin u3

x4 4 0 0 tin u4

x5 5 0 0 tc 14

x6 6 1 1 t+ 0

x7 6 2 2 t+ 0

x8 6 3 3 t+ 0

x9 6 4 4 t+ 0

x10 7 5 1 t× 0

x11 7 6 2 t× 0

x12 8 7 1 tout 0

a set T = {tc, t in , t out , t +, t ×} ⊆ R of different constants for the gate types. For V = {v1, . . . , v |V |} we define the 

encoding EC (C, u ) ∈ (R5)∗ as follows: • For each constant gate vi ∈ { v | v ∈ V, β (v) ∈ R} we add the vector (i, 0, 0, t c, β (vi)) .• For each input gate vi ∈ { v | β(v) ∈ { x1, . . . , x n}} 

we add the vector (i, 0, 0, t in , u k) where β(vi) = xk.• For each edge (vi, v j ) ∈ E we add the vector 

(j, i, α (( vi, v j )) , t, 0) where t is either t+, t × or tout 

depending on whether vj is a +, × or output gate. To improve the readability, we denote the components 

(x1, x 2, x 3, x 4, x 5) of these vectors by (xs, x p, x i, x t, x v ),so the vector x for edge (u, v ) stores the index of the succes-sor v in xs, the index of the predecessor u in xp, α(( u, v )) 

in xi, the type of v in xt and the value assigned to v in xv .

Example 4.2 . The circuit in Figure 1 will be encoded into the vector sequence in Table 1. The addition gate has predeces-sors 1, 2, 3, 4 with each edge index matching its predecessor. The multiplication gate has two edges: The first one from gate 5 and the second one from gate 6. The output node has only one predecessor and, therefore, the encoding has only one vector with tout .5Average Attention Transformers and Arithmetic Circuits 

Definition 4.3. We say that a transformer T simulates acircuit C with n inputs encoded with EC , if for all x ∈ Rn,the values assigned to the incoming edges of output gates in 

T (EC (C, x )) are exactly fC (x).We can use attention heads to fetch the values of the prede-cessors of each gate and propagate them along the edges. This is a relatively simple construction if we leave some freedom for the attention and pooling functions, but it is still possible with a restriction to hard dot-product attention. The proofs for all statements in this section have been deferred to the appendix. 

4.1. Using Generalized Attention 

For the input embedding we use the identity function id , so the transformer uses the same dimension as the encoding. We define two attention functions f att  

> E

, f att  

> V

: R5 × R5 → R.The first one, 

f att  

> E

(x, y ) = 

(

1 if xp = ys

0 otherwise ,

lets x attend only to those vectors describing incoming edges of the xp-th gate. It will be used to transfer the values from the incoming to the outgoing edges of each node. The second attention function, 

f att  

> V

(x, y ) = 

(

1 if xs = ys

0 otherwise ,

lets the vectors of all incoming edges of the same gate attend only to each other. It will be used to compute the values of the gates from the values of its predecessors. If R is a ring, both functions are computed by an R-circuit using the zero function, since f att  

> E

(x, y ) = zero( xp − ys) and 

f att  

> V

(x, y ) = zero( xs − ys).Now we can define one pooling function for addition and one for multiplication to simulate the arithmetic gates. They will be used by different attention heads in the same layer and the activation function will choose which one to use based on the gate type. One possible choice for pooling functions is 

f pool  

> +

(X, a ) = 

> |X|

X

> i=1

ai × Xi and f pool  

> ×

(X, a ) = 

> |X|

K 

> i=1
> ai̸=0

ai × Xi.

Note that f pool  

> +

∈ WS R(id) and f pool  

> ×

∈ WP R(id) where 

id : R∗ → R∗ is the identity on sequences. To choose the right value based on the gate type, we need the functions 

χTt : T → { 0, 1} for t ∈ T = {tc, t in , t out , t +, t ×}. We define an activation function f act  

> V

: ( R5)3 → R5 as 

f act  

> V

(xin , x +, x ×)v =  χTtin (xin t ) + χTtc (xin t ) × xin v +

 χTtout (xin t ) + χTt+ (xin t ) × x+ 

> v

+

χTt× (xin t ) × x×

> v

and f act  

> V

(xin , x +, x ×)j = xin j for j ∈ { s, p, i, t }. This function retains the values for input and constant gates as well as all information about the circuit. Due to Lemma 2.11, we get f act  

> V

∈ FC IRC R[zero] for any ring R, but if R is a field, we can use polynomials from Lemma 2.12 instead. Because the attention function f att  

> E

must be used before this to move the predecessor values into xv , we add a second transformer layer. The activation function f act  

> E

: ( R5)3 →

R5 for this layer is defined as 

f act  

> E

(x, y, z )v =  χTtin (xt) + χTtc (xt) × xv +

 χTtout (xt) + χTt+ (xt) + χTt× (xt) × yv

and also retains the other values. For the pooling function we can use f pool  

> +

or f pool  

> ×

, because each vector of a correct encoding attends to at most one vector. When we use all these functions to define a transformer 



R, 5, R 5, id , 0, f att  

> E

, 0, f att  

> V

, f att  

> V

,f pool  

> +

, 0, f pool  

> +

, f pool  

> ×

, f act  

> E

, f act 

> V



with two layers, it is able to simulate circuits of depth 1

and by stacking more copies of these two layers, we can simulate circuits of higher depth. We write 0 for functions of unused attention heads, but f att  

> E

and f pool  

> +

could be used instead, because their values are ignored by f act  

> E

.

Theorem 4.4. Let R be a commutative ring exten-sion of Z. Then for each K ∈ N there is a trans-former in GT R

 {id }, {0}, FC IRC R[zero] , FC IRC R[zero] ,

WS R(id) ∪ WP R(id)  of dimension 5 with 2K layers that simulates any unbounded fan-in R-circuit of depth ≤ K

encoded with EC .

This also implies that, for each FAC 0

> R

-circuit family, there is a transformer that can simulate all of its circuits. 

4.2. Using Hard Attention 

A similar construction can be done using average dot-product attention, but there are two key differences. Firstly it attends only to the maximal attention values. Therefore we need two dot-products f att  

> E

, f att  

> V

such that for a fixed x the values f att  

> E

(x, y ) and f att  

> V

(x, y ) reach their maximum among all vectors y when xp = ys and xs = ys respectively. Sec-ondly average attention scales the result by a factor of 1

> k

where k is the number of such values. This can be coun-teracted by computing how many vectors were attended to 6Average Attention Transformers and Arithmetic Circuits 

and multiplying the result by that number. We will use the pooling functions 

f pool  

> +

(X, a ) = 

> |X|

X

> i=1

avg( a)i × Xi ∈ WS R(avg) and 

f pool  

> ×

(X, a ) = 

> |X|

K 

> i=1 avg( a)i̸=0

avg( a)i × Xi ∈ WP R(avg) .

They require the underlying semiring to have a (partial) order and we assume it is compatible with the usual order of the integers. Then we can define 

f att  

> E

(x, y ) = x2 

> p

− (ys − xp)2

= 2 xpys − y2 

> s

= 2 ys × xp − xone × yssq and 

f att  

> V

(x, y ) = x2 

> s

− (ys − xs)2

= 2 xsys − y2 

> s

= 2 xs × ys − xone × yssq .

This is implemented by adding two more dimensions to each vector and setting them to xone = 1 and xssq = x2

> s

.These values can be set either by the input embedding or by the activation function of an additional transformer layer. To count the predecessors of each gate, we can use the num-bering of the edges. When using average attention on a vec-tor with predecessors x1, . . . , x k having values {1, . . . , k }

in xi, the result y of the pooling function f pool  

> +

has 

yi =

> k

X

> j=1

jk = k(k + 1) 2k = k + 1 2 .

Therefore we can compute the number of predecessors as 

k = 2 × yi − 1. If this computation is also done in the first layer, we can multiply the intermediate values by k to correct for the scaling of average attention. Because the definition of average attention requires multi-plicative inverses of N \ { 0}, the following theorems only hold, when R is an extension of Q. This also allows for using polynomials from Lemma 2.12 for the χTt functions. The modified version of f act  

> E

is defined as 

f act  

> E

(x, y, z )v =  χTtin (xt) + χTtc (xt) × xv +

 χTtout (xt) + χTt+ (xt) + χTt× (xt) × (2 × zi − 1) × yv

and f act  

> E

(x, y, z )j = xj for j ∈ { s, p, i, t, n, one, ssq }. For 

f act  

> V

, we use the previous definition while also retaining the new components. Finally, we define the input embedding 

f in : R5 → R7 as f in (x) =  xs, . . . , x v , 1, x 2

> s

. The new two layer transformer is now 



R, 7, R 5, f in , 0, f att  

> E

, f att  

> V

, f att  

> V

, f att  

> V

,f pool  

> +

, f pool  

> +

, f pool  

> +

, f pool  

> ×

, f act  

> E

, f act 

> V



.

Table 2. Attention values f att  

> V

(x, y ) for Example 4.2 where x is given by column and y by row. The max of each column is bold. 

f att  

> V

x1 x2 x3 x4 x5 x6, . . . , x 9 x10 , x 11 x12 

x1 1 3 5 7 9 11 13 15 

x2 0 4 8 12 16 20 24 28 

x3 −3 3 9 15 21 27 33 39 

x4 −8 0 8 16 24 32 40 48 

x5 −15 −5 5 15 25 35 45 55 

x6 −24 −12 0 12 24 36 48 60 

x7 −24 −12 0 12 24 36 48 60 

x8 −24 −12 0 12 24 36 48 60 

x9 −24 −12 0 12 24 36 48 60 

x10 −35 −21 −7 7 21 35 49 63 

x11 −35 −21 −7 7 21 35 49 63 

x12 −48 −32 −16 0 16 32 48 64 

Theorem 4.5. Let R be a Z-ordered, commutative ring extension of Q. Then for each K ∈ N there is a transformer in GT R

 FC IRC R, {0}, FC IRC R, DPA R,

WS R(avg) ∪ WP R(avg)  of dimension 7 with 2K layers that simulates any unbounded fan-in R-circuit of depth ≤ K

encoded with EC .Example 4.6 . For the circuit from Example 4.2, the attention function f att  

> V

produces the values in Table 2. 

4.3. Simulating Bounded Fan-in 

If we only look at semi-unbounded fan-in circuits, the sec-ond pooling function can be eliminated from the transformer. To compute the values of binary multiplication gates, we have one attention head attend to the first edge of each gate and one to the second. The two values are then multiplied by the activation function. We define the attention function 

f att  

> B(n)

: R8 × R8 → R as 

f att 

> B(n)

(x, y ) = x2 

> s

− (ys − xs)2 + n2 − (yi − n)2

= 2 xs × ys − xone × yssq + 2 nx one × yi − xone × yisq .

This requires another dimension with xisq = x2 

> i

. When x

has an n-th predecessor, the function reaches its maximum value only when ys = xs and yi = n. We can now replace the attention head using f pool  

> ×

with two attention heads using 

f att  

> B(1)

and f att  

> B(2)

and redefine the second activation function 

f act  

> V

(xin , x +, x 1, x 2)v =

 χTtin (xin t ) + χTtc (xin t ) × xin v +

 χTtout (xin t ) + χTt+ (xin t ) × x+ 

> v

+

χTt× (xin t ) × (x1 

> v

× x2 

> v

).

Because the two attention heads attend only to one value, we need to exclude multiplication gates from the correction 7Average Attention Transformers and Arithmetic Circuits         

> Table 3. Attention values fatt
> B(2) (x, y )for Example 4.2 where xis given by column and yby row. The max of each column is bold.

f att  

> B(2)

x1 x2 x3 x4 x5 x6, . . . , x 9 x10 , x 11 x12 

x1 1 3 5 7 9 11 13 15 

x2 0 4 8 12 16 20 24 28 

x3 −3 3 9 15 21 27 33 39 

x4 −8 0 8 16 24 32 40 48 

x5 −15 −5 5 15 25 35 45 55 

x6 −21 −9 3 15 27 39 51 63 

x7 −20 −8 4 16 28 40 52 64 

x8 −21 −9 3 15 27 39 51 63 

x9 −24 −1 0 12 24 36 48 60 

x10 −32 −1 −4 10 24 38 52 66 

x11 −31 −1 −3 11 25 39 53 67 

x12 −45 −2 −13 3 19 35 51 67 

factor we previously applied in f act  

> E

. Therefore 

f act  

> E

(x, y, z, w )v =  χTtin (xt) + χTtc (xt) × xv +

 χTtout (xt) + χTt+ (xt) × (2 zi − 1) × yv

+ χTt× (xt) × yv .

We also add the new dimension to the activation functions and the input embedding f in (x) =  xs, . . . , x v , 1, x 2

> s

, x 2

> i

.The new two layer transformer is now 



R, 8, R 5, f in , 0, f att  

> E

, f att  

> V

, 0, f att  

> V

, f att 

> B(1)

, f att 

> B(2)

,f pool  

> +

, f pool  

> +

, 0, f pool  

> +

, f pool  

> +

, f pool  

> +

, f act  

> E

, f act 

> V



.

Theorem 4.7. Let R be a Z-ordered ring extension of 

Q. Then for each K ∈ N there is a transformer in GT R

 FC IRC R, {0}, FC IRC R, DPA R, WS R(avg)  of dimension 8 with 2K layers that simulates any semi-unbounded fan-in R-circuit of depth ≤ K with EC .Example 4.8 . For the circuit from Example 4.2, the attention function f att  

> B(2)

produces the values in Table 3. Here we see that our method of attention only works for gates that have a second predecessor, as for some others, we get multiple maximal scores. This is not an issue, because for other gates the values from the corresponding attention head are never used in the activation functions. If we only look at bounded fan-in circuits, we can apply the same construction to addition and output gates and also remove the attention head for predecessor counting. As this makes all heads attend to at most one relevant vector, we can even replace average with leftmost or rightmost attention. 

Theorem 4.9. Let R be a Z-ordered ring extension of 

Q. Then for each K ∈ N there is a transformer in 

GT R

 FC IRC R, {0}, FC IRC R, DPA R, WS R(left)  of di-mension 8 with 2K layers that simulates any bounded fan-in 

R-circuit of depth ≤ K encoded with EC .

4.4. Simulating Circuit Extensions 

Using the attention functions f att 

> B(3)

, f att 

> B(4)

, . . . we can fetch the values from an arbitrary number of predecessors. This works as long as the fan-in of the gates is bounded by the number of attention heads. So for any set B of functions 

f : Rk → R, we can simulate circuits over the basis (R, B )

if we allow activation functions from FC IRC R[B]. This requires an expansion of the encoding EC to include the gate types for B.

Theorem 4.10. Let R be a Z-ordered ring extension of 

Q, and B a finite set of functions f : Rk → R. Then for each K ∈ N there is a transformer in GT R

 FC IRC R,

{0}, FC IRC R[B], DPA R, WS R(avg)  with 2K layers that simulates any semi-unbounded fan-in R-circuit of depth 

≤ K encoded with EC , which also uses functions from B.

The avg function is non-continuous and we can use 

WS R(avg) to compute the sign function. By construct-ing an attention function that attends to one specific vector if xv < 0 and to two vectors if xv = 0 , we can use the difference in the resulting value to compute the zero func-tion. If we add another dimension containing only values 0

and 1, we can construct an attention head that distinguishes between positive and negative values. Together with zero 

this is sufficient to compute sign .

Theorem 4.11. Let R be a Z-ordered ring extension of 

Q. Then for each K ∈ N there is a transformer in 

GT R

 FC IRC R, {0}, FC IRC R, DPA R, WS R(avg)  of di-mension 9 with 2K layers that simulates any FSAC 0

> R

[sign] -circuit of depth ≤ K encoded with EC .

## 5. Conclusion 

In this paper, we established a correspondence between generalized average attention transformers using circuits as activation functions and the arithmetic circuit class 

FSAC 0

> R

[sign] . Over any semiring R, these transformers only compute functions in FSAC 0

> R

[sign] and if R is a Z-ordered ring extension of Q, there is a transformer for each 

FSAC 0

> R

[sign] -family that can simulate all of its circuits en-coded with EC . Unfortunately, this is not possible while only allowing feed-forward networks as activation functions, because those cannot perform multiplication of two inputs. This does not imply that any FSAC 0

> R

[sign] -function is also computed by a transformer. In fact, this would be impossi-ble, as circuit families are a non-uniform model of computa-tion. If we used transformers to generate longer sequences of vectors, similar to how they are used in natural language processing, we could also compute the encoding of a circuit from just the input vector. Our result would then lead to an equivalence between circuit families that are uniformly described by a transformer and the functions computed by such transformers. 8Average Attention Transformers and Arithmetic Circuits 

## Impact Statement 

This paper presents work whose goal is to advance the field of machine learning theory. There are many potential soci-etal consequences of our work, none of which we feel must be specifically highlighted here. 

## References 

Barcel ´o, P., Kozachinskiy, A., Lin, A. W., and Podolskii, V. V. Logical languages accepted by transformer encoders with hard attention. In The Twelfth International Confer-ence on Learning Representations, ICLR 2024, Vienna, Austria, May 7-11, 2024 . OpenReview.net, 2024. URL 

https://openreview.net/forum?id=gbrH Zq07mq .Barlag, T., Holzapfel, V., Strieker, L., Virtema, J., and Vollmer, H. Graph neural networks and arithmetic cir-cuits. In Globersons, A., Mackey, L., Belgrave, D., Fan, A., Paquet, U., Tomczak, J. M., and Zhang, C. (eds.), Advances in Neural Information Processing Sys-tems 38: Annual Conference on Neural Information Processing Systems 2024, NeurIPS 2024, Vancouver, BC, Canada, December 10 - 15, 2024 , 2024. URL 

http://papers.nips.cc/paper_files/p aper/2024/hash/09bf6a87e80d099cf17c6 347301c6120-Abstract-Conference.html .Chiang, D., Cholak, P., and Pillay, A. Tighter bounds on the expressivity of transformer encoders. In Krause, A., Brunskill, E., Cho, K., Engelhardt, B., Sabato, S., and Scarlett, J. (eds.), International Conference on Ma-chine Learning, ICML 2023, 23-29 July 2023, Honolulu, Hawaii, USA , volume 202 of Proceedings of Machine Learning Research , pp. 5544–5562. PMLR, 2023. URL 

https://proceedings.mlr.press/v202/c hiang23a.html .Ehrmuth, L. Expressivity of transformers. Master’s thesis, Leibniz University Hannover, 2025. URL https://ww w.thi.uni-hannover.de/fileadmin/thi/a bschlussarbeiten/2025/ma_ehrmuth.pdf .Hao, Y., Angluin, D., and Frank, R. Formal language recognition by hard attention transformers: Perspec-tives from circuit complexity. Transactions of the As-sociation for Computational Linguistics , 10:800–810, 2022. doi: 10.1162/tacl a 00490. URL https: //aclanthology.org/2022.tacl-1.46 .Merrill, W. and Sabharwal, A. The parallelism tradeoff: Limitations of log-precision transformers. Transactions of the Association for Computational Linguistics , 11:531– 545, 2023. doi: 10.1162/tacl a 00562. URL https: //aclanthology.org/2023.tacl-1.31 .Savage, J. E. Models of Computation: Exploring the Power of Computing . Addison-Wesley Longman Publishing Co., Inc., USA, 1st edition, 1997. ISBN 9780201895391. Strobl, L. Average-hard attention transformers are constant-depth uniform threshold circuits, 2023. URL https: //arxiv.org/abs/2308.03212 .Vaswani, A., Shazeer, N., Parmar, N., Uszkoreit, J., Jones, L., Gomez, A. N., Kaiser, L., and Polosukhin, I. Attention is all you need. In Guyon, I., von Luxburg, U., Bengio, S., Wallach, H. M., Fergus, R., Vishwanathan, S. V. N., and Garnett, R. (eds.), Advances in Neural Information Processing Systems 30: Annual Conference on Neural Information Processing Systems 2017, December 4-9, 2017, Long Beach, CA, USA , pp. 5998–6008, 2017. URL 

https://proceedings.neurips.cc/paper /2017/hash/3f5ee243547dee91fbd053c1c 4a845aa-Abstract.html .Vollmer, H. Introduction to Circuit Complexity: A Uni-form Approach . Springer Berlin Heidelberg, Berlin, Heidelberg, 1999. ISBN 978-3-662-03927-4. doi: 10.1007/978-3-662-03927-4. URL https://do i.org/10.1007/978-3-662-03927-4 .Yang, A., Chiang, D., and Angluin, D. Masked hard-attention transformers recognize exactly the star-free languages. In Globersons, A., Mackey, L., Belgrave, D., Fan, A., Paquet, U., Tomczak, J. M., and Zhang, C. (eds.), Advances in Neural Information Processing Systems 38: Annual Conference on Neural Informa-tion Processing Systems 2024, NeurIPS 2024, Vancou-ver, BC, Canada, December 10 - 15, 2024 , 2024. URL 

http://papers.nips.cc/paper_files/pap er/2024/hash/13d7f172259b11b230cc5da 8768abc5f-Abstract-Conference.html .9Average Attention Transformers and Arithmetic Circuits 

## Appendix 

Lemma 2.12. Let R be a field, A ⊆ R be finite and a ∈ A.Then there is a polynomial pAa : R → R in FC IRC R such that pAa (x) = χAa (x) for all x ∈ A.Proof. The Lagrange polynomial 

pAa (x) = Y

> b∈A\{ a}

x − ba − b

has the desired property. It can be computed by a circuit using constants from A∪{ (a−b)−1 | a, b ∈ A, a ̸ = b}.

Proofs of Section 4 Theorem 4.4. Let R be a commutative ring exten-sion of Z. Then for each K ∈ N there is a trans-former in GT R

 {id }, {0}, FC IRC R[zero] , FC IRC R[zero] ,

WS R(id) ∪ WP R(id)  of dimension 5 with 2K layers that simulates any unbounded fan-in R-circuit of depth ≤ K

encoded with EC .Proof. Let C be an unbounded fan-in arithmetic circuit over R of depth k with m inputs and u ∈ Rm. We assume that R is commutative to avoid values of multiplication gates being dependent on the order of the input vector. Let 

ℓ : ( R5)∗ → (R5)∗ be the function computed by the two layers described in Section 4.1. We construct a transformer by simply copying those layers k times and show that in its output, each vector x has xv = fC,g (u) where g is the gate with index xs. This is done by induction over path lengths in C.The input to the first layer is equal to EC (C, u ), because there is no positional embedding. Therefore, all input and constant gates already have the correct value. Assume that each vector x in ℓk(EC (C, u )) whose corre-sponding gate g has no path of length > k from an input or constant gate has xv = fC,g (u). Now let x be any vector in 

ℓk(EC (C, u )) whose corresponding gate g has no such path of length > k + 1 and let y be the vector corresponding to 

x in ℓk+1 (EC (C, u )) .• If g is an input or constant gate, we get y = x =

fC,g (u), because y = f act  

> V

(f act  

> E

(x, z 1, z 2), z 3, z 4) = x

for any z1, . . . , z 4 ∈ R5.• If g is an output or addition gate, let zE be the result of the attention head using f att  

> E

on x, x′ = f act  

> E

(x, z 1, 0) 

and z+ be the result of the attention head using f att 

> V

and f pool  

> +

on x′. Then f att  

> E

makes x attend to exactly those vectors w which corresponds to the predecessor 

p of g that has index xp. At least one such vector must exist, if the input was a circuit encoded with EC . From our assumption, we get x′ 

> v

= wv = fC,p (u). In the second layer, f att  

> V

makes x′ attend to all vectors that correspond to g including itself. Therefore, 

z+ 

> v

= X

> p∈Nin (g)

fC,p (u) = fC,g (u).

Then for any z×, y = f act  

> V

(x′, z +, z ×) has yv = z+ 

> v

=

fC,g (u) and yj = x′ 

> j

= xj for j ∈ { s, p, i, t }.• If g is a multiplication gate, we analogously get 

yv = Y

> p∈Nin (g)

fC,p (u).

So by induction we get xv = fC,g (u) for all x corre-sponding to g in ℓK (EC (C, u )) if depth( C) ≤ K. Since 

ℓK (EC (C, u )) is the output of a transformer which applies the two layers K times, this proves Theorem 4.4. 

Theorem 4.5. Let R be a Z-ordered, commutative ring extension of Q. Then for each K ∈ N there is a transformer in GT R

 FC IRC R, {0}, FC IRC R, DPA R,

WS R(avg) ∪ WP R(avg)  of dimension 7 with 2K layers that simulates any unbounded fan-in R-circuit of depth ≤ K

encoded with EC .Proof. This proof is very similar to the one of Theorem 4.4. For any x, the two new attention functions f att  

> E

(x, y ) and 

f att  

> V

(x, y ) reach their maximum values x2 

> p

and x2 

> s

only when the previous attention functions output 1. If the input is a circuit encoded with EC , such a y always exists for output, 

+ and × gates. So in those cases, the attention heads attend to exactly the same vectors as before and because of the scaling factor, the component zv of their output does not change. For input and constant gates, the output of the attention heads is ignored. The remainder of the proof is the same as for Theorem 4.4. 

Theorem 4.7. Let R be a Z-ordered ring extension of 

Q. Then for each K ∈ N there is a transformer in GT R

 FC IRC R, {0}, FC IRC R, DPA R, WS R(avg)  of dimension 8 with 2K layers that simulates any semi-unbounded fan-in R-circuit of depth ≤ K with EC .Proof. This proof is very similar to the one of Theorem 4.5. All steps for input, constant, output and + gates are the same. In the induction step, if g is a multiplication gate, f att  

> E

works in the same way as before, but the resulting value is not scaled by a factor in f act  

> E

. Then the attention functions f att 

> B(1)

and f att  

> B(2)

attend only to the vectors y1 and y2 corresponding to g that have y1 

> i

= 1 and y2 

> i

= 2 . If the input is a semi-unbounded fan-in circuit encoded with EC , all × gates must have exactly two predecessors, so these vectors must exist. For the resulting vector z = f act  

> V

(x′, x +, y 1, y 2) we get zv = y1 

> v

× y2 

> v

= fC,g (u).10 Average Attention Transformers and Arithmetic Circuits 

Theorem 4.9. Let R be a Z-ordered ring extension of 

Q. Then for each K ∈ N there is a transformer in 

GT R

 FC IRC R, {0}, FC IRC R, DPA R, WS R(left)  of di-mension 8 with 2K layers that simulates any bounded fan-in 

R-circuit of depth ≤ K encoded with EC .Proof. We redefine the activation functions: 

f act  

> V

(xin , x 1, x 2)v =

 χTtin (xin t ) + χTtc (xin t ) × xin v +

χTtout (xin t ) × x1 

> v

+

χTt+ (xin t ) × (x1 

> v

+ x2 

> v

)+ 

χTt× (xin t ) × (x1 

> v

× x2 

> v

)

f act  

> E

(x, y, z )v =  χTtin (xt) + χTtc (xt) × xv +

 χTtout (xt) + χTt+ (xt) + χTt× (xt) × yv

The new two layer transformer is now 



R, 8, R 5, f in , 0, f att  

> E

, 0, f att 

> B(1)

, f att 

> B(2)

,f pool  

> +

, 0, f pool  

> +

, f pool  

> +

, f act  

> E

, f act 

> V



.

We can also replace f pool  

> +

with the function 

f pool  

> left

(X, a ) = 

> |X|

X

> i=1

left( a)i × Xi ∈ WS R(left) .

The proof for f pool  

> +

follows the same steps for addition as the proof of Theorem 4.7 does for multiplication. In addition, the result from f att  

> B(2)

is ignored for output gates, but f att 

> B(1)

still works in the same way. Because f att  

> B(1)

and f att  

> B(2)

attend to exaclty one vector in all relevant cases, using f pool  

> left

makes no difference for the corresponding attention heads. The third head might attend to multiple vectors at a time, but, if the induction hypothesis holds, all of those share the same yv . Since yv is now the only component used in f act  

> E

, f pool  

> left

produces the same results as f pool  

> +

.

Theorem 4.10. Let R be a Z-ordered ring extension of 

Q, and B a finite set of functions f : Rk → R. Then for each K ∈ N there is a transformer in GT R

 FC IRC R,

{0}, FC IRC R[B], DPA R, WS R(avg)  with 2K layers that simulates any semi-unbounded fan-in R-circuit of depth 

≤ K encoded with EC , which also uses functions from B.Proof. Let m be the maximum arity of functions in B. We modify the transformer from Theorem 4.7 by adding at-tention heads for f att 

> B(3)

, . . . , f att  

> B(m)

using f pool  

> +

in the same way as for f att  

> B(1)

and f att 

> B(2)

. If the encoding uses constants 

TB = {tb | b ∈ B} for the new functions, we can de-fine T ′ = T ∪ TB and use the functions χT ′ 

> t

to define 

f act  

> V

: ( Rd)m+2 → Rd as 

f act  

> V

(xin , x +, x 1, . . . , x m)v =

 χT ′ 

> tin

(xin t ) + χT ′ 

> tc

(xin t ) × xin v +

 χT ′ 

> tout

(xin t ) + χT ′ 

> t+

(xin t ) × x+ 

> v

+

χT ′ 

> t×

(xin t ) × (x1 

> v

× x2 

> v

)+ 

X

> b∈B

χT ′ 

> tb

× b(x1 

> v

, . . . , x kb 

> v

)

where kb is the arity of b.

Proof of Theorem 4.11. To compute the zero function we use two attention functions f att 

> z+

, f att  

> z−

∈ DPA R defined as 

f att 

> z±

(x, y ) = ±xv × ys + 94 − (ys − 32 )2 + 14 − (yi − 12 )2

= ±xv × ys + 32 xone × ys − xone × yssq 

+ 12 xone × yi − xone × yisq .

Because every non-empty circuit has at least two nodes and every valid encoding of a circuit has only integer values in 

xs and xi, the term −(ys − 32 )2 reaches its maximum only when ys = 1 or ys = 2 and the term −(yi − 12 )2 reaches its maximum only when yi = 0 or yi = 1 . There are two cases for each of the two gates with indices 1 and 2:• If the gate is an input or constant gate, it has only one corresponding vector y with yi = 0 .• Otherwise, the gate may have multiple corresponding vectors, but it has exactly one with yi = 1 .In both cases, there are exactly two vectors for which the term −(ys − 32 )2 − (yi − 12 )2 is maximal. Let those be y

and z such that ys = 1 and zs = 2 . Then for any vector 

x, we get f att 

> z+

(x, z ) − f att 

> z+

(x, y ) = ( zs − ys) × xv = xv

and f att 

> z−

(x, z ) − f att 

> z−

(x, y ) = −xv . When these attention functions are used in an attention head with f pool  

> +

resulting in vectors u+ and u−, there are three cases for u+ 

> s

and u− 

> s

:• If xv = 0 , x attends only to y and z. Therefore, u+ 

> s

=

u− 

> s

= 1+2 2 = 32 .• If xv < 0, f att  

> z+

makes x attend only to y, because 

f att 

> z+

(x, y ) > f att 

> z+

(x, z ). Therefore, u+ 

> s

= 1 .• If xv > 0, f att  

> z−

makes x attend only to y, because 

f att 

> z−

(x, y ) > f att 

> z−

(x, z ). Therefore, u− 

> s

= 1 .11 Average Attention Transformers and Arithmetic Circuits 

Any other vector w cannot have an effect here, because we get −xv ×ws ≤ − xv ×ys for xv > 0 and xv ×ws ≤ xv ×ys

for xv < 0, as ws ≥ ys. We can now compute the zero 

function from u+ and u− as follows: 

zero( xv ) = 4 × (u+ 

> s

− 1) × (u− 

> s

− 1) 

To compute the sign function, we define a new dimension 

xb = χTtout (xt). Because the transformer does not need to compute anything if there are no output gates and if there is an output gate there must also be a non-output gate, without loss of generality there is a vector with xb = 1 and a vector with xb = 0 . We define another attention function 

f att 

> sign

(x, y ) = xv × yb

If xv = 0 , it attends uniformly to all vectors, if xv < 0, it attends only to vectors with yb = 0 and if xv > 0, it attends only to vectors with yb = 1 . Therefore, if an attention head using f att  

> sign

and f pool  

> +

produces the vector u from x, we get 

sign( xv ) = (1 − zero( xv )) × (2 × ub − 1) .

We add this term to f act  

> V

and define a new input embedding 

f in (x) =  xs, . . . , x v , 1, x 2

> s

, x 2 

> i

, χ Ttout (xt). The resulting two layer transformer is 



R, 9, R 5, f in , 0, f att  

> E

, f att  

> V

, 0, 0, 0, 0,f att  

> V

, f att 

> B(1)

, f att 

> B(2)

, f att 

> z+

, f att 

> z−

, f att 

> sign

, f pool  

> +

, f pool  

> +

, 0, 0, 0, 0,f pool  

> +

, f pool  

> +

, f pool  

> +

, f pool  

> +

, f pool  

> +

, f pool  

> +

, f act  

> E

, f act 

> V



.

As before, it can be concatenated with itself to simulate circuit families of constant depth which use sign gates. 12