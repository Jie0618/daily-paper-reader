---
title: Average Attention Transformers and Arithmetic Circuits
title_zh: 平均注意力 Transformer 与算术电路
authors: "Lena Ehrmuth, Laura Strieker"
date: 2026-05-06
pdf: "https://arxiv.org/pdf/2605.04683v1"
tags: ["query:math-ai"]
score: 8.0
evidence: Transformer编码器模拟用于符号函数的算术电路
tldr: 本研究深入分析了Transformer编码器作为向量序列到序列函数的计算能力。作者证明了利用平均硬注意力机制，Transformer能够模拟作为输入提供的常数深度算术电路，这些电路支持无界加法、二进制乘法和符号门。研究还发现，当Transformer使用算术电路替代传统前馈网络时，其计算的函数类与此类电路族一致。该结论在实数、有理数及相关环结构上均成立，为理解Transformer的理论表达极限提供了新视角。
selection_source: fresh_fetch
motivation: 探索Transformer编码器在处理向量序列时的理论计算能力及其与算术电路之间的内在联系。
method: 通过将算术电路作为输入，并利用平均硬注意力机制来模拟电路中的逻辑门运算。
result: 证明了Transformer可以模拟包含无界加法、二进制乘法和符号门的常数深度算术电路族。
conclusion: 揭示了特定结构的Transformer与算术电路计算类之间的等价性，且该结论具有广泛的数域适用性。
---

## 摘要
我们分析了 Transformer 编码器作为向量上序列到序列函数的计算能力。我们证明，如果将算术电路作为编码器的输入，平均硬注意力（average hard attention）可以用于模拟这些电路。通过这种方式可以模拟的电路族具有常数深度，同时使用无界加法、二元乘法和符号门。我们使用的 Transformer 采用算术电路而非前馈网络。在典型的平均注意力下，它们计算的函数也可以由同一类电路族计算。我们的结果适用于实数、有理数以及介于两者之间的任何环上的 Transformer。

## Abstract
We analyse the computational power of transformer encoders as sequence-to-sequence functions on vectors. We show that average hard attention can be used to simulate arithmetic circuits if they are given as an input to an encoder. The circuit families that can be simulated this way have constant depth while using unbounded addition, binary multiplication and sign gates. The transformers we use have arithmetic circuits instead of feed-forward networks. With typical average attention the functions they compute are also computed by the same class of circuit families. Our results hold for transformers over the reals, rationals and any ring in between the two.

---

## 论文详细总结（自动生成）

这篇论文《Average Attention Transformers and Arithmetic Circuits》（平均注意力 Transformer 与算术电路）是一篇关于 Transformer 表达能力的**纯理论研究**论文。以下是对该论文的结构化总结：

### 1. 论文的核心问题与整体含义
*   **研究动机**：以往对 Transformer 表达能力的研究多集中在“判定问题”（即识别某种语言），并常与布尔电路（如 $AC^0$, $TC^0$）进行比较。然而，Transformer 在实际应用中是作为处理实数/向量序列的函数。
*   **核心问题**：论文旨在分析 Transformer 编码器作为“向量序列到向量序列”函数的计算能力，并探讨其与**算术电路（Arithmetic Circuits）**之间的等价关系。
*   **整体含义**：通过将算术电路编码为输入向量序列，证明了特定配置的 Transformer 能够模拟复杂的算术运算逻辑，从而在代数计算理论和深度学习架构之间建立了桥梁。

### 2. 论文提出的方法论
*   **核心思想**：将 Transformer 视为一种能够执行并行算术运算的机器。通过精心设计的注意力机制和激活函数，让 Transformer 的每一层模拟算术电路中的一层逻辑门运算。
*   **关键技术细节**：
    *   **广义 Transformer 模型**：定义了一种在半环（Semiring）上的 Transformer，允许使用算术电路作为激活函数（替代传统的 FFN）。
    *   **电路编码（Encoding）**：提出一种方法，将算术电路的拓扑结构（门类型、输入、边、索引等）编码为一个向量序列 $E_C$。每个向量代表电路中的一条边或一个输入/常量门。
    *   **平均硬注意力（Average Hard Attention）**：利用点积注意力找到最大分值，并对这些位置进行平均。论文证明了这种机制可以用来在向量间传递信息（如从前驱门传递值到后继门）。
    *   **模拟流程**：
        1.  **第一层（注意力层）**：利用注意力机制根据电路编码找到每个门的前驱节点。
        2.  **第二层（激活层）**：利用算术激活函数执行加法、乘法或符号运算。
        3.  **循环/堆叠**：通过堆叠 $2K$ 层 Transformer，可以模拟深度为 $K$ 的算术电路。

### 3. 实验设计
*   **研究性质**：本论文属于**理论计算机科学/数学证明**，不包含实证实验。
*   **对比对象（Benchmark）**：论文的对比基准是计算复杂性理论中的电路类，特别是 $FSAC^0_R[sign]$（常数深度、多项式大小、具有无界加法、二元乘法和符号门的算术电路族）。
*   **场景**：证明在实数域 $\mathbb{R}$、有理数域 $\mathbb{Q}$ 或其间的环结构上，Transformer 与上述电路类的等价性。

### 4. 资源与算力
*   **算力消耗**：**无**。论文中未提及任何 GPU、显卡型号或训练时长，因为所有结论均通过数学推导和逻辑证明得出。

### 5. 实验数量与充分性
*   **实验充分性**：作为理论论文，其“充分性”体现在证明的严密性上。论文通过多个引理和定理（如定理 4.4, 4.5, 4.11 等）逐步构建了从简单算术电路到包含符号门的复杂电路的模拟过程。
*   **客观性**：证明过程遵循标准的计算复杂性分析框架，逻辑严密。

### 6. 论文的主要结论与发现
*   **等价性发现**：使用算术电路作为激活函数的平均注意力 Transformer，其计算能力等价于算术电路类 $FSAC^0_R[sign]$。
*   **模拟能力**：对于任何常数深度的算术电路族，都存在一个单一的 Transformer 编码器，只要将电路结构作为输入提供，该 Transformer 就能模拟该族中的所有电路。
*   **算子支持**：证明了平均注意力不仅能处理加法，还能通过特定的维度构造和注意力头组合来处理乘法和符号函数（sign function）。

### 7. 优点
*   **理论深度**：在向量函数层面（而非简单的布尔分类）给出了 Transformer 的精确代数刻画。
*   **通用性**：结果适用于多种数域（实数、有理数等），具有很强的数学普适性。
*   **创新视角**：将电路结构作为“输入数据”而非“固定架构”的思想，为理解 Transformer 处理结构化数据的能力提供了新解释。

### 8. 不足与局限
*   **激活函数的假设**：论文假设 Transformer 使用算术电路作为激活函数。虽然这在理论上可行，但与实际中使用的 ReLU 或 GELU 前馈网络（FFN）存在差异（FFN 难以直接实现两个输入的乘法）。
*   **硬注意力的局限**：研究基于硬注意力（Hard Attention），这是一种理想化的离散选择机制，与实际训练中使用的平滑 Softmax 注意力在连续性上有所不同。
*   **非统一性（Non-uniformity）**：电路族通常是非统一的计算模型，而 Transformer 权重是固定的。论文虽然通过输入编码解决了部分问题，但仍存在计算模型上的本质差异。
*   **应用限制**：该结论主要存在于理论层面，尚未直接转化为改进实际 Transformer 训练或架构的指导方案。

（完）
