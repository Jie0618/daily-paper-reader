---
title: Deep Variational Inference Symbolic Regression
title_zh: 深度变分推理符号回归
authors: "James Butterworth, Gevik Grigorian, Alejandro DiazDelaO"
date: 2026-05-01
pdf: "https://arxiv.org/pdf/2605.01067v1"
tags: ["query:sr-alg"]
score: 10.0
evidence: 深度符号回归的变分贝叶斯扩展，用于不确定性量化
tldr: 符号回归旨在发现可解释的数学方程，但传统深度学习方法往往只寻找单一最优解而忽略了不确定性。本文提出深度变分推理符号回归（DVISR），将变分贝叶斯框架引入深度符号回归（DSR）。通过将奖励函数替换为证据下界（ELBO）的被积函数，并扩展网络架构以输出常数分布，DVISR 实现了对表达式结构及其常数的后验分布推断，为处理噪声数据和量化模型不确定性提供了新途径。
selection_source: fresh_fetch
motivation: 现有的深度符号回归方法通常只能识别单一的最佳拟合表达式，无法在数据受限或存在噪声时量化模型的不确定性。
method: 提出 DVISR 框架，通过变分贝叶斯方法扩展 DSR，利用证据下界（ELBO）优化网络，并支持对表达式树及常数项的后验分布进行推断。
result: 实验表明 DVISR 在简单设置下能够成功恢复真实的后验分布，并能有效处理包含常数标记的复杂表达式空间。
conclusion: DVISR 为实现可扩展的贝叶斯符号回归迈出了重要一步，能够同时对符号模型结构和参数进行不确定性量化。
---

## 摘要
符号回归在不预先假设函数形式的情况下发现显式且可解释的方程。贝叶斯方法通过候选表达式上的概率分布来增强这一过程，从而在存在噪声和有限数据的情况下量化不确定性。深度符号回归（DSR）使用神经网络生成符号表达式，但其设计目的是识别单个最佳拟合表达式，而非推断模型上的后验分布。我们提出了深度变分推理符号回归（DVISR），这是 DSR 的一种变分贝叶斯扩展。DVISR 将原始奖励替换为证据下界（ELBO）的被积函数。它还扩展了网络架构，以输出表达式中常数的分布，从而实现对表达式树及其相关常数的后验推理。我们证明了 DVISR 在简单设置下（无论是否包含常数标记）都能恢复真实的后验分布，并研究了其性能随表达式空间增大而发生的变化。这些结果使 DVISR 成为迈向可扩展贝叶斯符号回归的一步，能够处理完整符号模型的不确定性。

## Abstract
Symbolic regression discovers explicit, interpretable equations without assuming a functional form in advance. A Bayesian approach strengthens this through probability distributions over candidate expressions, thus quantifying uncertainty in the presence of noisy and limited data. Deep Symbolic Regression (DSR) uses a neural network to generate symbolic expressions, but it is designed to identify a single best-fitting expression rather than infer a posterior distribution over models. We introduce Deep Variational Inference Symbolic Regression (DVISR), a variational Bayesian extension of DSR. DVISR replaces the original reward with the integrand of the evidence lower bound. It also extends the network architecture to output distributions over constants within expressions, enabling posterior inference over both expression trees and their associated constants. We show that DVISR can recover the true posterior in simple settings, both with and without constant tokens, and we examine how its performance changes as the size of the expression space increases. These results position DVISR as a step toward scalable Bayesian symbolic regression with uncertainty over full symbolic models.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **DVISR（Deep Variational Inference Symbolic Regression，深度变分推理符号回归）** 的新算法。以下是对该论文的结构化总结：

### 1. 论文的核心问题与整体含义
*   **研究动机**：传统的符号回归（SR）方法（如遗传算法或深度符号回归 DSR）通常旨在寻找单一的“最佳拟合”方程。然而，在面对噪声数据或有限样本时，这种方法无法量化模型的不确定性。
*   **核心问题**：如何将贝叶斯推理引入深度符号回归框架，从而不仅能找到方程，还能推导出候选方程及其内部常数的后验概率分布。
*   **整体含义**：论文通过变分推理（VI）扩展了 DSR，使其能够从单纯的优化工具转变为概率建模工具，为实现可扩展的贝叶斯符号回归迈出了关键一步。

### 2. 论文提出的方法论
*   **核心思想**：将 DSR 中的强化学习框架与变分推理相结合。通过最小化变分分布与真实后验分布之间的 KL 散度，使神经网络生成的表达式概率趋近于真实的后验概率。
*   **关键技术细节**：
    *   **奖励函数重构**：DVISR 将 DSR 原有的基于误差（如 NRMSE）的奖励函数替换为**证据下界（ELBO）的被积项**。公式为：$R(f) = \log L_\theta(f|X, y) + \log p(f) - \log q_\phi(f)$。
    *   **常数分布建模**：不同于 DSR 对常数进行点估计优化，DVISR 的循环神经网络（RNN）不仅输出标记（Token）的类别分布，还输出常数项的概率分布参数（如正态分布的均值和方差）。
    *   **训练算法**：使用 REINFORCE 策略梯度算法来最大化期望奖励（即最大化 ELBO），从而间接实现变分推理。
    *   **约束机制**：在采样过程中加入语法约束（如最大深度、禁止嵌套三角函数等），以缩小搜索空间。

### 3. 实验设计
*   **实验场景**：
    1.  **无常数实验**：在预定义的算子库中，尝试恢复 $y = x_0^2$、$y = x_0$ 和 $y = 0.5$ 的后验分布。
    2.  **含常数实验**：在包含常数标记（const）的情况下，恢复上述函数的后验分布。
    3.  **可扩展性实验**：通过改变最大表达式长度（1 到 12 个标记），观察算法性能的变化。
*   **Benchmark（基准）**：由于实验设置在极小且受限的表达式空间内，作者通过数值积分或全空间枚举计算出**真实的理论后验分布（True Posterior）**作为金标准。
*   **对比方法**：主要与理论真实后验进行对比，验证 DVISR 输出的分布 $q_\phi(f)$ 是否与其一致。

### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的 GPU 型号或训练时长。
*   **实现细节**：提到了使用 GRU（门控循环单元）作为 RNN 架构，隐藏层大小为 32 或 64，使用了 RMSprop 优化器。

### 5. 实验数量与充分性
*   **实验规模**：针对每个简单函数进行了 10 次独立运行，并报告了中位数和四分位距（IQR）。
*   **充分性评价**：
    *   **优点**：实验设计非常严谨，通过在小空间内对比理论真实值，确凿地证明了算法在数学上的正确性（即确实收敛到了真实后验）。
    *   **局限**：实验仅限于非常简单的目标函数和极小的搜索空间（最大 12 个标记），尚未在复杂的基准测试集（如 Nguyen 或 Feynman 数据集）上进行大规模验证。

### 6. 论文的主要结论与发现
*   **准确性**：DVISR 能够在高精度下重复且精确地收敛到真实的后验分布，无论是对于表达式树的结构还是其中的常数参数。
*   **常数处理**：成功实现了对表达式内常数的变分推理，这是对现有深度符号回归方法的重要补充。
*   **扩展性瓶颈**：随着表达式空间的增大（标记数量增加），KL 散度会上升，表明算法在处理超大规模搜索空间时仍面临挑战。

### 7. 优点
*   **理论完备性**：将变分推理完美融入策略梯度框架，为符号回归提供了坚实的统计学基础。
*   **不确定性量化**：能够输出多个高概率候选模型及其概率，有助于在安全关键领域（如医疗、国防）提供决策支持。
*   **端到端建模**：同时对结构（离散）和参数（连续）进行概率建模，比传统的两阶段方法（先找结构再优化参数）更具整体性。

### 8. 不足与局限
*   **可扩展性受限**：实验涉及的表达式非常短，在处理现实世界复杂方程时，巨大的离散搜索空间可能导致采样效率低下。
*   **计算成本**：虽然比 MCMC 快，但相比于非贝叶斯的 DSR，变分推理需要更多的采样和更精细的超参数调优。
*   **基准测试单一**：缺乏在含有高噪声、多变量或真实世界数据集上的表现评估。

（完）
