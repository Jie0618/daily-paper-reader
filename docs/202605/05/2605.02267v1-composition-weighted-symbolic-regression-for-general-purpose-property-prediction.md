---
title: Composition-Weighted Symbolic Regression for General-Purpose Property Prediction
title_zh: 用于通用性能预测的成分加权符号回归
authors: "Yang Huang, Jingrun Chen"
date: 2026-05-04
pdf: "https://arxiv.org/pdf/2605.02267v1"
tags: ["query:sr-alg"]
score: 10.0
evidence: 结合MCTS和遗传算法的符号回归用于材料属性预测
tldr: 本研究提出了一种成分加权符号回归框架，用于直接从化学成分预测材料性质。该方法无需预定义描述符，能同时学习解析函数形式和元素权重，并通过引入max/min算子处理物理约束。该框架在MatBench基准测试中表现出与黑盒模型相当的准确性，同时提供了可解释的解析表达式，为材料发现和筛选提供了一种可扩展且透明的路径。
selection_source: fresh_fetch
motivation: 旨在解决现有材料性质预测模型缺乏可解释性以及对预定义描述符依赖的问题。
method: 采用结合蒙特卡洛树搜索与遗传算法的混合框架，自动学习解析函数和任务相关的元素权重。
result: 在MatBench任务中达到了与最先进黑盒模型竞争的精度，并成功应用于III-V族半导体合金的性质预测。
conclusion: 该框架实现了高精度与强可解释性的平衡，是材料科学领域属性预测和规律发现的有力工具。
---

## 摘要
我们提出了一种成分加权符号回归框架，用于直接从化学成分对材料性质进行可解释性预测。该方法在无需预定义描述符的情况下，联合学习解析函数形式和任务相关的元素权重。通过引入最大值/最小值（max/min）算子，它自然地施加了诸如非负带隙和有界分类概率等约束，从而统一了回归和分类任务。通过结合基于梯度的细化和并行计算的混合蒙特卡洛树搜索-遗传规划算法，实现了高效搜索。在 MatBench 任务上的基准测试显示，该方法在产生显式解析表达式的同时，具有与最先进的黑盒模型相当的准确性。应用于 III-V 族半导体合金时，该模型产生了平滑的成分依赖趋势，且学习到的元素权重表现出具有化学意义的周期性行为。该框架为材料发现和性能筛选提供了一种可扩展且可解释的途径。

## Abstract
We introduce a composition-weighted symbolic regression framework for interpretable prediction of materials properties directly from chemical composition. The method jointly learns analytical functional forms and task-dependent elemental weightings without predefined descriptors. By incorporating max/min operators, it naturally enforces constraints such as non-negative band gaps and bounded classification probabilities, unifying regression and classification tasks. Efficient search is achieved through a hybrid Monte Carlo tree search--genetic programming algorithm with gradient-based refinement and parallel computation. Benchmarks on MatBench tasks show competitive accuracy relative to state-of-the-art black-box models while yielding explicit analytical expressions. Applied to III--V semiconductor alloys, the model produces smooth composition-dependent trends and learned elemental weights with chemically meaningful periodic behavior. This framework provides a scalable and interpretable route for materials discovery and property screening.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为“成分加权符号回归”（Composition-Weighted Symbolic Regression）的框架，旨在解决材料科学中属性预测的透明度与准确性平衡问题。以下是对该论文的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：如何在仅依赖化学成分（不依赖复杂的晶体结构信息）的前提下，实现既具有高预测精度又具备物理可解释性的材料属性预测？
*   **研究动机**：
    *   **结构 vs. 成分**：基于结构的模型（如图神经网络）精度高但依赖昂贵的结构表征；基于成分的模型速度快但多为“黑盒”神经网络，难以提取物理规律。
    *   **可解释性需求**：科学家需要显式的解析表达式来理解材料属性与元素组成之间的非线性关系。
    *   **物理约束**：现有的符号回归难以自然处理如“带隙非负”或“概率在0-1之间”等物理边界约束。

### 2. 核心方法论
*   **核心思想**：将材料属性 $P$ 表示为成分加权变量的解析函数。公式为 $P = F(x; \theta)$，其中 $x_k = \sum w_{k,i} c_i$。这里 $c_i$ 是元素占比，$w_{k,i}$ 是可学习的元素权重，$F$ 是通过符号回归发现的函数形式。
*   **关键技术细节**：
    *   **算子集扩展**：除了加减乘除、指数对数外，引入了 **max/min 算子**。这使得模型能自然捕捉分段函数行为和物理边界（如 `max(0, ...)` 确保非负性）。
    *   **混合搜索算法（MCTS-GP）**：结合了蒙特卡洛树搜索（MCTS）的定向探索能力和遗传规划（GP）的“跨阶段跳转”能力，提高了在巨大搜索空间中的效率。
    *   **梯度细化**：使用 L-BFGS 算法对连续参数（元素权重 $w$ 和函数系数 $\theta$）进行优化。
    *   **并行化设计**：在 GP 的变异/交叉阶段和 MCTS 的模拟阶段均实现了并行计算，加速了收敛。

### 3. 实验设计
*   **数据集**：使用了 **MatBench** 基准测试中的三个代表性任务：
    1.  `matbench_expt_gap`（实验带隙回归）
    2.  `matbench_expt_is_metal`（金属性分类）
    3.  `matbench_glass`（玻璃形成能力分类）
*   **对比方法（Baselines）**：
    *   **大型语言模型**：Darwin (7B参数), GPTChem。
    *   **深度学习模型**：CrabNet (注意力机制), MODNet。
    *   **传统机器学习**：随机森林 (RF-SCM/Magpie)。
    *   **解析基准**：简单的 ReLU 模型。
*   **应用场景**：针对 III-V 族半导体三元合金（如 InAs-InSb, AlP-GaP 等）的带隙随成分变化的趋势预测。

### 4. 资源与算力
*   **算力说明**：论文中**未明确指出**具体的 GPU 型号、数量或具体的训练时长。
*   **效率暗示**：作者提到通过并行化提高了效率，且模型参数量极小（约 $10^2$ 数量级），远低于深度学习模型的 $10^6$ 或 LLM 的 $10^9$，暗示其推理和训练对硬件的需求相对较低。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 3 个标准的 MatBench 任务和 1 个详细的合金系统案例研究。
*   **充分性评价**：
    *   **客观性**：使用了标准化的 MatBench 排行榜进行对比，确保了结果的公正性。
    *   **深度**：不仅对比了误差（MAE/ROC-AUC），还深入分析了学习到的元素权重是否符合元素周期律，以及函数形式的物理含义。
    *   **局限性**：在样本量极小（如数百个）的数据集上（如 MatBench steel），实验显示符号回归容易过拟合，这反映了实验设计的诚实性。

### 6. 主要结论与发现
*   **精度竞争力**：尽管是全显式的解析公式，其精度仍能与复杂的黑盒模型（如 CrabNet, MODNet）相媲美，且参数量减少了几个数量级。
*   **物理一致性**：在 III-V 族合金预测中，该模型产生的成分-属性曲线是**平滑且连续**的，而 Darwin 和 CrabNet 等模型在未见成分处常出现不连续的波动。
*   **规律发现**：模型自动学习到的元素权重在周期表上呈现出明显的周期性趋势（如卤素与过渡金属呈现截然不同的权重模式），证明模型捕捉到了潜在的化学本质。

### 7. 优点与亮点
*   **统一框架**：通过 max/min 算子将回归和分类任务统一在同一个符号回归框架下。
*   **自然约束**：无需特殊的输出层即可强制执行物理约束（如非负性）。
*   **高透明度**：输出的是紧凑的数学公式，便于材料科学家直接分析和二次开发。
*   **插值能力强**：在数据稀疏区域表现出比神经网络更好的平滑插值性能。

### 8. 不足与局限
*   **解释性挑战**：虽然公式是显式的，但当嵌套层数过多或算子过于复杂时，提取直观的物理意义仍然困难。
*   **局部最优风险**：混合搜索和梯度优化本质上是近似的，不能保证找到全局最优的函数形式。
*   **小样本过拟合**：在极小数据集上，符号回归的灵活性可能导致模型拟合噪声而非物理规律。
*   **复杂现象处理**：对于具有极强多尺度特性或尖锐相界行为的现象（如复杂的金属玻璃相图），目前的函数空间可能仍显不足。

（完）
