---
title: Discover Fast Power Allocation Solution for Multi-Target Tracking via AlphaEvolve Evolution
title_zh: 通过 AlphaEvolve 演化发现多目标跟踪的快速功率分配方案
authors: "Zhenkang Hou, Wenqiang Pu, Junkun Yan, Rui Zhou, Hongwei Liu"
date: 2026-05-03
pdf: "https://arxiv.org/pdf/2605.01794v1"
tags: ["query:sr-alg"]
score: 9.0
evidence: 大语言模型引导的进化搜索，自主发现闭式功率分配方案
tldr: 针对多目标跟踪中雷达资源分配计算复杂度高的问题，本文提出一种基于大语言模型引导的进化搜索（AlphaEvolve）方法。该方法通过演化出一种紧凑且可解释的闭式功率分配函数，将高维雷达状态映射为可行解。实验表明，该方案在保持近乎最优跟踪精度的同时，实现了比传统迭代求解器快三个数量级的速度，且具有极强的泛化能力。
selection_source: fresh_fetch
motivation: 传统的雷达资源分配算法依赖高复杂度的迭代优化，难以满足实时调度和低数据依赖的需求。
method: 利用LLM引导的AlphaEvolve进化搜索，自动发现一种基于物理特征的闭式评分函数，并将其转化为满足约束的功率分配方案。
result: "该闭式解在多种场景下仅有1.51%的性能损失，且计算速度提升了1000倍以上。"
conclusion: LLM引导的符号搜索为解决雷达资源管理及更广泛的工程优化问题提供了一种高效且可解释的新范式。
---

## 摘要
高效的雷达资源分配是一个基础性但具有计算挑战性的问题，因为最优解通常需要高复杂度的迭代优化。受实时调度、鲁棒泛化和低数据依赖性需求的启发，本文提出了一种新范式，利用大语言模型（LLM）引导的演化搜索（AlphaEvolve）自主发现多目标跟踪的闭式功率分配方案。该方法将高维雷达状态编码为受物理启发的特征，然后演化出一个紧凑且可解释的评分函数，并通过确定性的约束满足变换将其转化为可行的功率分配。大量实验表明，所发现的闭式方案实现了接近最优的跟踪精度（平均相对性能损失仅为 $1.51\\%$），在不同场景和目标数量下具有可靠的泛化能力，并且与传统的迭代求解器相比，速度提升了三个数量级以上。这些结果突显了 LLM 引导的符号搜索不仅在变革雷达资源管理方面，而且在更广泛的工程优化问题中都具有巨大潜力。

## Abstract
Efficient radar resource allocation is a fundamental yet computationally challenging problem, as optimal solutions typically require iterative optimization with high complexity. Motivated by the need for real-time scheduling, robust generalization, and low data dependency, this paper proposes a novel paradigm that leverages large language model (LLM)-guided evolutionary search (AlphaEvolve) to autonomously discover a closed-form power allocation solution for multi-target tracking. The approach encodes high-dimensional radar states into physically inspired features, then evolves a compact and interpretable scoring function, which is transformed to feasible power allocations via a deterministic constraint-satisfying transformation. Extensive experiments demonstrate that the discovered closed-form solution achieves near-optimal tracking accuracy (average relative performance loss of only $1.51\%$), reliable generalization across diverse scenarios and target counts, and over three orders of magnitude speedup compared to conventional iterative solvers. These results highlight the potential of LLM-guided symbolic search to revolutionize not only radar resource management but also broader classes of engineering optimization problems.

---

## 论文详细总结（自动生成）

这是一份关于论文《Discover Fast Power Allocation Solution for Multi-Target Tracking via AlphaEvolve Evolution》的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：在多目标跟踪（MTT）中，雷达功率分配通常被建模为复杂的凸优化问题。虽然迭代算法（如内点法 IPM）能求得最优解，但其计算复杂度高（涉及矩阵求逆），难以满足实时调度需求。
*   **研究动机**：
    *   **传统方法局限**：迭代法太慢；深度学习（DNN）方法虽快但属于“黑盒”，缺乏可解释性且在分布偏移时泛化性差。
    *   **新范式需求**：寻求一种既具有闭式解的计算效率（$O(N)$ 复杂度），又具备物理可解释性和强泛化能力的资源分配方案。
*   **整体含义**：本文利用大语言模型（LLM）引导的符号演化搜索（AlphaEvolve），自主发现了一个接近最优的闭式功率分配公式，实现了计算效率与跟踪精度的平衡。

### 2. 方法论
*   **核心思想**：将功率分配问题转化为物理驱动的符号搜索任务。通过提取物理特征，利用 LLM 引导进化算法在数学表达式空间中搜索最优评分函数。
*   **关键技术细节**：
    1.  **物理启发特征提取**：从高维雷达状态中提取 20 个特征，包括绝对需求因子（$D_i$）、基准边际收益（$M_{base,i}$）和悬崖边际收益（$M_{cliff,i}$）等。
    2.  **AlphaEvolve 搜索架构**：
        *   **初始化**：LLM 作为领域专家，以物理动机公式（如 $D_i^{2/3}$）为种子进行演化。
        *   **级联适应度评估**：采用四层过滤机制（语法筛选 -> 快速近似测试 -> 训练集验证 -> 全样本泛化评审），平衡搜索效率与严谨性。
        *   **诊断反馈**：将性能指标和错误信息反馈给 LLM，指导其生成下一代更优公式。
    3.  **约束满足变换**：将演化出的评分函数 $S_i$ 通过确定性变换，强制满足功率总和约束及最小功率约束。
*   **最终发现的公式**：$S_i = \max((\eta_i \zeta_i)^{0.495}, 10^{-6})$，其中 $\eta_i$ 和 $\zeta_i$ 分别是归一化的需求因子和边际收益。

### 3. 实验设计
*   **场景设置**：单雷达多目标跟踪，目标数量 $N$ 从 10 到 200 不等，采用恒定速度（CV）运动模型。
*   **Benchmark（基准方法）**：
    *   **IPM（内点法）**：作为理论最优基准（使用 MATLAB 的 `fmincon`）。
    *   **均匀分配（Uniform Allocation）**：基准对比。
    *   **高 SNR 近似（High-SNR Approx）**：传统的解析近似方法。
*   **评估指标**：相对性能损失（$\Delta L\%$）、计算耗时、加速比、动态跟踪下的 BCRLB 和 RMSE。

### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的硬件配置（如 GPU 型号或数量）以及演化搜索的具体时长。
*   **计算复杂度分析**：文中强调了推理时的复杂度，所提方法为 $O(N)$，而 IPM 为 $O(N^3)$。

### 5. 实验数量与充分性
*   **实验规模**：
    *   **静态精度分析**：使用了 $10^4$ 个独立随机场景。
    *   **泛化性测试**：在 $N=10$ 到 $200$ 的范围内，每隔 5 个目标数取样，每个点进行 $10^4$ 次实验。
    *   **动态闭环跟踪**：进行了 $10^4$ 次蒙特卡洛循环，跟踪步数达 80 步。
*   **充分性评价**：实验设计非常充分。通过跨规模（从少量目标到超大规模目标）的泛化测试，证明了公式并非过拟合，且动态闭环实验验证了误差不会随时间累积导致滤波器发散。

### 6. 主要结论与发现
*   **高精度**：所发现的闭式解平均相对性能损失仅为 **1.51%**，远优于高 SNR 近似（27.61%）。
*   **极速推理**：在 100 个目标场景下，比 IPM 快 **4754 倍**，比数值二分法快 1004 倍，单次分配耗时低于 300 $\mu s$。
*   **强泛化性**：在目标数量大幅增加（扩展至 200 个）时，平均损失仍稳定在 1.67% 以内。
*   **物理一致性**：演化出的公式自动筛选掉了冗余特征，仅保留了最具物理意义的项。

### 7. 优点
*   **创新范式**：将 LLM 的符号理解能力与演化算法结合，解决了传统优化问题难以推导闭式解的痛点。
*   **白盒模型**：结果是一个简单的数学公式，具有极高的可解释性，便于工程部署和验证。
*   **低数据依赖**：不同于深度学习，该方法不需要海量标注数据进行训练，而是基于物理先验进行搜索。

### 8. 不足与局限
*   **场景单一性**：目前仅针对单雷达、恒定速度模型进行了验证，在机动目标或组网雷达等更复杂场景下的适用性有待进一步研究。
*   **特征依赖**：公式的性能高度依赖于预先设计的物理特征（Feature Engineering），如果特征提取不全，可能无法发现最优解。
*   **搜索成本**：虽然推理极快，但 AlphaEvolve 的搜索过程（调用 LLM 和多次评估）可能涉及较高的离线计算成本。

（完）
