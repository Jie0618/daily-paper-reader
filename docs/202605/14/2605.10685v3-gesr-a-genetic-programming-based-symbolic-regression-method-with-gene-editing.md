---
title: "GESR: A Genetic Programming-Based Symbolic Regression Method with Gene Editing"
title_zh: GESR：一种基于基因编辑的遗传规划符号回归方法
authors: "Yanjie Li, Liping Zhang, Min Wu, Weijun Li, Lina Yu, Jingyi Liu, Yusong Deng, Mingzhu Wan, Xin Ning"
date: 2026-05-11
pdf: "https://arxiv.org/pdf/2605.10685v3"
tags: ["query:sr-alg"]
score: 10.0
evidence: 基于遗传规划的符号回归用于发现数学定律
tldr: 符号回归旨在从数据中发现数学规律。传统遗传编程（GP）因变异和交叉的随机性导致效率较低。本文提出GESR，引入“基因编辑”概念，利用两个BERT模型分别指导符号变异和交叉点的选择。该方法将深度学习的预测能力与进化算法结合，显著提升了搜索效率，在多个符号回归任务中表现优异。
selection_source: fresh_fetch
motivation: 传统遗传编程中的随机变异和交叉具有盲目性，导致进化效率低下，需要更精准的引导机制。
method: 提出GESR方法，利用两个BERT模型分别通过掩码语言建模指导符号变异，以及预测最优交叉点来优化基因交叉。
result: 实验证明GESR在计算效率上显著优于传统GP算法，并在多个符号回归基准任务中展现出卓越的性能。
conclusion: 通过引入基于深度学习的基因编辑机制，可以有效提升遗传编程的进化效率，为符号回归提供了新思路。
---

## 摘要
数学公式是人类与自然沟通的语言。从科学数据中发现数学规律以描述自然现象，是人类几个世纪以来长期追求的目标。在人工智能领域，这一挑战被称为符号回归问题。在现有的符号回归方法中，基于进化算法的遗传规划（GP）仍然是最经典且应用最广泛的方法之一。GP 通过遗传变异和交叉模拟跨代的进化过程。然而，GP 中的变异和交叉完全是随机的。虽然这种随机性有效地模拟了自然进化，但它不可避免地会产生有益和有害的变异。如果存在一个隐喻性的“上帝”，能够预见哪些遗传变异或交叉会产生更好的结果，并据此进行有针对性的基因编辑，进化的效率将得到大幅提升。受此启发，本文提出了一种基于基因编辑的符号回归方法，称为 GESR。在 GESR 中，我们训练了两个“上帝之手”（两个 BERT 模型）。其中，第一个模型利用 BERT 的掩码语言建模能力来指导基因（表达式符号）的变异。另一个 BERT 模型通过预测交叉点来指导个体基因的交叉。实验结果表明，与传统的 GP 算法相比，GESR 显著提高了计算效率，并在多个符号回归任务中取得了强大的综合性能。

## Abstract
Mathematical formulas serve as a language through which humans communicate with nature. Discovering mathematical laws from scientific data to describe natural phenomena has been a long-standing pursuit of humanity for centuries. In the field of artificial intelligence, this challenge is known as the symbolic regression problem. Among existing symbolic regression approaches, Genetic Programming (GP) based on evolutionary algorithms remains one of the most classical and widely adopted methods. GP simulates the evolutionary process across generations through genetic mutation and crossover. However, mutations and crossovers in GP are entirely random. While this randomness effectively mimics natural evolution, it inevitably produces both beneficial and detrimental variations. If there existed a metaphorical `God` capable of foreseeing which genetic mutations or crossovers would yield superior outcomes and performing targeted gene editing accordingly, the efficiency of evolution could be substantially improved. Motivated by this idea, we propose in this paper a symbolic regression approach based on gene editing, termed GESR. In GESR, we trained two "hands of God" (two BERT models). Among them, the first leverages the BERT's masked language modeling capability to guide the mutation of genes (expression symbols). The other BERT model guides the crossover of individual genes by predicting the crossover point. Experimental results demonstrate that GESR significantly improves computational efficiency compared with traditional GP algorithms and achieves strong overall performance across multiple symbolic regression tasks.

---

## 论文详细总结（自动生成）

这是一份关于论文《GESR: A Genetic Programming-Based Symbolic Regression Method with Gene Editing》的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
符号回归（Symbolic Regression, SR）旨在从观测数据中自动发现数学表达式。传统的遗传规划（Genetic Programming, GP）是该领域的经典方法，但其核心操作——**变异和交叉——具有高度的随机性**。这种随机性虽然保证了多样性，但也产生了大量无效或有害的变异，导致搜索效率低下、收敛缓慢且容易产生冗余复杂的表达式。
论文的研究动机是引入“**基因编辑**”的概念，利用深度学习模型（BERT）作为“上帝之手”，预见并引导更有利的遗传变异和交叉，从而将无序的随机搜索转化为数据驱动的定向优化。

### 2. 方法论：核心思想与关键技术
GESR 框架将多模态 BERT 模型与传统 GP 结合，核心流程如下：
*   **核心思想**：训练两个多模态 BERT 模型，分别担任“变异编辑器”和“交叉编辑器”，根据输入数据 $(X, y)$ 的分布来指导符号序列的演化。
*   **数据编码器（SetTransformer）**：由于输入数据点具有置换不变性，采用 SetTransformer 将原始数据编码为固定维度的嵌入向量。
*   **变异编辑器（Mutation BERT）**：
    *   将表达式树线性化为前序遍历序列。
    *   随机掩码（Mask）部分符号，利用 BERT 的掩码语言建模能力，结合数据嵌入，预测最可能提高拟合精度（$R^2$）的符号进行替换。
*   **交叉编辑器（Crossover BERT）**：
    *   将两个父代个体的序列拼接，预测两个序列中最适合交换的子树根节点位置，实现定向交叉。
*   **训练数据构建**：设计了一种基于“适应度提升”的流水线。通过 BFGS 优化连续参数并离散化，筛选出变异后性能提升的样本对来训练 BERT，确保模型学习到的是“改进策略”。

### 3. 实验设计
*   **数据集/场景**：
    *   **标准基准测试**：包括 Nguyen、Keijzer、Korns、Constant、Livermore、Vladislavleva、Jin、Neat 等 13 个经典数据集。
    *   **SRBench**：包含 Feynman 物理公式、Strogatz 动力系统和黑盒数据集。
    *   **混沌动力系统**：16 个复杂的非线性常微分方程（如 Lorenz、Duffing、Brusselator 等），测试其对向量场的建模能力。
*   **对比方法（Baselines）**：
    *   经典/先进 GP 方法：PySR, GPlearn, NGGP。
    *   深度学习/强化学习方法：SNIP, uDSR, NeSymRes, TPSR, End2End。

### 4. 资源与算力
论文明确记录了预训练阶段的算力消耗：
*   **硬件**：8 台 NVIDIA A100 GPU (80GB 显存)。
*   **训练规模**：在 1000 万（10M）个符号表达式及其对应数据对上进行训练。
*   **时长**：训练 10 个 Epoch，总耗时约 56 小时。
*   **模型大小**：每个 Checkpoint 约 0.5 GB。

### 5. 实验数量与充分性
实验设计非常充分且具有客观性：
*   **多维度评估**：不仅对比了拟合精度（$R^2$），还对比了表达式复杂度（节点数）、推理时间、恢复率（Recovery Rate）和噪声鲁棒性。
*   **消融实验**：分别测试了“仅变异引导”、“变异+交叉引导”以及“无引导”的效果，验证了各模块的贡献。
*   **鲁棒性与泛化性**：在不同噪声水平（0-0.1）和不同数据维度（1D-3D）下进行了压力测试。
*   **公平性**：严格控制了种群大小、搜索空间和常数优化算法（BFGS）等变量，确保对比在相同规则下进行。

### 6. 主要结论与发现
*   **效率提升**：GESR 的搜索效率比传统 GP 提高了约 **33%**，在达到相同精度时耗时更短。
*   **简洁性**：生成的数学表达式比主流方法更紧凑（节点数更少），具有更好的可解释性。
*   **性能卓越**：在多个基准测试中，GESR 的 $R^2$ 和公式恢复率均达到或超过了 SOTA 方法（如 PySR）。
*   **动力系统建模**：在混沌系统的向量场预测中，GESR 表现出极强的稳定性，能够更准确地捕捉吸引子的几何结构。

### 7. 优点与亮点
*   **即插即用**：训练好的 BERT 模块可以作为插件集成到其他基于 GP 的算法中（实验证明集成到 NGGP 后同样有效）。
*   **引入新概念**：首次将“基因编辑”概念引入进化计算领域，解决了 GP 随机性导致的盲目搜索问题。
*   **高质量数据生成**：提出了一种利用连续优化辅助离散符号筛选的训练数据构建方法，解决了符号回归监督信号难获取的问题。

### 8. 不足与局限
*   **预训练依赖**：算法性能在很大程度上取决于预训练 BERT 模型的质量和覆盖的表达式分布。
*   **维度限制**：目前的模型对输入数据的维度有一定限制（受限于预训练时的设置），在处理极高维数据时可能面临扩展性挑战。
*   **OOD 风险**：如果测试数据的数学规律完全超出了预训练数据的分布范围，BERT 的引导效果可能会打折扣。

（完）
