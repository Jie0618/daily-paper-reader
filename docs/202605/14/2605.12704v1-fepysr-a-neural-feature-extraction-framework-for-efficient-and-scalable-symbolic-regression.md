---
title: "FePySR: A Neural Feature Extraction Framework for Efficient and Scalable Symbolic Regression"
title_zh: FePySR：一种用于高效且可扩展符号回归的神经特征提取框架
authors: "Zhiming Yu, Wangtao Lu, Xin Lai"
date: 2026-05-12
pdf: "https://arxiv.org/pdf/2605.12704v1"
tags: ["query:sr-alg"]
score: 10.0
evidence: 用于符号回归的神经特征提取
tldr: 针对符号回归（SR）搜索空间巨大且属于 NP 难的问题，本文提出 FePySR 框架。该方法利用复杂表达式通常由非线性特征模块组成的特性，采用两阶段策略：首先通过异构神经网络从观测数据中提取候选特征，随后利用 PySR 在精简后的空间内进行结构优化。实验表明，FePySR 在多个基准测试中优于现有方法，能高效恢复复杂方程及生物系统控制方程，具有更强的抗噪性和计算效率。
selection_source: fresh_fetch
motivation: 符号回归搜索空间极广，但实际表达式往往由可重用的非线性特征模块组成，这为通过特征提取缩小搜索范围提供了可能。
method: 提出一种两阶段框架，先用神经网络将数据约束为候选表达式集合，再利用 PySR 在该精简空间内执行结构优化。
result: 在 75 个复杂合成方程和生物系统微分方程测试中，FePySR 的恢复率和均方误差表现均显著优于 PySR 等先进方法。
conclusion: FePySR 是一个可推广的增强框架，能有效提升符号回归求解器在跨学科科学领域中发现复杂数学规律的能力。
---

## 摘要
符号回归 (SR) 的一个核心挑战是从观测数据中高效地恢复复杂的数学表达式。尽管这一问题属于 NP 难问题，但许多具有实际应用价值的表达式可以自然地分解为非线性特征模块的组合，从而将结构复杂性集中在少数可重用的组件中。本文提出了 FePySR，这是一个两阶段框架，通过在方程搜索之前提取有效特征来缩小 SR 的搜索空间。FePySR 首先利用异构神经网络将观测数据约束到一组候选表达式中，随后使用 PySR 在这一精炼的表达式空间内进行结构优化。在五个标准基准测试中，FePySR 通过实现更高的方程恢复率优于现有最先进的方法。在一组包含 75 个高度复杂合成方程的测试中，FePySR 成功恢复了 36 个方程，且在其余未恢复的案例中产生的均方误差显著更小，同时比 PySR 耗时更短。FePySR 的第一阶段在不同数量的顶级特征选择以及观测数据噪声增加的情况下，均能保持稳定的性能。在应用于描述生物系统的常微分方程时，FePySR 在 PySR 无法恢复任何方程的 100 次测试中成功识别了 24 次控制方程。综上所述，FePySR 是一个具有泛化能力的框架，能够增强 SR 求解器的性能，实现在各科学领域中高效且可靠地恢复符号表达式。

## Abstract
A fundamental challenge in symbolic regression (SR) is efficiently recovering complex mathematical expressions from observational data. Although this problem is NP-hard, many expressions of practical interest decompose naturally into combinations of nonlinear feature modules, concentrating structural complexity into a small number of reusable components. Here, we introduce FePySR, a two-stage framework that reduces the SR search space by extracting valid features prior to equation search. FePySR first employs a heterogeneous neural network to constrain observational data to a set of candidate expressions, then performs structural optimization within this refined expression space using PySR. Across five standard benchmarks, FePySR outperforms state-of-the-art methods by achieving higher equation recovery rates. On a set of 75 highly complex synthesized equations, FePySR recovers 36 equations, while producing substantially smaller mean squared errors on the remaining unrecovered cases, with reduced computation time compared to PySR. FePySR's first stage also maintains consistent performance under varying numbers of selected top features and increasing levels of noise in the observational data. Applied to ordinary differential equations governing biological systems, FePySR successfully identifies governing equations in 24 out of 100 tests where PySR recovers none. Taken together, FePySR is a generalizable framework that can enhance the SR solvers, enabling the efficient and reliable recovery of symbolic expressions across scientific domains.

---

## 论文详细总结（自动生成）

### FePySR：一种用于高效且可扩展符号回归的神经特征提取框架

#### 1. 核心问题与研究动机
符号回归（SR）旨在从观测数据中直接发现简洁且可解释的数学模型，但其面临两个 NP 难任务：**特征选择**（从高维输入中识别有效变量）和**结构优化**（在海量算子组合中寻找最优公式）。
*   **研究动机**：现有的 SR 方法（如遗传算法或深度学习）在面对复杂方程时，往往因搜索空间爆炸而难以收敛。作者观察到，许多复杂的数学表达式可以分解为简单的非线性特征模块。如果能预先提取这些“有效特征”，就能显著降低搜索树的深度，将复杂问题简化。

#### 2. 方法论：FePySR 两阶段框架
FePySR 将符号回归分为特征提取和结构搜索两个阶段：
*   **第一阶段：特征映射网络 (FMN)**
    *   **核心思想**：扩展了方程学习器（EQL）架构，利用异构神经网络学习数据的非线性特征。
    *   **关键技术**：
        *   **异构激活单元 (HAU)**：每个单元使用特定的数学算子（如 $sin, exp, x^2, +, \times$）作为激活函数。
        *   **密集连接架构**：每一层的输出是当前层 HAU 结果与之前所有层特征的拼接，类似于 DenseNet，允许构建复杂的嵌套特征。
        *   **损失函数**：包含平方误差（$L_2$）、稀疏正则化（$L_{sparse}$，用于特征筛选）和**对比损失**（$L_{contrast}$，防止对称二元算子如 $+$ 或 $\times$ 退化）。
    *   **特征提取流程**：训练 FMN 后，根据权重大小提取最具影响力的算子组合，利用 SymPy 进行代数简化，并按频率排序构建特征库。
*   **第二阶段：结构优化**
    *   将提取的非线性特征与原始输入拼接，输入到 **PySR** 求解器中。由于搜索空间已通过高质量特征得到精简，PySR 能更高效地找到最终方程。

#### 3. 实验设计
*   **数据集**：
    *   **标准基准**：Nguyen、Livermore、Jin、Constant 和 R 数据集。
    *   **合成复杂基准**：由大语言模型（LLM）生成的 75 个高度复杂方程（包含深层嵌套、复杂分式和超越函数）。
    *   **实际案例**：描述生物调节网络动力学的常微分方程（Tyson 模型）。
*   **对比方法**：PySR、DSO（深度符号回归）、NGGP（神经引导遗传算法）、DSR、PQT、Eureqa、GEGL 等。

#### 4. 资源与算力
*   **算力说明**：论文提到在进行噪声敏感性实验时，使用了 **16 核 CPU** 进行并行计算（每个数据集运行 256 次 FMN 训练）。
*   **未明确信息**：文中未详细列出具体的 GPU 型号或总训练时长，但考虑到 FMN 架构相对轻量，其计算开销主要集中在多次重复实验以保证统计显著性上。

#### 5. 实验数量与充分性
*   **实验规模**：
    *   对标准基准中的每个方程进行了 **100 次独立试验**，以消除随机性影响。
    *   针对 75 个复杂方程，分别对比了恢复率（36 个可恢复方程）和最小均方误差（39 个极复杂方程）。
    *   进行了**特征数量敏感性分析**（4 到 9 个特征）和**噪声敏感性分析**（7 个噪声等级）。
*   **评价**：实验设计非常充分且客观。通过引入 LLM 生成的复杂方程和生物系统 ODE，验证了模型在极端复杂情况和实际科学场景下的泛化能力。

#### 6. 主要结论与发现
*   **性能提升**：在 Nguyen 数据集上，FePySR 的恢复率比现有最先进方法提高了 6.4% - 23.7%。
*   **复杂方程处理**：在 75 个复杂方程测试中，FePySR 恢复了 36 个，而 PySR 和 DSO 的表现远逊于此；在不可恢复的方程中，FePySR 的 MSE 显著低于 PySR。
*   **生物系统应用**：在 Tyson 生物模型测试中，FePySR 成功识别了 24% 的控制方程，而 PySR 的成功率为 0%。
*   **启发式引导**：即使提取的特征不完全精确，只要结构相似，也能引导 PySR 进入更有希望的搜索区域，加速收敛。

#### 7. 优点与亮点
*   **空间简化策略**：通过“特征工程自动化”有效解决了 SR 的算子组合爆炸问题。
*   **对比损失函数**：创新性地引入对比损失，解决了异构网络中二元算子容易发生的“功能坍塌”问题。
*   **鲁棒性**：FMN 模块在噪声环境下表现出极强的特征筛选稳定性。
*   **通用性**：作为一个增强框架，它可以与任何现有的 SR 求解器（如 PySR）集成。

#### 8. 不足与局限
*   **常数优化难题**：FMN 在处理带有复杂常数的特征（如 $sin(1.5x)$）时存在困难，因为引入可学习常数会导致特征空间充斥大量无效变体。
*   **噪声传递**：虽然第一阶段特征提取抗噪性强，但第二阶段的 PySR 在面对噪声数据时仍容易过拟合，导致端到端的精确恢复率下降。
*   **算子依赖**：特征提取的质量仍受限于预定义的算子库（Library），若目标方程包含库外算子，效果会受限。

（完）
