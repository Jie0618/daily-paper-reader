---
title: "YFPO: A Preliminary Study of Yoked Feature Preference Optimization with Neuron-Guided Rewards for Mathematical Reasoning"
title_zh: YFPO：一种基于神经元引导奖励的联动特征偏好优化在数学推理中的初步研究
authors: Yifan Le
date: 2026-05-12
pdf: "https://arxiv.org/pdf/2605.11906v1"
tags: ["query:math-ai"]
score: 8.0
evidence: 与符号操作和数学推理相关的神经元组
tldr: 偏好优化是提升大模型推理能力的关键，但现有方法多依赖外部偏好数据，忽视了模型内部表示中的能力相关信息。本文提出YFPO框架，通过AttnLRP识别数学相关神经元，并利用其在优劣回答间的激活差异构建辅助奖励。实验表明，这种结合内部神经元信号与外部偏好的方法能有效提升数学推理性能，为细粒度且可解释的后训练提供了新方向。
selection_source: fresh_fetch
motivation: 现有的偏好优化方法主要依赖外部样本级信号，未能充分利用模型内部与数学推理等特定能力相关的神经元激活信息。
method: 提出YFPO框架，利用AttnLRP识别数学神经元，并根据其激活边际构建辅助奖励，将内部神经元信号引入偏好学习过程。
result: 在GSM8K基准上的初步实验证明，神经元级信号能与偏好优化协同作用，在一定程度上提升了模型的数学推理表现。
conclusion: 将内部特征激活与外部偏好优化相结合，是实现更精细、更具可解释性的推理导向后训练的一种有前景的路径。
---

## 摘要
偏好优化已成为提升大语言模型推理能力的重要后训练范式。现有方法通常依赖于外部构建的偏好数据，将偏好响应和非偏好响应作为样本级监督。然而，此类外部信号很少显式利用模型内部表示中包含的与能力相关的信息。对于数学推理，某些神经元组可能会表现出与数学知识、符号操作或逻辑推理相关的激活模式。类似于反射性行为信号，这些内部激活可以粗略地指示模型是否正在调用与数学相关的能力。我们提出了 YFPO（联动特征偏好优化，Yoked Feature Preference Optimization 的缩写），这是一种用于数学推理的初步神经元引导偏好优化框架。YFPO 首先使用 AttnLRP 识别与数学相关的神经元，然后根据偏好响应和非偏好响应之间的激活差值构建辅助奖励。这种设计利用内部神经元级信号增强了外部偏好学习。我们在小规模语言模型上进行了初步实验，并以 GSM8K 作为主要基准。结果表明，神经元级信号可以与偏好优化相互作用，并偶尔提高推理性能，为更细粒度且可解释的面向推理的后训练提供了一个有前景的方向。

## Abstract
Preference optimization has become an important post-training paradigm for improving the reasoning abilities of large language models. Existing methods typically rely on externally constructed preference data, using preferred and dispreferred responses as sample-level supervision. However, such external signals rarely make explicit use of capability-related information contained in the model's internal representations. For mathematical reasoning, certain neuron groups may exhibit activation patterns associated with mathematical knowledge, symbolic manipulation, or logical reasoning. Similar to reflexive behavioral signals, these internal activations may provide a coarse indication of whether the model is engaging math-related capabilities.We introduce YFPO, short for Yoked Feature Preference Optimization, a preliminary neuron-guided preference optimization framework for mathematical reasoning. YFPO first uses AttnLRP to identify math-related neurons, and then constructs an auxiliary reward from their activation margin between preferred and dispreferred responses. This design augments external preference learning with internal neuron-level signals. We conduct preliminary experiments on a small-scale language model using GSM8K as the main benchmark. Results suggest that neuron-level signals can interact with preference optimization and occasionally improve reasoning performance, offering a promising direction for more fine-grained and interpretable reasoning-oriented post-training.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **YFPO**（Yoked Feature Preference Optimization，联动特征偏好优化）的框架，旨在通过结合大语言模型内部的神经元激活信号来增强数学推理能力的偏好优化。

以下是对该论文的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：现有的偏好优化方法（如 DPO、PPO）主要依赖外部监督信号（如人类偏好、正确答案或奖励模型），将模型视为“黑盒”，仅通过输出结果的优劣来调整参数。
*   **研究动机**：作者认为模型内部表示中包含与特定能力（如数学推理、逻辑操作）相关的神经元激活模式。如果能显式地利用这些内部信号作为辅助奖励，可能比单纯依靠外部样本级监督更有效地引导模型学习复杂的推理能力。
*   **整体含义**：YFPO 尝试将“可解释性研究”与“模型训练”结合，探索内部神经元激活是否可以作为一种细粒度的、面向能力的辅助监督信号。

### 2. 方法论：核心思想与技术细节
YFPO 的核心思想是在标准偏好优化（DPO）的基础上，增加一个基于“数学相关神经元”激活差异的辅助奖励项。其流程分为两个阶段：
*   **阶段一：识别数学神经元（离线）**
    *   使用 **AttnLRP**（归因方法）在数学推理样本上计算每个 MLP 神经元对输出结果的贡献度（相关性得分）。
    *   筛选出得分最高的 Top-K（默认 K=1024）个神经元，定义为数学相关神经元集合 $N_{math}$。
*   **阶段二：神经元引导的偏好训练（在线）**
    *   **神经元得分计算**：在训练过程中，通过前向钩子（Forward Hooks）提取 $N_{math}$ 中神经元在处理偏好回答 $y^+$ 和非偏好回答 $y^-$ 时的激活值。
    *   **构建神经元奖励边际**：计算 $\Delta_{neu} = s_{neu}(x, y^+) - s_{neu}(x, y^-)$，即偏好回答相比非偏好回答在数学神经元上的激活增量。
    *   **优化目标**：最终损失函数为 $L_{YFPO} = L_{DPO} - \lambda \Delta_{neu}$。其中 $\lambda$ 是调节内部信号强度的超参数。该目标鼓励模型在满足外部偏好的同时，让偏好回答更强地激活内部的数学相关神经元。

### 3. 实验设计
*   **基础模型**：Qwen2-0.5B（小规模模型，便于快速验证）。
*   **训练数据**：使用 `Math-Step-DPO-10K` 数据集，并额外构建了一个 2K 样本的子集以测试低资源场景。
*   **评估基准（Benchmark）**：主要使用 **GSM8K**（小学数学应用题），部分提及 Math500。
*   **对比方法**：标准的 **DPO** 算法。
*   **评估指标**：报告了三个不同训练检查点（Ckpt）的准确率、平均准确率（Avg.）以及最佳准确率（Best）。

### 4. 资源与算力
*   **算力说明**：论文中**未明确说明**具体的 GPU 型号、数量或总训练时长。
*   **实现细节**：提到使用了前向钩子（Forward Hooks）来实时提取激活值，这通常会增加一定的内存开销和计算延迟，但由于模型规模较小（0.5B），整体算力需求相对较低。

### 5. 实验数量与充分性
*   **实验规模**：作者进行了两组主要实验（10K 全量数据和 2K 子集数据），并针对超参数 $\lambda$ 进行了多组消融实验（$\lambda$ 取值从 0.05 到 1.0 不等）。
*   **充分性评价**：
    *   **初步性**：正如标题所示，这是一项“初步研究（Preliminary Study）”。实验主要集中在极小规模模型（0.5B）和单一任务（数学）上。
    *   **客观性**：作者诚实地报告了实验结果的波动性（不同检查点表现不一），并指出 $\lambda$ 的选择对结果有显著影响。
    *   **局限性**：缺乏在更大规模模型（如 7B 及以上）和更多样化推理任务上的验证，实验的普适性有待加强。

### 6. 主要结论与发现
*   **性能提升**：在 10K 和 2K 数据集上，YFPO 的最佳检查点准确率均优于标准 DPO（例如在 10K 任务中，GSM8K 准确率从 0.3662 提升至 0.3738）。
*   **训练动力学**：YFPO 在保持外部偏好信号的同时，成功引入了独立的内部神经元优化信号。
*   **超参数敏感性**：$\lambda$ 的取值至关重要。过大或过小的奖励强度都会影响最终效果，且最佳 $\lambda$ 随数据规模变化。
*   **内部信号的价值**：实验证明内部神经元激活不仅可以用于解释模型，还可以作为有效的训练辅助信号，改变模型的优化轨迹。

### 7. 优点（亮点）
*   **新颖的视角**：打破了偏好优化的“黑盒”限制，将模型内部的机械解释性（Mechanistic Interpretability）成果直接应用于对齐训练。
*   **细粒度引导**：相比于粗糙的样本级标签，神经元级的激活差异提供了更细致的反馈。
*   **潜在的可解释性**：通过观察神经元奖励的变化，可以更直观地理解模型在训练过程中推理能力的演变。

### 8. 不足与局限
*   **模型规模受限**：仅在 0.5B 模型上验证，尚不清楚该结论是否能推广到具有更复杂涌现能力的超大模型。
*   **神经元选择的静态性**：目前采用离线筛选固定神经元的方法，忽略了训练过程中神经元功能可能发生的动态迁移。
*   **任务覆盖面窄**：仅针对数学推理，未测试其他逻辑推理或通用任务。
*   **性能增益较小**：虽然有提升，但绝对准确率的增幅相对有限，且存在检查点不稳定的问题。

（完）
