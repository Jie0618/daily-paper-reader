---
title: "From LLM-Generated Conjectures to Lean Formalizations: Automated Polynomial Inequality Proving via Sum-of-Squares Certificates"
title_zh: 从 LLM 生成的猜想到 Lean 形式化：通过平方和证书实现自动多项式不等式证明
authors: "Ruobing Zuo, Hanrui Zhao, Gaolei He, Zhengfeng Yang, Jianlin Wang"
date: 2026-05-14
pdf: "https://arxiv.org/pdf/2605.15445v1"
tags: ["query:math-ai"]
score: 9.0
evidence: 通过神经符号框架自动证明多项式不等式
tldr: 本研究针对多项式不等式自动证明中的扩展性难题，提出了NSPI神经符号框架。该框架结合了LLM的启发式搜索能力与符号计算的严谨性：首先由LLM提出近似平方和（SOS）分解猜想，随后利用符号计算将其精炼为精确的SOS表示以证明不等式，最后在Lean证明辅助器中完成形式化验证。实验表明，该方法能有效处理多达10个变量的复杂多项式，实现了从启发式发现到机器验证的端到端自动化证明。
selection_source: fresh_fetch
motivation: 传统符号方法在处理多变量高次数多项式时因代数运算开销巨大而难以扩展，且LLM在复杂数学证明中仍面临可靠性挑战。
method: 提出一种结合LLM生成SOS猜想、符号计算精炼及Lean形式化验证的神经符号端到端框架。
result: 在涉及多达10个变量的挑战性多项式不等式基准测试中，该方法显著提升了证明的成功率和扩展性。
conclusion: 神经符号结合的方法为自动化数学推理提供了新思路，成功实现了从启发式猜想向机器检查证明的转化。
---

## 摘要
多项式不等式的自动证明是自动数学推理中的一个基本挑战，其中丰富的代数结构和快速增长的证书搜索空间阻碍了其可扩展性。纯符号方法虽然提供了强有力的保证，但由于昂贵的代数运算和快速增长的中间表达式，当变量数量或次数增加时，其扩展性往往较差。与此同时，由大语言模型（LLM）引导的方法取得了显著进展，特别是在变量较少的竞赛级不等式上。为了解决剩余的可扩展性挑战，我们提出了 NSPI，这是一个神经符号框架，结合了 LLM 和符号计算在多项式不等式证明方面的互补优势。具体而言，LLM 以近似多项式平方和（SOS）分解的形式提出猜想；我们通过符号计算对其进行细化，以获得精确的多项式 SOS 表示，从而直接证明目标不等式，并进一步在 Lean 中对证明进行验证，实现了从启发式发现到机器检查证明的端到端流程。在涉及多达 10 个变量的多项式的挑战性基准测试上的实验证明了所提方法的有效性和可扩展性。

## Abstract
Automated proving of polynomial inequalities is a fundamental challenge in automated mathematical reasoning, where rich algebraic structure and a rapidly growing certificate search space hinder scalability. Purely symbolic approaches provide strong guarantees but often scale poorly as the number of variables or the degree increases, due to expensive algebraic manipulations and rapidly growing intermediate expressions. In parallel, LLM-guided methods have made notable progress, particularly on competition-style inequalities with a small number of variables. To address the remaining scalability challenges, we propose NSPI, a neuro-symbolic framework that combines the complementary strengths of LLMs and symbolic computation for polynomial-inequality proving. Concretely, an LLM proposes a conjecture in the form of an approximate polynomial Sum-Of-Squares (SOS) decomposition; we refine it via symbolic computation to obtain an exact polynomial SOS representation, which directly proves the target inequality, and we further certify the proof in Lean, yielding an end-to-end pipeline from heuristic discovery to machine-checked proof. Experiments on challenging benchmarks involving polynomials with up to 10 variables demonstrate the effectiveness and scalability of the proposed method.

---

## 论文详细总结（自动生成）

### 论文总结：从 LLM 生成的猜想到 Lean 形式化

#### 1. 核心问题与整体含义（研究动机和背景）
多项式不等式的自动证明是数学推理中的基础难题。传统方法主要面临两大挑战：
*   **符号计算的扩展性瓶颈：** 纯符号方法（如基于半正定规划 SDP 的平方和 SOS 分解）在处理多变量、高次数多项式时，由于代数运算开销巨大和中间表达式爆炸，难以扩展到高维情况。
*   **神经推理的严谨性缺失：** 尽管大语言模型（LLM）在竞赛级数学题上表现出色，但其生成的证明往往缺乏严谨的逻辑保证，且在处理高维复杂代数结构时能力有限。

**研究动机：** 开发一个名为 **NSPI** 的神经符号框架，利用 LLM 的启发式搜索能力生成 SOS 结构猜想，再通过符号计算进行精确修正，最后在 Lean 证明辅助器中生成机器可检查的正式证明，从而解决高维多项式不等式的自动证明难题。

#### 2. 方法论：核心思想与关键技术
NSPI 采用三阶段的端到端流程：
*   **神经猜想（Neural Conjecture）：**
    *   **数据构建：** 通过计算驱动（如特征值偏移、LMI 优化）和结构驱动（如对角占优矩阵）的方法，合成超过 100 万组“多项式-SOS 证书”训练对。
    *   **模型训练：** 使用 **Qwen3-8B** 作为基座，采用两阶段训练：首先进行大规模监督微调（SFT）以获得基础能力；随后进行**课程强化学习（Progressive RL）**，利用 **GRPO 算法**，根据准确率、格式和代数结构一致性设置奖励函数，逐步提升模型处理高维问题的能力。
*   **符号修正（Symbolic Correction）：**
    *   **高斯-牛顿精炼：** 将 LLM 生成的近似 SOS 猜想作为初始值，通过迭代优化降低数值误差。
    *   **有理数恢复（Rational Recovery）：** 利用正交投影或联立丢番图逼近技术，将数值解转换为精确的有理数表示，确保满足多项式恒等式。
*   **形式化验证（Formal Verification）：**
    *   将精确的 SOS 证书填入预定义的 **Lean 4 证明模板**。
    *   调用 Lean 的 `linear_combination` 验证代数等价性，并使用 `positivity` 策略自动证明平方和的非负性，最终生成机器检查过的证明脚本。

#### 3. 实验设计
*   **数据集/基准测试：** 构建了 **PolyIneqBench**，包含 522 个不等式问题，变量范围从 3 到 10 个。
    *   **PolyIneq-Real：** 102 道来自国际数学竞赛（IMO 等）和经典文献的真实题目。
    *   **PolyIneq-Synth：** 420 道系统生成的具有挑战性的高维合成题目。
*   **对比方法：**
    *   **符号方法：** Maple, Z3。
    *   **形式化证明 LLM：** DeepSeek-Prover-V2, Goedel-Prover-V2, Kimina Prover。
    *   **通用 LLM：** GPT-5.2, Gemini-3-Pro, DeepSeek-V3.2。
    *   **混合系统：** LIPS。

#### 4. 资源与算力
*   **模型：** 使用了 Qwen3-8B 作为基础模型。
*   **算力细节：** 论文**未明确说明**具体的 GPU 型号、数量及总训练时长。
*   **推理配置：** 推理时每个问题给予 32 次尝试机会（pass@32），最大时间限制为 1 小时。

#### 5. 实验数量与充分性
*   **实验规模：** 针对 3-10 个变量的 522 个问题进行了详尽测试，并展示了随变量增加的性能趋势。
*   **消融实验：** 验证了合成数据 SFT、GRPO 强化学习以及课程学习（CL）的必要性。
*   **客观性：** 实验涵盖了从 3 变量到 10 变量的梯度，对比了当前最顶尖的各类模型和工具，结果显示 NSPI 在高维场景（n=10）下仍能保持 11.7% 的成功率，而大多数基准方法在 5 变量后便迅速失效。实验设计较为客观、公平。

#### 6. 主要结论与发现
*   **高维处理能力：** NSPI 显著提升了多变量不等式证明的上限，在 10 变量问题上表现远超传统符号工具和纯 LLM 方法。
*   **神经符号协同：** 证明了 LLM 可以作为高效的“结构猜想器”，而符号计算则是不可或缺的“精度桥梁”。
*   **强化学习效果：** 课程强化学习（GRPO + CL）能有效迁移模型能力边界，使其从低维简单问题泛化到高维复杂结构。

#### 7. 优点与亮点
*   **端到端自动化：** 实现了从自然语言/LaTeX 描述到 Lean 形式化证明的全自动转化。
*   **创新的训练范式：** 提出了 LLM 作为核心符号猜想引擎的新范式，而非仅仅作为搜索引导。
*   **证书可靠性：** 通过 SOS 证书和 Lean 验证，确保了结果的绝对数学正确性，消除了 LLM 的幻觉问题。

#### 8. 不足与局限
*   **适用范围限制：** 目前主要针对**无约束**的多项式不等式。对于带有复杂约束条件（如 $g_i(x) \ge 0$）的半代数集上的不等式证明，虽然理论上可扩展，但文中未做深入探讨。
*   **对初始猜想的依赖：** 如果 LLM 无法提出合理的 SOS 结构（例如多项式本身不是 SOS 可分解的），符号修正阶段将无法发挥作用。
*   **算力信息缺失：** 缺乏具体的训练资源描述，不便于其他研究者评估复现成本。

（完）
