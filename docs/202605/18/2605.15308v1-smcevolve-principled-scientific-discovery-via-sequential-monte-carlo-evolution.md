---
title: "SMCEvolve: Principled Scientific Discovery via Sequential Monte Carlo Evolution"
title_zh: SMCEvolve：通过序贯蒙特卡洛演化实现规范化的科学发现
authors: "Jiachen Jiang, Huminhao Zhu, Zhihui Zhu"
date: 2026-05-14
pdf: "https://arxiv.org/pdf/2605.15308v1"
tags: ["query:sr-alg"]
score: 9.0
evidence: 通过程序演化和SMC进行原则性的科学发现
tldr: SMCEvolve是一个基于大语言模型（LLM）的自动化科学发现框架，它将程序进化搜索重新定义为从奖励加权目标分布中进行采样。通过引入序贯蒙特卡洛（SMC）采样器，该框架实现了自适应父代重采样、带接受机制的变异混合以及自动收敛控制。实验证明，SMCEvolve在数学、算法优化和机器学习研究等多个基准测试中，以更少的LLM调用次数超越了现有最先进的进化系统，并提供了理论上的收敛保证。
selection_source: fresh_fetch
motivation: 现有的LLM驱动程序进化框架缺乏系统性的设计指导和搜索收敛性的理论保证。
method: 将程序搜索建模为序贯蒙特卡洛采样过程，结合自适应重采样、变异接受机制和自动收敛控制。
result: 在数学、符号回归和机器学习研究等任务中，该方法以更低的计算成本实现了优于现有SOTA系统的性能。
conclusion: SMCEvolve为自动化科学发现提供了一个具有理论支撑且高效的进化搜索范式。
---

## 摘要
LLM 驱动的程序演化已成为自动化科学发现的强大工具，然而现有框架在设计其各个组件时缺乏规范化的指导，且无法保证搜索的收敛性。我们提出了 SMCEvolve，它将程序搜索重新表述为从奖励倾斜的目标分布中进行采样，并利用序贯蒙特卡洛（SMC）采样器对其进行近似。从这一视角出发，涌现出三个作为规范化组件的核心机制：自适应父代重采样、带接受机制的变异混合以及自动收敛控制。我们进一步提供了有限样本复杂度分析，界定了达到目标近似误差所需的 LLM 调用预算。在数学、算法效率、符号回归和端到端机器学习研究基准测试中，SMCEvolve 在自主终止的情况下，以更少的 LLM 调用次数超越了最先进的演化系统。代码可在 https://github.com/kongwanbianjinyu/SMCEvolve 获取。

## Abstract
LLM-driven program evolution has emerged as a powerful tool for automated scientific discovery, yet existing frameworks offer no principled guide for designing their individual components and provide no guarantee that the search converges. We introduce SMCEvolve, which recasts program search as sampling from a reward-tilted target distribution and approximates it with a Sequential Monte Carlo (SMC) sampler. From this view, three core mechanisms emerge as principled components: adaptive parent resampling, mixture of mutation with acceptance, and automatic convergence control. We further provide a finite-sample complexity analysis that bounds the LLM-call budget required to reach a target approximation error. Across math, algorithm efficiency, symbolic regression, and end-to-end ML research benchmarks, SMCEvolve surpasses state-of-the-art evolving systems while using fewer LLM calls under self-determined termination. The code is available at https://github.com/kongwanbianjinyu/SMCEvolve.

---

## 论文详细总结（自动生成）

以下是对论文《SMCEvolve: Principled Scientific Discovery via Sequential Monte Carlo Evolution》的深度结构化总结：

### 1. 核心问题与整体含义
*   **研究动机**：当前的 LLM 驱动程序演化系统（如 AlphaEvolve, ShinkaEvolve）虽然在科学发现中表现出色，但其设计大多基于经验主义。现有的框架在父代选择、变异策略和种群管理上缺乏统一的理论指导，且无法保证搜索过程的收敛性。
*   **核心目标**：将程序演化搜索建立在严谨的概率框架之上，通过序贯蒙特卡洛（SMC）理论统一演化组件，并提供收敛性保证。

### 2. 方法论
*   **核心思想**：将寻找高奖励程序的过程建模为从“奖励倾斜目标分布”（Reward-tilted Target Distribution） $p^*(x) \propto p_0(x) e^{\beta R(x)}$ 中进行采样。由于直接采样不可行，SMCEvolve 利用 SMC 采样器通过一系列中间桥接分布（逐渐增加奖励权重 $\beta$）来逼近目标。
*   **关键技术细节**：
    *   **自适应父代重采样**：根据当前桥接分布的权重进行重采样。搜索初期接近均匀采样以鼓励探索，后期则聚焦于高奖励个体以进行利用。
    *   **带接受机制的变异混合**：
        *   **MH 修正**：引入 Metropolis-Hastings (MH) 接受/拒绝步骤，确保采样过程符合目标分布的稳定性。
        *   **混合算子**：设计了四种变异算子（局部 Diff vs. 全局重写；单父代 vs. 跨程序借鉴），并利用 **Thompson 采样** 动态选择表现最好的算子。
    *   **自动收敛控制**：利用**有效样本量（ESS）**动态调整温度步长。当种群多样性降低或达到目标分布时，系统会自动决定终止搜索，无需预设迭代次数。
*   **理论贡献**：提供了首个程序演化的有限样本复杂度分析，证明了在给定 LLM 调用预算下达到目标近似误差的界限。

### 3. 实验设计
*   **实验场景与数据集**：
    *   **数学领域**：AlphaEvolve 基准测试中的 10 个组合几何问题（如圆填充、Heilbronn 三角形等）。
    *   **算法效率**：AlgoTune 基准测试，涉及数值计算、信号处理和线性代数的运行时优化。
    *   **符号回归**：LLM-SRBench，涵盖物理振荡器、细菌生长、化学反应等 16 个科学方程发现任务。
    *   **端到端机器学习研究**：AutoResearch 任务，在 TinyStories 数据集上进行 GPT 预训练脚本的自动化优化。
*   **对比方法**：ReEvo、OpenEvolve、ShinkaEvolve 等当前最先进的（SOTA）演化代理系统。

### 4. 资源与算力
*   **算力消耗**：
    *   在 **AutoResearch** 任务中，每个候选程序在单张 **NVIDIA RTX A5000 (24GB)** GPU 上训练 60 秒。
    *   **LLM 模型**：实验使用了 GPT-4o-mini 和 Gemini-1.5-Flash 的集成（文中表述为 gpt-5-mini 和 gemini-3-flash，可能为论文撰写时的预设或特定版本）。
*   **调用预算**：基准方法通常固定在 200-500 次 LLM 调用，而 SMCEvolve 通过 ESS 控制实现了更少的平均调用次数。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了从纯数学到系统工程的 4 大领域，共计 35 个具体任务。
*   **消融实验**：针对父代重采样策略、变异算子选择、种群宽度（N）与深度（K）的平衡进行了详细测试。
*   **充分性评价**：实验设计非常充分且客观。通过在不同领域使用相同的模型配置，并引入自动终止机制与固定预算基准对比，有力地证明了该方法在效率和性能上的双重优势。

### 6. 主要结论与发现
*   **性能领先**：SMCEvolve 在绝大多数任务中超越了现有 SOTA 系统，尤其在复杂的数学构造和算法优化任务中表现突出。
*   **高效性**：得益于自动收敛控制，SMCEvolve 在达到更高奖励的同时，平均 LLM 调用次数显著低于基准方法。
*   **理论验证**：实验结果验证了 SMC 框架在处理离散程序空间搜索时的鲁棒性，证明了理论上的收敛保证在实际应用中是有效的。

### 7. 优点
*   **原则性设计**：将零散的启发式搜索组件统一在 SMC 概率框架下，使设计不再是“黑盒”。
*   **自适应性强**：能够根据任务难度自动调整搜索步长和终止时机，减少了人工调参的负担。
*   **算子多样性**：结合 Thompson 采样和多种变异模式，有效解决了程序搜索中容易陷入局部最优的问题。

### 8. 不足与局限
*   **MH 比例近似**：由于黑盒 LLM 无法提供先验概率密度，MH 接受率采用了基于奖励的近似值，这在理论严密性上存在微小偏差。
*   **实现复杂度**：相比于简单的演化循环，SMC 采样器的实现逻辑（如 ESS 计算、双层循环、Thompson 采样）更为复杂。
*   **领域限制**：虽然跨度大，但仍依赖于可执行程序的评估反馈，对于无法快速自动评估的科学发现领域（如需实验室验证的材料发现）应用受限。

（完）
