---
title: Diversified Residual Symbolic Regression
title_zh: 多样化残差符号回归
authors: "Koki Ikeda, Masahiro Nomura, Ryoki Hamano"
date: 2026-05-15
pdf: "https://arxiv.org/pdf/2605.15809v1"
tags: ["query:sr-alg"]
score: 10.0
evidence: 多样化残差符号回归，用于从数据中稳健地发现表达式
tldr: 符号回归（SR）旨在发现可解释的数学表达式，但易受异常值干扰。本文提出多样化残差符号回归（DRSR），利用质量-多样性（Quality-Diversity）范式，在保证预测准确性的同时，生成具有不同残差分布模式的候选表达式。该方法允许用户根据领域知识从多种潜在关系中进行选择，在合成数据集和天文实际数据中均表现出优于传统SR的多样性和物理一致性。
selection_source: fresh_fetch
motivation: 传统的符号回归对异常值敏感，且识别异常值往往具有主观性，需要提供多样化的候选表达式供专家根据领域知识进行选择。
method: 提出DRSR方法，基于质量-多样性框架，在优化拟合精度的同时，通过差异化的残差分布模式来提升候选表达式的多样性。
result: 在合成混合数据集和真实天文数据集中，DRSR成功发现了多种符合物理规律且具有不同残差特征的表达式。
conclusion: DRSR通过提供多样化的残差模式选择，增强了符号回归在处理复杂现实数据时的鲁棒性和实用性。
---

## 摘要
符号回归 (SR) 旨在发现解释观测数据的显式数学表达式，广泛应用于对可解释性有极高要求的领域。由于可解释性要求表达式能够反映有意义的规律，SR 对偏离主导关系的观测值非常敏感。这种不规则的观测值（即离群值）在现实世界的数据中很常见，可能会阻碍 SR 识别潜在的规律。稳健回归通过降低具有大残差的观测值的权重来缓解这一问题。然而，决定哪些观测值应被视为离群值通常具有模糊性，且取决于用户的解释和领域知识，这一视角在现有的 SR 研究中很大程度上被忽视了。这促使了能够提供多个候选表达式的方法，允许用户检查不同的残差模式，并选择与他们的专业知识相一致的表达式。我们提出了多样化残差符号回归 (DRSR)，它在实现高预测准确性的同时，基于“质量-多样性”（Quality-Diversity）范式提升了残差模式的多样性。DRSR 收集了多个能很好拟合数据但在残差分布上有所不同的表达式，从而实现了与领域知识相一致的搜索后选择。在合成混合数据集上，DRSR 比传统 SR 产生了更多样化的表达式，同时捕捉到了多个潜在关系。在真实世界的天文数据集上，DRSR 发现了多个与已知物理关系一致的表达式。

## Abstract
Symbolic regression (SR) aims to discover explicit mathematical expressions that explain observed data and is widely used in domains where interpretability is essential. Because interpretability requires expressions to reflect meaningful regularities, SR is sensitive to observations that deviate from the dominant relationship. Such irregular observations, or outliers, are common in real-world data and can hinder SR from identifying underlying regularities. Robust regression mitigates this by downweighting observations with large residuals. However, deciding which observations should be treated as outliers is often ambiguous and depends on user interpretation and domain knowledge, a perspective largely overlooked in existing SR studies. This motivates approaches that present multiple candidate expressions, allowing users to examine different residual patterns and choose expressions consistent with their expertise. We propose diversified residual symbolic regression (DRSR), which achieves high predictive accuracy while promoting diversity with respect to residual patterns based on the Quality-Diversity paradigm. DRSR collects multiple expressions that fit the data well but differ in how residuals are distributed, enabling post-search selection aligned with domain knowledge. On a synthetic mixture dataset, DRSR produces more diverse expressions than conventional SR while capturing multiple underlying relationships. On a real-world astronomical dataset, DRSR discovers multiple expressions consistent with known physical relationships.

---

## 论文详细总结（自动生成）

这是一份关于论文《Diversified Residual Symbolic Regression》（多样化残差符号回归，简称 DRSR）的结构化深入分析报告：

### 1. 核心问题与整体含义（研究动机和背景）
符号回归（SR）的核心目标是发现能够解释数据的显式数学表达式。然而，传统的 SR 方法（通常基于均方误差 MSE）对**离群值（Outliers）**极其敏感。
*   **研究动机**：在现实数据中，离群值可能源于测量误差，也可能代表数据的**异质性**（即数据由多种不同的潜在模型混合而成）。
*   **核心矛盾**：现有的稳健回归方法虽然能通过稳健损失函数（如 MAE、MedAE）减轻离群值影响，但这些损失函数的优化空间通常是多峰的。不同的局部最优解可能对应于将不同的观测点视为离群值。
*   **整体含义**：由于“哪些点是离群值”往往取决于领域知识，论文认为不应只给出一个“最优”解，而应提供一组具有**不同残差模式**的高质量候选表达式，供专家选择。

### 2. 方法论：核心思想与关键技术
论文提出了 **DRSR** 框架，将符号回归转化为一个**质量-多样性（Quality-Diversity, QD）**搜索问题。

*   **核心思想**：利用 MAP-Elites 算法维护一个网格存档（Archive），在保证表达式拟合质量的同时，强制搜索空间覆盖不同的“行为特征”。
*   **关键技术细节**：
    1.  **行为描述符（Behavior Descriptors, BD）**：
        *   **离群值簇索引 ($b_{out}$)**：将数据集聚类，识别表达式在哪个簇上的平均绝对残差最大。这决定了表达式“不擅长”解释哪部分数据。
        *   **表达能力 ($b_{rep}$)**：表达式树的节点总数。
        *   **超越函数计数 ($b_{trans}$)**：包含 `log`、`exp` 等函数的数量。
    2.  **稳健损失函数**：采用中值绝对误差（MedAE）或平均绝对误差（MAE）作为质量度量，以增强对离群值的容忍度。
    3.  **系数优化**：由于稳健损失函数的景观是非凸且多峰的，DRSR 引入了 **CMA-ES（协方差矩阵自适应进化策略）** 对表达式中的常数系数进行局部优化，而非简单的梯度下降。
    4.  **算法流程**：从存档中选择父代 -> 交叉/变异 -> 符号简化 -> CMA-ES 系数优化 -> 根据质量和 BD 更新存档。

### 3. 实验设计
论文设计了三个层面的实验来验证 DRSR 的有效性：
*   **合成基准测试**：使用 Nguyen 系列基准函数（Nguyen-1, 7, 11, 12），人为注入不同比例的噪声（离群值）。
*   **混合数据集场景**：构建了一个包含线性关系（Linear）和逻辑回归关系（Logistic）的混合数据集，模拟异质性数据源。
*   **真实世界应用**：使用 **DEBCat 天文数据集**（双星系统的质量-光度数据），尝试发现符合物理定律的幂律关系。
*   **对比方法（Benchmark）**：
    *   标准单目标符号回归（SR）。
    *   多目标符号回归（MOSR）：同时优化准确性和简洁性（Pareto 前沿）。

### 4. 资源与算力
*   **算力说明**：论文**未明确提及**具体的硬件型号（如 GPU 或 CPU 核心数）。
*   **计算预算**：实验设定了明确的计算规模，每个实验运行 10 次独立重复试验，每次试验的适应度评估预算（Fitness Evaluation Budget）为 **2,000,000 次**。
*   **工具使用**：提及使用了生成式 AI 工具辅助英文写作和实验代码实现。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 4 个合成函数、1 个复杂的混合模型实验和 1 个真实物理数据集实验。
*   **充分性**：
    *   每组实验均进行了 10 次独立重复，并提供了 95% 的自助法（Bootstrap）置信区间，统计学意义较为明确。
    *   针对不同离群值数量（$n_{noise}=5$ vs $20$）进行了对比，验证了 MedAE 在高噪声下的优越性。
    *   **客观性**：通过 Coverage（覆盖率）和 QD-Score 等标准 QD 指标评估多样性，对比实验设置公平（相同的评估预算和系数优化机制）。

### 6. 主要结论与发现
*   **稳健性**：在存在离群值的情况下，使用 MedAE 损失的 DRSR 显著优于基于 MSE 的传统方法。
*   **混合模型恢复**：在混合数据集中，DRSR 能够在单次运行中同时发现线性模型和逻辑模型，而标准 SR 和 MOSR 通常只能捕捉到其中一种（通常是较简单的线性模型）。
*   **物理一致性**：在天文数据实验中，DRSR 成功识别出了符合物理学幂律（$y = Ax^b$）的多种表达式，证明了其在科学发现中的潜力。

### 7. 优点（亮点）
*   **范式创新**：首次将 QD 框架应用于解决符号回归中的残差分配歧义问题，从“寻找唯一解”转向“提供多样化解释”。
*   **技术融合**：巧妙结合了 CMA-ES 处理非凸系数优化问题，解决了稳健损失函数难以优化的痛点。
*   **实用性强**：引入离群值簇索引作为 BD，直接关联了领域专家的直觉（即：这个公式解释不了哪部分异常数据）。

### 8. 不足与局限
*   **计算开销**：引入 CMA-ES 和 QD 存档管理显著增加了单次迭代的计算成本，论文未详细讨论其在大规模数据集上的扩展性。
*   **聚类依赖**：离群值簇索引 BD 依赖于预先的聚类效果。如果聚类不合理，可能无法有效区分不同的残差模式。
*   **超参数敏感性**：存档的网格分辨率、CMA-ES 的参数等可能需要针对不同问题进行精细调优。
*   **搜索空间限制**：实验中最大节点数限制为 20，对于极其复杂的科学公式可能不足。

（完）
