---
title: "Likelihood scoring for continuations of mathematical text: a self-supervised benchmark with tests for shortcut vulnerabilities"
title_zh: 数学文本续写的似然评分：一个带有捷径漏洞测试的自监督基准
authors: Daniel Ranard
date: 2026-05-11
pdf: "https://arxiv.org/pdf/2605.10810v2"
tags: ["query:math-ai"]
score: 8.0
evidence: 预测数学方程的基准
tldr: 本研究提出一种自动生成的自监督基准，用于评估模型对技术论文（特别是数学公式）后续内容的预测能力。通过计算预测文本对隐藏后续内容似然度的提升，该基准无需人工标注即可区分不同模型的推理能力。实验表明，GPT-5.5等高性能模型在公式补全任务中显著优于基准，且该方法能有效识别模型利用捷径而非真实推理的漏洞，为模型评估提供了新工具。
selection_source: fresh_fetch
motivation: 旨在开发一种无需人工标注、能有效评估技术文档推理能力并识别模型“捷径”漏洞的自动化基准测试方法。
method: 采用“上下文-预测-评分”框架，通过计算评分模型在给定预测文本前后对隐藏后续内容的似然度变化来衡量预测质量。
result: 在1363个公式补全测试中，GPT-5.5等模型显著提升了似然度得分，且在对抗性微调的评分器测试下依然表现稳健。
conclusion: 跨模型似然度评分是一种有效的静态基准测试方法，可用于在强化学习或模型优化前探测模型的推理能力和捷径漏洞。
---

## 摘要
我们介绍了一个用于预测技术论文中隐藏文本的自动生成基准。论文提供可见上下文 $X$ 和隐藏续写 $Y$；被评估的模型编写一个辅助预测字符串 $Z$，而一个独立的评分器在有无 $Z$ 条件的情况下分别对 $Y$ 分配下一个 token 的概率。这提供了一种无标签测试，用于衡量 $Z$ 是否传递了关于续写的信息，并与 $Z$ 为近期上下文而非预测的对照组进行比较。我们的主要测试平台是等式后缀预测：预测器查看上下文和显示等式的第一部分，然后预测剩余部分。该任务结合了表层的 arXiv/TeX 文本建模与推理敏感型推理；由于后缀是许多大致等效的续写之一，因此该基准是从统计角度而非逐项进行解读的。在来自 138 篇近期物理和数学论文的 1363 个等式续写中，GPT-5.5、Opus 4.7 和 GPT-5.4 nano 的预测在 Qwen3-8B 和 Kimi K2.6 评分器下，相较于上下文对照组均提高了截断似然（clipped likelihood），在无需人类标签的情况下区分了模型系列和推理工作量设置。为了模拟 $Z$ 仅进一步引导评分器而非做出有用预测的捷径，我们还在仅包含上下文的提示词上对评分器进行了微调，并将其应用于留出论文作为更强的对照。GPT-5.5 的预测仍然优于这种微调后的对照；而 GPT-5.4 nano 的预测则不然。较长的散文/TeX 续写在对照组之上显示出正向但噪声较大的提升，且集中在目标内容的开头附近。这些结果支持将跨模型似然评分作为静态基准，并作为在应用强化学习或模型选择优化之前探测捷径漏洞的实验设置。

## Abstract
We introduce an automatically generated benchmark for predicting hidden text in technical papers. A paper supplies visible context $X$ and a hidden continuation $Y$; the evaluated model writes an auxiliary forecast string $Z$, and a separate scorer assigns next-token probability to $Y$ both with and without conditioning on $Z$. This gives a label-free test of whether $Z$ transmits information about the continuation, compared against controls where $Z$ is recent context rather than a forecast. Our main testbed is equation-suffix prediction: the predictor sees context and the first part of a displayed equation, then forecasts the rest. The task mixes surface-level arXiv/TeX text modeling with reasoning-sensitive inference; the suffix is one of many roughly equivalent continuations, so the benchmark is read statistically rather than item-by-item. On 1363 equation continuations from 138 recent physics and mathematics papers, forecasts from GPT-5.5, Opus 4.7, and GPT-5.4 nano all improve clipped likelihood over the context control under both Qwen3-8B and Kimi K2.6 scorers, distinguishing model families and reasoning-effort settings without human labels. To emulate shortcuts where $Z$ further primes the scorer rather than making a useful forecast, we also fine-tune the scorer on context-only prompts and apply it to held-out papers as a stronger control. GPT-5.5 forecasts still beat this fine-tuned control; GPT-5.4 nano forecasts do not. Longer prose/TeX continuations show positive but noisier lift over controls, concentrated near the beginning of the target. These results support cross-model likelihood scoring as a static benchmark and as a setup for probing shortcut vulnerabilities before reinforcement learning or model-selection optimization is applied.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种针对技术文档（特别是数学公式）续写任务的自动生成基准测试方法。以下是对该论文的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：如何在大规模、无需人工标注的情况下，客观评估语言模型在技术领域（如物理、数学）的推理能力？
*   **研究动机**：
    *   **现有评估的局限**：传统的“下一个 Token 预测”主要奖励表层建模而非深层推理；而“可验证任务”（如编程、竞赛数学）虽然要求推理，但数据稀缺且难以覆盖前沿科研领域。
    *   **评估歧义性**：技术论文的续写往往有多种等价形式，传统的字符串匹配或 LLM 打分（LLM-as-a-judge）存在主观性、风格偏好或易被操控的风险。
*   **整体含义**：提出一种基于“似然度提升”（Likelihood Lift）的自监督框架，利用一个冻结的评分模型来衡量预测文本对真实后续内容的预测贡献。

### 2. 方法论：核心思想与技术细节
*   **核心框架 (X, Y, Z)**：
    *   **X (可见上下文)**：论文中截断点之前的文本。
    *   **Y (隐藏续写)**：论文中真实的后续内容（如公式的剩余部分）。
    *   **Z (辅助预测)**：被评估模型根据 $X$ 生成的预测字符串。
*   **似然度提升 ($\Delta J$)**：使用一个独立的自回归语言模型作为评分器 $J$。计算在给定 $Z$ 的情况下，$J$ 对真实续写 $Y$ 的对数似然度，减去不给 $Z$（或给定对照组）时的似然度。
*   **关键技术细节**：
    *   **clipLL2 评分规则**：为了防止个别“灾难性 Token”（即预测虽合理但与原文微小差异导致概率骤降）主导总分，将每个 Token 的对数似然值限制在 $[-2, 0]$ 之间再取平均。
    *   **脚手架 (Scaffold)**：设计特定的提示词模板，让评分器先看一遍包含 $Z$ 的公式，再在相同位置对 $Y$ 进行评分。
    *   **捷径审计 (Shortcut Audit)**：为了防止模型通过“填充近期上下文”而非“预测”来刷分，设置了相同字符预算的上下文对照组 ($C_B$)。

### 3. 实验设计
*   **数据集**：
    *   **主基准**：从 138 篇近期 arXiv 论文（涵盖量子物理、高能物理、数学物理）中自动提取的 **1363 个等式后缀剪裁**。
    *   **扩展测试**：661 个较长的散文/TeX 混合续写片段。
*   **对比方法（被评估模型）**：
    *   **GPT-5.5**（设置了高、中、低、无四种推理强度）。
    *   **Claude Opus 4.7**（中、低推理）。
    *   **GPT-5.4 nano**（高、中、低推理）。
*   **评分器 (Scorers)**：使用了两个架构和厂商完全不同的模型：**Qwen3-8B**（阿里巴巴）和 **Kimi K2.6**（月之暗面，1T MoE 架构）。

### 4. 资源与算力
*   **算力说明**：文中**未明确说明**具体的 GPU 型号、数量或训练时长。
*   **实现方式**：实验主要通过 API 调用完成（OpenAI、Anthropic 和 Fireworks AI 的 completions 接口）。
*   **推理成本**：记录了不同推理设置下的“隐藏推理 Token”数量（如 GPT-5.5 高推理模式平均使用约 4790 个推理 Token）。

### 5. 实验数量与充分性
*   **实验规模**：
    *   进行了 1363 组等式实验和 661 组长文本实验。
    *   针对评分规则的敏感度进行了消融实验（对比了 raw LL, clipLL 3/5, sqrt-loss 等）。
    *   引入了基于 SFT（监督微调）的强力对照组来测试捷径漏洞。
*   **充分性与公平性**：实验设计较为充分。通过跨模型（不同评分器）验证，证明了信号的稳健性；通过设置“字符预算匹配”的对照组，确保了对比的公平性，排除了模型仅靠复制上下文获胜的可能性。

### 6. 主要结论与发现
*   **模型区分度**：该基准能清晰区分模型家族（GPT-5.5 > Opus 4.7 > GPT-5.4 nano）以及同一模型的不同推理强度设置。
*   **推理与性能正相关**：随着 API 报告的推理 Token 增加，似然度提升呈现对数增长趋势，符合推理模型的扩展定律。
*   **捷径抵抗力**：强模型（GPT-5.5）生成的预测信息量超越了经过微调的“仅上下文”评分器，而弱模型（GPT-5.4 nano）则无法通过这一强力对照测试。
*   **任务难度**：长文本续写比等式后缀预测更具挑战性，信号更嘈杂，且对评分器的选择更敏感。

### 7. 优点
*   **全自动且可再生**：无需人工标注，可随 arXiv 更新持续生成新任务，有效防止数据污染。
*   **抗作弊设计**：通过 `clipLL2` 和上下文对照组，针对性地解决了奖励作弊（Reward Hacking）和表层拟合问题。
*   **统计显著性**：不依赖于单个样本的绝对正确性，而是通过大量样本的似然度增量提供统计学上的能力评估。

### 8. 不足与局限
*   **领域局限性**：目前主要集中在公式密集的物理和数学领域，在非公式类技术文本上的表现尚待进一步验证。
*   **非绝对度量**：似然度提升是一个相对指标，受限于评分器本身的能力；如果评分器太弱，可能无法识别出高质量的预测。
*   **静态审计局限**：虽然通过了静态捷径测试，但如果将此信号直接作为强化学习（RL）的奖励函数，模型可能会发现更隐蔽的对抗性攻击手段。

（完）
