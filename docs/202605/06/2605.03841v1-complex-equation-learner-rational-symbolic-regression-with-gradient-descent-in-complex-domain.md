---
title: "Complex Equation Learner: Rational Symbolic Regression with Gradient Descent in Complex Domain"
title_zh: 复数方程学习器：复数域内基于梯度下降的有理符号回归
authors: "Sergei Garmaev, Maurice Gauché, Olga Fink"
date: 2026-05-05
pdf: "https://arxiv.org/pdf/2605.03841v1"
tags: ["query:sr-alg"]
score: 10.0
evidence: 复数域内基于梯度下降的有理符号回归
tldr: 本研究针对符号回归中梯度下降法难以处理除法、对数和平方根等具有奇异性或定义域限制算子的问题，提出了复数权重方程学习器。该方法通过将权重扩展至复数域，使优化轨迹能够绕过实轴上的退化点，从而解决了实值优化中的病态问题。实验证明，该方法在存在实域极点的情况下仍能稳定收敛，并支持无约束地使用多种复杂算子，在基准测试和实验频率响应数据中均成功恢复了奇异行为。
selection_source: fresh_fetch
motivation: 现有的基于梯度的符号回归方法在处理除法、对数和平方根等具有奇异性或定义域限制的算子时，容易因极点或定义域约束导致优化失败。
method: 提出一种复数权重扩展的方程学习器，利用复数域优化轨迹绕过实轴上的退化点，实现对复杂算子的稳定处理。
result: 该方法在包含实域极点的目标表达式上实现了稳定收敛，并能从实验频率响应数据中准确恢复奇异行为。
conclusion: 通过引入复数权重，符号回归能够更有效地处理具有奇异性的有理函数和复杂算子，显著扩大了其假设空间和适用范围。
---

## 摘要
符号回归旨在从数据中发现可解释的方程，但现代基于梯度的方法在处理引入奇异性或定义域限制的算子（包括除法、对数和平方根）时往往会失效。因此，方程学习器（Equation Learner）类模型通常会避开这些算子或施加限制（例如限制分母以防止极点），从而缩小了假设空间。我们提出了一种方程学习器的复数权重扩展，通过允许优化轨迹绕过实轴退化点，缓解了实值优化中的病态问题。即使目标表达式在实数域存在极点，所提方法也能稳定收敛，并支持无约束地使用对数和平方根等运算。我们在符号回归基准测试上验证了该方法，并证明其能够从实验频率响应数据中恢复奇异行为。

## Abstract
Symbolic regression aims to discover interpretable equations from data, yet modern gradient-based methods fail for operators that introduce singularities or domain constraints, including division, logarithms, and square roots. As a result, Equation Learner-type models typically avoid these operators or impose restrictions, e.g. constraining denominators to prevent poles, which narrows the hypothesis class. We propose a complex weight extension of the Equation Learner that mitigates real-valued optimization pathologies by allowing optimization trajectories to bypass real-axis degeneracies. The proposed approach converges stably even when the target expression has real-domain poles, and it enables unconstrained use of operations such as logarithm and square root. We Validate the method on symbolic regression benchmarks and show it can recover singular behavior from experimental frequency response data.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **Complex Equation Learner (CEQL)** 的新型符号回归方法。以下是对该论文的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
符号回归（SR）旨在从数据中发现可解释的数学方程。虽然基于梯度的深度学习方法（如 Equation Learner, EQL）在处理连续函数时表现良好，但在面对**除法（有理函数）、对数和平方根**等算子时会遇到严重的优化障碍：
*   **梯度病态（Gradient Pathologies）：** 在实数域优化中，分母接近零会导致梯度爆炸或正负梯度相互抵消，使得模型难以收敛到正确的极点结构。
*   **定义域限制：** 对数和平方根在实数域有严格的非负限制，优化过程中的中间值一旦越界，前向计算就会中断。
*   **现有局限：** 传统方法通常通过限制分母符号或使用“安全除法”来规避，但这极大地缩小了模型能够搜索的方程范围。

### 2. 论文提出的方法论
**核心思想：** 将方程学习器的权重扩展到**复数域（Complex Domain）**。通过在复空间进行优化，训练轨迹可以“绕过”实轴上的奇异点（退化点），从而实现稳定的梯度下降。

**关键技术细节：**
*   **复数参数化：** 网络权重 $w$ 为复数，优化空间从 $\mathbb{R}^p$ 扩展到 $\mathbb{C}^p$。
*   **代理算子（Surrogate Operators）：**
    *   **除法：** 定义为 $bdiv(x, y) = \Re(\Re(x)/y)$，利用复数分母提供的额外自由度来平滑损失景观。
    *   **多值函数：** 使用复数对数和复数平方根，它们在复平面上（除支割线外）处处可导，避免了实域定义域受限的问题。
*   **三阶段优化策略：**
    1.  **初始训练：** 引入稀疏惩罚和虚部惩罚（将参数拉回实轴）。
    2.  **剪枝阶段：** 移除小权重连接，简化网络拓扑。
    3.  **精调阶段：** 禁用稀疏正则化，对剩余参数进行高精度微调。
*   **架构改进：** 引入了跳跃连接（Skip Connections），允许模型根据需要选择更短的计算路径，简化方程结构。

### 3. 实验设计
*   **合成基准测试（E-1 到 E-10）：** 包含线性、多项式、有理函数（带极点）、对数和平方根方程。
*   **真实物理场景：** 悬臂钢梁的**频率响应函数（FRF）**发现。目标是根据损伤程度预测共振峰值的移动。
*   **对比方法（Baselines）：**
    *   **PySR：** 基于遗传算法的状态最先进（SOTA）符号回归工具。
    *   **SINDy：** 基于稀疏线性回归的方法。
    *   **EQL$\div$：** 带有除法算子的标准实值方程学习器。
*   **评估指标：** 插值 MSE（域内）、外推 MSE（域外，验证真实物理规律恢复情况）和节点数（NC，衡量简洁度）。

### 4. 资源与算力
*   论文**未明确说明**具体的 GPU 型号或训练时长。
*   **训练参数：** 使用 Adam 优化器，总训练轮数最高达 350,000 轮（分布在三个阶段中）。
*   **计算特性：** 由于采用固定架构的连续优化而非组合搜索，其在大规模输入维度下的扩展性理论上优于基于遗传算法的方法。

### 5. 实验数量与充分性
*   **实验规模：** 针对 10 个复杂的合成基准每个进行了 5 次独立运行，并提供了均值和标准差。
*   **充分性：** 实验设计较为全面，涵盖了从简单多项式到带奇异点的复杂有理函数。
*   **客观性：** 引入了外推测试集，这是衡量符号回归是否真正发现“底层公式”而非仅仅是“曲线拟合”的关键标准。对比了三种不同技术路线的代表性方法，实验结果具有说服力。

### 6. 论文的主要结论与发现
*   **复数优化的有效性：** CEQL 成功解决了实值优化中因分母零点导致的梯度抵消问题，能够稳定地发现带极点的有理方程。
*   **性能优势：** 在处理非线性、有理项和定义域受限的算子时，CEQL 显著优于 SINDy 和 EQL$\div$。
*   **物理发现能力：** 在钢梁实验中，CEQL 准确捕捉到了共振峰随损伤变化的非线性位移，证明了其在处理真实实验数据中的实用性。
*   **外推能力：** CEQL 发现的方程在训练域外仍保持极低误差，证明其恢复了正确的数学结构。

### 7. 优点
*   **数学优雅性：** 利用复分析视角解决实域优化难题，思路新颖且具有理论支撑。
*   **算子解禁：** 真正实现了除法、对数等算子的无约束梯度优化，扩大了符号回归的搜索空间。
*   **结构简洁：** 相比于遗传算法，基于梯度的框架更容易集成到现代深度学习流水线中。

### 8. 不足与局限
*   **周期性函数挑战：** 论文提到三角函数（sin/cos）仍然难以处理，因为其剧烈的振荡会导致梯度信号在全域平均时相互抵消。
*   **稀疏性控制敏感：** 剪枝策略和正则化系数的选择对最终方程的简洁度影响较大，过度剪枝可能误删正确项。
*   **初始化依赖：** 尽管复数域改善了景观，但对于极其复杂的嵌套结构，优化仍可能陷入局部最优。

（完）
