Title: Neural networks as fuzzy logic formulas

URL Source: https://arxiv.org/pdf/2605.03064v1

Published Time: Wed, 06 May 2026 00:10:31 GMT

Number of Pages: 38

Markdown Content:
# Neural networks as fuzzy logic formulas 

Damian Heiman 1, Antti Kuusisto 1, and Esko Turunen 11Mathematics Research Centre, Tampere University, Tampere, Finland 

Abstract 

Neural networks are a fundamental aspect of modern artificial intelligence, playing a key role in various important machine learning architectures including transformers and graph neural networks. Recently, logical characterisations have been used to study the expressive power of many machine learning architectures, but logical characterisations of plain neural networks have received less attention. In this paper, we provide fuzzy logic characterisations of rational-weight ReLU -activated neural networks via two well-established fuzzy logics: Rational Pavelka Logic RPL (and extensions thereof) and (fragments of) LΠ 12 . The activation values of the neural networks are allowed to be arbitrary real numbers. We also provide fuzzy logic characterisations of a generalised polynomial ring over Q in countably many variables where the use of the ReLU -function is permitted. 

# 1 Introduction 

In this paper, we study the expressive power of neural networks via logical characterisa-tions. While there have been numerous logical characterisations of other machine learning architectures such as graph neural networks and transformers in recent years, plain neural networks have received less attention. We address this by giving multiple fuzzy logic char-acterisations of neural networks. We also give a fuzzy logic characterisation of a generalised polynomial ring that subsumes neural networks. Neural networks are directed graphs where each vertex (or neuron) obtains an activation value (a real number) either as an input or by aggregating the activation values of its in-neighbours. The aggregation scheme we consider is a standard one, i.e., we take a weighted sum of the activation values of in-neighbours, add a bias and apply the activation function ReLU : R → [0 , ∞[, ReLU( x) = max {0, x }. The neural networks we consider are feedforward (i.e., there are no directed cycles in the graph) and fully connected (i.e., the neurons are partitioned into layers, and from each neuron of a layer there is an edge to exactly every neuron of the next layer), and the weights and biases are rational numbers. An illustration of a neural network is given in Figure 1. On the formal level, we identify a neuron with its aggregation formula broken down recur-sively to the level of input neurons, and we subsequently identify a neural network with the tuple of its output neurons. This makes neurons a special case of polynomials of the poly-nomial ring Q[xi]i∈N (i.e., the ring of polynomials over rational numbers in countably many variables) extended with the ReLU -function. We refer to this extended ring of polynomials as Q[ReLU , x i]i∈N, and we refer to its ReLU -polynomials as terms. Neural networks are a subclass of tuples of terms of Q[ReLU , x i]i∈N.1

> arXiv:2605.03064v1 [cs.LO] 4 May 2026

i5

i4

i3

i2

i1

h7

h6

h5

h4

h3

h2

h1

o3

o2

o1

Figure 1: A visualization of a neural network consisting of three layers: an input layer 

(i1, . . . , i 5), a hidden layer (h1, . . . , h 7) and an output layer (o1, o 2, o 3).We study the expressive power of Q[ReLU , x i]i∈N and of neural networks via two fuzzy logics that can express rational numbers and multiplication. The first of these is the logic 

RPL( ⊙) [16], which is an extension of Rational Pavelka Logic ( RPL ) [15, 26, 27, 28] with the product conjunction ⊙, while RPL extends the classic infinite-valued Łukasiewicz logic [19] by adding a truth constant for every rational number between 0 and 1. The other fuzzy logic we consider is the logic LΠ 12 [11] which combines standard Łukasiewicz logic with Product Logic (adding the product conjunction ⊙ and the product implication →P ) and adds the truth constant 12 . The logic LΠ 12 allows the construction of formulae equivalent to rational numbers between 0 and 1 by using the product implication →P which corresponds to division. However, the connective →P also increases the expressive power of LΠ 12 beyond that of RPL( ⊙). We therefore restrict to the fragment LΠ 12 (→− 

> P

) which limits LΠ 12 by not allowing proposition symbols in the scope of →P .As the values of terms of Q[ReLU , x i]i∈N and the activation values of neurons are allowed to be arbitrary reals and the truth values of fuzzy logic lie between 0 and 1, it is obviously impossible in the general case to find a formula of fuzzy logic whose truth values would be identical to the values of a given term of Q[ReLU , x i]i∈N. Therefore, we naturally scale the activation values down to the interval [0 , 1] when translating to the logics. This is the most technical part of the translation, as multiplication behaves differently in the interval [0 , 1] 

compared to arbitrary intervals [−k, k ]. Our first theorem is as follows: 

Theorem 3.1. Q[ReLU , x i]i∈N has the same expressive power (w.r.t. scaling) as RPL( ⊙)

and LΠ 12 (→− 

> P

).

Theorem 3.1 also holds when we restrict the degrees of the ReLU -polynomials and apply a corresponding restriction on the logic side. This involves defining fragments RPL( ⊙)≤d and 

LΠ 12 (→− 

> P

)≤d of RPL( ⊙) and LΠ 12 (→− 

> P

), respectively, which limit how proposition symbols are allowed to appear in the scope of the product conjunction ⊙. We obtain the following result: 

Theorem 3.2. For each d ∈ N, Q[ReLU , x i]i∈N with degree bound d has the same expressive power (w.r.t. scaling) as RPL( ⊙)≤d and LΠ 12 (→− 

> P

)≤d.

2Table 1: A summary of our results. The symbol ≡ denotes same expressive power (subject to scaling when translating from left to right). 

Q[ReLU , x i]i∈N ≡ RPL( ⊙), LΠ 12 (→− 

> P

)

Q[ReLU , x i]i∈N with degree bound d ≡ RPL( ⊙)≤d, LΠ 12 (→− 

> P

)≤d

Neural networks ≡ RPL( ⊙)≤1, LΠ 12 (→− 

> P

)≤1, RPL , LΠ 12 (⊙−, →− 

> P

)

When characterising neural networks, we consider on the one hand the fragments RPL( ⊙)≤1

and LΠ 12 (→− 

> P

)≤1, which restrict the respective logics RPL( ⊙) and LΠ 12 (→− 

> P

) by demanding that all proposition symbols in the scope of a product conjunction ⊙ appear on the same side of the connective. On the other hand, we also consider RPL and LΠ 12 (⊙−, →− 

> P

), where 

LΠ 12 (⊙−, →− 

> P

) limits both ⊙ and →P by not allowing proposition symbols in the scope of either connective. We find that we can translate RPL( ⊙)≤1 and LΠ 12 (→− 

> P

)≤1 into neural networks and that we can translate neural networks into RPL and LΠ 12 (⊙−, →− 

> P

). Our third result is then given as follows: 

Theorem 3.5. Neural networks have the same expressive power (w.r.t. scaling) as the logics RPL( ⊙)≤1, LΠ 12 (→− 

> P

)≤1, RPL and LΠ 12 (⊙−, →− 

> P

).

Note that in the results listed above, the notion of expressive power is tied to scaling; it does not mean that we could, e.g., translate each formula φ of RPL( ⊙)≤1 into a formula 

φ′ of RPL such that φ′ would always receive the same truth value as φ. However, it does 

follow that we could obtain a formula φ′ of RPL that arithmetically simulates the truth values obtained by φ. A summary of our fuzzy logic characterisations is given in Table 1. In Appendix F, we discuss why no analogous characterisation can be given via infinite-valued Łukasiewicz logic. 

Related work 

Past characterisations of neural networks have been obtained via representation theorems, showing that a given extension of Łukasiewicz logic is capable of expressing exactly all Mc-Naughton functions with appropriate restrictions placed on the coefficients. For example, a well-known result by McNaughton [23] states that plain Łukasiewicz logic can express exactly all McNaughton functions with integer coefficients, linking Łukasiewicz logic to neural networks that use the truncated identity f (x) = min {1, max {0, x }} (also known as the truncated ReLU ) as the activation function. Amato, Di Nola and Gerla [4] gave a fuzzy logic characterisation for rational-parameter neural networks via Rational Łukasiewicz logic via McNaughton functions with rational coefficients. An analogous result was given by Di Nola, Gerla and Leustean in [9], connecting Łukasiewicz logic extended with multiplication with reals and neural networks of the above kind with real weights and biases via a represen-tation theorem relating to McNaughton functions with real coefficients. Unlike the present paper, the activation values of the neural networks in the above characterisations, inputs and outputs included, were in the range [0 , 1] . The activation function was the truncated identity function, whereas the present paper uses the ordinary ReLU -function. Moreover, the above articles did not consider Q[ReLU , x i]i∈N or any analogous generalisation of neural networks. There also exists a characterisation of neural networks via fuzzy logic that is not quite a straightforward logical characterisation. Castro and Trillas [7] gave a characterisation of 3neural networks that use real parameters and apply a squashing function on every layer via linear combinations of squashed transformations of propositions of Łukasiewicz logic. In contrast to fuzzy logic characterisations, non-fuzzy logical characterisations of neural networks trace back to McCulloch and Pitts [22], who characterised neural networks with Boolean activation values and threshold activation functions via temporal propositional expressions. Much more recently, Ahvonen, Heiman and Kuusisto [1] gave a logical charac-terisation of recurrent neural networks operating with floating-point numbers via a recursive propositional logic called Boolean Network Logic (BNL). The same authors also gave an analogous characterisation of feedforward neural networks in [2]. Whereas the activation values, inputs and outputs included, are Boolean in [22] and floating-point numbers in [1, 2], and whereas the truth values in the characterising logics are Boolean, in the current paper all activation values are real numbers, and likewise the truth values of the characterising fuzzy logics are reals from the interval [0 , 1] .Regarding connections between LΠ 12 and real algebra, Marchioni and Montagna [21] gave a constructive translation from terms of the language of ordered fields to terms of LΠ 12 -algebra. This bears a close similarity to Lemma D.1 of the present paper, where we give a constructive translation from terms of Q[ReLU , x i]i∈N to formulae of the logic LΠ 12 (→− 

> P

).As in the current paper, the correspondence between terms and their translations was shown by the semantics of the field of reals and the canonical semantics of LΠ 12 -algebras over the interval [0 , 1] . The translation in [21] is uniform, based on a single non-linear function that bijectively maps the real interval ]0 , 1[ to R, and it was used to further interpret the universal theory of the reals in the equational theory of LΠ 12 -algebras. By contrast, the translation in the current paper is based on a linear function, but it is non-uniform; for each positive integer k, we use a separate linear function that maps the interval [−k, k ]

to the interval [0 , 1] . In Lemma D.2, we provide a reverse translation from LΠ 12 (→− 

> P

)

to Q[ReLU , x i]i∈N, but no reverse translation from terms of LΠ 12 -algebra to terms of the language of ordered fields was given in [21]. Also, Q[ReLU , x i]i∈N is more expressive than the language of ordered fields as it also includes rational constants and the function ReLU ,and likewise, our logic LΠ 12 (→− 

> P

) is less expressive than the logic LΠ 12 or the corresponding 

LΠ 12 -algebra used in [21], i.e., we can express a wider variety of terms in a weaker logic. Recently, there have been many logical characterisations of graph neural networks (GNNs). In [5], Barceló et al. gave a logical characterisation of GNNs in restriction to first-order logic via graded modal logic. They also showed that every property expressible by a formula of two-variable first-order logic with counting is also expressible by a GNN with global readout. Recently in [17], Hauke and Wałęga showed that the reverse does not hold. In the wake of [5], there have been numerous other (non-fuzzy) logical characterisations of GNNs [14, 6, 25, 13, 31, 33, 20] and of recurrent GNNs [29, 3, 32], and the lists sampled here are still growing. There are various studies extending fuzzy logics with truth constants that generalise the ordinary truth constants ⊤ (“always true”) and ⊥ (“always false”) used in Boolean logics. While the Łukasiewicz implication φ →L ψ is valid exactly when the truth value of ψ is at least as great as the truth value of φ in every valuation, the inclusion of truth constants r for all r ∈ S for some S ⊆ [0 , 1] allows the truth degrees of formulae to be studied independently. In particular, it allows formulae r →L ψ which are valid exactly when the truth value of ψ is at least as great as r (again in every valuation). This allows generalising the notions of truth and provability of formulae to cover various degrees of truth and provability. Pavelka-style completeness is when these generalised notions of truth and provability coincide, named after Jan Pavelka who showed in [26, 27, 28] that Łukasiewicz logic extended with a truth 4constant for each real in [0 , 1] , called Pavelka Logic, enjoys this property. While the number of truth constants makes the language of Pavelka Logic nondenumerable, Hájek showed in [15] that Rational Pavelka Logic, obtained from Pavelka Logic by including truth constants only for rational numbers in [0 , 1] , also enjoys Pavelka-style completeness. In [16], Hájek also showed Pavelka-style completeness for the logic RPL( ⊙) obtained from Rational Pavelka Logic by adding the product conjunction. Extensions of other fuzzy logics with truth constants have been studied, e.g., [12, 30, 10, 11, 18]. So far, truth constants do not appear to have a status other than as a technical means to calibrate degrees of truth., i.e., they do not appear to hold any independent status. By contrast, truth constants play a central role in the present paper, and our results establish a natural connection between truth constants and the weights and biases of neural networks. 

# 2 Preliminaries 

Let N denote the set of non-negative integers, Z the set of integers, Q the set of rational numbers and R the set of real numbers. We let N+, Z+, Q+ and R+ denote the restrictions of N, Z, Q and R to positive numbers. Given a, b ∈ R such that a ≤ b, we let [a, b ]

denote the closed interval { x ∈ R | a ≤ x ≤ b }. Likewise, we define ]a, b [ := [ a, b ] \ { a, b },

]a, b ] := [ a, b ] \ { a} and [a, b [ := [ a, b ] \ { b}. As a special case, we let [0 , ∞[ denote the set 

R+ ∪ { 0}.Let PROP = { pi | i ∈ N } be a countably infinite set of proposition symbols p0, p 1, . . . and let VAR = { xi | i ∈ N } be a countably infinite set of variables x0, x 1, . . . . We may use metasymbols p, q, r, . . . to denote proposition symbols in PROP and x, y, z, . . . to denote variables in VAR . We fix a bijection b : PROP → VAR , and for each p ∈ PROP we let xp

denote b(p), and for each x ∈ VAR we let px denote b−1(x).A valuation is a function V : PROP → [0 , 1] assigning a value V (p) ∈ [0 , 1] to each proposition symbol p ∈ PROP . Intuitively, V (p) is the “degree of truth” of the proposition symbol p. An evaluation map is a function E : VAR → R that assigns a real value 

E(x) ∈ R to each variable x ∈ VAR .

2.1 Q[ReLU , x i]i∈N

We begin by defining a generalised ring structure that our neural networks will be embedded in. It is based on the polynomial ring Q[xi]i∈N, i.e., the polynomial ring over Q with countably many variables. We do not require these polynomials to be in a reduced form; for example, we consider both x(y + z) and xy + xz to be valid polynomial expressions. We only make one modification to Q[xi]i∈N: we add the function ReLU : R → [0 , ∞[,

ReLU( x) = max {0, x } as another building block. We call such modified polynomials ReLU -polynomials or terms , and the structure consisting of them as Q[ReLU , x i]i∈N.More formally, ReLU -polynomials or terms of Q[ReLU , x i]i∈N are constructed recursively as follows: 

• For each r ∈ Q, r is a term. 

• Each x ∈ VAR is a term. 

• If t and t′ are terms, then (t + t′) and (t · t′) are terms. 5• If t is a term, then ReLU( t) is a term. When there is no danger of confusion, we omit the parentheses from (t + t′) and (t · t′). For brevity, we will usually write tt ′ to denote t · t′. We will also write −t to denote −1 · t and 

t − t′ to denote t + ( −t′).We interpret terms of Q[ReLU , x i]i∈N over the field of real numbers as follows. An evaluation map E fixes a value for each term when +, · and ReLU are given natural interpretations over the field of reals. 

• If t = r for some r ∈ Q, then E(t) = r.

• If t = t′ + t′′ for some terms t′ and t′′ , then E(t) = E(t′) + E(t′′ ).1

• If t = t′ · t′′ for some terms t′ and t′′ , then E(t) = E(t′)E(t′′ ).

• If t = ReLU( t′) for some term t′, then E(t) = max {0, E (t′)}.For each k ∈ Z+ and terms t1, . . . , t k, we write Pki=1 ti to denote the term t1 + · · · + tn.The omitted parentheses in the sum can be placed in some canonical way, but given the semantics above, + is associative and thus it does not make a difference how the parentheses are placed. 

Example 2.1. Let t := ReLU( x + xy ) + y. Now t is a term of Q[ReLU , x i]i∈N, and given an evaluation map E such that E(x) = 3 and E(y) = 0 .5, we have 

E(t) = max {0, 3 + 3 · 0.5} + 0 .5 = max {0, 4.5} + 0 .5 = 4 .5 + 0 .5 = 5 .

We say that terms t and t′ are equivalent if E(t) = E(t′) for each evaluation map E,and we say that t and t′ are [0 , 1] -equivalent if E(t) = E(t′) for each evaluation map E

such that E(x) ∈ [0 , 1] for each x ∈ VAR . For two tuples of terms T = ( t1, . . . , t k) and 

T ′ = ( t′

> 1

, . . . , t ′

> k

) of equal length, we say that T and T ′ are equivalent if the terms ti and 

t′ 

> i

are equivalent for each i ∈ { 1, . . . , k }, and we say that T and T ′ are [0 , 1] -equivalent if 

ti and t′ 

> i

are [0 , 1] -equivalent for each i ∈ { 1, . . . , k }.

Example 2.2. The terms x + y and 12 x + 12 x + 2 y − y are equivalent. The terms x and 

ReLU( x) are not equivalent: if E is an evaluation map such that E(x) < 0, then we get 

E(ReLU( x)) = max {0, E (x)} = 0 ̸ = E(x).Next, we define three natural notions related to terms: degree, atomic length and subterm. Intuitively, degree relates to the maximum number of times that a variable is multiplied with another variable, whereas atomic length refers to the number of atomic terms contained in a term, and subterm refers to vertices in the syntax tree of a term. The formal definitions will now follow. The degree of a term t, denoted deg( t), is defined recursively as follows:           

> 1Here and throughout the paper, we abuse notation and use +and ·to denote both a symbol in the syntax of Q[ReLU , x i]i∈Nand the standard sum and multiplication over the field of real numbers. We could use different symbols like ⊞and in the syntax to differentiate the syntax and the semantics, but this would be more likely to increase confusion than decrease it. In the case of rationals however, it is useful to make the difference between syntax and semantics explicit, because a rational number can be represented in infinitely many ways, all of which can be covered with a single constant symbol.

6• If t = r for some r ∈ Q, then deg( t) = 0 .

• If t = x for some x ∈ VAR , then deg( t) = 1 .

• If t = t′ + t′′ for some terms t′ and t′′ , then deg( t) = max {deg( t′), deg( t′′ )}.

• If t = t′t′′ for some terms t′ and t′′ , then deg( t) = deg( t′) + deg( t′′ ).

• If t = ReLU( t′) for some term t′, then deg( t) = deg( t′).When we discuss Q[ReLU , x i]i∈N with degree bound d ∈ N, we mean the collection of exactly those terms t of Q[ReLU , x i]i∈N for which deg( t) ≤ d.Likewise, we define the atomic length of a term t, denoted by length( t), as follows: 

• If t = r for some r ∈ Q or t = x for some x ∈ VAR , then length( t) = 1 .

• If t = t′ + t′′ or t = t′t′′ for terms t′ and t′′ , then length( t) = length( t′) + length( t′′ ).

• If t = ReLU( t′) for some term t′, then length( t) = length( t′).We define the set subt( t) of subterms of a term t as follows: 

• If t = r for some r ∈ Q or t = x for some x ∈ VAR , then subt( t) = {t}.

• If t = t′ + t′′ or t = t′t′′ for terms t′ and t′′ , then subt( t) = subt( t′) ∪ subt( t′′ ) ∪ { t}.

• If t = ReLU( t′) for some term t′, then subt( t) = subt( t′) ∪ { t}.

2.2 Neural networks 

Next, we discuss neural networks. We begin by defining an auxiliary notion of a proto-neuron, which we will use throughout the paper. Then we define neural networks and neurons. Proto-neurons resemble neurons, but offer a more flexible syntax to work with, which is useful when constructing translations. First, we consider proto-neurons, which are intuitively terms of Q[ReLU , x i]i∈N of degree at most 1. Syntactically, proto-neurons are constructed recursively as follows: 

• For each r ∈ Q, r is a proto-neuron. 

• Each x ∈ VAR is a proto-neuron. 

• If t and t′ are proto-neurons, then t + t′ is a proto-neuron. 

• If t and t′ are proto-neurons such that deg( t) + deg( t′) ≤ 1, then tt ′ is a proto-neuron. 

• If t is a proto-neuron, then ReLU( t) is a proto-neuron. 

Lemma 2.3. For all terms t of Q[ReLU , x i]i∈N, t is a proto-neuron if and only if deg( t) ≤ 1.Proof. We prove the claim by induction over the structure of t; the details are in Ap-pendix A. 7Example 2.4. The term 3( x + y) is a proto-neuron, but the term (x + 3) y is not. In general, t is a proto-neuron if there is no subterm t′t′′ of t and subterms x ∈ VAR of t′ and 

y ∈ VAR of t′′ .Proto-neurons, as the name suggests, resemble the neurons of a neural network. However, neurons only constitute a subclass of proto-neurons. Next, we introduce the concept of a neuron, which is closely related to the architecture of neural networks, which we shall define simultaneously. For an intuitive description of neural networks and neurons, see Section 1. Given d ∈ N, a rational-parameter ReLU -activated neural network of depth d is defined recursively as follows. A neural network of depth 0 is a tuple (y1, . . . , y k) where 

k ∈ Z+ and y1, . . . , y k are distinct variables in VAR . Next, assume we have defined neural networks of depth d. A neural network of depth d + 1 is a tuple N = ( n1, . . . , n k) where 

k ∈ Z+ and each nj is a proto-neuron of the form ReLU 

Pℓi=1 wi,j n′ 

> i

+ bj



where ℓ ∈ Z+,

wi,j , b j ∈ Q and (n′

> 1

, . . . , n ′

> ℓ

) is a neural network of depth d. Note that the value of ℓ and the neurons n′

> 1

, . . . , n ′ 

> ℓ

are the same regardless of j. The components n1, . . . , n k of a neural network are called neurons ; in particular, they are the output neurons of N . Each wi,j 

is called the weight from the neuron n′ 

> i

to the neuron nj ,2 and each bj is called the bias of the neuron nj . From the definition, we see that a neural network of depth d is built on top of a neural network of depth d − 1 by adding additional neurons; thus, each neural network of depth d has embedded within it a neural network of each depth 0, . . . , d . We say that two neural networks of depth at least d are identical up to depth d if their embedded neural networks of depth d are identical. 

Example 2.5. The proto-neuron −x is not a neuron and neither is ReLU(2 x) + ReLU(3 y),but the following equivalent proto-neuron is a neuron: 

ReLU  1 · ReLU(2 x + 0 y + 0) + 1 · ReLU(0 x + 3 y + 0) + 0  .

2.3 Logics 

In this section, we introduce the variety of fuzzy logics that will be used to characterise 

Q[ReLU , x i]i∈N and neural networks. Key roles here are played by Rational Pavelka Logic 

RPL as well as another fuzzy logic called LΠ 12 . Both of these are extensions of Łukasiewicz logic, which we will define first. 

2.3.1 Łukasiewicz logic 

The first logic we define is Łukasiewicz logic, which is syntactically analogous to propo-sitional logic but interpreted differently. While the canonical semantics for propositional logic gives each formula a truth value 0 or 1, the canonical semantics for infinite-valued Łukasiewicz logic generalises this, giving each formula a truth value from the interval [0 , 1] .Under this interpretation, certain arithmetic operations like sum and subtraction are de-finable, though truncated at the end points 0 and 1 of the interval.   

> 2Technically, there may be multiple such weights if some of the neurons n′
> 1, . . . , n ′
> ℓare identical to each other, since our definition of a neuron simply refers to an aggregation formula, but in such situations we naturally consider the identical neurons to be distinct from each other since they occupy separate positions in the neural network.

8The PROP -formulae of infinite-valued Łukasiewicz logic [19] (also called Łukasiewicz logic or the Łukasiewicz-Tarski logic) are defined by the following grammar: 

φ ::= 0 | p | φ →L φ, 

where p ∈ PROP . Note that similar to terms of Q[ReLU , x i]i∈N, formulae are also terms, only of a different language, but we call them formulae to keep the terminologies distinct from each other. The semantics of infinite-valued Łukasiewicz logic is defined over MV-algebras [8, 24]. Rather than presenting the full variety of MV-algebras, we present the semantics given by the canonical MV-algebra defined over the real interval [0 , 1] which satisfies all the ax-ioms of Łukasiewicz logic. Let V : PROP → [0 , 1] be a valuation. We extend the function 

V to determine the truth value of any formula of Łukasiewicz logic as follows: 

• V (0) := 0 .

• V (φ →L ψ) := min {1, 1 − V (φ) + V (ψ)} for all formulae φ and ψ of Łukasiewicz logic. For the reader unfamiliar with fuzzy logics, it may be useful to check how the connective 

→L and subsequent connectives behave when V (φ), V (ψ) ∈ { 0, 1}. In the case of →L, such an examination retrieves the canonical Boolean semantics for implication. We define the following abbreviated connectives and give the resulting corresponding truth value of the formula in the valuation V (the details are left for the reader): 

¬Lφ := φ →L 0, V (¬Lφ) = 1 − V (φ),φ ⊕ ψ := ¬Lφ →L ψ, V (φ ⊕ ψ) = min {1, V (φ) + V (ψ)},φ ⊗ ψ := ¬L(φ →L ¬Lψ), V (φ ⊗ ψ) = max {0, V (φ) + V (ψ) − 1},φ ⊖ ψ := φ ⊗ ¬ Lψ, V (φ ⊖ ψ) = max {0, V (φ) − V (ψ)},φ ∧ ψ := φ ⊗ (φ →L ψ), V (φ ∧ ψ) = min {V (φ), V (ψ)},φ ∨ ψ := ( φ →L ψ) →L ψ, V (φ ∨ ψ) = max {V (φ), V (ψ)},φ ↔L ψ := ( φ →L ψ) ∧ (ψ →L φ), V (φ ↔L ψ) = 1 − | V (φ) − V (ψ)|.

These abbreviated connectives are also definable in the same way in all the extensions of Łukasiewicz logic that we consider in this paper. 

2.3.2 Rational Pavelka Logic 

Next, we define Rational Pavelka Logic ( RPL ), which extends Łukasiewicz logic by adding a truth constant for each rational number in the interval [0 , 1] . Pavelka logic was originally introduced in [26, 27, 28], where a truth constant was included for each real number in the interval [0 , 1] including for irrational numbers. Later in [15] the rational version of Pavelka Logic was introduced, where truth constants are only included for rational numbers. The PROP -formulae of Rational Pavelka Logic ( RPL ) are defined by the following grammar: 

φ ::= r | p | φ →L φ, 

where r ∈ Q ∩ [0 , 1] and p ∈ PROP .9The canonical semantics of RPL over the real interval [0 , 1] extends the canonical semantics of Łukasiewicz logic as follows. Let V : PROP → [0 , 1] be a valuation. The semantics for proposition symbols and →L is defined as for Łukasiewicz logic. For truth constants, we define 

• V (r) := r for each r ∈ Q ∩ [0 , 1] .

2.3.3 RPL( ⊙)

In [16], Hájek introduced the logic RPL( ⊙), which extends RPL with the product conjunc-tion ⊙.3 As the name suggests, the product conjunction allows multiplying the truth value of a formula with the truth value of another formula. The PROP -formulae of RPL( ⊙) are defined over the following grammar: 

φ ::= r | p | φ →L φ | φ ⊙ φ, 

where r ∈ Q ∩ [0 , 1] and p ∈ PROP .The canonical semantics of RPL( ⊙) over the real interval [0 , 1] extends the semantics of 

RPL as follows. Let V : PROP → [0 , 1] be a valuation. The semantics for truth constants, proposition symbols and →L is defined as in RPL . For ⊙, we define 

• V (φ ⊙ ψ) := V (φ)V (ψ) for all formulae φ and ψ of RPL( ⊙).

2.3.4 LΠ 12

The logic LΠ 12 is another extension of Łukasiewicz logic. Rather than adding a truth constant for each rational number in [0 , 1] like RPL and RPL( ⊙), LΠ 12 only adds one for  

> 12

. On top of the product conjunction ⊙, LΠ 12 adds the corresponding residual division connective: the product implication →P .The PROP -formulae of LΠ 12 are obtained by the following grammar: 

φ ::= 0 | 12 | p | φ →L φ | φ ⊙ φ | φ →P φ, 

where p ∈ PROP .The canonical semantics of LΠ 12 over [0 , 1] is defined as follows. Let V : PROP → [0 , 1] be a valuation. The semantics for truth constants, proposition symbols, →L and ⊙ is defined as in RPL( ⊙). The semantics of →P is defined as follows. Given formulae φ and ψ of LΠ 12 ,

V (φ →P ψ) := 

(1, if V (φ) ≤ V (ψ)   

> V(ψ)
> V(φ)

, otherwise .

Note that V (φ) = 0 implies V (φ) ≤ V (ψ), which means that we do not need to worry about the denominator V (φ) in V (ψ)  

> V(φ)

going to zero. It is shown in [11] that for each r ∈ Q ∩ [0 , 1] , we can construct a formula r of LΠ 12 without proposition symbols such that V (r) = r for each valuation V . We show an alternative construction in Appendix B.           

> 3In the literature on MV-algebras, the connective ⊙commonly refers to the dual operator of ⊕denoted in this paper by ⊗. In this work, ⊙denotes a primitive connective that is not derived from ⊕or →L.

10 2.3.5 Fragments of RPL( ⊙) and LΠ 12

In this section, we define various syntactic fragments of LΠ 12 and RPL( ⊙). We first estab-lish two fragments of LΠ 12 that place limitations on the use of the connectives ⊙ and →P .Then, we define a hierarchy of fragments of LΠ 12 and RPL( ⊙), starting with fragments containing no proposition symbols and fragment-by-fragment allowing more proposition symbols nested within product conjunctions ⊙.We start by defining an auxiliary fragment of LΠ 12 that intuitively consists of exactly the formulae of LΠ 12 that do not contain proposition symbols. More formally, the formulae of LΠ 12 ≤0 are constructed according to the following grammar: 

φ ::= 0 | 12 | φ →L φ | φ ⊙ φ | φ →P φ. 

Next, we define a fragment of LΠ 12 that intuitively restricts LΠ 12 by not allowing proposi-tion symbols to appear in the scope of the connectives ⊙ and →P . The PROP -formulae of LΠ 12 (⊙−, →− 

> P

) are constructed according to the following grammar: 

φ ::= ψ | p | φ →L φ, 

where ψ is a formula of LΠ 12 ≤0 and p ∈ PROP .Analogously, we define a fragment of LΠ 12 that only restricts the use of →P . The PROP -formulae of LΠ 12 (→− 

> P

) are constructed according to the following grammar: 

φ ::= ψ | p | φ →L φ | φ ⊙ φ, 

where ψ is a formula of LΠ 12 ≤0 and p ∈ PROP .Next, we define a hierarchy of syntactic fragments of both RPL( ⊙) and LΠ 12 (→− 

> P

).First, we introduce the fragments RPL( ⊙)≤0 and LΠ 12 (→− 

> P

)≤0, which consist of all formulae of RPL( ⊙) and LΠ 12 (→− 

> P

) respectively that do not contain proposition symbols. In the case of LΠ 12 (→− 

> P

), the formulae of LΠ 12 (→− 

> P

)≤0 are simply those of the logic LΠ 12 ≤0 defined above. The formulae of RPL( ⊙)≤0 are obtained according to the following grammar: 

φ ::= r | φ →L φ | φ ⊙ φ, 

where r ∈ Q ∩ [0 , 1] .Next, let L be either RPL( ⊙) or LΠ 12 (→− 

> P

) and assume we have defined the fragment L≤n

where n ∈ N. The PROP -formulae of L≤n+1 are obtained according to the following grammar: 

φ ::= p | ψ | α ⊙ β | φ →L φ, 

where p ∈ PROP , ψ is a formula of L≤n, and α and β are formulae of L≤i and L≤j

respectively for any i, j ∈ N such that i + j ≤ n + 1 . If n ≥ 1, then the atomic proposition symbols p could be omitted from the grammar, as they would be implicitly included with 

ψ. The fragments L≤0, L≤1, . . . form a hierarchy where each fragment is subsumed by the next. 

Lemma 2.6. Let L be RPL( ⊙) or LΠ 12 (→− 

> P

). Let i, j ∈ N, let φ be a formula of L≤i and let ψ be a formula of L≤j . Then the following hold. 

11 • For each r ∈ Q∩[0 , 1] , r is a formula of RPL( ⊙)≤0 and r is a formula of LΠ 12 (→− 

> P

)≤0.

• Each p ∈ PROP is a formula of L≤1.

• ¬Lφ is a formula of L≤i.

• φ →L ψ, φ ⊕ ψ, φ ⊗ ψ and φ ⊖ ψ are formulae of L≤max {i,j }.

• φ ⊙ ψ is a formula of L≤i+j .Proof. The cases for r, p, φ →L ψ and φ⊙ψ follow from the definition of the fragments L≤n.The formula r is built without proposition symbols and thus a formula of LΠ 12 (→− 

> P

)≤0.The cases for ¬Lφ, φ ⊕ ψ, φ ⊗ ψ and φ ⊖ ψ are then easy to check by the definitions of the abbreviated connectives ¬L, ⊕, ⊗ and ⊖, which are constructed using only the truth constant 0 and the implication →L.We say that two formulae φ and ψ are equivalent if V (φ) = V (ψ) for every valuation V .We say that two logics L and L′ have the same expressive power if for each formula of L there is an equivalent formula of L′ and vice versa. 

Proposition 2.7. For all n ∈ N, the logics RPL( ⊙)≤n and LΠ 12 (→− 

> P

)≤n have the same expressive power. Proof. We show the claim by induction over n. The interesting part is the base case where 

n = 0 , as the induction step is then trivial by the identical way in which the fragments 

RPL( ⊙)≤n and LΠ 12 (→− 

> P

)≤n are constructed from RPL( ⊙)≤0 and LΠ 12 (→− 

> P

)≤0, respec-tively. First, note that for each formula φ of either RPL( ⊙)≤0 or LΠ 12 (→− 

> P

)≤0, we have 

V (φ) = V ′(φ) = r ∈ Q ∩ [0 , 1] for all valuations V and V ′; this is easy to see because φ

contains no proposition symbols, which guarantees that the truth value of φ is a constant and indeed a rational number. Now, by the semantics of r and Lemma 2.6, if φ is a formula of RPL( ⊙)≤0, then r is an equivalent formula of LΠ 12 (→− 

> P

)≤0. Likewise, if φ is a formula of LΠ 12 (→− 

> P

)≤0, then r is an equivalent formula of RPL( ⊙)≤0.By similar reasoning, we also obtain the following two propositions. 

Proposition 2.8. RPL( ⊙) and LΠ 12 (→− 

> P

) have the same expressive power. 

Proposition 2.9. RPL and LΠ 12 (⊙−, →− 

> P

) have the same expressive power. 

2.4 Equivalence 

In this section, we define notions of equivalence between Q[ReLU , x i]i∈N and the logics 

RPL( ⊙) and LΠ 12 . The most basic notion is centered around formulae and terms receiving identical values with matching valuations and evaluation maps. A more involved notion of equivalence involves scaling the values of real intervals down to the interval [0 , 1] , since unlike terms of Q[ReLU , x i]i∈N, the truth values of formulae of RPL( ⊙) and LΠ 12 are always in [0 , 1] .We start with the standard notion of equivalence. We already defined equivalence for terms in Section 2.1 and for formulae in Section 2.3.5; what remains is the standard equivalence between terms and formulae. 12 Definition 2.10. Let t be a term of Q[ReLU , x i]i∈N and φ a formula of RPL( ⊙) or LΠ 12 .We say that t and φ are equivalent if given any evaluation map E and valuation V such that E(x) = V (px) for all x ∈ VAR , we have 

E(t) = V (φ).

The remaining notions of equivalence are asymmetric and tied to a real parameter k ∈ R+.First, we define a necessary scaling function. 

Definition 2.11. Let k ∈ R+. We define the bijection scale k : R → R, scale k(x) = k+x 

> 2k

.Clearly, scale k scales the interval [−k, k ] to the interval [0 , 1] , as we have scale k(−k) = 0 ,

scale k(0) = 12 and scale k(k) = 1 . The idea behind the following notion of equivalence is that the truth values of formulae being restricted to [0 , 1] , a formula can simulate a term when both the inputs and outputs are scaled down using scale k. This naturally means that the term can only gain values within the interval [−k, k ], and to accomplish this, the input interval also naturally needs to be restricted, as otherwise a term such as x could obtain arbitrarily great values. 

Definition 2.12. Let i, k ∈ R+ such that i ≤ k. Let φ be a formula of RPL( ⊙) or LΠ 12

and let t be a term of Q[ReLU , x i]i∈N. We say that φ is (i, k )-equivalent to t if given any evaluation map E such that E(x) ∈ [−i, i ] for each x ∈ VAR and valuation V such that 

V (p) = scale k(E(xp)) for all p ∈ PROP , we have 

V (φ) = scale k(E(t)) .

Next, we establish a notion of expressive power that will be used in our characterisation theorems. 

Definition 2.13. Let T be a subclass of Q[ReLU , x i]i∈N and let L be one of the defined logics. We say that T and L have the same expressive power (w.r.t. scaling) if for each n ∈ Z+ the following conditions hold: 1. for each tuple (t1, . . . , t n) of terms in T and all i ∈ R+, there exists some k ≥ i and some tuple (φ1, . . . , φ n) of formulae of L such that for each j ∈ { 1, . . . , n } the formula 

φj is (i, k )-equivalent to the term tj , and 2. for each tuple (φ1, . . . , φ n) of formulae of L there exists a tuple (t1, . . . , t n) of terms in T such that for each j ∈ { 1, . . . , n } the term tj and the formula φj are equivalent. Note that, appropriately, the numbers i and k in the first condition must be independent of which components of the tuple are being compared. For an illustration of how the scaling works in this kind of characterisation, see Figure 2. Lastly, we use the scaling function scale k to define an alternative notion of equivalence and expressive power between logics. This equivalence notion applies scale k between two logic formulae. Intuitively, this means that a formula simulates another formula, but does so in the interval  12 , k+1 2k

.

Definition 2.14. Let k ∈ R+, let φ be a formula of RPL( ⊙) or LΠ 12 and likewise for 

ψ. We say that ψ is k-equivalent to φ if given any valuations V and V ′ such that 

V ′(p) = scale k(V (p)) for each p ∈ PROP , we have 

V ′(ψ) = scale k(V (φ)) .

13 ...

...

scale k

k

1

0

x

−k

...

... id 

1

> k+1 2k
> 12
> k+x
> 2k

0

...

...

k

1

0

−k

Figure 2: An illustration of the scaling in our translations. The left and right number lines correspond to values of terms, while the middle number line shows the truth values of formulae. From terms to formulae (left-to-middle), we scale [−k, k ] linearly into [0 , 1] ,while from formulae to terms (middle-to-right) we use the identity function. 

Definition 2.15. Let L and L′ be logics. We say that L has the same expressive power as L′ (w.r.t. scaling) if the following conditions hold: 1. for each formula of L, there exists a k-equivalent formula of L′ for some k ∈ R+, and 2. for each formula of L′, there exists an equivalent formula of L.We establish a useful lemma, connecting the three notions of equivalence discussed in this section. 

Lemma 2.16. Let i, k ∈ R+ such that 1 ≤ i ≤ k, let φ and ψ each be a formula of 

RPL( ⊙) or LΠ 12 and let t be a term of Q[ReLU , x i]i∈N. If φ and t are equivalent and ψ is 

(i, k )-equivalent to t, then ψ is k-equivalent to φ.Proof. Let V be a valuation and let V ′ be the valuation such that V ′(p) = scale k(V (p)) 

for each p ∈ PROP . Let E be the unique evaluation map such that E(x) = V (px) for each x ∈ VAR , which also implies E(x) ∈ [0 , 1] ⊂ [−i, i ] for each x ∈ VAR as well as 

V ′(p) = scale k(E(xp)) for each p ∈ PROP . Because ψ is (i, k )-equivalent to t, we have 

V ′(ψ) = scale k(E(t)) , and because φ and t are equivalent, we have E(t) = V (φ). Thus 

V ′(ψ) = scale k(V (φ)) , i.e., ψ is k-equivalent to φ.

# 3 Fuzzy logic characterisations 

In this section, we give fuzzy logic characterisations of Q[ReLU , x i]i∈N and specifically of neural networks. In Section 3.1, we give a fuzzy logic characterisation of Q[ReLU , x i]i∈N,as well as for subclasses of Q[ReLU , x i]i∈N obtained by giving a constant upper bound for the degrees of terms. Then, we give a fuzzy logic characterisation of neural networks in Section 3.2. 14 3.1 Characterising Q[ReLU , x i]i∈N

In this section, we give a fuzzy logic characterisation for Q[ReLU , x i]i∈N with and without degree bound via the logics RPL( ⊙) and LΠ 12 (→− 

> P

).

Theorem 3.1. Q[ReLU , x i]i∈N has the same expressive power (w.r.t. scaling) as RPL( ⊙)

and LΠ 12 (→− 

> P

).Proof. (Sketch) First, we define auxiliary connectives in RPL( ⊙) and LΠ 12 (→− 

> P

) that sim-ulate the addition and multiplication of reals in a given interval [−k, k ] down in the interval 

[0 , 1] ; the details of this are given in Appendix C. We then prove the theorem by giving recursive translations from terms of Q[ReLU , x i]i∈N to formulae of RPL( ⊙) and LΠ 12 (→− 

> P

),and vice versa. The correctness of the translations is proven by induction. The details are given in Appendix D. By restricting the degree of the terms and the fragment of the logic in the above theo-rem and carrying the restriction through the proof, we obtain the following fuzzy logic characterisation of Q[ReLU , x i]i∈N with degree bound d ∈ N via the logics RPL( ⊙)≤d and 

LΠ 12 (→− 

> P

)≤d.

Theorem 3.2. For each d ∈ N, Q[ReLU , x i]i∈N with degree bound d has the same expressive power (w.r.t. scaling) as RPL( ⊙)≤d and LΠ 12 (→− 

> P

)≤d.

So far in relation to both Q[ReLU , x i]i∈N and RPL , we have restricted to the case where con-stant parameters and truth constants are rational numbers. Analogously to Q[ReLU , x i]i∈N

and RPL , we can define R[ReLU , x i]i∈N and RPL by replacing the rationals in the defi-nitions of Q[ReLU , x i]i∈N and RPL with reals. All the same notions and lemmas from Section 2 and Appendix C relating to Q[ReLU , x i]i∈N and RPL can then analogously be defined and obtained for R[ReLU , x i]i∈N and RPL , including real-parameter neural net-works, the logic RPL( ⊙) and the fragments RPL( ⊙)≤n for each n ∈ N. We also note that the proofs of Theorems 3.1 and 3.2 did not hinge upon the restriction to rationals. Thus, we obtain the following corollaries. 

Corollary 3.3. R[ReLU , x i]i∈N has the same expressive power (w.r.t. scaling) as RPL( ⊙).

Corollary 3.4. For each d ∈ N, R[ReLU , x i]i∈N with degree bound d has the same expressive power (w.r.t. scaling) as RPL( ⊙)≤d.

3.2 Characterising neural networks 

In this section, we give fuzzy logic characterisations of neural networks. 

Theorem 3.5. Neural networks have the same expressive power (w.r.t. scaling) as the logics RPL( ⊙)≤1, LΠ 12 (→− 

> P

)≤1, RPL and LΠ 12 (⊙−, →− 

> P

).Proof. (Sketch) We first give a recursive constructive translation from formulae of RPL( ⊙)≤1

and LΠ 12 (→− 

> P

)≤1 into neurons. Then, we give a recursive constructive translation from proto-neurons to formulae of RPL and LΠ 12 (⊙−, →− 

> P

). The correctness of the translation is shown via induction. The details are in Appendix E. 15 In the proof of Theorem 3.5, the translation from RPL( ⊙)≤1 to neurons does not hinge on the restriction to rational constant symbols in the proto-neurons and rational truth constants in the formulae; the same translation works with reals. The translation from proto-neurons to formulae of RPL does depend on this restriction, but if we only want to translate neurons into formulae of RPL( ⊙)≤1 instead of RPL , then we can instead use Theorem 3.2 where the proof does not depend on restricting to rationals. Thus, we obtain the following corollary. 

Corollary 3.6. Real-parameter neural networks have the same expressive power (w.r.t. scaling) as RPL( ⊙)≤1.

As a corollary of Theorem 3.5, we also obtain the following connection between the logics 

RPL( ⊙)≤1, LΠ 12 (→− 

> P

)≤1, RPL and LΠ 12 (⊙−, →− 

> P

).

Corollary 3.7. RPL( ⊙)≤1 and LΠ 12 (→− 

> P

)≤1 have the same expressive power (w.r.t. scal-ing) as RPL and LΠ 12 (⊙−, →− 

> P

).Proof. Each formula of RPL is also a formula of RPL( ⊙)≤1 and by Proposition 2.7 there also exists an equivalent formula of LΠ 12 (→− 

> P

)≤1. Similarly, each formula of LΠ 12 (⊙−, →− 

> P

)

is also a formula of LΠ 12 (→− 

> P

)≤1 and equivalent to some formula of RPL( ⊙)≤1. For the other direction, Theorem 3.5 implies that for each formula φ of RPL( ⊙)≤1 or LΠ 12 (→− 

> P

)≤1

there exists an equivalent neuron n, for which there exists a (1 , k )-equivalent formula ψ of 

RPL or LΠ 12 (⊙−, →− 

> P

) for some k > 1; by Lemma 2.16, ψ is k-equivalent to φ.In Appendix F, we consider some proto-neurons which cannot be translated into (i, k )-equivalent formulae of Łukasiewicz logic. In light of this, we also consider what kinds of neurons are expressible in Łukasiewicz logic. 

# 4 Conclusion 

We have given fuzzy logic characterisations of ReLU -activated rational-weight neural net-works via fragments of two fuzzy logics, RPL( ⊙) and LΠ 12 . We also gave a fuzzy logic characterisation of a more general structure consisting of tuples of polynomials from the polynomial ring over Q in countably many variables appended with ReLU . The inputs for the neural networks and for the polynomials are allowed to be arbitrary real numbers. Future work could involve studying how neural networks are connected to tautologies of the characterising fuzzy logics, as well as studying the completeness properties of the fuzzy logic fragments introduced here. 

# 5 Acknowledgments 

Damian Heiman was supported by the Magnus Ehrnrooth Foundation. Antti Kuusisto was supported by the project Perspectives on computational logic , funded by the Research Council of Finland, project number 369424. 16 References 

[1] Veeti Ahvonen, Damian Heiman, and Antti Kuusisto. Descriptive Complexity for Neural Networks via Boolean Networks. In Aniello Murano and Alexandra Silva, edi-tors, 32nd EACSL Annual Conference on Computer Science Logic, CSL 2024, Naples, Italy, February 19-23, 2024 , volume 288 of LIPIcs , pages 9:1–9:22. Schloss Dagstuhl - Leibniz-Zentrum für Informatik, 2024. URL: https://doi.org/10.4230/LIPIcs. CSL.2024.9 , doi:10.4230/LIPICS.CSL.2024.9 .[2] Veeti Ahvonen, Damian Heiman, and Antti Kuusisto. Descriptive complexity for neural networks via Boolean networks. Journal of Logic and Computation , 36(3):exag011, 03 2026. doi:10.1093/logcom/exag011 .[3] Veeti Ahvonen, Damian Heiman, Antti Kuusisto, and Carsten Lutz. Logical Charac-terizations of Recurrent Graph Neural Networks with Reals and Floats. Advances in Neural Information Processing Systems , 37:104205–104249, 2024. [4] Paolo Amato, Antonio Di Nola, and Brunella Gerla. Neural Networks and Rational McNaughton Functions. Journal of Multiple-Valued Logic and Soft Computing , 11(1-2):95–110, 2005. [5] Pablo Barceló, Egor V. Kostylev, Mikael Monet, Jorge Pérez, Juan Reutter, and Juan Pablo Silva. The Logical Expressiveness of Graph Neural Networks. In In-ternational conference on learning representations , 2020. [6] Michael Benedikt, Chia-Hsuan Lu, Boris Motik, and Tony Tan. Decidabil-ity of Graph Neural Networks via Logical Characterizations. In Karl Bring-mann, Martin Grohe, Gabriele Puppis, and Ola Svensson, editors, 51st Interna-tional Colloquium on Automata, Languages, and Programming (ICALP 2024) , vol-ume 297 of Leibniz International Proceedings in Informatics (LIPIcs) , pages 127:1– 127:20, Dagstuhl, Germany, 2024. Schloss Dagstuhl – Leibniz-Zentrum für Infor-matik. URL: https://drops.dagstuhl.de/entities/document/10.4230/LIPIcs. ICALP.2024.127 , doi:10.4230/LIPIcs.ICALP.2024.127 .[7] Juan Luis Castro and Enric Trillas. The Logic of Neural Networks. Mathware and Soft Computing , 5:23–37, 1998. [8] Chen Chung Chang. Algebraic Analysis of Many Valued Logics. Transactions of the American Mathematical society , 88(2):467–490, 1958. [9] Antonio Di Nola, Brunella Gerla, and Ioana Leustean. Adding real coefficients to Łukasiewicz logic: An application to neural networks. In International Workshop on Fuzzy Logic and Applications , pages 77–85. Springer, 2013. [10] Francesc Esteva, Lluís Godo, Petr Hájek, and Mirko Navara. Residuated fuzzy logics with an involutive negation. Archive for mathematical logic , 39(2):103–124, 2000. [11] Francesc Esteva, Lluís Godo, and Franco Montagna. The LΠ and LΠ 12 logics: two complete fuzzy systems joining Łukasiewicz and Product Logics. Archive for Mathe-matical Logic , 40(1):39–67, 2001. [12] Francesc Esteva, Lluís Godo, and Carles Noguera. First-order t-norm based fuzzy logics with truth-constants: distinguished semantics and completeness properties. Annals of Pure and Applied Logic , 161(2):185–202, 2009. 17 [13] Bernardo Cuenca Grau, Eva Feng, and Przemysław Andrzej Wałęga. The Correspon-dence Between Bounded Graph Neural Networks and Fragments of First-Order Logic. 

Proceedings of the AAAI Conference on Artificial Intelligence , 40(23):19135–19142, Mar. 2026. URL: https://ojs.aaai.org/index.php/AAAI/article/view/38987 ,

doi:10.1609/aaai.v40i23.38987 .[14] Martin Grohe. The Descriptive Complexity of Graph Neural Networks. TheoretiCS ,3, 2024. [15] Petr Hájek. Fuzzy logic and arithmetical hierarchy. Fuzzy sets and Systems , 73(3):359– 363, 1995. [16] Petr Hájek. Metamathematics of Fuzzy Logic , volume 4 of Trends in Logic . Kluwer, 1998. doi:10.1007/978-94-011-5300-3 .[17] Stan P Hauke and Przemysław Andrzej Wałęga. Aggregate-Combine-Readout GNNs Can Express Logical Classifiers Beyond the Logic C2. Proceedings of the AAAI Con-ference on Artificial Intelligence , 40(26):21594–21601, Mar. 2026. URL: https:// ojs.aaai.org/index.php/AAAI/article/view/39308 , doi:10.1609/aaai.v40i26. 39308 .[18] Rostislav Horčík and Petr Cintula. Product Łukasiewicz Logic. Archive for Mathe-matical Logic , 43(4):477–503, 2004. [19] Jan Łukasiewicz and Alfred Tarski. Untersuchungen über den Aussagenkalküls. 

Comptes rendus de la Société des Sciences et des Lettres de Varsovie , 23:1–21, 1930. [20] Huan Luo and Jonni Virtema. Unifying approach to uniform expressivity of graph neu-ral networks, 2026. URL: https://arxiv.org/abs/2602.18409 , arXiv:2602.18409 .[21] Enrico Marchioni and Franco Montagna. Complexity and Definability Issues in Ł Π 12 .

Journal of Logic and Computation , 17(2):311–331, 2007. URL: https://doi.org/10. 1093/logcom/exl044 , doi:10.1093/LOGCOM/EXL044 .[22] Warren S. McCulloch and Walter Pitts. A logical calculus of the ideas immanent in nervous activity. The bulletin of mathematical biophysics , 5(4):115–133, 1943. [23] Robert McNaughton. A Theorem About Infinite-Valued Sentential Logic. The Journal of Symbolic Logic , 16(1):1–13, 1951. [24] Daniele Mundici. Advanced Łukasiewicz calculus and MV-algebras , volume 35. Springer Science & Business Media, 2011. [25] Pierre Nunn, Marco Sälzer, François Schwarzentruber, and Nicolas Troquard. A Logic for Reasoning about Aggregate-Combine Graph Neural Networks. In Kate Larson, editor, Proceedings of the Thirty-Third International Joint Conference on Artificial Intelligence, IJCAI-24 , pages 3532–3540. International Joint Conferences on Artificial Intelligence Organization, 8 2024. Main Track. doi:10.24963/ijcai.2024/391 .[26] Jan Pavelka. On Fuzzy Logic I. Many-valued rules of inference. Mathematical Logic Quarterly , 25(3-6):45–52, 1979. URL: https://doi.org/10.1002/malq.19790250304 ,

doi:10.1002/MALQ.19790250304 .[27] Jan Pavelka. On Fuzzy Logic II. Enriched residuated lattices and semantics of propositional calculi. Mathematical Logic Quarterly , 25(7-12):119–134, 1979. URL: 

https://doi.org/10.1002/malq.19790250706 , doi:10.1002/MALQ.19790250706 .18 [28] Jan Pavelka. On Fuzzy Logic III. Semantical completeness of some many-valued propositional calculi. Mathematical Logic Quarterly , 25(25-29):447–464, 1979. URL: 

https://doi.org/10.1002/malq.19790252510 , doi:10.1002/MALQ.19790252510 .[29] Maximilian Pflueger, David Tena Cucala, and Egor V. Kostylev. Recurrent Graph Neural Networks and Their Connections to Bisimulation and Logic. In Proceedings of the AAAI Conference on Artificial Intelligence , volume 38, pages 14608–14616, 2024. [30] Petr Savický, Roberto Cignoli, Francesc Esteva, Lluís Godo, and Carles Noguera. On Product Logic with Truth-constants. Journal of Logic and Computation , 16(2):205– 225, 2006. [31] Moritz Schönherr and Carsten Lutz. Logical Characterizations of GNNs with Mean Ag-gregation. Proceedings of the AAAI Conference on Artificial Intelligence , 40(30):25218– 25225, Mar. 2026. URL: https://ojs.aaai.org/index.php/AAAI/article/view/ 39713 , doi:10.1609/aaai.v40i30.39713 .[32] Matias Selin. Descriptive Complexity and Graph Neural Networks. Master’s thesis, Tampere University, 2025. URL: https://trepo.tuni.fi/handle/10024/232168 .[33] Przemysław Andrzej Wałęga and Bernardo Cuenca Grau. Preservation Theorems for Unravelling-Invariant Classes: A Uniform Approach for Modal Logics and Graph Neural Networks. arXiv preprint arXiv:2602.01856 , 2026. 

# A Proof of Lemma 2.3 

We recall Lemma 2.3: 

Lemma 2.3. For all terms t of Q[ReLU , x i]i∈N, t is a proto-neuron if and only if deg( t) ≤ 1.Proof. We prove the claim by induction over the structure of t. If t = r for some r ∈ Q

or if t = x for some x ∈ VAR , then t is a proto-neuron by definition. Next, assume that 

t′ and t′′ are terms of Q[ReLU , x i]i∈N and that t′ and t′′ are proto-neurons if and only if 

deg( t′) ≤ 1 and deg( t′′ ) ≤ 1, respectively. 

• If t = t′ + t′′ , then t is a proto-neuron if and only if t′ and t′′ are proto-neurons. By the induction hypothesis, this is equivalent to deg( t′), deg( t′′ ) ≤ 1 which in turn is equivalent to deg( t) = max {deg( t′), deg( t′′ )} ≤ 1.

• If t = t′t′′ , then t is a proto-neuron if and only if t′ and t′′ are proto-neurons such that we have deg( t′) + deg( t′′ ) ≤ 1. Since deg( t) = deg( t′) + deg( t′′ ), this is equivalent to 

deg( t) ≤ 1.

• If t = ReLU( t′), then t is a proto-neuron if and only if t′ is a proto-neuron. By the induction hypothesis, this is equivalent to deg( t′) ≤ 1, and since deg( t) = deg( t′),this is equivalent to deg( t) ≤ 1.This concludes the induction. 19 B Defining rational numbers in LΠ 12

It is shown in [11] that all rational numbers in [0 , 1] are definable in LΠ 12 , meaning that for every r ∈ Q ∩ [0 , 1] there exists a formula φ of LΠ 12 such that V (φ) = r for each valuation V . We show an alternative construction to the one in [11], but the results of the current paper hold regardless of which way the construction is done. We define each rational number r ∈ Q ∩ [0 , 1] in LΠ 12 as a formula r recursively as follows. First, we can obviously define 0 = 0 and 12 = 12 . Second, we define 1 := ¬L0. Next, we cover the case where r = k 

> 2j

∈]0 , 1[ for some k, j ∈ Z+ such that k < 2j . If k = j = 1 , then r = 12 ,and thus r is already defined. Next, assume that j > 1 and we have defined k′ 

> 2j−1

for each 

k′ ∈ Z+ such that k′ < 2j−1. If k = 1 , then we define  

> 12j

:= 12j−1 ⊙ 12 .

Next, assume that k > 1 and we have defined k′ 

> 2j

for each k′ ∈ Z+ such that k′ < k . If k

is even, then k 

> 2j

= k′ 

> 2j−1

where k′ = k 

> 2

∈ Z+, meaning that r is already defined. If k is odd, then 2j is not divisible by k, meaning that r is not yet defined. We define  

> k
> 2j

:= k−12j ⊕ 12j .

Lastly, we consider the case where r = kℓ ∈]0 , 1[ for some k, ℓ ∈ Z+ such that k < ℓ and ℓ is not divisible by k nor is ℓ a power of 2. Let m be the smallest integer such that k, ℓ < 2m.Then kℓ := ℓ 

> 2m

→P k 

> 2m

.

This completes the construction. 

Lemma B.1. For each r ∈ Q ∩ [0 , 1] and each valuation V , we have V (r) = r.Proof. By induction over the recursive definition of r.

# C Auxiliary connectives 

In this section, we establish auxiliary connectives that will be useful when translating terms of Q[ReLU , x i]i∈N to the logics RPL( ⊙) and LΠ 12 . The goal with these auxiliary connectives is to simulate real arithmetic from some arbitrary interval [−k, k ] in the interval 

[0 , 1] . While we already have the connectives ⊕ and ⊙ whose semantics are similar to the sum and multiplication of real numbers, this does not yet suffice for our characterisations. Consider for example the multiplication of two real numbers a, b > 1. We have ab > a and 

ab > b . On the other hand, multiplying any two numbers a, b ∈ [0 , 1] , we get ab ≤ a and 

ab ≤ b. Thus, ⊙ by itself does not suffice to simulate multiplication of two real numbers, but we will see by the end of the section that a more involved auxiliary connective will accomplish the task. To begin with, we first define for each n ∈ N the iterated sum operator J

> n

. Intuitively, J 

> n

corresponds to multiplying the truth value of a formula by n, which is achieved by iteratively summing the formula with itself. For n = 0 we define J 

> 0

φ := 0 . Then, for each 

n ∈ N, we define J 

> n+1

φ := ( J 

> n

φ) ⊕ φ. 

20 Lemma C.1. For all n ∈ N, all valuations V and all formulae φ of RPL( ⊙) or LΠ 12 , we have that V (J 

> n

φ) = min {1, nV (φ)}.Proof. By induction over n.For RPL( ⊙) and its fragments, we introduce the connective ⊕′. The intuition behind ⊕′ is to simulate the addition of real numbers belonging to the interval [−k, k ] for any k ∈ R+ in the interval [0 , 1] using the scaling function scale k. More formally, we define ⊕′ as follows: 

φ ⊕′ ψ := 



φ ⊖ 14



⊕



ψ ⊖ 14



.

For LΠ 12 and its fragments, where the truth constant 14 does not exist, we instead define the connective ⊕′ which is defined the same as ⊕′ except that we replace 14 with the formula  

> 14

(recall Appendix B). In the rest of the paper, the logic is always clear from the context, and thus we just write ⊕′ instead of ⊕′ or ⊕′.The following lemma shows that ⊕′ indeed simulates the addition of two real numbers in the interval [−k, k ] correctly, assuming that the inputs lie in the interval − k 

> 2

, k

> 2

. The restriction of inputs is natural, since a, b ∈ − k 

> 2

, k

> 2

 implies a + b ∈ [−k, k ].

Lemma C.2. Let L be RPL( ⊙) or LΠ 12 . For all valuations V , all k ∈ R+ and all formulae 

φ and ψ of L: if V (φ) = scale k(ℓ) and V (ψ) = scale k(ℓ′) for some ℓ, ℓ ′ ∈ − k 

> 2

, k

> 2

, then 

V (φ ⊕′ ψ) = scale k(ℓ + ℓ′).Proof. We cover the case where L is RPL( ⊙) as the case where L is LΠ 12 is analogous. By the semantics of ⊖, we have V



φ ⊖ 14



= max 

n

0, V (φ) − V

 14

o 

. Because V

 14



= 14

and 

V (φ) = scale k(ℓ) ≥ scale k(− k 

> 2

) = k− k 

> 2
> 2k

= 14 ,

we have V (φ) − V

 14



≥ 14 − 14 = 0 . Thus, we have V



φ ⊖ 14



= V (φ) − 14 . Analogously, we see that V



ψ ⊖ 14



= V (ψ) − 14 .Now, by the semantics of ⊕ we have V (φ⊕′ ψ) = min 

n

1, V 



φ ⊖ 14



+ V



ψ ⊖ 14

o 

. By the above, we have V



φ ⊖ 14



+ V



ψ ⊖ 14



=  V (φ) − 14

 +  V (ψ) − 14

 = V (φ) + V (ψ) − 12 ,which implies V (φ ⊕′ ψ) = min 1, V (φ) + V (ψ) − 12 . Because ℓ, ℓ ′ ≤ k 

> 2

, we have 

V (φ), V (ψ) ≤ scale k

  k

> 2

 = k+ k 

> 2
> 2k

= 34 ,

which means that 

V (φ) + V (ψ) − 12 ≤ 34 + 34 − 12 = 1 .

Thus V (φ ⊕′ ψ) = V (φ) + V (ψ) − 12

= scale k(ℓ) + scale k(ℓ′) − 12

= k+ℓ 

> 2k

+ k+ℓ′ 

> 2k

− k

> 2k

= k+( ℓ+ℓ′)2k

= scale k(ℓ + ℓ′).

This completes the proof. 21 Remark C.3. The definition of ⊕′ may raise the following question: why not simply define φ ⊕′ ψ := ( φ ⊕ ψ) ⊖ 12 ? The problem is that, while the formula would still give correct truth values when V (φ) = scale k(ℓ) and V (ψ) = scale k(ℓ′) for some ℓ, ℓ ′ ∈ − k 

> 2

, 0,it would not give correct values when ℓ, ℓ ′ ∈ 0, k

> 2

, because in all such cases we would get V (φ ⊕′ ψ) = 12 = scale k(0) ̸ = scale k(ℓ + ℓ′). The chosen definition of ⊕′ is intended to maximize the working range of the connective while also treating the two connected formulae φ and ψ symmetrically. Analogously to the connective J

> n

, we define a second iterated sum operator J′ 

> n

for each 

n ∈ N, this time corresponding to the connective ⊕′. Again, the operator J′ 

> n

corresponds to multiplying by an integer n, but this time the multiplication occurs in the larger interval 

[−k, k ] and the result is scaled down to [0 , 1] . For n = 0 , we define J′ 

> 0

φ := 12 , and for 

n = 1 , we define J′ 

> 1

φ := φ. Next, let n > 1, assume we have defined J′ 

> m

φ for each non-negative integer m < n and let j ∈ N be the smallest integer such that n ≤ 2j . We define J′ 

> n

φ := ( J′ 

> 2j−1

φ) ⊕′ (J′ 

> n−2j−1

φ).

The next lemma shows that J′ 

> n

correctly simulates the multiplication of a real number in the interval [−k, k ] by n, assuming that the input is in the interval − k 

> 2j

, k

> 2j

. This restriction is natural, since a ∈ − k 

> 2j

, k

> 2j

 implies na ∈ − nk  

> 2j

, nk 

> 2j

 ⊆

h

− 2j k 

> 2j

, 2j k

> 2j

i

= [ −k, k ].

Lemma C.4. Let L be RPL( ⊙) or LΠ 12 . Let n ∈ N, k ∈ R+, let V be a valuation and φ aformula of L such that V (φ) = scale k(ℓ) for some ℓ ∈ − k 

> 2j

, k

> 2j

 where j ∈ N is the smallest integer such that n ≤ 2j . Then V (J′ 

> n

φ) = scale k(nℓ ).Proof. We prove the claim by induction over n. If n = 0 , then J′ 

> 0

φ = 12 and therefore 

V (J′ 

> 0

φ) = 12 = scale k(0 ℓ). If n = 1 , then J′ 

> 1

φ = φ and V (J′ 

> 1

φ) = V (φ) = scale k(1 ℓ).Next, assume n > 1 and the claim holds for each n′ < n . Assume that V (φ) = scale k(ℓ) for some ℓ ∈ − k 

> 2j

, k

> 2j

, where j ∈ N is the smallest integer such that n ≤ 2j . By the definition of J′

> n

, we have J′ 

> n

φ = ( J′ 

> 2j−1

φ) ⊕′ (J′ 

> n−2j−1

φ). By the induction hypothesis, we see that V (J′ 

> 2j−1

φ) = scale k(2 j−1ℓ) and V (J′ 

> n−2j−1

φ) = scale k(( n − 2j−1)ℓ). We want to use Lemma C.2, for which we need to show that 2j−1ℓ, (n − 2j−1)ℓ ∈ − k 

> 2

, k

> 2

.First, because ℓ ∈ − k 

> 2j

, k

> 2j

, we get 2j−1ℓ ∈

h

− 2j−1k 

> 2j

, 2j−1k

> 2j

i

= − k 

> 2

, k

> 2

. Second, because 

n ≤ 2j , we get n − 2j−1 ≤ 2j − 2j−1 = 2 j−1 which together with ℓ ∈ − k 

> 2j

, k

> 2j

 implies 

(n − 2j−1)ℓ ∈

h

− (n−2j−1)k 

> 2j

, (n−2j−1)k

> 2j

i

⊆

h

− 2j−1k 

> 2j

, 2j−1k

> 2j

i

= − k 

> 2

, k

> 2

. Now by Lemma C.2, we get V (J 

> n

φ) = scale k(2 j−1ℓ + ( n − 2j−1)ℓ) = scale k(nℓ ), as desired. Lastly, for RPL( ⊙) and its fragments, we define the connective ⊙k, which is tied to an integer parameter k ∈ Z+. The intuition behind ⊙k is to simulate the multiplication of two real numbers belonging to the interval [−k, k ] in the interval [0 , 1] . Formally, ⊙k is defined as follows: 

φ ⊙k ψ := J

> k

 J

> 2

(φ ⊙ ψ) ⊕ 12k



⊖ (φ ⊕′ ψ)



.

Again, for LΠ 12 and its fragments, we instead define ⊙k whose definition is otherwise the same as ⊙k except that we again replace the truth constant 12k with the formula 12k . As with ⊕′, for the rest of the paper the logic is always clear from the context, and so we simply write ⊙k.22 The next lemma shows that ⊙k indeed correctly simulates the multiplication of reals in 

[−k, k ], assuming the inputs are in the interval −√k, √k . The restriction of inputs is once again natural, since a, b ∈ −√k, √k  implies ab ∈ [−k, k ].

Lemma C.5. Let L be RPL( ⊙) or LΠ 12 . For all integers k ≥ 8, valuations V and formulae 

φ and ψ of L: if V (φ) = scale k(ℓ) and V (ψ) = scale k(ℓ′) for some ℓ, ℓ ′ ∈ −√k, √k , then 

V (φ ⊙k ψ) = scale k(ℓℓ ′).Proof. We show the case where L is RPL( ⊙) as the case where L is LΠ 12 is analogous. For convenience, we let θleft := J

> 2

(φ ⊙ ψ) ⊕ 12k and θright := φ ⊕′ ψ. This means that 

φ⊙k ψ = J

> k

(θleft ⊖θright ). We start by calculating V (θleft ), for which we must first calculate 

V (J

> 2

(φ ⊙ ψ)) and V (φ ⊙ ψ). First, by definition we get 

V (φ ⊙ ψ) = V (φ)V (ψ) = scale k(ℓ)scale k(ℓ′) = k+ℓ 

> 2kk+ℓ′
> 2k

= k2+k(ℓ+ℓ′)+ ℓℓ ′ 

> 4k2

.

By Lemma C.1, we have V (J

> 2

(φ ⊙ ψ)) = min {1, 2V (φ ⊙ ψ)}. Because ℓ, ℓ ′ ∈ −√k, √k 

we find that 

2V (φ ⊙ ψ) = 2 k2+k(ℓ+ℓ′)+ ℓℓ ′

> 4k2

= k2+k(ℓ+ℓ′)+ ℓℓ ′

> 2k2

≤ k2+k

 √k+√k

+√k√k

> 2k2

= k2+2 k√k+k

> 2k2

= k+2 √k+1 2k ,

which is at most 1 when k ≥ 3 + 2 √2 ≈ 5.83 . Because of the assumption that k ≥ 8, we have 2V (φ ⊙ ψ) ≤ 1 and thus V (J

> 2

(φ ⊙ ψ)) = k2+k(ℓ+ℓ′)+ ℓℓ ′ 

> 2k2

. Next, by the semantics of 

⊕ we have V (θleft ) = min 

n

1, V (J

> 2

(φ ⊙ ψ)) + V

 12k

o 

. Utilizing the analysis above, we find that 

V (J

> 2

(φ ⊙ ψ)) + V

 12k



= k2+k(ℓ+ℓ′)+ ℓℓ ′ 

> 2k2

+ 12k

≤ k+2 √k+1 2k + 12k

= k+2 √k+2 2k ,

which is at most 1 when k ≥ 4 + 2 √3 ≈ 7.46 . Again, because of the assumption k ≥ 8, we see that V (J

> 2

(φ ⊙ ψ)) + V

 12k



≤ 1 and thus 

V (θleft ) = k2+k(ℓ+ℓ′)+ ℓℓ ′ 

> 2k2

+ 12k = k2+k(ℓ+ℓ′)+ k+ℓℓ ′ 

> 2k2

.

Next, we calculate V (θright ). We see that √k ≤ k 

> 2

when k ≥ 4 and thus ℓ, ℓ ′ ∈ −√k, √k 

implies ℓ, ℓ ′ ∈ − k 

> 2

, k

> 2

, which means we can apply Lemma C.2 to θright , giving us 

V (θright ) = scale k(ℓ + ℓ′) = k+( ℓ+ℓ′)2k = k2+k(ℓ+ℓ′)2k2 .

We finish the proof by calculating V (φ ⊙k ψ), for which we first calculate V (θleft ⊖ θright ).By the semantics of ⊖, we have V (θleft ⊖ θright ) = max {0, V (θleft ) − V (θright )}. We see that 

V (θleft ) − V (θright ) = k2+k(ℓ+ℓ′)+ k+ℓℓ ′ 

> 2k2

− k2+k(ℓ+ℓ′)2k2 = k+ℓℓ ′ 

> 2k2

≥ k−√k√k 

> 2k2

= 0 ,

which means that V (θleft ⊖ θright ) = V (θleft ) − V (θright ) = k+ℓℓ ′ 

> 2k2

. Lastly, by Lemma C.1, we have V (φ ⊙k ψ) = min {1, k V (θleft ⊖ θright )} = min 

n

1, k+ℓℓ ′

> 2k

o

. Because ℓ, ℓ ′ ∈ −√k, √k ,we get k+ℓℓ ′ 

> 2k

≤ k+√k√k 

> 2k

= 1 , which gives us V (φ ⊙k ψ) = k+ℓℓ ′ 

> 2k

= scale k(ℓℓ ′), as desired. 23 The following lemma extends Lemma 2.6 to the auxiliary connectives defined in this sec-tion, relating them to the fragments of RPL( ⊙) and LΠ 12 (→− 

> P

) defined in Section 2.3.5. Importantly, it shows that the scaled sum ⊕′ and multiplication ⊙k do not differ from the ordinary sum ⊕ and multiplication ⊙ in terms of how they relate to the fragments. 

Lemma C.6. Let L be RPL( ⊙) or LΠ 12 (→− 

> P

). Let i, j ∈ N, and let φ be a formula of L≤i

and ψ a formula of L≤j . Then the following hold. 

• For each n ∈ N, J 

> n

φ and J′ 

> n

φ are formulae of L≤i.

• φ ⊕′ ψ is a formula of L≤max {i,j }.

• For each k ∈ Z+, φ ⊙k ψ is a formula of L≤i+j .Proof. The proof is straightforward by the definitions of the operators and the fragments 

L≤n and by Lemma 2.6. 

# D Proof of Theorem 3.1 

In this section, we prove Theorem 3.1. First, we recall the theorem. 

Theorem 3.1. Q[ReLU , x i]i∈N has the same expressive power (w.r.t. scaling) as RPL( ⊙)

and LΠ 12 (→− 

> P

).

Before proving the theorem, we establish two lemmas. In the first lemma, we give a con-structive translation from terms of Q[ReLU , x i]i∈N to formulae of RPL( ⊙) and LΠ 12 (→− 

> P

).

Lemma D.1. Let L be RPL( ⊙) or LΠ 12 (→− 

> P

). For each term t of Q[ReLU , x i]i∈N and each i ∈ R+, there exists some K ∈ Z+ such that for each integer k ≥ K we can construct an (i, k )-equivalent formula φt of L. For all d ∈ N, if deg( t) ≤ d, then φt is a formula of 

L≤d.Proof. The strategy is to recursively build an (i, k )-equivalent formula for each subterm of 

t including t itself. We show the proof for the case where L is RPL( ⊙). The case where L

is LΠ 12 (→− 

> P

) follows by Propositions 2.7 and 2.8. Let t be a term and let i ∈ R+. Let rmax be the largest rational number such that rmax 

or −rmax is a subterm of t and let j := max {i, r max , 2}. For subterms t′ of t, it is easy to show by induction over the structure of t′ that given any evaluation map E such that 

E(x) ∈ [−i, i ] for all x ∈ VAR , we have |E(t′)| ≤ jlength( t′). Now let K := max {j2length( t), 8}

and let k ≥ K be an integer. For each subterm s of t, we recursively construct a formula φs that is (i, k )-equivalent to s

as follows: 

• If s = r for some r ∈ Q, then |r| ≤ rmax ≤ j ≤ K ≤ k, which implies scale k(r) ∈ [0 , 1] .We define φs := scale k(r).

• If s = x for some x ∈ VAR , then φs := px.

• If s = t′ + t′′ for some terms t′ and t′′ , then φs := φt′ ⊕′ φt′′ .24 • If s = t′t′′ for some terms t′ and t′′ , then φs := φt′ ⊙k φt′′ .

• If s = ReLU( t′) for some term t′, then φs := 



φt′ ⊖ 12



⊕ 12 .We now prove the correctness of our construction. Let s be a subterm of t, let E be an evaluation map such that E(x) ∈ [−i, i ] for each x ∈ VAR , and let V be the valuation such that V (p) = scale k(E(xp)) for each p ∈ PROP . We have to show V (φs) = scale k(E(s)) .We prove the claim by induction over the structure of s. First, if s = r for some r ∈ Q,then V (φs) = V



scale k(r)



= scale k(r) = scale k(E(s)) . Next, if s = x for some x ∈ VAR ,then the claim is true by the definition of the valuation V .Next, assume that the claim holds for terms t′ and t′′ , i.e., assume that V (φt′ ) = scale k(E(t′)) 

and V (φt′′ ) = scale k(E(t′′ )) . Before delving into further steps, we show that the conditions of Lemmas C.2 and C.5 are met. First, we already know that |E(t′)| ≤ jlength( t′). Because 

length( t′) ≤ length( t), we have |E(t′)| ≤ jlength( t). Since jlength( t) = pj2length( t), we get 

|E(t′)| ≤ pj2length( t). Now, because k ≥ K ≥ j2length( t), we have |E(t′)| ≤ √k. Analo-gously, we see that |E(t′′ )| ≤ √k. This means that E(t′), E (t′′ ) ∈ −√k, √k . Furthermore, since k ≥ K ≥ 8 > 4, we have √k < k 

> 2

< k , which means that E(t′), E (t′′ ) ∈ − k 

> 2

, k

> 2

 and 

E(t′), E (t′′ ) ∈ [−k, k ]. Now, we are ready to cover the remaining cases. 

• If s = t′ + t′′ , then φs = φt′ ⊕′ φt′′ . Because it is the case that E(t′), E (t′′ ) ∈ − k 

> 2

, k

> 2

,we may apply Lemma C.2, which gives us V (φs) = scale k(E(t′)+ E(t′′ )) . Now because 

E(t′) + E(t′′ ) = E(t′ + t′′ ) = E(s), we have V (φs) = scale k(E(s)) .

• If s = t′t′′ , then φs = φt′ ⊙k φt′′ . As E(t′), E (t′′ ) ∈ −√k, √k , by Lemma C.5 we get V (φs) = scale k(E(t′)E(t′′ )) . Because E(t′)E(t′′ ) = E(t′t′′ ) = E(s), we have 

V (φs) = scale k(E(s)) .

• If s = ReLU( t′), then φs =



φt′ ⊖ 12



⊕ 12 . By the semantics of ⊕, ⊖ and 12 , we have 

V (φs) = V

 

φt′ ⊖ 12



⊕ 12



= min 

n

1, V 



φt′ ⊖ 12



+ 12

o

= min 1, max 0, V (φt′ ) − 12 + 12 .

Because max {a, b } + c = max {a + c, b + c} for all a, b, c ∈ R, we get 

V (φs) = min 1, max  12 , V (φt′ ) .

By the induction hypothesis, we have V (φt′ ) = scale k(E(t′)) and thus 

V (φs)= min 1, max  12 , scale k(E(t′)) .

There are two cases to explore: E(t′) < 0 and E(t′) ≥ 0.1. If E(t′) < 0, then scale k(E(t′)) < 12 and thus max  12 , scale k(E(t′)) = 12 and 

V (φs) = min 1, 12 = 12 . Because 12 = scale k(0) , we have V (φs) = scale k(0) .Because E(t′) < 0, we have E(s) = E(ReLU( t′)) = 0 , which finally gives us 

V (φs) = scale k(E(s)) .25 2. If E(t′) ≥ 0, then scale k(E(t′)) ≥ 12 and max  12 , scale k(E(t′)) = scale k(E(t′)) ,and thus V (φs) = min {1, scale k(E(t′)) }. Because E(t′) ∈ [−k, k ], we know that 

scale k(E(t′)) ∈ [0 , 1] , which means that min {1, scale k(E(t′)) } = scale k(E(t′)) 

and thus V (φs) = scale k(E(t′)) . Furhtermore, because E(t′) ≥ 0, we have 

E(s) = E(ReLU( t′)) = E(t′) and therefore V (φs) = scale k(E(s)) .We have now shown the correctness of our construction. The claim “For all d ∈ N, if 

deg( t) ≤ d, then φt is a formula of L≤d” now follows from the definition of degree, our construction and Lemmas 2.6 and C.6 by a simple induction over the structure of t.In the next lemma, we give an opposite translation from formulae of RPL( ⊙) and LΠ 12 (→− 

> P

)

to terms of Q[ReLU , x i]i∈N.

Lemma D.2. Let L be RPL( ⊙) or LΠ 12 (→− 

> P

). For each formula φ of L, we can construct an equivalent term tφ of Q[ReLU , x i]i∈N. For all d ∈ N, if φ is a formula of L≤d, then 

deg( tφ) ≤ d.Proof. The strategy is to recursively build an equivalent term for each formula φ of L. We cover the case where L is RPL( ⊙). The case where L is LΠ 12 (→− 

> P

) follows by Proposi-tions 2.7 and 2.8. Let φ be a formula of RPL( ⊙). We will construct a term tφ equivalent to φ by induction over the structure of φ as follows. First, we cover the base cases. 

• If φ = r for some r ∈ Q ∩ [0 , 1] , then tφ = r.

• If φ = p for some p ∈ PROP , then tφ = xp.Next, assume ψ and θ are formulae of RPL( ⊙), and let tψ and tθ be the corresponding terms of Q[ReLU , x i]i∈N.

• If φ = ψ →L θ, then tφ = 1 − ReLU( tψ − tθ).

• If φ = ψ ⊙ θ, then tφ = tψtθ.Next, we prove the correctness of the given construction. Let V be a valuation and let E

be an evaluation map such that E(x) = V (px) for all x ∈ VAR . We have to show that 

E(tφ) = V (φ); we show this by induction over the structure of φ. First, we cover the base cases. 

• If φ = r for some r ∈ Q ∩ [0 , 1] , then tφ = r and E(tφ) = E(r) = r = V (r) = V (φ).

• If φ = p for some p ∈ PROP , then tφ = xp and E(tφ) = E(xp) = V (p) = V (φ).Next, assume the claim holds for formulae ψ and θ of RPL( ⊙), i.e., E(tψ) = V (ψ) and 

E(tθ) = V (θ).

• If φ = ψ →L θ, then tφ = 1 − ReLU( tψ − tθ). By the semantics of ReLU , we thus have E(tφ) = 1 − max {0, E (tψ) − E(tθ)}. By the induction hypothesis, we know that E(tφ) = 1 − max {0, V (ψ) − V (θ)}. Because − max {a, b } = min {− a, −b} for 26 all a, b ∈ R, we have E(tφ) = 1 + min {0, −V (ψ) + V (θ)}. Furthermore, because 

a + min {b, c } = min {a + b, a + c} for all a, b, c ∈ R, E(tφ) = min {1, 1 − V (ψ) + V (θ)}.Thus by the semantics of →L, we have E(tφ) = V (φ).

• If φ = ψ ⊙ θ, then tφ = tψtθ. Thus E(tφ) = E(tψtθ) = E(tψ)E(tθ). By the induction hypothesis, we have E(tφ) = V (ψ)V (θ), and by the semantics of ⊙, we get 

E(tφ) = V (φ).We have now shown the correctness of our construction. The claim “For all d ∈ N, if φ

is a formula of L≤d, then deg( tφ) ≤ d” follows from Lemma 2.6, our construction and the definition of degree by a straightforward induction over the structure of φ.Now, we are ready to prove Theorem 3.1. 

Theorem 3.1. Q[ReLU , x i]i∈N has the same expressive power (w.r.t. scaling) as RPL( ⊙)

and LΠ 12 (→− 

> P

).Proof. Let L be RPL( ⊙) or LΠ 12 (→− 

> P

). First, let (t1, . . . , t n) be a sequence of terms of 

Q[ReLU , x i]i∈N. For each i ∈ R+, we need to find a sequence (φ1, . . . , φ n) of formulae of 

L and some k ∈ R+ such that φj is (i, k )-equivalent to tj for each j ∈ { 1, . . . , n }. By Lemma D.1, for each j ∈ { 1, . . . , n } and for each i ∈ R+ there exists some Ki,j ∈ Z+

such that for each integer k ≥ Ki,j we can construct a formula φj that is (i, k )-equivalent to tj . Let Ki = max {Ki, 1, . . . , K i,n }. Now for each integer k ≥ Ki there exists for each 

j ∈ { 1, . . . , n } a formula φj that is (i, k )-equivalent to tj , and (φ1, . . . , φ n) is the tuple we seek. Next, let (φ1, . . . , φ n) be a sequence of formulae of L. We have to find a sequence (t1, . . . , t n)

of terms such that for each j ∈ { 1, . . . , n }, the term tj and the formula φj are equivalent. To obtain this sequence, we simply apply Lemma D.2 to each formula φj to obtain the equivalent term tj .

# E Proof of Theorem 3.5 

In this section, we give the proof of Theorem E. We start by recalling the theorem. 

Theorem 3.5. Neural networks have the same expressive power (w.r.t. scaling) as the logics RPL( ⊙)≤1, LΠ 12 (→− 

> P

)≤1, RPL and LΠ 12 (⊙−, →− 

> P

).

We first establish two auxiliary lemmas concerning neural networks that will be needed in the translations. The first lemma states that we can arbitrarily increase the depth of a neural network. Recall the definitions of equivalence and [0 , 1] -equivalence from Section 2.1. 

Lemma E.1. For each d ∈ N, each neural network N of depth d and each d′ > d , there exists a [0 , 1] -equivalent neural network N ′ of depth d′. If d̸ = 0 , then N and N ′ are equivalent. Proof. Let N = ( n1, . . . , n k) be a neural network of depth d. We define N ′ := ( n′

> 1

, . . . , n ′

> k

)

where n′ 

> j

:= ReLU( Pki=1 wi,j ni + 0) where wj,j = 1 and wi,j = 0 for each i̸ = j. Now n′

> j

is equivalent to ReLU( nj ) and thus [0 , 1] -equivalent to nj for each j ∈ { 1, . . . , k }. This 27 is because each neuron nj is either a variable (if d = 0 ) or of the form ReLU( t) for some proto-neuron t (if d̸ = 0 ). In either case, for each evaluation map E such that E(x) ∈ [0 , 1] 

for each x ∈ VAR , we have E(nj ) ≥ 0 and thus E(n′ 

> j

) = E(ReLU( nj )) = E(nj ). Thus, N ′

is a [0 , 1] -equivalent neural network of depth d + 1 and we may iterate this process until we reach depth d′. If d̸ = 0 , then nj is of the form ReLU( t), and thus for each evaluation map 

E we have E(n′ 

> j

) = E(ReLU( nj )) = E(nj ). Thus, nj and n′ 

> j

are equivalent, which means that N and N ′ are equivalent. We next prove another auxiliary lemma, which intuitively shows that any number of neural networks can be modified such that the neural networks are pairwise identical apart from the output neurons. 

Lemma E.2. Given k ∈ Z+ and neural networks N1, . . . , N k, there exist neural networks 

N ′

> 1

, . . . , N ′ 

> k

such that Ni and N ′ 

> i

are [0 , 1] -equivalent for each i ∈ { 1, . . . , k }, and N ′

> 1

, . . . , N ′

> k

all have the same depth d and are identical up to depth d − 1. For each i ∈ { 1, . . . , k }, if 

Ni has depth at least 1, then Ni and N ′ 

> i

are equivalent. Proof. Intuitively, given t ∈ { 1, . . . , k }, we add every neuron from N1, . . . , N k to N ′ 

> t

, except for output neurons which are only added from Nt. We copy the weights and biases from 

N1, . . . , N k, and we also add the weight 0 between neurons that are not from the same neural network, since our neural networks need to be fully connected. See Figure 3 for an illustrated example. 

N1:

x1

x2

h1

h2

o1

o2

N2:

x2

x3

h′

> 1

h′

> 2

o′

> 1

o′

> 2

w

N ′

> 1

:

x1

x2

x3

h1

h2

h′

> 1

h′

> 2

o1

o2

w

N ′

> 2

:

x1

x2

x3

h1

h2

h′

> 1

h′

> 2

o′

> 1

o′

> 2

Figure 3: Top: Two neural networks N1 and N2. Bottom: Equivalent versions N ′ 

> 1

and N ′

> 2

where only the output neurons are different. Dashed lines indicate the weight 0 between two neurons. Next, we give the construction formally. First, let d be the maximum depth of N1, . . . , N k.By Lemma E.1, we can construct neural networks M1, . . . , M k of depth d such that Mj is 

[0 , 1] -equivalent to Nj for each j ∈ { 1, . . . , k }. Next, we construct the neural network N ′

> t

that is equivalent to Mt, starting with the embedded neural network N 0 

> t

of depth 0 of N ′

> t

and recursively moving to embedded neural networks N d′ 

> t

of increasing depths d′ until we reach N dt = N ′ 

> t

.28 • First, we define N 0 

> t

:= ( y1, . . . , y ℓ0 ), where y1, . . . , y ℓ0 are the variables of the input layers of M1, . . . , M k with no duplicates (unless d = 0 , in which case y1, . . . , y ℓ0 are the variables of just Mt). 

• Second, we define N 1 

> t

. Let (m1, . . . , m ℓ) be the concatenation of the embedded neural networks of depth 1 of M1, . . . , M k (unless d = 1 in which case let (m1, . . . , m ℓ) be 

Mt). We define N 1 

> t

= ( n1, . . . , n ℓ), where nj = ReLU 

Pℓ0 

> i=1

wi,j yi + bj



where bj is the bias of mj and wi,j is the weight from yi to mj if they are from the same neural network and otherwise wi,j = 0 .

• Next, we cover the case where d′ > 1. Let (m1, . . . , m ℓ) be the concatenation of the embedded neural networks of depth d′ of M1, . . . , M k (unless, again, d = d′ in which case let (m1, . . . , m ℓ) be Mt). Let N d′ 

> t

= ( n1, . . . , n ℓ) such that each nj corresponds to the neuron mj . Let (m′

> 1

, . . . , m ′ 

> ℓ′

) be the concatenation of the embedded neural networks of depth d′ + 1 of M1, . . . , M k. We define N d′+1  

> t

:= ( n′

> 1

, . . . , n ′ 

> ℓ′

) where 

n′ 

> j

= ReLU 

Pℓi=1 wi,j ni + bj



where bj is the bias of m′ 

> j

and wi,j is the weight from 

mi to m′ 

> j

if they are from the same neural network and otherwise wi,j = 0 .Lastly, we obviously define N ′ 

> t

:= N dt . The equivalence of Mt and N ′ 

> t

is now clear to see, and because Nt and Mt are [0 , 1] -equivalent, Nt and N ′ 

> t

are thus also [0 , 1] -equivalent. If 

Nt has depth at least 1, then Nt and Mt are equivalent, and thus Nt and N ′ 

> t

are equivalent. With the exception of the output neurons, the construction was identical for each t, which means that N ′

> 1

, . . . , N ′ 

> k

are pairwise identical up to depth d − 1.No, we are ready to show the translation from the logics RPL( ⊙)≤1 and LΠ 12 (→− 

> P

)≤1 to neurons. 

Lemma E.3. Let L be RPL( ⊙) or LΠ 12 (→− 

> P

). For each formula φ of L≤1, we can construct an equivalent neuron nφ.Proof. We modify the proof of Lemma D.2 such that the constructed terms are, in fact, neurons. We cover the case where L is RPL( ⊙). The case where L is LΠ 12 (→− 

> P

) follows by Propositions 2.7 and 2.8. Let φ be a formula of RPL( ⊙)≤1. We will construct a neuron nφ equivalent to φ by recursion over the structure of φ as follows. First, we cover the base cases. 

• If φ = p for some p ∈ PROP , then nφ := xp.

• If φ is a formula of RPL( ⊙)≤0, then there exists some r ∈ Q ∩ [0 , 1] such that φ is equivalent to r. We define nφ := ReLU  0 · x + r.Next, assume that ψ is a formula of RPL( ⊙)≤i and θ a formula of RPL( ⊙)≤j for some 

i, j ∈ { 0, 1}, and let nψ and nθ be the equivalent neurons. Both nψ and nθ can be seen as neural networks with a single output neuron, so by Lemma E.2 we can construct neurons 

n′ 

> ψ

and n′ 

> θ

that are [0 , 1] -equivalent to nψ and nθ (and thus equivalent to ψ and θ) such that n′ 

> ψ

and n′ 

> θ

have the same depth d and are identical up to depth d − 1.

• If i + j ≤ 1 and φ = ψ ⊙ θ, then without loss of generality we may assume that i = 0 .Because ψ is now a formula of RPL( ⊙)≤0, there exists some r ∈ Q ∩ [0 , 1] such that 

ψ is equivalent to r. We define nφ := ReLU( r · nθ + 0) .29 • If φ = ψ →L θ, then we first define mφ := ReLU((1 · n′ 

> ψ

+ −1 · n′

> θ

) + 0) , after which we define nφ := ReLU( −1 · mφ + 1) .Next, we prove the correctness of our construction. Let V be a valuation, and let E be the evaluation map such that E(x) = V (px) for each x ∈ VAR . We have to show that 

E(nφ) = V (φ).

• If φ = p for some p ∈ PROP , then nφ = xp and the claim is trivially true. 

• If φ is a formula of RPL( ⊙)≤0, then nφ = ReLU  0 · x + r where r ∈ Q ∩ [0 , 1] such that φ is equivalent to r. Now E(nφ) = max {0, 0 · E(x) + r} = max {0, r }. Now 

r ∈ Q ∩ [0 , 1] implies r ≥ 0 and thus E(nφ) = r = V (r). Because φ is equivalent to 

r, we have V (r) = V (φ) and thus E(nφ) = V (φ).Next, assume that the claim holds for formulae ψ of RPL( ⊙)≤i and θ of RPL( ⊙)≤j where 

i, j ∈ { 0, 1}, let nψ and nθ be the equivalent neurons, and let n′ 

> ψ

and n′ 

> θ

be as above. 

• If i + j ≤ 1 and φ = ψ ⊙ θ, then without loss of generality we may assume that 

i = 0 . Then nφ = ReLU( r · nθ + 0) where r ∈ Q ∩ [0 , 1] such that ψ is equivalent to r. Therefore we have E(nφ) = max {0, rE (nθ) + 0 } = max {0, rE (nθ)}. Because 

ψ is equivalent to r, we have r = V (ψ), and by the induction hypothesis we have 

E(nθ) = V (θ), and thus we get E(nφ) = max {0, V (ψ)V (θ)} = max {0, V (φ)}. Lastly, because V (φ) ∈ [0 , 1] , we have E(nφ) = V (φ).

• If φ = ψ →L θ, then nφ = ReLU( −1·mφ +1) where mφ = ReLU((1 ·n′ 

> ψ

+−1·n′

> θ

)+0) .Therefore E(nφ) = max {0, −E(mφ) + 1 } and E(mφ) = max {0, E (n′

> ψ

) − E(n′

> θ

)}. By the induction hypothesis, E(n′

> ψ

) = E(nψ) = V (ψ) and E(n′

> θ

) = E(nθ) = V (θ) and thus E(mφ) = max {0, V (ψ) − V (θ)}. Because V (ψ), V (θ) ∈ [0 , 1] we must have 

E(mφ) ≤ max {0, 1 − 0} = 1 which implies −E(mφ) + 1 ≥ − 1 + 1 = 0 and therefore 

E(nφ) = −E(mφ) + 1 . Because − max {a, b } = min {− a, −b} for all a, b ∈ R, we have 

−E(mφ) = min {0, −V (ψ) + V (θ)} and thus E(nφ) = min {0, −V (ψ) + V (θ)} + 1 .Because min {a, b } + c = min {a + c, b + c} for all a, b, c ∈ R and by the semantics of 

→L, we finally get E(nφ) = min {1, 1 − V (ψ) + V (θ)} = V (ψ →L θ) = V (φ).This completes the proof of correctness. 

Example E.4. Consider the formula φ = 13 →L



p ⊙ 12



of RPL( ⊙)≤1. By the construc-tion from Lemma E.3, we obtain the equivalent neuron nφ:

ReLU 



−1 · ReLU 



1 · ReLU 



0x + 0 xp + 13



+ −1 · ReLU 



0x + 12 xp + 0 



+ 0 



+ 1 



.

An illustration of the neural network N := ( nφ) is given in Figure 4. Before delving into the other direction of the characterisation, we first establish a useful lemma. 

Lemma E.5. Let k ∈ R+, let φ be a formula of RPL( ⊙) or LΠ 12 , let V be a valuation and let ℓ ∈ [−k, k ] such that V (φ) = scale k(ℓ). Then V (¬Lφ) = scale k(−ℓ).

30 x

xp

+ 13

+0 

+0 +1 

· 0

· 0

· 0

· 12

· 1

· (−1) 

· (−1) 

Figure 4: An illustration of the neural network N . Weights are written alongside edges and biases are written inside neurons. 

Proof. By the semantics of ¬L, V (¬Lφ) = 1 − V (φ) = 1 − scale k(ℓ). By the definition of 

scale k,

1 − scale k(ℓ) = 1 − k+ℓ 

> 2k

= 2k 

> 2k

− k+ℓ 

> 2k

= k−ℓ 

> 2k

= scale k(−ℓ),

and thus V (¬Lφ) = scale k(−ℓ).Next we will show the other direction of the characterisation, where we translate proto-neurons into the logics RPL and LΠ 12 (⊙−, →− 

> P

). We split the translation into two parts presented as lemmas. In the first lemma, we replace the rationals of a proto-neuron with integers. In the second lemma, we translate the modified proto-neuron into a formula of 

RPL or LΠ 12 (⊙−, →− 

> P

).

Lemma E.6. For each proto-neuron t there exists some D ∈ Z+ such that we can construct a proto-neuron s that satisfies the following: 

• s is equivalent to Dt and 

• s contains only integers, i.e., if s contains a subterm r for some r ∈ Q, then r ∈ Z.Proof. To obtain s, we intuitively multiply t with an appropriate integer constant D (which can be constructively chosen based on the rational constants in t) and recursively cancel out the denominators of all the rationals in t. For example, if t = 57



x + 37



, then we choose 

D = 7 · 7 = 49 and starting from 49 t = 49 

 57



x + 37

 

, we obtain the following sequence of proto-neurons that are all equivalent to 49 t: s0 = 5 



7



x + 37

 

, s1 = 5 



7 x + 7 · 37



and s2 = 5  7 x + 3 .We now present the general case formally. We start with the observation that if r ∈ Q,then r = nr 

> dr

for some numerator nr ∈ Z and denominator dr ∈ Z+ such that dr is not divisible by nr.Next, we show how to construct, for each subterm s of t, the appropriate integer Ds. First, if s = r for some r ∈ Q, then Ds := dr. If s = x for some x ∈ VAR , then Ds := 1 . Next, assume we have defined Ds′ and Ds′′ for subterms s′ and s′′ of t. If s = s′ + s′′ , then let 

d be the greatest common divisor of Ds′ and Ds′′ , and Ds := Ds′ Ds′′  

> d

. If s = s′s′′ , then 

Ds := Ds′ Ds′′ . Finally, if s = ReLU( s′), then Ds := Ds′ .Let D denote Dt. We start with the proto-neuron Dt and modify Dt into an equivalent proto-neuron s0 as follows. 31 • If t = r for some r ∈ Q, then r = nr 

> dr

and D = dr and thus we set s0 := nr.

• If t = x for some x ∈ VAR , then D = 1 and we set s0 := x.

• If t = t′ +t′′ for some proto-neurons t′ and t′′ , then D = Dt′ Dt′′  

> d

where d is the greatest common divisor of Dt′ and Dt′′ . We set s0 := Dt′′  

> d

Dt′ t′ + Dt′ 

> d

Dt′′ t′′ .

• If t = t′t′′ for some proto-neurons t′ and t′′ , then D = Dt′ Dt′′ and so we define 

s0 := ( Dt′ t′)( Dt′′ t′′ ).

• If t = ReLU( t′) for some proto-neuron t′, then D = Dt′ and we set s0 := ReLU( Dt′ t′).In each of the above cases, it is straightforward to show that s0 is indeed equivalent to 

Dt . Now in the case of the two first bullets above, we are done. In the case of the last three bullets, we move into the subterm Dt′ t′ (and Dt′′ t′′ ) of s0 and perform the same transformation to obtain a term s1 equivalent to s0. Recursively, we thus obtain s2, s3, etc. The process only branches in the case of the third and fourth bullets, and in each such case, the further transformations in one branch do not affect the further transformations in the other. When this process reaches the atomic level, we will have the proto-neuron s that we seek. A simple induction shows that s is equivalent to Dt and contains only integers. The next lemma intuitively shows how the proto-neuron s constructed in the above lemma can be translated into RPL and LΠ 12 (⊙−, →− 

> P

).

Lemma E.7. Let L be RPL or LΠ 12 (⊙−, →− 

> P

), and let t be a proto-neuron that contains only integers. For each i ∈ R+, there exists some K ∈ Z+ such that for each k ≥ K we can construct an (i, k )-equivalent formula φt of L.Proof. Intuitively, because in each multiplication in a proto-neuron one of the multiplicands does not contain any variables (and thus gets the same value in every evaluation map) and since t contains only integers, we can replace ⊙k in the proof of Lemma D.1 with the iterated sum operator J′ 

> n

defined in Appendix C. We cover the case where L is RPL as the case where L is LΠ 12 (⊙−, →− 

> P

) follows by Proposition 2.9. Let i ∈ R+, define rmax , j and K as in the proof of Lemma D.1 and let k ≥ K. We construct a formula φs for each subterm s of t as in the proof of Lemma D.1 with the exception of multiplications, where we define instead: 

• If s = t′t′′ for some some proto-neurons t′ and t′′ , then deg( t′) = 0 or deg( t′′ ) = 0 .

– If deg( t′) = deg( t′′ ) = 0 , then there exists some z ∈ Z such that E(s) = z for each evaluation map E. By the choice of k, we also have z ∈ [−k, k ]. We define 

φs := scale k(z).

– If deg( t′)̸ = 0 or deg( t′′ )̸ = 0 , then by symmetry and without loss of generality, we may assume that deg( t′′ ) = 0 . Then there exists some z ∈ Z such that 

E(t′′ ) = z for each evaluation map E. We define 

φs := 

(J′ 

> z

φt′ if z ∈ N

¬L

J′−z φt′ if z ∈ Z \ N.

32 We modify the proof of the correctness of the construction from the proof of Lemma D.1 as follows. Let s be a proto-neuron, let E be an evaluation map such that E(x) ∈ [−i, i ]

for each x ∈ VAR and let V be the unique valuation such that V (p) = scale k(E(xp)) 

for each p ∈ PROP . We have to show that V (φs) = scale k(E(s)) . The proof is again by induction over the structure of s and otherwise identical to the proof of Lemma D.1 except for multiplications where we do the following instead. Assume the claim holds for the proto-neurons t′ and t′′ , i.e., V (φt′ ) = scale k(E(t′)) and V (φt′′ ) = scale k(E(t′′ )) .Assume s = t′t′′ for some proto-neurons t′ and t′′ . We must show that V (φs) = scale k(E(s)) .If deg( t′) = deg( t′′ ) = 0 , then φs = scale k(E(s)) and thus V (φs) = scale k(E(s)) . Assume next that deg( t′)̸ = 0 or deg( t′′ )̸ = 0 . By symmetry, we again assume that deg( t′′ ) = 0 

and let z := E(t′′ ) whereby z ∈ Z. We begin by showing that E(t′) ∈ − k 

> 2n

, k

> 2n

 where 

n ∈ N is the smallest integer such that |z| ≤ 2n; by Lemma C.4, this will imply that 

V (J′|z| φt′ ) = scale k(|z|E(t′)) .We first show that jlength( t) ≥ 2n. Because j ≥ 1 and length( t) ≥ 2, we get jlength( t) ≥ j2.Next, since j ≥ 2 and j ≥ rmax , we get jlength( t) ≥ 2rmax . By the definition of rmax , we have rmax ≥ | z| and thus jlength( t) ≥ 2|z|. Lastly, by the choice of n we have |z| ≥ 2n−1,and thus jlength( t) ≥ 2 · 2n−1 = 2 n.Next, we show that E(t′) ∈ − k 

> 2n

, k

> 2n

. We recall from the proof of Lemma D.1 that 

|E(t′)| ≤ √k = k√k . Because k ≥ K ≥ j2length( t), we get √k ≥ pj2length( t) = jlength( t) and thus |E(t′)| ≤ kjlength( t) . Now because jlength( t) ≥ 2n, we have |E(t′)| ≤ k 

> 2n

, which implies 

E(t′) ∈ − k 

> 2n

, k

> 2n

. Now by Lemma D.1, we have V (J′|z| φt′ ) = scale k(|z|E(t′)) . We are now ready to cover the two cases. 

• If z ∈ N, then |z| = z and thus by the above observation we get 

V (φs) = V (J′ 

> z

φt′ ) = scale k(zE (t′)) = scale k(E(s)) .

• If z ∈ Z \ N, then |z| = −z. By the above, we get V (J′−z φt′ ) = scale k(−zE (t′)) .Now by Lemma E.5, we get 

V (φs) = V (¬L

J′−z φt′ ) = scale k(−(−zE (t′))) = scale k(zE (t′)) = scale k(E(s)) .

This concludes the proof of the correctness of the construction. 

Example E.8. Consider the neural network N = ( o) with the single output neuron 

o = ReLU  −8 · ReLU  4x + 8 y + −5 + 2 · ReLU(9 x + 7 y + 3) + 5  .

An illustration of N is given in Figure 5. Let i ∈ R+ and let k be a sufficiently large real number. By the construction from Lemma E.7, we obtain the following formula of RPL 

that is (i, k )-equivalent to the neuron o:

φo := 

 

¬L

J′

> 8

 J′ 

> 4

px ⊕′ J′ 

> 8

py ⊕′ scale k(−5) 



⊖ 12



⊕ 12



⊕′ J′

> 2

 J′ 

> 9

px ⊕′ J′ 

> 7

py ⊕′ scale k(3) 



⊖ 12



⊕ 12



⊕′ scale k(5) 



⊖ 12



⊕ 12 .

Now we can combine Lemmas E.6 and E.7 to obtain a translation from proto-neurons to formulae of RPL and LΠ 12 (⊙−, →− 

> P

).33 N : x

y

−5

+3 

+5 

· 4

· 8

· 9

· 7

· (−8) 

· 2

Figure 5: An illustration of the neural network N . Weights are written along the edges and biases are written inside the neurons. 

Lemma E.9. Let L be RPL or LΠ 12 (⊙−, →− 

> P

). For each proto-neuron and for each i ∈ R+,there exists some K ∈ Z+ such that for each k ≥ K we can construct an (i, k )-equivalent formula of L.Proof. Let t be a proto-neuron. By Lemma E.6 there exists some D ∈ Z+ such that we can construct a proto-neuron s equivalent to Dt such that s contains only integers. Then by Lemma E.7, for each i ∈ R+ there exists some Li ∈ Z+ such that for each integer ℓ ≥ Li

we can construct an (i, ℓ )-equivalent formula φs of L.Now let i ∈ R+, let K := 

l Li

> D

m

and let k ≥ K. Now kD ≥ Li and we can construct a formula φs of L that is (i, kD )-equivalent to s. It remains to show that φs is (i, k )-equivalent to t. Let E be an evaluation map such that E(x) ∈ [−i, i ] for each x ∈ VAR ,and let V be the unique valuation such that V (p) = scale k(E(xp)) for all p ∈ PROP . We have to show that V (φs) = scale k(E(t)) . Because φs is (i, kD )-equivalent to s, we have 

V (φs) = scale kD (E(s)) . Because s is equivalent to Dt we have E(s) = E(Dt ) = DE (t)

and thus V (φs) = scale kD (DE (t)) . Now by the definition of scale k, we get 

V (φs) = scale kD (DE (t)) = kD +DE (t)2kD 

= k+E(t)2k

= scale k(E(t)) .

Thus, φs is (i, k )-equivalent to t.We note that if we are interested in translating t into a formula of RPL( ⊙)≤1 or LΠ 12 (→− 

> P

)≤1

instead, then the application of Lemmas E.6 and E.7 is not necessary (in the case where k

is an integer). Instead, by Lemma 2.3, applying Lemma D.1 to t would directly result in a formula of RPL( ⊙)≤1 or LΠ 12 (→− 

> P

)≤1.Now, we are ready to prove Theorem 3.5. 

Theorem 3.5. Neural networks have the same expressive power (w.r.t. scaling) as the logics RPL( ⊙)≤1, LΠ 12 (→− 

> P

)≤1, RPL and LΠ 12 (⊙−, →− 

> P

).Proof. Let L be any of the mentioned logics. Given a neural network (n1, . . . , n m), we can apply the same reasoning as in the proof of Theorem 3.1 to find for each i ∈ R+ some 

K ∈ Z+ such that for each k ≥ K we can construct a tuple (φ1, . . . , φ m) of formulae of L

such that φj is (i, k )-equivalent to nj for each j ∈ { 1, . . . , m }.34 Given a tuple (φ1, . . . , φ m) of formulae of L, we first use Lemma E.3 to obtain for each φi an equivalent neuron ni. Now, as each ni can be interpreted as a neural network with a single output neuron, by Lemma E.2 we can replace n1, . . . , n m with neurons n′

> 1

, . . . , n ′ 

> m

such that each n′ 

> j

is [0 , 1] -equivalent to nj (and thus equivalent to φj ), and such that n′

> 1

, . . . , n ′

> m

all have the same depth d and are identical up to depth d − 1. This makes (n′

> 1

, . . . , n ′

> m

) aneural network. 

# F Translations between neurons and Łukasiewicz logic 

In this section, we will discuss the extent to which Łukasiewicz logic can express neural networks. We will first show some interesting places where our previous constructions fail when restricting to Łukasiewicz logic. Then, we will restrict our attention to a subclass of proto-neurons and the fragment of Łukasiewicz logic obtained by omitting the truth constant 0 from the syntax, and show translations back and forth. However, the result is not a fuzzy logic characterisation in the sense of Definition 2.13, because the translation to logic is more restricted than demanded by the definition. 

F.1 Applying the previous translations to Łukasiewicz logic 

By restricting Lemma E.3 to Łukasiewicz logic, we obtain the following corollary. 

Corollary F.1. For each formula φ of Łukasiewicz logic, we can construct an equivalent neuron nφ.

Next, we will consider the opposite direction and show some interesting places where the recursive construction from Lemmas D.1 and E.7 fails. The following proposition shows that the construction fails already in the atomic step for almost all rational constants. 

Proposition F.2. Let i, k ∈ R+ such that i ≤ k and let r ∈ Q \ {− k, k }. There exists no formula of Łukasiewicz logic that is (i, k )-equivalent to r.Proof. Theorem 2 in [23] states that for a function [0 , 1] n → [0 , 1] to be equivalent to a formula of Łukasiewicz logic, it must be representable by a finite number of polynomials with integer coefficients. In particular, this holds for constant functions. This implies that there is no formula of Łukasiewicz logic that is equivalent to r for any r ∈ Q ∩ ]0 , 1[ , as each polynomial with integer coefficients can only achieve the value r with finitely many inputs. This naturally holds even if we restrict to only those valuations where the truth values of proposition symbols belong to some subinterval [a, b ] of [0 , 1] where a̸ = b, as is the case with (i, k )-equivalence. Thus, for a formula φ to be (i, k )-equivalent to a term r for some 

r ∈ Q, we must have scale k(r) ∈ { 0, 1}, which implies that r ∈ {− k, k }.The construction from Lemmas D.1 and E.7 also fails in the recursion step. For example, the following proposition shows that even adding variables together is not possible to simulate in Łukasiewicz logic due to the scaling involved. 

Proposition F.3. Let i, k ∈ R+ such that i ≤ k and let x, y ∈ VAR . There exists no formula of Łukasiewicz logic that is (i, k )-equivalent to the proto-neuron x + y.

35 Proof. For the sake of contradiction, assume that φ is a formula of Łukasiewicz logic (i, k )-equivalent to x + y. Let E be an evaluation map such that E(z) ∈ [−i, i ] for all z ∈ VAR 

and let V be the valuation such that V (p) = scale k(E(xp)) for all p ∈ PROP . Because φ is 

(i, k )-equivalent to x + y, we must have V (φ) = scale k(E(x + y)) = scale k(E(x) + E(y)) .Now, let ψ denote the formula obtained from φ by replacing each instance of py with ¬Lpx.Now Lemma E.5 gives us V (¬Lpx) = scale k(−E(x)) = scale k(E(−x)) . This means that 

V (ψ) depends on E(−x) in exactly the same way that V (φ) depends on E(y), i.e., 

V (ψ) = scale k(E(x) + E(−x)) = scale k(E(x) − E(x)) = scale k(0) = scale k

 E  0 .

This is a contradiction, because ψ is now a formula of Łukasiewicz logic that is (i, k )-equivalent to 0, which is not possible by Proposition F.2. The construction from Lemmas D.1 and E.7 also fails when we try to apply the ReLU -function to a simple variable, again due to the scaling involved. 

Proposition F.4. Let i, k ∈ R+ such that i ≤ k. There exists no formula of Łukasiewicz logic that is (i, k )-equivalent to the proto-neuron ReLU( x).Proof. The proof is similar to the proof of Proposition F.3. For the sake of contradiction, let φ be a formula of Łukasiewicz logic that is (i, k )-equivalent to ReLU( x). Let E be an evaluation map such that E(y) ∈ [−i, i ] for all y ∈ VAR and let V be a valuation such that V (p) = scale k(E(xp)) for all p ∈ PROP . Because φ is (i, k )-equivalent to ReLU( x),we have V (φ) = scale k(E(ReLU( x))) = scale k(max {0, E (x)}).Now let ψ be the formula obtained from φ by replacing each instance of px with ¬Lφ. By Lemma E.5, V (¬Lφ) = scale k(−E(ReLU( x))) = scale k(E(−ReLU( x))) . Therefore, V (ψ)

depends on E(−ReLU( x)) in the exact same way as V (φ) depends on E(x), that is, 

V (ψ) = scale k(max {0, E (−ReLU( x)) }).

By the semantics of Q[ReLU , x i]i∈N and since − max {a, b } = min {− a, −b} for all a, b ∈ R,we get E(−ReLU( x)) = −E(ReLU( x)) = − max {0, E (x)} = min {0, −E(x)}, and therefore we have V (ψ) = scale k(max {0, min {0, −E(x)}} ). Now since max {0, min {0, a }} = 0 for all 

a ∈ R, we have V (ψ) = scale k(0) = scale k

 E  0 . This is a contradiction, because now ψ

is (i, k )-equivalent to 0, which is not possible by Proposition F.2. 

F.2 Łukasiewicz logic without any truth constants 

In spite of the fact that the recursive construction from Lemmas D.1 and E.7 fails for Łukasiewicz logic, we can nevertheless reverse-engineer which proto-neurons can be ex-pressed in Łukasiewicz logic. Let k ∈ R+. Łukasiewicz proto-neurons over k are defined as follows. 

• Each x ∈ VAR is a Łukasiewicz proto-neuron over k.

• If t and t′ are Łukasiewicz proto-neurons over k, then so is k − ReLU( t − t′).

Proposition F.5. Let k ∈ R+. For each Łukasiewicz proto-neuron over k and each i ∈ R+

such that i ≤ k, we can construct an (i, k )-equivalent formula of Łukasiewicz logic that does not contain the truth constant 0.

36 Proof. We construct the formula recursively over the structure of the Łukasiewicz proto-neuron. Let E be an evaluation map such that E(x) ∈ [−i, i ] for each x ∈ VAR and let V be the valuation such that V (p) = scale k(E(xp)) for each p ∈ PROP . Let t be a Łukasiewicz proto-neuron over k. We construct the (i, k )-equivalent formula φt by recursion over the structure of t.

• If t = x for some x ∈ VAR , then φt := px.

• If t = k − ReLU( t′ − t′′ ) for some Łukasiewicz proto-neurons t′ and t′′ over k, then we define φt := φt′ →L φt′′ .Next, we prove the correctness of the translation. The atomic case where t = x for some 

x ∈ VAR is trivial, so assume that the claim holds for Łukasiewicz proto-neurons t′ and t′′ 

over k and let t = k − ReLU( t′ − t′′ ). It remains to show that V (φt) = scale k(E(t)) .By the semantics of →L, we have V (φt) = min {1, 1−V (φt′ )+ V (φt′′ )}. Because φt′ and φt′′ 

are (i, k )-equivalent to t′ and t′′ , we have V (φt) = min {1, 1 − scale k(E(t′)) + scale k(E(t′′ )) }.By the definition of scale k, we get 

V (φt) = min 

n

1, 1 − k+E(t′)2k + k+E(t′′ )2k

o

= min 

n 2k 

> 2k

, 2k 

> 2k

+ −k−E(t′)2k + k+E(t′′ )2k

o

= min 

n k+k 

> 2k

, k+( k−E(t′)+ E(t′′ )) 2k

o

= min {scale k(k), scale k(k − E(t′) + E(t′′ )) }.

Now because scale k is an increasing function, min {scale k(a), scale k(b)} = scale k(min {a, b })

for all a, b ∈ R and thus V (φt) = scale k(min {k, k −E(t′)+ E(t′′ )}). Because for all a, b, c ∈ R

we have min {a+b, a +c} = a+min {b, c } , we get V (φt) = scale k(k+min {0, −E(t′)+ E(t′′ )}).As min {− a, −b} = − max {a, b } for all a, b ∈ R, V (φt) = scale k(k − max {0, E (t′) − E(t′′ )}).Now because of the semantics of ReLU , we have E(t) = k − max {0, E (t′) − E(t′′ )} and thus 

V (φt) = scale k(E(t)) . This means that φt is (i, k )-equivalent to t.For Łukasiewicz proto-neurons over 1, we also obtain a reverse translation, expressed below. 

Proposition F.6. For each formula of Łukasiewicz logic that does not contain the truth constant 0, we can construct an equivalent Łukasiewicz proto-neuron over 1.Proof. This follows directly from the proof of Lemma D.2, when we restrict to formulae of Łukasiewicz logic that do not contain the truth constant 0, which are simply built from proposition symbols and the connective →L.Propositions F.5 and F.6 together constitute a fuzzy logic characterisation of Łukasiewicz proto-neurons over 1 via the fragment L of Łukasiewicz logic obtained by omitting 0 from the grammar. However, it is not a characterisation in the sense of Definition 2.13, since the definition requires that we obtain an (i, k )-equivalent formula for each i ∈ R+ while Proposition F.5 only holds when i ≤ k and we have k = 1 . It also remains an open question for which Łukasiewicz proto-neurons over 1 we can obtain an equivalent neuron. Lastly, by Propositions F.5 and F.6, we obtain the following perhaps amusing corollary. 37 Corollary F.7. Each formula of Łukasiewicz logic that does not contain the truth constant 

0 is 1-equivalent to itself. Proof. Let φ be a formula of Łukasiewicz logic that does not contain the truth constant 0.By Proposition F.6, we can construct a Łukasiewicz proto-neuron t over 1 that is equivalent to φ, and by Proposition F.5, we can construct a formula φ′ of Łukasiewicz logic that does not contain the truth constant 0 such that φ′ is (1 , 1) -equivalent to t. By Lemma 2.16, φ′

is 1-equivalent to φ.It remains to show that φ′ = φ. We prove the claim by induction over the structure of 

φ. If φ = p for some p ∈ PROP , then by the construction from Lemma D.2, we have 

t = xp, and by the construction in Proposition F.5, we get φ′ = p = φ. Next, assume the claim holds for formulae ψ and θ, let tψ and tθ denote the proto-neurons obtained by the construction in Lemma D.2, and let ψ′ and θ′ denote the formulae obtained by the construction in Proposition F.5. Let φ = ψ →L θ. Now, by the construction in Lemma D.2, we get t = 1 − ReLU( tψ − tθ). By the construction in the proof of Proposition F.5, we have φ′ = ψ′ →L θ′. By the induction hypothesis, we have ψ′ = ψ and θ′ = θ and thus 

φ′ = ψ →L θ = φ.Corollary F.7 shows that each formula of Łukasiewicz logic that does not contain the truth constant 0 can use the interval  12 , 1 to simulate its own behaviour in the interval [0 , 1] .38