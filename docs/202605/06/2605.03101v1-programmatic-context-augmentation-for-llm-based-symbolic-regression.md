---
title: Programmatic Context Augmentation for LLM-based Symbolic Regression
title_zh: 基于大语言模型的符号回归的程序化上下文增强
authors: "Hao Liu, Xiao-Wen Yang, Atharva Sehgal, Yixin Wang, Lan-Zhe Guo, Yu-Feng Li, Yisong Yue"
date: 2026-05-04
pdf: "https://arxiv.org/pdf/2605.03101v1"
tags: ["query:sr-alg"]
score: 10.0
evidence: 基于大语言模型的符号回归与程序化上下文增强
tldr: 符号回归旨在从数据中发现数学表达式。针对现有基于大语言模型（LLM）的进化搜索方法仅依赖标量评估指标（如MSE）而忽略数据集丰富信息的问题，本文提出一种程序化上下文增强框架。该方法通过代码与数据集交互，主动进行数据分析并提取除聚合评分外的深入信号，从而引导LLM生成更精确的数学公式，显著提升了搜索效率和准确性。
selection_source: fresh_fetch
motivation: 现有的基于LLM的符号回归方法主要依赖单一的标量评估分数作为反馈，忽略了数据集中蕴含的丰富结构化信息。
method: 提出一种程序化上下文增强框架，利用代码交互主动分析数据集并提取信息信号，为LLM提供更丰富的搜索上下文。
result: 在LLM-SRBench等基准测试中，该方法在发现数学表达式的效率和准确性方面均显著优于现有的强基准模型。
conclusion: 通过程序化手段增强上下文信息，可以有效克服LLM在符号回归中的反馈局限性，为自动化科学发现提供更强大的工具。
---

## 摘要
符号回归（SR）是发现最能描述给定数据集的数学表达式的任务，仍然是科学发现中的一个基本挑战。传统方法主要基于遗传算法和相关的进化方法，已被证明是有用的，但存在可扩展性和表达能力的局限性。最近，基于大语言模型（LLM）的进化搜索方法被引入到符号回归中，并展现出良好的前景。然而，现有的基于 LLM 的方法通常依赖于标量评估指标（如均方误差）作为搜索过程中的唯一反馈来源，从而忽略了数据集中嵌入的丰富信息。为了解决这一局限性，我们提出了一种结合了程序化上下文增强的新型基于 LLM 的进化搜索框架。通过实现与数据集的基于代码的交互，我们的方法可以主动进行数据分析并提取信息信号，而不仅仅是聚合的评估分数。我们在 LLM-SRBench 等先进基准测试上评估了我们的框架，并证明了与强基线方法相比，该框架具有卓越的效率和准确性。

## Abstract
Symbolic regression (SR), the task of discovering mathematical expressions that best describe a given dataset, remains a fundamental challenge in scientific discovery. Traditional approaches, primarily based on genetic algorithms and related evolutionary methods, have proven useful but suffer from scalability and expressivity limitations. Recently, large language model (LLM)-based evolutionary search methods have been introduced into SR and show promise. However, existing LLM-based approaches typically rely on scalar evaluation metrics, such as mean squared error, as the sole source of feedback during the search process, thereby overlooking the rich information embedded in the dataset. To address this limitation, we propose a novel LLM-based evolutionary search framework that incorporates programmatic context augmentation. By enabling code-based interactions with the dataset, our method can actively perform data analysis and extract informative signals, beyond aggregated evaluation scores. We evaluate our framework on advanced benchmarks, such as LLM-SRBench, and demonstrate superior efficiency and accuracy compared to strong baselines.

---

## 论文详细总结（自动生成）

### 论文总结：基于大语言模型的符号回归程序化上下文增强（ProAug）

#### 1. 核心问题与整体含义
符号回归（SR）旨在从观测数据中自动发现简洁、可解释的数学公式。虽然基于大语言模型（LLM）的进化搜索方法在 SR 领域展现了潜力，但现有方法（如 LLM-SR）存在一个重大缺陷：**反馈机制过于单一**。它们仅依赖标量评估指标（如均方误差 MSE）作为反馈，忽略了数据集中蕴含的丰富统计特征和变量间关系。这导致 LLM 在搜索空间中缺乏明确的物理或数学引导。本文的研究动机是模拟人类科学家的探索过程，通过主动的数据分析（EDA）来增强 LLM 的上下文信息，从而提高公式发现的效率和准确性。

#### 2. 方法论
论文提出了 **ProAug（Programmatic Context Augmentation）** 框架，其核心思想是赋予 LLM “双重角色”：既是公式的提案者，也是数据的分析者。
*   **核心流程**：
    1.  **程序化数据分析阶段**：LLM 根据任务指令生成 Python 分析代码。这些代码用于计算描述性统计量（均值、方差等）、线性相关性，以及对特征进行非线性变换（如 $log, exp, sin, cos$）后的 $R^2$ 相关性测试。
    2.  **上下文增强阶段**：执行生成的代码，将提取到的统计信号（如“$log(y)$ 与 $log(x_1)$ 具有极高相关性”）转化为文本，嵌入到提示词（Prompt）中。
    3.  **假设生成与优化阶段**：LLM 在富含统计信息的上下文中生成候选公式骨架。随后使用外部求解器（如 BFGS）优化公式中的自由参数。
    4.  **进化管理**：维护一个经验缓冲区，根据适应度分数（负 NMSE）采样优秀的候选公式进行迭代改进。

#### 3. 实验设计
*   **数据集/基准测试**：使用了 **LLM-SRBench**，这是一个专门为防止 LLM 记忆已知公式而设计的基准。
    *   **LSR-Transform**：对费曼物理公式进行变量替换和符号重写，增加结构复杂度。
    *   **LSR-Synth**：结合科学术语生成的合成表达式，涵盖化学、生物、物理和材料科学四个领域。
*   **对比方法**：
    *   **LLM-SR**：现有的基于 LLM 的进化搜索基线。
    *   **Statistical Hint**：手动设计的固定统计提示（作为消融对比）。
*   **骨干模型**：Qwen3-4B-Instruct、Qwen3-8B、DeepSeek-V3.1。

#### 4. 资源与算力
*   **训练算力**：在进行监督微调（SFT）实验时，使用了 **2 台 NVIDIA A800 GPU**。
*   **训练细节**：Batch size 为 16，学习率为 5e-6，采用了 ZeRO Stage 2 优化技术。
*   **推理/搜索**：每个任务进化 150 个迭代步，每步生成 2 个样本。

#### 5. 实验数量与充分性
*   **实验规模**：从 LSR-Transform 中随机抽取 17 个任务，从 LSR-Synth 中抽取 16 个任务（共 33 个核心测试案例）。
*   **重复性**：每个实验重复运行 **3 次**并报告平均值，以减少 LLM 生成随机性的影响。
*   **充分性评价**：实验设计较为充分，不仅涵盖了零样本（Zero-shot）设置，还进行了监督微调（SFT）的对比，并提供了收敛效率分析和方差分析（Boxplot），证明了结果的客观性和公平性。

#### 6. 主要结论与发现
*   **性能飞跃**：ProAug 在所有模型规模上均优于基线。在使用 DeepSeek-V3.1 时，在 LSR-Transform 任务上的误差（NMSE）降低了 **3 倍**。
*   **模型能力正相关**：基础模型越强（如从 4B 到 DeepSeek-V3.1），ProAug 带来的增益越明显，说明强模型能更好地利用复杂的统计上下文。
*   **收敛速度快**：ProAug 在搜索初期就能快速锁定正确的函数形式，胜率随迭代次数增加稳步上升。
*   **稳定性高**：相比 LLM-SR，ProAug 的多次运行方差显著降低，表现出更强的鲁棒性。

#### 7. 优点
*   **主动交互**：改变了 LLM 仅作为“黑盒生成器”的角色，通过代码执行实现了与数据的深度交互。
*   **归纳偏置引导**：通过统计信号（如幂律关系的对数线性特征）为 LLM 提供了强有力的数学引导，缩小了搜索空间。
*   **互补性**：实验证明该方法与监督微调（SFT）可以结合使用，进一步提升性能。

#### 8. 不足与局限
*   **分析模板限制**：目前生成的分析代码仍受限于预定义的算子集（如 $sin, log$ 等），对于极其复杂或非标准的数学关系，可能无法提取有效信号。
*   **计算开销**：相比单一的 MSE 反馈，每一步增加代码生成与执行环节会增加推理时间和计算资源消耗。
*   **代码执行风险**：虽然在沙盒中运行，但依赖 LLM 生成代码的正确性，若代码报错则无法获取增强信息（尽管文中提到了重试机制）。

（完）
