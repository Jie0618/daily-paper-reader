---
title: "Frequency Ordered Ratio Families Arising from the Factorization of $p_{m-1}+1$"
title_zh: "源于 $p_{m-1}+1$ 分解的按频率排序的比例族"
authors: Alexander R Povolotsky
date: 2026-05-07
pdf: "https://arxiv.org/pdf/2605.08256v1"
tags: ["query:math-ai"]
score: 6.0
evidence: 研究比例序列和素数分解结构
tldr: "本研究探讨了由第 m-1 个素数加 1 的分解式 p_{m-1}+1 = L_m R_m（其中 L_m 为最大素因子）所产生的比例序列。针对满足 L_m > m 的项，研究者通过对 R_m 的出现频率进行降序排列，揭示了素数分布中的“族群”现象。文章解释了这种结构的数学来源，提出了基于算术级数中素数分布规律的启发式渐近模型，并通过数值对数分析验证了该模型对频率排序的解释力。"
selection_source: fresh_fetch
motivation: "旨在解释在 p_{m-1}+1 的分布图中观察到的“族群”现象及其背后的数学结构。"
method: "通过对 p_{m-1}+1 进行因子分解并提取非最大素因子的乘积项 R_m，根据其出现频率进行排序并建立启发式渐近模型。"
result: 成功识别出主导“族群”的频率排序序列，并利用算术级数中的素数理论解释了 R_m 的分布规律。
conclusion: 研究证明了 R_m 的频率排序能够有效捕捉素数加 1 结构中的主导族群，且启发式模型与数值观测结果高度吻合。
---

## 摘要
我们研究了一个源自 $p_{m-1}+1$ 分解的比例序列，其中 $p_n$ 表示第 $n$ 个素数。对于每个 $m \ge 3$，将 $p_{m-1}+1$ 写作 $L_m R_m$，其中 $L_m$ 是其最大素因子。限制在满足 $L_m > m$（等价于 $m \in \mathrm{A223881}$）的 $m$ 值，我们得到了一个 $R_m$ 值的多重集。将不同的 $R_m$ 按频率降序排列，得到一个以 $2,3,4,8,6,12,10,14,15,18,20,24,\dots$ 开头的新序列。本文解释了这种构造如何自然地源于 A223881 的结构，为什么在 $p_{m-1}+1$ 的图中会出现“族”现象，以及 $R_m$ 的频率排序如何捕捉到主要的族。此外，我们提出了一个启发式渐近模型，通过算术级数中素数的经典结果来解释观察到的频率排序，并利用数值双对数分析支持该模型。

## Abstract
We investigate a ratio sequence derived from the factorization of $p_{m-1}+1$, where $p_n$ denotes the $n$th prime. For each $m \ge 3$, write $p_{m-1}+1 = L_m R_m$ with $L_m$ the largest prime factor. Restricting to those $m$ for which $L_m > m$ (equivalently, $m \in \mathrm{A223881}$), we obtain a multiset of values $R_m$. Sorting the distinct $R_m$ by decreasing frequency yields a new sequence beginning \[ 2,3,4,8,6,12,10,14,15,18,20,24,\dots. \] This article explains how this construction arises naturally from the structure of A223881, why the ``family'' phenomenon appears in plots of $p_{m-1}+1$, and how the frequency ordering of $R_m$ captures the dominant families. Additionally, we propose a heuristic asymptotic model explaining the observed frequency ordering via classical results on primes in arithmetic progressions and support the model with numerical log-log analysis.