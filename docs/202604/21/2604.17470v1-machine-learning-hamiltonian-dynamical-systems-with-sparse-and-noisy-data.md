---
title: Machine Learning Hamiltonian Dynamical Systems with Sparse and Noisy Data
title_zh: 利用稀疏且含噪声的数据进行哈密顿动力系统的机器学习
authors: "Vedanta Thapar, Abhinav Gupta"
date: 2026-04-19
pdf: "https://arxiv.org/pdf/2604.17470v1"
tags: ["query:math-ai"]
score: 9.0
evidence: 学习哈密顿动力系统的符号表示
tldr: 针对非线性哈密顿动力系统在数据稀疏、噪声大或采样不规则时难以建模的问题，本文提出了一种可自适应的辛循环神经网络（ASRNNs）。该模型通过将物理结构显式融入学习架构，结合哈密顿学习与辛循环积分，避免了时间导数估计。实验证明，ASRNNs 仅需极少的数据点即可实现长期动力学预测，并能辅助 SINDy 等方法发现精确的符号方程，为从复杂数据中提取可解释的物理规律提供了稳健途径。
selection_source: fresh_fetch
motivation: 现有的动力系统发现方法在面对稀疏、高噪声或不规则采样的数据时表现不佳，难以提取准确的物理规律。
method: 提出 ASRNNs 模型，将哈密顿结构与辛循环积分结合，构建参数感知的结构保持架构，无需估计时间导数。
result: 该模型在每条轨迹仅有两个不规则时间点且存在噪声的情况下，仍能实现准确的长期预测并辅助恢复符号方程。
conclusion: 显式引入物理结构的深度学习架构能有效提升从极端稀疏和噪声数据中发现可解释哈密顿动力系统的鲁棒性。
---

## 摘要
机器学习已成为从数据中发现动力系统控制规律的强大工具。然而，当观测数据稀疏、含噪声或采样不规则时，大多数现有方法的性能会严重下降。在这项工作中，我们通过在学习架构中显式地融入物理结构，解决了在极端数据稀缺情况下学习非线性哈密顿动力系统符号表示的问题。我们引入了自适应辛循环神经网络（ASRNNs），这是一种感知参数且保持结构的模型，它将哈密顿学习与辛循环积分相结合，避免了时间导数估计，并实现了在噪声下的稳定学习。我们证明了即使每个训练轨迹仅由两个不规则间隔的时间点组成（且可能受到相关噪声的干扰），ASRNNs 也能准确预测长期动力学。利用 ASRNNs 作为保持结构的数据生成器，我们进一步利用独立的回归方法（SINDy 和 PySR）实现了符号发现，恢复了多项式系统的精确符号方程，并为非多项式哈密顿量提供了连贯的多项式逼近。我们的结果表明，此类架构可以为从稀疏且含噪声的数据中可解释地发现哈密顿动力学提供一条稳健的途径。

## Abstract
Machine learning has become a powerful tool for discovering governing laws of dynamical systems from data. However, most existing approaches degrade severely when observations are sparse, noisy, or irregularly sampled. In this work, we address the problem of learning symbolic representations of nonlinear Hamiltonian dynamical systems under extreme data scarcity by explicitly incorporating physical structure into the learning architecture. We introduce Adaptable Symplectic Recurrent Neural Networks (ASRNNs), a parameter-cognizant, structure-preserving model that combines Hamiltonian learning with symplectic recurrent integration, avoiding time derivative estimation, and enabling stable learning under noise. We demonstrate that ASRNNs can accurately predict long-term dynamics even when each training trajectory consists of only two irregularly spaced time points, possibly corrupted by correlated noise. Leveraging ASRNNs as structure-preserving data generators, we further enable symbolic discovery using independent regression methods (SINDy and PySR), recovering exact symbolic equations for polynomial systems and consistent polynomial approximations for non-polynomial Hamiltonians. Our results show that such architectures can provide a robust pathway to interpretable discovery of Hamiltonian dynamics from sparse and noisy data.

---

## 论文详细总结（自动生成）

这篇论文由牛津大学和德里大学的研究人员合作完成，探讨了在极端恶劣的数据条件下（稀疏、高噪声、不规则采样）如何利用机器学习发现哈密顿动力系统的物理规律。

以下是对该论文的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：在现实实验或天文观测中，数据往往是**稀疏**（采样点极少）、**含噪**且**采样不规则**的。现有的物理信息神经网络（如 HNN）通常依赖于时间导数的估计（如有限差分法），但在噪声环境下，导数估计会放大误差，导致模型失效。
*   **研究动机**：如何构建一种既能保持哈密顿系统物理结构（如能量守恒、辛结构），又能在极少量观测数据下稳健学习，并最终给出可解释的符号方程的架构？

### 2. 方法论
*   **核心思想**：提出**可自适应辛循环神经网络（ASRNNs）**。它结合了“参数感知”和“结构保持”两大特性，通过积分器（而非导数）进行训练。
*   **关键技术细节**：
    *   **哈密顿分解**：假设哈密顿量可分离为动能和势能之和：$H(q, p; \lambda) = K(p) + V(q; \lambda)$。其中 $\lambda$ 是系统参数。
    *   **参数感知（Adaptable）**：模型包含两个小型多层感知机（MLP），分别参数化 $K$ 和 $V$，并将系统参数 $\lambda$ 作为输入，使其能在不同参数空间内泛化。
    *   **辛循环积分（Symplectic Recurrence）**：不直接拟合导数，而是将神经网络嵌入到 **Verlet 积分算法**中。通过循环迭代，模型预测未来时刻的状态，并直接最小化预测轨迹与观测点之间的误差（轨迹匹配损失）。
    *   **符号发现流水线**：训练好的 ASRNN 充当“数据生成器”，产生加密、去噪的规则轨迹，再输入到 **SINDy** 或 **PySR** 等符号回归工具中提取显式物理方程。

### 3. 实验设计
*   **实验场景**：
    1.  **Henon-Heiles 系统**：一个经典的多参数多项式哈密顿系统，具有复杂的动力学行为（包括混沌）。
    2.  **Morse 势能系统**：一个非多项式、单参数系统，具有极强的不对称性和类似“硬墙”的动量反转特性。
*   **数据条件（极端挑战）**：
    *   **极度稀疏**：每个训练轨迹窗口仅包含 **2 个时间点**（初始点 + 1 个随机未来点）。
    *   **复杂噪声**：引入了 **Ornstein-Uhlenbeck (OU) 相关噪声**（模拟真实物理测量中的时间相关误差），而非简单的白噪声。
*   **对比基准（Benchmark）**：主要对比了自适应哈密顿神经网络（AHNNs）以及真实物理轨迹（Ground Truth）。

### 4. 资源与算力
*   **硬件**：使用单张 **Nvidia 4070Ti Super GPU (16GB VRAM)**。
*   **训练时长**：每个模型训练仅需**几分钟**。
*   **网络规模**：非常轻量化，MLP 仅有 2-3 层隐藏层，每层 30-50 个神经元。

### 5. 实验数量与充分性
*   **实验规模**：
    *   针对 Henon-Heiles 系统，在 16 组参数对上训练，在 81 组参数对上测试。
    *   进行了 40 组独立随机初始化的集成实验，以评估统计稳健性。
    *   涵盖了不同噪声强度（NSR 5%, 10%）和不同噪声相关时间（$\tau$）的消融实验。
*   **充分性与客观性**：实验设计非常严苛（2 点采样），并提供了**理论分析（定理 1 和 2）**，从数学上证明了为什么基于积分的 ASRNN 比基于导数的 AHNN 在噪声下更稳健。这种理论与实验结合的方式使其结论非常具有说服力。

### 6. 论文的主要结论与发现
*   **高精度预测**：即使在 10% 噪声和极度稀疏的条件下，ASRNN 仍能准确预测数百个时间步长的长期动力学，能量守恒误差保持在极低水平（约 $10^{-4}$）。
*   **符号恢复成功**：通过 ASRNN 生成的数据，SINDy 成功恢复了 Henon-Heiles 的精确多项式方程；PySR 为非多项式的 Morse 势能提供了准确的泰勒展开近似。
*   **参数泛化**：模型能够准确预测训练集中未出现过的参数值下的系统行为。
*   **理论验证**：证明了辛循环架构的损失梯度受噪声影响的程度远低于导数回归架构，解释了其鲁棒性的来源。

### 7. 优点
*   **物理归纳偏置强**：通过显式嵌入辛积分器，强制模型遵守物理定律，极大地降低了对数据量的需求。
*   **抗噪性极佳**：解决了传统 HNN 在处理真实（含噪）实验数据时的痛点。
*   **模块化与可解释性**：不只是一个黑盒预测器，而是作为符号回归的前置组件，实现了从原始数据到物理公式的闭环。

### 8. 不足与局限
*   **外推限制**：在附录中提到，当参数超出训练范围并发生**分叉（Bifurcation）**等定性性质改变时，模型在外推预测上仍面临挑战，且对权重初始化敏感。
*   **结构假设**：目前模型假设哈密顿量是**可分离的**（$K(p)+V(q)$），对于某些不可分离的复杂哈密顿系统（如强耦合系统）可能需要进一步扩展。
*   **符号回归依赖**：最终方程的质量仍受限于下游符号回归工具（如 PySR）的搜索效率和基函数库的选择。

（完）
