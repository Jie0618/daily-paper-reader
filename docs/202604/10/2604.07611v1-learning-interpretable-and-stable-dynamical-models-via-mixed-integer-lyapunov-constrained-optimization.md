---
title: Learning interpretable and stable dynamical models via mixed-integer Lyapunov-constrained optimization
title_zh: 通过混合整数李雅普诺夫约束优化学习可解释且稳定的动力学模型
authors: "Zhe Li, Ilias Mitrai"
date: 2026-04-08
pdf: "https://arxiv.org/pdf/2604.07611v1"
tags: ["query:sr-alg"]
score: 9.0
evidence: 数据驱动的可解释动力学模型和物理方程发现
tldr: 本研究提出一种基于混合整数李雅普诺夫约束优化的数据驱动方法，用于发现具有单一平衡点的稳定动力学模型。该方法通过基函数参数化微分方程和李雅普诺夫函数，将稳定性条件转化为训练数据的约束，并利用混合整数二次约束优化（MIQCP）求解全局最优解。该方法不仅能发现可解释的模型，还能在噪声环境下保持比基准模型更高的预测精度，实现了模型发现与稳定性证明的同步完成。
selection_source: fresh_fetch
motivation: 旨在解决从数据中学习既具有可解释性又具备数学稳定性保证的动力学模型这一难题。
method: 采用基函数参数化动力学方程与李雅普诺夫函数，并将稳定性判据建模为混合整数二次约束优化问题进行求解。
result: 实验表明该方法能准确还原系统真实模型及其李雅普诺夫函数，且在含噪声数据上的预测表现优于传统基准方法。
conclusion: 将李雅普诺夫稳定性约束引入优化过程，是构建稳健、可解释且具备稳定性保障的动力学模型的有效途径。
---

## 摘要
本文研究了具有单一平衡点的稳定动力学模型的数据驱动发现。所提出的方法对微分方程及其相关的李雅普诺夫（Lyapunov）函数采用了基函数参数化。这种建模方法能够以可解释的形式同时发现动力学模型和李雅普诺夫函数。稳定性的李雅普诺夫条件被作为训练数据的约束强制执行。由此产生的学习任务是一个混合整数二次约束优化问题，可以使用当前最先进的全局优化求解器求解至最优。对两个案例研究的应用表明，所提出的方法可以发现系统的真实模型及相关的李雅普诺夫函数。此外，在存在噪声的情况下，与不考虑李雅普诺夫相关约束的基准模型相比，使用该方法学习的模型具有更高的预测精度。

## Abstract
In this paper, we consider the data-driven discovery of stable dynamical models with a single equilibrium. The proposed approach uses a basis-function parameterization of the differential equations and the associated Lyapunov function. This modeling approach enables the discovery of both the dynamical model and a Lyapunov function in an interpretable form. The Lyapunov conditions for stability are enforced as constraints on the training data. The resulting learning task is a mixed-integer quadratically constrained optimization problem that can be solved to optimality using current state-of-the-art global optimization solvers. Application to two case studies shows that the proposed approach can discover the true model of the system and the associated Lyapunov function. Moreover, in the presence of noise, the model learned with the proposed approach achieves higher predictive accuracy than models learned with baselines that do not consider Lyapunov-related constraints.

---

## 论文详细总结（自动生成）

这篇论文提出了一种结合混合整数优化与李雅普诺夫（Lyapunov）稳定性理论的方法，旨在从数据中学习既具有可解释性又具备数学稳定性保障的动力学模型。以下是对该论文的深度总结：

### 1. 核心问题与研究动机
*   **核心问题**：如何在从观测数据中发现动力学方程（如 $\dot{x} = f(x)$）的同时，确保所学习的模型在数学上是稳定的（即轨迹会收敛到平衡点），并且模型形式对人类透明可读。
*   **研究动机**：
    *   **稳定性缺失**：传统的系统辨识或稀疏回归方法（如 SINDy）虽然能拟合数据，但在处理噪声数据时，学习到的模型可能在长程演化中发散，缺乏稳定性保障。
    *   **黑盒困境**：基于神经网络的稳定动力学模型（如 Neural Lyapunov Functions）虽然强大，但其内部机制不可解释，且验证其全局稳定性在计算上极其困难。
    *   **可解释性需求**：在工程和物理领域，研究者更倾向于获得简洁的符号表达式，以便进行后续的控制设计和理论分析。

### 2. 核心方法论
该方法的核心思想是将模型发现任务转化为一个**混合整数二次约束优化问题 (MIQCP)**。
*   **基函数参数化**：
    *   将动力学方程 $f(x)$ 和李雅普诺夫函数 $V(x)$ 分别表示为预定义基函数（如多项式、三角函数）的线性组合。
    *   引入**二进制变量**来控制基函数的选择，从而实现模型的稀疏性（即只保留最关键的项）。
*   **李雅普诺夫约束**：
    *   **正定性约束**：强制 $V(0)=0$ 且在其他数据点上 $V(x) > 0$。
    *   **负定导数约束**：强制 $\dot{V}(x) = \nabla V(x)^T f(x) \leq 0$。由于 $V$ 和 $f$ 的系数都是待优化的变量，这一项会产生**双线性项**（系数相乘），导致非凸性。
*   **优化目标**：最小化预测误差（$L_1$ 范数）以及模型复杂度的惩罚项（$L_0$ 范数）。
*   **求解技术**：利用最先进的全局优化求解器 **Gurobi**，通过分支定界法处理混合整数和非凸二次约束，确保在给定容差内找到全局最优解。

### 3. 实验设计
*   **实验场景**：
    1.  **阻尼摆 (Damped Pendulum)**：经典的非线性系统，包含三角函数项。
    2.  **交叉耦合振荡器 (Cross-coupled Oscillator)**：具有三次方项的复杂动力学系统。
*   **基准方法 (Benchmarks)**：
    *   **SSR (Stepwise Sparse Regression)**：逐步稀疏回归。
    *   **MIOSR (Mixed-Integer Optimization Sparse Regression)**：基于混合整数优化但不带稳定性约束的稀疏回归（类似于 SINDy 的优化版本）。
*   **对比维度**：在不同噪声水平（$\sigma \in [0, 0.1]$）下，对比向量场误差、系数还原准确度以及模型的稳定性。

### 4. 资源与算力
*   **软件环境**：使用 Gurobi 12.0.3 求解器，Python 编程环境（PySINDy 用于基准测试）。
*   **算力细节**：文中未明确提及具体的 CPU/GPU 型号。
*   **训练时长**：对于阻尼摆案例，求解时间约为 **2.7 秒**。这表明对于小规模系统，该方法的计算效率非常高。

### 5. 实验数量与充分性
*   **实验规模**：针对两个典型物理案例进行了深入分析。
*   **充分性评价**：
    *   **噪声鲁棒性测试**：实验涵盖了从无噪声到高噪声（10%）的多个梯度，证明了稳定性约束在噪声环境下具有显著的正则化作用。
    *   **消融对比**：通过与不带约束的 MIOSR 对比，清晰地展示了李雅普诺夫约束对提高预测精度的贡献。
    *   **局限性**：实验主要集中在二维系统（$N_x=2$），尚未展示在高维复杂系统上的表现。

### 6. 主要结论与发现
*   **同步发现**：该方法能够同时准确还原动力学方程的符号形式及其对应的李雅普诺夫函数。
*   **噪声下的优越性**：在存在测量噪声时，带有稳定性约束的模型比纯数据驱动模型具有更高的预测精度（误差降低可达两个数量级），因为稳定性约束排除了物理上不可能的非稳定解。
*   **全局最优性**：通过 MIQCP 框架，能够保证找到的是当前基函数库下的最优稀疏模型，而非局部最优。

### 7. 优点
*   **理论完备性**：将控制理论中的稳定性判据直接嵌入优化过程，提供了硬性的数学保证。
*   **高度可解释**：输出的是简洁的数学公式，而非黑盒参数。
*   **鲁棒性强**：稳定性约束有效地限制了搜索空间，防止模型过拟合噪声。

### 8. 不足与局限
*   **计算复杂度**：MIQCP 是 NP-hard 问题。随着状态维度和基函数库规模的增加，求解时间可能会呈指数级增长，难以扩展到超大规模系统。
*   **数据依赖性**：稳定性约束仅在训练数据点上强制执行。虽然实验表现良好，但在数据未覆盖的区域，模型仍可能存在不稳定性（尽管文中提到了后验验证方法）。
*   **平衡点假设**：目前的方法假设系统具有单一的已知平衡点（原点），对于具有多个平衡点或极限环的复杂吸引子系统，需要进一步扩展。

（完）
