---
title: "Multi-Modal Learning meets Genetic Programming: Analyzing Alignment in Latent Space Optimization"
title_zh: 多模态学习遇上遗传编程：分析潜空间优化中的对齐问题
authors: "Benjamin Léger, Kazem Meidani, Christian Gagné"
date: 2026-04-09
pdf: "https://arxiv.org/pdf/2604.08324v1"
tags: ["query:sr-alg"]
score: 10.0
evidence: 在潜空间符号回归中对齐符号和数值编码器
tldr: 本研究探讨了多模态学习在符号回归中的应用，重点分析了SNIP模型在潜空间优化中的对齐效果。SNIP通过对齐符号与数值编码器，试图将组合搜索转化为连续优化。然而，实验发现SNIP的跨模态对齐在优化过程中并未改善，且对齐粒度过粗，难以有效引导符号空间的精确搜索。该研究揭示了当前多模态符号回归方法的局限性，并指出细粒度对齐是未来的关键改进方向。
selection_source: fresh_fetch
motivation: 探究SNIP模型中符号与数值模态的对齐是否足够精细，以支持在潜空间中进行有效的符号回归优化。
method: 通过实验评估SNIP在优化过程中的对齐质量变化，并分析其对齐程度对符号搜索效率的实际影响。
result: 实验证明跨模态对齐并未随优化进程而提升，且现有的粗粒度对齐无法实现高效的原则性符号空间搜索。
conclusion: 多模态潜空间优化在符号回归中尚未实现预期的对齐引导效果，未来研究应聚焦于实现更细粒度的跨模态对齐。
---

## 摘要
符号回归 (SR) 旨在从数据中发现数学表达式，这一任务传统上通过遗传编程 (GP) 在符号结构上进行组合搜索来解决。潜空间优化 (LSO) 方法使用神经编码器将符号表达式映射到连续空间，从而将组合搜索转化为连续优化。SNIP (Meidani 等人, 2024) 是一种受 CLIP 启发的对比预训练模型，它通过引入多模态方法推进了 LSO：在共享潜空间中对齐符号编码器和数值编码器，以学习表型-基因型映射，从而使数值空间中的优化能够隐式地引导符号搜索。然而，这依赖于细粒度的跨模态对齐，而关于 CLIP 等类似模型的文献表明，这种对齐通常是粗粒度的。在本文中，我们研究了 SNIP 是否实现了其对符号回归进行有效双模态优化的承诺。我们的实验表明：(1) 在优化过程中，即使适应度增加，跨模态对齐也没有改善；(2) SNIP 学习到的对齐过于粗糙，无法在符号空间中有效地进行有原则的搜索。这些发现表明，虽然多模态 LSO 在符号回归方面具有巨大潜力，但有效的对齐引导优化在实践中仍未实现，这突显了细粒度对齐是未来工作的一个关键方向。

## Abstract
Symbolic regression (SR) aims to discover mathematical expressions from data, a task traditionally tackled using Genetic Programming (GP) through combinatorial search over symbolic structures. Latent Space Optimization (LSO) methods use neural encoders to map symbolic expressions into continuous spaces, transforming the combinatorial search into continuous optimization. SNIP (Meidani et al., 2024), a contrastive pre-training model inspired by CLIP, advances LSO by introducing a multi-modal approach: aligning symbolic and numeric encoders in a shared latent space to learn the phenotype-genotype mapping, enabling optimization in the numeric space to implicitly guide symbolic search. However, this relies on fine-grained cross-modal alignment, whereas literature on similar models like CLIP reveals that such an alignment is typically coarse-grained. In this paper, we investigate whether SNIP delivers on its promise of effective bi-modal optimization for SR. Our experiments show that: (1) cross-modal alignment does not improve during optimization, even as fitness increases, and (2) the alignment learned by SNIP is too coarse to efficiently conduct principled search in the symbolic space. These findings reveal that while multi-modal LSO holds significant potential for SR, effective alignment-guided optimization remains unrealized in practice, highlighting fine-grained alignment as a critical direction for future work.

---

## 论文详细总结（自动生成）

这是一份关于论文《Multi-Modal Learning meets Genetic Programming: Analyzing Alignment in Latent Space Optimization》的结构化总结：

### 1. 论文的核心问题与整体含义
*   **研究背景**：符号回归（SR）传统上依赖遗传编程（GP）进行组合搜索。近年来，潜空间优化（LSO）通过神经编码器将符号表达式映射到连续空间，将搜索转化为连续优化问题。
*   **核心动机**：SNIP 模型（2024年提出）受 CLIP 启发，通过对比学习对齐“符号（基因型）”和“数值（表型）”模态，旨在利用数值空间的优化来隐式引导符号搜索。然而，尽管 SNIP 在拟合精度上表现良好，但在检索准确的符号表达式方面却表现不佳。
*   **核心问题**：本文旨在探究 SNIP 的多模态对齐是否真正发挥了作用。具体研究两个假设：
    1.  **算法利用不足（H1）**：LSO 过程在优化时并未主动利用学习到的跨模态对齐。
    2.  **对齐粒度过粗（H2）**：学习到的对齐过于粗糙，无法区分结构相似但功能不同的符号表达式。

### 2. 论文提出的方法论
*   **核心思想**：通过实证研究和检索任务，对 SNIP 的跨模态对齐质量进行“压力测试”。
*   **关键技术细节**：
    *   **SNIP 架构**：包含两个 Transformer 编码器，分别处理符号序列（前缀表达式）和数值观测值（x, y 对）。
    *   **对齐机制**：使用 **InfoNCE 损失函数**进行对比预训练，通过最大化匹配对的余弦相似度来对齐两个模态。
    *   **LSO 流程**：在数值潜空间中进行搜索，使用**灰狼优化算法（GWO）**采样候选向量，通过解码器转为符号表达式，并利用 **BFGS 算法**进行常数精炼。
*   **分析方法**：
    *   **研究1**：在优化迭代过程中实时跟踪 $R^2$（拟合度）和跨模态相似度（对齐度）的变化。
    *   **研究2（检索任务）**：给定目标数值数据，要求模型从一组包含微小扰动（如算子替换、变量交换）的候选表达式中识别出正确的原表达式。

### 3. 实验设计
*   **数据集**：
    *   **SRBench 基础库**：包括 Feynman（费曼物理方程）和 Strogatz 动力系统基准。
    *   **合成数据集**：100 个符合 SNIP 训练分布的随机生成表达式，作为性能上限参考。
*   **Benchmark 与对比**：
    *   对比了 LSO 优化前后的对齐得分。
    *   检索任务中对比了**随机基准（Random Baseline）**。
*   **扰动策略**：设计了四种扰动：一元算子交换、二元算子交换、常数变更、变量替换。

### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的 GPU 型号、数量或总训练时长。
*   **实现细节**：作者使用了 SNIP 官方提供的预训练编码器和 LSO 微调后的解码器，实验侧重于推理和分析而非从头训练。

### 5. 实验数量与充分性
*   **实验规模**：
    *   使用了 82 个 Feynman 方程和 100 个合成方程进行检索测试。
    *   优化轨迹分析覆盖了 Feynman 和 Strogatz 全集，迭代次数固定为 80 次。
*   **充分性评价**：实验设计较为**充分且客观**。通过引入“合成数据集”排除了分布偏移的影响；通过“敏感性分析”排除了常数优化对结果的干扰。实验直接针对 SNIP 的核心假设进行验证，逻辑链条完整。

### 6. 论文的主要结论与发现
*   **对齐与优化脱节**：在 LSO 过程中，尽管 $R^2$ 显著提升，但符号与数值的对齐得分（相似度）保持平坦甚至下降。这证明优化过程完全独立于跨模态对齐（证实 H1）。
*   **对齐粒度极低**：在检索任务中，SNIP 在 Feynman 集上的准确率（18.3%）甚至**低于随机猜测**（27.3%）。在合成数据上仅与随机持平。
*   **算子敏感性缺失**：模型最难区分二元算子的交换（如 + 换成 ×），这表明对齐仅捕捉了粗略的数值趋势，而非精确的数学逻辑（证实 H2）。
*   **变量识别较好**：模型对变量替换较为敏感，这可能是因为变量索引在符号和数值模态中有直接的对应关系。

### 7. 优点
*   **批判性视角**：对当前流行的“多模态+符号回归”方向提出了冷静的审视，揭示了模型表现良好的假象（主要靠常数优化和强大的解码器，而非对齐引导）。
*   **分析工具创新**：将视觉-语言模型（如 CLIP）中的检索分析方法引入符号回归领域，为评估符号表示学习提供了新工具。
*   **路径指引**：明确指出“细粒度对齐”是未来多模态 LSO 的关键瓶颈。

### 8. 不足与局限
*   **对象单一**：主要针对 SNIP 模型进行了深入分析，虽然 SNIP 具有代表性，但结论是否完全适用于其他多模态 SR 模型（如 MMSR）尚需进一步验证。
*   **未提出改进方案**：论文属于“诊断型”研究，指出了问题但未给出如何实现细粒度对齐的具体算法改进。
*   **搜索算法限制**：实验主要基于 GWO 优化器，虽然这是 SNIP 原作的选择，但不同优化器是否会改变对齐的利用方式仍有探讨空间。

（完）
