---
title: Tensor-based computation of the Koopman generator via operator logarithm
title_zh: 基于算子对数的 Koopman 生成元张量计算
authors: "Tatsuya Kishimoto, Jun Ohkubo"
date: 2026-04-09
pdf: "https://arxiv.org/pdf/2604.07685v1"
tags: ["query:sr-alg"]
score: 8.0
evidence: 利用Koopman生成器从数据中识别非线性动力系统的控制方程
tldr: 本研究针对非线性动力系统控制方程识别中的维度灾难问题，提出了一种基于张量分解的Koopman算子生成器计算方法。该方法利用算子对数技术避免了对时间导数的依赖，并采用低秩张量训练（TT）格式来降低计算复杂度。通过在Lotka-Volterra和Lorenz-96系统上的实验，证明了该方法在处理高维系统时具有较高的系数恢复精度和良好的可扩展性。
selection_source: fresh_fetch
motivation: 传统的非线性动力系统识别方法在处理高维数据时面临维度灾难，且对采样间隔有较高要求。
method: 提出一种在低秩张量训练（TT）格式下，通过对Koopman特征值取对数来计算Koopman生成器的数据驱动方法。
result: 在4维和10维动力系统实验中，该方法成功恢复了向量场系数，并展示了对高维系统的扩展能力。
conclusion: 结合算子对数与张量训练格式能有效解决高维非线性系统的识别难题，提高计算效率与精度。
---

## 摘要
从数据中识别非线性动力系统的控制方程具有挑战性。虽然非线性动力学稀疏识别 (SINDy) 及其扩展被广泛用于系统辨识，但算子对数方法通过使用对数来避免时间微分，从而允许更大的采样间隔。然而，这些方法仍受维数灾难的影响。为此，我们提出了一种数据驱动的方法，通过在保持张量列 (TT) 格式的同时对 Koopman 特征值取对数，以低秩 TT 格式计算 Koopman 生成元。在 4 维 Lotka-Volterra 系统和 10 维 Lorenz-96 系统上的实验表明，该方法能够准确恢复向量场系数，并对更高维系统具有良好的可扩展性。

## Abstract
Identifying governing equations of nonlinear dynamical systems from data is challenging. While sparse identification of nonlinear dynamics (SINDy) and its extensions are widely used for system identification, operator-logarithm approaches use the logarithm to avoid time differentiation, enabling larger sampling intervals. However, they still suffer from the curse of dimensionality. Then, we propose a data-driven method to compute the Koopman generator in a low-rank tensor train (TT) format by taking logarithms of Koopman eigenvalues while preserving the TT format. Experiments on 4-dimensional Lotka-Volterra and 10-dimensional Lorenz-96 systems show accurate recovery of vector field coefficients and scalability to higher-dimensional systems.

---

## 论文详细总结（自动生成）

这是一份关于论文《Tensor-based computation of the Koopman generator via operator logarithm》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
论文的核心问题是如何从观测数据中高效、准确地识别高维非线性动力系统的控制方程。
*   **研究背景**：传统的系统辨识方法如 SINDy 依赖于对时间导数的估计，这在噪声环境下或采样间隔较大时表现不佳。
*   **研究动机**：基于 Koopman 算子的对数方法（Operator-logarithm）可以通过算子对数直接获取生成元，从而避免显式的时间微分，允许更大的采样间隔。然而，随着系统维度的增加，Koopman 矩阵的大小呈指数级增长，导致“维度灾难”，使得计算和存储在普通硬件上变得不可行。
*   **核心目标**：提出一种结合张量训练（Tensor Train, TT）格式和算子对数技术的方法，以低秩张量形式计算 Koopman 生成元，从而解决高维系统的辨识难题。

### 2. 方法论：核心思想与技术细节
该方法的核心思想是在不破坏张量列（TT）结构的前提下，通过特征分解实现算子对数的计算。
*   **核心流程**：
    1.  **张量化表示**：将观测函数（字典）定义为一维基函数的张量积，使 Koopman 算子自然具备张量结构。
    2.  **特征分解（AMUSEt 算法）**：利用 AMUSEt 算法直接在 TT 格式下计算 Koopman 算子的特征值 $\lambda_i$ 和对应的特征张量（Eigentensor）$\Xi$。
    3.  **对数映射**：根据 Koopman 算子与生成元的关系 $K = e^{tL}$，通过对特征值取对数得到生成元的特征值 $\mu_i = \log(\lambda_i) / T_s$。
    4.  **TT 格式重构**：在 TT 格式下通过收缩（Contraction）操作重构生成元 $L = \Xi \cdot \text{diag}(\mu_i) \cdot \Xi^+$。
    5.  **系数提取**：从生成的 TT 格式生成元中提取特定行，即可获得描述系统向量场的系数。

### 3. 实验设计
论文通过两个典型的动力系统验证了方法的有效性：
*   **数据集/场景**：
    1.  **4 维 Lotka-Volterra 系统**：用于验证系数恢复的精确度。
    2.  **10 维 Lorenz-96 系统**：用于验证方法在高维、混沌系统下的可扩展性和内存优势。
*   **Benchmark 与对比**：
    *   **对比对象**：传统的基于全矩阵的算子对数方法（Matrix-logarithm approach）。
    *   **评估指标**：均方根误差（RMSE）、归一化均方根误差（NRMSE）以及内存消耗（张量元素数量）。

### 4. 资源与算力
*   **软件环境**：使用了 Python 及其科学计算库（`scipy.integrate` 用于生成轨迹，`scipy.linalg` 用于基础线性代数运算）。
*   **硬件/算力说明**：论文**未明确说明**具体的 GPU 或 CPU 型号、核心数以及具体的训练/计算时长。但文中强调了该方法显著降低了内存需求（10 维系统下内存占用从约 $3.49 \times 10^9$ 降至 $4.37 \times 10^6$ 个元素）。

### 5. 实验数量与充分性
*   **实验规模**：论文主要进行了两组核心实验（4 维和 10 维系统）。
*   **充分性评价**：
    *   **优点**：实验涵盖了从简单非线性到复杂混沌系统的跨度，且 10 维系统的测试足以证明 TT 格式在处理维度灾难方面的优越性。
    *   **不足**：实验组数相对较少，缺乏对不同噪声水平、不同采样频率以及不同基函数选择的系统性消融实验。实验主要集中在确定性系统，未深入探讨随机动力系统。

### 6. 主要结论与发现
*   **高精度恢复**：在 4 维系统中，该方法恢复的向量场系数与真实值几乎完全一致（NRMSE 达到 $10^{-5}$ 级别）。
*   **内存效率**：在 10 维系统中，TT 格式将内存消耗降低了约三个数量级，证明了处理更高维系统的潜力。
*   **大采样间隔可行性**：通过算子对数技术，该方法在不需要时间导数信息的情况下，成功识别了复杂的 Lorenz-96 动力学。

### 7. 优点（亮点）
*   **突破维度限制**：首次将算子对数法扩展到 TT 格式，使得原本只能处理低维系统的对数辨识法能够应用于高维场景。
*   **无需数值微分**：继承了对数法的优点，对采样间隔的鲁棒性优于 SINDy 等直接微分方法。
*   **数学优雅性**：利用特征分解巧妙地绕过了在 TT 格式下直接计算矩阵对数的数学难题。

### 8. 不足与局限
*   **噪声敏感性未知**：论文主要在干净的模拟数据上进行实验，未量化分析测量噪声对 TT 格式特征分解及对数计算的影响。
*   **缺乏稀疏性约束**：目前的生成元计算未引入类似 LASSO 的稀疏正则化，对于具有大量候选函数但实际项很少的系统，可能会产生过拟合或非稀疏解。
*   **计算瓶颈**：虽然内存降低了，但 AMUSEt 算法中的特征分解在极大规模系统下仍可能面临计算时间上的挑战。
*   **基函数依赖**：TT 格式的效果高度依赖于基函数的张量积构造，对于某些无法简单分解的复杂观测空间可能不适用。

（完）
