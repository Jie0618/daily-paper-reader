---
title: Why Architecture Choice Matters in Symbolic Regression
authors: Chakshu Gupta
date: 2026-04-25
pdf: "https://arxiv.org/pdf/2604.23256v1"
tags: ["query:sr-alg"]
score: 9.0
evidence: 研究了基于树结构的符号回归架构的基本原理以及梯度下降优化方法。
tldr: 本研究探讨了在基于梯度下降的符号回归中，固定算子树结构对公式发现的影响。通过对比三种具有不同变量输入方式的结构，研究发现尽管某些结构在表达能力上更强，但其优化景观（Optimization Landscape）决定了最终能否成功恢复目标公式。实验表明，表达能力并不等同于可优化性，特定的架构选择和算子梯度特性对回归结果至关重要，这一发现挑战了仅关注模型表达能力的传统观点。
selection_source: fresh_fetch
motivation: 旨在探究在基于梯度的符号回归中，预定义的算子树结构如何影响目标公式的成功恢复。
method: "对比了三种共享相同算子集但变量输入方式不同的树结构，并在超过12,700次训练运行中分析其表现差异。"
result: 实验显示表达能力最强的结构在某些目标上反而无法收敛，且算子的梯度特性和树的平衡性对恢复率有决定性影响。
conclusion: 符号回归的成功主要取决于优化景观而非单纯的表达能力，架构选择在梯度搜索过程中起着至关重要的作用。
---

## Abstract
Symbolic regression discovers mathematical formulas from data. Some methods fix a tree of operators, assign learnable weights, and train by gradient descent. The tree's structure, which determines what operators and variables appear at each position, is chosen once and applied to every target. This paper tests whether that choice affects which targets are actually recovered. Three structures are compared, all sharing the same operator and target language but differing in how variables enter the tree; one is strictly more expressive. Across over 12,700 training runs, one structure recovers a target at 100% while another scores 0%, and the ranking reverses on a different target. Expressiveness guarantees that a solution exists in the search space, but not that gradient descent finds it: the most expressive structure fails on targets that a restricted alternative solves reliably. Switching the operator changes which targets succeed; reversing its gradient profile collapses recovery entirely. Balanced (non-chain) tree shapes are never recovered. These findings show that the optimization landscape, not expressiveness alone, determines what gradient-based symbolic regression recovers.

---

## 论文详细总结（自动生成）

这是一份关于论文《Why Architecture Choice Matters in Symbolic Regression》的结构化深入总结：

### 1. 论文的核心问题与整体含义
该研究探讨了**基于梯度的符号回归（SR）**中一个被长期忽视的问题：**算子树的架构（Architecture）选择如何影响公式的发现**。
在梯度下降驱动的符号回归中，通常会预先固定一个算子树结构，并通过学习权重来选择算子和变量。传统观点认为，只要架构具有足够的“表达能力”（Expressiveness），理论上就能找到目标公式。然而，本文通过实验证明，**表达能力并不等同于可优化性**。架构的选择会深刻改变优化景观（Optimization Landscape），导致某些目标公式在一种架构下能 100% 恢复，而在另一种表达能力更强的架构下却完全无法找回。

### 2. 论文提出的方法论
为了隔离“架构”这一变量，论文采用了以下核心设计：
*   **核心算子（EML）**：使用 $eml(a, b) = e^a - \ln b$ 作为基础算子。这是一个谢弗算子（Sheffer operator），仅靠这一个算子就能构建出所有初等函数。这使得研究者可以保持算子集不变，仅改变树的形状和变量输入路径。
*   **三种对比架构**：
    1.  **Eq. 6**：变量 $x$ 可以进入树的任何内部节点，表达能力最强。
    2.  **V16**：变量 $x, y$ 仅能从叶子节点进入，具有对称性。
    3.  **Hybrid**：结合了 V16 的主体和 Eq. 6 的根节点变量访问权限。
*   **训练流程**：采用两阶段调度。
    *   **搜索阶段**：使用 Adam 优化器和软权重（Softmax/Sigmoid）进行 6,000 次迭代。
    *   **硬化阶段（Hardening）**：通过退火和惩罚项，将软权重强制转换为确定的离散算子树结构。

### 3. 实验设计
*   **实验场景**：测试深度为 3 的 EML 树，涵盖了 4 种链式形状（LR, RL, RR, LL）和 1 种平衡形状（Balanced）。
*   **算子变体**：除了 EML，还引入了 **SML**（保持梯度不对称性）和 **RML**（反转梯度不对称性）来测试结论的普适性。
*   **对比基准**：在相同的目标语言（Target Language）下，对比不同架构对同一目标公式的恢复率（Exact Recovery Rate）。
*   **验证方式**：通过 500 个随机点的数值验证（RMSE < $10^{-6}$）来确认是否精确恢复。

### 4. 资源与算力
论文**未明确说明**具体的 GPU 型号或训练总时长。但文中提到进行了超过 **12,700 次**独立的训练运行（每个组合测试 256 次试验），这表明实验具有相当大的计算规模，通常需要集群支持。

### 5. 实验数量与充分性
*   **实验规模**：总计超过 12,700 次运行，样本量充足。
*   **充分性**：研究覆盖了多种架构、多种算子梯度特性以及所有可能的深度-3 树形结构。
*   **客观性**：通过固定随机种子和多种初始化策略（64 种子 × 4 初始化）来排除偶然性，并使用了 Clopper-Pearson 置信区间进行统计显著性分析，实验设计非常严谨。

### 6. 论文的主要结论与发现
*   **架构-目标交互作用**：没有一种架构在所有目标上都表现最好。例如，Eq. 6 在某些目标上 100% 成功，但在 V16 表现良好的目标上却是 0% 成功。
*   **表达能力悖论**：最强表达能力的架构（Eq. 6）在某些受限架构（V16）能轻松解决的任务上失败了。这证明**优化景观的几何形状比表达能力更重要**。
*   **梯度不对称性的影响**：算子的梯度传播特性（如左输入放大、右输入衰减）决定了架构的偏好。反转梯度方向会导致恢复率全面崩溃。
*   **平衡树的困境**：所有架构在恢复“平衡形状”的树时全部失败（0% 成功率）。作者推测这是因为梯度信号在根节点被平分，导致无法形成有效的搜索路径。

### 7. 优点
*   **变量隔离精准**：通过使用单一万能算子（EML），成功排除了语法和搜索算法的干扰，纯粹研究架构影响。
*   **揭示深层机制**：通过测量叶子节点的梯度范数比（Gradient Ratio），为架构为何失败提供了机械论层面的解释。
*   **挑战直觉**：有力地反驳了“模型越强（表达力越高）效果越好”的简单假设。

### 8. 不足与局限
*   **深度限制**：主要实验集中在深度-3 的树，虽然有少量深度-4 的测试，但更深层树的交互规律尚不明确。
*   **算子局限**：所选算子（EML, SML, RML）均为减法形式（$f(a) - g(b)$），未测试乘法或复合型算子的表现。
*   **基准脱节**：由于使用了特殊的 EML 语法，无法直接在 SRBench 或 Feynman 等标准符号回归基准数据集上进行对比。
*   **因果性证明**：梯度测量目前仅证明了相关性，尚未完全证明梯度分布是导致失败的唯一因果因素。

（完）
