---
title: Learning Interpretable PDE Representations for Generative Reconstructions with Structured Sparsity
authors: "Valerie Tsao, Nathaniel Chaney, Manolis Veveakis"
date: 2026-04-26
pdf: "https://arxiv.org/pdf/2604.23867v1"
tags: ["query:sr-alg"]
score: 8.0
evidence: 将潜变量参数化为控制偏微分方程的系数和源项
tldr: 针对科学测量中存在的噪声、空间覆盖不全和分辨率受限等挑战，本文提出了 LatentPDE 框架。该框架结合了潜扩散模型与可解释的偏微分方程（PDE）表示，将潜变量直接参数化为 PDE 的系数和源项。通过这种方式，模型不仅能实现高保真度的稀疏观测重建和超分辨率恢复，还能在处理结构化数据缺失时保持物理一致性，并提供预测不确定性估计。
selection_source: fresh_fetch
motivation: 传统的物理引导扩散模型往往依赖软损失惩罚或不可解释的表示，难以在复杂观测条件下实现精确的物理场重建。
method: 提出 LatentPDE 框架，通过将潜空间参数化为偏微分方程的系数和源项，构建具有物理可解释性的生成模型。
result: 实验证明该模型在多种配置下均能实现高保真度的场恢复，并能有效处理高度分散的结构化数据缺失。
conclusion: 该方法成功地将物理规律融入扩散模型，为科学数据的稀疏重建和超分辨率提供了一种兼具解释性与鲁棒性的新方案。
---

## Abstract
Scientific measurements are often bottlenecked by suboptimal conditions, whether that be noise, incomplete spatial coverage, or limited resolution, rendering accurate field reconstruction a difficult task. We introduce LatentPDE, a latent diffusion framework designed to simultaneously resolve sparse-observation reconstruction and super-resolution. While existing physics-guided diffusion models typically rely on soft loss penalties or uninterpretable representations, our approach enforces physical compliance by constructing an inherently interpretable latent space. Specifically, we parameterize the latent variables directly as the coefficients and source terms of an assumed governing PDE. In doing so, LatentPDE is able to reliably reconstruct dynamics across highly disparate and structured data gaps. Empirical results on diverse configurations demonstrate that our model achieves high-fidelity recovery at any desired resolution while also tracking the underlying predictive uncertainty.

---

## 论文详细总结（自动生成）

这是一份关于论文《Learning Interpretable PDE Representations for Generative Reconstructions with Structured Sparsity》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
在科学研究和工程监测中，获取高质量的物理场数据常受限于观测站稀疏、传感器噪声以及设备分辨率不足等客观条件。
*   **研究动机**：现有的逆向建模方法（如 PINNs 或算子学习）通常将物理规律作为“软约束”（损失函数中的惩罚项），这导致模型在推理时仍可能违反物理定律，且难以处理高度结构化的缺失数据（如特定区域完全无观测）。
*   **核心问题**：如何构建一个既能严格遵守物理定律，又能同时处理稀疏重建、去噪和超分辨率，并提供可靠不确定性量化的生成框架？

### 2. 方法论：LatentPDE 框架
该论文提出了 **LatentPDE**，其核心思想是将扩散模型的潜空间（Latent Space）直接定义为偏微分方程（PDE）的可解释参数。

*   **核心思想**：不学习抽象的特征向量，而是让潜变量 $z$ 直接代表 PDE 的物理系数（如扩散率 $\kappa$、平流速度 $v$）和源项（通过低频傅里叶系数参数化）。
*   **关键技术细节**：
    1.  **可解释潜空间**：潜变量被映射为具体的物理参数，通过区间约束的 Sigmoid 函数保证参数在物理合理范围内。
    2.  **硬性物理约束解码器**：使用一个**可微分谱求解器（Spectral Solver）**作为解码器。它接收潜空间中的 PDE 参数和初始条件，在频域内直接求解 PDE，从而保证输出场在数学上严格符合控制方程。
    3.  **双重编码策略**：
        *   **LatentPDE-MAP**：通过测试时优化（最大后验估计）寻找最匹配观测的潜变量。
        *   **LatentPDE-Enc**：使用摊销推理（Amortized Inference）通过神经网络直接预测潜变量。
    4.  **潜扩散模型（LDM）**：在参数化的潜空间中进行加噪和去噪，学习物理参数的先验分布，从而实现不确定性量化。
    5.  **物理引导采样**：在扩散模型的反向去噪过程中，引入观测残差的梯度引导，进一步提升重建精度。

### 3. 实验设计
*   **数据集与场景**：研究涵盖了三类具有代表性的线性 PDE：
    *   **平流-扩散方程**（抛物型）：模拟物质输送。
    *   **Klein-Gordon 方程**（双曲型）：模拟波动动力学。
    *   **Helmholtz 方程**（椭圆型）：模拟稳态场。
*   **Benchmark 与对比方法**：
    *   **传统数据同化**：3D-Var（三维变分）、EnKF（集合卡尔曼滤波）。
    *   **深度学习基准**：PINNs（物理信息神经网络）。
    *   **生成模型基准**：FunDPS（最新的基于功能空间的引导扩散模型）。
*   **测试条件**：设置了极端的观测环境，包括 5% 的极低采样率、高斯噪声（$\sigma=0.15$ 到 $0.45$）以及多种结构化掩码（如径向缺失、网格缺失、单块缺失）。

### 4. 资源与算力
*   **硬件使用**：论文明确指出所有模型训练和实验均在**单张 NVIDIA RTX A5000 GPU** 上完成。
*   **训练细节**：
    *   每个 PDE 方案贡献 5000 个合成训练样本。
    *   推理编码器训练 400 个 Epoch，扩散去噪器训练 600 个 Epoch。
    *   推理时间：LatentPDE-Enc 约为 33 秒/样本，LatentPDE-MAP 约为 78 秒/样本（包含扩散采样过程）。

### 5. 实验数量与充分性
*   **实验规模**：针对每类 PDE 进行了 500 组独立测试。
*   **消融实验**：论文对 MAP 优化步数、重建引导尺度（Guidance Scale）以及最终细化步骤（Final Refinement）进行了详细的消融研究。
*   **充分性评价**：实验设计较为充分且客观。通过对比不同类型的掩码（训练集未见的 OOD 掩码）验证了模型的泛化能力；通过 PSD（功率谱密度）分析验证了模型在多尺度特征恢复上的表现。对比基准既包含了工业界常用的传统方法，也包含了学术界前沿的生成模型。

### 6. 主要结论与发现
*   **高保真重建**：LatentPDE 在所有 PDE 类别中的 RMSE（均方根误差）均显著低于基准方法，尤其在处理结构化缺失时表现稳健。
*   **光谱一致性**：通过 PSD 曲线对比发现，LatentPDE 能精准捕获高频物理细节，而 PINNs 往往过度平滑，EnKF 则易产生高频伪影。
*   **不确定性量化**：模型生成的标准差图（Uncertainty Map）与实际误差图高度空间相关，证明了其预测置信度的可靠性。
*   **超分辨率能力**：由于解码器是基于连续谱分析的，模型可以无缝地将低分辨率观测重建为任意高分辨率的输出。

### 7. 优点与亮点
*   **硬约束保证**：通过将求解器嵌入架构，彻底解决了生成模型“违反物理常识”的顽疾。
*   **参数可解释性**：潜空间不再是黑盒，而是具有明确物理意义的系数，这为科学发现提供了潜在工具。
*   **鲁棒性**：在极高噪声和极端缺失（如只观测到边界）的情况下，依然能通过物理先验推断出全局场。

### 8. 不足与局限
*   **线性限制**：目前的方法主要针对线性、常系数 PDE。对于非线性系统（如 Navier-Stokes 方程）或具有复杂边界条件的非周期域，该方法的谱求解器可能不再适用。
*   **等效性问题（Equifinality）**：论文提到，在极度稀疏的情况下，可能存在多组不同的系数产生相似的观测结果，单时间步的快照可能不足以完全消除这种歧义。
*   **计算开销**：相比于纯判别式模型（如 FNO），扩散模型的迭代采样过程计算成本较高，实时性稍逊。

（完）
