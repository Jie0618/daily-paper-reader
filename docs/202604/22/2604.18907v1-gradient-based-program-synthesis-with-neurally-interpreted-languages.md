---
title: Gradient-Based Program Synthesis with Neurally Interpreted Languages
title_zh: 基于神经解释语言的梯度程序合成
authors: "Matthew V. Macfarlane, Clément Bonnet, Herke van Hoof, Levi H. S. Lelis"
date: 2026-04-20
pdf: "https://arxiv.org/pdf/2604.18907v1"
tags: ["query:math-ai"]
score: 9.0
evidence: 程序合成中符号化与神经方法的桥梁
tldr: 本研究提出神经语言解释器（NLI），通过结合符号方法的组合泛化能力与神经网络的端到端学习优势，解决了程序诱导中的权衡难题。NLI能自主学习离散的类符号语言，并利用可微神经执行器处理变长指令。通过Gumbel-Softmax实现端到端训练，并在推理时利用梯度下降优化程序搜索。实验表明，NLI在组合泛化和任务适应方面显著优于现有方法。
selection_source: fresh_fetch
motivation: 旨在克服符号方法难以扩展和神经网络泛化性差的局限，探索一种兼具组合性与梯度优化能力的程序合成方法。
method: 提出一种学习离散原始操作词汇的架构，利用可微神经执行器和Gumbel-Softmax松弛技术实现端到端训练及推理时的梯度搜索。
result: NLI在组合泛化和快速任务适应任务上优于上下文学习、测试时训练及连续潜程序网络等基准模型。
conclusion: 该研究成功展示了通过可微执行器学习离散语言，是结合神经网络搜索效率与符号系统泛化能力的有效路径。
---

## 摘要
程序归纳中的一个核心挑战长期以来一直是符号方法与神经方法之间的权衡。符号方法提供了组合泛化能力和数据效率，但其可扩展性受限于领域特定语言（DSL）等形式化方法，这些方法的创建耗费人力，且可能无法迁移到新领域。相比之下，神经网络能灵活地从数据中学习，但在组合和分布外设置下的泛化表现往往较差。我们通过一种名为神经语言解释器（NLI）的潜适应网络架构实例弥合了这一鸿沟，该架构能够端到端地学习其自身的离散、类符号编程语言。NLI 自主发现原语操作词汇表，并使用一种新型的可微神经执行器来解释这些原语的可变长度序列。这使得 NLI 能够表示不受固定计算步数限制的程序，从而使其能够解决比训练期间看到的更复杂的问题。为了使这些离散的、组合式的程序结构适用于基于梯度的优化，我们采用了 Gumbel-Softmax 松弛技术，使得整个模型能够进行端到端训练。至关重要的是，这种相同的可微性实现了强大的测试时适应。在推理时，NLI 的程序归纳器提供一个初始程序猜测。随后，该猜测通过神经执行器利用梯度下降进行细化，从而能够高效搜索最能解释给定数据的神经程序。我们证明了在需要组合泛化和对未知任务快速适应的任务中，NLI 的表现优于上下文学习、测试时训练和连续潜程序网络。我们的结果为开发结合了离散语言的组合性与神经网络的梯度搜索及端到端学习的模型开辟了一条新路径。

## Abstract
A central challenge in program induction has long been the trade-off between symbolic and neural approaches. Symbolic methods offer compositional generalisation and data efficiency, yet their scalability is constrained by formalisms such as domain-specific languages (DSLs), which are labour-intensive to create and may not transfer to new domains. In contrast, neural networks flexibly learn from data but tend to generalise poorly in compositional and out-of-distribution settings. We bridge this divide with an instance of a Latent Adaptation Network architecture named Neural Language Interpreter (NLI), which learns its own discrete, symbolic-like programming language end-to-end. NLI autonomously discovers a vocabulary of primitive operations and uses a novel differentiable neural executor to interpret variable-length sequences of these primitives. This allows NLI to represent programs that are not bound to a constant number of computation steps, enabling it to solve more complex problems than those seen during training. To make these discrete, compositional program structures amenable to gradient-based optimisation, we employ the Gumbel-Softmax relaxation, enabling the entire model to be trained end-to-end. Crucially, this same differentiability enables powerful test-time adaptation. At inference, NLI's program inductor provides an initial program guess. This guess is then refined via gradient descent through the neural executor, enabling efficient search for the neural program that best explains the given data. We demonstrate that NLI outperforms in-context learning, test-time training, and continuous latent program networks on tasks that require combinatorial generalisation and rapid adaptation to unseen tasks. Our results establish a new path toward models that combine the compositionality of discrete languages with the gradient-based search and end-to-end learning of neural networks.

---

## 论文详细总结（自动生成）

这篇论文提出了一种名为**神经语言解释器（Neural Language Interpreter, NLI）**的新型架构，旨在解决程序合成中符号化方法与神经网络方法之间的权衡难题。以下是对该论文的结构化总结：

### 1. 核心问题与研究动机
*   **核心矛盾**：符号化方法（如 DSL）具有极强的组合泛化能力和数据效率，但难以扩展且搜索成本极高；神经方法（如 Transformer）扩展性好，但在处理分布外（OOD）任务和组合逻辑时表现较差。
*   **研究动机**：作者希望构建一种既能像符号系统一样学习离散、可组合的“原语”，又能像神经网络一样利用梯度下降进行端到端训练和高效搜索的模型。
*   **目标**：实现一种能够自主发现离散编程语言并具备强大组合泛化能力的程序归纳系统。

### 2. 方法论：神经语言解释器 (NLI)
*   **核心思想**：NLI 属于潜适应网络（LAN）架构，它将程序表示为一系列离散的潜标记（Latent Tokens），并由一个可微的神经执行器逐一解释执行。
*   **关键技术细节**：
    *   **归纳器（Inductor/Encoder）**：将输入-输出示例对（Specification）映射为离散标记序列。使用 Transformer 提取特征，并通过均值池化实现对示例顺序的不变性。
    *   **解释器（Interpreter/Decoder）**：一个循环神经网络（RNN）结构的执行器。它每步消耗一个程序标记，更新中间状态 $s_t$，并结合跳跃门控（Skip-gating）机制处理变长程序。
    *   **Gumbel-Softmax 松弛**：在训练和搜索阶段，利用 Gumbel-Softmax 将离散的标记选择转化为可微过程，使得整个系统可以进行端到端梯度优化。
    *   **标记重用正则化（Token Reuse Loss）**：通过惩罚批次中唯一标记的数量，迫使模型发现通用的、可重用的原语，而非为每个任务记忆新标记。
*   **推理算法（梯度程序搜索）**：在测试时，模型首先通过归纳器给出初始程序猜测，然后利用梯度上升在连续松弛的潜空间中优化该程序，以最大化其对给定示例的解释力。

### 3. 实验设计
*   **数据集/场景**：
    1.  **自定义组合性基准测试**：包括 **Shift-L**（长度外推，如从位移1-5泛化到6-10）、**Shift-P**（原语提取，从复杂任务中反推基本操作）和 **Comp-I**（原语组合，将已知操作组合成新程序）。
    2.  **DeepCoder 组合版**：基于经典的 DeepCoder DSL，测试模型在处理更复杂函数（如 Map, Filter, Scanl1）时的组合能力。
*   **对比方法（Baselines）**：
    *   **上下文学习 (ICL)**：直接使用 Transformer 预测输出。
    *   **测试时训练 (TTT)**：在测试示例上微调模型参数。
    *   **潜程序网络 (LPN)**：使用连续潜空间的基准模型。
    *   **离散潜程序网络 (D-LPN)**：NLI 的消融变体，不具备循环执行机制。
    *   **神经符号方法**：如 ExeDec、Latent Programmer 等（在 DeepCoder 任务中对比）。

### 4. 资源与算力
*   **算力支持**：文中提到使用了 Google 的 **TPU Research Cloud (TRC)** 提供的 Cloud TPU，以及 **Modal** 提供的 GPU 算力。
*   **训练细节**：模型在自定义基准上训练了 10万个 Batch，在 DeepCoder 上训练了 20万个 Batch（Batch Size 为 512）。
*   **具体型号与时长**：文中**未明确说明**具体的 GPU/TPU 型号数量以及总训练时长。

### 5. 实验数量与充分性
*   **实验规模**：作者在 3 个自定义任务和 5 个 DeepCoder 泛化子任务上进行了测试，每个实验均使用 3 个随机种子取平均值。
*   **消融实验**：非常充分。作者针对循环执行、离散层、Gumbel 采样、跳跃标记、正则化损失等多个组件进行了消融，验证了每个设计对 OOD 泛化的必要性。
*   **客观性**：通过与多种主流神经架构（ICL, TTT, LPN）在相同计算预算下对比，证明了 NLI 的优势主要源于其架构设计而非单纯的参数量。

### 6. 主要结论与发现
*   **系统性泛化**：NLI 在所有 OOD 任务中显著优于纯神经基准。例如在 Shift-L 任务中，其他模型几乎完全失败（0%），而 NLI 达到了 99% 的准确率。
*   **梯度搜索的威力**：单纯的归纳器（Encoder）在 OOD 任务上表现一般，但结合梯度搜索后，性能大幅提升，证明了在离散原语空间进行梯度优化的有效性。
*   **自主发现原语**：可视化结果显示，NLI 能够自主学习到代表“左移1位”或“左移2位”的特定标记，并能通过组合这些标记来完成未见过的长距离位移任务。

### 7. 优点与亮点
*   **架构创新**：成功将“离散语言表示”与“可微神经执行”结合，实现了无需人工定义 DSL 的端到端程序合成。
*   **长度外推能力**：由于采用了循环解释机制，NLI 不受训练时程序长度的限制，展现了极强的组合灵活性。
*   **搜索效率**：相比于符号化搜索（如穷举或启发式搜索），基于梯度的搜索在处理大规模词汇表时更具潜力。

### 8. 不足与局限
*   **推理成本**：测试时的梯度搜索虽然有效，但计算开销较大（需要多次迭代优化），可能限制其在实时场景的应用。
*   **表达能力限制**：目前的解释器每层只能处理一个标记，且不支持复杂的条件分支或参数化原语（如 `add(k)`，其中 k 是变量）。
*   **梯度风险**：随着程序长度和词汇量的增加，可能会遇到梯度消失或爆炸的问题（尽管实验中尚未观察到）。
*   **任务复杂度**：目前主要集中在序列处理和简单的函数式编程，尚未在更复杂的通用编程任务上验证。

（完）
