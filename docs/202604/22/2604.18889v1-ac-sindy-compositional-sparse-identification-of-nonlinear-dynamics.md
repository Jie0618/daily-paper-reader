---
title: "AC-SINDy: Compositional Sparse Identification of Nonlinear Dynamics"
title_zh: AC-SINDy：非线性动力学的组合式稀疏识别
authors: Peter Racioppo
date: 2026-04-20
pdf: "https://arxiv.org/pdf/2604.18889v1"
tags: ["query:sr-alg"]
score: 10.0
evidence: 用于非线性动力学识别的 SINDy 组合扩展
tldr: AC-SINDy 是一种改进的非线性动力学稀疏识别框架，旨在解决传统 SINDy 在处理复杂特征库时的扩展性问题。该方法利用算术电路构建组合式特征表示，通过线性函数与乘法交互的组合替代显式枚举基函数。结合潜状态推理与多步监督机制，AC-SINDy 在提高抗噪能力的同时，实现了更高效的参数化和可解释的方程恢复，在非线性及混沌系统实验中表现优于标准 SINDy。
selection_source: fresh_fetch
motivation: 传统 SINDy 依赖显式枚举基函数，导致在处理复杂非线性特征时面临组合爆炸和扩展性差的问题。
method: 提出基于算术电路的组合式特征表示，通过线性函数与乘法交互构建特征，并结合潜状态推理与多步监督进行动力学识别。
result: 在非线性及混沌系统上的实验表明，该方法能准确恢复可解释的控制方程，且在扩展性和抗噪性方面优于标准 SINDy。
conclusion: AC-SINDy 通过结构化的组合表示提升了动力学识别的效率与鲁棒性，为复杂系统的建模提供了一种紧凑且可扩展的新方案。
---

## 摘要
我们提出了 AC-SINDy，这是非线性动力学稀疏识别（SINDy）框架的一个组合式扩展，它使用基于算术电路的结构化表示取代了显式特征库。该方法不再枚举候选基函数，而是通过线性函数和乘法交互的组合来构建非线性特征，从而实现紧凑且可扩展的参数化，并允许直接在计算图上实施稀疏性。我们还引入了一种将状态估计与动力学识别分离的方案，通过将隐状态推理与共享动力学及多步监督相结合，在保持可解释性的同时提高了对噪声的鲁棒性。在非线性和混沌系统上的实验表明，该方法能够恢复准确且可解释的控制方程，同时比标准 SINDy 具有更好的扩展性。

## Abstract
We present AC-SINDy, a compositional extension of the Sparse Identification of Nonlinear Dynamics (SINDy) framework that replaces explicit feature libraries with a structured representation based on arithmetic circuits. Rather than enumerating candidate basis functions, the proposed approach constructs nonlinear features through compositions of linear functions and multiplicative interactions, yielding a compact and scalable parameterization and enabling sparsity to be enforced directly over the computational graph. We also introduce a formulation that separates state estimation from dynamics identification by combining latent state inference with shared dynamics and multi-step supervision, improving robustness to noise while preserving interpretability. Experiments on nonlinear and chaotic systems demonstrate that the method recovers accurate and interpretable governing equations while scaling more favorably than standard SINDy.

---

## 论文详细总结（自动生成）

这是一份关于论文《AC-SINDy: Compositional Sparse Identification of Nonlinear Dynamics》的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：传统的非线性动力学稀疏识别（SINDy）框架依赖于预定义的候选基函数库（如多项式、三角函数）。当系统维度增加或非线性阶数提高时，该函数库的大小会呈**组合爆炸式增长**（$O(d^p)$），导致计算成本剧增且难以处理复杂的交互结构。
*   **研究动机**：许多物理系统具有稀疏且低阶的交互，但其整体函数形式可能很复杂。作者希望通过“组合式表示”而非“显式枚举”来构建特征，利用算术电路（Arithmetic Circuits）的思想，以更紧凑、可扩展的方式发现动力学方程。

### 2. 方法论
AC-SINDy 的核心思想是将动力学函数表示为一个可学习的计算图，主要包含以下关键技术：
*   **组合式架构（算术电路）**：
    *   不再预设基函数，而是通过**掩码线性层**（Masked Linear）和**交互层**（Interaction layer，即乘法运算）的堆叠来递归构建特征。
    *   这种结构等效于多变量多项式的低秩张量分解（如 CP 分解），能以 $O(pd^2)$ 的参数量表达原本需要 $O(d^p)$ 个项的复杂多项式。
*   **特征归一化（Feature Normalization）**：
    *   针对乘法交互导致的尺度不稳定性，引入数据驱动的特征归一化。通过固定特征的标准差（使用 `stopgrad`），确保学习到的系数反映的是功能重要性而非特征量级，解决了组合模型中的不可识别性问题。
*   **重要性剪枝（Importance-Based Pruning）**：
    *   不同于 SINDy 的权重绝对值阈值法，AC-SINDy 使用基于一阶泰勒展开的梯度重要性估计来剪枝计算图中的边，从而实现结构稀疏性。
*   **状态估计与动力学分离**：
    *   引入编码器提取潜状态，并结合**多步预测一致性（Multi-step Consistency）**损失。这种设计允许模型在含有噪声的观测数据中自动进行滤波，并强制动力学模型在长时间步内保持自洽。

### 3. 实验设计
*   **实验场景**：
    1.  **2D 非线性系统**：包含线性、双线性和二次项。
    2.  **Lorenz 系统**：经典的 3D 混沌系统。
    3.  **带正弦驱动的 Lorenz 系统**：增加非自治项以测试复杂性。
*   **基准对比（Benchmark）**：主要对比对象是**标准 SINDy**。
*   **评估指标**：方程恢复的准确性（系数对比）、预测轨迹的质量（相图一致性）、参数随维度的增长曲线。

### 4. 资源与算力
*   **算力说明**：论文中**未明确说明**具体的 GPU 型号、数量或训练时长。
*   **实现细节**：提到使用 PyTorch 实现，并在 GitHub 上开源了代码。由于处理的是低维动力学系统（2D 和 3D），预计单卡消费级 GPU 或 CPU 即可在短时间内完成训练。

### 5. 实验数量与充分性
*   **实验规模**：论文展示了 3 个典型动力学系统的恢复结果，并进行了消融性质的分析（如剪枝过程中的损失变化、不同阶数下的参数增长对比）。
*   **充分性评价**：
    *   **优点**：实验涵盖了从简单非线性到复杂混沌系统的过渡，并专门设置了噪声实验（$\sigma=0.05$ 的高斯噪声）来验证鲁棒性。
    *   **不足**：实验目前仅限于低维系统（$d \le 3$），尚未在高维偏微分方程（PDE）或大规模多体系统上进行验证。对比方法较为单一，主要针对 SINDy 及其变体。

### 6. 主要结论与发现
*   **参数效率**：AC-SINDy 的参数增长远慢于标准 SINDy，尤其在高阶交互场景下优势明显。
*   **方程恢复**：模型能够成功识别出 2D 系统和 Lorenz 系统的核心交互项（如 $xy, xz$ 等），恢复出的系数与真实值高度接近。
*   **抗噪性**：通过多步一致性训练，模型在噪声环境下仍能提取出平滑且准确的动力学规律，表现优于单纯的一步回归。

### 7. 优点
*   **扩展性强**：通过组合表示规避了基函数库的组合爆炸问题。
*   **结构灵活**：算术电路结构允许中间特征复用，比单纯的稀疏回归更具表达力。
*   **端到端滤波**：将状态估计与识别耦合，增强了对实际观测噪声的处理能力。
*   **可解释性**：尽管使用了神经网络架构，但通过剪枝和代数传播，最终仍能导出显式的符号方程。

### 8. 不足与局限
*   **高维验证缺失**：尚未证明在数十或数百维系统中的有效性。
*   **伪项风险**：在 Lorenz 系统实验中，虽然识别了主导项，但也产生了一些微小的伪项（Spurious terms），说明剪枝策略在复杂混沌系统中仍有优化空间。
*   **先验依赖**：虽然不需要显式库，但仍需预设电路的层数和交互类型（如乘法阶数），这在某种程度上是另一种形式的先验。

（完）
