---
title: Finite Expression Method with TranNet-based Function Learning for High-Dimensional Partial Differential Equations
title_zh: 基于 TransNet 函数学习的有限表达式方法求解高维偏微分方程
authors: "Phuoc-Toan Huynh, Feng Bao, Haizhao Yang, Ahmed Zytoon"
date: 2026-04-24
pdf: "https://arxiv.org/pdf/2604.22208v1"
tags: ["query:sr-alg"]
score: 8.0
evidence: 使用解析表达式生成的函数空间来逼近偏微分方程的解
tldr: 针对高维偏微分方程（PDE）求解中存在的“维度灾难”问题，本文提出了一种结合有限表达式方法（FEX）与TransNet初始化技术的机器学习求解器。该方法通过浅层神经网络算子生成函数池，并利用TransNet进行参数初始化，从而在函数空间内逼近PDE解。研究表明，该方法在保持多项式级内存复杂度的同时，能实现极高的计算精度，为解决高维PDE提供了一种高效且可扩展的新途径。
selection_source: fresh_fetch
motivation: 传统数值方法在处理高维偏微分方程时面临严重的维度灾难，导致计算效率和可扩展性受限。
method: 在有限表达式方法（FEX）基础上，引入由TransNet初始化的浅层神经网络算子来构建函数池。
result: 实验结果显示，该方法在多个高维PDE问题上达到了极高的精度，且内存需求随维度呈多项式增长。
conclusion: 结合TransNet的FEX扩展方法是求解高维偏微分方程的一种有效且具有竞争力的替代方案。
---

## 摘要
本文研究了一种用于求解高维偏微分方程（PDEs）的基于机器学习的求解器。由于维数灾难严重限制了经典数值方法的扩展性，高效地计算此类问题的精确解仍然具有挑战性。我们的方法基于近期开发的有限表达式方法（FEX），该方法在由有限个解析表达式生成的函数空间中逼近 PDE 的解。该框架已被证明能够以多项式级的内存复杂度和良好的计算成本实现高精度，在某些情况下甚至达到机器级精度。我们提出了 FEX 的一种扩展，其中函数池由浅层神经网络算子生成，其参数使用可迁移神经网络方法 TransNet 进行初始化。数值实验表明，所提出的扩展方法是求解若干高维偏微分方程的一种有效替代方案。

## Abstract
In this paper, we study a machine-learning-based solver for high-dimensional partial differential equations (PDEs). Computing accurate solutions efficiently for such problems remains challenging because of the curse of dimensionality, which severely limits the scalability of classical numerical methods. Our approach builds on the recently developed finite expression method (FEX), which approximates PDE solutions in a function space generated by finitely many analytic expressions. This framework has been shown to achieve high, and in some cases machine-level, accuracy with polynomial memory complexity and favorable computational cost. We propose an extension of FEX in which the functional pool is generated by shallow neural network operators whose parameters are initialized using the transferable neural network method TransNet. Numerical experiments suggest that the proposed extension is an effective alternative for solving several high-dimensional PDEs.

---

## 论文详细总结（自动生成）

这是一份关于论文《Finite Expression Method with TranNet-based Function Learning for High-Dimensional Partial Differential Equations》的结构化总结：

### 1. 核心问题与整体含义
该论文的核心问题是如何高效、精确地求解**高维偏微分方程（PDEs）**。
*   **研究动机**：传统数值方法（如有限元、有限差分）在处理高维问题时面临“维数灾难”，计算成本随维度呈指数级增长。虽然深度学习方法（如 PINNs）提供了一种路径，但在极高维度下往往难以达到高精度，且内存开销巨大。
*   **整体含义**：本文通过扩展**有限表达式方法（FEX）**，引入**TransNet（可迁移神经网络）**来生成和初始化候选函数池，旨在构建一个既能规避维数灾难，又能实现机器级精度的高维 PDE 求解框架。

### 2. 方法论
论文提出的核心方法是将 FEX 的符号搜索能力与 TransNet 的特征学习能力相结合：
*   **有限表达式方法 (FEX)**：将 PDE 的解表示为一棵二叉树，节点由算子（如 $+$, $\times$, $\sin$, $\exp$）组成。求解过程被转化为一个组合优化问题：
    1.  **结构搜索**：利用强化学习（策略梯度法）在算子库中搜索最优的表达式结构。
    2.  **参数优化**：对选定结构中的连续参数（缩放和偏置）进行两阶段优化（先用 Adam 粗调，再用 BFGS 精调）。
*   **TransNet 神经算子扩展**：
    1.  **神经算子生成**：不再仅依赖手工定义的解析函数，而是引入单隐层神经网络作为候选算子。
    2.  **TransNet 初始化**：通过重参数化技术（分离位置参数和形状参数），利用高斯随机场（GRF）作为辅助函数预训练神经元，构建一个分布均匀且具有强表达能力的特征空间。
*   **算法流程**：控制器采样算子序列 $\rightarrow$ 构建二叉树 $\rightarrow$ 计算 PDE 残差得分 $\rightarrow$ 更新控制器参数 $\rightarrow$ 维护高分候选池 $\rightarrow$ 最终精调。

### 3. 实验设计
论文针对三个极高维度的平稳 PDE 进行了数值实验：
*   **实验场景**：
    1.  **60维 (60D) 泊松方程**：测试对二次项结构的恢复能力。
    2.  **60维 (60D) 平稳反应扩散方程**：测试对三次项结构的恢复能力。
    3.  **55维 (55D) 半线性椭圆方程**：测试对复合非线性结构（$\exp(\sum \cos)$）的逼近能力。
*   **Benchmark 与对比**：
    *   主要对比了**包含真实算子的候选池 (P1)** 与 **不包含真实算子的候选池 (P2)**。
    *   在 P2 实验中，强制移除解中存在的关键算子（如移除 $x^2$ 改用 $\sin(x^2)$），观察 FEX 是否能通过其他算子的组合来拟合解。

### 4. 资源与算力
*   **算力说明**：论文**未明确提及**具体的硬件配置（如 GPU 型号、数量）以及总的训练时长。
*   **计算规模**：实验中使用了 500-1000 个采样点进行训练，搜索循环迭代次数在 700 到 1200 次之间，参数精调阶段使用了 15,000 次 Adam 迭代。

### 5. 实验数量与充分性
*   **实验数量**：共进行了 3 组大型 PDE 实验，每组实验均包含不同候选池配置的对比。
*   **充分性与客观性**：
    *   为了确保结果的可靠性，所有相对 $L^2$ 误差均通过 **50 次独立的蒙特卡洛积分重复实验**得出，并报告了均值和标准差。
    *   实验设计较为客观，通过故意剔除“正确答案”算子的消融实验，验证了该方法在算子库不完备时的鲁棒性和泛化能力。

### 6. 主要结论与发现
*   **高精度恢复**：当候选池包含解的组成算子时，FEX 能够以极高的精度（相对误差达 $10^{-7}$ 量级）恢复真实解。
*   **鲁棒性**：即使候选池不包含精确的算子，FEX 也能通过组合其他神经算子或解析算子（如用 $x^4$ 和 $\sin(x^2)$ 组合模拟 $x^2$）获得合理的近似解（误差在 $10^{-3}$ 量级）。
*   **克服维数灾难**：实验证明该方法在 55-60 维空间下依然稳定，且内存复杂度保持在多项式级别。

### 7. 优点
*   **精度优势**：相比纯神经网络方法，FEX 能够发现解的符号结构，从而在特定问题上达到接近机器精度的表现。
*   **灵活性**：引入 TransNet 神经算子后，算子库不再局限于简单的初等函数，增强了对复杂、未知结构的拟合能力。
*   **可解释性**：最终输出的是数学表达式或结构化的神经算子组合，比“黑盒”深度神经网络更具可解释性。

### 8. 不足与局限
*   **算子库依赖性**：实验显示，如果候选池中缺乏关键算子，精度会显著下降（从 $10^{-7}$ 降至 $10^{-3}$），说明方法对先验知识（算子选择）仍有一定依赖。
*   **优化挑战**：FEX 涉及离散（算子）和连续（参数）的联合优化，搜索空间巨大，且内部优化步骤（BFGS/Adam）可能陷入局部最优。
*   **应用限制**：目前实验主要集中在平稳（Time-independent）PDE，对于复杂的时间演化问题或具有奇异性的解，其表现尚待验证。

（完）
