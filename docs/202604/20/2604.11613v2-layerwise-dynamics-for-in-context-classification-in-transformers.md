---
title: Layerwise Dynamics for In-Context Classification in Transformers
title_zh: Transformer 中上下文分类的逐层动力学
authors: "Patrick Lutz, Themistoklis Haris, Arjun Chandra, Aditya Gangrade, Venkatesh Saligrama"
date: 2026-04-13
pdf: "https://arxiv.org/pdf/2604.11613v2"
tags: ["query:math-ai"]
score: 6.0
evidence: 从Transformer动力学中提取显式递归和涌现更新规则
tldr: 本研究探讨了Transformer在上下文分类（ICL）中的推理机制。通过在每一层强制执行特征和标签的置换等变性，研究者使计算过程变得可识别且具有高度结构化的权重。他们从中提取出一种显式的深度索引递归更新规则，揭示了注意力矩阵如何驱动训练点、标签和测试探针的耦合更新。该动态过程实现了一种几何驱动的算法模式，能够证明性地放大类别分离并实现稳健的类别对齐。
selection_source: fresh_fetch
motivation: 尽管Transformer具备强大的上下文分类能力，但其内部推理算法的运作机制仍然不透明。
method: 在硬无边距机制下研究多类线性分类，通过强制执行层级置换等变性来提取显式的深度索引递归更新规则。
result: 发现注意力矩阵驱动了训练数据、标签和测试样本之间的耦合更新，形成了一种能够放大类别分离的几何算法模式。
conclusion: 本研究首次在Softmax Transformer中识别出端到端的涌现更新规则，为理解Transformer的上下文学习提供了可解释的动力学视角。
---

## 摘要
Transformer 可以通过少量标记示例进行上下文分类，但其推理时的算法机制仍不透明。我们在硬无边距（hard no-margin）机制下研究多类线性分类，并通过在每一层强制执行特征和标签的置换等变性，使计算过程具有可辨识性。这在保持功能等效的同时实现了可解释性，并产生了高度结构化的权重。从这些模型中，我们提取了一个显式的深度索引递归：一个在 softmax Transformer 内部端到端识别出的、涌现的更新规则，据我们所知，这是同类研究中的首创。由混合特征-标签 Gram 结构形成的注意力矩阵驱动了训练点、标签和测试探针的耦合更新。由此产生的动力学实现了一种几何驱动的算法基元，该基元经证明可以放大类别分离度，并产生稳健的预期类别对齐。

## Abstract
Transformers can perform in-context classification from a few labeled examples, yet the inference-time algorithm remains opaque. We study multi-class linear classification in the hard no-margin regime and make the computation identifiable by enforcing feature- and label-permutation equivariance at every layer. This enables interpretability while maintaining functional equivalence and yields highly structured weights. From these models we extract an explicit depth-indexed recursion: an end-to-end identified, emergent update rule inside a softmax transformer, to our knowledge the first of its kind. Attention matrices formed from mixed feature-label Gram structure drive coupled updates of training points, labels, and the test probe. The resulting dynamics implement a geometry-driven algorithmic motif, which can provably amplify class separation and yields robust expected class alignment.