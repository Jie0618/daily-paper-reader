---
title: Neuro-Symbolic ODE Discovery with Latent Grammar Flow
title_zh: 基于潜在语法流的神经符号常微分方程发现
authors: "Karin Yu, Eleni Chatzi, Georgios Kissas"
date: 2026-04-17
pdf: "https://arxiv.org/pdf/2604.16232v1"
tags: ["query:sr-alg"]
score: 10.0
evidence: 从数据中发现常微分方程的神经符号生成框架
tldr: 本文提出了一种名为 Latent Grammar Flow (LGF) 的神经符号生成框架，用于从数据中自动发现常微分方程 (ODE)。LGF 将基于语法的方程表示嵌入到离散潜空间中，通过行为损失函数使语义相似的方程在空间上更接近，并利用离散流模型递归生成最符合观测数据的候选方程。该方法不仅提高了模型的可解释性，还支持嵌入领域知识和稳定性约束，为理解复杂系统提供了有力工具。
selection_source: fresh_fetch
motivation: 传统的黑盒模型缺乏可解释性和迁移性，因此需要一种能够从观测数据中自动发现符号化微分方程的方法。
method: 该方法将方程转化为基于语法的离散潜空间表示，并结合行为损失和离散流模型来引导候选方程的采样与生成。
result: LGF 能够高效地搜索并生成与观测数据高度拟合的 ODE，同时支持将稳定性等领域约束集成到生成过程中。
conclusion: LGF 为科学发现提供了一种强大的神经符号工具，通过结合离散流模型与语法约束，实现了可解释性与预测性能的平衡。
---

## 摘要
理解自然和工程系统通常依赖于符号化表述，例如微分方程，它们提供了超越黑盒模型的可解释性和迁移性。我们引入了潜在语法流（Latent Grammar Flow, LGF），这是一种用于从数据中发现常微分方程的神经符号生成框架。LGF 将方程作为基于语法的表示嵌入到离散潜在空间中，并通过行为损失强制语义相似的方程在空间位置上更接近。随后，一个离散流模型引导采样过程，递归地生成最拟合观测数据的候选方程。领域知识和约束（如稳定性）既可以嵌入到规则中，也可以作为条件预测器使用。

## Abstract
Understanding natural and engineered systems often relies on symbolic formulations, such as differential equations, which provide interpretability and transferability beyond black-box models.   We introduce Latent Grammar Flow (LGF), a neuro-symbolic generative framework for discovering ordinary differential equations from data. LGF embeds equations as grammar-based representations into a discrete latent space and forces semantically similar equations to be positioned closer together with a behavioural loss. Then, a discrete flow model guides the sampling process to recursively generate candidate equations that best fit the observed data. Domain knowledge and constraints, such as stability, can be either embedded into the rules or used as conditional predictors.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **Latent Grammar Flow (LGF)** 的神经符号框架，旨在从观测数据中自动发现常微分方程（ODE）。以下是对该论文的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **研究动机**：自然和工程系统通常由动力学模型描述，而符号化的微分方程比黑盒模型（如纯神经网络）具有更好的可解释性和跨系统迁移性。
*   **核心问题**：符号回归（SR）面临搜索空间巨大、对噪声敏感以及难以整合领域知识（如物理稳定性、系统阶数）等挑战。
*   **整体含义**：论文试图通过结合“语法约束”、“离散潜空间表示”和“生成式流模型”，构建一种既能利用深度学习生成能力，又能保持符号逻辑严谨性的 ODE 发现工具。

### 2. 方法论
LGF 采用了一种混合神经符号方法，核心组件包括：
*   **语法量化自编码器 (GQAE)**：
    *   使用上下文无关语法（CFG）将方程表示为规则序列。
    *   通过有限标量量化（FSQ）将离散的语法规则嵌入到一个**离散潜空间**中，避免了连续潜空间常见的拒绝采样问题。
*   **行为潜距离 (Behavioral Latent Distance)**：
    *   引入一种基于 **Wasserstein 距离 ($W_2$)** 的语义损失函数。它不根据方程的结构（字符长相）来聚类，而是根据方程产生的向量场（动力学行为）来重构潜空间，使行为相似的方程在空间上更接近。
*   **离散流匹配与条件引导 (Discrete Flow Matching)**：
    *   在离散潜空间中使用流模型进行采样。
    *   利用**预测器引导**技术，将领域知识（如 ODE 的阶数、局部稳定性、优化目标值）作为条件注入采样过程，引导模型向高概率、符合物理逻辑的区域搜索。
*   **双层优化流程**：外层通过流模型采样方程骨架（Skeleton），内层使用 Nelder-Mead 算法优化方程中的数值常数。

### 3. 实验设计
论文在三个基准测试上验证了 LGF 的性能：
*   **Benchmark 1**：包含 30 个一维一阶显式 ODE（源自 ODEBench）。对比方法包括 **ODEFormer**（基于 Transformer）、**PySR**（基于进化算法）、**ProGED**（基于概率语法）和 **GODE**（基于连续潜空间）。
*   **Benchmark 2**：包含 10 个线性与非线性 ODE，测试在已知阶数和稳定性条件下的表现。
*   **Benchmark 3**：针对 7 个复杂的动力学系统（如 Duffing 振子、Van der Pol 振子），在部分观测（仅观测加速度）和 5% 高斯噪声环境下进行测试，主要对比 **PySR**。

### 4. 资源与算力
*   **算力说明**：论文**未明确指出**具体的 GPU 型号或数量。
*   **训练细节**：提到了使用 PyTorch 框架和 Adam 优化器。GQAE 训练了 2000 个 epoch（带早停机制），Batch Size 为 512；离散流模型训练了 300 个 epoch。
*   **效率优势**：作者强调 LGF 比基于大型语言模型（LLM）或大规模 Transformer 的方法（如 ODEFormer，其需要 5000 万样本训练）更节省资源。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了从简单一阶到复杂二阶非线性系统的多种场景，并引入了 5% 的噪声，模拟真实数据环境。
*   **充分性**：实验对比了当前最流行的符号回归工具（如 PySR）和最新的深度学习方法（如 ODEFormer）。
*   **客观性**：通过相对 $L_2$ 误差和模型复杂度（Complexity）两个维度进行评估，展示了模型在准确性与简洁性之间的平衡。

### 6. 主要结论与发现
*   **性能优异**：LGF 在多个基准测试中达到了与 PySR 相当或更优的准确度，且在采样效率上显著高于传统的语法采样方法。
*   **领域知识的作用**：通过引入稳定性预测器，LGF 能够有效过滤掉物理上不合理的候选方程，从而在更少的迭代次数内发现正确模型。
*   **隐式方程支持**：与许多仅限于显式方程 ($y' = f(x)$) 的方法不同，LGF 的语法框架支持发现隐式 ODE。

### 7. 优点
*   **神经符号结合**：利用深度生成模型（Flow）的高效采样，同时通过语法（Grammar）保证输出的数学合法性。
*   **离散潜空间**：相比连续潜空间（如 VAE），离散空间更符合符号规则的本质，减少了无效方程的生成。
*   **行为感知**：通过行为损失函数（Wasserstein 距离）让模型“理解”方程背后的物理含义，而不仅仅是字符组合。

### 8. 不足与局限
*   **维度限制**：目前的实验主要集中在一维（单自由度）ODE 系统，尚未展示在多维耦合 ODE 组上的表现。
*   **语法依赖**：模型的表现高度依赖于预定义的上下文无关语法，如果真实方程超出了语法定义的搜索空间，模型将无法发现。
*   **计算开销**：虽然比 LLM 轻量，但内层的数值常数优化（Nelder-Mead）在处理复杂方程时仍可能成为计算瓶颈。

（完）
