---
title: "EVIL: Evolving Interpretable Algorithms for Zero-Shot Inference on Event Sequences and Time Series with LLMs"
title_zh: EVIL：利用大语言模型演化用于事件序列和时间序列零样本推理的可解释算法
authors: David Berghaus
date: 2026-04-17
pdf: "https://arxiv.org/pdf/2604.15787v1"
tags: ["query:sr-alg"]
score: 8.0
evidence: 利用大语言模型进化可解释的动力系统推断算法
tldr: 本研究提出EVIL框架，利用大语言模型（LLM）引导的进化搜索，自动发现用于动力系统推理的简洁、可解释算法。与传统深度学习不同，EVIL生成的Python/NumPy程序无需在大规模数据集上训练，即可在时间点过程预测、马尔可夫跳跃过程估计和时间序列插补等任务中实现零样本、上下文内推理。该方法在保持完全可解释性的同时，性能可媲美甚至超越最先进的深度学习模型，且推理速度提升了数个数量级。
selection_source: fresh_fetch
motivation: 旨在解决传统深度学习模型在动力系统推理中缺乏可解释性且需要大量训练数据的问题。
method: 通过LLM引导的进化搜索，自动生成执行零样本推理的纯Python/NumPy程序。
result: 发现的算法在多个动态系统任务中表现优异，性能比肩SOTA模型，且运行速度极快。
conclusion: 证明了LLM引导的程序进化能够发现通用的、紧凑且可解释的动力系统推理算法。
---

## 摘要
我们介绍了 EVIL（利用大语言模型演化可解释算法），这是一种利用大语言模型（LLM）引导的演化搜索来发现用于动力系统推理的简单、可解释算法的方法。EVIL 并非在大型数据集上训练神经网络，而是演化出纯 Python/NumPy 程序，这些程序可以在不同数据集上执行零样本、上下文内推理。我们将 EVIL 应用于三个不同的任务：时间点过程中的下一事件预测、马尔可夫跳跃过程的速率矩阵估计以及时间序列插补。在每种情况下，单个演化出的算法都能泛化到所有评估数据集，而无需针对每个数据集进行训练（类似于摊销推理模型）。据我们所知，这是首项证明 LLM 引导的程序演化可以为这些动力系统问题发现单一紧凑推理函数的工作。在这三个领域中，所发现的算法通常与最先进的深度学习模型具有竞争力，甚至优于后者，同时速度快几个数量级，并保持完全的可解释性。

## Abstract
We introduce EVIL (\textbf{EV}olving \textbf{I}nterpretable algorithms with \textbf{L}LMs), an approach that uses LLM-guided evolutionary search to discover simple, interpretable algorithms for dynamical systems inference. Rather than training neural networks on large datasets, EVIL evolves pure Python/NumPy programs that perform zero-shot, in-context inference across datasets. We apply EVIL to three distinct tasks: next-event prediction in temporal point processes, rate matrix estimation for Markov jump processes, and time series imputation. In each case, a single evolved algorithm generalizes across all evaluation datasets without per-dataset training (analogous to an amortized inference model). To the best of our knowledge, this is the first work to show that LLM-guided program evolution can discover a single compact inference function for these dynamical-systems problems. Across the three domains, the discovered algorithms are often competitive with, and even outperform, state-of-the-art deep learning models while being orders of magnitudes faster, and remaining fully interpretable.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **EVIL**（Evolving Interpretable Algorithms with LLMs）的新框架，旨在利用大语言模型（LLM）引导的进化搜索，自动发现用于动力系统推理的简洁、可解释算法。

以下是对该论文的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：当前的动力系统（如事件序列、时间序列）推理主要由深度学习（DL）模型主导。然而，DL 模型存在三大痛点：
    1.  **不透明性**：预测过程是“黑箱”，难以解释。
    2.  **高昂成本**：训练和运行需要大量算力和数据。
    3.  **泛化性差**：难以应对分布偏移（Distribution Shift）或未见过的输出维度（如更多的类别）。
*   **研究动机**：作者提出，与其训练复杂的神经网络参数，不如直接搜索能够解决问题的**纯 Python/NumPy 程序**。这些程序应具备“摊销推理”（Amortized Inference）的能力，即一次演化，处处可用，实现零样本（Zero-shot）推理。

### 2. 方法论
*   **核心思想**：利用 LLM 作为“变异算子”，在程序空间内进行进化搜索，寻找最优的启发式算法。
*   **关键技术细节**：
    *   **进化框架**：基于 `OpenEvolve`（AlphaEvolve 的开源实现），采用 `MAP-Elites` 算法维护种群多样性。
    *   **流程**：
        1.  **初始化**：从一个简单的平凡程序（如预测中位数）开始。
        2.  **变异**：LLM 根据系统提示词（System Prompt）和当前最优代码，以 `diff` 格式提出改进建议。
        3.  **评估**：在验证集（从训练集中抽取的子集）上运行程序，根据预测准确度、误差（RMSE/MAE）等指标打分。
        4.  **迭代**：保留高分程序，进入下一轮演化。
    *   **约束条件**：强制要求生成的代码仅使用 Python 标准库和 NumPy，严禁使用 PyTorch 等深度学习框架。
*   **算法产出**：为每个任务演化出一个通用的 Python 函数，该函数在测试时直接接收上下文数据并输出预测。

### 3. 实验设计
*   **应用场景与数据集**：
    1.  **时间点过程（TPP）**：预测下一事件的时间和类型。数据集包括 Taxi, Taobao, Amazon, Retweet 等，以及用于 OOD 测试的 MIMIC-II（包含 75 种标记）。
    2.  **马尔可夫跳跃过程（MJP）**：估计速率矩阵。数据集包括合成的 DFR（闪烁棘轮）以及真实的离子通道记录（IonCh）、分子动力学（ADP）等。
    3.  **时间序列插补**：填充缺失值。涵盖北京/意大利空气质量、电力消耗、人体动作捕捉（Motion Capture）等 10 个数据集。
*   **对比方法（Benchmark）**：
    *   **深度学习模型**：如 CDiff, NHP, SAITS, CSDI, LatentODE 等。
    *   **基础推理模型（FIM）**：如 FIM-PP, FIM-MJP, FIM-ℓ 等预训练模型。
    *   **传统方法**：如 HawkesEM, Spline 插值等。

### 4. 资源与算力
*   **演化阶段**：
    *   **时长**：每个任务的演化仅需 **15 到 40 分钟**。
    *   **成本**：LLM API 调用成本**低于 1 美元**。
    *   **模型**：使用了 GPT-5 和 GPT-5-mini（注：论文设定在 2026 年背景）。
*   **推理阶段**：
    *   **硬件**：仅需单核 CPU（实验中使用 Apple M3）。
    *   **效率**：在 TPP 任务中，EVIL 比基于 GPU 的 FIM-PP 模型快约 **200 倍**（5秒 vs 964秒）。

### 5. 实验数量与充分性
*   **实验规模**：在 3 个领域、超过 20 个数据集上进行了测试。
*   **充分性**：
    *   **零样本验证**：在完全未见过的数据集（如 MIMIC-II）上测试，验证了泛化性。
    *   **消融实验**：测试了不同随机种子下的稳定性、长时演化（800次迭代）的效果提升。
    *   **公平性**：EVIL 在演化时使用的训练数据量远少于基准模型训练时使用的数据量。
*   **客观性**：通过合成数据和真实数据双重验证，并对比了最先进的预训练基础模型。

### 6. 主要结论
*   **性能强劲**：EVIL 发现的简单算法在多个指标上与 SOTA 深度学习模型持平，甚至在 MIMIC-II 等复杂任务上超越了专门训练的模型。
*   **卓越的泛化力**：单个算法可处理任意数量的标记类别（Marks），而无需重新设计架构。
*   **极端高效**：推理速度极快，且无需 GPU，适合资源受限的实时场景。
*   **可解释性**：生成的 Python 代码逻辑清晰（如采用了“模式检索”和“加权平均”等策略），人类专家可直接阅读和修改。

### 7. 优点与亮点
*   **透明度**：打破了神经网络的“黑箱”，提供了可审计的推理逻辑。
*   **低门槛**：领域专家无需掌握深度学习知识，只需通过自然语言提示词即可引导算法发现。
*   **鲁棒性**：对分布偏移表现出比神经网络更强的抵抗力。
*   **发现新知识**：演化出的算法（如插补任务中的“模式检索”策略）与人类设计的先进启发式方法不谋而合。

### 8. 不足与局限
*   **复杂表征能力不足**：在需要极强非线性表征的任务（如 Motion Capture 的长窗口插补）中，表现略逊于经过大规模预训练的 FIM 模型。
*   **确定性限制**：目前生成的算法多为确定性的，尚未充分探索如何生成能够量化不确定性的随机算法。
*   **新颖性评估难**：虽然算法是自动发现的，但很难界定这些算法在多大程度上是全新的，还是对现有统计方法的重新组合。

（完）
