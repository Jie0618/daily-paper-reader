---
title: A Robust SINDy Autoencoder for Noisy Dynamical System Identification
title_zh: 一种用于噪声动力系统辨识的鲁棒 SINDy 自编码器
authors: Kairui Ding
date: 2026-04-06
pdf: "https://arxiv.org/pdf/2604.04829v1"
tags: ["query:sr-alg"]
score: 9.0
evidence: 用于发现动力系统控制方程的SINDy自动编码器
tldr: 针对非线性动力系统识别中 SINDy 算法对坐标系依赖及对噪声敏感的问题，本文提出了一种鲁棒的 SINDy 自动编码器。该方法在自动编码器框架中集成了一个噪声分离模块，能够从含有测量误差的数据中同时学习简化的潜空间坐标、稀疏的控制方程并准确估计噪声。在洛伦兹系统上的实验证明，该方法在噪声环境下具有更强的鲁棒性和模型可解释性。
selection_source: fresh_fetch
motivation: 传统的 SINDy 自动编码器在处理含有测量噪声的动力系统数据时，难以保证识别出的控制方程的准确性与鲁棒性。
method: 在 SINDy 自动编码器架构中引入专门的噪声分离模块，利用神经网络结构实现信号与测量噪声的有效解耦。
result: 在洛伦兹系统的数值实验中，该方法成功恢复了可解释的潜空间动力学，并能从高噪声观测中精准估计噪声水平。
conclusion: 通过结合噪声分离机制，该研究显著提升了从现实噪声数据中自动发现动力系统控制方程的可靠性与实用性。
---

## 摘要
非线性动力学稀疏识别 (SINDy) 已被广泛用于从数据中发现动力系统的控制方程。它利用稀疏回归技术，从候选函数库中识别未知系统的简洁模型。因此，它依赖于动力学在所使用的坐标系中具有稀疏表示的假设。为了解决这一局限性，人们寻求一种坐标变换，以提供能够重建原始系统的简化坐标。最近，SINDy 自编码器扩展了这一思路，通过将稀疏模型发现与自编码器架构相结合，来学习简化的潜坐标以及简洁的控制方程。该框架面临的一个核心挑战是对测量误差的鲁棒性。受噪声分离神经网络结构的启发，我们将噪声分离模块整合到 SINDy 自编码器架构中，从而提高了鲁棒性，并实现了对噪声动力系统更可靠的辨识。在 Lorenz 系统上的数值实验表明，所提出的方法恢复了可解释的潜动力学，并从含噪观测值中准确估计了测量噪声。

## Abstract
Sparse identification of nonlinear dynamics (SINDy) has been widely used to discover the governing equations of a dynamical system from data. It uses sparse regression techniques to identify parsimonious models of unknown systems from a library of candidate functions. Therefore, it relies on the assumption that the dynamics are sparsely represented in the coordinate system used. To address this limitation, one seeks a coordinate transformation that provides reduced coordinates capable of reconstructing the original system. Recently, SINDy autoencoders have extended this idea by combining sparse model discovery with autoencoder architectures to learn simplified latent coordinates together with parsimonious governing equations. A central challenge in this framework is robustness to measurement error. Inspired by noise-separating neural network structures, we incorporate a noise-separation module into the SINDy autoencoder architecture, thereby improving robustness and enabling more reliable identification of noisy dynamical systems. Numerical experiments on the Lorenz system show that the proposed method recovers interpretable latent dynamics and accurately estimates the measurement noise from noisy observations.

---

## 论文详细总结（自动生成）

这篇论文提出了一种名为 **Robust SINDy Autoencoder** 的框架，旨在解决从含有高噪声的观测数据中同时发现非线性动力系统坐标变换和稀疏控制方程的难题。以下是对该论文的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：传统的 SINDy（非线性动力学稀疏识别）方法高度依赖于观测坐标系。如果动力学在原始坐标下不稀疏，则难以识别。虽然“SINDy 自编码器”通过引入自编码器（AE）来寻找最优潜空间坐标，但它对**测量噪声（Measurement Noise）**极其敏感，因为稀疏回归过程通常需要计算不稳定的数值导数。
*   **研究动机**：在实际科学实验中，数据往往是高维且多噪的。作者希望建立一个统一的框架，能够**同时实现去噪、非线性坐标发现和稀疏模型识别**，从而在噪声环境下依然能提取出具有物理可解释性的控制方程。

### 2. 论文提出的方法论
该方法将三个关键组件集成到一个端到端的神经网络架构中：
*   **核心思想**：引入一个**噪声分离模块（Noise-separation module）**，将观测数据 $\hat{X}$ 分解为干净信号 $X$ 和测量误差 $N$（即 $\hat{X} = X + N$）。
*   **关键技术细节**：
    1.  **噪声分离网络**：利用前馈神经网络模拟动力学流图，并结合显式 Runge-Kutta（RK4）时间步进约束。通过前向和后向预测误差来联合估计噪声项 $N$。
    2.  **自编码器结构**：编码器 $\phi$ 将去噪后的数据映射到低维潜空间 $z$，解码器 $\psi$ 负责重构。
    3.  **潜空间 SINDy**：在潜空间中定义候选函数库 $\Theta(z)$，通过稀疏回归学习 $\dot{z} = \Theta(z)\Phi$。
*   **联合损失函数**：
    $$L_{total} = L_{recon} + L_{dx/dt} + L_{dz/dt} + \lambda\|\Phi\|_1$$
    包含重构误差、原始空间动力学一致性、潜空间动力学一致性以及促进方程稀疏的 $L_1$ 正则项。

### 3. 实验设计
*   **数据集/场景**：使用了经典的 **Lorenz 系统**（三维混沌系统）。
*   **Benchmark（基准）**：以原始 Lorenz 方程作为 Ground Truth。
*   **对比与评估**：
    *   设置了 **5%、10% 和 15%** 三种不同强度的测量噪声水平。
    *   评估指标包括：解码器重构相对误差、潜空间 SINDy 相对误差、以及噪声估计的准确度。
    *   验证了学习到的潜空间方程在经过坐标变换后是否与原始 Lorenz 方程结构一致。

### 4. 资源与算力
*   **软件环境**：论文提到使用了 **TensorFlow** 框架进行开发。
*   **硬件与时长**：文中**未明确说明**具体的 GPU 型号、数量或具体的训练时长。但作者提到该方法由于涉及多模块交互（去噪、AE、SINDy、RK4 约束），其**计算开销较大（Computationally intensive）**。

### 5. 实验数量与充分性
*   **实验规模**：作者针对 Lorenz 系统进行了多组噪声水平下的鲁棒性测试。
*   **充分性评价**：
    *   **优点**：实验展示了从 15% 的高噪声中恢复动力学的过程，并给出了具体的系数对比和噪声恢复曲线，证明了方法的有效性。
    *   **不足**：实验仅限于 Lorenz 系统这一个案例，缺乏在更高维系统（如流体力学数据）或其他类型动力系统（如具有过程噪声的系统）上的广泛验证。

### 6. 主要结论与发现
*   **成功恢复动力学**：该方法在存在显著测量噪声的情况下，成功识别出了与 Lorenz 系统结构高度相似的潜空间控制方程。
*   **噪声估计能力**：噪声分离模块能够准确估计注入的测量误差，从而保护了后续的 SINDy 识别过程。
*   **坐标等价性**：证明了学习到的潜空间坐标与原始物理坐标之间存在简单的线性/非线性变换关系，模型具有很强的可解释性。

### 7. 优点（亮点）
*   **集成化设计**：将去噪作为一个可学习的模块嵌入系统识别流程，而不是作为预处理步骤，实现了信号与噪声的动态解耦。
*   **鲁棒性强**：相比于标准的 SINDy 自编码器，该方法在处理高噪声数据时表现出更强的稳定性。
*   **物理可解释性**：保留了 SINDy 发现简洁、显式数学方程的特性，避免了纯黑盒模型。

### 8. 不足与局限
*   **计算成本高**：训练过程涉及复杂的 RK4 步进和多项损失函数优化，收敛速度和计算资源需求可能是大规模应用的瓶颈。
*   **超参数敏感**：损失函数中包含四个权重参数（$\lambda_1$ 到 $\lambda_4$），这些超参数的选择目前依赖于经验，缺乏自动化的调节机制。
*   **泛化范围有限**：目前仅在常微分方程（ODE）系统上验证，尚未扩展到偏微分方程（PDE）或受随机扰动（过程噪声）影响的系统。

（完）
