---
title: Discovery of Symbolic Hamiltonian Expressions with Buckingham-Symplectic Networks
title_zh: 利用 Buckingham-辛网络发现符号哈密顿表达式
authors: "Joe Germany, Joseph Bakarji, Sara Najem"
date: 2026-04-01
pdf: "https://arxiv.org/pdf/2604.00576v1"
tags: ["query:sr-alg"]
score: 10.0
evidence: 从物理数据中发现符号哈密顿表达式
tldr: 本研究针对哈密顿系统建模，提出了BuSyNet架构。该方法结合了量纲一致性和辛几何变换，通过辛层将输入轨迹映射到低维潜在的作用量-角度变量，并结合系统参数发现具有能量单位的符号哈密顿表达式。在谐振子和开普勒二体问题上的实验表明，BuSyNet能发现简洁的闭式表达式，在长期预测准确性和稳定性方面优于现有深度学习架构，同时保持了极高的可解释性。
selection_source: fresh_fetch
motivation: 现有的哈密顿系统深度学习模型通常忽略了哈密顿量的能量单位以及可积系统存在作用量-角度坐标变换的物理特性。
method: 提出BuSyNet架构，通过辛变换层将轨迹映射到潜在作用量-角度空间，并利用量纲分析发现符合能量单位的符号哈密顿量。
result: 在谐振子和开普勒问题中，该模型成功恢复了简洁的符号表达式，且在长期预测精度和数值稳定性上显著超越了现有技术。
conclusion: 通过将物理约束（量纲和辛对称性）嵌入神经网络，BuSyNet为发现具有可解释性和鲁棒性的物理定律提供了有效途径。
---

## 摘要
哈密顿系统是物理世界建模的核心。其定义标量——哈密顿量，在其相空间轨迹中编码了能量守恒和辛几何。最近的深度学习方法通过将哈密顿系统的特性嵌入到架构或损失函数中来进行建模。然而，它们通常忽略了：i) 哈密顿量具有能量单位，以及/或者 ii) 每个可积哈密顿量都允许通过正则变换转换为作用量-角坐标，在此坐标下，动力学简化为不变环面上的简单旋转。我们提出了 BuSyNet，这是一种深度学习架构，通过量纲一致的辛变换结合了这两个约束。辛层将输入轨迹映射到低维潜在作用量-角变量，然后将其与系统参数结合，以发现以能量为单位的符号哈密顿表达式。在谐振子和开普勒二体问题（2D 和 3D）上的评估表明，BuSyNet 恢复了简洁的闭式哈密顿量，在长期预测准确性和稳定性方面优于最先进的神经架构，同时保持了可解释性。

## Abstract
Hamiltonian systems lie at the heart of modeling the physical world. Their defining scalar, the Hamiltonian, encodes both energy conservation and symplectic geometry in its phase-space trajectories. Recent deep learning approaches model Hamiltonian systems by embedding their properties either in the architecture or in the loss function. However, they typically ignore that: i) a Hamiltonian carries units of energy and/or ii) that every integrable Hamiltonian admits a canonical transformation to action-angle coordinates in which the dynamics reduce to a simple rotation on an invariant torus. We propose BuSyNet, a deep learning architecture that combines these two constraints via a dimensionally-consistent, symplectic transformation. A symplectic layer maps input trajectories to lower-dimensional latent action-angle variables, which are then combined with system parameters to discover a symbolic Hamiltonian expression in units of energy. Evaluated on the harmonic oscillator and the Kepler two-body problem (in 2D and 3D), BuSyNet recovers concise, closed-form Hamiltonians that outperform state-of-the-art neural architectures in long-term prediction accuracy and stability, while maintaining interpretability.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **BuSyNet**（Buckingham-Symplectic Networks）的新型深度学习架构，旨在从物理系统的轨迹数据中自动发现具有物理单位且符合辛几何结构的符号哈密顿表达式。

以下是对该论文的结构化深度总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **研究动机**：哈密顿系统是物理建模的核心，但现有的物理信息机器学习（PIML）方法（如 HNN 或 SympNet）存在两个主要缺陷：
    1.  **忽略量纲一致性**：哈密顿量必须具有能量单位，且应由系统参数（如质量、弹簧系数）的幂次组合而成，现有模型往往将其视为无单位的标量函数。
    2.  **忽略可积系统的内在结构**：对于可积系统，存在正则变换将其映射到“作用量-角度”（Action-Angle）坐标，在此坐标下动力学变为简单的线性旋转。
*   **核心目标**：结合辛几何约束与白金汉 $\pi$ 定理（量纲分析），从原始轨迹数据中直接提取出简洁、可解释且符合物理单位的符号哈密顿方程。

### 2. 方法论：核心思想与技术细节
BuSyNet 采用两阶段架构，将辛变换与量纲感知层相结合：
*   **第一阶段：辛编码器（SympNet）**：
    *   使用基于 G-SympNet 的架构，学习一个从原始相空间坐标 $(q, p)$ 到潜在作用量-角度坐标 $(I, \theta)$ 的辛映射 $S_\phi$。
    *   **动作量接地（Grounding）**：在训练前，通过对原始轨迹进行快速傅里叶变换（FFT）估计周期，并利用数值积分计算作用量 $I$ 的数值，作为训练的基准。
*   **第二阶段：量纲感知头（BuckiNet）**：
    *   利用白金汉 $\pi$ 定理，将潜在作用量 $I$ 与系统参数 $m$（如质量、引力常数等）结合。
    *   通过一个特殊的对数空间线性层，强制输出的哈密顿量 $\hat{H}$ 具有能量单位（$M L^2 T^{-2}$）。
*   **损失函数与优化**：
    *   联合优化辛网络权重和哈密顿表达式的系数。
    *   损失函数包含：作用量一致性损失 $L_I$、角度演化线性损失 $L_{\dot{\theta}}$ 以及作用量守恒损失 $L_{\dot{I}}$。
    *   最终输出一个符号化的哈密顿表达式，例如 $\hat{H} = \omega I$ 或 $\hat{H} = -mk^2 / 2I^2$。

### 3. 实验设计
*   **实验场景**：
    1.  **一维简谐振子（SHM）**：验证基础的线性作用量关系。
    2.  **开普勒二体问题（2D 和 3D）**：验证在复杂非线性系统和高维空间下的有效性。
*   **Benchmark 与对比方法**：
    *   **Vanilla NN**：普通前馈神经网络。
    *   **HNN（Hamiltonian Neural Networks）**：经典的哈密顿神经网络。
    *   **SympNet**：纯辛网络架构。
*   **评估指标**：长期预测的均方误差（MSE）、能量守恒性（能量方差 $Var(\hat{H})$）、符号恢复的准确度。

### 4. 资源与算力
*   **算法实现**：使用了 L-BFGS 优化器（用于 BuckiNet 预训练，100 epoch）和 Adam 优化器（用于联合训练，1000 epoch）。
*   **算力说明**：论文**未明确说明**具体的 GPU 型号、数量或总训练时长。但从网络规模（4层，每层32个神经元）和迭代次数来看，该模型属于轻量级，通常在单张商用 GPU 或高性能 CPU 上即可快速完成训练。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了从 1D 到 3D 的典型可积系统。
*   **充分性评价**：
    *   实验设计较为**充分**，不仅对比了状态预测精度，还重点展示了能量守恒的长效性。
    *   **公平性**：所有基准模型均在相同的数据集和时间步长下运行。
    *   **客观性**：通过展示学习到的潜在空间（不变环面）和符号表达式的精确系数，证明了模型并非简单的曲线拟合，而是捕捉到了物理本质。

### 6. 主要结论与发现
*   **符号恢复**：BuSyNet 成功恢复了简谐振子和开普勒问题的闭式哈密顿表达式，系数与解析解高度吻合（如开普勒问题中恢复了 $-1/2I^2$ 的形式）。
*   **长期稳定性**：在长期预测（Rollout）中，BuSyNet 的误差比 HNN 和 SympNet 低 1-2 个数量级，且几乎没有相位漂移。
*   **能量守恒**：BuSyNet 的能量方差保持在 $10^{-11}$ 级别，比对比方法高出 5-6 个数量级，证明了显式量纲约束对数值稳定性的巨大贡献。

### 7. 优点
*   **高度可解释性**：输出不是黑盒函数，而是人类可读的物理公式。
*   **量纲一致性**：首次系统地将量纲分析引入辛学习框架，缩小了假设空间，提高了泛化能力。
*   **数值稳定性**：通过在潜在的作用量-角度空间进行线性演化，彻底解决了传统积分器在长期模拟中的漂移问题。

### 8. 不足与局限
*   **数据依赖**：目前仍假设可以获取完整的相空间测量值（位置和动量），在实际观测中动量往往难以直接获取。
*   **系统限制**：该方法目前主要针对**可积系统**（存在作用量-角度变换）。对于弱混沌或完全不可积系统，该架构的适用性尚待验证。
*   **预处理需求**：需要预先通过 FFT 等手段计算作用量的数值作为“接地”信号，这增加了流程的复杂性。
*   **参数已知性**：假设系统参数（如质量 $m$）及其量纲是预先已知的。

（完）
