---
title: "WGFINNs: Weak formulation-based GENERIC formalism informed neural networks'"
title_zh: WGFINNs：基于弱形式的 GENERIC 形式化物理信息神经网络
authors: "Jun Sur Richard Park, Auroni Huque Hashim, Siu Wun Cheung, Youngsoo Choi, Yeonjong Shin"
date: 2026-04-03
pdf: "https://arxiv.org/pdf/2604.02601v1"
tags: ["query:sr-alg"]
score: 8.0
evidence: 从噪声观测中进行控制方程的数据驱动发现
tldr: 针对从噪声数据中发现控制方程的挑战，本文提出WGFINNs框架。该方法将GENERIC热力学形式化与弱形式损失函数结合，克服了传统GFINNs对噪声敏感的缺陷。通过引入状态加权损失和残差注意力机制，WGFINNs在保持物理一致性的同时，显著提升了在噪声环境下的预测精度和物理量恢复的可靠性，为复杂动力系统的建模提供了鲁棒的科学机器学习方案。
selection_source: fresh_fetch
motivation: 传统的GFINNs依赖强形式损失函数，导致其在处理含有测量噪声的观测数据时表现出极高的敏感性和不稳定性。
method: 将动力系统的弱形式集成到结构保持的GENERIC架构中，并引入状态加权损失与残差注意力机制以平衡不同状态变量间的尺度差异。
result: 理论分析和数值实验表明，WGFINNs在不同噪声水平下均优于GFINNs，能够实现更精确的系统演化预测和物理参数恢复。
conclusion: WGFINNs通过弱形式建模有效增强了物理信息神经网络的鲁棒性，为噪声环境下的科学机器学习提供了可靠的结构保持方案。
---

## 摘要
从含噪声的观测中通过数据驱动发现控制方程仍然是科学机器学习中的一个基本挑战。虽然 GENERIC 形式化物理信息神经网络 (GFINNs) 提供了一个原则性的框架，通过结构设计强制执行热力学定律，但它们对强形式损失函数的依赖使其对测量噪声高度敏感。为了解决这一局限性，我们提出了基于弱形式的 GENERIC 形式化物理信息神经网络 (WGFINNs)，它将动力系统的弱形式与 GFINNs 的保结构架构相结合。WGFINNs 在保持精确满足 GENERIC 退化和对称条件的同时，显著增强了对噪声数据的鲁棒性。我们进一步引入了按状态加权的损失函数和基于残差的注意力机制，以缓解状态变量之间的尺度不平衡问题。理论分析对比了强形式估计器与弱形式估计器之间的定量差异。主要结论是，在存在噪声的情况下，强形式估计器会随着时间步长的减小而发散，而如果测试函数满足特定条件，弱形式估计器即使在噪声数据下也能保持准确。数值实验表明，WGFINNs 在不同噪声水平下均一致优于 GFINNs，实现了更准确的预测和更可靠的物理量恢复。

## Abstract
Data-driven discovery of governing equations from noisy observations remains a fundamental challenge in scientific machine learning. While GENERIC formalism informed neural networks (GFINNs) provide a principled framework that enforces the laws of thermodynamics by construction, their reliance on strong-form loss formulations makes them highly sensitive to measurement noise. To address this limitation, we propose weak formulation-based GENERIC formalism informed neural networks (WGFINNs), which integrate the weak formulation of dynamical systems with the structure-preserving architecture of GFINNs. WGFINNs significantly enhance robustness to noisy data while retaining exact satisfaction of GENERIC degeneracy and symmetry conditions. We further incorporate a state-wise weighted loss and a residual-based attention mechanism to mitigate scale imbalance across state variables. Theoretical analysis contrasts quantitative differences between the strong-form and the weak-form estimators. Mainly, the strong-form estimator diverges as the time step decreases in the presence of noise, while the weak-form estimator can be accurate even with noisy data if test functions satisfy certain conditions. Numerical experiments demonstrate that WGFINNs consistently outperform GFINNs at varying noise levels, achieving more accurate predictions and reliable recovery of physical quantities.

---

## 论文详细总结（自动生成）

### WGFINNs：基于弱形式的 GENERIC 形式化物理信息神经网络论文总结

#### 1. 核心问题与研究动机
*   **核心问题**：如何从含有高水平测量噪声的观测数据中，准确且稳健地发现符合热力学定律的动力系统控制方程。
*   **研究背景**：
    *   **GFINNs 的局限性**：现有的 GENERIC 形式化物理信息神经网络（GFINNs）虽然能通过架构设计强制执行能量守恒和熵增定律，但其依赖于“强形式”损失函数（即拟合逐点时间导数）。在实际应用中，导数通常需从噪声数据中数值计算，这会极大地放大噪声，导致模型失效。
    *   **弱形式的优势**：弱形式通过分部积分将导数从噪声数据转移到平滑的测试函数上，理论上对噪声更具鲁棒性，但此前多用于线性字典方法，而非复杂的深度神经网络架构。

#### 2. 方法论
*   **核心思想**：将动力系统的**弱形式（Weak Formulation）**集成到**GENERIC 框架**的保结构神经网络中，提出 **WGFINNs**。
*   **关键技术细节**：
    *   **GENERIC 架构**：使用四个神经网络分别模拟泊松矩阵 $L$、摩擦矩阵 $M$、能量 $E$ 和熵 $S$，通过硬约束确保满足退化条件和对称性，从而严格遵守热力学第一和第二定律。
    *   **弱形式损失函数**：不再直接拟合 $\dot{x} = f(x)$，而是最小化积分残差：$-\int \dot{\phi}x dt = \int f(x)\phi dt$。其中 $\phi$ 是具有紧支撑的平滑测试函数（如分段多项式）。
    *   **状态加权与残差注意力（State-RBA）**：引入状态维度的加权损失，并结合残差注意力机制动态调整权重，解决不同物理量（如位置与熵）之间量级差异巨大的尺度不平衡问题。
*   **理论支撑**：论文证明了在噪声存在时，强形式估计器随时间步长减小而发散，而弱形式估计器在特定测试函数条件下可收敛至真实参数。

#### 3. 实验设计
*   **实验场景**：
    1.  **双气体容器系统**：涉及热量和体积交换，4 维状态变量。
    2.  **热弹性双摆系统**：复杂的 10 维状态变量动力学。
    3.  **线性阻尼系统**：经典的热力学耗散系统。
    4.  **1D/1V Vlasov-Poisson 方程**：等离子体物理中的电场动力学。
*   **Benchmark 与对比方法**：以原始的 **GFINNs（强形式）** 作为主要对比基准。
*   **噪声设置**：在训练数据中加入 0%、5%、10% 等不同水平的状态相关高斯噪声。

#### 4. 资源与算力
*   **硬件环境**：使用了劳伦斯利弗莫尔国家实验室（LLNL）的 Tuolumne 系统，配备 **AMD MI300A GPU (APU)**。
*   **算力规模**：该系统共有 4608 个 GPU，每个节点 4 个。
*   **训练时长**：每个示例的训练时间约为 **4000 秒**（约 1.1 小时），对应 9 万至 24 万次迭代不等。

#### 5. 实验数量与充分性
*   **实验规模**：针对 4 个不同的物理模型进行了测试，涵盖了从简单到高维、从常微分方程（ODE）到偏微分方程（PDE）降维后的动力学。
*   **充分性与公平性**：
    *   每个噪声水平下均进行了 **5 次独立重复实验**并取平均值，以排除随机性影响。
    *   WGFINNs 和 GFINNs 使用了**完全相同的神经网络架构**，唯一的变量是损失函数的构造形式（强形式 vs 弱形式），对比具有高度的客观性和公平性。

#### 6. 主要结论与发现
*   **鲁棒性显著提升**：在 5% 和 10% 的高噪声水平下，GFINNs 的预测轨迹往往完全偏离真实值，而 WGFINNs 仍能保持极高的预测精度。
*   **物理量恢复准确**：WGFINNs 能够从噪声数据中准确恢复出系统的能量函数和熵函数，其学习到的能量等高线与真实物理规律高度吻合。
*   **理论验证一致**：实验结果验证了理论分析，即弱形式通过积分效应有效地过滤了高频噪声。

#### 7. 优点与亮点
*   **保结构与鲁棒性的结合**：首次将 GENERIC 的热力学一致性与弱形式的噪声鲁棒性完美结合。
*   **理论深度**：不仅给出了算法，还提供了关于强/弱形式估计器误差行为的数学证明（Proposition 2.1 及定理 4.1-4.3）。
*   **解决尺度问题**：State-RBA 机制的引入使得模型能够同时处理量级差异极大的物理变量，增强了实用性。

#### 8. 不足与局限
*   **超参数敏感性**：弱形式的效果高度依赖于测试函数（Test Functions）的选择，包括支撑集宽度 $\ell$、多项式阶数 $p$ 等，这些参数目前仍需针对不同问题进行手动调优。
*   **计算开销**：虽然弱形式增加了积分计算，但论文未详细对比其与强形式在单次迭代中的计算耗时差异。
*   **应用范围限制**：目前主要验证了确定性动力系统，对于随机动力系统或具有冲击波（Shock）等不连续特性的系统，其表现尚待进一步研究。

（完）
