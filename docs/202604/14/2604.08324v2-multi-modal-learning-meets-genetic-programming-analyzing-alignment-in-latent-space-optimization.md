---
title: "Multi-Modal Learning meets Genetic Programming: Analyzing Alignment in Latent Space Optimization"
title_zh: 多模态学习遇上遗传规划：分析潜在空间优化中的对齐
authors: "Benjamin Léger, Kazem Meidani, Christian Gagné"
date: 2026-04-09
pdf: "https://arxiv.org/pdf/2604.08324v2"
tags: ["query:sr-alg"]
score: 10.0
evidence: 结合神经编码器与遗传算法的符号回归潜空间优化
tldr: 本文探讨了多模态学习在符号回归潜空间优化（LSO）中的应用，特别是针对借鉴CLIP机制的SNIP模型。研究分析了符号与数值编码器在共享潜空间中的对齐效果，发现尽管该方法具有潜力，但现有的跨模态对齐过于粗糙，且在优化过程中并未实质性改善。这项工作揭示了当前多模态LSO在引导符号搜索方面的局限性，并强调了细粒度对齐是未来的关键研究方向。
selection_source: fresh_fetch
motivation: 旨在验证SNIP等模型通过对齐符号和数值编码器来引导符号回归优化的承诺是否在实践中真正实现。
method: 通过实验评估SNIP在优化过程中的跨模态对齐动态，并分析其潜空间对齐粒度对搜索效率的影响。
result: 发现跨模态对齐并未随适应度增加而提升，且粗粒度的对齐不足以在符号空间内进行高效的原则性搜索。
conclusion: 多模态LSO的有效对齐引导优化尚未实现，未来需重点攻克细粒度跨模态对齐的技术难题。
---

## 摘要
符号回归 (SR) 旨在从数据中发现数学表达式，这一任务传统上通过遗传规划 (GP) 对符号结构进行组合搜索来解决。潜在空间优化 (LSO) 方法使用神经编码器将符号表达式映射到连续空间，从而将组合搜索转化为连续优化。SNIP (Meidani et al., 2024) 是一种受 CLIP 启发的对比预训练模型，它通过引入多模态方法推进了 LSO：在共享潜在空间中对齐符号编码器和数值编码器，以学习表型-基因型映射，从而使数值空间中的优化能够隐式地引导符号搜索。然而，这依赖于细粒度的跨模态对齐，而关于 CLIP 等类似模型的文献表明，这种对齐通常是粗粒度的。在本文中，我们研究了 SNIP 是否实现了其对符号回归进行有效双模态优化的承诺。我们的实验表明：(1) 在优化过程中，即使适应度增加，跨模态对齐也没有改善；(2) SNIP 学习到的对齐过于粗糙，无法在符号空间中有效地进行有原则的搜索。这些发现表明，虽然多模态 LSO 在符号回归方面具有巨大潜力，但有效的对齐引导优化在实践中仍未实现，这突显了细粒度对齐是未来工作的一个关键方向。

## Abstract
Symbolic regression (SR) aims to discover mathematical expressions from data, a task traditionally tackled using Genetic Programming (GP) through combinatorial search over symbolic structures. Latent Space Optimization (LSO) methods use neural encoders to map symbolic expressions into continuous spaces, transforming the combinatorial search into continuous optimization. SNIP (Meidani et al., 2024), a contrastive pre-training model inspired by CLIP, advances LSO by introducing a multi-modal approach: aligning symbolic and numeric encoders in a shared latent space to learn the phenotype-genotype mapping, enabling optimization in the numeric space to implicitly guide symbolic search. However, this relies on fine-grained cross-modal alignment, whereas literature on similar models like CLIP reveals that such an alignment is typically coarse-grained. In this paper, we investigate whether SNIP delivers on its promise of effective bi-modal optimization for SR. Our experiments show that: (1) cross-modal alignment does not improve during optimization, even as fitness increases, and (2) the alignment learned by SNIP is too coarse to efficiently conduct principled search in the symbolic space. These findings reveal that while multi-modal LSO holds significant potential for SR, effective alignment-guided optimization remains unrealized in practice, highlighting fine-grained alignment as a critical direction for future work.

---

## 论文详细总结（自动生成）

这篇论文对多模态学习在符号回归（SR）中的应用进行了深入的批判性分析，重点探讨了 SNIP 模型在潜在空间优化（LSO）中的对齐有效性。以下是该论文的结构化总结：

### 1. 核心问题与整体含义
*   **研究背景**：符号回归传统上依赖遗传规划（GP）进行组合搜索。近年来，潜在空间优化（LSO）通过神经编码器将符号映射到连续空间，试图利用连续优化算法解决问题。
*   **核心动机**：SNIP 模型借鉴了 CLIP 的对比学习思路，试图通过对齐“符号（基因型）”与“数值（表型）”两个模态，实现以数值行为引导符号搜索。
*   **核心问题**：论文质疑 SNIP 的这种跨模态对齐是否真的在优化中起到了引导作用，以及这种对齐是否足够精细，能够区分结构相似但功能不同的数学表达式。

### 2. 方法论
*   **核心思想**：验证多模态 LSO 中的两个关键假设：1. 算法在优化过程中确实利用了对齐信息；2. 对齐的粒度足以支持符号搜索。
*   **关键技术细节**：
    *   **SNIP 架构**：包含两个 Transformer 编码器，分别处理符号序列（前缀表达式）和数值观测值 $(x, y)$。
    *   **对比学习（InfoNCE 损失）**：在预训练阶段，通过最大化匹配对（同一方程的符号与数值）的余弦相似度，最小化不匹配对的相似度。
    *   **LSO 流程**：在数值潜空间中进行搜索（如使用灰狼优化器 GWO），通过解码器将潜向量还原为符号表达式，并利用 BFGS 算法优化常数。
*   **对齐度量**：使用符号嵌入 $Z_S$ 与数值嵌入 $Z_V$ 之间的余弦相似度作为对齐指标。

### 3. 实验设计
*   **数据集**：
    *   **SRBench**：包含 Feynman 和 Strogatz 物理方程组（筛选变量数 $D \le 10$ 的方程）。
    *   **合成数据**：100 个符合 SNIP 训练分布的随机表达式，作为性能上限参考。
*   **对比实验设计**：
    *   **研究 1（动态分析）**：在 80 次优化迭代中，实时跟踪最佳个体的拟合优度（$R^2$）和跨模态对齐分数。
    *   **研究 2（粒度分析/检索任务）**：对目标方程进行四种微小扰动（一元算子交换、二元算子交换、常数修改、变量替换），测试模型能否通过对齐分数从候选集中检索出正确的原方程。
*   **Benchmark**：对比随机选择基准（Random Baseline）。

### 4. 资源与算力
*   **算力说明**：论文中**未明确提及**具体的 GPU 型号、数量或训练总时长。
*   **实现细节**：作者使用了 SNIP 原作者提供的预训练模型和超参数设置进行推理和分析实验，重点在于评估而非重新训练。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 82 个 Feynman 方程、100 个合成方程，并对每种扰动类型进行了详细的混淆矩阵分析。
*   **充分性评价**：实验设计非常充分且具有针对性。通过“动态演化”和“静态检索”两个维度的交叉验证，客观地揭示了模型在细粒度辨别上的失效，实验逻辑严密。

### 6. 主要结论与发现
*   **对齐与优化脱节**：在 LSO 过程中，虽然 $R^2$ 随迭代显著提升，但对齐分数几乎保持不变或下降。这表明优化过程主要靠数值拟合驱动，并未利用预训练学到的跨模态对齐信息。
*   **对齐粒度极粗**：在检索任务中，SNIP 在 Feynman 数据集上的准确率（18.3%）甚至低于随机基准（27.3%）。
*   **算子敏感度缺失**：模型最难区分二元算子的交换（如“+”换成“-”），说明其对齐仅捕获了宏观的数值趋势，而非精确的符号逻辑。
*   **变量识别较好**：模型对变量替换的识别率相对较高，这可能与输入数据的维度特征直接对应有关。

### 7. 优点
*   **批判性深度**：不同于以往只关注性能指标的论文，本文深入挖掘了多模态模型内部的表示机制，揭示了“看似有效”背后的真实原因。
*   **评估方法创新**：将视觉-语言领域评估 CLIP 组合性的方法引入符号回归，设计了针对数学公式的细粒度扰动测试。
*   **路线图意义**：明确指出了“细粒度对齐”是多模态符号回归未来的核心挑战，为后续研究指明了方向。

### 8. 不足与局限
*   **未提供改进方案**：论文主要是一个“诊断性”研究，虽然指出了问题，但没有提出或验证能够实现细粒度对齐的具体新算法（如硬负采样策略）。
*   **模型局限性**：主要针对 SNIP 架构进行分析，虽然 SNIP 是该领域的代表作，但结论是否完全适用于所有基于扩散模型或非 Transformer 架构的多模态 SR 模型仍有待验证。
*   **常数处理**：实验显示常数优化（BFGS）对结果干扰较大，虽然作者做了消融实验，但如何更好地在潜空间中表征连续常数仍是未解难题。

（完）
