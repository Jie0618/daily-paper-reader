---
title: Uncertainty-Aware Sparse Identification of Dynamical Systems via Bayesian Model Averaging
title_zh: 基于贝叶斯模型平均的动力系统不确定性感知稀疏识别
authors: "Shuhei Kashiwamura, Yusuke Kato, Hiroshi Kori, Masato Okada"
date: 2026-04-12
pdf: "https://arxiv.org/pdf/2604.10854v1"
tags: ["query:sr-alg"]
score: 9.0
evidence: 动力系统控制方程的贝叶斯稀疏识别
tldr: 针对动力系统建模中控制方程未知且存在模型选择不确定性的问题，本文提出了一种结合稀疏建模与贝叶斯模型平均（BMA）的识别框架。该方法不仅能推断系统的交互结构和函数形式，还能通过后验包含概率量化各候选组件的可信度。在振子网络等实验中，该框架成功识别了复杂交互并提供了可靠的不确定性度量，增强了数据驱动发现动力学模型的可靠性。
selection_source: fresh_fetch
motivation: 传统的点估计方法在数据有限或模型可辨识性差时，无法量化模型选择过程中的不确定性，导致结果可能具有误导性。
method: 提出一种基于贝叶斯模型平均的稀疏识别框架，利用后验包含概率来评估各候选交互项和基函数的可信度。
result: 实验证明该方法能准确恢复振子网络的稀疏结构及复杂交互，并在真实方程不在候选集中时仍能识别有效的函数组件。
conclusion: 贝叶斯不确定性量化对于构建可靠的数据驱动动力学模型至关重要，能有效提升模型选择的透明度和可信度。
---

## 摘要
在许多动力系统的数据驱动建模问题中，控制方程并非预先已知，必须从大量的候选相互作用和基函数中通过现象学方法进行选择。在这种情况下，仅凭点估计可能会产生误导，因为多个模型组件可能同样好地解释观测数据，特别是在数据有限或动力学特性表现出较差的可识别性时。因此，量化与模型选择相关的不确定性对于从数据中构建可靠的动力学模型至关重要。在这项工作中，我们为具有耦合组件的动力系统开发了一个贝叶斯稀疏识别框架，旨在推断相互作用结构和函数形式，并进行原则性的不确定性量化。所提出的方法将稀疏建模与贝叶斯模型平均相结合，产生后验包含概率，从而量化每个候选相互作用和基组件的可信度。通过对振子网络的数值实验，我们展示了该框架能够准确恢复具有量化不确定性的稀疏相互作用结构，包括高阶谐波分量、相位滞后效应和多体相互作用。我们还证明，即使在真实控制方程不包含在假设模型类中的现象学设定下，该方法仍能识别出具有量化不确定性的有效函数组件。这些结果突显了贝叶斯不确定性量化在动力学模型数据驱动发现中的重要性。

## Abstract
In many problems of data-driven modeling for dynamical systems, the governing equations are not known a priori and must be selected phenomenologically from a large set of candidate interactions and basis functions. In such situations, point estimates alone can be misleading, because multiple model components may explain the observed data comparably well, especially when the data are limited or the dynamics exhibit poor identifiability. Quantifying the uncertainty associated with model selection is therefore essential for constructing reliable dynamical models from data. In this work, we develop a Bayesian sparse identification framework for dynamical systems with coupled components, aimed at inferring both interaction structure and functional form together with principled uncertainty quantification. The proposed method combines sparse modeling with Bayesian model averaging, yielding posterior inclusion probabilities that quantify the credibility of each candidate interaction and basis component. Through numerical experiments on oscillator networks, we show that the framework accurately recovers sparse interaction structures with quantified uncertainty, including higher-order harmonic components, phase-lag effects, and multi-body interactions. We also demonstrate that, even in a phenomenological setting where the true governing equations are not contained in the assumed model class, the method can identify effective functional components with quantified uncertainty. These results highlight the importance of Bayesian uncertainty quantification in data-driven discovery of dynamical models.

---

## 论文详细总结（自动生成）

### 论文总结：基于贝叶斯模型平均的动力系统不确定性感知稀疏识别

#### 1. 核心问题与整体含义（研究动机和背景）
在科学研究中，从观测数据中推断动力系统的控制方程（方程发现）是核心任务。传统方法（如 SINDy 或 LASSO）通常采用**点估计**来选择最可能的函数组件。然而，当数据有限、噪声较大或系统处于特定状态（如相位锁定）时，多个候选模型可能具有相似的解释力，仅凭点估计容易导致过度自信或误导性结论。
**核心问题**：如何在识别动力系统交互结构和函数形式的同时，量化模型选择过程中的**不确定性**，从而构建更可靠的物理模型。

#### 2. 方法论：核心思想与关键技术
论文提出了一种结合稀疏建模与**贝叶斯模型平均（Bayesian Model Averaging, BMA）**的框架：
*   **线性基函数表示**：假设动力学向量场可表示为候选基函数的线性组合 $\dot{X}_i = \sum a_e F_e(X_e)$。
*   **二值指示变量（Indicators）**：为每个候选基函数引入二值变量 $c_\gamma \in \{0, 1\}$，表示该项是否存在于方程中。
*   **分层贝叶斯模型**：
    *   **先验分布**：对指示变量采用伯努利分布，对系数采用“尖峰与平板”（Spike-and-slab）先验（即 $c=1$ 时为高斯分布，$c=0$ 时为狄拉克 $\delta$ 函数）。
    *   **后验包含概率（PIP）**：通过边缘化参数，计算每个候选组件被包含在模型中的后验概率，以此量化可信度。
*   **并行回火（Parallel Tempering, PT）**：由于离散指示变量导致后验景观高度崎岖且具有组合爆炸特性，采用并行回火 MCMC 算法在不同温度下进行采样，以确保全局搜索能力并避免陷入局部最优。

#### 3. 实验设计
论文通过数值实验验证了框架的有效性：
*   **场景一：合成振子网络（N=3）**：
    *   包含二体交互、三体交互（对称与非对称）、高阶谐波和相位滞后效应。
    *   **对比配置**：异步动力学（易辨识）、相位锁定动力学（难辨识）、带噪声的相位锁定（通过噪声恢复辨识度）。
*   **场景二：节拍器系统的现象学建模**：
    *   使用复杂的机械方程生成数据，但推断时仅使用简化的相位模型基函数。
    *   **目标**：验证在“真实模型不在候选集中”的情况下，能否识别出有效的简化动力学组件。
*   **Benchmark**：主要对比了不同数据量、动力学噪声和观测噪声水平下的识别准确率（Hamming 距离）和系数误差（RMSE）。

#### 4. 资源与算力
*   **算力说明**：文中**未明确说明**具体的 GPU 型号、数量或具体的训练时长。
*   **算法复杂度**：作者提到该方法涉及高维积分和组合求和，计算成本较高。特别是三体交互的候选空间随节点数 $N$ 呈 $O(N^3)$ 增长，采样过程具有挑战性。

#### 5. 实验数量与充分性
*   **实验规模**：针对不同动力学制度（异步 vs 同步）进行了详细对比；对数据量（$M$）和噪声强度（$\sigma_d, \sigma_o$）进行了参数扫描。
*   **充分性与客观性**：
    *   每组实验重复了 **10 次独立运行**并计算均值和标准差，保证了结果的统计可靠性。
    *   实验设计涵盖了从理想情况到模型失配（节拍器案例）的过渡，体现了评估的全面性。
    *   通过对比“相位锁定”导致的辨识度失效，客观展示了算法的局限性及不确定性量化的必要性。

#### 6 主要结论与发现
*   **不确定性量化的价值**：在相位锁定状态下，点估计会给出错误的结构，而该框架通过**低包含概率**正确反映了模型的不确定性。
*   **噪声的积极作用**：适度的动力学噪声可以打破相位锁定，增加轨迹的丰富度，从而显著提高复杂交互（如三体交互）的辨识度。
*   **现象学建模能力**：在节拍器实验中，即使基函数不完全匹配，框架也能准确识别出导致双稳态同步的关键项（如 $\sin(2\psi)$），且其可信度随数据量增加而提升。

#### 7. 优点（亮点）
*   **可解释性强**：后验包含概率（PIP）为物理学家提供了一个直观的指标，用于判断某个物理相互作用是否真实存在。
*   **处理复杂交互**：能够同时处理高阶谐波、相位滞后和多体相互作用，这在以往的稀疏识别研究中较为少见。
*   **鲁棒性**：通过并行回火算法有效解决了离散空间搜索的收敛问题。

#### 8. 不足与局限
*   **计算开销**：随着系统规模 $N$ 的增加，候选基函数数量呈爆炸式增长，MCMC 采样的效率将成为瓶颈，难以直接扩展到超大规模网络。
*   **节点独立性假设**：目前的公式假设各节点的回归是独立的，未充分利用节点间可能存在的共享参数（如全局耦合强度）。
*   **基函数依赖**：虽然能处理模型失配，但仍高度依赖于预设的字典质量；如果关键物理项完全不在字典中，模型将失效。

（完）
