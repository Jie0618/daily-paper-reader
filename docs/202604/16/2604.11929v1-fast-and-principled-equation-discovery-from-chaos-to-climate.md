---
title: Fast and principled equation discovery from chaos to climate
title_zh: 从混沌到气候：快速且规范的方程发现
authors: "Yuzheng Zhang, Weizhen Li, Rui Carvalho"
date: 2026-04-13
pdf: "https://arxiv.org/pdf/2604.11929v1"
tags: ["query:sr-alg"]
score: 10.0
evidence: 从噪声观测中自动发现方程
tldr: 针对从噪声和有限观测中发现复杂系统动力学方程的挑战，本文提出 Bayesian-ARGOS 框架。该方法结合了快速频率派筛选与聚焦贝叶斯推理，实现了具备不确定性量化的自动化方程发现。在多个混沌系统和气候数据重建任务中，其在数据效率、噪声容忍度和计算速度上均优于 SINDy 和传统 ARGOS，为从稀疏观测到可解释方程的转化提供了高效且稳健的路径。
selection_source: fresh_fetch
motivation: 现有的稀疏回归方法在自动化程度、统计严谨性和计算效率之间存在权衡，且难以处理噪声数据中的不确定性。
method: 开发了 Bayesian-ARGOS 混合框架，通过频率派快速筛选候选算子并结合贝叶斯推理进行精确的参数估计与统计诊断。
result: 该方法在数据效率和噪声容忍度上显著超越 SINDy，且计算成本比基于自助法的 ARGOS 降低了两个数量级。
conclusion: Bayesian-ARGOS 为从混沌系统到全球气候模式的跨尺度动力学方程发现提供了一个高效、严谨且可解释的通用框架。
---

## 摘要
我们预测、控制并最终理解复杂系统的能力取决于发现控制其动力学的方程。因此，直接从含有噪声且有限的观测数据中识别这些方程已成为数据驱动科学中的核心挑战，然而现有的基于库的稀疏回归方法被迫在自动化、统计严谨性和计算效率之间做出妥协。在此，我们开发了 Bayesian-ARGOS，这是一个通过结合快速频率派筛选与针对性贝叶斯推断来调和这些需求的混合框架，能够以现有方法极低的计算成本实现具有规范不确定性量化的自动化方程发现。在不同数据稀缺程度和噪声水平下的七个混沌系统测试中，Bayesian-ARGOS 在大多数场景下优于两种最先进的方法。在所有系统中，其数据效率均超过了 SINDy，且在七个系统中的六个系统中，其噪声容忍度也优于 SINDy，同时与基于自助法（bootstrap）的 ARGOS 相比，计算成本降低了两个数量级。此外，概率化表述还支持一套标准的统计诊断工具，包括影响分析和多重共线性检测，从而揭示了原本不透明的失效模式。当与用于高维海表温度重建的表示学习（SINDy-SHRED）集成时，Bayesian-ARGOS 增加了有效潜在方程的产出，并显著提高了长期稳定性。因此，Bayesian-ARGOS 提供了一条从稀缺且有噪声的观测到可解释控制方程的规范、自动且高效的计算路径，为跨尺度的方程发现提供了一个实用框架，涵盖了从基准混沌系统到全球气候模式背后的潜在动力学。

## Abstract
Our ability to predict, control, and ultimately understand complex systems rests on discovering the equations that govern their dynamics. Identifying these equations directly from noisy, limited observations has therefore become a central challenge in data-driven science, yet existing library-based sparse regression methods force a compromise between automation, statistical rigor, and computational efficiency. Here we develop Bayesian-ARGOS, a hybrid framework that reconciles these demands by combining rapid frequentist screening with focused Bayesian inference, enabling automated equation discovery with principled uncertainty quantification at a fraction of the computational cost of existing methods. Tested on seven chaotic systems under varying data scarcity and noise levels, Bayesian-ARGOS outperforms two state-of-the-art methods in most scenarios. It surpasses SINDy in data efficiency for all systems and noise tolerance for six out of the seven, with a two-order-of-magnitude reduction in computational cost compared to bootstrap-based ARGOS. The probabilistic formulation additionally enables a suite of standard statistical diagnostics, including influence analysis and multicollinearity detection that expose failure modes otherwise opaque. When integrated with representation learning (SINDy-SHRED) for high dimensional sea surface temperature reconstruction, Bayesian-ARGOS increases the yield of valid latent equations with significantly improved long horizon stability. Bayesian-ARGOS thus provides a principled, automated, and computationally efficient route from scarce and noisy observations to interpretable governing equations, offering a practical framework for equation discovery across scales, from benchmark chaotic systems to the latent dynamics underlying global climate patterns.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **Bayesian-ARGOS** 的新型方程发现框架，旨在从噪声和稀疏数据中高效、严谨地提取动力学方程。以下是对该论文的深度结构化总结：

### 1. 核心问题与整体含义（研究动机）
科学发现正从传统的“第一性原理”转向“数据驱动”范式。虽然基于库的稀疏回归（如 SINDy）在识别控制方程方面取得了进展，但现有方法面临**“三难困境”**：
*   **自动化程度低**：需要大量手动调节超参数。
*   **统计严谨性不足**：难以量化参数的不确定性，且缺乏诊断模型失效的工具。
*   **计算效率低**：现有的具备统计严谨性的方法（如基于自助法的 ARGOS）计算成本极高。
**Bayesian-ARGOS** 的提出是为了同时解决这三个问题，实现从混沌系统到复杂气候模式的快速且规范的方程发现。

### 2. 方法论：核心思想与关键技术
该框架采用**“粗筛+精算”**的混合策略，分为两个主要阶段：
*   **频率派筛选阶段（快速降维）**：
    *   使用**自适应 LASSO（Adaptive LASSO）**进行初步变量选择。
    *   执行**双阶段回归**：第一遍使用岭回归（Ridge）派生的权重以应对多重共线性；第二遍使用普通最小二乘（OLS）派生的权重以实现渐近无偏性。
    *   利用 **BIC（贝叶斯信息准则）** 自动选择最优稀疏模型，极大地减少了候选基函数的数量。
*   **贝叶斯推理阶段（不确定性量化）**：
    *   在筛选后的精简库上，利用**哈密顿蒙特卡洛（HMC）**采样估计系数的后验分布。
    *   通过**置信区间（Credible Intervals）**排除包含零的项，确保最终方程的显著性。
*   **统计诊断工具**：引入了 VIF（多重共线性检测）、PSIS-LOO（影响点检测）和残差分析，使模型失效模式（如数据过密导致的共线性或噪声消失导致的异方差）变得透明可查。

### 3. 实验设计与 Benchmark
*   **基准测试集**：选取了 7 个经典的 3D 混沌系统（Lorenz, Thomas, Rössler, Dadras, Aizawa, Sprott, Halvorsen）。
*   **对比方法（Benchmarks）**：
    *   **SINDy**：经典的确定性稀疏识别方法。
    *   **ARGOS**：基于自助法（Bootstrap）的自动化频率派方法。
*   **实验变量**：系统性地改变观测数量 $n$（从 $10^2$ 到 $10^5$）和信噪比 SNR（从 1dB 到 $\infty$）。
*   **实际应用**：将该方法集成到 **SINDy-SHRED** 框架中，处理高维海表温度（SST）数据的潜在动力学重建。

### 4. 资源与算力
*   **硬件环境**：使用了高性能计算（HPC）集群，配备 128 核 CPU（2 × AMD EPYC 7702）和 256GB RAM。
*   **深度学习部分**：在 SST 数据实验中，使用了 **NVIDIA A100 80GB GPU** 进行神经网络（GRU 和解码器）的训练。
*   **效率提升**：论文强调在 $n=10^5$ 时，Bayesian-ARGOS 比 ARGOS 快了约 **100 倍**（从 $>10^{4.7}$ 秒降至 $<10^{2.5}$ 秒）。

### 5. 实验数量与充分性
*   **实验规模**：针对每个混沌系统的每种参数组合（$n$ 和 SNR），均进行了 **100 次独立重复实验**（随机初始条件），以计算“成功率”。
*   **气候数据实验**：进行了 **150 次独立训练实验**，涵盖了 250 个随机分布的传感器配置。
*   **充分性评价**：实验设计非常充分且客观。通过大规模重复实验排除了随机性干扰，并覆盖了从极度稀缺到充足、从高噪声到无噪声的极端情况，能够全面反映算法的鲁棒性。

### 6. 主要结论与发现
*   **性能卓越**：在数据效率上，Bayesian-ARGOS 在所有系统中均优于 SINDy；在噪声容忍度上，优于 SINDy（6/7 系统）和 ARGOS（4/7 系统）。
*   **计算优势**：实现了比 ARGOS 低两个数量级的计算成本，同时保留了不确定性量化能力。
*   **诊断价值**：成功解释了为何“更多数据”或“更低噪声”有时反而会导致识别失败（如 Aizawa 系统中的流形共线性问题）。
*   **气候预测**：在 SST 重建中，Bayesian-ARGOS 的有效方程产出率（77%）显著高于 SINDy（60%），且长期预测稳定性更强。

### 7. 优点与亮点
*   **混合架构**：巧妙结合了频率派的速度和贝叶斯派的严谨，解决了大规模基函数库下 MCMC 采样难的问题。
*   **诊断透明化**：首次在方程发现领域系统性地应用 PSIS-LOO 和 VIF 等统计诊断工具，将“黑箱”识别过程变为可解释的统计推断。
*   **模块化集成**：展示了与深度学习（表示学习）结合的巨大潜力，能够处理现实世界的高维时空数据。

### 8. 不足与局限
*   **库依赖性**：作为基于库的方法，其准确性仍受限于候选基函数的完备性，若真实项不在库中则无法发现。
*   **选择不确定性**：虽然贝叶斯阶段量化了参数不确定性，但第一阶段（频率派筛选）引入的选择偏差（Selection Uncertainty）尚未在全流程中得到完全的理论量化。
*   **计算开销**：虽然比 ARGOS 快，但由于涉及 HMC 采样，其速度仍慢于纯确定性的 SINDy，在超大规模实时识别场景中可能受限。

（完）
