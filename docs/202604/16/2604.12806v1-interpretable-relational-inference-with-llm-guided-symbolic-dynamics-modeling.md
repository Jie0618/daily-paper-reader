---
title: Interpretable Relational Inference with LLM-Guided Symbolic Dynamics Modeling
title_zh: 基于大语言模型引导的符号动力学建模的可解释关系推理
authors: "Xiaoxiao Liang, Juyuan Zhang, Liming Pan, Linyuan Lü"
date: 2026-04-14
pdf: "https://arxiv.org/pdf/2604.12806v1"
tags: ["query:sr-alg"]
score: 10.0
evidence: 稀疏符号动力学和交互图的可微框架
tldr: 本研究针对多体系统中的潜在交互结构推断问题，提出了COSINE框架。该框架通过可微分优化联合发现交互图与稀疏符号动力学，并引入大语言模型（LLM）作为外环，根据优化反馈动态调整函数假设空间。实验表明，COSINE在合成系统和真实疫情数据上均能实现稳健的结构恢复，并生成简洁且符合物理机制的动力学表达式，兼顾了预测精度与机制可解释性。
selection_source: fresh_fetch
motivation: 现有的神经方法缺乏可解释性，而传统符号回归通常受限于固定的函数库和已知的网络拓扑。
method: 提出一种联合优化交互图与符号动力学的可微分框架，并利用LLM引导的外部循环自适应地修剪和扩展假设空间。
result: 在合成系统和大规模真实世界疫情数据上的实验证明，该方法能够准确恢复系统结构并提取出紧凑的动力学方程。
conclusion: 结合LLM引导的符号建模为复杂多体系统的可解释关系推断提供了一种兼具灵活性与物理一致性的新方案。
---

## 摘要
从观测到的动力学中推断潜在的相互作用结构是多体相互作用系统中的一个基本逆问题。大多数神经方法依赖于可训练图上的黑盒代理模型，以牺牲机理可解释性为代价换取准确性。符号回归提供了显式的动力学方程和更强的归纳偏置，但通常假设拓扑结构已知且函数库固定。我们提出了 COSINE（符号相互作用与网络边协同优化），这是一个能够联合发现相互作用图和稀疏符号动力学的微分框架。为了克服固定符号库的局限性，COSINE 进一步引入了一个外层循环大语言模型，利用内层优化循环的反馈自适应地剪枝和扩展假设空间。在合成系统和大规模真实世界流行病数据上的实验表明，该方法具有鲁棒的结构恢复能力，并能生成简洁且符合机理的动力学表达式。代码：https://anonymous.4open.science/r/COSINE-6D43。

## Abstract
Inferring latent interaction structures from observed dynamics is a fundamental inverse problem in many-body interacting systems. Most neural approaches rely on black-box surrogates over trainable graphs, achieving accuracy at the expense of mechanistic interpretability. Symbolic regression offers explicit dynamical equations and stronger inductive biases, but typically assumes known topology and a fixed function library. We propose \textbf{COSINE} (\textbf{C}o-\textbf{O}ptimization of \textbf{S}ymbolic \textbf{I}nteractions and \textbf{N}etwork \textbf{E}dges), a differentiable framework that jointly discovers interaction graphs and sparse symbolic dynamics. To overcome the limitations of fixed symbolic libraries, COSINE further incorporates an outer-loop large language model that adaptively prunes and expands the hypothesis space using feedback from the inner optimization loop. Experiments on synthetic systems and large-scale real-world epidemic data demonstrate robust structural recovery and compact, mechanism-aligned dynamical expressions. Code: https://anonymous.4open.science/r/COSINE-6D43.

---

## 论文详细总结（自动生成）

这篇论文介绍了一个名为 **COSINE** 的框架，旨在解决从观测到的动力学轨迹中同时推断“潜在交互结构（图）”和“显式控制方程（符号动力学）”的挑战。

以下是对该论文的结构化总结：

### 1. 论文的核心问题与整体含义
*   **研究动机**：在物理、生物和社会系统中，实体间的交互图（如神经元连接、社交网络）往往不可直接观测，只能获得随时间变化的动力学数据。
*   **核心挑战**：
    1.  **黑盒问题**：现有的神经关系推理方法（如 NRI）虽然准确，但缺乏物理机理的可解释性，且容易通过过度参数化拟合虚假边。
    2.  **符号回归局限**：传统的符号回归（SR）通常假设网络拓扑已知，且依赖于预定义的固定函数库，难以处理未知结构和复杂演化。
*   **研究意义**：论文提出了一种联合优化方案，通过符号化建模增强归纳偏置，从而在推断图结构的同时，发现简洁、符合物理规律的控制方程。

### 2. 论文提出的方法论：COSINE 框架
COSINE 采用**双层闭环优化结构**：
*   **内环（联合优化层）**：
    *   **图生成模块**：使用可微分的 **Gumbel-Softmax** 采样技术，将潜在的边参数化为概率分布，实现图结构的可微学习。
    *   **稀疏符号消息传递**：将动力学分解为“消息流（Message）”和“更新流（Update）”。每一部分都表示为符号基函数库（如 $x, \sin(x), x_j-x_i$ 等）的稀疏线性组合。
    *   **损失函数**：包含预测误差（NLL）、图稀疏先验（KL 散度）和系数稀疏惩罚（L1 正则化）。
*   **外环（LLM 引导进化层）**：
    *   **LLM 监督者**：引入大语言模型（如 GPT-OSS 20B）作为符号假设生成器。
    *   **自适应调整**：LLM 根据内环反馈（验证集损失、各项系数权重、残差模式）自动修剪贡献小的项，并提出新的非线性项，打破了固定函数库的限制。

### 3. 实验设计
*   **数据集/场景**：
    1.  **合成系统**：涵盖 6 种典型动力学（Michaelis-Menten 生化反应、扩散过程、弹簧系统、Kuramoto 同步模型、FJ 舆论动力学、CMN 耦合映射网络）。
    2.  **网络拓扑**：在 ER、BA、WS 三种随机图上进行测试（节点数 $N=50$）。
    3.  **真实数据**：美国四个州（亚利桑那、康涅狄格、伊利诺伊、密歇根）的 COVID-19 县级疫情传播数据。
*   **对比方法（Benchmark）**：
    *   **统计方法**：Granger 因果关系（GC）、互信息（MI）、传递熵（TE）。
    *   **神经方法**：NRI、GDP（图动力学先验）、RIVA（基于注意力机制）。
*   **评价指标**：图恢复的 AUC 值、符号项的准确度（Term Accuracy）。

### 4. 资源与算力
*   **硬件环境**：实验使用了本地部署的 **NVIDIA RTX 4090 GPU**。
*   **模型配置**：默认使用 **GPT-OSS (20B)** 模型通过 Ollama 框架运行。
*   **训练细节**：每个外环回合运行 1000 个内环 Epoch，总共进行最多 10 个外环回合。
*   **效率表现**：论文提到 COSINE 在训练时间和显存占用上均优于 NRI，尤其在节点数增加到 200 时，显存优势显著。

### 5. 实验数量与充分性
*   **实验规模**：在 6 种动力学 $\times$ 3 种图结构上进行了详尽测试，并包含低数据量（10x10 样本）的极端情况测试。
*   **消融实验**：对比了“无 LLM 更新（固定库）”和“基于阈值的简单剪枝”，证明了 LLM 引导进化的必要性。
*   **客观性**：通过多尺度 LLM（从 8B 到 120B）的对比实验，探讨了模型规模对结果的影响，实验设计较为全面、公平。

### 6. 论文的主要结论与发现
*   **结构恢复**：COSINE 在几乎所有合成系统上的图恢复 AUC 均达到或接近 1.0，显著超过传统统计方法和黑盒神经方法。
*   **机理发现**：该方法能准确识别出如 $\sin(x_j-x_i)$（Kuramoto）或 $x_i \cdot x_j$（疫情传播）等核心物理项。
*   **真实应用**：在疫情数据中，COSINE 发现了不同州之间传播模式的异质性（如某些州受外部输入驱动，某些州受本地非线性增长驱动）。
*   **鲁棒性**：在极少样本下，由于符号建模的强归纳偏置，COSINE 表现出比神经方法更稳健的性能。

### 7. 优点
*   **开创性结合**：首次将 LLM 的符号推理能力与可微图学习、稀疏回归结合，解决了“结构未知”下的符号回归难题。
*   **高可解释性**：输出的是人类可读的数学公式，而非不可解释的权重矩阵。
*   **计算高效**：相比于全连接的 GNN，稀疏线性组合的计算开销更小，扩展性更好。

### 8. 不足与局限
*   **LLM 依赖性**：对于极度复杂的动力学，较小规模的 LLM（如 8B）可能无法提出有效的符号假设。
*   **非唯一性风险**：在存在噪声的情况下，多个不同的符号组合可能产生相似的拟合效果（不可辨识性问题），可能导致推导出的公式与真实物理规律有偏差。
*   **算力门槛**：虽然推理高效，但外环调用大模型（尤其是 20B 以上规模）仍需一定的本地显存或 API 开销。

（完）
