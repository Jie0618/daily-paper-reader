---
title: Field Inversion Symbolic Regression with Embedded Equation Learner for Interpretable Turbulence Model Correction
title_zh: 结合嵌入式方程学习器的场反演符号回归方法，用于可解释的湍流模型修正
authors: "Li Jiazhe, Wu Chenyu, He Zizhou, Zhang Yufei"
date: 2026-04-16
pdf: "https://arxiv.org/pdf/2604.14569v1"
tags: ["query:sr-alg"]
score: 10.0
evidence: 将方程学习嵌入到偏微分方程约束的场反演中以获得可解释模型
tldr: 针对湍流模型修正中可解释性与物理一致性的挑战，本文提出FISR-EQL框架。该方法将方程学习器（EQL）直接嵌入基于伴随法的PDE约束场反演过程中，实现了端到端的符号回归优化。通过在SST模型上对分离流进行训练，该方法不仅获得了性能媲美神经网络的紧凑解析表达式，还显著提升了模型在多种未见工况下的泛化能力和可解释性。
selection_source: fresh_fetch
motivation: 传统的两阶段数据驱动湍流修正方法往往缺乏物理一致性或可解释性，难以直接获得简洁的解析修正项。
method: 将方程学习网络（EQL）嵌入到基于伴随法的场反演流程中，通过端到端优化直接从流场数据中学习符号化的修正表达式。
result: 在SST模型修正中，该方法成功识别出简洁的解析公式，显著改善了分离流预测精度，且在多种复杂几何构型上表现出良好的泛化性。
conclusion: FISR-EQL为构建既具有高性能又具备透明可解释性的数据驱动湍流模型提供了一条切实可行的路径。
---

## 摘要
提出了一种可解释且物理一致的湍流模型修正框架，称为 FISR-Equation Learner (EQL)。该框架通过将方程学习直接嵌入到基于伴随方法的偏微分方程 (PDE) 约束场反演过程中实现。与传统的两阶段方法不同，该修正模型利用 EQL 架构在参数空间内进行端到端优化，从而在保持与控制方程一致性的同时，能够直接识别出紧凑的解析表达式。该方法被应用于剪切应力输运 (SST) 模型，并在两种典型的分离流（弯曲后向阶梯流和 NASA 驼峰流）上进行了训练。所得的显式表达式显著减少了对分离泡的过度预测，并改善了对再附着点的预测，在保持完全可解释性的同时，实现了与基于神经网络的端到端方法相当的性能。在未见过的构型上验证了泛化能力，包括周期性山丘、表面安装立方体以及高升力 NLR7301 翼型。该模型在不降低附着边界层性能的情况下，改善了分离流预测和失速特性。总的来说，FISR-EQL 为实现最优且透明的数据驱动湍流模型修正提供了一条切实可行的途径。

## Abstract
An interpretable, physics-consistent turbulence model correction framework, termed FISR-Equation Learner (EQL), is proposed by embedding equation learning directly into a Partial Differential Equations (PDE)-constrained field inversion process based on the adjoint method. Unlike conventional two-stage approaches, the correction model is optimized end-to-end in parameter space using an EQL architecture, enabling the direct identification of compact analytical expressions while maintaining consistency with the governing equations. The method is applied to the shear-stress-transport (SST) model and trained on two canonical separated flows, the curved backward-facing step and the NASA hump. The resulting explicit expression significantly reduces separation bubble overprediction and improves reattachment prediction, achieving performance comparable to neural-network-based end-to-end methods while retaining full interpretability. Generalization is demonstrated on unseen configurations, including periodic hills, a surface-mounted cube, and the high-lift NLR7301 airfoil. The model improves separated-flow predictions and stall characteristics without degrading attached boundary-layer performance. Overall, FISR-EQL provides a practical pathway toward optimal yet transparent data-driven turbulence model correction.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **FISR-EQL** 的新型数据驱动湍流模型修正框架。以下是对该论文的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
传统的雷诺平均纳维-斯托克斯（RANS）模型（如 SST 模型）在预测复杂流动（尤其是分离流）时存在固有缺陷，通常会过度预测分离泡大小。
*   **研究动机**：现有的数据驱动修正方法主要分为两类：一是基于神经网络的“黑盒”模型，缺乏可解释性且难以集成到工业 CFD 软件中；二是“两阶段”符号回归方法（先进行场反演获取修正场，再进行符号拟合），这会导致“目标不匹配”问题，即拟合出的公式并不一定是使流场误差最小的最优解。
*   **核心目标**：开发一种既能保证物理一致性（PDE 约束），又具有端到端优化能力（消除目标不匹配），且最终输出为简洁、可解释解析表达式的修正框架。

### 2. 方法论：核心思想与关键技术
论文提出了 **FISR-EQL** 框架，其核心是将**方程学习器（Equation Learner, EQL）**直接嵌入到**场反演（Field Inversion）**的优化循环中。
*   **核心思想**：利用伴随法（Adjoint Method）计算流场误差对模型参数的梯度，直接在参数空间内优化 EQL 网络，从而得到最优的修正公式。
*   **关键技术细节**：
    *   **EQL 架构**：一种特殊的神经网络，其激活函数被替换为符号算子（如 $id, \tanh, \frac{1}{1+|x|}$ 和乘法）。
    *   **三阶段训练流程**：
        1.  **常规训练**：初步拟合数据。
        2.  **稀疏化（L1 正则化）**：通过惩罚项迫使网络权重变稀疏，剔除不重要的分支。
        3.  **剪枝与微调**：设定阈值将微小权重归零，提取最终的解析表达式并进行最后的精度优化。
    *   **特征选择**：输入特征包括产生项与耗散项之比 ($P/D$)、涡流雷诺数 ($Re_v$) 以及应变率和旋转率张量的三个不变量 ($\lambda_1, \lambda_2, \lambda_5$)。
    *   **屏蔽函数（Shielding Function）**：确保修正仅在分离区激活，而不破坏原本预测准确的附着边界层。

### 3. 实验设计
*   **训练场景**：使用了两个经典的二维分离流案例：弯曲后向阶梯（CBFS）和 NASA 驼峰（Hump）。
*   **Benchmark（基准）**：原始 SST 湍流模型。
*   **对比方法**：
    *   **Field Inversion (FI)**：纯空间场反演（理论上限）。
    *   **FIML-classic**：传统的两阶段神经网络修正。
    *   **FIML-direct**：端到端的神经网络修正（不可解释）。
    *   **两阶段 FISR**：先反演再符号回归的方法。
*   **测试场景（泛化性验证）**：
    *   周期性山丘（Periodic Hills，三种不同坡度）。
    *   NLR7301 双段翼型（高升力/失速预测）。
    *   FAITH Hill（三维复杂分离流）。
    *   零压力梯度平板边界层（验证对原模型的保护）。

### 4. 资源与算力
*   **软件框架**：基于开源伴随框架 **DAFoam** 和 **OpenFOAM** 实现。
*   **优化算法**：使用 **SNOPT** 优化器处理大规模约束优化问题。
*   **算力说明**：文中**未明确列出**具体的 GPU/CPU 型号、核心数量或具体的训练时长。但考虑到伴随法的高效性，该方法通常比基于采样（如遗传算法）的符号回归快得多。

### 5. 实验数量与充分性
*   **实验规模**：论文进行了 42 组随机初始化的训练实验（针对三种不同规模的 EQL 网络），以证明结果的稳健性。
*   **充分性**：实验覆盖了从二维到三维、从低速不可压缩到亚声速可压缩、从简单几何到复杂翼型的多种工况。
*   **客观性**：通过对比 FIML-direct（神经网络上限），客观展示了符号回归在牺牲极小精度的情况下换取了巨大的可解释性优势。

### 6. 主要结论与发现
*   **精度提升**：FISR-EQL 提取的简洁公式显著改善了再附着点的预测，误差相比原始 SST 模型大幅降低。
*   **可解释性**：获得了一个具体的解析表达式（如表 4 中的公式 5），可以直接观察到哪些物理特征（如 $\lambda_2, \lambda_5$）对修正起主导作用。
*   **泛化能力强**：在未参与训练的周期性山丘和三维 FAITH Hill 案例中，修正模型依然表现良好，甚至在翼型失速预测上优于原始模型。
*   **数值稳定性**：相比于复杂的神经网络，简洁的解析表达式在 CFD 迭代求解过程中表现出更好的数值稳定性。

### 7. 优点与亮点
*   **端到端一致性**：解决了两阶段方法中的目标不匹配问题，确保找到的是对流场预测最优的公式。
*   **透明度**：将“黑盒”修正转化为“白盒”公式，符合航空航天领域对模型可信度的要求。
*   **高效性**：结合伴随法，使得在 PDE 约束下搜索符号表达式变得计算可行。
*   **物理保护**：通过屏蔽函数设计，确保了模型在处理新问题时不会“修坏”原本正确的部分。

### 8. 不足与局限
*   **权重调优困难**：多目标优化（两个训练案例+正则化项）的权重系数（$\lambda$）需要大量前期的人工调优。
*   **算子限制**：为了数值稳定性，EQL 中未直接引入除法算子，这可能限制了某些复杂物理关系的表达。
*   **精度折损**：由于追求极度的稀疏性和简洁性，其在训练集上的表现略逊于参数量巨大的深度神经网络（FIML-direct）。
*   **特征依赖**：模型的表现高度依赖于预选的物理特征，如果关键物理量未被包含在输入中，模型性能将受限。

（完）
