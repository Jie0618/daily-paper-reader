---
title: Bias Inheritance in Neural-Symbolic Discovery of Constitutive Closures Under Function-Class Mismatch
title_zh: 函数类失配下神经符号发现本构闭合中的偏差继承
authors: "Hanbing Liang, Ze Tao, Fujun Liu"
date: 2026-04-01
pdf: "https://arxiv.org/pdf/2604.01335v1"
tags: ["query:sr-alg"]
score: 9.0
evidence: 数据驱动的物理定律和符号闭合项发现
tldr: 本研究探讨非线性反应扩散系统中本构关系的神经符号发现。针对函数类不匹配问题，提出包含物理约束代理学习、符号压缩和前向验证的三阶段框架。研究揭示了“偏差继承”机制，即符号模型会继承神经代理的误差，证明建模瓶颈在于初始数值反问题而非符号压缩，强调了前向验证对物理规律发现的关键作用。
selection_source: fresh_fetch
motivation: 旨在解决从时空观测数据中恢复物理规律时，低残差或短期预测容易误导物理规律真实性判定的问题。
method: 提出一种三阶段框架，结合弱形式驱动的神经代理学习、受限符号族压缩以及基于新初始条件的前向重仿真验证。
result: 实验发现符号闭包的误差与神经代理高度相关（偏差继承比接近1），且在函数类不匹配时神经代理比传统基函数更具灵活性。
conclusion: 神经符号建模的瓶颈在于初始数值反问题而非符号压缩，物理规律的发现必须通过严格的前向仿真验证而非仅依赖残差最小化。
---

## 摘要
我们研究了在已知控制偏微分方程（PDE）结构的非线性反应-扩散系统中，数据驱动的本构闭合发现。我们的目标是从时空观测数据中稳健地恢复扩散和反应规律，同时避免将低残差或短时预测与物理恢复混为一谈的常见陷阱。我们提出了一个三阶段神经符号框架：(1) 利用抗噪的弱形式驱动目标，在物理约束下学习数值代理模型；(2) 将这些代理模型压缩为受限的可解释符号族（如多项式、有理式和饱和形式）；(3) 通过在未见过的初始条件下进行显式正向重仿真来验证符号闭合。大量的数值实验揭示了两种不同的机制。在库匹配设置下，弱多项式基准表现为正确指定的参考估计器，表明神经代理模型并不一致地优于经典基函数。相反，在函数类失配下，神经代理模型提供了必要的灵活性，并可以被压缩为紧凑的符号规律，且重仿真退化极小。然而，我们发现了一种关键的“偏差继承”机制，即符号压缩并不能自动修复本构偏差。在各种观测机制下，符号闭合的真实误差紧随神经代理模型的误差，产生的偏差继承率接近于 1。这些发现表明，神经符号建模的主要瓶颈在于初始的数值逆问题，而非随后的符号压缩。我们强调，本构关系的断言必须通过正向验证得到严格支持，而不能仅依靠残差最小化。

## Abstract
We investigate the data-driven discovery of constitutive closures in nonlinear reaction-diffusion systems with known governing PDE structures. Our objective is to robustly recover diffusion and reaction laws from spatiotemporal observations while avoiding the common pitfall where low residuals or short-horizon predictions are conflated with physical recovery. We propose a three-stage neural-symbolic framework: (1) learning numerical surrogates under physical constraints using a noise-robust weak-form-driven objective; (2) compressing these surrogates into restricted interpretable symbolic families (e.g., polynomial, rational, and saturation forms); and (3) validating the symbolic closures through explicit forward re-simulation on unseen initial conditions. Extensive numerical experiments reveal two distinct regimes. Under matched-library settings, weak polynomial baselines behave as correctly specified reference estimators, showing that neural surrogates do not uniformly outperform classical bases. Conversely, under function-class mismatch, neural surrogates provide necessary flexibility and can be compressed into compact symbolic laws with minimal rollout degradation. However, we identify a critical "bias inheritance" mechanism where symbolic compression does not automatically repair constitutive bias. Across various observation regimes, the true error of the symbolic closure closely tracks that of the neural surrogate, yielding a bias inheritance ratio near one. These findings demonstrate that the primary bottleneck in neural-symbolic modeling lies in the initial numerical inverse problem rather than the subsequent symbolic compression. We underscore that constitutive claims must be rigorously supported by forward validation rather than residual minimization alone.

---

## 论文详细总结（自动生成）

### 论文总结：函数类失配下神经符号发现本构闭合中的偏差继承

#### 1. 核心问题与研究动机
*   **核心问题**：在已知偏微分方程（PDE）结构（如反应-扩散方程）但未知具体本构关系（如扩散系数 $D(u)$ 和反应项 $R(u)$）的情况下，如何从观测数据中稳健地恢复这些物理规律。
*   **研究动机**：科学机器学习（SciML）中存在一个常见陷阱，即开发者往往将“低残差”或“短时预测准确”等同于“物理规律的正确恢复”。实际上，由于逆问题的病态性，不同的本构组合可能产生相似的轨迹。论文旨在揭示神经符号（Neural-Symbolic）方法在处理函数类失配（即真实规律不在预设符号库中）时的表现，并探讨符号化过程是否能修复数值模型的偏差。

#### 2. 方法论
论文提出了一个**三阶段神经符号框架**：
*   **阶段 1：弱形式驱动的数值本构恢复**
    *   使用神经网络（MLP）作为 $D(u)$ 和 $R(u)$ 的数值代理模型。
    *   **核心技术**：采用**空间弱形式（Weak Form）**残差作为损失函数，通过测试函数积分降低对噪声和空间导数的敏感性。
    *   **损失函数**：结合了弱形式残差、质量平衡约束（Mass Balance）、辅助前向预测（Rollout）以及物理约束（如扩散项的正定性）。
*   **阶段 2：受限符号压缩**
    *   将学习到的神经网络代理模型在状态空间上进行采样，并拟合到受限的可解释符号族（如低阶多项式、有理函数、饱和形式等）。
    *   **思想**：这被视为一种“蒸馏”过程，而非直接从原始轨迹中搜索符号。
*   **阶段 3：前向重仿真验证**
    *   将提取的符号规律重新代入 PDE，在**未见过的初始条件**下进行长时前向仿真，以验证其物理真实性。

#### 3. 实验设计
*   **实验场景**：一维周期性反应-扩散系统。
*   **数据集/案例**：
    *   **Case A & B（库匹配）**：真实规律为多项式，用于验证基准性能。
    *   **Case Exp（函数类失配）**：真实规律包含指数项，用于测试框架在模型形式错误时的鲁棒性。
*   **对比方法（Benchmark）**：
    *   强形式多项式基准（Strong-form Polynomial）。
    *   弱形式多项式基准（Weak-form Polynomial）。
    *   纯神经代理模型（Neural Surrogate）。
    *   神经符号组合模型（Neural+Symbolic）。
*   **测试条件**：涵盖干净数据、5% 高斯噪声、时空稀疏采样（Downsampling）以及不同程度的动力学激发（Excitation）。

#### 4. 资源与算力
*   **训练细节**：使用 Adam 优化器，学习率为 $10^{-3}$，训练 150 个 Epoch。
*   **算力说明**：论文**未明确说明**具体的 GPU 型号或数量。但从实验规模（1D 系统，$N_x=64$，8 条轨迹）来看，该实验对算力的需求较低，普通的单卡 GPU 或高性能 CPU 即可完成。

#### 5. 实验数量与充分性
*   **实验规模**：进行了多项式匹配测试、指数项失配压力测试、噪声敏感性扫描（0%-5%）、稀疏性消融实验、激发程度对比实验，以及初步的 2D 扩展实验。
*   **充分性评价**：实验设计非常**系统且客观**。每个实验均采用 3 个随机种子取平均值，并引入了“反逆向犯罪”（Anti-inverse-crime）检查（即训练和验证使用不同的数值格式和网格），确保了结论的可靠性。

#### 6. 主要结论与发现
*   **偏差继承（Bias Inheritance）**：这是论文最重要的发现。符号压缩阶段并不能修复第一阶段神经代理模型产生的物理偏差。符号模型的误差高度追踪神经模型的误差，**偏差继承比（BIR）接近 1**。
*   **瓶颈位置**：神经符号建模的主要瓶颈在于**初始的数值逆问题求解**（阶段 1），而非后续的符号回归（阶段 2）。
*   **基准表现**：在库匹配的情况下，简单的弱形式多项式回归表现极强，神经模型并不总是优于经典方法。
*   **验证标准**：低残差不代表物理正确，只有通过**未见初始条件的前向验证**才能证明本构规律的有效性。

#### 7. 优点与亮点
*   **解耦分析**：通过三阶段框架，成功分离了数值逼近误差、符号压缩误差和前向仿真误差，使问题的根源清晰化。
*   **理论支撑**：提供了关于弱形式一致性、偏差继承和前向稳定性的理论分析（Proposition 1-5）。
*   **批判性视角**：挑战了“符号回归可以自动提炼物理真理”的乐观假设，提出了更严谨的验证流程。

#### 8. 不足与局限
*   **维度限制**：主要集中在 1D 系统，2D 实验仅作为初步验证，尚未深入探讨高维复杂几何下的表现。
*   **噪声敏感性**：实验显示，当噪声达到 5% 时，即使有弱形式保护，第一阶段的本构恢复仍会出现显著退化。
*   **符号库依赖**：阶段 2 使用了受限符号族，虽然论文通过 PySR 进行了对比，但在处理极其复杂的非线性形式时，符号库的预设仍可能限制发现能力。

（完）
