---
title: Neural Discovery of Memory and Nonlocal Kernels in Integro-Differential Equations with Constrained Kolmogorov--Arnold Networks
title_zh: 利用受约束的 Kolmogorov--Arnold 网络在积分微分方程中进行记忆核与非局部核的神经发现
authors: "Aruzhan Tleubek, Salah A Faroughi"
date: 2026-07-13
pdf: "https://arxiv.org/pdf/2607.11110v1"
tags: ["query:sr-alg"]
score: 8.0
evidence: 发现积分微分方程中的记忆和非局部核
tldr: 本研究针对从稀疏噪声数据中发现积分微分方程（IDE）记忆核的病态逆问题，提出了一种基于可微求解器和约束Kolmogorov-Arnold网络（KAN）的框架。通过引入基于Bernstein多项式的硬约束MC-KAN和基于Chebyshev的软约束Cheb-KAN来表征未知核，并结合符号回归获取解析式。实验证明，在处理高维、稀疏且有噪声的数据时，硬约束机制比软惩罚项具有更强的鲁棒性和更高的重构精度。
selection_source: fresh_fetch
motivation: 传统的积分微分方程核函数识别方法往往依赖于特定的解析推导或严格的假设，难以处理稀疏且有噪声的观测数据。
method: 提出一种结合可微求解器与约束KAN（包括硬约束MC-KAN和软约束Cheb-KAN）的框架，并利用符号回归提取核函数的闭式表达式。
result: 在1D和2D基准测试中，该方法均能准确恢复核函数形式，且硬约束MC-KAN在处理复杂的2D稀疏噪声问题时表现更优。
conclusion: 在多维核函数发现任务中，通过网络结构强制执行物理形状约束比使用软惩罚项更具鲁棒性。
---

## 摘要
从稀疏且含噪声的观测数据中发现控制积分微分方程 (IDE) 的记忆核或非局部核是一个病态反问题。现有的识别方法通常依赖于特定问题的解析推导、专门的观测要求或对核函数的限制性假设，从而限制了它们在不同类别 IDE 中的适用性。在这项工作中，我们提出了一个基于可微求解器的框架，用于直接从时空观测中发现记忆核和非局部核。在求解器内部，未知的核函数使用受约束的 Kolmogorov--Arnold 网络 (KAN) 参数化表示，并通过两种不同的方法施加物理约束：一种是基于 Bernstein 多项式的单调凸 KAN (MC-KAN)，其系数约束通过构造强制实现了正性、单调递减和凸性；另一种是基于 Chebyshev 的 KAN (Cheb-KAN)，其中通过软惩罚项来鼓励实现相同的性质。训练完成后，对学习到的核函数应用符号回归，以获得可解释的闭式表达式。我们在涵盖一维 Volterra 方程、一维粘弹性波偏积分微分方程以及具有各向异性耦合核的二维非局部反应扩散方程的基准测试上评估了这两种方法。对于一维问题，两种方法都能恢复正确的核函数形式，并达到相当的解重构精度。相比之下，对于稀疏且含噪声的二维非局部问题，硬约束的 MC-KAN 始终比软约束的 Cheb-KAN 获得更低的核重构误差。我们的结果表明，在从稀疏且含噪声的观测中进行多维核发现时，通过构造强制执行物理驱动的形状约束比软惩罚具有更强的鲁棒性。

## Abstract
Discovering the memory or nonlocal kernel governing an integro-differential equation (IDE) from sparse and noisy observations is an ill-posed inverse problem. Existing identification methods often rely on problem-specific analytical derivations, specialized observation requirements, or restrictive assumptions about the kernel, limiting their applicability across different classes of IDEs. In this work, we propose a differentiable-solver-based framework for discovering memory and nonlocal kernels directly from spatiotemporal observations. Within the solver, the unknown kernel is represented using a constrained Kolmogorov--Arnold Network (KAN) parameterization, with the physical constraints imposed through two different approaches: a Bernstein-polynomial-based Monotone--Convex KAN (MC-KAN), whose coefficient constraints enforce positivity, monotonic decrease, and convexity by construction, and a Chebyshev-based KAN (Cheb-KAN), in which the same properties are encouraged through soft penalty terms. After training, symbolic regression is applied to the learned kernels to obtain interpretable closed-form representations. We evaluate both methods on benchmarks spanning a one-dimensional Volterra equation, a one-dimensional viscoelastic wave partial integro-differential equation, and a two-dimensional nonlocal reaction-diffusion equation with an anisotropic coupled kernel. For the 1D problems, both methods recover the correct kernel functional form and achieve comparable solution-reconstruction accuracy. In contrast, for the sparse and noisy 2D nonlocal problem, the hard-constrained MC-KAN consistently achieves lower kernel reconstruction errors than the soft-constrained Cheb-KAN. Our results demonstrate that enforcing physically motivated shape constraints by construction provides greater robustness than soft penalties for multidimensional kernel discovery from sparse and noisy observations.

---

## 论文详细总结（自动生成）

这是一份关于论文《利用受约束的 Kolmogorov--Arnold 网络在积分微分方程中进行记忆核与非局部核的神经发现》的结构化分析报告：

### 1. 核心问题与整体含义（研究动机和背景）
在物理、生物和工程领域，许多系统具有“记忆效应”或“非局部相互作用”，通常由**积分微分方程 (IDE)** 描述。然而，从观测数据中反推这些方程中的**记忆核（Memory Kernel）**或**非局部核（Nonlocal Kernel）**是一个极具挑战性的**病态逆问题**。
*   **研究动机**：现有的核函数识别方法往往依赖于特定的解析假设、高质量的密集数据，或者对核函数的函数形式有严格限制。
*   **核心目标**：开发一种能够从**稀疏且有噪声**的观测数据中，自动发现并提取具有物理一致性（如正性、单调性、凸性）的核函数解析表达式的通用框架。

### 2. 论文提出的方法论
该研究提出了一种结合可微求解器与受约束网络架构的框架，主要包含以下核心组件：
*   **核心思想**：将未知核函数参数化为 **Kolmogorov-Arnold 网络 (KAN)**，并将其嵌入到可微的 IDE 求解器中。通过最小化观测数据与求解器预测值之间的残差来训练网络。
*   **关键技术细节**：
    *   **MC-KAN (硬约束)**：基于 Bernstein 多项式构造。通过对网络系数施加特定的不等式约束，从结构上强制核函数满足**正性、单调递减和凸性**。
    *   **Cheb-KAN (软约束)**：基于 Chebyshev 多项式构造。通过在损失函数中添加惩罚项（Penalty Terms）来鼓励核函数满足上述物理性质。
    *   **可微求解器**：利用自动微分（AD）计算损失函数对网络参数的梯度，允许端到端的训练。
    *   **符号回归 (SR)**：在网络训练完成后，利用符号回归技术（如 PySR）将学习到的数值核函数转化为简洁、可解释的闭式数学表达式。

### 3. 实验设计
研究者在三个具有代表性的基准场景上验证了方法的有效性：
1.  **1D Volterra 积分方程**：基础的记忆效应模型。
2.  **1D 粘弹性波偏积分微分方程 (PIDE)**：模拟具有时间记忆的波动传播。
3.  **2D 非局部反应扩散方程**：具有各向异性耦合核的复杂空间非局部相互作用。
*   **对比基准**：主要对比了**硬约束 (MC-KAN)** 与 **软约束 (Cheb-KAN)** 在不同噪声水平（0% 到 5%）和不同数据稀疏度下的表现。

### 4. 资源与算力
*   **算力说明**：论文中**未明确说明**具体的 GPU 型号、数量或具体的训练时长。
*   **实现细节**：提到使用了 Python 环境下的深度学习框架和符号回归库（PySR），但缺乏硬件基准数据。

### 5. 实验数量与充分性
*   **实验规模**：研究涵盖了从 1D 到 2D、从常微分到偏微分的多种 IDE 类型。
*   **充分性评价**：
    *   实验设计较为全面，考虑了**噪声鲁棒性分析**（不同噪声百分比）和**数据稀疏性影响**。
    *   通过对比硬约束和软约束，清晰地展示了在极端条件下（高噪声、高维）硬约束的必要性。
    *   **公平性**：两种 KAN 变体在相同的求解器框架和优化条件下进行测试，对比具有客观性。

### 6. 论文的主要结论与发现
*   **硬约束的优越性**：在处理 2D 复杂问题和高噪声数据时，**MC-KAN（硬约束）** 的核重构误差显著低于 Cheb-KAN。这表明通过网络结构强制执行物理规律比通过损失函数引导更具鲁棒性。
*   **KAN 的有效性**：KAN 架构在表示核函数方面表现出极高的精度，且其基函数特性使得与符号回归的结合非常顺畅。
*   **解析恢复**：该框架能够成功从噪声数据中恢复出如指数衰减、分数阶幂律等形式的精确核函数表达式。

### 7. 优点（亮点）
*   **物理一致性**：通过 Bernstein 多项式巧妙地将物理先验（单调性、凸性）转化为硬性的系数约束，避免了神经网络产生非物理的震荡。
*   **端到端发现**：实现了从原始时空观测数据到闭式数学公式的全自动化流程。
*   **可解释性**：利用 KAN 和符号回归的结合，克服了传统深度学习“黑箱”模型的缺点。

### 8. 不足与局限
*   **核函数形状限制**：MC-KAN 目前主要针对单调且凸的核函数。如果实际物理系统的核函数具有震荡性（如某些量子系统或特定波动核），该方法可能不适用。
*   **计算开销**：可微求解器在处理大规模 2D/3D 动态系统时，内存消耗和计算时间可能会随网格加密呈指数增长。
*   **超参数敏感性**：虽然硬约束减少了惩罚项权重的调优，但符号回归阶段仍需要人工干预来选择最合适的函数复杂度。

（完）
