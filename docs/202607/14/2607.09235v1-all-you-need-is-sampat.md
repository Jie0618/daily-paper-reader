---
title: All you need is SAMPAT
title_zh: 你只需要 SAMPAT
authors: "Jayadeva, Madhur Aswani"
date: 2026-07-10
pdf: "https://arxiv.org/pdf/2607.09235v1"
tags: ["query:sr-alg"]
score: 9.0
evidence: 近似值表示为封闭且紧凑的代数表达式
tldr: SAMPAT 是一种三层神经架构，旨在解决深度学习模型缺乏可解释性的问题。它通过多元多项式和解析变换，能够以闭式代数表达式的形式逼近任何平滑函数。该模型在合成和基准数据集上表现出竞争力，且能通过调整连接方式表示多项式、高斯混合等多种模型族。SAMPAT 不仅能优化参数，还能在学习过程中优化模型类别的选择，为科学数据分析提供了高度可解释的工具。
selection_source: fresh_fetch
motivation: 深度神经网络虽然预测能力强，但因缺乏可解释性而难以让科学家从实验数据中获取深入的物理洞察。
method: 提出 SAMPAT 架构，利用多元多项式和解析变换构建三层网络，生成可微且具有闭式代数表达式的函数逼近器。
result: 在多个数据集上的实验表明，SAMPAT 以更简单的表示形式实现了与现有技术相当的性能，并能有效建模非线性系统。
conclusion: SAMPAT 证明了可以通过紧凑的解析表达式实现高性能的函数逼近，为 AI/ML 模型的家族选择和参数优化提供了统一框架。
---

## 摘要
当前人工智能/机器学习（AI/ML）的最先进技术依赖于深度神经架构，但这些架构通常缺乏可解释性。在分析实验数据时，可解释性对于获取洞察至关重要，因为对于科学家而言，仅有定量预测可能并不足够。我们提出了一种三层神经架构 SAMPAT（通过多元多项式和解析变换实现的平滑逼近），它可以证明能够学习一个连续且处处可微的函数，并能以任意精度逼近任何平滑函数。SAMPAT 的逼近式可以表示为封闭且紧凑的代数解析表达式，从而提供了完全的可解释性。在合成数据集和基准数据集上的实验表明，SAMPAT 以更简单的表示实现了具有竞争力的性能。对于许多任务，两层 SAMPAT 就足够了。通过对神经元之间的连接施加限制，SAMPAT 可用于提供一系列逼近式，包括常规多项式和三角多项式、有理表达式、高斯函数、高斯混合模型以及它们的任意组合；在没有限制的情况下，它会学习一个合适的结构。SAMPAT 可用于多项式因式分解和非线性系统建模。通过添加跳跃连接，4 到 6 层的 SAMPAT 足以表示 AI/ML 中广泛使用的实质性方法，从而允许将模型家族的选择（而不仅仅是其参数）也作为学习过程的一部分进行优化。

## Abstract
The current state of the art in AI/ML rests on deep neural architectures, which, in general, suffer from a lack of interpretability. Interpretability is crucial to gleaning insights while analyzing experimental data, where quantitative predictions may not be adequate for a scientist. We present a three layer neural architecture, SAMPAT (Smooth Approximation via Multivariate Polynomials and Analytic Transformations), that can provably learn a continuous, everywhere differentiable function, that can approximate any smooth function arbitrarily closely. SAMPAT's approximant can be expressed as a closed and compact algebraic, analytic expression, providing complete interpretability. Experiments on synthetic and benchmark datasets indicate that SAMPAT yields competitive performance with simpler representations. For many tasks, a two layer SAMPAT suffices. By imposing restrictions on the connectivity between neurons, SAMPAT may be used to provide a range of approximants, including regular and trigonometric polynomials, rational expressions, Gaussians, mixtures of Gaussians, as well as arbitrary combinations of the same; without restrictions, it learns a suitable structure. SAMPAT may be used to factorize polynomials and model nonlinear systems. With the addition of skip connections, a 4 to 6 layer SAMPAT is adequate to represent a substantive range of methods widely used in AI/ML, allowing the choice of a model's family, not just its parameters, to also be optimized as part of the learning process.

---

## 论文详细总结（自动生成）

这是一份关于论文《All you need is SAMPAT》的结构化深度分析报告：

### 1. 论文的核心问题与整体含义
*   **研究动机**：当前的深度学习模型（如深度神经网络）虽然预测精度高，但普遍存在“黑盒”问题，缺乏**可解释性**。对于科学家而言，仅有定量预测是不够的，他们需要从实验数据中提取代数表达式以获得物理洞察。
*   **核心问题**：如何构建一种既能像神经网络一样具备强大拟合能力（通用逼近性），又能生成简洁、闭式（Closed-form）代数表达式的架构？
*   **整体含义**：论文提出了一种名为 **SAMPAT** 的三层神经架构，通过对数和指数变换将神经网络转化为多元多项式的组合，从而实现模型的可解释化。

### 2. 方法论：核心思想与技术细节
SAMPAT 的核心思想是利用**对数-求和-指数**的结构来模拟多项式的乘积与求和。
*   **架构组成**：
    1.  **第一层（对数层）**：输入经线性加权后通过 $\log$ 激活函数。输出为 $y_i = \log(\sum u_{ij} x_j)$。
    2.  **第二层（指数层）**：对第一层输出进行加权求和，再通过 $\exp$ 激活函数。根据对数运算法则，这一层实际上实现了输入线性组合的幂乘积：$z_k = \prod (\sum u_{ij} x_j)^{v_{ki}}$。
    3.  **第三层（线性层）**：对第二层的输出进行线性加权求和，得到最终的多元多项式形式：$t_l = \sum w_{lk} z_k$。
*   **关键特性**：
    *   **通用逼近性**：基于 Stone-Weierstrass 定理，三层 SAMPAT 可以以任意精度逼近任何平滑函数。
    *   **解析性**：模型输出是处处可微的代数表达式，可以直接提取导数和偏导数。
    *   **扩展性**：引入**复数权重**可处理频率响应和三角函数；引入**跳跃连接（Skip Connections）**可表示高斯混合模型或有理函数。

### 3. 实验设计
论文在多个维度上验证了 SAMPAT 的有效性：
*   **函数逼近与符号回归**：逼近 $\sin(x)$、$\text{ReLU}(x)$ 以及复杂的多元非线性函数。
*   **多项式因式分解**：通过训练网络自动寻找高次多项式的根（利用复数权重）。
*   **系统建模（ODE 恢复）**：对 RLC 电路的时间序列数据进行建模，并成功恢复其控制微分方程。
*   **分类基准测试**：使用 UCI 仓库的多个数据集（如 Iris、Wine 等），对比 **ELM（极限学习机）**、**SVM（RBF 核）**和**优化后的多层感知机（NN Optim）**。
*   **图像分类（SAMPAT+）**：在 **CIFAR10** 和 **CatsVsDogs** 数据集上，将 SAMPAT 单元与卷积层结合，对比 **DenseNet** 和 **MobileNet V2**。

### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的硬件配置（如 GPU 型号、数量）或具体的训练时长。
*   **参数量对比**：文中强调了 SAMPAT 的参数效率。例如，在多元函数逼近中，SAMPAT 使用 161-171 个参数达到了比拥有 1100+ 参数的传统神经网络更低的均方误差（MSE）。在图像任务中，SAMPAT+ 的参数量（约 8.9 万）远少于 DenseNet（80 万）或 MobileNet V2（350 万）。

### 5. 实验数量与充分性
*   **实验组数**：论文涵盖了从基础数学运算（因式分解、三角函数）到工程应用（电路建模、频率响应），再到标准机器学习任务（分类、图像识别）的约 10 组不同类型的实验。
*   **充分性评价**：
    *   **广度足够**：证明了该架构在回归、分类和符号发现方面的通用性。
    *   **对比客观**：在分类任务中使用了标准基准数据集，并与多种主流算法进行了对比。
    *   **深度略显不足**：在图像分类（CIFAR10 等）上的实验属于初步阶段，虽然参数量极低且精度尚可，但尚未在大规模数据集（如 ImageNet）上验证其扩展性。

### 6. 主要结论与发现
*   **三层架构的威力**：三层 SAMPAT 足以逼近任何平滑函数，且其形式是完全可解释的代数表达式。
*   **参数高效性**：相比传统深度神经网络，SAMPAT 能以更少的参数（通常少 7-8 倍甚至更多）达到同等或更高的精度。
*   **模型家族优化**：SAMPAT 不仅优化参数，还能通过学习过程自动选择最适合数据的模型族（如多项式、有理函数或高斯分布）。
*   **系统辨识能力**：能够从噪声数据中准确提取物理系统的控制方程（如 RLC 电路的微分方程）。

### 7. 优点
*   **极致的可解释性**：直接输出公式，解决了深度学习的“黑盒”痛点。
*   **数学严谨性**：基于成熟的数学定理（Stone-Weierstrass），保证了逼近能力。
*   **灵活性**：通过简单的连接限制或复数扩展，可以模拟几乎所有常见的数学模型。
*   **硬件友好**：生成的紧凑表达式非常适合在资源受限的硬件（如嵌入式芯片）上通过查找表或简单运算实现。

### 8. 不足与局限
*   **训练稳定性**：对数函数 $\log(x)$ 在参数导致参数为负或零时可能存在数值不稳定问题（虽然文中提到了复数域的解决方法，但实际实现复杂度会增加）。
*   **深度学习集成尚浅**：在处理高维感知数据（如复杂图像、视频）时，仍需依赖传统的卷积层，SAMPAT 目前更多是作为一种高效的非线性激活/组合单元。
*   **超参数敏感性**：虽然模型结构简单，但如何确定第一层和第二层的神经元数量（即多项式的阶数和项数）可能需要较多的先验知识或搜索成本。

（完）
