---
title: "GAE: Graph-Augmented Evolution for Scientific Discovery via Reinforcement Optimization"
title_zh: GAE：通过强化优化实现科学发现的图增强演化
authors: "Xuanzhou Chen, Taoli Cheng"
date: 2026-07-11
pdf: "https://arxiv.org/pdf/2607.10127v1"
tags: ["query:sr-alg"]
score: 9.0
evidence: 用于科学发现中演化程序搜索的强化学习优化元控制器
tldr: 针对大模型驱动的演化程序搜索在科学发现中面临的父代选择盲目、奖励稀疏和变异算子静态等瓶颈，本文提出GAE框架。该框架通过图神经网络解析程序结构，利用强化学习优化的元控制器实现定向搜索，并结合在线GRPO算法动态微调大模型的变异策略。在非线性振荡系统的符号回归任务中，GAE成功发现了精确的物理方程，显著提升了搜索效率和泛化性能，为自动化科学探索提供了新路径。
selection_source: fresh_fetch
motivation: 现有的LLM驱动演化搜索受限于结构盲目的父代选择和无法自适应的静态变异算子，难以高效处理复杂的科学发现任务。
method: GAE整合了用于结构感知的图神经网络、用于定向策略选择的强化学习元控制器，以及用于在线微调变异算子的GRPO算法。
result: 在复杂非线性振荡系统的符号回归实验中，GAE高效发现了闭式物理方程，性能达到SOTA并展现出优异的分布外泛化能力。
conclusion: 通过将随机演化搜索转化为受强化学习引导的自改进轨迹，GAE证明了结构感知与动态微调在自动化科学发现中的核心价值。
---

## 摘要
由大语言模型（LLMs）引导的演化程序搜索已成为自动化科学发现的一种强大范式。然而，当前的方法从根本上受到三个瓶颈的制约：结构盲目的父代选择、稀疏的全程序评估奖励，以及在搜索过程中无法自适应的静态变异算子。我们提出了 GAE（图增强演化），这是一个通过紧密耦合的三支柱架构解决这些局限性的框架。首先，关系图神经网络（GNN）将程序解析为有类型的计算图，生成结构感知的嵌入。其次，一个经过强化学习（RL）优化的元控制器利用这些嵌入，将盲目的演化采样替换为定向策略，根据奖励历史动态选择最优父代和变异方向。第三，在线 GRPO 微调循环在测试时利用分组归一化的评估奖励持续更新 LLM 变异算子，使模型的生成分布直接与高适应度的结构编辑对齐。我们在一个具有挑战性的科学发现任务上评估了 GAE：复杂非线性振子系统的符号回归。通过将随机搜索转化为定向的、自我改进的轨迹，GAE 高效地发现了闭式物理方程，持续达到或超越了静态 LLM 驱动的基准模型，并实现了最先进的分布外性能。

## Abstract
Evolutionary program search guided by Large Language Models (LLMs) has emerged as a powerful paradigm for automated scientific discovery. However, current approaches are fundamentally constrained by three bottlenecks: structurally blind parent selection, sparse whole-program evaluation rewards, and static mutation operators that fail to adapt during search. We present GAE (Graph-Augmented Evolution), a framework that resolves these limitations through a tightly coupled, three-pillar architecture. First, a relational graph neural network (GNN) parses programs into typed computation graphs, producing structure-aware embeddings. Second, an RL-optimized meta-controller leverages these embeddings to replace blind evolutionary sampling with a directed policy, dynamically selecting optimal parents and mutation directions based on reward history. Third, an online GRPO fine-tuning loop continuously updates the LLM mutation operator at test-time using group-normalized evaluation rewards, directly aligning the model's generation distribution with high-fitness structural edits. We evaluate GAE on a challenging scientific discovery task: symbolic regression for complex nonlinear oscillator systems. By transforming stochastic search into a directed, self-improving trajectory, GAE efficiently discovers closed-form physical equations, consistently matching or outperforming static LLM-driven baselines and achieving state-of-the-art out-of-distribution performance.

---

## 论文详细总结（自动生成）

这是一份关于论文《GAE: Graph-Augmented Evolution for Scientific Discovery via Reinforcement Optimization》的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
论文聚焦于利用大语言模型（LLM）驱动的演化程序搜索来实现自动化科学发现（如符号回归）。尽管现有方法（如 FunSearch, AlphaEvolve）取得了一定成功，但仍面临三个核心瓶颈：
*   **奖励稀疏性（Reward Sparsity）：** 只有在程序运行结束时才能获得单一的标量评分，忽略了程序内部结构的中间贡献。
*   **盲目的父代选择（Uninformed Parent Selection）：** 现有的演化算法通常基于适应度随机采样父代，忽略了程序代码背后的结构相似性和局部性。
*   **静态变异算子（Static Mutation Operator）：** LLM 作为变异算子在搜索过程中是固定的，无法根据已积累的搜索经验自适应地调整变异策略。

**GAE（图增强演化）** 的提出旨在将随机的演化搜索转变为一个定向的、自我改进的轨迹。

### 2. 方法论：核心思想与关键技术
GAE 采用了一个三支柱的紧密耦合架构：
*   **关系图神经网络（Relational GNN）编码器：**
    *   将程序的抽象语法树（AST）解析为有类型的计算图。
    *   利用关系卷积层处理不同类型的边（如数据流、操作符关系），生成结构感知的嵌入向量 $z$。
    *   该编码器在演化过程中通过 MSE 损失函数在线微调，以预测程序的适应度，从而捕捉不断演化的程序分布。
*   **强化学习优化的元控制器（Discrete SAC）：**
    *   使用离散软行为者-评论者（Discrete SAC）算法。
    *   **状态：** 父代程序的 GNN 嵌入。**动作：** 选择特定的变异类型（如替换操作符、插入常数、子树重写等）。
    *   **奖励函数：** 结合了任务得分（NMSE）、嵌入空间的新颖性奖励以及程序复杂度的惩罚项。
*   **在线 GRPO 微调循环：**
    *   利用组相对策略优化（GRPO）在测试时实时更新 LLM 变异算子。
    *   每一代生成一组（$G$ 个）候选程序，计算它们相对于父代的改进程度，并进行分组归一化处理，以此作为优势信号来微调 LLM，使其生成分布向高适应度的结构编辑对齐。

### 3. 实验设计
*   **任务场景：** 复杂非线性振子系统（Nonlinear Oscillator）的符号回归。目标是根据位置、速度和时间预测加速度 $\ddot{x}$。
*   **数据集：** 使用 LLM-SRBench 中的 `phys_osc` 领域数据集，包含训练集、分布内（ID）测试集和分布外（OOD）测试集。
*   **对比基准（Benchmarks）：**
    *   非 LLM 方法：uDSR。
    *   LLM 驱动方法：LLM-SR, OpenEvolve, CodeEvolve, ShinkaEvolve, PACEvolve。
*   **实验设置：** 分为高预算（1000 次迭代）和低预算（150 次迭代）两种环境，以验证算法的效率。

### 4. 资源与算力
*   **模型：** 使用了 **Qwen3.5-35B-A3B** 作为 LLM 骨干网络。
*   **算力细节：** 论文**未明确说明**具体的 GPU 型号和数量。但提到在每一代中并行生成 8 个候选程序，并涉及在线微调（GRPO 和 GNN），这暗示了实验环境需要支持中大规模并行计算和实时训练的算力支撑。

### 5. 实验数量与充分性
*   **实验规模：** 进行了多轮独立运行（如 10 轮或 3 轮），报告了平均值和最佳值。
*   **消融与对比：** 对比了不同 LLM 骨干（Qwen vs Gemini）下的表现，并详细展示了发现的物理方程及其物理含义。
*   **充分性评价：** 实验设计较为充分，涵盖了从算法效率（迭代次数）到泛化能力（OOD 测试）的全面评估。通过与 SOTA 方法（如 PACEvolve）在相同预算下的直接对比，证明了其优越性。

### 6. 主要结论与发现
*   **性能领先：** GAE 在 ID 和 OOD 测试中均取得了最低的 NMSE（归一化均方误差），达到了当前最先进的水平（SOTA）。
*   **高效发现：** 在仅 150 次迭代内，GAE 就能发现极其接近真实物理规律的闭式方程，而传统方法往往需要更多迭代。
*   **结构感知的重要性：** 实验证明，通过 GNN 捕捉代码结构并引导 RL 选择变异方向，能显著提升搜索的定向性和成功率。

### 7. 优点
*   **闭环自适应：** 首次在演化搜索中同时实现了“选哪个变异”和“怎么变异”的双重在线学习。
*   **结构感知：** 引入关系 GNN 解决了代码作为纯文本处理时丢失结构信息的问题。
*   **泛化能力强：** 在 OOD 任务上的表现优异，说明发现的方程具有真实的物理外推能力，而非简单的过拟合。

### 8. 不足与局限
*   **领域依赖：** 依赖于 Python AST 解析，若要迁移到其他编程语言或非程序化领域，需要重新设计节点类型和关系。
*   **计算开销：** 引入 GNN 微调、SAC 优化和 GRPO 实时训练，虽然提升了样本效率，但增加了单次迭代的计算延迟和系统复杂性。
*   **动作空间限制：** 离散 SAC 的动作空间（变异类型）是预定义的，可能限制了某些极端新颖结构的产生。

（完）
