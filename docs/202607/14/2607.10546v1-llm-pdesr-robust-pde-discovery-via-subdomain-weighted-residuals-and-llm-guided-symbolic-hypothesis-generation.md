---
title: "LLM-PDESR: Robust PDE Discovery via Subdomain Weighted Residuals and LLM-Guided Symbolic Hypothesis Generation"
title_zh: LLM-PDESR：通过子域加权残差与 LLM 引导的符号假设生成实现鲁棒的 PDE 发现
authors: "Jinyang Du, Hao Ma, Xiaohu Shi, Bo Yang, Yanchun Liang, Heow Pueh Lee, Chunguo Wu"
date: 2026-07-12
pdf: "https://arxiv.org/pdf/2607.10546v1"
tags: ["query:sr-alg"]
score: 10.0
evidence: 利用大语言模型引导的符号假设生成从噪声数据中发现偏微分方程
tldr: 本研究提出LLM-PDESR框架，旨在从噪声观测数据中发现偏微分方程（PDE）。针对传统符号回归在搜索空间和噪声敏感性方面的局限，该框架结合了大语言模型（LLM）的结构化假设生成能力与子域加权残差评估环境。通过五次样条插值和帕累托驱动的反馈循环，LLM-PDESR能有效抑制噪声干扰并平衡方程的准确性与简洁性，在多项基准测试和真实气象数据中展现出卓越的鲁棒性和发现能力。
selection_source: fresh_fetch
motivation: 传统符号回归方法在处理高噪声数据时，因缺乏领域先验知识且易受有限差分引起的噪声放大影响，难以准确识别PDE结构。
method: 结合LLM进行符号假设生成，并利用C^4连续五次样条和子域加权残差构建鲁棒的评估环境与帕累托反馈机制。
result: 在23个经典PDE、5个新结构方程及ERA5真实气象数据上的实验表明，该方法在结构恢复和抗噪性方面显著优于现有技术。
conclusion: LLM-PDESR通过整合LLM的推理能力与严谨的数学评估，为复杂物理系统的可解释建模提供了一种高效且鲁棒的新途径。
---

## 摘要
从噪声观测数据中发现控制偏微分方程（PDEs）是科学机器学习中的一项基本挑战。传统的符号回归（SR）方法通常难以在庞大的组合搜索空间中识别出准确的方程，这在很大程度上归因于它们无法整合必要的领域特定先验知识。此外，对逐点评估和离散有限差分的依赖会固有地放大高频噪声，从而产生具有误导性的适应度景观，使优化过程偏离轨道。为了解决这些瓶颈，我们提出了 LLM-PDESR，这是一个将大语言模型（LLMs）的结构化假设生成与数学严谨的评估环境相结合的框架。通过采用 C^4 连续的五次样条进行鲁棒微分，并利用子域加权残差作为天然低通滤波器，我们的方法有效地缓解了困扰现有方法的适应度景观失真问题。随后，帕累托驱动的反馈循环使 LLM 能够迭代地优化候选方程，在预测准确性与结构简洁性之间取得平衡。我们在 23 个经典 PDE 和 5 个结构新颖的方程（包括一个多元系统）上评估了 LLM-PDESR，这些方程专门设计用于排除数据集记忆并测试真实的发现能力。为了展示实际应用价值，该框架直接从带噪声的 ERA5 再分析数据中，成功为一个可解释的一维动力学代理模型（1D-CACE）提取了连贯的结构骨架。广泛的实验和分布外测试证实，LLM-PDESR 在结构恢复、噪声鲁棒性以及避免伪复杂性和方程膨胀方面显著优于现有最先进的方法。

## Abstract
Discovering governing partial differential equations (PDEs) from noisy observational data is a fundamental challenge in scientific machine learning. Traditional symbolic regression (SR) methods often struggle to identify accurate equations within vast combinatorial search spaces, largely due to their inability to incorporate essential domain-specific prior knowledge. Furthermore, reliance on pointwise evaluations and discrete finite differences inherently amplifies high-frequency noise, creating deceptive fitness landscapes that derail the optimization process. To resolve these bottlenecks, we propose LLM-PDESR, a framework that integrates the structural hypothesis generation of Large Language Models (LLMs) with a mathematically rigorous evaluation environment. By employing C^4-continuous quintic splines for robust differentiation and subdomain weighted residuals as natural low-pass filters, our approach effectively mitigates the fitness landscape distortion that plagues existing methods. A Pareto-driven feedback loop then enables the LLM to iteratively refine candidate equations, balancing predictive accuracy with structural parsimony. We evaluate LLM-PDESR on 23 canonical PDEs and five structurally novel equations (including a multivariate system) specifically designed to preclude dataset memorization and test true discovery capabilities. Demonstrating real-world applicability, the framework successfully extracts a consistent structural skeleton for an interpretable 1D dynamical surrogate (1D-CACE) directly from noisy ERA5 reanalysis data. Extensive experiments and out-of-distribution testing confirm that LLM-PDESR significantly outperforms state-of-the-art methodologies in structural recovery, noise resilience, and the avoidance of spurious complexity and equation bloat.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **LLM-PDESR** 的新型框架，旨在从含有噪声的观测数据中自动发现控制物理系统的偏微分方程（PDE）。以下是对该论文的深度结构化总结：

### 1. 核心问题与研究背景
*   **核心问题**：如何从高噪声、有限的观测数据中准确识别出底层的偏微分方程结构。
*   **研究背景**：
    *   **传统符号回归（SR）的局限**：搜索空间巨大，且缺乏物理先验知识，容易产生“方程膨胀”（模型过于复杂但无物理意义）。
    *   **噪声敏感性**：传统方法依赖有限差分计算导数，这会放大高频噪声，导致适应度景观（Fitness Landscape）剧烈波动，使优化算法陷入局部最优。
    *   **数据记忆问题**：现有基于深度学习的方法可能只是“记住”了常见方程，而非真正理解物理规律。

### 2. 方法论：LLM-PDESR 框架
该方法结合了大语言模型（LLM）的启发式搜索能力与严谨的数学评估环境，核心流程如下：
*   **C⁴ 连续五次样条插值（Quintic Splines）**：首先对原始噪声数据进行平滑处理。使用五次样条不仅能提供连续的导数估计，还能有效抑制高频噪声。
*   **子域加权残差（Subdomain Weighted Residuals, SWR）**：这是该方法的数学核心。不同于传统的逐点（Pointwise）误差评估，SWR 在局部子域上进行积分。这种积分操作充当了天然的“低通滤波器”，显著平滑了适应度景观，增强了对噪声的鲁棒性。
*   **LLM 引导的符号假设生成**：利用 LLM（如 GPT-4o）作为进化算子。LLM 根据物理背景、变量信息以及前几轮迭代的反馈，生成候选方程的符号结构。
*   **帕累托驱动的反馈循环（Pareto-driven Feedback）**：
    1.  **评估**：计算候选方程的残差（准确性）和项数（简洁性）。
    2.  **筛选**：维护一个帕累托前沿（Pareto Front），平衡精度与复杂度。
    3.  **反馈**：将帕累托前沿的方程及其表现反馈给 LLM，引导其在下一轮生成更合理的假设。

### 3. 实验设计
*   **数据集**：
    *   **23 个经典 PDE**：涵盖流体力学、量子力学、电磁学等（如 Burgers, KdV, Schrodinger, Navier-Stokes 等）。
    *   **5 个新结构方程**：专门设计了 LLM 训练数据中未出现过的方程结构，以验证其真正的发现能力而非记忆。
    *   **真实世界数据**：使用 ERA5 全球大气再分析数据，提取一维动力学代理模型（1D-CACE）的结构。
*   **基准方法（Benchmarks）**：对比了 DeepMoD、PDE-Net 2.0、PySR（经典符号回归）以及其他最先进的 PDE 发现算法。
*   **噪声水平**：测试了从 0% 到 10% 不等的加性高斯噪声。

### 4. 资源与算力
*   **算力说明**：论文中**未明确说明**具体的 GPU 型号或训练总时长。
*   **实现细节**：由于核心逻辑涉及 LLM API 调用（如 GPT-4o）和数学计算（样条插值与积分），其主要开销在于 LLM 的 Token 消耗和迭代次数，而非传统深度学习的大规模显存占用。

### 5. 实验数量与充分性
*   **实验规模**：共进行了 28 个合成案例和 1 个真实案例的测试。
*   **充分性**：
    *   **消融实验**：验证了 SWR 相比于逐点评估的优越性，以及五次样条对噪声抑制的贡献。
    *   **分布外（OOD）测试**：通过新结构方程验证了模型的泛化能力。
    *   **鲁棒性测试**：在不同噪声强度下进行了多次重复实验，结果具有统计学意义。
*   **客观性**：通过与多种不同类型的基线方法对比，证明了该框架在结构恢复率和抗噪性上的全面领先。

### 6. 主要结论与发现
*   **卓越的鲁棒性**：在 10% 的高噪声水平下，LLM-PDESR 仍能保持极高的结构恢复准确率，而传统方法往往失效。
*   **有效抑制膨胀**：通过帕累托反馈，框架能自动剔除无意义的复杂项，生成的方程简洁且具有物理可解释性。
*   **超越记忆**：实验证明 LLM 能够通过逻辑推理发现其训练集中不存在的新物理结构。
*   **现实应用潜力**：成功从复杂的 ERA5 气象数据中提取出连贯的动力学骨架，证明了其处理真实噪声数据的能力。

### 7. 优点与亮点
*   **跨学科融合**：巧妙地将 LLM 的语义推理与经典的加权残差法（有限元分析中的概念）结合。
*   **适应度景观平滑**：引入 SWR 解决了符号回归中最核心的“噪声放大”痛点。
*   **零样本/少样本发现**：不需要针对特定方程进行大规模预训练，具有很强的通用性。

### 8. 不足与局限
*   **LLM 依赖性**：性能高度依赖于底层 LLM 的推理能力和 API 的稳定性，且存在 Token 成本。
*   **高维扩展性**：虽然在 1D 和 2D 案例上表现优异，但对于极高维（如 4D 以上）的偏微分方程，样条插值和子域积分的计算复杂度会显著增加。
*   **计算延迟**：迭代反馈过程涉及多次 LLM 调用，相比于纯数值优化方法，实时性可能较弱。

（完）
