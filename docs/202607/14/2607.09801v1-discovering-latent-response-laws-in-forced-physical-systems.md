---
title: Discovering Latent Response Laws in Forced Physical Systems
title_zh: 发现受迫物理系统中的潜在响应律
authors: "Yi Zhu, Su Chen, Xiaojun Li, Xiuli Du"
date: 2026-07-09
pdf: "https://arxiv.org/pdf/2607.09801v1"
tags: ["query:sr-alg"]
score: 9.0
evidence: 发现物理系统中的控制方程和潜在响应规律
tldr: 本研究针对受外部强迫影响的高维物理系统，提出了FLARE框架。该方法利用自编码器从复杂观测中提取紧凑的潜变量，并识别出受输入驱动的稀疏潜空间动力学方程。FLARE实现了状态估计与外部强迫的分离，能够基于历史响应和未来输入进行长期预测。实验证明，该方法在多种物理场景和视觉观测中均能准确恢复动力学规律，为高维强迫系统的可解释建模提供了新途径。
selection_source: fresh_fetch
motivation: 物理系统的控制方程往往隐藏在高维观测中，且受外部强迫影响，难以直接从复杂数据中提取简洁的动力学规律。
method: 提出FLARE框架，通过自编码器学习紧凑的潜变量坐标，并结合稀疏回归识别受外部输入驱动的潜空间动力学方程。
result: 在已知动力系统、大规模强迫响应及视觉观测数据上，FLARE成功恢复了紧凑的强迫动力学，并实现了对未知输入的长期高维响应预测。
conclusion: 该方法为复杂观测下的强迫动力系统提供了可解释的建模与预测路径，扩展了方程发现的应用范围。
---

## 摘要
控制方程提供了物理系统的简洁描述，然而使方程保持简洁的变量往往隐藏在高维测量数据中。对于受迫系统，这一挑战更为严峻，因为其响应同时取决于内在动力学和随时间变化的输入。在此，我们介绍了 FLARE，一种用于响应方程的受迫潜自动编码器（forced latent autoencoder），它能够学习简洁的响应坐标，识别稀疏的、依赖于输入的潜动力学，并将方程演化解码为完整的响应。通过从数据中估计潜维度并将状态估计与外部驱动分离，FLARE 使得预测能够从过去的响应中初始化，并由预设的未来输入驱动。在已知动力系统、应用规模的受迫响应和视觉观测中，FLARE 恢复了简洁的受迫动力学，并在未用于训练的输入下预测了长程高维响应。通过将学习到的坐标转化为动力学接口，FLARE 将方程发现扩展到有效状态隐藏在复杂观测中的系统，为受迫动力系统中高维响应的可解释建模和预测提供了一条途径。

## Abstract
Governing equations provide compact descriptions of physical systems, yet the variables in which they are simple are often hidden in high-dimensional measurements. This challenge is sharper for forced systems, whose responses depend on both intrinsic dynamics and time-dependent inputs. Here we introduce FLARE, a forced latent autoencoder for response equations that learns compact response coordinates, identifies sparse input-dependent latent dynamics and decodes equation rollouts to full responses. By estimating latent dimension from data and separating state estimation from external forcing, FLARE enables forecasts to be initialized from past responses and driven by prescribed future inputs. Across known dynamical systems, application-scale forced responses and visual observations, FLARE recovers compact forced dynamics and predicts long-horizon high-dimensional responses under inputs not used for training. By turning learned coordinates into a dynamical interface, FLARE extends equation discovery to systems whose effective states are hidden within complex observations, providing a route for interpretable modelling and prediction of high-dimensional responses in forced dynamical systems.

---

## 论文详细总结（自动生成）

这是一份关于论文《Discovering Latent Response Laws in Forced Physical Systems》的结构化深入总结：

### 1. 论文的核心问题与整体含义
*   **研究动机**：物理系统的控制方程通常隐藏在高维观测数据中（如传感器阵列或视频），且许多系统受到随时间变化的外部强迫（External Forcing）影响。
*   **核心挑战**：现有的方程发现方法（如 SINDy）多针对自主系统。对于受迫系统，观测到的响应既取决于内在动力学，也取决于外部输入及其与状态的耦合。如何在不知道有效状态变量、甚至不知道潜空间维度的情况下，从高维数据中同时提取紧凑的表示和显式的受迫动力学方程，是该领域的主要难题。
*   **整体含义**：论文提出了 **FLARE**（Forced Latent Autoencoder for Response Equations）框架，旨在将表示学习、方程发现和高维响应预测统一起来，为受迫物理系统提供一种可解释的建模路径。

### 2. 方法论
*   **核心思想**：通过自编码器将高维观测映射到低维潜空间，并在潜空间中利用稀疏回归识别受外部输入驱动的演化方程。
*   **关键技术细节**：
    1.  **潜维度估计**：在预训练阶段，利用 Levina-Bickel 最大似然估计（LB-MLE）、双近邻估计（TwoNN）或主成分分析（PCA）自动确定潜空间的内在维度，而非人为指定。
    2.  **状态与强迫分离**：编码器仅接收响应的历史窗口（Causal Window）来估计当前状态 $z_k$，外部输入 $u_k$ 被排除在编码器之外，仅显式进入潜方程的候选函数库 $\Theta$。
    3.  **稀疏潜方程**：形式为 $\dot{z} = \Theta(z, u)\Xi$，其中 $\Xi$ 是稀疏系数矩阵。这使得模型能够清晰地展示外部输入如何影响系统演化。
    4.  **三阶段训练流程**：
        *   **预训练**：构建初始流形并估计维度。
        *   **正式训练**：联合优化编码器、解码器和稀疏方程系数，损失函数包含重构误差、潜空间演化一致性和稀疏正则项。
        *   **后训练**：固定编码器和方程，微调解码器以优化长程预测的图像/信号质量。
*   **视频扩展**：针对视频数据，将全连接层替换为空间卷积（2D Conv）和因果时间卷积（1D Conv），保持潜方程逻辑不变。

### 3. 实验设计
*   **实验场景**：
    1.  **合成基准**：受迫阻尼摆、受迫 Hopf 振荡器、受迫刚体旋转（均嵌入 64 维非线性观测中）。
    2.  **应用规模系统**：薄板瞬态传热（100 维）、机器人机械臂运动（105 维）、建筑结构地震响应（36 维）。
    3.  **视频观测**：受迫 Van der Pol 振荡器视频、受迫悬臂梁振动视频、受迫圆柱绕流流场视频。
*   **对比方法（Benchmark）**：
    *   线性/库普曼模型：DMDc, Deep Koopman with control。
    *   方程发现类：POD-SINDYc, AE-SINDYc。
    *   非线性状态空间识别：CT-SUBNET。
    *   通用序列预测模型：TiDE, TFT, TCN, GRU。
*   **测试类型**：包含 **Held-out 测试**（与训练集同分布的输入）和 **Unseen-input 测试**（外推测试，输入在频率、组织方式上与训练集完全不同）。

### 4. 资源与算力
*   **硬件环境**：论文明确提到所有实验均在一台配备 **NVIDIA Tesla V100 GPU** 的工作站上完成。
*   **训练时长**：文中未详细列出每个模型的具体训练分钟数或小时数，但描述了其多阶段训练的逻辑。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了从 1 维到 3 维内在维度的合成系统，以及具有复杂物理背景的传热、机器人和结构动力学系统，最后扩展到像素级的视频数据。
*   **充分性与客观性**：
    *   实验设计非常充分，不仅验证了方程系数的恢复（针对合成系统），还重点测试了在**未见过的外部输入**下的长程预测能力（500 步演化）。
    *   使用了 NRMSE 和 SSIM（针对视频）等多种客观指标。
    *   对比了 9 种不同类型的基线模型，涵盖了从经典控制理论到现代深度学习的多种路径，评估较为公平。

### 6. 主要结论与发现
*   **方程恢复**：FLARE 能够从高维非线性观测中准确恢复受迫系统的动力学形式（在仿射变换等价的意义下）。
*   **长程预测**：在应用规模系统中，FLARE 在 500 步的长程预测中表现优于大多数基线模型，尤其是在处理非平稳、非线性强的结构响应时。
*   **泛化能力**：FLARE 在面对训练集中未出现的强迫模式（如改变地震波频率或机械臂指令组合）时，仍能保持物理上一致且准确的预测，显示出极强的外推能力。
*   **视频理解**：证明了即使动力学变量隐藏在像素场中，FLARE 也能提取出符合物理直觉的低维演化规律（如悬臂梁的第一阶振型）。

### 7. 优点
*   **物理可解释性**：通过稀疏回归直接给出潜空间方程，比纯黑盒模型（如 GRU/TFT）更易于分析状态-输入耦合。
*   **解耦设计**：将状态估计（编码器）与强迫响应（方程库）分离，符合物理系统受迫演化的本质。
*   **自动维度确定**：引入内在维度估计，解决了自编码器建模中“潜空间维度设为多少”的痛点。
*   **多模态适用**：一套框架同时兼容向量传感器数据和视频流数据。

### 8. 不足与局限
*   **候选库依赖**：方程发现的效果高度依赖于预设的候选函数库 $\Theta$。如果真实的非线性项不在库中，模型效果会受限。
*   **坐标非唯一性**：学习到的潜坐标与物理坐标之间可能存在仿射变换，需要额外的数据对齐才能直接对应物理参数。
*   **计算复杂度**：多阶段训练和长程演化损失函数的计算开销可能高于简单的端到端预测模型。
*   **噪声敏感性**：虽然稀疏回归具有一定鲁棒性，但在极端噪声或极度稀疏的观测下，潜维度的准确估计和方程的稳定性仍面临挑战。

（完）
