---
title: "Differentiate the Evaluator, Not the Program: An Efficient Runtime Representation for Neuro-Symbolic Learning"
title_zh: 微分求值器而非程序：一种用于神经符号学习的高效运行时表示
authors: Lucas Sheneman
date: 2026-07-03
pdf: "https://arxiv.org/pdf/2607.03574v1"
tags: ["query:sr-alg"]
score: 9.0
evidence: 神经符号程序搜索的高效参数校准
tldr: 针对神经符号学习中程序参数校准效率低下的问题，本文提出了原生微分虚拟机（NDVM）。在科学发现等任务中，搜索算法会生成大量候选程序，传统的编译图方法缺乏灵活性，而解释器方法则开销巨大。NDVM通过将符号结构与可微数值状态分离，在运行时记录执行轨迹的梯度，实现了高效的批量化参数优化。实验表明，NDVM在保持程序作为数据灵活性的同时，显著提升了搜索效率，比现有方法快约24倍。
selection_source: fresh_fetch
motivation: 现有的神经符号学习系统在处理大量候选程序的参数校准时，面临编译开销大或解释器运行慢的性能瓶颈。
method: 提出NDVM运行时表示，将符号逻辑与数值负载分离，通过在执行轨迹上记录反向模式梯度来实现高效的批量微分。
result: NDVM实现了约60倍的批量摊销效率和近乎线性的多核扩展，在LLM生成的程序搜索任务中提速达24倍。
conclusion: 运行时微分技术为科学发现工作流提供了一个高效且实用的系统基础，平衡了程序搜索的灵活性与数值优化的性能。
---

## 摘要
AI 系统越来越多地提出可执行的科学模型，其价值取决于其符号结构和拟合的连续参数。这使得参数校准成为“程序-参数”协同搜索的瓶颈：外层循环可以生成数千个候选程序，但每个程序在评估前都需要进行内层的基于梯度的优化。将每个候选程序分阶段构建为独立的微分图可以使单个模型运行迅速，但牺牲了保持搜索灵活性的“程序即数据”属性；基于解释器的方法将程序保留为运行时数据，但解释器开销占据了数值计算的主导地位。我们提出了原生微分虚拟机（NDVM），这是一种无需将每个候选程序编译为独立图即可对可执行程序进行微分的运行时表示。NDVM 将符号结构与可微分的数值状态分离：标签、符号、环境和控制流保持为原生运行时数据，而数值负载存储在稠密的批处理缓冲区中，并沿实现的执行轨迹记录精确的反向模式梯度，从而使一次求值器遍历的开销被大量的参数矢量群体所摊销。一个真实的、可微分的自托管 Scheme 解释器的锁定成本模型驱动了这一设计。我们将 NDVM 实现为一个原生运行时，其前向计算和梯度与参考后端等效，具有约 60 倍的单通道批处理摊销、近线性的多核扩展性以及两个独立的后端。在对大语言模型（LLM）提出的程序进行固定预算的协同搜索中，NDVM 在实际运行时间上达到高质量解的速度提高了约 24 倍，这表明运行时微分是科学发现工作流中实用的系统基础。

## Abstract
AI systems increasingly propose executable scientific models whose value depends on both their symbolic structure and their fitted continuous parameters. This makes parameter calibration the bottleneck of program-and-parameter co-search: an outer loop can generate thousands of candidate programs, but each needs an inner gradient-based optimization before it can be assessed. Staging each candidate into its own differentiable graph makes individual models fast but sacrifices the program-as-data property that keeps search fluid; interpreter-based approaches preserve programs as runtime data but pay interpreter overhead that dominates the numerical work.   We present the Native Differentiable Virtual Machine (NDVM), a runtime representation that differentiates executable programs without compiling each candidate into a separate graph. NDVM separates symbolic structure from differentiable numeric state: tags, symbols, environments, and control remain native runtime data, while numeric payloads live in dense batched buffers with exact reverse-mode gradients recorded along the realized execution trace, so one evaluator walk is amortized across large populations of parameter vectors. A locked cost model of a real differentiable self-hosted Scheme interpreter motivates the design. We realize NDVM as a native runtime with forward and gradient equivalence to the reference backend, about 60x per-lane batch amortization, near-linear multicore scaling, and two independent front ends. In fixed-budget co-search over LLM-proposed programs, NDVM reaches high-quality solutions about 24x sooner in wall-clock time, suggesting runtime differentiation as a practical systems foundation for scientific discovery workflows.

---

## 论文详细总结（自动生成）

这篇论文由 Lucas Sheneman 撰写，提出了一种名为 **原生微分虚拟机（NDVM）** 的高效运行时表示，旨在解决神经符号学习（Neuro-Symbolic Learning）中程序搜索与参数校准之间的效率瓶颈。

以下是对该论文的结构化总结：

### 1. 核心问题与研究动机
*   **研究背景**：在科学发现和神经符号系统中，AI 需要生成大量候选程序（符号结构），并针对观测数据校准这些程序的连续参数。
*   **核心矛盾**：
    *   **编译/分阶段方法（Staging）**：将程序编译为微分图。虽然执行快，但每个候选程序都需要昂贵的编译开销，不适合频繁变动结构的搜索过程。
    *   **解释器方法（Interpreter-based）**：将程序视为运行时数据。虽然灵活，但解释器在处理数值时的“装箱（Boxing）”和遍历开销巨大，导致数值计算只占总耗时的极小部分（约1%）。
*   **核心问题**：如何既保留“程序即数据”的灵活性，又能实现接近原生代码的参数校准效率？

### 2. 方法论
*   **核心思想**：**“微分求值器，而非程序”**。通过编译一个通用的求值器（Evaluator）来处理任意程序，而不是为每个程序单独生成微分图。
*   **关键技术细节**：
    *   **结构与数值分离（Structural/Numeric Split）**：将程序中的离散结构（标签、符号、堆地址）与可微数值分离。结构部分使用原生标量表示，数值部分存储在稠密的批处理缓冲区（Payload Table）中。
    *   **批处理原生化（Batch-native）**：一次符号结构的遍历（Walk）可以同时服务于 B 个参数矢量。这意味着解释器的开销被整个参数群体摊销。
    *   **仅数值磁带（Payload-only Tape）**：反向模式自动微分（AD）仅记录数值算子的执行轨迹，忽略符号查找、分支跳转等非微分操作，从而极大地压缩了微分磁带的大小。
    *   **精确轨迹控制流**：支持数据相关的分支和循环，通过“车道掩码（Lane Masking）”处理批处理中的控制流分叉，确保梯度计算的精确性。

### 3. 实验设计
*   **测试程序集**：涵盖标量表达式、超越函数（如 Michaelis-Menten）、递归循环（Logistic-map）以及复杂的矩阵运算（80步卡尔曼滤波器）。
*   **对比基准（Benchmarks）**：
    *   **Baseline**：基于 PyTorch 的 Eager 模式解释器（DMCI）。
    *   **Tuned-eager**：优化了表示但仍运行在 Python 环境下的解释器。
    *   **Staged JAX**：手动编写并使用 JAX 编译的静态图。
    *   **Hand-coded C++**：手动编写的原生 C++ 代码（性能上限）。
*   **场景应用**：
    *   **参数校准**：针对固定程序的参数优化。
    *   **协同搜索（Co-search）**：由大语言模型（LLM）提出程序结构，NDVM 负责快速校准参数。

### 4. 资源与算力
*   **硬件环境**：主要实验在单台高性能计算节点上完成，配置为 **AMD Ryzen Threadripper PRO 5975WX (32核/64线程)**，125 GiB 内存。
*   **GPU 使用**：论文明确指出 NDVM 目前是 **CPU 驱动** 的。仅在附录 A 中使用了一块 **RTX 4090** 进行“数值天花板”实验，以评估未来 GPU 解释器的潜力。
*   **软件**：Python 3.11, PyTorch 2.12, GCC 12.1.0。

### 5. 实验数量与充分性
*   **实验规模**：
    *   进行了 **Phase-0 成本模型分析**，精确分解了解释器的耗时占比。
    *   **一致性测试**：通过随机微分测试套件（Fuzzer）运行了 **600 到 1500 组** 自动生成的程序，验证前向输出和梯度与基准完全一致。
    *   **扩展性实验**：测试了从 1 到 1024 的批处理大小，以及从 1 到 64 线程的多核并行效率。
    *   **端到端搜索**：在符号回归和迭代映射两个任务上进行了固定预算（900秒）的对比实验。
*   **充分性评价**：实验设计非常严谨，不仅有性能对比，还通过消融实验分离了“表示优化”与“原生执行”的贡献，客观地指出了不同方法的适用边界（Crossover point）。

### 6. 主要结论与发现
*   **瓶颈定位**：证实了微分符号计算的瓶颈在于**表示（装箱）和遍历**，而非浮点运算。
*   **性能提升**：
    *   NDVM 通过批处理摊销，将单车道校准成本降低了约 **60 倍**。
    *   在多核环境下实现了近乎线性的加速（16核下约 15 倍）。
    *   在卡尔曼滤波器任务中，校准速度比 PyTorch 基准快 **8100 倍**。
*   **搜索效率**：在 LLM 驱动的程序搜索中，NDVM 达到相同拟合质量的速度比基准快 **24 倍**，并能探索更深的候选空间。
*   **摊销阈值**：发现只有当单个程序的优化步数超过数百甚至数万步时，JAX 等编译方法才会优于 NDVM。在典型的搜索场景（每候选程序仅优化几步）中，NDVM 具有绝对优势。

### 7. 优点
*   **系统级创新**：深刻洞察了神经符号计算的底层开销，提出了“结构/数值分离”的原创性运行时表示。
*   **实用性强**：无需修改搜索算法即可直接提速，且支持复杂的矩阵运算和递归。
*   **通用性**：在 Scheme 解释器和字节码虚拟机两个不同的前端上均验证了有效性。
*   **开源透明**：提供了详细的复现指南和锁定成本模型。

### 8. 不足与局限
*   **平台限制**：目前仅实现了 CPU 版本，虽然有 GPU 潜力分析，但完整的 GPU 解释器尚在开发中。
*   **语言特性支持有限**：目前不支持堆内存的变动（Mutation）、异常处理或动态形状（Dynamic Shapes）。
*   **分支分叉代价**：如果批处理中的程序执行路径严重分叉，批处理的摊销收益会显著下降。
*   **理论证明**：虽然实验验证了梯度一致性，但缺乏关于梯度正确性的形式化数学证明（计划作为未来工作）。

（完）
