---
title: Language models guide symbolic equation discovery by controlling search
title_zh: 语言模型通过控制搜索引导符号方程发现
authors: "Zikai Xie, Wenmei Li, Man Luo, Jun Jiang, Linjiang Chen"
date: 2026-07-05
pdf: "https://arxiv.org/pdf/2607.04156v1"
tags: ["query:sr-alg"]
score: 10.0
evidence: 大语言模型引导的符号回归用于科学方程发现
tldr: 本研究探讨了大语言模型（LLM）在科学方程发现中的角色，提出了 LLM-PySR 框架。该框架将 LLM 定位为搜索控制器，负责指定变量、算子和搜索深度，而由符号回归负责具体的公式拟合。实验表明，相比于直接生成方程或纯数值搜索，这种“LLM 引导搜索”的方法在 AI-Feynman 等基准测试中实现了准确性、复杂度和成本的最佳平衡，并能有效发现复杂的物理规律。
selection_source: fresh_fetch
motivation: 传统的符号回归面临组合搜索空间爆炸的问题，而直接利用大模型生成方程则往往缺乏严谨的数值验证。
method: 提出 LLM-PySR 框架，利用 LLM 指定搜索空间（如变量、算子和变换），再结合符号回归进行自动化的公式枚举与拟合。
result: 在 74 个物理方程和电池寿命预测任务中，该方法在精度、稳定性和计算成本上均优于端到端模型和纯数值基准。
conclusion: 大语言模型在科学发现中更适合作为引导假设探索的控制器，而非直接作为方程的最终决策者。
---

## 摘要
科学方程发现必须将广泛的领域先验与严格的数值测试相结合。符号回归提供了数值基础，但面临组合搜索空间的挑战，而许多语言模型系统则要求模型直接提出或选择公式。我们测试了一种不同的分工方式。我们比较了语言模型分别作为方程作者、候选决策者或搜索控制者的角色设定，并与端到端语言模型及纯数值基准进行了对比。在我们提出的控制者设定中（实现为 LLM-PySR），语言模型指定变量、算子、变换和搜索深度；符号回归负责枚举和拟合表达式；而确定性指标则决定结果的保留。在 74 个 AI-Feynman 方程和 7 个复杂的公式恢复任务中，搜索控制在准确性、复杂性、稳定性和成本之间实现了观察到的最强平衡。在一个独立的电池数据集上，LLM-PySR 识别出了早期电压曲线位移与循环寿命之间简洁的分段线性关系。结果表明，语言模型应当塑造假设探索的过程，而非决定哪些方程最终胜出。

## Abstract
Scientific equation discovery must combine broad domain priors with strict numerical testing. Symbolic regression supplies numerical grounding but faces a combinatorial search space, whereas many language-model systems ask the model to propose or select formulas directly. We test a different division of labour. We compare role specifications in which the language model acts as equation author, candidate decider or search controller, alongside end-to-end language-model and purely numerical baselines. In the controller setting we propose here, implemented as LLM-PySR, language models specify variables, operators, transformations and search depth; symbolic regression enumerates and fits expressions; and deterministic metrics govern retention. Across 74 AI-Feynman equations and seven complex formula-recovery tasks, search control achieved the strongest observed balance of accuracy, complexity, stability and cost. On an independent battery dataset, LLM-PySR identified a compact piecewise-linear relation between early voltage-curve displacement and cycle life. The results suggest that language models should shape hypothesis exploration rather than decide which equations survive.

---

## 论文详细总结（自动生成）

这篇论文由中国科学技术大学（USTC）等机构的研究团队完成，探讨了大语言模型（LLM）在科学方程发现（符号回归）中的最佳角色定位。

### 1. 核心问题与整体含义
**研究动机：** 科学发现需要结合“领域先验知识”与“严格的数值验证”。传统的符号回归（SR）虽然数值严谨，但面临搜索空间随变量和算子增加而呈指数级爆炸的问题。现有的 LLM 辅助方法通常让模型直接生成方程（容易产生幻觉或过度依赖记忆）或直接筛选结果（数值敏感度低）。
**核心问题：** 在符号回归的闭环系统中，LLM 应该承担什么角色？是直接写方程的“作者”，还是决定方程生死的“裁判”，亦或是规划搜索边界的“控制者”？

### 2. 方法论
论文提出了 **LLM-PySR** 框架，其核心思想是将 LLM 定位为**搜索控制者（Search Controller）**。
*   **分工逻辑：** LLM 负责高层规划（选择变量、算子、特征工程、搜索深度）；数值引擎（PySR）负责底层枚举与参数拟合；确定性指标（如 ACC、NMSE）负责最终的筛选。
*   **关键技术细节（三个 Agent）：**
    *   **Agent A (Planner)：** 根据科学背景和历史经验，提出搜索空间约束（变量子集、算子白名单、自定义变换）。
    *   **数值引擎 (PySR)：** 在约束空间内进行组合搜索和常数优化。
    *   **Agent B (Reviewer)：** 提供语义解释性评分，但**不具备**最终决定权。
    *   **确定性门控 (Gating)：** 使用数值指标（如验证集准确率）强制执行筛选，防止 LLM 的主观偏见或幻觉干扰结果。
    *   **Agent C (Memory Builder)：** 将数值反馈转化为结构化经验，指导下一轮迭代。
*   **算法流程：** 规划搜索空间 -> 符号枚举与拟合 -> 统一数值评估 -> 经验总结与记忆更新 -> 循环迭代 -> 后处理简化。

### 3. 实验设计
*   **数据集/场景：**
    1.  **AI-Feynman 基准：** 包含 74 个来自《费曼物理学讲义》的经典物理方程。
    2.  **复杂公式压力测试：** 7 个涵盖生物（细菌生长）、材料（应力应变）、物理（非线性振子）的复杂方程。
    3.  **真实世界应用：** 锂离子电池循环寿命预测（独立数据集，124 个电池样本）。
*   **对比方法（Benchmark）：**
    *   **Search Controller (LLM-PySR)：** 本文方法。
    *   **Equation Author：** LLM 直接生成方程骨架，仅由数值引擎优化常数。
    *   **Candidate Decider：** 数值引擎生成候选，LLM 拥有最终筛选权。
    *   **Full-stack LLM Search：** 端到端生成模型（如 LLM-SR, DrSR）。
    *   **No-LLM (Pure PySR)：** 纯数值搜索基准。

### 4. 资源与算力
*   **模型：** 实验主要使用了 **GPT-5.1**（论文中提到的版本，反映了研究的前瞻性或特定 API 版本）作为底层语言模型。
*   **算力说明：** 论文未详细列出具体的 GPU 硬件型号和训练时长，但重点对比了 **Token 消耗量**。
*   **效率：** LLM-PySR 的 Token 消耗仅为领先的端到端 LLM 方法（如 DrSR）的 **3.2% 到 4.1%**，显著降低了推理成本。

### 5. 实验数量与充分性
*   **实验规模：** 涵盖了 80 多个不同领域的方程发现任务，并进行了多轮独立运行以测试稳定性。
*   **充分性：** 实验设计非常系统，通过“角色消融”实验（即改变 LLM 的权限）深入探讨了不同架构的优劣。
*   **客观性：** 采用了统一的评估指标（$ACC_{0.1}$ 和 NMSE），并在独立测试集上验证了电池寿命预测的泛化能力，实验结果具有较强的说服力。

### 6. 主要结论与发现
*   **角色定位：** LLM 作为“搜索控制者”表现最优。它能有效利用先验知识缩小搜索空间，同时避免了直接生成方程时的脆弱性。
*   **性能优势：** 在 AI-Feynman 基准上，LLM-PySR 达到了 95.8% 的准确率，显著高于纯数值搜索和端到端 LLM 生成。
*   **稳定性：** 确定性数值门控比 LLM 筛选更稳定，有效解决了 LLM 对微小数值差异不敏感的问题。
*   **科学发现：** 在电池实验中，LLM-PySR 自动发现了一个描述早期电压位移与寿命之间关系的**分段线性经验定律**，这与电化学降解机制高度吻合。

### 7. 优点
*   **高效分工：** 巧妙地结合了 LLM 的语义推理能力和符号回归的数值搜索能力，解决了“幻觉”与“组合爆炸”的双重难题。
*   **极低成本：** 通过限制 LLM 仅参与高层规划，大幅降低了 Token 成本。
*   **可解释性：** 发现的方程紧凑且具有物理意义，优于黑盒机器学习模型。
*   **鲁棒性：** 即使在 LLM 缺乏相关领域知识的“冷门”方程上，由于有数值引擎底座，系统仍能保持基本性能。

### 8. 不足与局限
*   **剪枝风险：** 如果 LLM 规划的搜索空间过于狭窄（例如错误地排除了某个关键变量），数值引擎将无法找回正确方程。
*   **依赖基础模型：** 系统的上限仍受限于 LLM 的推理能力和对科学上下文的理解水平。
*   **形式限制：** 目前主要针对显式解析方程，对于涉及复杂微分方程或隐式关系的科学问题，处理能力尚待验证。

（完）
