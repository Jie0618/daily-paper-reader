---
title: "Dictionaries, Not Darwin: Set-Level Selection Beats LLM Evolution in Scientific Equation Discovery"
title_zh: 词典而非达尔文：科学方程发现中集合级选择优于大语言模型进化
authors: Pan Li
date: 2026-07-05
pdf: "https://arxiv.org/pdf/2607.04108v1"
tags: ["query:sr-alg"]
score: 10.0
evidence: 审计用于科学方程发现的大模型进化方法
tldr: 本研究质疑了在科学方程发现中利用大语言模型（LLM）进行进化式迭代的有效性。研究发现，基于父代的进化循环并不优于独立采样，其本质只是生成了候选术语字典。据此，作者提出了PTB-Search方法：仅进行单代LLM采样提取术语，随后通过集合级稀疏选择和最小二乘法确定方程。该方法在LLM-SRBench上以十分之一的调用预算显著超越了现有进化基准，证明了LLM在科学发现中更适合作为“材料供应商”而非进化引擎。
selection_source: fresh_fetch
motivation: 探究LLM在科学方程发现中常用的进化迭代机制是否真的比简单的独立采样更有效。
method: 提出PTB-Search方法，通过单次采样构建术语字典，并利用集合级稀疏选择和最小二乘法进行方程重组。
result: "PTB-Search在LLM-SRBench上仅用十分之一的预算便达到了77.0%的准确率，大幅领先于现有最佳进化基准。"
conclusion: LLM在科学发现中更适合扮演提供候选组件的“材料供应商”，而核心发现能力在于外部的集合级筛选机制。
---

## 摘要
大语言模型正越来越多地被用作科学发现的进化引擎：生成候选方案、筛选胜出者、将其作为父代反馈，并循环往复。我们审计了这种循环是否真的能在科学方程发现中促进发现，在这种场景下，有限的样本使得结构欠定且插值容易。在相同的 LLM 调用预算下，基于父代的进化与全新的独立采样并无区别：中位数 OOD NMSE 分别为 0.045 和 0.049，指令式多父代交叉表现更差，最终的成功取决于初始建议的质量，且多种迭代方案未能增加解决问题的数量。从操作层面看，该循环退化为其产物：一个候选术语词典。我们将这一诊断转化为 PTB-Search，一种用于组件化科学发现的单代方法。PTB-Search 仅进行一次独立 LLM 提案采样，将可重用术语提取到针对每个问题的词典中，并使用最小二乘系数进行仅训练集的集合级稀疏选择。其核心原理是，欠定数据识别的是术语集的联合行为，而非可靠的单项贡献。在相同的词典且不增加 LLM 调用次数的情况下，集合级选择器解决了 717 个单元格中的 165--169 个，而单项约简仅解决了 74--78 个。在官方包含 239 个问题的 LLM-SRBench 划分上，PTB-Search 使用 Llama-3.1-8B 达到了 73.2% 的 Acc0.1，使用单种子 DeepSeek-V4 锚点达到了 77.0%，而报告的最佳基线仅为 49.2%，且仅使用了标准调用预算的十分之一。程序域压力测试给出了一个范围边界：生成计数仍然不可靠，而保留的外部状态在更困难的非线性空间中会有所帮助。综上所述，LLM 最好被理解为材料供应商；科学发现是由对可重用组件的外部集合级选择所承载的。

## Abstract
Large language models are increasingly used as evolutionary engines for scientific discovery: generate candidates, select winners, feed them back as parents, and repeat. We audit whether this loop actually compounds discovery in scientific equation discovery, a setting where finite samples make structure underdetermined and interpolation easy. Under matched LLM-call budgets, parent-conditioned evolution is indistinguishable from fresh independent sampling: median OOD NMSE is 0.045 vs. 0.049, instructed multi-parent crossover is worse, final success is predicted by initial proposal quality, and multiple iteration schemes fail to add solved problems. Operationally, the loop reduces to what it produces: a dictionary of candidate terms. We turn that diagnosis into PTB-Search, a one-generation method for componentized scientific discovery. PTB-Search samples independent LLM proposals once, extracts reusable terms into a per-problem dictionary, and performs train-only set-level sparse selection with least-squares coefficients. Its central principle is that underdetermined data identifies the joint behavior of term sets, not reliable per-term credit. On identical dictionaries and zero additional LLM calls, set-level selectors solve 165--169 of 717 cells, while single-term reductions solve only 74--78. On the official 239-problem LLM-SRBench split, PTB-Search reaches 73.2% Acc0.1 with Llama-3.1-8B and 77.0% with a single-seed DeepSeek-V4 anchor, versus 49.2% for the best reported baseline, using one tenth of the standardized call budget. A program-domain stress test gives a scoped boundary: generation count remains unreliable, while retained external state can help in harder non-linear spaces. Across these results, LLMs are best understood as material suppliers; discovery is carried by external set-level selection over reusable components.

---

## 论文详细总结（自动生成）

这篇论文对当前大语言模型（LLM）在科学发现领域流行的“进化循环”范式提出了深刻质疑，并提出了一种更为高效、基于统计原理的替代方案。以下是对该论文的结构化总结：

### 1. 核心问题与整体含义
*   **研究动机**：目前 LLM 驱动的科学发现（如方程发现、程序合成）普遍采用类似进化的架构：生成候选方案 -> 筛选胜出者 -> 作为父代反馈给 LLM 进行变异 -> 重复。
*   **核心质疑**：这种“进化循环”真的比简单的独立采样更有效吗？作者怀疑所谓的“进化”在科学方程发现中其实只是在做低效的重复采样，并没有积累科学结构。
*   **整体含义**：论文通过审计证明，LLM 在科学发现中的真正价值是作为“材料供应商”（提供候选术语），而核心的“发现”功能应由外部的、基于集合的统计选择器完成，而非依赖 LLM 的提示词演化。

### 2. 方法论：PTB-Search
*   **核心思想**：放弃多代进化，采用“单代采样 + 术语提取 + 集合级重组”的策略。
*   **算法流程**：
    1.  **变异（LLM 采样）**：仅进行一次（One-shot）独立采样，生成 100 个候选方程，不进行父代调节。
    2.  **继承（术语提取）**：解析所有候选方程，将其拆解为加性术语（如 $\sin(x)$、$x^2$ 等），构建一个针对该问题的“术语词典”。
    3.  **重组与选择（外部搜索）**：在词典上进行全局稀疏子集搜索。使用**束搜索（Beam Search）**寻找最佳术语组合，并通过**最小二乘法（Least Squares）**拟合系数。
    4.  **集合级识别原则**：核心技术细节在于“集合级选择”而非“单项评分”。因为在数据欠定的情况下，单个术语的贡献难以独立评估，只有术语集合的联合行为才是可识别的。

### 3. 实验设计
*   **数据集**：官方 **LLM-SRBench**（包含 239 个科学方程发现问题，涵盖物理、化学、生物、材料科学等领域）。
*   **对比基线**：
    *   **LLM-SR**（当前最佳进化基准）。
    *   **LaSR**、**SGA**、**直接提示（Direct Prompting）**。
    *   **PySR**（传统的符号回归工具，作为局部参考）。
*   **评估指标**：数值准确率（Acc 0.1）、符号准确率（SA）、中位数 OOD NMSE（均方根误差）。
*   **消融实验**：对比了“父代进化”与“独立采样”、不同选择器（单项信用 vs. 集合级选择）、词典容量等。

### 4. 资源与算力
*   **模型使用**：使用了本地部署的 **Llama-3.1-8B**（用于大规模基准测试）和 **DeepSeek-V4**（作为强力 API 锚点）。
*   **算力预算**：论文强调了“预算公平性”。基线方法通常使用 1,000 次 LLM 调用，而 PTB-Search 仅使用 **100 次**调用（即 1/10 的预算）。
*   **硬件细节**：文中未详细列出具体的 GPU 型号或训练时长，但明确指出外部搜索是在 CPU 上完成的，且 LLM 调用次数是衡量成本的核心指标。

### 5. 实验数量与充分性
*   **实验规模**：在 Llama-3.1-8B 上进行了 717 组实验（239 问题 × 3 种子），在 DeepSeek 上进行了多轮开发集和盲测集审计。
*   **充分性**：实验设计非常严谨，包含了预注册的解释分支、防泄漏审计、以及跨领域的压力测试（如在线装箱程序的合成）。
*   **客观性**：作者通过“匹配预算”的审计，直接揭示了进化循环在 Acc 0.1 指标上与独立采样无异，这一结论具有很强的说服力。

### 6. 主要结论与发现
*   **进化是虚假的**：在方程发现中，基于父代的进化循环与新鲜的独立采样表现一致（NMSE 0.045 vs 0.049），甚至指令式交叉会导致结果变差。
*   **初始提案决定成败**：最终能否成功高度取决于初始独立采样的质量（AUC 0.959），后续的迭代几乎没有贡献。
*   **集合选择胜过单项信用**：在相同词典下，集合级选择器解决的问题数量（165-169）是单项信用评分方法（74-78）的两倍以上。
*   **性能飞跃**：PTB-Search 在官方榜单上以 1/10 的预算达到了 **73.2% - 77.0%** 的准确率，远超之前最佳基线（49.2%）。

### 7. 优点
*   **效率极高**：将 LLM 调用成本降低了 90%，同时显著提升了发现成功率。
*   **理论深刻**：指出了科学发现中“识别性（Identifiability）”的问题，即数据决定的是术语集合的联合行为。
*   **审计严谨**：打破了 LLM 领域对“进化/达尔文主义”的盲目迷信，回归到统计学习的本质。

### 8. 不足与局限
*   **符号准确率（SA）的挑战**：由于 PTB-Search 输出的是稀疏加性模型，虽然数值上非常精确，但在符号形式上可能与紧凑的闭式解（Closed-form）不完全一致，导致 SA 指标需要额外的简化处理。
*   **结构限制**：该方法目前主要针对“组件化”的发现空间（如加性方程），对于逻辑极其复杂的自由格式程序合成，其优势可能减弱（如文中 bin-packing 实验所示）。
*   **对词典覆盖的依赖**：如果 LLM 在初始采样中完全没有提到某个关键术语，外部选择器也无法将其凭空变出。

（完）
