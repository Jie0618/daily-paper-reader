---
title: Data-Driven Discovery of Multiscale Power System Oscillation Governing Equations Using SINDy-SENDAI
title_zh: 基于 SINDy-SENDAI 的多尺度电力系统振荡控制方程数据驱动发现
authors: "Andrea Pomarico, Yuxuan Bao, Liyao Mars Gao, Salvatore Tessitore, Giorgio Maria Giannuzzi, Alberto Berizzi, J. Nathan Kutz"
date: 2026-07-03
pdf: "https://arxiv.org/pdf/2607.03485v1"
tags: ["query:sr-alg"]
score: 10.0
evidence: 利用SINDy从传感器数据中识别简洁的控制方程
tldr: 针对含高比例逆变器电源的现代电力系统，本文提出了一种名为 SINDy-SENDAI 的分层多尺度框架，用于从广域测量数据中发现振荡控制方程。该方法结合深度学习与稀疏识别技术，能有效分离多尺度频率成分并提取具有物理可解释性的动力学模型。通过在 Kundur 系统及伊比利亚、意大利真实电网数据上的验证，证明了其在鲁棒性、预测精度和稳定性评估方面优于传统方法。
selection_source: fresh_fetch
motivation: 随着逆变器电源增加，电力系统动态行为日益复杂，传统黑盒模型缺乏可解释性，难以满足系统稳定性监测的需求。
method: 提出 SINDy-SENDAI 架构，利用深度学习在潜空间分离高低频分量，并结合 SINDy 算法识别简洁的非线性动力学控制方程。
result: 实验表明该框架在重构和预测电磁振荡方面优于 Hankel-DMD 方法，且能有效处理真实世界的环境测量数据。
conclusion: 该研究为现代电力系统提供了一种兼具鲁棒性与可解释性的数据驱动建模工具，有助于提升电网的安全稳定运行水平。
---

## 摘要
监测机电振荡对于维持现代电力系统的稳定性至关重要，特别是在基于逆变器的资源（IBRs）渗透率不断提高的情况下，这些资源引入了新的动态行为。在这项工作中，我们提出了一种基于 SINDy-SENDAI 算法的分层多尺度框架，用于表征广域测量捕获的暂态动力学。所提出的深度学习架构能够稳健地分离传感器数据中嵌入的低频和高频分量，并在潜空间中结合了非线性动力系统稀疏识别（SINDy）模块，以识别简洁的控制方程。与通常产生可解释性有限的黑盒模型的传统深度学习方法相比，所提出的框架学习了一种显式的动力学表示，从而实现了机电振荡的物理诠释、稳定性评估和预测。鉴于现代电力系统的社会重要性，所提出的方法专门设计用于满足实际部署的关键要求，即鲁棒性、可解释性以及在各种运行条件下的稳定性能。该框架首先在 Kundur 两区域测试系统上进行了验证，并以传统的模态分析作为基准，随后在两个真实世界数据集上进行了演示：2016 年伊比利亚半岛振荡事件和 2021 年意大利南部电网的环境测量数据。结果表明，SINDy-SENDAI 的性能始终优于最先进的 Hankel-DMD 方法，并且学习到的潜空间动力学具有足够的信息量，能够准确地重构和预测原始状态空间中整个系统的行为。

## Abstract
Monitoring electromechanical oscillations is crucial for maintaining the stability of modern power systems, particularly in the presence of increasing penetrations of inverter-based resources (IBRs), which introduce new dynamic behaviors. In this work, we propose a hierarchical multiscale framework based on the SINDy-SENDAI algorithm to characterize the transient dynamics captured by wide-area measurements. The proposed deep learning architecture robustly separates low- and high-frequency components embedded in sensor data and incorporates a Sparse Identification of Nonlinear Dynamical Systems (SINDy) module in the latent space to identify parsimonious governing equations. In contrast to conventional deep learning approaches that often produce black-box models with limited interpretability, the proposed framework learns an explicit dynamical representation, enabling physical interpretation, stability assessment, and forecasting of electromechanical oscillations. Given the societal importance of modern power systems, the proposed approach is specifically designed to satisfy key requirements for practical deployment, namely robustness, interpretability, and stable performance under diverse operating conditions. The framework is first validated on the two-area Kundur test system using conventional modal analysis as ground truth and subsequently demonstrated on two real-world datasets: the 2016 Iberian oscillatory event and the 2021 ambient measurements from the southern Italian power grid. The results show that SINDy-SENDAI consistently outperforms the state-of-the-art Hankel-DMD method and that the learned latent dynamics are sufficiently informative to accurately reconstruct and predict the behavior of the full system in the original state space.

---

## 论文详细总结（自动生成）

这篇论文提出了一种名为 **SINDy-SENDAI** 的数据驱动框架，旨在从电力系统广域测量系统（WAMS）的稀疏数据中，识别并提取具有物理可解释性的多尺度机电振荡控制方程。

以下是对该论文的结构化总结：

### 1. 核心问题与整体含义
*   **研究动机**：随着逆变器电源（IBR）和可再生能源的大规模接入，现代电力系统的动态行为变得愈发复杂且具有多尺度特性。传统的机电振荡监测方法（如模态分析、DMD 等）在处理高噪声环境数据或提供物理可解释性方面存在局限。
*   **核心问题**：如何在保证模型鲁棒性的同时，从稀疏且含噪的相量测量单元（PMU）数据中，自动发现能够描述系统长短期动态、且具备物理含义（非黑盒）的数学方程。

### 2. 方法论
SINDy-SENDAI 结合了分层频率剥离（SENDAI）和稀疏非线性动力学识别（SINDy）技术：
*   **核心思想**：采用“分而治之”的策略，将系统动态分解为低频（LF）主导路径和高频（HF）修正路径。
*   **低频路径（LF Pathway）**：
    *   利用 **SINDy-SHRED** 架构，通过门控循环单元（GRU）将时序观测映射到低维潜空间（Latent Space）。
    *   在潜空间中嵌入 **SINDy 模块**，强制学习简洁的常微分方程（ODE），如 $\dot{z} = \Xi^\top \Theta(z)$，其中 $\Xi$ 是稀疏系数矩阵。
*   **高频路径（HF Pathway）**：
    *   采用**分层频率剥离（Hierarchical Frequency Peeling）**机制，通过多个 1D-CNN 修正层逐层提取 LF 路径留下的残差。
    *   引入**频域稀疏正则化**，确保每一层只捕捉特定频段的相干振荡模式。
*   **算法流程**：先训练 LF 路径识别主导动态，再冻结 LF 参数，依次训练多个 HF 剥离层，最后通过潜空间 ODE 进行稳定性评估和前向预测。

### 3. 实验设计
论文通过三个递进的案例验证了方法的有效性：
*   **案例一：Kundur 两区域测试系统（仿真）**
    *   **基准（Benchmark）**：传统的线性模态分析（MA）。
    *   **对比方法**：Hankel-DMD (hDMD)。
*   **案例二：2016 年伊比利亚半岛振荡事件（真实数据）**
    *   分析欧洲电网在重大扰动下的东-中-西振荡模式（约 0.15 Hz）。
*   **案例三：2021 年意大利南部电网环境测量（真实数据）**
    *   测试在正常运行（弱激励）条件下的环境振荡识别能力。

### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的硬件配置（如 GPU 型号、数量）或具体的训练时长。
*   **实现细节**：提到使用了双层 GRU（每层 64 神经元）和 MLP 解码器，代码已在 GitHub 开源，暗示其计算需求在常规深度学习工作站的可接受范围内。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了标准仿真模型、极端扰动事件和日常环境测量三种典型场景，实验设计较为全面。
*   **充分性与公平性**：
    *   使用了 70%/15%/15% 的训练/验证/测试集划分。
    *   对比了工业界常用的 hDMD 方法，并使用 RMSE、增益（Gain）和模态参数误差等客观指标进行评估。
    *   实验结果显示在不同尺度和噪声水平下均具有一致性，表现出较好的客观性。

### 6. 主要结论与发现
*   **性能优越**：SINDy-SENDAI 在频率和阻尼比的估计精度上显著优于 hDMD（例如在 Kundur 系统中，频率误差仅为 1.08%）。
*   **多尺度重构**：HF 路径的加入使重构精度提升了近 50%，能有效捕捉 LF 路径忽略的快速瞬态。
*   **预测能力**：通过潜空间的 SINDy 方程，可以实现约 1 分钟的自主前向预测，准确捕捉低频趋势。
*   **物理洞察**：该方法能够识别出清晰的振荡模式形状（Mode Shapes），准确划分了欧洲电网的相干区域（如伊比利亚、希腊-土耳其、中欧）。

### 7. 优点
*   **可解释性强**：不同于纯黑盒模型，它直接输出数学方程，方便调度员进行稳定性评估（如特征值分析）。
*   **鲁棒性高**：通过 SENDAI 的分层结构，有效缓解了 SINDy 对测量噪声的敏感性。
*   **多功能性**：一个框架同时实现了数据去噪、状态重构、模态识别和短期预测。

### 8. 不足与局限
*   **超参数依赖**：模型性能高度依赖于超参数（如剥离层数、潜空间维度、稀疏阈值）的调节，目前缺乏自动调优机制。
*   **强迫振荡识别**：当前框架尚无法有效区分“强迫振荡”与“自然机电模式”，这在实际电网运行中是一个重要挑战。
*   **漂移问题**：长时预测（超过 30-60 秒）时，由于模型参数固定且缺乏实时反馈，预测轨迹会逐渐偏离真实值。

（完）
