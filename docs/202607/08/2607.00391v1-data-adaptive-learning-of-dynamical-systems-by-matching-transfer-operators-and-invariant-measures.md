---
title: Data-Adaptive Learning of Dynamical Systems by Matching Transfer Operators and Invariant Measures
title_zh: 通过匹配转移算子和不变测度进行动力系统的数据自适应学习
authors: "Yinong Huang, Jonah Botvinick-Greenhouse, Yunan Yang"
date: 2026-07-01
pdf: "https://arxiv.org/pdf/2607.00391v1"
tags: ["query:sr-alg"]
score: 8.0
evidence: 从轨迹数据中进行系统辨识和动力学学习
tldr: 针对动力系统学习中轨迹匹配易受噪声和混沌干扰的问题，本文提出一种基于转移统计的方法。该方法通过构建数据自适应网格，利用正则化Ulam转移矩阵近似Perron-Frobenius算子，将硬单元指标替换为平滑权重以支持梯度优化。通过匹配不变测度和转移矩阵，该方法在Lorenz系统及海温预测等任务中展现出比传统轨迹匹配更稳健的长时动力学捕捉能力。
selection_source: fresh_fetch
motivation: 传统的基于轨迹的动力系统学习在面对噪声、混沌或稀疏观测时，由于逐点误差的快速放大而显得非常脆弱。
method: 提出一种算子理论方法，通过数据自适应分区和正则化Ulam转移矩阵，匹配概率质量的运动规律及不变测度。
result: 在Lorenz-63、Lorenz-96及NOAA海温预测实验中，该方法在噪声和稀疏采样下比逐点轨迹匹配具有更可靠的长时动力学表现。
conclusion: 该研究为学习混沌及部分观测的动力系统提供了一种鲁棒的算子理论替代方案，有效克服了轨迹级损失函数的局限性。
---

## 摘要
基于轨迹的动力系统学习在存在噪声、混沌或稀疏观测时往往表现脆弱，因为微小的逐点误差会迅速放大。我们引入了一种用于系统辨识的转移统计方法，通过匹配跨数据自适应网格的概率质量诱导运动来学习动力学。给定轨迹数据，我们构建了状态空间的非结构化划分，并使用正则化的 Ulam 转移矩阵来近似 Perron--Frobenius 算子。我们将硬单元指示函数替换为连续、分段平滑的单位分解权重，从而产生一个支持针对所学向量场参数进行梯度优化的马尔可夫矩阵。这实现了两个相关的训练目标：通过转移矩阵的平稳特征向量匹配不变测度，以及匹配完整的转移矩阵以捕获状态空间区域之间的输运。在 Lorenz-63、Lorenz-96 以及降阶 NOAA 海表温度预测问题上的数值实验表明，转移统计匹配比逐点轨迹匹配提供了更可靠的长期动力学，特别是在测量噪声和稀疏采样的情况下。该方法为学习混沌和部分观测的动力系统提供了一种稳健的算子理论替代方案，用以取代轨迹级损失函数。

## Abstract
Trajectory-based learning of dynamical systems is often fragile in the presence of noise, chaos, or sparse observations, as small pointwise errors can rapidly amplify. We introduce a transition-statistics approach to system identification that learns dynamics by matching the induced motion of probability mass across a data-adaptive mesh. Given trajectory data, we build an unstructured partition of state space and approximate the Perron--Frobenius operator with a regularized Ulam transition matrix. We replace hard cell indicators with continuous, piecewise-smooth partition-of-unity weights, yielding a Markov matrix supporting gradient-based optimization with respect to the parameters of a learned vector field. This enables two related training objectives: matching invariant measures through the stationary eigenvectors of the transition matrices, and matching the full transition matrices to capture transport between regions of state space. Numerical experiments on Lorenz-63, Lorenz-96, and a reduced-order NOAA sea surface temperature forecasting problem show that transition-statistics matching gives more reliable long-time dynamics than pointwise trajectory matching, particularly under measurement noise and sparse sampling. The approach provides a robust operator-theoretic alternative to trajectory-level losses for learning chaotic and partially observed dynamical systems.

---

## 论文详细总结（自动生成）

这篇论文提出了一种基于算子理论的动力系统学习新框架，旨在解决传统轨迹匹配方法在处理混沌系统和噪声数据时的脆弱性。以下是对该论文的详细总结：

### 1. 核心问题与整体含义（研究动机和背景）
传统的动力系统学习（如系统辨识）通常依赖于**轨迹匹配（Trajectory Matching）**，即最小化预测轨迹与观测轨迹之间的逐点均方误差（MSE）。
*   **痛点**：在混沌系统中，由于“蝴蝶效应”，微小的初始误差或参数偏差会随时间指数级放大，导致 MSE 迅速失效。此外，观测噪声和稀疏采样会进一步干扰逐点匹配的准确性。
*   **核心思想**：论文转向**统计描述**，不再关注单个点的演化，而是关注概率密度函数在状态空间中的演化（即 Perron-Frobenius 算子）。通过匹配转移统计特性（如不变测度和转移矩阵），学习能够保持系统长期拓扑特征和统计规律的动力学模型。

### 2. 论文提出的方法论
该方法被称为**数据自适应转移统计匹配（Data-Adaptive Transition Statistics Matching）**，核心步骤如下：
*   **数据自适应分区**：不使用传统的结构化网格（在高维下会发生维度灾难），而是根据观测数据点的分布，利用 K-means 等聚类算法构建非结构化的状态空间划分（Voronoi 胞腔）。
*   **正则化 Ulam 转移矩阵**：
    *   传统的 Ulam 方法使用硬指示函数（Hard Indicators），不可微，无法进行梯度优化。
    *   **关键创新**：引入**平滑的单位分解权重（Partition-of-Unity Weights）**。通过连续且分段平滑的权重函数将点分配到不同的“软”胞腔中，从而构建一个关于待学习参数可微的马尔可夫转移矩阵 $P(\theta)$。
*   **损失函数设计**：
    1.  **不变测度损失（Invariant Measure Loss）**：匹配转移矩阵的左特征向量（平稳分布），确保模型能捕捉系统的长期吸引子特征。
    2.  **转移矩阵损失（Transition Matrix Loss）**：直接匹配数据诱导的经验转移矩阵与模型预测的转移矩阵，捕捉状态空间区域间的输运规律。
*   **优化流程**：利用自动微分技术，针对向量场（如神经网络或多项式模型）的参数 $\theta$ 进行梯度下降。

### 3. 实验设计
*   **实验场景/数据集**：
    1.  **Lorenz-63 系统**：经典的 3 维混沌系统，用于验证对吸引子几何结构的捕捉能力。
    2.  **Lorenz-96 系统**：40 维高维混沌系统，用于测试方法在较高维度下的扩展性。
    3.  **NOAA 海表温度（SST）数据**：真实世界的地球物理数据，通过本征正交分解（POD）降维后进行预测。
*   **Benchmark（基准方法）**：标准的**逐点轨迹匹配（Pointwise Trajectory Matching/MSE）**。
*   **对比维度**：在不同水平的测量噪声、不同的采样频率以及观测时长下对比预测性能。

### 4. 资源与算力
*   论文中**未明确说明**具体的 GPU 型号、数量或具体的训练时长。
*   由于涉及矩阵特征值分解和 K-means 聚类，其计算开销主要集中在构建转移矩阵和求解平稳分布上。对于 Lorenz-96 等系统，实验是在可接受的学术计算资源内完成的。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了从低维合成系统到高维混沌系统，再到真实世界降阶模型的完整梯度。
*   **消融与鲁棒性测试**：
    *   测试了不同噪声水平（从无噪声到高噪声）。
    *   测试了不同采样间隔（稀疏采样）。
    *   对比了“仅匹配不变测度”与“匹配完整转移矩阵”的效果。
*   **评价**：实验设计较为充分，通过可视化吸引子轨迹和计算统计分布距离（如 Wasserstein 距离），客观地证明了该方法在长时动力学保持上的优势。

### 6. 论文的主要结论与发现
*   **长时稳定性**：在混沌系统中，即使轨迹匹配在短期内表现尚可，其长期预测往往会偏离吸引子；而本方法学习的模型能始终保持在吸引子上。
*   **抗噪性强**：由于转移统计是对区域概率的积分，它对局部观测噪声具有天然的鲁棒性，表现远优于 MSE。
*   **数据效率**：在稀疏采样下，通过捕捉区域间的转移概率，该方法比逐点匹配更能有效地提取动力学本质。

### 7. 优点
*   **理论完备**：将算子理论（Perron-Frobenius）与现代深度学习优化相结合。
*   **可微性改进**：通过平滑权重解决了 Ulam 方法不可微的经典难题，使其能直接集成到反向传播框架中。
*   **自适应性**：数据驱动的分区方法避免了对状态空间进行盲目的网格划分，提高了高维处理能力。

### 8. 不足与局限
*   **计算复杂度**：转移矩阵的大小随分区数量 $K$ 增加，计算特征向量和矩阵匹配的开销在极高维度下可能仍然显著。
*   **超参数敏感性**：分区数量 $K$ 的选择以及平滑权重的带宽参数（Bandwidth）对结果有一定影响，需要调优。
*   **局部细节丢失**：由于采用了区域统计的方法，在极短期的精确轨迹预测上，可能不如在干净数据上训练的 MSE 方法。

（完）
