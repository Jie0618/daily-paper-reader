---
title: Physics-Audited Agentic Discovery in Scientific Machine Learning
title_zh: 科学机器学习中的物理审计智能体发现
authors: "Diab W. Abueidda, Bilal Ahmed, Panos Pantidis, Mostafa E. Mobasher"
date: 2026-07-08
pdf: "https://arxiv.org/pdf/2607.07379v1"
tags: ["query:sr-alg"]
score: 8.0
evidence: 在SciML中通过物理审计验证进行代理式代理模型发现
tldr: 本研究针对科学机器学习（SciML）中大模型代理仅依赖误差指标而忽视物理一致性的问题，提出了PA-SciML框架。该框架采用“验证优先”的工作流，通过自动化的物理审计机制（如检查因果律、边界条件等）对候选模型进行筛选。在固体力学案例中，PA-SciML不仅能发现误差更低的模型，还能确保其满足物理约束，解决了传统方法在复杂动力学场景下违反因果律的缺陷，显著提升了科学发现的可靠性。
selection_source: fresh_fetch
motivation: 传统的科学机器学习代理主要依赖误差指标进行模型选择，往往忽略了模型是否符合物理定律（如因果律和边界条件）。
method: 提出PA-SciML工作流，通过预设的机器可检查物理要求对候选模型进行输出验证，并在无参考解的情况下搜索高违规案例。
result: 在固体力学实验中，该方法选出的模型不仅误差更低，且成功通过了基准模型未能通过的严苛因果律检查。
conclusion: 物理审计是确保科学机器学习发现具有物理可靠性的关键，其提供的物理证据比单纯的误差聚合指标更具筛选价值。
---

## 摘要
在智能体科学机器学习（SciML）中，大语言模型（LLM）智能体可以发现代理模型，并根据自动评分（通常是误差指标）选择其中一个。然而，低误差并不意味着预测场满足力学中重要的物理特性，例如边界条件、叠加原理、刚度缩放或因果性。我们提出了物理审计智能体 SciML（PA-SciML），这是一种用于智能体 SciML 发现的验证优先工作流。该工作流在搜索前固定评分评估器，推导出可审查且机器可检查的物理要求，检查每个训练好的候选模型的输出，并针对没有参考解场的情况，在规定的输入范围或测量的载荷历史跨度内单独搜索高违规案例。只有在通过所述检查的情况下，代理模型才会被报告为已验证。启用后，该工作流还会在训练前添加建议性数值探测，并一次测试一个建模更改，以记录哪些孤立的编辑与重用前的评分提升相关。在报告的计算固体力学数值示例中，静态弹性运行选择了一个验证误差低于仅误差基线的代理模型，而两个选定的模型都通过了通用的线性弹性检查。在瞬态弹性动力学运行中，具有相似平均误差的仅误差基线模型因对载荷历史的未来部分产生响应而未能通过更严格的因果性检查，而选定的代理模型则通过了所述检查。主要区别在于对预测场进行的针对每个候选模型的物理证据检查，而非更丰富的综合评分。

## Abstract
In agentic scientific machine learning (SciML), large language model (LLM) agents can discover surrogate models and select one by an automated score, typically an error metric. A low error, however, does not establish that the predicted fields satisfy the physics that matter for mechanics, such as boundary conditions, superposition, stiffness scaling, or causality. We introduce Physics-Audited Agentic SciML (PA-SciML), a verification-first workflow for agentic SciML discovery. The workflow fixes a scoring evaluator before search, derives reviewable machine-checkable physics requirements, checks each trained candidate on its outputs, and separately searches prescribed input ranges or measured load-history spans for high-violation cases without reference solution fields. A surrogate is reported as verified only under the stated checks. When enabled, the workflow also adds advisory numerical probes before training and tests one modeling change at a time to record which isolated edits are associated with score gains before reuse. In the reported computational-solid-mechanics numerical examples, the static elasticity run selects a surrogate with lower validation error than the error-only baseline while both selected models pass the common linear-elastic checks. In the transient elastodynamics run, an error-only baseline with similar mean error fails a stricter causality check by responding to future parts of the loading history, while the selected surrogate passes the stated checks. The main distinction is per-candidate physics evidence on predicted fields, not a richer aggregate score.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **PA-SciML（Physics-Audited Agentic SciML）** 的框架，旨在解决大语言模型（LLM）智能体在自动发现科学机器学习（SciML）模型时，过度依赖误差指标而忽视物理一致性的问题。

以下是对该论文的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：当前的智能体 SciML 系统（如使用 LLM 自动设计神经网络架构）主要通过验证集误差（如 MSE）来筛选模型。然而，**低误差并不等同于物理正确性**。一个模型可能在数值上接近参考解，但在物理上却违反了边界条件、叠加原理、刚度缩放或因果律（Causality）。
*   **研究动机**：在工程和力学领域，物理可靠性是模型可用的前提。作者希望建立一种“验证优先”的工作流，使智能体在发现模型的同时，必须通过严格的、机器可检查的物理审计。

### 2. 方法论：核心思想与关键技术
PA-SciML 框架将模型发现过程分为三个主要层次：
*   **发现循环（Discovery Loop）**：
    *   利用 LLM 扮演不同角色（提议者、批评者、工程师、调试者等）来迭代生成和训练代理模型（如 DeepONet 或 FNO）。
    *   固定一个评估器（Fixed Evaluator），确保所有候选模型在相同的标准下评分。
*   **验证层（Verification Layer）**：
    *   **物理合同（Physics Contracts）**：在搜索前定义可检查的物理要求（如线性、对称性、边界条件）。
    *   **物理审计器（Physics Auditor）**：对训练好的模型输出进行采样检查，计算最大归一化违规值 $viol_{max}$。
    *   **对抗搜索（Adversary）**：在没有参考解的情况下，通过 Adam 优化算法在输入空间搜索使模型物理违规最大的极端案例。
*   **后发现归因（Post-discovery Attribution）**：
    *   **消融实验（Ablator）**：将复杂的模型改进分解为单个编辑，通过重新训练评估每个编辑对性能提升的贡献份额 $\phi_i$。
    *   **方法卡片（Method Card）**：只有通过物理审计且贡献明确的改进才会被记录为可重用的知识。

### 3. 实验设计
论文设计了两个典型的计算固体力学场景：
*   **场景 A：参数化线性弹性（静态）**
    *   **任务**：预测受顶端载荷作用的二维正方形弹性体的位移场。
    *   **物理检查**：载荷叠加性、杨氏模量倒数缩放、滚轴边界条件（$u_x=0$ 或 $u_y=0$）。
    *   **对比基准**：仅依据误差选择模型的“仅误差基线（Error-only baseline）”。
*   **场景 B：瞬态弹性动力学（动态）**
    *   **任务**：预测受右端随时间变化位移驱动的薄条带的瞬态响应。
    *   **物理检查**：**因果律**（当前响应不应依赖未来载荷）、初始条件、边界条件一致性。
    *   **对比基准**：同上。

### 4. 资源与算力
*   **LLM 后端**：使用了 **Claude 3.5 Sonnet**（负责提议、实现、总结）和 **GPT-4o**（负责根工程师、批评、调试和选择器集成）。
*   **算力资源**：文中提到了使用 NYUAD 的研究计算中心资源，但**未明确列出具体的 GPU 型号、数量或总训练时长**。实验中设置了训练时间预算，例如在瞬态示例中有 2 个候选模型因超时被标记为失败。

### 5. 实验数量与充分性
*   **实验规模**：静态弹性实验生成了 28 个候选模型，瞬态实验生成了 16 个。
*   **充分性评价**：
    *   **客观性**：通过固定评估器和物理审计器，排除了 LLM 主观判断的干扰。
    *   **消融实验**：对改进路径进行了详细的单点编辑测试，证明了物理结构化架构（如因果卷积、线性分解）是性能提升的主因。
    *   **局限性**：实验基于单次随机种子运行，且候选模型数量相对较少（受限于 LLM 调用成本和训练算力）。

### 6. 主要结论与发现
*   **物理审计的必要性**：在瞬态实验中，仅误差基线模型虽然平均误差很低，但严重违反了**因果律**（它对未来的载荷产生了响应）。而 PA-SciML 选出的模型通过构造（Causal Convolution）天然满足因果律。
*   **性能提升**：在静态实验中，PA-SciML 选出的模型不仅物理正确，其验证误差（$8.86 \times 10^{-5}$）也优于基线（$2.10 \times 10^{-4}$）。
*   **自动发现物理结构**：智能体能够自动发现并采用符合力学原理的架构（如位移场的线性叠加表示），这些结构在消融实验中表现出最高的贡献度。

### 7. 优点
*   **验证优先**：将物理一致性作为硬性门控，而非仅仅是损失函数中的一个正则项。
*   **对抗性验证**：引入 Adversary 搜索，能够在没有真实标签的情况下发现模型的物理缺陷，增强了模型的可靠性。
*   **知识沉淀**：通过“方法卡片”机制，将自动发现的有效建模经验结构化，便于跨问题迁移。

### 8. 不足与局限
*   **计算成本**：自动发现过程涉及数十个模型的训练，且需要多次调用昂贵的 LLM API。
*   **线性假设**：目前的物理检查（如叠加性）主要针对线性系统，对于非线性力学问题的适用性有待扩展。
*   **单种子偏差**：由于成本限制，未进行大规模的重复实验来消除随机性影响。
*   **合同依赖**：物理审计的效果高度依赖于预设的“物理合同”是否完备，如果 Mechanician（智能体角色）漏掉了关键物理特性，审计仍可能失效。

（完）
