---
title: RL Post-Training Builds Compositional Reasoning Strategies
title_zh: RL 后训练构建组合式推理策略
authors: "Azwar Abdulsalam, Nishil Patel, Andrew Saxe"
date: 2026-07-08
pdf: "https://arxiv.org/pdf/2607.07646v1"
tags: ["query:math-ai"]
score: 8.0
evidence: 符号重写任务中组合推理的强化学习后期训练
tldr: 本研究探讨了强化学习（RL）后训练如何构建组合推理策略。通过在重写语法环境中的实验发现，RL 不仅能放大原始技能，还能通过分阶段机制将其整合为顺序和并行的更高阶策略。相比拒绝微调，RL 具有更强的选择性，能将探索集中于有效的可重用结构，从而显著提升模型解决复杂推理问题的能力。
selection_source: fresh_fetch
motivation: 探究强化学习后训练是仅放大预训练模型中的潜在技能，还是能将基础技能组合成更高阶的推理策略。
method: 在可审计的重写语法环境中，对比研究了基于二进制奖励的 RL 后训练与拒绝微调在推理任务中的表现。
result: RL 通过分阶段机制将基础技能重组为顺序和并行组合策略，在解决复杂问题上显著优于预训练模型和拒绝微调。
conclusion: RL 的关键在于其选择性地将基础过程整合为可靠的高级策略，而非单纯增加探索量。
---

## 摘要
RL 后训练仅仅是放大基座模型中已有的潜在原始技能，还是能将原始技能组合成新的更高级策略？我们在一个完全可观测的重写语法（rewrite-grammar）环境中研究这一问题，该环境的预训练分布已知，且每个生成的重写步骤均可审计。Transformer 在原始符号重写链上进行预训练，并在仅提供二元最终答案奖励的基于轨迹（Trace-based）的推理任务上进行后训练。RL 解决了预训练模型即使在更大的采样预算下也极少能解决的留出（held-out）问题，而拒绝微调（RFT）虽然在早期有所提升，但随后陷入停滞。轨迹分析表明，RL 通过一种阶段性的组合机制重新组织原始能力：它首先强化原始归约（primitive reductions），随后发现有效的组合程序。这些程序包括顺序组合（将有序的原始收缩链折叠）和并行组合（在单一步骤中结合独立的原始收缩）。这些组合程序并非孤立的样本，而是被重复使用并巩固为稳定的技能库。将 RL 与拒绝微调进行对比发现，关键区别不在于探索量，而在于选择性：RFT 产生许多类似捷径的重写，其中大部分是无效的，而 RL 将探索集中在有效的可重用结构上。预训练消融实验表明，组合策略的出现不仅取决于原始技能的暴露程度，还取决于预训练是否将原始能力组织成 RL 随后可以压缩的归约程序。基座模型提供了微弱的程序成分；RL 则将它们构建成可靠的高级策略。

## Abstract
Does RL post-training merely amplify primitive skills already latent in a base model, or can it compose primitive skills into new higher-level strategies? We study this question in a fully observable rewrite-grammar environment where the pretraining distribution is known and every generated rewrite can be audited. A Transformer is pretrained on primitive symbol-rewrite chains and post-trained on a Trace-based reasoning task with only a binary final-answer reward. RL solves held-out problems that remain rarely solved by the pretrained model even under much larger sampling budgets, while rejection fine-tuning improves early but plateaus. Trace analysis shows that RL reorganizes primitive competence through a phased compositional mechanism: it first strengthens primitive reductions, then discovers valid composed procedures. These include sequential compositions, which collapse ordered chains of primitive contractions, and parallel compositions, which combine independent primitive contractions in a single step. The composed procedures are not isolated samples; they are reused and consolidated into a stable repertoire. Comparing RL with rejection fine-tuning shows that the key difference is not exploration volume but selectivity: RFT produces many shortcut-like rewrites, much of them invalid, whereas RL concentrates exploration into valid reusable structure. Pretraining ablations show that the emergence of compositional strategies is gated not by primitive exposure alone, but by whether pretraining organizes primitive competence into reduction procedures that RL can later compress. The base model provides weak procedural ingredients; RL builds them into reliable higher-level strategies.

---

## 论文详细总结（自动生成）

这是一份关于论文《RL Post-Training Builds Compositional Reasoning Strategies》的结构化深度总结：

### 1. 核心问题与整体含义
*   **研究动机**：当前学术界对强化学习（RL）后训练的作用存在争议：RL 是仅仅放大了基座模型中已有的潜在技能（Reweighting），还是能将原始技能组合成全新的高阶策略（Composition）？
*   **核心问题**：在推理任务中，RL 如何从零散的原始操作中构建出复杂的、超越基座模型采样极限的组合推理能力？
*   **背景**：由于大语言模型（LLM）的预训练数据和内部机制不透明，很难判断一个新行为是 RL 创造的还是预训练中见过的。本文通过构建一个**完全可观测的重写语法（Rewrite-Grammar）环境**，使每一个推理步骤都可审计，从而揭示 RL 的演化机制。

### 2. 方法论
*   **核心思想**：利用符号重写任务模拟多步推理。模型需要通过一系列“收缩”操作将长字符串还原为目标符号。
*   **关键技术细节**：
    *   **环境构建**：定义一套语法规则（如 $b \to dff$）。模型在预训练阶段只学习原始的单步扩展和收缩。
    *   **后训练任务**：给定一个长字符串，要求模型生成收缩轨迹，最终得到目标符号。奖励仅基于最终答案的正确性（二元奖励），不提供中间步骤指导。
    *   **算法流程**：
        *   **GRPO（组相对策略优化）**：通过同一提示词下的多个样本（Group）计算相对优势，利用成功与失败轨迹的对比来更新模型。
        *   **RFT（拒绝微调）**：作为对比基准，仅对成功的采样轨迹进行监督学习。
    *   **策略分类**：将模型行为分为四类：**原始操作**（Primitive）、**顺序组合**（Macro，折叠多步操作）、**并行组合**（Parallel，单步执行多个独立操作）和**伪造操作**（Spurious，无效重写）。

### 3. 实验设计
*   **场景与数据集**：自定义的重写语法环境。
*   **难度分级**：根据解决问题所需的原始步骤是否超过生成预算（256 token）分为 Difficulty 1-6。高难度问题必须使用“组合策略”（压缩步骤）才能在预算内完成。
*   **Benchmark 与对比方法**：
    *   **基座模型（Base Model）**：测试其在极大采样预算（pass@1024）下的表现。
    *   **RFT（Rejection Fine-Tuning）**：模拟基于模仿学习的后训练。
    *   **RL（GRPO）**：本文核心研究对象。
*   **消融实验**：调整预训练数据中“收缩操作”的权重（$\rho$）和链式结构，观察预训练底座对 RL 策略涌现的影响。

### 4. 资源与算力
*   **硬件设备**：实验在一台配备 **4 张 NVIDIA H100 GPU** 的节点上运行。
*   **模型规模**：使用 12 层、隐藏维度 512 的 Transformer 模型。
*   **训练细节**：文中未明确给出总训练时长，但记录了训练迭代次数（约 20,000 次迭代）。

### 5. 实验数量与充分性
*   **实验规模**：
    *   核心学习曲线基于 **3 个随机种子** 的平均值。
    *   评估涵盖了 5 个难度等级的留出（held-out）问题。
    *   进行了详细的预训练消融实验（不同 $\rho$ 值及匹配频率控制组）。
*   **充分性评价**：实验设计非常严谨。通过“完全可观测性”，作者能够定量分析无效操作的比例，这在自然语言任务中几乎是不可能实现的。对比 RFT 与 RL 的数学推导（有限样本对比视角）为实验结果提供了坚实的理论支撑。

### 6 主要结论与发现
*   **阶段性涌现**：RL 表现出明显的阶段性：先强化原始归约能力，随后发现并巩固“顺序组合”策略，最后发展出“并行组合”策略。
*   **超越基座极限**：RL 能够解决基座模型在 $k=1024$ 采样下成功率为 0% 的极难问题（Difficulty 4-5），证明 RL 确实扩展了能力边界。
*   **选择性而非探索量**：RFT 也会尝试组合操作，但会产生大量无效（Spurious）步骤；RL 的优势在于其**选择性**，能通过负反馈抑制无效的“乘客”行为，将探索集中在有效的可重用结构上。
*   **预训练是基石**：组合策略的涌现依赖于预训练如何组织原始能力。如果预训练缺乏连续的归约过程，RL 很难凭空构建出组合策略。

### 7. 优点
*   **高透明度**：通过合成环境解决了 LLM 研究中常见的“数据污染”和“黑盒轨迹”问题。
*   **机制洞察**：首次清晰地展示了从“原始技能”到“程序化块（Chunking）”的转化过程。
*   **理论与实验结合**：利用有限群视角解释了 GRPO 为何在抑制无效操作上优于 RFT，具有很强的说服力。

### 8. 不足与局限
*   **环境简化**：重写语法虽然逻辑严密，但与自然语言推理（如数学证明、代码编写）的复杂性仍有差距。
*   **规模限制**：实验基于较小的 Transformer 模型，尚未在大规模参数模型上验证这种“阶段性涌现”是否完全一致。
*   **应用限制**：该研究主要关注“验证性奖励（RLVR）”场景，对于无法自动验证结果的开放式任务，其结论的适用性有待商榷。

（完）
