---
title: "DegenDetector: Symbolic Recovery of Parameter Degeneracies in Bayesian Posteriors"
title_zh: DegenDetector：贝叶斯后验中参数退化的符号化恢复
authors: "Chaipat Tirapongprasert, Matthew Ho"
date: 2026-07-09
pdf: "https://arxiv.org/pdf/2607.08755v1"
tags: ["query:sr-alg"]
score: 10.0
evidence: 使用符号回归将参数退化识别为闭式符号方程
tldr: DegenDetector是一个用于识别贝叶斯后验分布中参数退化关系的框架。它通过结合互信息筛选和交替符号回归，能够自动将复杂的多参数退化关系转化为可解释的闭式符号方程。相比于传统的转角图，该方法不仅能发现相关性，还能揭示底层的数学函数形式，为高维参数空间提供了更深层的结构化理解和自动化分析手段。
selection_source: fresh_fetch
motivation: 传统的后验分析工具如转角图仅能展示参数间的相关性，无法揭示其底层的具体数学函数形式。
method: 该框架结合了互信息筛选与交替符号回归技术，实现了对多参数退化关系的自动化符号化建模。
result: DegenDetector能够将复杂的多参数退化关系表达为闭式方程，提供了具有可解释性的结构化信息并能扩展至高阶空间。
conclusion: 该研究为理解高维参数空间中的退化问题提供了一种高效且自动化的符号恢复工具，增强了贝叶斯推断的可解释性。
---

## 摘要
我们介绍了 DegenDetector，这是一个将后验分布中的参数退化识别并表征为闭式符号方程的框架。通过将互信息筛选与交替符号回归相结合，我们在无需领域特定输入的情况下，实现了退化关系的自动化和可解释性识别。虽然诸如角点图（corner plots）之类的标准工具可以指示相关性的存在，但它们无法揭示底层的函数形式。DegenDetector 通过将多参数退化表示为闭式方程填补了这一空白，提供了可扩展至高阶参数空间的可解释结构。

## Abstract
We introduce DegenDetector, a framework for identifying and characterizing parameter degeneracies in posterior distributions as closed-form symbolic equations. By combining mutual information screening with alternating symbolic regression, we facilitate automated and interpretable identification of degenerate relationships without domain-specific input. While standard tools such as corner plots can indicate that correlations exist, they do not reveal the underlying functional form. DegenDetector fills this gap by expressing multi-parameter degeneracies as closed-form equations, providing interpretable structure that scales to high-order parameter spaces.

---

## 论文详细总结（自动生成）

这是一份关于论文《DegenDetector: Symbolic Recovery of Parameter Degeneracies in Bayesian Posteriors》的结构化总结：

### 1. 论文的核心问题与整体含义（研究动机和背景）
在物理学和天文学的贝叶斯推断中，**参数退化（Parameter Degeneracy）**是一个核心挑战。当观测数据对参数组合的约束远强于对单个参数的约束时，后验样本会集中在低维流形上。
*   **痛点：** 传统的诊断工具（如角点图/Corner Plots）只能显示参数间存在相关性，但无法给出具体的数学函数形式。此外，涉及三个或更多参数的高阶退化在二维投影中很难被察觉。
*   **核心目标：** 开发一个名为 **DegenDetector** 的自动化框架，旨在从后验样本中识别退化参数子集，并将其表征为可解释的、闭式的**符号方程**。

### 2. 论文提出的方法论
DegenDetector 结合了信息论筛选和符号回归技术，主要流程如下：
*   **候选参数筛选（互信息）：** 使用 $k$-最近邻估计器计算参数间的互信息（MI）。通过对参数元组的成对 MI 求和进行排名，识别出统计依赖性最强的参数组合，从而避免组合爆炸。
*   **符号曲面拟合：** 将退化模型假设为可分离函数的形式：$g_1(\theta_{j1}) + g_2(\theta_{j2}) + \dots + g_k(\theta_{jk}) = c$。
*   **交替优化（Alternating Optimization）：** 为了降低搜索难度，算法每次固定其他组件，将 $k$ 维拟合问题分解为 $k$ 个独立的 1D 符号回归问题。利用 **PySR** 库循环执行符号回归，直到收敛。
*   **正交拟合质量评估：** 定义了正交 $R^2_\perp$ 指标，通过计算样本到拟合曲面的平均平方垂直距离来衡量拟合优度。
*   **对数变换（LogDegen）：** 针对乘法退化关系（如幂律），提供坐标变换选项，在对数空间内进行线性化处理。

### 3. 实验设计
论文通过合成数据和真实科学数据验证了方法的有效性：
*   **合成基准测试（Synthetic Benchmarks）：**
    *   **Banana（香蕉形）：** 扩展的 Rosenbrock 函数，测试非线性多项式约束。
    *   **S-curve（S型曲线）：** 测试复杂的非单调退化。
    *   **Trig（三角函数）：** 测试周期性函数或其泰勒展开的近似能力。
    *   **Cubic（三次曲线）：** 测试在参数尺度不匹配（各向异性先验）下的鲁棒性。
*   **真实科学实验：** 使用 **Planck 2018** 宇宙微波背景（CMB）测量的后验链，尝试恢复著名的宇宙学视界角退化关系（$\Omega_m h^3 \approx \text{const}$）。

### 4. 资源与算力
*   **算力说明：** 文中**未明确提及**具体的硬件配置（如 GPU 型号、数量）或具体的训练时长。
*   **软件依赖：** 提到了使用 Python 库，包括用于符号回归的 `PySR`、用于符号简化的 `SymPy` 以及用于互信息计算的工具。由于 1D 符号回归相对高效，该方法通常不需要大规模计算集群。

### 5. 实验数量与充分性
*   **实验规模：** 论文展示了 4 组不同类型的合成退化实验和 1 组真实宇宙学数据实验。
*   **充分性评价：** 实验设计较为全面，覆盖了多项式、非单调、周期性和对数线性等多种函数形式。通过在合成实验中加入 7 个干扰参数（Nuisance parameters），证明了互信息筛选能有效从大量组合中精准定位退化参数。实验结果显示 $R^2_\perp$ 均超过 0.98，具有较强的说服力。

### 6. 论文的主要结论与发现
*   **自动化恢复：** DegenDetector 能够成功从高维后验分布中自动提取出准确的符号方程。
*   **科学发现一致性：** 在 Planck 2018 数据中，该框架在无物理先验的情况下，自动发现了 $\log \Omega_m$ 和 $\log H_0$ 之间的线性关系，计算出的系数比值（约 2.947）与理论预期的 $\Omega_m h^3$ 幂律关系高度吻合。
*   **可解释性：** 相比于黑盒模型，该方法提供的闭式方程可以直接用于物理模型的重新参数化（Reparameterization），从而消除参数相关性。

### 7. 优点
*   **无需领域知识：** 纯数据驱动，不需要预先假设物理公式的形式。
*   **可解释性强：** 输出结果是人类可读的数学公式，便于物理学家理解底层逻辑。
*   **处理高阶退化：** 能够识别两个以上参数间的复杂耦合，超越了传统 2D 投影工具的限制。
*   **鲁棒性：** 交替优化策略提高了符号回归在多维空间中的搜索效率和稳定性。

### 8. 不足与局限
*   **可分离性假设：** 目前的模型假设退化函数是可分离的（即各参数函数之和）。对于不可分离的复杂耦合（如 $x \cdot \sin(y)$），该方法可能无法直接捕获，需要依赖坐标变换（如 LogDegen）。
*   **计算复杂度：** 虽然 1D 分解降低了难度，但对于极高维度的参数空间，互信息筛选的组合搜索仍可能面临挑战。
*   **噪声敏感度：** 符号回归对样本中的噪声水平较为敏感，若后验分布本身非常弥散，可能难以提取出简洁的方程。

（完）
