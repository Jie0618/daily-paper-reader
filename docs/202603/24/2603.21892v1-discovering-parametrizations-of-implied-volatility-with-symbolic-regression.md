---
title: Discovering parametrizations of implied volatility with symbolic regression
title_zh: 利用符号回归发现隐含波动率的参数化表示
authors: "Martin Keller-Ressel, Hannes Nikulski"
date: 2026-03-23
pdf: "https://arxiv.org/pdf/2603.21892v1"
tags: ["query:sr-alg"]
score: 9.0
evidence: 应用符号回归从市场数据中发现解析公式
tldr: 本研究探讨了利用符号回归从数据中自动发现隐含波动率切片的参数化表示。通过直接从市场数据中搜索简单的解析公式，该方法无需预设函数形式即可拟合总隐含方差。实验表明，符号回归发现的紧凑公式在拟合精度和简洁性上可与广泛使用的SVI模型相媲美，为波动率建模提供了新的数据驱动路径。
selection_source: fresh_fetch
motivation: 旨在克服传统隐含波动率参数化模型预设形式的局限性，寻求更灵活且简洁的数据驱动表示。
method: 采用符号回归技术，直接从市场数据中搜索以对数货币值和到期时间为变量的解析公式。
result: 符号回归能够识别出结构紧凑且拟合性能与SVI模型相当的候选参数化公式。
conclusion: 符号回归是发现隐含波动率参数化形式的有效工具，能够平衡模型的简洁性与准确性。
---

## 摘要
我们研究了隐含波动率切片参数化表示的数据驱动发现。利用符号回归，我们寻找简单的解析公式，将总隐含方差表示为对数值度和到期期限的函数。我们的方法直接从市场数据中生成候选参数化方案，而无需预设函数形式。我们在准确性和简洁性方面将所得公式与广泛使用的 SVI 参数化方案进行了比较。数值实验表明，符号回归能够识别出具有竞争力拟合性能的紧凑参数化方案。

## Abstract
We investigate the data-driven discovery of parametric representations for implied volatility slices. Using symbolic regression, we search for simple analytic formulas that approximate the total implied variance as a function of log-moneyness and maturity. Our approach generates candidate parametrizations directly from market data without imposing a predefined functional form. We compare the resulting formulas with the widely used SVI parametrization in terms of accuracy and simplicity. Numerical experiments indicate that symbolic regression can identify compact parametrizations with competitive fitting performance.

---

## 论文详细总结（自动生成）

这是一份关于论文《Discovering parametrizations of implied volatility with symbolic regression》的结构化深入总结：

### 1. 论文的核心问题与整体含义
*   **研究动机**：在期权市场中，构建和维护隐含波动率（IV）曲面是核心任务。目前主流方法是使用预设的参数化模型（如 SVI 或 SABR），但这些模型往往基于经验或特定随机过程的近似。
*   **核心问题**：是否存在比广泛使用的 SVI 模型更简洁、更准确的隐含波动率参数化公式？
*   **整体含义**：本研究试图打破“人工设计公式”的传统，利用**符号回归（Symbolic Regression）**这一数据驱动技术，从海量市场数据中自动“发现”描述总隐含方差（Total Implied Variance）的解析表达式。

### 2. 论文提出的方法论
*   **核心思想**：将隐含波动率的拟合建模为一个“条件参数化符号回归”问题。即寻找一个全局统一的函数形式 $f(x, p)$，其中 $x$ 是对数货币值（log-moneyness），而参数 $p$ 随不同的波动率切片（slice）而变化。
*   **关键技术细节**：
    *   **PySR 框架**：利用基于进化算法的 PySR 工具搜索数学表达式树。
    *   **无套利约束集成**：在损失函数中引入了 **Lee 尾部边界（Lee’s tail bounds）** 惩罚项，作为归纳偏置（Inductive Bias），确保生成的公式符合基本的金融物理逻辑。
    *   **计算优化（聚类）**：由于 PySR 处理数万个切片时计算压力巨大，作者采用 **k-means 聚类** 将 6 万多个切片简化为 10-30 个原型切片进行公式搜索，随后在全量数据上验证。
    *   **损失函数**：采用全局均方误差（MSE）结合无套利惩罚项的结构。

### 3. 实验设计
*   **数据集**：
    *   **D1 (SPX 数据集)**：包含 2005 年至 2023 年的标准普尔 500 指数期权数据，共计 61,428 个隐含波动率切片。
    *   **D2 (策展数据集)**：来自 VolaDynamics 的 34 个特殊切片，包含 SVI5 型、C6 型和 C8 型（如复杂的 W 型波动率曲线）。
*   **Benchmark（基准）**：工业界标准模型 **SVI (Stochastic Volatility Inspired)**。
*   **对比维度**：公式复杂度（Complexity） vs. 拟合精度（Log-Loss），以及无套利指标（$\alpha_K$）。

### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的硬件配置（如 GPU 型号或 CPU 核心数）。
*   **训练时长**：未提及具体时长，但作者指出 PySR 的运行时间对切片数量（$N_{slice}$）非常敏感，因此被迫采用了聚类启发式方法来降低计算负载。

### 5. 实验数量与充分性
*   **实验组数**：
    *   针对 D1 数据集进行了 16 次独立的符号回归运行，设置了不同的基函数集和模板。
    *   针对 D2 数据集进行了多次条件回归运行，并包含了一组**消融实验**（对比有无无套利惩罚项对公式发现的影响）。
*   **充分性评价**：实验设计较为充分。作者不仅在大规模真实历史数据（D1）上验证了泛化性，还在极端/特殊形状数据（D2）上测试了模型的捕捉能力。通过训练-测试集分割（60/40）确认了模型不存在过拟合。

### 6. 论文的主要结论与发现
*   **发现新公式**：符号回归发现了一系列在“复杂度-精度”前沿面上优于 SVI 的公式。其中公式 $f_4(x) = p_4(\tanh(p_1x) + p_2)(p_3 + x) + p_5$ 在复杂度更低的情况下，拟合误差低于 SVI。
*   **重新发现 SVI**：在 SVI5 型数据的实验中，当加入无套利惩罚项时，符号回归能够成功从零开始重新推导出 SVI 的数学形式。
*   **约束的重要性**：无套利约束不仅能防止渐近套利，还能作为有效的引导，帮助算法更快找到低损失的函数区域。
*   **复杂形状处理**：对于 W 型等异常曲线，SR 发现了 SVI 的简单加性扩展形式，能够完美拟合 C6/C8 型数据。

### 7. 优点
*   **方法创新**：首次将符号回归应用于隐含波动率参数化发现，展示了 AI 辅助科学发现（AI for Science）在金融工程中的潜力。
*   **理论与数据结合**：并非纯黑盒搜索，而是巧妙地将金融领域的无套利硬约束转化为正则化项。
*   **实用性强**：发现的公式（如 $f_4$）具有解析形式，易于在交易系统中实现和求导。

### 8. 不足与局限
*   **约束覆盖不全**：由于计算复杂度限制，实验未包含**蝶式套利（Butterfly Arbitrage）**约束（涉及二阶导数），这可能导致某些发现的公式在特定参数下存在套利空间。
*   **维度限制**：目前仅针对单一到期期限的“切片”进行建模，尚未扩展到包含时间维度（Maturity）的完整波动率曲面。
*   **计算瓶颈**：对大规模数据的处理依赖于聚类近似，可能丢失了一些长尾的极端市场场景信息。

（完）
