---
title: "FACTO: Function-space Adaptive Constrained Trajectory Optimization for Robotic Manipulators"
title_zh: FACTO：机器人机械臂的函数空间自适应约束轨迹优化
authors: "Yichang Feng, Xiao Liang, Minghui Zheng"
date: 2026-02-23
pdf: "https://arxiv.org/pdf/2602.20225v1"
tags: ["query:opt-lm"]
score: 8.0
evidence: 自适应约束轨迹优化算法
tldr: "本文提出 FACTO 算法，一种针对单臂和多臂机器人的函数空间自适应约束轨迹优化方法。该算法将轨迹参数化为正交基函数的线性组合，并在系数空间直接优化。通过结合高斯-牛顿近似、指数移动平均和 Levenberg-Marquardt 算法，FACTO 能够有效处理非线性目标和轨迹约束。实验证明，与 CHOMP、TrajOpt 及 RRT* 等主流规划器相比，FACTO 在复杂约束环境下具有更高的解质量和可行性，并已在 Franka 机器人上成功部署验证。"
selection_source: carryover_cache
motivation: 旨在解决单臂及多臂机器人在复杂约束环境下的高效、高质量轨迹规划难题。
method: 将轨迹表示为正交基函数的线性组合，并在系数空间利用高斯-牛顿法和零空间投影技术进行自适应约束优化。
result: 在与多种优化和采样规划器的对比中，FACTO 在解的质量和可行性方面表现更优，特别是在多臂协作场景下。
conclusion: FACTO 是一种稳健且高效的轨迹优化框架，适用于具有复杂约束的实际机器人操作任务。
---

## 摘要
本文介绍了函数空间自适应约束轨迹优化（FACTO），这是一种适用于单臂和多臂机械臂的新型轨迹优化算法。轨迹表示被参数化为正交基函数的线性组合，并直接在系数空间中进行优化。该约束问题公式由目标泛函和定义在截断系数上的有限维目标组成。为了处理非线性，FACTO 使用带有指数移动平均的 Gauss-Newton 近似，从而产生一个平滑的二次子问题。全轨迹约束通过系数空间映射来处理，并使用 Levenberg-Marquardt 算法在激活约束的零空间中执行自适应约束更新。与基于优化的规划器（CHOMP、TrajOpt、GPMP2）和基于采样的规划器（RRT-Connect、RRT*、PRM）的对比表明，该算法提高了求解质量和可行性，特别是在受限的单臂和多臂场景中。在 Franka 机器人上对 FACTO 进行的实验评估验证了其部署的可行性。

## Abstract
This paper introduces Function-space Adaptive Constrained Trajectory Optimization (FACTO), a new trajectory optimization algorithm for both single- and multi-arm manipulators. Trajectory representations are parameterized as linear combinations of orthogonal basis functions, and optimization is performed directly in the coefficient space. The constrained problem formulation consists of both an objective functional and a finite-dimensional objective defined over truncated coefficients. To address nonlinearity, FACTO uses a Gauss-Newton approximation with exponential moving averaging, yielding a smoothed quadratic subproblem. Trajectory-wide constraints are addressed using coefficient-space mappings, and an adaptive constrained update using the Levenberg-Marquardt algorithm is performed in the null space of active constraints. Comparisons with optimization-based planners (CHOMP, TrajOpt, GPMP2) and sampling-based planners (RRT-Connect, RRT*, PRM) show the improved solution quality and feasibility, especially in constrained single- and multi-arm scenarios. The experimental evaluation of FACTO on Franka robots verifies the feasibility of deployment.

---

## 论文详细总结（自动生成）

以下是对论文《FACTO: Function-space Adaptive Constrained Trajectory Optimization for Robotic Manipulators》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
论文针对在复杂、拥挤的工作空间中，如何为单臂及多臂机器人规划出**连续时间可行（Time-continuous feasible）**且满足**任务约束**的轨迹这一难题展开研究。
*   **研究动机**：现有的基于路标点（Waypoint）的优化方法（如 CHOMP, TrajOpt）在路标点稀疏时无法保证点与点之间的安全性，而增加路标点会显著提升计算开销。基于采样的算法（如 RRT*）虽然完备，但难以处理复杂的流形约束且生成的轨迹不够平滑。
*   **核心目标**：提出一种在“函数空间”而非“离散状态空间”进行优化的框架，以实现高效、平滑且严格满足约束的轨迹生成。

### 2. 核心方法论
FACTO 的核心思想是将轨迹参数化为**正交基函数的线性组合**，并在**系数空间**中直接进行自适应约束优化。
*   **轨迹参数化**：使用余弦（Cosine）、正弦（Sine）或切比雪夫（Chebyshev）多项式作为基函数。轨迹表示为 $\xi(t) = \phi(t)^T \psi + \varphi(t)$，其中 $\psi$ 是待优化的系数，$\varphi(t)$ 用于处理边界条件（起点/终点位置和速度）。
*   **目标函数构建**：包含平滑度项（基于系数的二次型）和障碍物避障项（基于符号距离场 SDF）。
*   **关键技术细节**：
    *   **EMA 平滑高斯-牛顿法**：引入指数移动平均（EMA）来平滑非凸障碍物代价函数的梯度和海森矩阵（Hessian），提高收敛稳定性。
    *   **零空间投影（Null-space Projection）**：将等式约束（如边界条件、末端执行器姿态任务）线性化，并将优化步骤投影到这些约束的零空间内，确保迭代过程中始终满足硬约束。
    *   **自适应约束更新**：利用 Levenberg-Marquardt (LM) 算法和自适应信赖域策略处理非线性。
    *   **时间缩放（TimeScaler）**：在优化出轨迹形状后，通过解析方法缩放执行时间 $T$，以满足关节速度和力矩限制。

### 3. 实验设计
*   **数据集与场景**：使用了 MotionBenchMaker (MBM) 标准数据集，涵盖书架（bookshelf）、箱子（box）、厨房（kitchen）、笼子（cage）和桌面（table）等 8 个典型场景。
*   **实验规模**：
    *   **单臂任务**：800 组无约束任务，200 组受限任务（如持杯平移）。
    *   **双臂任务**：40 组无约束协作，40 组受限协作（闭链运动）。
*   **对比方法**：
    *   **优化类**：CHOMP, TrajOpt, GPMP2。
    *   **采样类**：RRT-Connect, RRT*, PRM, 以及处理约束的 IMACS-RRT-C。
*   **硬件验证**：在两台 Franka FR3 机器人上进行了 10 组物理实验，验证了单臂避障和双臂协同操作。

### 4. 资源与算力
*   **计算平台**：实验在一台配备 2.3 GHz Intel Core i9 处理器（单线程执行）和 16 GB RAM 的 MacBook Pro 上完成。
*   **软件工具**：使用 Pinocchio 进行运动学计算，OSQP 作为二次规划（QP）求解器，FCL 进行碰撞检测。
*   **算力说明**：文中未提及使用 GPU，该算法主要依赖 CPU 进行数值优化。由于是数值优化方法，不涉及深度学习的训练时长。

### 5. 实验数量与充分性
*   **实验数量**：总计超过 1000 组仿真实验和 10 组物理实验，样本量充足。
*   **充分性与客观性**：实验覆盖了从简单避障到复杂末端约束、从单臂到双臂协作的多种维度。通过对比成功率、计算时间、轨迹粗糙度（Roughness）等多个指标，客观地展示了 FACTO 在不同维度下的权衡（Trade-off）。对比基准涵盖了过去十年中最主流的规划算法，具有很强的说服力。

### 6. 主要结论与发现
*   **可靠性**：FACTO-Cos（余弦基）在复杂约束场景下的成功率最高（常达 90% 以上），甚至优于经典的 RRT-Connect。
*   **效率**：作为优化算法，其计算速度（通常在 0.03s - 0.3s 之间）远快于采样类算法（通常需数秒）。
*   **平滑度**：由于在低频基函数空间优化，生成的轨迹天然平滑，粗糙度显著低于采样类算法。
*   **约束处理**：零空间投影法能有效处理严格的流形约束，而 CHOMP 等方法在强约束下容易失效。

### 7. 优点（亮点）
*   **连续时间保证**：通过函数空间参数化，避免了离散路标点之间的碰撞风险。
*   **维度压缩**：将高维轨迹优化简化为少量系数（如 $N=6$）的优化，极大地减少了决策变量。
*   **鲁棒性**：EMA 平滑技术解决了碰撞梯度不连续导致的优化震荡问题。
*   **解耦设计**：多臂系统被解耦为独立的机器人实例，通过交换碰撞信息实现高效协作。

### 8. 不足与局限
*   **单步计算开销**：虽然总时间短，但由于每一步都要调用 OSQP 求解受限 QP 问题，其单次迭代的计算成本高于 CHOMP 或 GPMP2。
*   **局部最优风险**：作为一种基于梯度的局部优化方法，FACTO 仍可能陷入局部最优解（尽管低带宽基函数有助于跳过某些高频噪声）。
*   **扩展性挑战**：随着机器人数量进一步增加或约束变得极其复杂，零空间投影的线性化误差可能会增大，需更频繁的迭代。

（完）
