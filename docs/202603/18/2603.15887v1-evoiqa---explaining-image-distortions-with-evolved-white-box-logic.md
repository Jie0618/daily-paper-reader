---
title: EvoIQA - Explaining Image Distortions with Evolved White-Box Logic
title_zh: EvoIQA——利用演化白盒逻辑解释图像失真
authors: "Ruchika Gupta, Illya Bakurov, Nathan Haut, Wolfgang Banzhaf"
date: 2026-03-16
pdf: "https://arxiv.org/pdf/2603.15887v1"
tags: ["query:sr-alg"]
score: 10.0
evidence: 基于遗传规划的符号回归框架用于显式公式
tldr: 针对传统图像质量评估（IQA）指标在硬性手工模型与不可解释的深度学习黑盒模型之间的矛盾，本文提出EvoIQA框架。该框架利用遗传编程进行符号回归，从VSI、VIF等经典指标中提取特征，演化出人类可读的显式数学公式。EvoIQA在保持完全透明性的同时，性能超越了传统指标并比肩最先进的深度学习模型，实现了可解释性与高精度的统一。
selection_source: fresh_fetch
motivation: 旨在解决现有图像质量评估模型在数学公式的僵化性与深度学习模型的不可解释性之间的权衡难题。
method: 采用基于遗传编程的符号回归方法，通过演化包含结构、色彩和信息论特征的终端集，自动生成显式的数学评估公式。
result: 实验表明演化出的模型与人类视觉感知高度一致，性能优于传统手工指标且达到了与DB-CNN等深度学习模型相当的水平。
conclusion: 该研究证明了通过符号回归可以构建出既具有高度可解释性又具备最先进性能的白盒图像质量评估模型。
---

## 摘要
传统的图像质量评估 (IQA) 指标通常处于两个极端：要么是僵化的手工数学模型，要么是完全缺乏可解释性的“黑盒”深度学习架构。为了弥补这一差距，我们提出了 EvoIQA，这是一个基于遗传规划 (Genetic Programming) 的完全可解释符号回归框架，用于演化出显式的、人类可读的图像质量评估数学公式。通过利用来自 VSI、VIF、FSIM 和 HaarPSI 指标的丰富终结符集，我们的框架能够将结构、色彩和信息论退化内在映射为可观察的数学方程。实验结果表明，演化出的 GP 模型在预测与人类视觉偏好之间始终保持高度一致。此外，它们不仅优于传统的手工指标，而且在性能上与 DB-CNN 等复杂的先进深度学习模型相当，证明了我们不再需要为了追求最先进的性能而牺牲可解释性。

## Abstract
Traditional Image Quality Assessment (IQA) metrics typically fall into one of two extremes: rigid, hand-crafted mathematical models or "black-box" deep learning architectures that completely lack interpretability. To bridge this gap, we propose EvoIQA, a fully explainable symbolic regression framework based on Genetic Programming that Evolves explicit, human-readable mathematical formulas for image quality assessment (IQA). Utilizing a rich terminal set from the VSI, VIF, FSIM, and HaarPSI metrics, our framework inherently maps structural, chromatic, and information-theoretic degradations into observable mathematical equations. Our results demonstrate that the evolved GP models consistently achieve strong alignment between the predictions and human visual preferences. Furthermore, they not only outperform traditional hand-crafted metrics but also achieve performance parity with complex, state-of-the-art deep learning models like DB-CNN, proving that we no longer have to sacrifice interpretability for state-of-the-art performance.

---

## 论文详细总结（自动生成）

以下是对论文《EvoIQA - Explaining Image Distortions with Evolved White-Box Logic》的结构化总结：

### 1. 核心问题与整体含义
*   **研究动机**：传统的图像质量评估（IQA）模型存在两个极端：一是手工设计的数学模型（如SSIM），虽然透明但表达能力有限；二是深度学习黑盒模型（如CNN），虽然精度高但完全不可解释，无法揭示人类视觉系统（HVS）如何权衡不同维度的失真（如结构损坏与色彩偏移）。
*   **核心目标**：利用**遗传规划（Genetic Programming, GP）**驱动的**符号回归（Symbolic Regression）**，自动演化出既具有高预测精度又具备人类可读性的显式数学公式，从而构建“白盒”IQA模型。

### 2. 方法论
*   **核心思想**：将现有的多种顶级IQA指标（HaarPSI, FSIM, VIF, VSI）分解为底层的感知特征图，通过统计建模将其转化为紧凑的特征向量，再利用GP演化出最优的组合公式。
*   **关键技术细节**：
    *   **特征分解**：从参考图像和失真图像中提取相位一致性（PC）、梯度幅值（GM）、色度相似度等空间特征图。
    *   **统计表征（AGGD）**：使用**非对称广义高斯分布（AGGD）**拟合特征图。相比普通高斯分布，AGGD能通过形状参数（$\alpha$）和尺度参数（$\sigma^2$）更精准地捕捉失真引起的“统计自然性”偏移（如JPEG伪影导致的分布尖峰）。
    *   **符号回归引擎（StackGP）**：采用基于栈的遗传规划算法。为了保证可解释性，严格限制算子集仅包含算术运算（+、-、*、/），禁止使用复杂的非线性激活函数。
    *   **演化策略**：分为“热启动初始化”（快速过滤无效程序）和“探索阶段”（通过交叉、变异和帕累托锦标赛选择来平衡公式的复杂性与准确度）。
*   **模型变体**：
    *   **EvoIQA-Full**：使用全量特征集以追求最高精度。
    *   **EvoIQA-Subset**：利用随机森林进行特征重要性排序，仅保留核心特征以提升计算效率。

### 3. 实验设计
*   **数据集**：
    *   **训练集**：TID2013（包含24种失真类型）。
    *   **验证/测试集**：KADID-10k（大规模）、CSIQ、CID:IQ、VDID2014（跨场景验证）。
*   **Benchmark（基准）**：使用斯皮尔曼等级相关系数（SROCC）衡量模型预测与人类主观评分（MOS）的一致性。
*   **对比方法**：
    *   **传统指标**：SSIM, MS-SSIM, FSIM, HaarPSI。
    *   **机器学习/黑盒**：SVR, 随机森林, BRISQUE。
    *   **深度学习**：MEON, DB-CNN, CoDI-IQA。

### 4. 资源与算力
*   **算力说明**：论文**未明确列出**具体的GPU型号或训练时长。
*   **效率暗示**：作者强调该模型极其轻量化，最终演化出的公式参数少于10个。相比拥有4700万参数的深度学习模型（如DB-CNN），EvoIQA在推理时的计算开销几乎可以忽略不计。

### 5. 实验数量与充分性
*   **实验规模**：进行了**30次独立运行**以消除随机性影响，并报告了平均值和标准差。
*   **覆盖范围**：涵盖了从合成噪声到压缩失真等24-25种失真类型。
*   **充分性评价**：实验设计较为客观公平。通过跨数据库测试验证了模型的泛化能力；通过消融实验验证了超参数（如算子集、种群规模）的影响；并提供了定性的可视化分析（如AGGD曲线对比），证明了实验的深度。

### 6. 主要结论与发现
*   **性能比肩SOTA**：EvoIQA在TID2013上的SROCC达到0.8979，在24种失真类型中的18种表现优于HaarPSI，且达到了与顶级深度学习模型（DB-CNN）相当的水平。
*   **白盒优势**：演化出的公式揭示了感知逻辑。例如，模型自动发现通过“梯度异质性”（Ωcv）来惩罚局部结构坍塌，通过“色度集成器”来捕捉色彩饱和度变化。
*   **高鲁棒性**：尽管只在TID2013上训练，但在未见过的CID:IQ和VDID数据集上依然表现稳健，优于传统的SVR和随机森林。

### 7. 优点
*   **极高的可解释性**：输出的是简洁的数学方程，研究人员可以直接分析各感知分量的权重。
*   **计算极其高效**：公式简单，适合部署在资源受限的嵌入式设备或实时传输系统中。
*   **无需大量数据**：相比深度学习，符号回归在较小的数据集上也能演化出泛化性强的物理规律。

### 8. 不足与局限
*   **特征依赖性**：EvoIQA本质上是一种“元指标”或特征融合方法，其性能高度依赖于预定义的底层感知特征（如VSI、FSIM提供的特征图）。
*   **算子限制**：为了可解释性禁用了非线性算子，虽然消融实验显示影响不大，但在处理极度复杂的非线性失真时可能存在表达瓶颈。
*   **应用范围**：目前主要集中在全参考（FR-IQA）领域，对于无参考（NR-IQA）场景的有效性尚待进一步验证（尽管作者将其列为未来工作）。

（完）
