---
title: Calabi-Yau Metrics with Kähler Moduli Dependence
title_zh: 具有 Kähler 模依赖性的 Calabi-Yau 度规
authors: "Andrei Constantin, Andre Lukas, Luca A. Nutricati"
date: 2026-03-12
pdf: "https://arxiv.org/pdf/2603.12384v1"
tags: ["query:sr-alg"]
score: 9.0
evidence: 使用符号回归确定物理度量的系数解析形式
tldr: "本研究提出了一种构建Calabi-Yau流形Ricci平坦度规解析近似表达式的新方法，并显式包含Kähler模依赖性。通过结合机器学习获取的数值数据、Kähler势的解析Ansatz以及符号回归技术，研究者在两个h1,1=2的流形上成功重建了闭式近似度规。该方法不仅达到了百分比级别的精度，还保持了流形的离散对称性，为连接数值计算与解析构造提供了重要桥梁。"
selection_source: fresh_fetch
motivation: 旨在解决Calabi-Yau流形Ricci平坦度规缺乏显式Kähler模依赖解析表达式的问题。
method: 结合神经网络学习Kähler势、解析Ansatz拟合以及符号回归，提取模依赖的解析系数。
result: 在两个具体的Calabi-Yau三维流形上实现了具有百分比级精度且符合对称性的解析度规近似。
conclusion: 该方法成功架起了数值度规与解析构造之间的桥梁，为系统研究度规对Kähler模的依赖性开辟了道路。
---

## 摘要
我们提出了一种为 Calabi-Yau 三维流形上的 Ricci 平坦 Kähler 度规构建近似解析表达式的方法，该方法显式地依赖于 Kähler 模。我们的策略将通过机器学习获得的数值数据与 Kähler 势的显式解析 Ansatz 以及符号回归方法相结合。具体而言，我们使用神经网络学习 Kähler 模空间中选定点的 Kähler 势，将这些数据拟合到具有 Kähler 模依赖参数的解析表达式中，并利用符号回归确定这些系数作为 Kähler 模函数的解析形式。通过这种方式，我们重构了保留显式 Kähler 模依赖性的 Ricci 平坦度规的闭式近似。我们将该方法应用于两个 $h^{1,1}=2$ 的 Calabi-Yau 三维流形，即 $\mathbb{P}^2 \times \mathbb{P}^2$ 中的双三次超曲面和 $\mathbb{P}^1 \times \mathbb{P}^3$ 中的双次数 $(2,4)$ 超曲面，两者都具有简化度规结构的非平凡离散对称群。在这两种情况下，所得的解析表达式均能以百分比级的精度重现数值学习到的 Kähler 势，并遵循底层流形的离散对称性。我们的结果在 Calabi-Yau 度规的纯数值结果与解析构造之间架起了一座具体的桥梁，为系统研究其对 Kähler 模的依赖性开辟了道路。

## Abstract
We present a method to construct approximate analytic expressions for Ricci-flat Kähler metrics on Calabi-Yau threefolds with explicit dependence on the Kähler moduli. Our strategy combines numerical data obtained from machine learning with an explicit analytic Ansatz for the Kähler potential and symbolic regression methods. Specifically, we use neural networks to learn the Kähler potential at selected points in Kähler moduli space, fit this data to analytic expressions with Kähler moduli-dependent parameters, and determine an analytic form of these coefficients as functions of the Kähler moduli using symbolic regression. In this way, we reconstruct closed-form approximations to the Ricci-flat metric that retain explicit Kähler-moduli dependence. We apply this method to two Calabi-Yau threefolds with $h^{1,1}=2$, namely a bicubic hypersurface in $\mathbb{P}^2 \times \mathbb{P}^2$ and a bi-degree $(2,4)$ hypersurface in $\mathbb{P}^1 \times \mathbb{P}^3$, both of which admit nontrivial discrete symmetry groups that simplify the structure of the metric. In both cases, the resulting analytic expressions reproduce the numerically learned Kähler potentials with percent-level accuracy and respect the discrete symmetry of the underlying manifold. Our results represent a concrete bridge between purely numerical results for Calabi-Yau metrics and analytic constructions, opening the door to a systematic study of their dependence on Kähler moduli.

---

## 论文详细总结（自动生成）

这篇论文由 Andrei Constantin、Andre Lukas 和 Luca A. Nutricati 撰写，提出了一种结合机器学习与符号回归的方法，旨在解决 Calabi-Yau (CY) 流形上 Ricci 平坦度规缺乏显式解析表达式（尤其是对 Kähler 模依赖性）的长期难题。

以下是对该论文的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：在弦理论现象学中，推导标准模型参数（如 Yukawa 耦合、费米子质量）需要精确的 Ricci 平坦度规。然而，现有的数值方法（如 Donaldson 算法或纯神经网络方法）通常只能给出模空间中特定点的度规，无法提供显式的、连续依赖于 Kähler 模（$h^{1,1} > 1$）的解析表达式。
*   **研究动机**：为了系统地研究物理观测值随模空间的变化，以及实现模稳定（moduli stabilisation），迫切需要一种能够显式表达 Kähler 模依赖性的度规构造方法。

### 2. 方法论
该论文提出了一种“数值学习 + 解析拟合 + 符号回归”的混合策略：
*   **核心思想**：将 Kähler 势 $K$ 分解为 Fubini-Study 项 $K_{FS}$ 和一个全局定义的修正函数 $\phi$。通过神经网络获取 $\phi$ 的数值点，再利用符号回归将其转化为解析函数。
*   **关键技术细节**：
    1.  **解析 Ansatz**：提出 $\phi$ 的解析形式为：
        $$\phi(x, t) = V(t)^{1/3} \sum_{I,J} \alpha_{IJ}(t) \frac{s_I(x) \bar{s}_J(x)}{\prod |x|^2}$$
        其中 $s_I$ 是线丛 $L^k$ 的全纯截面，系数 $\alpha_{IJ}$ 被设定为仅依赖于 Kähler 模比例（如 $t_1/t_2$）的函数。
    2.  **数值数据获取**：使用 `cymetric` 软件包，通过神经网络最小化 Monge-Ampère 损失函数，在 Kähler 模空间的多个采样点处学习 $\phi$。
    3.  **对称性利用**：利用流形的离散对称性（如双三次流形的 $G$ 对称性）将 $\alpha_{IJ}$ 矩阵简化为少数几个独立的不变量系数 $\alpha_i$。
    4.  **符号回归**：使用 `PySR` 工具，对拟合得到的系数 $\alpha_i(t_1/t_2)$ 进行符号回归，提取出包含 $\log$、$\exp$ 等初等函数的闭式解析表达式。

### 3. 实验设计
*   **研究对象（数据集/场景）**：
    1.  **双三次超曲面（Bicubic）**：位于 $\mathbb{P}^2 \times \mathbb{P}^2$ 中，具有 $h^{1,1}=2$。
    2.  **双次数 (2,4) 超曲面**：位于 $\mathbb{P}^1 \times \mathbb{P}^3$ 中，具有 $h^{1,1}=2$。
*   **Benchmark（基准）**：以神经网络学习到的高精度数值 Kähler 势作为“真值”。
*   **对比实验**：
    *   对比了不同截断阶数（线丛次数 $k$）对精度的影响（如 $k=1$ vs $k=2$）。
    *   对比了纯 Fubini-Study 度规与解析修正后的度规精度。

### 4. 资源与算力
*   **算力设备**：使用了牛津大学理论物理系的 Hydra 计算集群。
*   **训练细节**：
    *   采用了“热启动”（warm-start）策略：仅在第一个模空间点进行 240 个 epoch 的完整训练，后续邻近点只需 10 个 epoch 即可收敛。
    *   每个流形采样了 127 个不同的 Kähler 模比例点。
    *   神经网络架构为 3 层深度、64 维宽度的全连接网络（GeLU 激活函数）。

### 5. 实验数量与充分性
*   **实验规模**：针对两个不同的流形，各选取了 127 个模空间采样点（基于 $t_1/t_2 = p/q$ 的比例）。
*   **充分性评价**：实验设计较为充分。作者不仅验证了不同 $k$ 值的收敛性，还通过数值结果验证了度规是否遵循预期的离散对称性（即使这些对称性并非预先强制加入）。对于 $h^{1,1}=2$ 的情况，127 个采样点足以覆盖一维的模比例空间。

### 6. 主要结论与发现
*   **精度表现**：在 $k=2$ 的截断下，解析表达式能够以 **2% 左右的平均误差** 重现数值 Kähler 势。
*   **模依赖性**：成功提取了系数 $\alpha_i$ 随 $t_1/t_2$ 变化的解析函数（见论文表 1 和表 2），这些函数通常包含对数项和指数项。
*   **对称性验证**：数值结果自发地尊重流形的非自由作用离散对称性，这简化了 Ansatz 并证明了方法的鲁棒性。
*   **首创性**：这是首次实现具有显式 Kähler 模依赖性的 Ricci 平坦度规解析近似。

### 7. 优点
*   **桥梁作用**：成功架起了纯数值机器学习结果与形式化解析构造之间的桥梁。
*   **显式化**：将“黑盒”神经网络转化为可读、可求导的解析公式，极大方便了后续的物理计算。
*   **效率**：通过热启动策略显著降低了在整个模空间采样所需的计算成本。

### 8. 不足与局限
*   **维度限制**：目前仅展示了 $h^{1,1}=2$ 的案例。对于 $h^{1,1}$ 较大的流形，模空间的采样和符号回归的复杂度将呈指数级增长。
*   **复结构固定**：实验中固定了复结构模，尚未实现 Kähler 模与复结构模的同时解析依赖。
*   **误差传递**：虽然 Kähler 势的拟合精度很高，但度规涉及势函数的二阶导数，拟合误差在求导过程中可能会被放大，导致 Ricci 平坦条件的保持能力下降。
*   **解析形式的任意性**：符号回归给出的函数形式虽然简洁，但缺乏深层的几何原理支撑（即为什么是这种函数形式而非另一种）。

（完）
