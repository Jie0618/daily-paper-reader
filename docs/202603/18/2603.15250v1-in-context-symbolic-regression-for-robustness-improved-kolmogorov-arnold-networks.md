---
title: In-Context Symbolic Regression for Robustness-Improved Kolmogorov-Arnold Networks
title_zh: 用于鲁棒性增强的柯尔莫哥洛夫-阿诺德网络的上下文符号回归
authors: "Francesco Sovrano, Lidia Losavio, Giulia Vilone, Marc Langheinrich"
date: 2026-03-16
pdf: "https://arxiv.org/pdf/2603.15250v1"
tags: ["query:sr-alg"]
score: 9.0
evidence: 研究用于Kolmogorov-Arnold网络的上下文符号回归
tldr: "本研究针对Kolmogorov-Arnold网络（KAN）在符号回归中提取解析表达式不稳定的问题，提出了上下文符号回归方法。传统方法因独立拟合各边函数而对初始化敏感，且忽略了全局交互。本文引入了贪婪上下文符号回归（GSR）和门控匹配追踪（GMP）两种策略，通过端到端损失评估和可微门控层优化算子选择。实验表明，该方法显著增强了模型鲁棒性，在公式恢复的一致性和预测精度上均有大幅提升，中位测试误差最高降低了99.8%。"
selection_source: fresh_fetch
motivation: 传统的KAN符号化过程由于独立拟合各边函数，导致其对初始化高度敏感且忽略了网络内部的全局交互作用。
method: 提出了贪婪上下文符号回归（GSR）和门控匹配追踪（GMP），分别通过端到端损失优化和可微门控算子层来实现稳健的算子提取。
result: "实验结果显示，该方法在保持公式一致性的同时，将中位OFAT测试均方误差最高降低了99.8%。"
conclusion: 上下文符号回归有效解决了KAN符号提取的鲁棒性瓶颈，为构建可解释的科学机器学习模型提供了更可靠的路径。
---

## 摘要
符号回归旨在用简洁的解析表达式取代黑盒预测器，这些表达式在科学机器学习中可以被检查和验证。柯尔莫哥洛夫-阿诺德网络 (KANs) 非常适合这一目标，因为相邻单元之间的每个连接（即“边”）都由一个可学习的一元函数参数化，原则上该函数可以被符号算子替换。然而在实践中，符号提取是一个瓶颈：标准的 KAN 到符号（KAN-to-symbol）方法孤立地为每个学习到的边函数拟合算子，这使得离散选择对初始化和非凸参数拟合非常敏感，并且忽略了局部替换如何通过整个网络进行交互。我们研究了用于 KAN 中算子提取的上下文符号回归，并提出了两种互补的实例化方法。贪婪上下文符号回归 (GSR) 通过在简短微调后根据端到端损失的改进来选择边替换，从而执行贪婪的上下文选择。门控匹配追踪 (GMP) 通过训练一个可微分的门控算子层来摊销这种上下文选择，该层在每条边的稀疏门后放置一个算子库；收敛后，门被离散化（可选地随后进行简短的上下文贪婪细化过程）。我们通过逐个因素 (OFAT) 超参数扫描来量化鲁棒性，并评估预测误差和恢复公式的定性一致性。在多项实验中，贪婪上下文符号回归在 OFAT 测试均方误差 (MSE) 中位数上实现了高达 99.8% 的降低。

## Abstract
Symbolic regression aims to replace black-box predictors with concise analytical expressions that can be inspected and validated in scientific machine learning. Kolmogorov-Arnold Networks (KANs) are well suited to this goal because each connection between adjacent units (an "edge") is parametrised by a learnable univariate function that can, in principle, be replaced by a symbolic operator. In practice, however, symbolic extraction is a bottleneck: the standard KAN-to-symbol approach fits operators to each learned edge function in isolation, making the discrete choice sensitive to initialisation and non-convex parameter fitting, and ignoring how local substitutions interact through the full network. We study in-context symbolic regression for operator extraction in KANs, and present two complementary instantiations. Greedy in-context Symbolic Regression (GSR) performs greedy, in-context selection by choosing edge replacements according to end-to-end loss improvement after brief fine-tuning. Gated Matching Pursuit (GMP) amortises this in-context selection by training a differentiable gated operator layer that places an operator library behind sparse gates on each edge; after convergence, gates are discretised (optionally followed by a short in-context greedy refinement pass). We quantify robustness via one-factor-at-a-time (OFAT) hyper-parameter sweeps and assess both predictive error and qualitative consistency of recovered formulas. Across several experiments, greedy in-context symbolic regression achieves up to 99.8% reduction in median OFAT test MSE.

---

## 论文详细总结（自动生成）

以下是对论文《In-Context Symbolic Regression for Robustness-Improved Kolmogorov-Arnold Networks》的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：Kolmogorov-Arnold Networks (KANs) 虽然在符号回归（SR）中具有天然优势（边上是可学习的一元函数），但其**符号提取过程存在严重的鲁棒性瓶颈**。
*   **研究动机**：传统的 KAN 符号化方法（如 AutoSym）通常孤立地对每条边的样条函数进行算子拟合。这种“局部拟合”方式存在两个缺陷：
    1.  **不稳定性**：对初始化和非凸优化高度敏感，微小的超参数变化会导致提取出完全不同的公式。
    2.  **误差累积**：忽略了边与边之间的全局交互，局部最优的算子替换可能导致整体网络性能大幅下降。
*   **整体含义**：本文旨在通过“上下文（In-Context）”评估，提升 KAN 在科学机器学习任务中提取符号公式的稳健性和准确性。

### 2. 方法论：核心思想与关键技术
论文提出了两种互补的上下文符号回归策略：

*   **贪婪上下文符号回归 (GSR)**：
    *   **核心思想**：模仿“匹配追踪（Matching Pursuit）”算法。不再孤立拟合，而是逐一尝试算子库中的候选算子。
    *   **流程**：选择重要性最高的边，将其替换为候选算子，对**整个网络**进行简短微调（Fine-tuning），根据端到端（End-to-End）的全局损失改进来决定最终算子。
*   **门控匹配追踪 (GMP)**：
    *   **核心思想**：将算子选择过程集成到训练阶段，实现“摊销”上下文选择。
    *   **关键技术**：
        *   **可微门控层**：每条边维护一个算子库的加权混合，通过 Softmax 门控权重进行选择。
        *   **方差压缩**：使用 `asinh` 变换处理不同算子输出量级差异巨大的问题，稳定优化过程。
        *   **稀疏化约束**：引入熵正则化和周期性的 Top-k 剪枝，强制门控权重趋向离散（即每条边最终只选一个算子）。
        *   **可选细化**：离散化后，可运行简短的 GSR 进行最终校准。

### 3. 实验设计
*   **数据集**：使用符号回归标准基准 **SRBench** 中的 **Feynman** 集合（包含 10 个具有已知物理公式的任务，如 I.10.7, II.6.15a 等）。
*   **对比方法（Baselines）**：
    1.  **AutoSym**：标准 KAN 的局部样条-符号拟合方法。
    2.  **FastKAN + AutoSym**：基于径向基函数（RBF）的 KAN 变体配合局部拟合。
    3.  **GSR / FastKAN + GSR**：本文提出的贪婪上下文方法。
    4.  **GMP**：本文提出的门控训练方法。
*   **评估指标**：测试集均方误差（MSE）、公式恢复的定性一致性、以及通过 **OFAT（逐个因素）** 扫描得到的超参数鲁棒性分布。

### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的 GPU 型号和数量。
*   **训练细节**：
    *   每个拟合阶段固定为 200 个优化步（Steps）。
    *   GSR 的候选算子评估预算为每个算子 100 步。
    *   使用了 Adam 优化器，学习率为 $10^{-2}$。
    *   算子库包含 25 个原始算子（如 sin, exp, log, 幂函数等）。

### 5. 实验数量与充分性
*   **实验规模**：针对 10 个不同的 Feynman 数据集进行了实验。
*   **鲁棒性测试**：采用了严谨的 **OFAT 敏感性扫描**，改变了隐藏层宽度（5 到 100）、正则化强度、剪枝周期和随机种子。
*   **充分性评价**：实验设计较为充分，不仅关注最终精度，更通过“小提琴图”展示了性能在超参数扰动下的分布情况。通过 Mann-Whitney U 检验和 Holm 校正进行了统计显著性分析，增强了结论的说服力。

### 6. 主要结论与发现
*   **鲁棒性大幅提升**：上下文方法（尤其是 GSR）在超参数变化下表现出极高的稳定性，中位测试 MSE 相比 AutoSym 最高降低了 **99.8%**。
*   **全局优于局部**：在 10 个任务中，有 7 个任务由上下文方法取得最优性能，证明了考虑全局交互对符号提取至关重要。
*   **架构影响**：数值参数化方式（样条 vs RBF）会影响鲁棒性，但在大多数情况下，上下文评估的增益超过了架构更换的增益。
*   **效率权衡**：GSR 虽然最稳健但计算成本高；GMP 通过门控机制降低了成本，但在某些任务上由于门控分离较慢，表现略逊于 GSR。

### 7. 优点
*   **视角新颖**：首次系统性地指出并解决了 KAN 符号化过程中的“局部拟合不稳定性”问题。
*   **方法实用**：GSR 逻辑简单直接，GMP 则为大规模 KAN 的自动化符号化提供了可扩展的思路。
*   **评估严谨**：引入 XAI 领域的鲁棒性评估方法（OFAT），而非仅仅追求单一指标的 SOTA。

### 8. 不足与局限
*   **计算开销**：GSR 需要对每个候选算子进行端到端微调，当算子库很大或网络很深时，计算成本将变得非常高昂。
*   **GMP 的调度敏感性**：GMP 的性能高度依赖于剪枝策略和训练时长，若门控未充分收敛即进行剪枝，可能剔除正确的算子路径。
*   **任务覆盖面**：实验主要集中在 Feynman 物理公式上，对于具有高噪声、高维特征或缺乏解析解的现实世界数据集，其表现尚待验证。
*   **算子等价性**：未深入讨论数学上等价但形式不同的表达式（如 $\sin^2 x + \cos^2 x$ vs $1$）在提取过程中的冲突问题。

（完）
