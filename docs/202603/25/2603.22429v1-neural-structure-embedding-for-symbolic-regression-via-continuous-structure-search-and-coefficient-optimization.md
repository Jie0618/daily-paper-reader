---
title: Neural Structure Embedding for Symbolic Regression via Continuous Structure Search and Coefficient Optimization
title_zh: 基于连续结构搜索与系数优化的符号回归神经结构嵌入
authors: "Fateme Memar, Tao Zhe, Dongjie Wang"
date: 2026-03-23
pdf: "https://arxiv.org/pdf/2603.22429v1"
tags: ["query:sr-alg"]
score: 10.0
evidence: 基于 Transformer 的符号回归连续结构搜索框架
tldr: 符号回归旨在从数据中发现可解释方程，但传统离散搜索方法效率低且扩展性差。本文提出SRCO框架，通过Transformer将符号结构压缩至连续嵌入空间，实现了高效的连续结构搜索与梯度系数优化。该方法将符号发现与连续优化相结合，在多个数据集上展现出优于现有技术的准确性、鲁棒性和搜索效率，为符号回归提供了新范式。
selection_source: fresh_fetch
motivation: 传统的离散结构搜索方法在处理大规模方程空间时存在计算成本高、性能不稳定且难以扩展的问题。
method: 提出SRCO框架，利用Transformer构建连续结构嵌入空间，并结合梯度搜索与系数优化来高效发现方程。
result: 实验结果表明，该方法在合成和真实数据集上的方程准确性、鲁棒性及搜索效率均显著优于现有最先进方法。
conclusion: 本研究通过将符号方程发现转化为连续嵌入学习与优化问题，为符号回归领域引入了一种高效的新范式。
---

## 摘要
符号回归旨在发现能够解释观测数据且具有人类可解释性的方程。然而，现有方法严重依赖离散结构搜索（如遗传规划），这往往导致计算成本高、性能不稳定，且在大规模方程空间中的可扩展性有限。为应对这些挑战，我们提出了 SRCO，这是一个统一的嵌入驱动符号回归框架，它将符号结构转换为连续且可优化的表示空间。该框架由三个关键组件组成：(1) 结构嵌入：我们首先利用传统符号回归算法生成大量探索性方程池，并训练 Transformer 模型将符号结构压缩到连续嵌入空间中；(2) 连续结构搜索：嵌入空间支持使用基于梯度或基于采样的优化进行高效探索，显著降低了在组合结构空间中导航的成本；(3) 系数优化：对于发现的每个结构，我们将符号系数视为可学习参数，并应用梯度优化以获得准确的数值。在合成数据集和真实世界数据集上的实验表明，我们的方法在方程准确性、鲁棒性和搜索效率方面始终优于现有最先进的方法。本研究通过将符号方程发现与连续嵌入学习及优化相结合，为符号回归引入了一种新的范式。

## Abstract
Symbolic regression aims to discover human-interpretable equations that explain observational data. However, existing approaches rely heavily on discrete structure search (e.g., genetic programming), which often leads to high computational cost, unstable performance, and limited scalability to large equation spaces. To address these challenges, we propose SRCO, a unified embedding-driven framework for symbolic regression that transforms symbolic structures into a continuous, optimizable representation space. The framework consists of three key components: (1) structure embedding: we first generate a large pool of exploratory equations using traditional symbolic regression algorithms and train a Transformer model to compress symbolic structures into a continuous embedding space; (2) continuous structure search: the embedding space enables efficient exploration using gradient-based or sampling-based optimization, significantly reducing the cost of navigating the combinatorial structure space; and (3) coefficient optimization: for each discovered structure, we treat symbolic coefficients as learnable parameters and apply gradient optimization to obtain accurate numerical values. Experiments on synthetic and real-world datasets show that our approach consistently outperforms state-of-the-art methods in equation accuracy, robustness, and search efficiency. This work introduces a new paradigm for symbolic regression by bridging symbolic equation discovery with continuous embedding learning and optimization.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **SRCO**（Symbolic Regression via Continuous structure search and coefficient Optimization）的新型符号回归框架。以下是对该论文的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：传统的符号回归（SR）方法（如遗传规划 GP）主要在**离散符号空间**中进行组合搜索。
*   **研究动机**：
    *   **不稳定性**：离散结构的微小变化会导致函数输出的剧烈波动，优化景观极不平滑。
    *   **耦合难题**：方程结构搜索与数值系数优化往往耦合在一起，导致搜索空间巨大且效率低下。
    *   **缺乏泛化性**：现有神经符号回归方法多是逐个 Token 生成，缺乏能够捕捉结构相似性的连续嵌入表示，难以复用已发现的结构模式。

### 2. 方法论
SRCO 提出了一个三阶段的解耦框架，将符号回归转化为连续空间中的结构搜索与梯度驱动的系数优化：
1.  **结构嵌入（Structure Embedding）**：
    *   利用传统的 GP 工具（如 `gplearn`）生成大量候选方程。
    *   **系数抽象化**：将方程中的所有常数替换为统一占位符 `COF`，并转换为逆波兰表达式（Postfix）。
    *   **Transformer 训练**：训练一个 Transformer 模型作为结构先验，学习符号组合的概率分布，从而将离散结构映射到连续的嵌入空间。
2.  **连续结构搜索（Continuous Structure Search）**：
    *   利用训练好的 Transformer 进行受限采样（Top-k 采样、温度控制）。
    *   通过语法解析器、语义约束和复杂度过滤，筛选出合法的方程模板。
3.  **系数优化（Coefficient Optimization）**：
    *   将模板中的 `COF` 实例化为可学习参数 $w$。
    *   在固定结构的前提下，利用**梯度下降**（最小化 MSE）对系数进行精确拟合。

### 3. 实验设计
*   **数据集**：采用了经典的 **Feynman 物理方程基准测试**，分为 **Feynman-synthetic**（合成数据）和 **Feynman-real-world**（真实世界数据）两个子集。
*   **难度分级**：每个数据集包含 Easy（简单）、Medium（中等）、Hard（困难）三个等级。
*   **对比基准（Baselines）**：
    *   **DSO** (Deep Symbolic Optimization)：基于强化学习的方法。
    *   **FFX** (Fast Function Extraction)：确定性稀疏回归。
    *   **EFS** (Evolutionary Feature Synthesis)：进化特征合成。
    *   **gplearn**：标准的遗传规划方法。
*   **评估指标**：$R^2$（决定系数）、Pearson $\rho$（相关系数）、MSE（均方误差）。

### 4. 资源与算力
*   **算力说明**：论文**未明确列出**具体的 GPU 型号、数量或具体的训练时长。
*   **实现细节**：提到了使用 Transformer 架构处理序列，并使用 `gplearn` 生成了 1000 个初始种子方程作为训练语料。

### 5. 实验数量与充分性
*   **实验规模**：
    *   在两个大类基准（合成与真实）的 6 个难度梯度上进行了全面测试。
    *   **消融实验**：对比了梯度优化与随机爬山算法（Hill-climbing）在系数拟合上的差异。
    *   **鲁棒性测试**：在 0% 到 100% 的特征噪声干扰下测试模型性能。
    *   **参数敏感性**：分析了方程最大项数（max_term）和三角函数变量数（max_trig_vars）对结果的影响。
    *   **效率评估**：对比了各方法的推理耗时。
*   **充分性评价**：实验设计较为全面，涵盖了准确性、鲁棒性、效率和超参数分析，对比了多种主流 SR 范式，实验结果具有较强的说服力。

### 6. 主要结论与发现
*   **性能领先**：SRCO 在所有难度等级和数据集上均优于 DSO、FFX 等基准方法，尤其在 Hard 等级下表现出更强的稳定性。
*   **解耦优势**：通过将结构搜索与系数优化分离，利用梯度下降拟合系数比随机搜索更高效且精确（$R^2$ 从 0.928 提升至 0.999）。
*   **高效推理**：SRCO 生成的方程紧凑，推理速度极快（约 0.006s），比 `gplearn` 快约 38 倍。
*   **抗噪性**：在高达 70% 的噪声下，模型仍能保持 0.95 以上的 $R^2$。

### 7. 优点（亮点）
*   **范式创新**：成功将符号回归从纯离散搜索引入了“连续嵌入+梯度优化”的新路径。
*   **结构先验**：通过 Transformer 学习符号组合规律，有效利用了已有的符号知识。
*   **可解释性与效率并重**：在保持方程简洁、可解释的同时，解决了传统方法难以收敛到精确数值系数的问题。

### 8. 不足与局限
*   **语料依赖**：结构嵌入的质量高度依赖于第一阶段 `gplearn` 生成的初始方程池，如果初始池缺乏多样性，可能会限制搜索空间。
*   **基准局限**：实验主要集中在 Feynman 物理方程上，对于更复杂的生物、化学或金融领域的非线性系统泛化能力尚待验证。
*   **算力细节缺失**：缺乏训练阶段的资源消耗说明，难以评估其在大规模预训练时的成本。

（完）
