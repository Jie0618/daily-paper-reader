---
title: Symbolic Graph Networks for Robust PDE Discovery from Noisy Sparse Data
title_zh: 符号图网络：从噪声稀疏数据中鲁棒地发现偏微分方程
authors: "Xingyu Chen, Junxiu An, Jun Guo, Yuqian Zhou"
date: 2026-03-23
pdf: "https://arxiv.org/pdf/2603.22380v1"
tags: ["query:sr-alg"]
score: 10.0
evidence: 用于从噪声数据中稳健发现偏微分方程的符号图网络
tldr: 针对从噪声大且采样稀疏的观测数据中发现偏微分方程（PDE）的难题，本文提出了一种符号图网络（SGN）框架。该方法利用图消息传递机制建模空间交互，提供对高频噪声不敏感的非局部表示，并结合符号回归模块提取可解释的数学表达式。在波动方程、对流扩散方程及纳维-斯托克斯方程等基准测试中，SGN展现出优于传统方法的鲁棒性，为从不完美观测中发现物理规律提供了新思路。
selection_source: fresh_fetch
motivation: 现有的PDE发现方法在面对高噪声和稀疏采样数据时，由于过度依赖局部微分近似而表现不佳。
method: 提出SGN框架，通过图神经网络的消息传递机制学习非局部空间特征，并利用符号回归模块提取解析表达式。
result: 在多种物理系统上的实验表明，SGN在不同噪声水平下均能准确恢复控制方程，且鲁棒性显著优于基准方法。
conclusion: 将图表示学习与符号回归相结合，是实现从不完美观测数据中稳健发现物理规律的有效途径。
---

## 摘要
数据驱动的偏微分方程（PDE）发现为从观测数据中揭示支配物理规律提供了一种极具前景的范式。然而，在实际场景中，测量值往往受到噪声污染并受限于稀疏采样，这对基于数值微分或积分形式的现有方法提出了重大挑战。在这项工作中，我们提出了一种符号图网络（SGN）框架，用于在噪声和稀疏条件下进行 PDE 发现。SGN 不依赖于局部微分近似，而是利用图消息传递来建模空间相互作用，提供了一种对高频噪声不敏感的非局部表示。基于这种表示，学习到的潜特征由符号回归模块进一步处理，以提取可解释的数学表达式。我们在多个基准系统上评估了所提方法，包括波动方程、对流扩散方程和不可压缩纳维-斯托克斯（Navier-Stokes）方程。实验结果表明，SGN 能够在不同噪声水平下恢复有意义的支配关系或解的形式，并在稀疏和噪声设置下表现出比基准方法更强的鲁棒性。这些结果表明，将基于图的表示与符号回归相结合，为从不完善的观测中鲁棒地进行数据驱动的物理规律发现提供了一个可行的方向。代码可在 https://github.com/CXY0112/SGN 获取。

## Abstract
Data-driven discovery of partial differential equations (PDEs) offers a promising paradigm for uncovering governing physical laws from observational data. However, in practical scenarios, measurements are often contaminated by noise and limited by sparse sampling, which poses significant challenges to existing approaches based on numerical differentiation or integral formulations. In this work, we propose a Symbolic Graph Network (SGN) framework for PDE discovery under noisy and sparse conditions. Instead of relying on local differential approximations, SGN leverages graph message passing to model spatial interactions, providing a non-local representation that is less sensitive to high frequency noise. Based on this representation, the learned latent features are further processed by a symbolic regression module to extract interpretable mathematical expressions. We evaluate the proposed method on several benchmark systems, including the wave equation, convection-diffusion equation, and incompressible Navier-Stokes equations. Experimental results show that SGN can recover meaningful governing relations or solution forms under varying noise levels, and demonstrates improved robustness compared to baseline methods in sparse and noisy settings. These results suggest that combining graph-based representations with symbolic regression provides a viable direction for robust data-driven discovery of physical laws from imperfect observations. The code is available at https://github.com/CXY0112/SGN

---

## 论文详细总结（自动生成）

### 论文总结：用于从噪声稀疏数据中鲁棒发现 PDE 的符号图网络 (SGN)

#### 1. 核心问题与整体含义（研究动机和背景）
本文旨在解决从**高噪声**且**采样稀疏**的观测数据中自动发现偏微分方程（PDE）的难题。
*   **背景**：传统的 PDE 发现方法（如 SINDy, PDE-FIND）依赖局部数值微分，在面对噪声时会产生指数级的误差放大；而基于积分的弱形式方法（如 Weak-PDE-LEARN）虽然抗噪，但对数据密度要求极高，难以处理稀疏采样。
*   **核心问题**：如何在不依赖脆弱的局部微分算子和过度依赖稠密网格的前提下，提取出具有物理可解释性的控制方程？

#### 2. 方法论：核心思想与关键技术
论文提出了 **Symbolic Graph Network (SGN)** 框架，将图神经网络（GNN）的非局部表示能力与符号回归的可解释性相结合。
*   **核心思想**：将空间交互建模为图上的消息传递（Message Passing）。从泛函分析角度看，图聚合操作等效于非局部积分算子，根据 Riemann-Lebesgue 引理，这种积分机制天然具有低通滤波特性，能有效抵消高频观测噪声。
*   **关键技术流程**：
    1.  **图神经网络模拟器 (Graph Neural Simulator)**：
        *   **几何预热**：使用 Savitzky-Golay (S-G) 多项式滤波器对原始噪声数据进行初步平滑。
        *   **消息传递机制**：通过 MLP 参数化消息函数 $\psi$ 和更新函数 $\phi$，将空间相互作用转化为节点间的特征流动。
        *   **对抗训练**：引入动态噪声注入策略，增强模型对数据扰动的鲁棒性。
    2.  **符号逆分析器 (Symbolic Inverse Analyzer)**：
        *   利用 **PySR**（基于遗传算法的符号回归引擎）对 GNN 学习到的潜特征进行蒸馏。
        *   **分阶段发现**：先通过符号回归找回消息函数（空间交互规律），再找回更新函数（时间演化规律），最终组合成完整的解析表达式。

#### 3. 实验设计
*   **物理系统（数据集）**：
    *   **2D 波动方程**：测试基础算子恢复能力。
    *   **2D 对流扩散方程**：测试处理非线性输运和边界截断的能力。
    *   **2D 不可压缩纳维-斯托克斯 (Navier-Stokes) 方程**：测试处理高维、混沌及非局部压力约束的能力。
*   **Benchmark（对比方法）**：主要对比了 **PDE-Net 2.0**（一种结合卷积神经网络与数值微分的代表性强形式方法）。
*   **实验变量**：设置了从 0% 到 10% 的不同高斯噪声水平，并在 32×32 的稀疏网格上进行测试。

#### 4. 资源与算力
*   **算力说明**：论文中**未明确提及**具体的硬件配置（如 GPU 型号、数量）以及具体的训练时长。
*   **代码实现**：作者提供了 GitHub 开源仓库，表明实验基于 Python 环境，使用了 PyTorch 和 PySR 库。

#### 5. 实验数量与充分性
*   **实验组数**：针对三种物理系统，分别在约 7 个噪声梯度（0%, 0.1%, 0.2%, 0.5%, 1.0%, 2.0%, 5.0%, 10.0%）下进行了详尽的定量对比。
*   **充分性评价**：实验设计较为充分。不仅提供了 MSE 误差分析，还展示了发现的符号公式与真实物理方程的对比。
*   **客观性**：通过展示 PDE-Net 2.0 在噪声超过 0.2% 时即出现数值爆炸（NaN）的现象，有力地证明了 SGN 在极端条件下的生存能力。不过，论文未提供针对不同网格稀疏度的消融实验。

#### 6. 主要结论与发现
*   **鲁棒性极强**：SGN 在 10% 的极高噪声下仍能保持结构稳定，而基准方法在微小噪声下就会因数值微分失效而崩溃。
*   **多模态发现**：SGN 具有灵活性，在低噪声下能直接发现闭式解析解（如 $u = \sin(x)\sin(y)\cos(t)$），在高噪声下则能退而求其次发现稳定的离散时间步更新规则。
*   **物理一致性**：在 N-S 方程实验中，SGN 能够以极高精度（误差 < 0.0006%）提取出流体能量耗散率（粘性系数相关的衰减因子），并成功绕过了复杂的压力项求解陷阱。

#### 7. 优点与亮点
*   **架构创新**：将 PDE 发现从“局部微分近似”转向“非局部图聚合”，从根本上解决了噪声敏感性问题。
*   **理论支撑**：通过数学证明展示了图消息传递与积分算子的等效性，为其抗噪性提供了理论依据。
*   **网格无关性**：作为一种 Mesh-free 方法，SGN 不受传统卷积核对规则网格依赖的限制，更易扩展至复杂几何区域。

#### 8. 不足与局限
*   **表示能力权衡**：为了使符号回归可收敛，必须限制图消息的维度（通常为 1 或 2 维），这可能导致模型在处理极其复杂的耦合物理效应时丢失细节。
*   **过度平滑风险**：图聚合本质是低通滤波，虽然滤除了噪声，但也可能模糊掉物理系统中的高频特征或间断（如激波、突变界面）。
*   **计算复杂度**：符号回归阶段（PySR）通常涉及大规模的遗传算法搜索，对于超大规模系统的实时发现可能存在效率瓶颈。

（完）
