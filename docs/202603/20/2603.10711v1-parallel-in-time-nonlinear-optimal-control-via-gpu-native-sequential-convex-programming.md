---
title: Parallel-in-Time Nonlinear Optimal Control via GPU-native Sequential Convex Programming
title_zh: 基于 GPU 原生序列凸规划的时间并行非线性最优控制
authors: "Yilin Zou, Zhong Zhang, Fanghua Jiang"
date: 2026-03-11
pdf: "https://arxiv.org/pdf/2603.10711v1"
tags: ["query:opt-lm"]
score: 8.0
evidence: 针对非线性约束系统的GPU原生轨迹优化
tldr: "针对非线性约束自主系统的实时轨迹优化，本文提出了一种完全基于GPU原生的框架。该框架结合了序列凸规划（SCP）与基于共识的交替方向乘子法（ADMM），通过时间分割策略将优化时域解耦为大规模并行子问题。实验证明，该方法在无人机敏捷飞行和火星着陆任务中，相比高性能CPU基准实现了4倍吞吐量提升和51%的能耗降低，支持高频实时规划。"
selection_source: carryover_cache
motivation: 传统的CPU串行求解器受限于全局稀疏线性代数或动态规划的串行特性，难以充分利用GPU的大规模并行计算能力。
method: 提出一种结合SCP与时间分割ADMM的GPU原生算法，将优化时域解耦为独立的并行子问题，避免了昂贵的内存传输和大规模稀疏分解。
result: "在嵌入式平台上验证显示，该算法实现了超过96%的GPU利用率，吞吐量提升4倍，能耗降低51%，且规划频率超过100Hz。"
conclusion: 该框架为非线性最优控制提供了高效、可扩展的并行求解方案，并能有效支持鲁棒模型预测控制等复杂场景。
---

## 摘要
非线性约束自主系统的实时轨迹优化至关重要，通常由基于 CPU 的序列求解器执行。具体而言，对全局稀疏线性代数的依赖或动态规划算法的串行特性限制了对 GPU 等大规模并行计算架构的利用。为弥补这一差距，我们引入了一个完全 GPU 原生的轨迹优化框架，该框架将序列凸规划与基于共识的交替方向乘子法（ADMM）相结合。通过应用时间拆分策略，我们的算法将优化时域解耦为独立的、按节点划分的子问题，从而实现大规模并行执行。整个过程完全在 GPU 上运行，消除了昂贵的内存传输和大规模稀疏分解。该架构可自然扩展到多轨迹优化。我们在边缘计算平台上，通过四旋翼飞行器敏捷飞行任务和火星动力下降问题验证了该求解器。基准测试显示，与经过深度优化的 12 核 CPU 基准相比，吞吐量持续提升了 4 倍，能耗降低了 51%。至关重要的是，该框架使硬件达到饱和状态，保持 96% 以上的 GPU 活跃利用率，从而实现超过 100 Hz 的规划频率。此外，我们通过联合优化随机扰动下的动态耦合场景，展示了该求解器在鲁棒模型预测控制（MPC）方面的可扩展性，从而实现了可扩展且安全的自主性。

## Abstract
Real-time trajectory optimization for nonlinear constrained autonomous systems is critical and typically performed by CPU-based sequential solvers. Specifically, reliance on global sparse linear algebra or the serial nature of dynamic programming algorithms restricts the utilization of massively parallel computing architectures like GPUs. To bridge this gap, we introduce a fully GPU-native trajectory optimization framework that combines sequential convex programming with a consensus-based alternating direction method of multipliers. By applying a temporal splitting strategy, our algorithm decouples the optimization horizon into independent, per-node subproblems that execute massively in parallel. The entire process runs fully on the GPU, eliminating costly memory transfers and large-scale sparse factorizations. This architecture naturally scales to multi-trajectory optimization. We validate the solver on a quadrotor agile flight task and a Mars powered descent problem using an on-board edge computing platform. Benchmarks reveal a sustained 4x throughput speedup and a 51% reduction in energy consumption over a heavily optimized 12-core CPU baseline. Crucially, the framework saturates the hardware, maintaining over 96% active GPU utilization to achieve planning rates exceeding 100 Hz. Furthermore, we demonstrate the solver's extensibility to robust Model Predictive Control by jointly optimizing dynamically coupled scenarios under stochastic disturbances, enabling scalable and safe autonomy.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **ucenter** 的 GPU 原生轨迹优化框架，旨在解决非线性约束自主系统在实时规划中的计算瓶颈。以下是对该论文的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：传统的非线性最优控制（OCP）求解器（如基于 SQP 或 iLQR 的方法）高度依赖 CPU 的串行逻辑或全局稀疏线性代数运算。这种串行特性导致其难以利用 GPU 的大规模并行能力，成为复杂机器人任务（如敏捷飞行、精确着陆）实时规划的瓶颈。
*   **研究动机**：随着机器人任务复杂化，需要处理高维状态、硬约束及不确定性。现有的 GPU 加速方案往往在非线性前向模拟或稀疏矩阵分解上存在串行依赖。本文旨在通过“时间并行”策略，实现一个完全在 GPU 上运行、无 CPU-GPU 同步开销的高效求解器。

### 2. 方法论：核心思想与技术细节
该框架采用两层分层架构：
*   **外层循环（SCP 接口）**：使用序列凸规划（SCP）将非线性动力学和非凸成本函数在当前轨迹附近进行线性化和二次近似，转化为一系列凸二次规划（QP）子问题。
*   **内层循环（并行共识 ADMM）**：这是本文的核心创新。
    *   **时间分割策略**：不将轨迹视为连续链条，而是将每个时间节点（配置点）视为独立实体。
    *   **变量拆分**：引入物理变量（$x, u$）、动态辅助变量（$z$）和几何镜像变量（$\hat{x}, \hat{u}$）。
    *   **三层更新逻辑**：
        1.  **物理层**：求解独立的每步稠密线性系统（Cholesky 分解），计算最优状态和控制。
        2.  **动态层**：通过闭式解（加权平均）强制相邻节点间的动力学一致性。
        3.  **几何层**：通过近端算子（Proximal Operator，如简单的 Clamp 操作）处理硬约束（如推力限制、信赖域）。
*   **GPU 原生实现**：利用 JAX 框架的 `vmap`（矢量化映射）和 `jit`（即时编译），避免了全局稀疏矩阵分解，将计算映射到 GPU 的流处理器上。

### 3. 实验设计
*   **实验场景**：
    1.  **四旋翼敏捷飞行**：6 自由度模型，包含障碍物规避、执行器限制和高频重规划需求。
    2.  **火星动力下降着陆**：14 维状态，涉及质量消耗、推力矢量控制、滑移坡度约束及燃料优化。
*   **Benchmark（对比基准）**：
    *   **硬件基准**：高度优化的 12 核 CPU 并行基准（基于 iLQR，使用多进程和 Numba 加速）。
    *   **算法验证**：对比了不同惩罚参数 $\rho$ 和 ADMM 迭代次数对收敛性的影响。
*   **对比维度**：吞吐量（Hz）、计算耗时、能耗（焦耳）、硬件利用率、成功率。

### 4. 资源与算力
*   **硬件平台**：**Nvidia Jetson AGX Orin 64GB**（高性能嵌入式边缘计算平台）。
*   **软件框架**：基于 Python 和 **JAX** 后端。
*   **算力表现**：在处理大规模批次（如 5000 条轨迹）时，GPU 活跃利用率稳定在 **96% 以上**，实现了硬件饱和运行。

### 5. 实验数量与充分性
*   **实验规模**：
    *   进行了消融实验，测试了 4 种不同的惩罚参数和 4 种 ADMM 迭代步数。
    *   进行了大规模可扩展性测试，批处理规模从 1 跨越到 5000。
    *   在随机障碍物环境中进行了 1000 次蒙特卡洛模拟，验证成功率（四旋翼 93.9%，火星着陆 99.8%）。
    *   展示了鲁棒 MPC 场景，同时优化 15 个耦合场景。
*   **充分性评价**：实验设计较为充分，涵盖了从单轨迹实时性到大规模批处理吞吐量的全方位评估，且在嵌入式硬件上进行了实测，具有较强的说服力。

### 6. 主要结论与发现
*   **性能提升**：相比 12 核 CPU 基准，GPU 求解器实现了 **4.1 倍的吞吐量加速**。
*   **能效比**：在处理相同任务时，**能耗降低了 51%**，这对电池驱动的机器人至关重要。
*   **实时性**：规划频率超过 **100 Hz**，完全满足实时 MPC 的需求。
*   **鲁棒性**：通过并行优化多个扰动场景（Scenario Optimization），能够实时生成考虑不确定性的安全轨迹“管道”。

### 7. 优点与亮点
*   **完全去中心化**：通过时间分割避开了 GPU 最忌讳的稀疏矩阵分解和串行 Riccati 递归。
*   **硬件饱和**：极高的 GPU 利用率（>96%）证明了算法与 SIMT 架构的高度契合。
*   **多任务扩展**：天然支持“系综求解”（Ensemble Solving），可同时处理数百个不同初始条件或扰动下的轨迹，为数据驱动控制（如专家演示生成）提供了极速工具。

### 8. 不足与局限
*   **理论收敛性**：论文提到内层 ADMM 的部分收敛与外层 SCP 迭代之间的理论相互作用仍需进一步研究。
*   **应用限制**：目前仅在四旋翼和火星着陆两个特定动力学模型上验证，对于更高维度（如多足机器人或复杂机械臂）的适用性有待观察。
*   **Python 开销**：虽然使用了 JAX JIT，但在极低延迟需求下，Python 运行时的开销可能仍高于纯 C++/CUDA 实现。

（完）
