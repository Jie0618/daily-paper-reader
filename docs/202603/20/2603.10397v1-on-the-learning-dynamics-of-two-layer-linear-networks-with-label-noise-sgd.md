---
title: On the Learning Dynamics of Two-layer Linear Networks with Label Noise SGD
title_zh: 关于带有标签噪声 SGD 的两层线性网络学习动力学研究
authors: "Tongcheng Zhang, Zhanpeng Zhou, Mingze Wang, Andi Han, Wei Huang, Taiji Suzuki, Junchi Yan"
date: 2026-03-11
pdf: "https://arxiv.org/pdf/2603.10397v1"
tags: ["query:opt-lm"]
score: 8.0
evidence: 带有标签噪声的SGD学习动力学与隐式偏差
tldr: 本研究深入探讨了带标签噪声的随机梯度下降（SGD）在两层超参数化线性网络中的学习动力学。研究揭示了标签噪声如何诱导模型从“懒惰制度”转向“丰富制度”，并将其学习过程分为权重减小以进入丰富制度和权重对齐以实现收敛两个阶段。这一发现不仅解释了标签噪声提升泛化能力的内在机制，还成功扩展至SAM等优化算法，并通过实验验证了理论的普适性。
selection_source: carryover_cache
motivation: 旨在探究带标签噪声的SGD在提升模型泛化能力背后的隐式偏差及其动力学机制。
method: 通过对两层超参数化线性网络进行理论建模，分析了标签噪声对模型权重演化和制度转换的影响。
result: 研究发现学习过程包含两个阶段：模型首先脱离懒惰制度进入丰富制度，随后权重与真实插值器对齐并最终收敛。
conclusion: 标签噪声是驱动模型向丰富制度转变的关键，且该动力学原理可推广至SAM等更广泛的优化算法中。
---

## 摘要
深度学习成功的关键因素之一在于基于梯度的训练算法中固有噪声所诱导的隐式偏置。受“使用噪声标签训练能提高模型泛化能力”这一经验观察的启发，我们深入探讨了带有标签噪声的随机梯度下降（SGD）背后的底层机制。通过聚焦于两层过参数化线性网络，我们分析了标签噪声 SGD 的学习动力学，揭示了一个两阶段的学习行为。在第一阶段，模型权重的量级逐渐减小，模型脱离了懒惰机制（lazy regime）并进入丰富机制（rich regime）。在第二阶段，模型权重与真实插值器（ground-truth interpolator）之间的对齐度增加，模型最终收敛。我们的分析强调了标签噪声在驱动从懒惰机制向丰富机制转变中的关键作用，并对其经验上的成功给出了极简解释。此外，我们将这些见解扩展到锐度感知最小化（SAM），表明支配标签噪声 SGD 的原理也适用于更广泛的优化算法。在合成和真实场景下进行的大量实验有力地支持了我们的理论。我们的代码已发布在 https://github.com/a-usually/Label-Noise-SGD。

## Abstract
One crucial factor behind the success of deep learning lies in the implicit bias induced by noise inherent in gradient-based training algorithms. Motivated by empirical observations that training with noisy labels improves model generalization, we delve into the underlying mechanisms behind stochastic gradient descent (SGD) with label noise. Focusing on a two-layer over-parameterized linear network, we analyze the learning dynamics of label noise SGD, unveiling a two-phase learning behavior. In \emph{Phase I}, the magnitudes of model weights progressively diminish, and the model escapes the lazy regime; enters the rich regime. In \emph{Phase II}, the alignment between model weights and the ground-truth interpolator increases, and the model eventually converges. Our analysis highlights the critical role of label noise in driving the transition from the lazy to the rich regime and minimally explains its empirical success. Furthermore, we extend these insights to Sharpness-Aware Minimization (SAM), showing that the principles governing label noise SGD also apply to broader optimization algorithms. Extensive experiments, conducted under both synthetic and real-world setups, strongly support our theory. Our code is released at https://github.com/a-usually/Label-Noise-SGD.

---

## 论文详细总结（自动生成）

这篇论文对带有标签噪声的随机梯度下降（Label Noise SGD）在两层线性网络中的学习动力学进行了深入的理论与实证分析。以下是该论文的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：在过参数化模型中，通常被视为有害的“标签噪声”为何能提高模型的泛化能力并诱导稀疏解？
*   **研究动机**：尽管已有研究指出标签噪声具有隐式正则化作用，但大多数理论分析局限于局部几何性质或极简的对角线性网络。本文旨在揭示两层（或多层）可训练参数网络中，标签噪声如何通过影响学习动力学，驱动模型从“懒惰制度”（Lazy Regime）转向“丰富制度”（Rich Regime/Feature Learning）。

### 2. 方法论
*   **核心思想**：将学习过程划分为两个阶段，并证明标签噪声是触发阶段转换的关键动力。
*   **模型设置**：考虑一个两层线性网络 $\hat{y} = a^\top W x$，其中 $W$ 和 $a$ 均为可训练参数。
*   **算法流程**：
    *   **标签噪声 SGD**：在回归任务中，为标签添加方差为 $\sigma^2$ 的噪声 $\epsilon \sim \{-\sigma, \sigma\}$。
    *   **两阶段动力学**：
        *   **阶段 I（逃离懒惰制度）**：证明了第二层参数 $a$ 的剧烈震荡会导致第一层权重 $W$ 的范数逐渐减小（Progressively Diminishing）。这种权重的减小使得模型脱离了参数几乎不动的“懒惰制度”，进入特征学习活跃的“丰富制度”。
        *   **阶段 II（对齐与收敛）**：模型权重开始与真实插值器（Ground-truth Interpolator）方向对齐，模型逐渐收敛并呈现稀疏性。
*   **理论扩展**：将该机制推广至锐度感知最小化（SAM）算法，证明 SAM 同样通过放大噪声诱导类似的制度转换。

### 3. 实验设计
*   **数据集/场景**：
    *   **合成数据**：用于验证神经元级别的对齐、范数演化及两阶段理论。
    *   **真实数据**：使用 CIFAR-10 数据集。
*   **Benchmark 与对比方法**：
    *   对比了 **Vanilla SGD**（无噪声）与 **Label Noise SGD**。
    *   对比了 **线性化模型（NTK 映射）** 与原模型，以区分懒惰与丰富制度。
    *   对比了不同标签翻转概率 $\tau \in \{0.05, 0.1, 0.2\}$ 下的表现。
    *   在扩展实验中对比了 **GD** 与 **SAM**。
*   **评估指标**：测试损失、测试准确率、权重范数 $\|W\|_F$、神经元对齐度、剪枝后的模型性能（验证稀疏性）。

### 4. 资源与算力
*   **算力说明**：论文中**未明确列出**具体的 GPU 型号、数量或总训练时长。
*   **计算复杂度限制**：作者提到在进行 NTK（神经切线核）相关实验时，由于 $O(n^2)$ 的计算复杂度，仅在 CIFAR-10 的 64 张图像子集上运行了 WideResNet 实验。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了从合成回归任务到 ResNet-18/WideResNet 在 CIFAR-10 上的分类任务。
*   **充分性评价**：实验设计较为全面。不仅有宏观的损失/准确率曲线，还有微观的神经元范数和对齐度分析。此外，通过“交替添加/移除噪声”的消融实验进一步验证了噪声在驱动制度转换中的必要性。实验结果在不同噪声水平下保持一致，具有较好的客观性和说服力。

### 6. 主要结论与发现
*   **制度转换**：标签噪声 SGD 是驱动模型从泛化较差的懒惰制度转向具有特征学习能力的丰富制度的有效手段。
*   **震荡诱导减小**：第二层权重的震荡是导致第一层权重范数减小的直接原因，这是逃离懒惰制度的前提。
*   **隐式偏置**：标签噪声不仅能提高泛化，还能诱导模型产生更稀疏的解，使其在剪枝后仍能保持高性能。
*   **普适性**：SAM 算法的成功在很大程度上也可以通过这种“噪声诱导的制度转换”理论来解释。

### 7. 优点
*   **理论突破**：首次在具有两个可训练层的网络中详细刻画了标签噪声 SGD 的完整学习动力学，比之前的单层或对角模型更接近实际。
*   **机制解释**：为“噪声为何有益”提供了一个极简且直观的物理图像（震荡 -> 范数减小 -> 制度转换）。
*   **算法关联**：成功建立了标签噪声 SGD 与 SAM 之间的理论联系。

### 8. 不足与局限
*   **线性限制**：核心理论分析基于线性网络，尚未完全涵盖非线性激活函数（如 ReLU）可能带来的复杂动力学。
*   **任务差异**：理论推导主要基于回归任务（平方损失），而实证部分涉及分类任务，两者之间虽有联系但仍存在数学上的鸿沟。
*   **规模限制**：部分验证丰富制度的实验（如 NTK 对比）受限于计算量，只能在极小规模的数据子集上进行。

（完）
