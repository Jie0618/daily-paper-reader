---
title: Parallel-in-Time Nonlinear Optimal Control via GPU-native Sequential Convex Programming
title_zh: 基于 GPU 原生序列凸规划的时间并行非线性最优控制
authors: "Yilin Zou, Zhong Zhang, Maxime Robic, Fanghua Jiang"
date: 2026-03-11
pdf: "https://arxiv.org/pdf/2603.10711v2"
tags: ["query:opt-lm"]
score: 8.0
evidence: 针对约束系统的 GPU 原生序列凸规划
tldr: "针对非线性约束自主系统的实时轨迹优化，本文提出了一种完全基于GPU原生的框架。该框架结合了序列凸规划（SCP）与基于共识的交替方向乘子法（ADMM），通过时间分割策略将优化问题解耦为可大规模并行执行的子问题。实验证明，该方法在无人机敏捷飞行和火星着陆任务中，相比高性能CPU基准实现了4倍吞吐量提升和51%的能耗降低，支持超过100Hz的规划频率，显著提升了自主系统的计算效率。"
selection_source: carryover_cache
motivation: 传统的CPU串行求解器受限于全局稀疏线性代数或动态规划的串行特性，难以充分利用GPU的大规模并行计算能力进行实时轨迹优化。
method: 提出一种结合SCP与共识ADMM的GPU原生框架，通过时间分割将优化时域解耦为独立的并行子问题，并消除昂贵的内存传输和大规模稀疏分解。
result: "在嵌入式平台上实现了超过100Hz的规划速率，相比12核CPU基准，吞吐量提升4倍，能耗降低51%，且GPU利用率保持在96%以上。"
conclusion: 该框架通过充分利用硬件并行性，为非线性最优控制和鲁棒模型预测控制提供了一种高效、节能且具有高度可扩展性的解决方案。
---

## 摘要
非线性约束自主系统的实时轨迹优化至关重要，通常由基于 CPU 的序列求解器执行。具体而言，对全局稀疏线性代数的依赖或动态规划算法的串行特性限制了对 GPU 等大规模并行计算架构的利用。为弥补这一差距，我们引入了一个完全 GPU 原生的轨迹优化框架，该框架将序列凸规划与基于共识的交替方向乘子法（ADMM）相结合。通过应用时间拆分策略，我们的算法将优化时域解耦为独立的、按节点划分的子问题，从而实现大规模并行执行。整个过程完全在 GPU 上运行，消除了昂贵的内存传输和大规模稀疏分解。该架构可自然扩展到多轨迹优化。我们在机载边缘计算平台上，通过四旋翼飞行器敏捷飞行任务和火星动力下降问题验证了该求解器。基准测试显示，与经过深度优化的 12 核 CPU 基准相比，吞吐量持续提升了 4 倍，能耗降低了 51%。至关重要的是，该框架使硬件达到饱和状态，保持 96% 以上的 GPU 活跃利用率，从而实现超过 100 Hz 的规划频率。此外，我们通过联合优化随机扰动下的动态耦合场景，展示了该求解器在鲁棒模型预测控制（MPC）方面的可扩展性，从而实现了可扩展且安全的自主性。

## Abstract
Real-time trajectory optimization for nonlinear constrained autonomous systems is critical and typically performed by CPU-based sequential solvers. Specifically, reliance on global sparse linear algebra or the serial nature of dynamic programming algorithms restricts the utilization of massively parallel computing architectures like GPUs. To bridge this gap, we introduce a fully GPU-native trajectory optimization framework that combines sequential convex programming with a consensus-based alternating direction method of multipliers. By applying a temporal splitting strategy, our algorithm decouples the optimization horizon into independent, per-node subproblems that execute massively in parallel. The entire process runs fully on the GPU, eliminating costly memory transfers and large-scale sparse factorizations. This architecture naturally scales to multi-trajectory optimization. We validate the solver on a quadrotor agile flight task and a Mars powered descent problem using an on-board edge computing platform. Benchmarks reveal a sustained 4x throughput speedup and a 51% reduction in energy consumption over a heavily optimized 12-core CPU baseline. Crucially, the framework saturates the hardware, maintaining over 96% active GPU utilization to achieve planning rates exceeding 100 Hz. Furthermore, we demonstrate the solver's extensibility to robust Model Predictive Control by jointly optimizing dynamically coupled scenarios under stochastic disturbances, enabling scalable and safe autonomy.

---

## 论文详细总结（自动生成）

这篇论文介绍了一个名为 **ucenter** 的 GPU 原生轨迹优化框架，旨在解决非线性约束自主系统在实时控制中的计算瓶颈。以下是对该论文的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：传统的非线性最优控制（OCP）求解器（如基于 DDP 或 SQP 的方法）高度依赖 CPU 的串行逻辑或全局稀疏线性代数运算，难以充分利用 GPU 的大规模并行能力。
*   **研究动机**：随着机器人任务（如敏捷飞行、火箭着陆）复杂性增加，实时生成大规模、非凸且带约束的轨迹需求日益迫切。现有的 GPU 加速方法往往在非线性前向模拟或处理硬约束时存在瓶颈，导致硬件利用率低且能耗高。

### 2. 方法论：核心思想与技术细节
该框架采用**两层分层架构**，将时间维度解耦以实现并行化：
*   **外层循环（SCP 接口）**：使用序列凸规划（SCP）将非线性动力学和非凸成本函数在当前名义轨迹附近进行线性化和二次近似，转化为一系列凸二次规划（QP）子问题。
*   **内层循环（并行共识 ADMM）**：这是核心创新点。通过**时间拆分策略**，将优化时域解耦为独立的节点：
    *   **变量拆分**：引入物理变量（优化成本）、动态辅助变量（确保时间连续性）和几何镜像变量（处理硬约束）。
    *   **并行求解**：每个时间节点的计算被映射到独立的 GPU 线程块。通过 ADMM 迭代，各节点独立进行稠密矩阵运算、闭式解更新和投影操作，完全避免了全局稀疏矩阵分解。
*   **GPU 原生设计**：整个算法循环（包括线性化、ADMM 迭代、对偶更新）完全在 GPU 上执行，消除了昂贵的 CPU-GPU 内存同步开销。

### 3. 实验设计与 Benchmark
*   **实验场景**：
    1.  **四旋翼敏捷飞行**：6 自由度模型，包含障碍物规避、执行器限制和复杂的非线性动力学。
    2.  **火星动力下降着陆**：14 维状态空间，涉及质量消耗、推力矢量限制、下滑道约束和姿态限制。
*   **Benchmark（对比方法）**：
    *   **CPU 基准**：经过高度优化的 12 核 CPU 并行化 iLQR 算法（使用 Python 多进程和 Numba JIT 编译）。
*   **对比维度**：吞吐量（Hz）、计算耗时、能耗（焦耳）、GPU 利用率、成功率。

### 4. 资源与算力
*   **硬件平台**：**Nvidia Jetson AGX Orin 64GB**（一种高性能嵌入式边缘计算平台）。
*   **软件框架**：基于 **Python 和 JAX** 开发，利用 JAX 的 `vmap`（自动向量化）和 `jit`（即时编译）功能实现高效的张量运算。
*   **算力表现**：在处理 5000 条轨迹的批处理任务时，GPU 活跃利用率超过 **96%**。

### 5. 实验数量与充分性
*   **实验规模**：
    *   进行了**消融实验**，测试了惩罚参数 $\rho$ 和 ADMM 迭代次数对收敛性的影响。
    *   进行了**可扩展性测试**，批处理规模从 1 跨越到 5000。
    *   在随机障碍物环境下进行了 **1000 次蒙特卡洛模拟**，成功率达到 93.9%（四旋翼）和 99.8%（火星着陆）。
    *   展示了**鲁棒 MPC 案例**，同时优化 15 个耦合场景以应对随机阵风干扰。
*   **评价**：实验设计较为充分，涵盖了从单轨迹实时性到大规模批处理吞吐量的全方位评估，对比基准（12 核 CPU）具有较强的说服力。

### 6. 主要结论与发现
*   **高性能**：在边缘平台上实现了超过 **100 Hz** 的规划速率。
*   **高吞吐量**：相比 12 核 CPU 基准，实现了 **4.1 倍** 的持续吞吐量提升。
*   **低能耗**：在处理相同规模的任务时，能耗降低了 **51%**。
*   **多轨迹能力**：该框架天然支持“系综求解”（Ensemble Solving），能够同时优化数百个扰动场景，使鲁棒 MPC 在嵌入式硬件上达到实时可行。

### 7. 优点与亮点
*   **架构创新**：通过时间拆分将原本串行的最优控制问题转化为高度并行的共识问题，极大地适配了 GPU 的 SIMT 架构。
*   **硬件饱和**：超过 96% 的 GPU 利用率证明了该算法几乎消除了计算闲置，充分压榨了硬件性能。
*   **实用性强**：在嵌入式边缘设备上验证，直接面向无人机、航天器等对功耗和实时性敏感的实际应用。

### 8. 不足与局限
*   **收敛性理论**：虽然论文提到 ADMM 在两块拆分下有保证，但其三变量拆分的收敛性主要依赖结构分离的解释，缺乏更深层的非线性 SCP 与内层 ADMM 交互影响的严谨数学证明。
*   **参数敏感性**：ADMM 算法通常对惩罚参数（$\rho$）较为敏感，虽然文中给出了消融实验，但在不同任务间迁移时可能需要精细调优。
*   **物理验证缺失**：目前所有实验均在仿真环境（虽然是在边缘硬件上运行）中完成，尚未在真实的物理机器人上进行闭环飞行验证。

（完）
