---
title: "Rethinking Adam for Time Series Forecasting: A Simple Heuristic to Improve Optimization under Distribution Shifts"
title_zh: 重新审视用于时间序列预测的 Adam：一种改进分布偏移下优化的简单启发式方法
authors: "Yuze Dong, Jinsong Wu"
date: 2026-03-10
pdf: "https://arxiv.org/pdf/2603.10095v1"
tags: ["query:opt-lm"]
score: 8.0
evidence: 针对非平稳目标重新审视并修改 Adam 优化器
tldr: 针对时间序列预测中的非平稳性和分布偏移问题，本文重新审视了Adam优化器，发现其二阶偏差修正限制了对动态损失景观的响应。为此，作者提出了TS_Adam，通过移除二阶修正来增强模型对分布漂移的适应性。该方法简单高效，无需额外超参数，在长短期预测任务中均表现出色，显著提升了模型在现实非平稳数据下的预测精度。
selection_source: carryover_cache
motivation: 传统的Adam优化器在处理具有非平稳分布偏移的时间序列数据时，其二阶偏差修正机制会导致对动态变化的响应滞后。
method: 提出TS_Adam，通过在学习率计算中移除二阶偏差修正，增强优化器对不断演化的数据分布的响应速度。
result: "在ETT数据集上，TS_Adam使MICN模型的平均MSE降低了12.8%，MAE降低了5.7%，显著优于原始Adam。"
conclusion: TS_Adam作为一种简单且无需额外超参数的优化策略，为解决现实世界中非平稳时间序列预测问题提供了有效的方案。
---

## 摘要
时间序列预测经常面临非平稳性的挑战，特别是分布漂移（distributional drift），即数据分布随时间演变。这种动态行为会削弱自适应优化器（如 Adam）的有效性，因为这些优化器通常是为平稳目标设计的。在本文中，我们在非平稳预测的背景下重新审视了 Adam，并发现其二阶偏置校正限制了对不断变化的损失景观（loss landscapes）的响应能力。为了解决这一问题，我们提出了 TS_Adam，这是一种轻量级变体，它从学习率计算中移除了二阶校正。这种简单的修改在保留优化器核心结构且无需额外超参数的情况下，提高了对分布漂移的适应性。TS_Adam 可以轻松集成到现有模型中，并在长期和短期预测任务中持续提升性能。在使用 MICN 模型的 ETT 数据集上，与 Adam 相比，它在 MSE 上平均降低了 12.8%，在 MAE 上平均降低了 5.7%。这些结果强调了 TS_Adam 作为一种有效的优化策略，在涉及非平稳数据的现实预测场景中的实用性和通用性。代码可在以下网址获取：https://github.com/DD-459-1/TS_Adam。

## Abstract
Time-series forecasting often faces challenges from non-stationarity, particularly distributional drift, where the data distribution evolves over time. This dynamic behavior can undermine the effectiveness of adaptive optimizers, such as Adam, which are typically designed for stationary objectives. In this paper, we revisit Adam in the context of non-stationary forecasting and identify that its second-order bias correction limits responsiveness to shifting loss landscapes. To address this, we propose TS_Adam, a lightweight variant that removes the second-order correction from the learning rate computation. This simple modification improves adaptability to distributional drift while preserving the optimizer core structure and requiring no additional hyperparameters. TS_Adam integrates easily into existing models and consistently improves performance across long- and short-term forecasting tasks. On the ETT datasets with the MICN model, it achieves an average reduction of 12.8% in MSE and 5.7% in MAE compared to Adam. These results underscore the practicality and versatility of TS_Adam as an effective optimization strategy for real-world forecasting scenarios involving non-stationary data. Code is available at: https://github.com/DD-459-1/TS_Adam.

---

## 论文详细总结（自动生成）

这篇论文针对时间序列预测中的非平稳性问题，对广泛使用的 Adam 优化器进行了重新审视，并提出了一种简单而有效的改进方案。以下是对该论文的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：时间序列数据具有显著的**非平稳性（Non-stationarity）**，特别是**分布偏移（Distributional Drift）**，即数据的统计属性随时间演变。
*   **研究动机**：传统的自适应优化器（如 Adam）是为平稳目标设计的。作者发现 Adam 的**二阶偏差修正（Second-order Bias Correction）**机制在处理非平稳目标时存在局限性。由于二阶动量衰减系数 $\beta_2$ 通常很大（如 0.999），导致修正项在训练前期长时间限制了有效学习率，使得优化器对快速变化的损失景观（Loss Landscape）响应迟钝，从而影响预测精度。

### 2. 论文提出的方法论：TS_Adam
*   **核心思想**：通过移除 Adam 中的二阶偏差修正项，提高优化器在分布漂移环境下的响应速度和适应能力。
*   **关键技术细节**：
    *   **保留一阶修正**：继续使用 $\hat{m}_t = m_t / (1 - \beta_1^t)$ 来稳定梯度方向。
    *   **移除二阶修正**：直接使用原始二阶动量 $v_t$ 而非修正后的 $\hat{v}_t = v_t / (1 - \beta_2^t)$。
    *   **更新规则**：参数更新公式变为 $\theta_t = \theta_{t-1} - \alpha \cdot \hat{m}_t / (\sqrt{v_t} + \epsilon)$。
*   **理论支撑**：作者利用**动态回归（Dynamic Regret）**边界理论证明，在存在漂移（Drift）的非平稳环境中，适当调高有效学习率（即移除二阶修正的效果）能更好地平衡“噪声抑制”与“漂移追踪”之间的矛盾。

### 3. 实验设计
*   **数据集**：
    *   **长期预测**：ETT 系列（ETTh1, ETTh2, ETTm1, ETTm2）、ECL（电力消耗）、Weather（天气）。
    *   **短期预测**：M4 数据集（包含 10 万条不同频率的序列）。
*   **Benchmark 模型**：采用了三种最先进（SOTA）的架构：**MICN**、**PatchTST** 和 **SegRNN**。
*   **对比方法**：Adam、AdamW、Lookahead、SGD 和 Yogi。
*   **评估指标**：MSE、MAE、sMAPE、MASE 和 OWA。

### 4. 资源与算力
*   **硬件环境**：实验在一台 **NVIDIA RTX 3090 GPU** 上完成。
*   **训练细节**：每个配置固定随机种子运行 3 次，取平均值和标准差。论文未明确给出总训练时长，但提到 TS_Adam 相比 Adam 在每步计算上可减少约 **8.3% 的浮点运算量（FLOPs）**，因为它减少了 $n$ 次除法操作。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了长短期预测、6 个主流数据集、3 种不同类型的深度学习架构，以及 4 种预测长度（96 到 720 步）。
*   **消融与鲁棒性实验**：
    *   对超参数 $\beta_1$ 和学习率 $\alpha$ 的敏感性分析。
    *   不同 Batch Size 的影响。
    *   在存在**高斯噪声**和**极端离群点**情况下的鲁棒性测试。
    *   不同学习率调度策略（如 Cosine Annealing）的兼容性。
*   **充分性评价**：实验设计非常全面，不仅验证了性能提升，还通过统计显著性检验（P-value 和 Bonferroni 校正）证明了结果的客观性和公平性。

### 6. 主要结论与发现
*   **性能显著提升**：在 ETT 数据集上配合 MICN 模型，TS_Adam 相比 Adam 平均降低了 **12.8% 的 MSE** 和 **5.7% 的 MAE**。
*   **收敛更快**：实验观察到 TS_Adam 在大多数任务中具有更快的收敛速度和更低的最终测试损失。
*   **非平稳性关联**：通过 STL 分解发现，TS_Adam 在**季节性成分强**（即分布变化快）的数据集上表现提升最明显。
*   **通用性**：将该策略（移除二阶修正）应用到 AdamW、Yogi 等其他优化器上，同样能获得性能提升。

### 7. 优点
*   **极简且高效**：作为一种“即插即用”的启发式改进，它不需要引入任何额外的超参数，且降低了计算开销。
*   **理论与实证结合**：不仅有直观的启发式想法，还通过动态回归理论解释了为何该修改在非平稳场景下有效。
*   **强鲁棒性**：在噪声、离群点和不同训练配置下均表现稳定，优于标准 Adam。

### 8. 不足与局限
*   **趋势主导序列的局限**：作者指出，在以长期趋势（Trend）为主导而非季节性波动的序列上，TS_Adam 的改进效果相对有限。
*   **初期波动**：移除二阶修正可能导致训练极早期出现轻微的震荡（虽然很快会稳定）。
*   **应用范围限制**：目前主要验证了时间序列预测任务，对于其他非平稳任务（如强化学习或在线学习）的普适性仍需进一步探索。

（完）
