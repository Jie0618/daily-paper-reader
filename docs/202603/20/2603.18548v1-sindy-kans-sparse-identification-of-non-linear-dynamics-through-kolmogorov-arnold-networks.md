---
title: "SINDy-KANs: Sparse identification of non-linear dynamics through Kolmogorov-Arnold networks"
title_zh: SINDy-KANs：通过 Kolmogorov-Arnold 网络进行非线性动力学的稀疏识别
authors: "Amanda A. Howard, Nicholas Zolman, Bruno Jacob, Steven L. Brunton, Panos Stinis"
date: 2026-03-19
pdf: "https://arxiv.org/pdf/2603.18548v1"
tags: ["query:sr-alg"]
score: 10.0
evidence: 利用 Kolmogorov-Arnold 网络进行非线性动力学的稀疏识别
tldr: 针对Kolmogorov-Arnold网络（KAN）在稀疏性和可解释性方面的不足，以及稀疏非线性动力学识别（SINDy）受限于预定义库的问题，本文提出了SINDy-KANs。该方法通过在KAN的每个激活函数层引入SINDy机制，实现了在保持深度网络函数组合能力的同时，自动发现稀疏且精确的动力学方程。实验证明，该方法在多种符号回归和动力学系统任务中表现优异，显著提升了模型的可解释性。
selection_source: fresh_fetch
motivation: 旨在解决KAN模型缺乏稀疏性以及SINDy方法受限于预定义函数库的局限性。
method: 提出SINDy-KANs架构，将SINDy的稀疏识别机制应用于KAN的每个激活函数，实现端到端的稀疏方程学习。
result: 在多个符号回归和动力学系统任务中成功发现了准确且稀疏的数学表达式。
conclusion: SINDy-KANs结合了两者的优势，为构建具有高度可解释性和稀疏性的非线性动力学模型提供了有效途径。
---

## 摘要
Kolmogorov-Arnold 网络 (KANs) 已成为增强机器学习可解释性的一种潜在途径。然而，从稀疏性或简洁性的角度来看，KANs 学习到的解并不一定具有可解释性。非线性动力学稀疏识别 (SINDy) 是一种互补的方法，允许从数据中学习动力系统的稀疏方程；然而，学习到的方程受限于预设的函数库。在这项工作中，我们提出了 SINDy-KANs，它同时训练 KAN 和类 SINDy 表示，通过在每个激活函数层面应用 SINDy 来提高 KAN 表示的可解释性，同时保持了深度 KAN 所能实现的函数复合能力。我们将该方法应用于多项符号回归任务（包括动力系统），展示了在多种系统中准确的方程发现能力。

## Abstract
Kolmogorov-Arnold networks (KANs) have arisen as a potential way to enhance the interpretability of machine learning. However, solutions learned by KANs are not necessarily interpretable, in the sense of being sparse or parsimonious. Sparse identification of nonlinear dynamics (SINDy) is a complementary approach that allows for learning sparse equations for dynamical systems from data; however, learned equations are limited by the library. In this work, we present SINDy-KANs, which simultaneously train a KAN and a SINDy-like representation to increase interpretability of KAN representations with SINDy applied at the level of each activation function, while maintaining the function compositions possible through deep KANs. We apply our method to a number of symbolic regression tasks, including dynamical systems, to show accurate equation discovery across a range of systems.

---

## 论文详细总结（自动生成）

以下是对论文《SINDy-KANs: Sparse identification of non-linear dynamics through Kolmogorov-Arnold networks》的结构化深入总结：

### 1. 论文的核心问题与整体含义
*   **研究动机**：
    *   **KAN 的局限性**：虽然 Kolmogorov-Arnold 网络（KAN）通过可训练的激活函数提供了比传统 MLP 更好的可解释性，但其学习到的函数往往过于复杂，缺乏物理上的“稀疏性”或“简洁性”。
    *   **SINDy 的局限性**：稀疏非线性动力学识别（SINDy）能够发现简洁的控制方程，但它高度依赖于预定义的函数库。如果方程涉及复杂的函数复合（如 $\cos(x^2)$），SINDy 很难在没有先验知识的情况下构建合适的库。
*   **核心问题**：如何结合 KAN 的**深度复合能力**与 SINDy 的**稀疏识别能力**，从而在不需要庞大预定义库的情况下，从数据中自动发现简洁、可解释的非线性动力学方程？

### 2. 论文提出的方法论：SINDy-KANs
*   **核心思想**：在 KAN 的每一个单变量激活函数 $\phi$ 层面引入类 SINDy 的稀疏回归。即不再仅仅用 B-样条表示激活函数，而是将其表示为一个候选函数库的稀疏线性组合。
*   **关键技术细节**：
    *   **激活函数表示**：对于第 $l$ 层的激活函数 $\phi_{l,j,i}$，其输入为 $x_i^{(l)}$，输出表示为 $\phi = \Theta(x) \xi$，其中 $\Theta$ 是候选函数库（如多项式、三角函数），$\xi$ 是待学习的稀疏系数。
    *   **多层复合**：通过 KAN 的多层结构，SINDy-KANs 可以表示复杂的复合函数（如第一层学习 $x^2$，第二层学习 $\cos(\cdot)$），这是传统单层 SINDy 难以实现的。
    *   **乘法节点**：引入了类似 MultKAN 的乘法节点，以便更容易地捕捉动力系统中的变量乘积项（如 Lorenz 系统中的 $xy$）。
    *   **损失函数**：
        $$L = \lambda_{KAN} L_{KAN} + \lambda_S L_S + \lambda_\Lambda L_\Lambda + \lambda_1 ||\Lambda||_1 + \lambda_2 ||\Lambda - \Xi_S||_2^2$$
        其中引入了“影子矩阵” $\Lambda$ 来辅助实现 $L_1$ 正则化，确保系数的稀疏性。
    *   **直接法与间接法**：论文探讨了直接优化系数（Direct SINDy-KANs）和通过稀疏回归更新系数两种模式。

### 3. 实验设计
*   **实验场景**：
    1.  **符号回归**：拟合 $f(x,y) = \cos(x^2 + y)$。
    2.  **线性 ODE 系统**：3D 线性动力学发现。
    3.  **阻尼摆（Damped Pendulum）**：包含三角函数的非线性系统。
    4.  **ABC 流（ABC Flow）**：学习带有未知频率参数的动力学。
    5.  **Lorenz 系统**：经典的混沌系统，测试乘法节点。
    6.  **Kuramoto 振子**：测试系统对称性和稀疏性。
    7.  **噪声鲁棒性实验**：在 30% 相对噪声下测试线性 ODE 的恢复情况。
*   **Benchmark 与对比方法**：
    *   主要对比了 **pykan**（原始 KAN 的符号回归实现）。
    *   在某些案例中参考了 **ADAM-SINDy** 的思路。

### 4. 资源与算力
*   **硬件环境**：所有实验均在一台 **Apple M3 Max Macbook Pro** 上完成。
*   **训练时长**：每个问题的训练时间通常**少于几分钟**。
*   **软件框架**：基于 `jaxKAN`（JAX 框架）实现，利用了自动微分和高效的线性代数运算。

### 5. 实验数量与充分性
*   **实验规模**：论文涵盖了从简单符号回归到复杂混沌动力系统的 6 组主要实验，并附带了噪声影响分析和不同架构（层数）的消融对比。
*   **充分性评价**：实验设计较为全面，覆盖了线性、非线性、复合函数、混沌和振荡等多种动力学特性。
*   **客观性**：通过与 `pykan` 的直接对比，证明了 SINDy-KANs 在处理复合函数和保持方程简洁性方面的显著优势。

### 6. 主要结论与发现
*   **准确性**：SINDy-KANs 能够准确识别出所有测试案例的控制方程，系数误差极小。
*   **简洁性**：相比原始 KAN 产生的复杂符号表达式，SINDy-KANs 发现的方程非常稀疏，直接对应物理项。
*   **复合能力**：成功解决了 SINDy 难以处理的函数嵌套问题（如 $\cos(x^2+y)$）。
*   **参数学习**：该方法不仅能发现函数形式，还能自动学习函数内部的参数（如 ABC 流中的频率 $w$），这是标准 SINDy 无法做到的。

### 7. 优点
*   **强强联手**：完美结合了 KAN 的深度表达能力和 SINDy 的稀疏解释性。
*   **架构灵活**：允许在不同层使用不同的候选函数库，甚至可以混合使用 B-样条和符号项。
*   **训练高效**：基于 JAX 的实现使得在消费级硬件上即可快速完成复杂方程的发现。
*   **鲁棒性**：通过预训练“前向 KAN”来计算数值导数，增强了对噪声数据的处理能力。

### 8. 不足与局限
*   **架构依赖**：网络深度（层数）的选择对结果影响较大，目前仍需人工预设，缺乏自动确定层数的机制。
*   **可扩展性挑战**：随着输入变量维度的增加，激活函数的数量呈指数级增长，可能导致模型解释难度增加。
*   **导数敏感性**：动力学发现依赖于数值导数的质量，在极高噪声下，虽然比标准 SINDy 稳健，但仍可能引入错误的高阶项。
*   **库的选择**：虽然减少了对库的依赖，但仍需预设基本的原子函数（如 $\sin, \cos, \text{poly}$）。

（完）
