---
title: Dual Space Preconditioning for Gradient Descent in the Overparameterized Regime
title_zh: 过参数化机制下梯度下降的对偶空间预条件化
authors: "Reza Ghane, Danil Akhtiamov, Babak Hassibi"
date: 2026-03-11
pdf: "https://arxiv.org/pdf/2603.10485v1"
tags: ["query:opt-lm"]
score: 8.0
evidence: 包括Adam在内的预条件梯度下降的收敛性质
tldr: 本文研究了超参数化线性模型中对偶空间预处理梯度下降（涵盖Adam、梯度裁剪等）的收敛性。通过引入一种新型Bregman散度，证明了在凸预处理函数下算法始终收敛于全局最小值。研究进一步探讨了隐式偏差，发现各向同性预处理器的收敛点与标准梯度下降一致，而一般预处理器的收敛点与初始点的距离在常数倍范围内受限，为理解复杂优化器的行为提供了理论支撑。
selection_source: carryover_cache
motivation: 旨在探究如Adam和梯度裁剪等预处理梯度下降算法在超参数化模型中的收敛机制及其隐式偏差特性。
method: 利用对偶空间预处理框架，并引入一种改进的Bregman散度及其相关恒等式来建立收敛性证明体系。
result: 证明了该类算法在超参数化线性模型下必然收敛至全局解，并量化了不同预处理函数对最终收敛点（隐式偏差）的影响。
conclusion: 揭示了预处理梯度下降在收敛性上具有稳健保障，且其隐式偏差在特定条件下与标准梯度下降具有等价性或紧密的常数倍关联。
---

## 摘要
在这项工作中，我们研究了对偶空间预条件梯度下降（Dual Space Preconditioned Gradient Descent）的收敛性质，该方法涵盖了诸如归一化梯度下降（Normalized Gradient Descent）、梯度裁剪（Gradient Clipping）和 Adam 等优化器。我们考虑形式为 $\nabla K$ 的预条件子，其中 $K: \mathbb{R}^p \to \mathbb{R}$ 是凸函数，并假设该预条件子被应用于训练一个过参数化线性模型，其损失函数形式为 $\ell({

## Abstract
In this work we study the convergence properties of the Dual Space Preconditioned Gradient Descent, encompassing optimizers such as Normalized Gradient Descent, Gradient Clipping and Adam. We consider preconditioners of the form $\nabla K$, where $K: \mathbb{R}^p \to \mathbb{R}$ is convex and assume that the latter is applied to train an over-parameterized linear model with loss of the form $\ell({X} {W} - {Y})$, for weights ${W} \in \mathbb{R}^{d \times k}$, labels ${Y} \in \mathbb{R}^{n \times k}$ and data ${X} \in \mathbb{R}^{n \times d}$. Under the aforementioned assumptions, we prove that the iterates of the preconditioned gradient descent always converge to a point ${W}_{\infty} \in \mathbb{R}^{d \times k}$ satisfying ${X}{W}_{\infty} = {Y}$. Our proof techniques are of independent interest as we introduce a novel version of the Bregman Divergence with accompanying identities that allow us to establish convergence. We also study the implicit bias of Dual Space Preconditioned Gradient Descent. First, we demonstrate empirically that, for general $K(\cdot)$, ${W}_\infty$ depends on the chosen learning rate, hindering a precise characterization of the implicit bias. Then, for preconditioners of the form $K({G}) = h(\|{G}\|_F)$, known as \textit{isotropic preconditioners}, we show that ${W}_\infty$ minimizes $\|{W}_\infty - {W}_0\|_F^2$ subject to ${X}{W}_\infty = {Y}$, where ${W}_0$ is the initialization. Denoting the convergence point of GD initialized at ${W}_0$ by ${W}_{\text{GD}, \infty}$, we thus note ${W}_{\infty} = {W}_{\text{GD}, \infty}$ for isotropic preconditioners. Finally, we show that a similar fact holds for general preconditioners up to a multiplicative constant, namely, $\|{W}_0 - {W}_{\infty}\|_F \le c \|{W}_0 - {W}_{\text{GD}, \infty}\|_F$ for a constant $c>0$.

---

## 论文详细总结（自动生成）

这篇论文《Dual Space Preconditioning for Gradient Descent in the Overparameterized Regime》由加州理工学院（Caltech）的研究团队撰写，深入探讨了现代优化器在过参数化模型中的理论行为。以下是对该论文的结构化总结：

### 1. 核心问题与整体含义
*   **研究背景**：在深度学习中，Adam、梯度裁剪（Gradient Clipping）和归一化梯度下降（Normalized GD）等优化器被广泛使用。这些优化器可以被统一建模为“对偶空间预条件梯度下降”。
*   **核心问题**：在“过参数化”机制下（即参数数量远多于数据量，存在无数个全局最小值），这些复杂的优化器是否一定能收敛？它们最终会收敛到哪一个解（即隐式偏差 Implicit Bias 是什么）？
*   **研究动机**：现有的理论研究大多假设损失函数是严格凸的（有唯一解），但在现代机器学习中，模型通常是过参数化的。本文旨在填补这一理论空白。

### 2. 方法论
*   **核心思想**：将优化更新规则定义为 $W_i = W_{i-1} - \eta \nabla K(\nabla L(W_{i-1}))$，其中 $K$ 是一个凸函数（预条件子）。
*   **关键技术细节**：
    *   **调整后的Bregman散度（Adjusted Bregman Divergence）**：作者引入了一个新定义的散度 $\tilde{D}_f(A, B) = f^*(\nabla f(A)) - f^*(\nabla f(B)) - \text{Tr}(B^T(\nabla f(A) - \nabla f(B)))$，其中 $f^*$ 是 Fenchel 对偶。
    *   **基本恒等式**：推导出了 Proposition 1 中的能量等式，将连续两次迭代与目标解之间的距离联系起来，这是证明收敛性的核心工具。
    *   **矩阵结构**：不同于以往将权重视为向量的研究，本文保留了权重的矩阵结构 $W \in \mathbb{R}^{d \times k}$，使其能够分析如 Muon、Soap 和 Shampoo 等现代矩阵预条件优化器。

### 3. 实验设计
*   **场景**：过参数化线性回归问题（$n < d$），使用平方损失函数。
*   **Benchmark（基准）**：标准梯度下降（GD）的收敛点（即最小 $L_2$ 范数解）。
*   **对比方法与对象**：
    *   **Adam（无动量版）**：研究其在不同超参数 $\epsilon$ 下的行为。
    *   **范数解对比**：对比算法收敛点与 $L_1, L_2, L_\infty$ 范数最小化解的距离。
    *   **学习率影响**：观察学习率 $\eta$ 如何改变最终的收敛点。

### 4. 资源与算力
*   **算力说明**：论文中**未明确说明**具体的硬件配置（如 GPU 型号、数量）或训练时长。
*   **分析**：由于实验部分主要针对线性模型进行理论验证，计算量相对较小，通常在单台工作站甚至笔记本电脑上即可完成。

### 5. 实验数量与充分性
*   **实验规模**：论文包含两组主要实验图表（Fig 1 和 Fig 2）。
*   **充分性评价**：
    *   作为一篇**理论导向**的论文，实验的主要目的是验证理论推导的正确性，而非追求大规模性能指标。
    *   实验展示了 Adam 在不同 $\epsilon$ 下向 GD 解靠拢的过程，以及收敛点对学习率的依赖性，这足以支持其理论发现。
    *   **局限性**：实验仅限于合成数据和线性模型，缺乏在真实大规模深度神经网络（如 Transformer）上的实证。

### 6. 主要结论与发现
*   **收敛性保证**：证明了在过参数化线性模型下，只要预条件函数 $K$ 是凸的，该类算法始终能收敛到插值解（即全局最小值）。
*   **各向同性预条件子的隐式偏差**：对于形式为 $K(G) = h(\|G\|_F)$ 的预条件子（如梯度裁剪），其收敛点与标准 GD 完全一致，均收敛于距离初始点最近的 $L_2$ 解。
*   **一般预条件子的隐式偏差**：对于非各向同性预条件子（如 Adam），收敛点**依赖于学习率** $\eta$。虽然它不一定收敛于 $L_2$ 最小解，但其与初始点的距离在 GD 解的常数倍范围内受限。

### 7. 优点
*   **理论工具创新**：提出的“调整后的Bregman散度”为分析非线性预条件优化器提供了强有力的数学框架。
*   **统一性**：成功将 Adam、梯度裁剪、归一化梯度下降等多种看似不同的算法纳入同一个理论框架。
*   **矩阵支持**：对矩阵权重的支持使其更符合当前大模型优化技术的发展趋势。

### 8. 不足与局限
*   **模型复杂度**：结论仅严格适用于线性模型，对于非线性深度神经网络的适用性仍需进一步探索。
*   **动量缺失**：分析中未包含动量（Momentum）项，而动量是 Adam 和 Lion 等优化器在实践中取得成功的关键因素。
*   **隐式偏差的精确性**：对于非各向同性情况，论文仅给出了距离的上界，未能像镜像下降（Mirror Descent）那样给出收敛点的精确解析刻画。

（完）
