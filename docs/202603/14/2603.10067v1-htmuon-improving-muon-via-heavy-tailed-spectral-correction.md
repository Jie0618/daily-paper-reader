---
title: "HTMuon: Improving Muon via Heavy-Tailed Spectral Correction"
title_zh: HTMuon：通过重尾谱修正改进 Muon
authors: "Tianyu Pang, Yujie Fang, Zihang Liu, Shenyang Deng, Lei Hsiung, Shuhua Yu, Yaoqing Yang"
date: 2026-03-10
pdf: "https://arxiv.org/pdf/2603.10067v1"
tags: ["query:opt-lm"]
score: 9.0
evidence: LLM 预训练中 Muon 优化器的理论改进
tldr: 本研究针对在LLM训练中表现出色的Muon优化器进行了改进。作者指出Muon的正交化更新规则会抑制重尾权重谱并过度强调噪声方向，为此基于重尾自正则化理论提出了HTMuon。该方法在保留Muon捕捉参数依赖能力的同时，通过重尾谱修正诱导产生更具重尾特性的权重谱。实验证明HTMuon在LLM预训练和图像分类中表现优异，且具有坚实的理论收敛性保证。
selection_source: fresh_fetch
motivation: Muon优化器的正交化更新规则会抑制有利于泛化的重尾权重谱，并导致模型在噪声方向上过度训练。
method: 提出HTMuon算法，利用重尾谱修正技术使更新规则产生更重的尾部分布，从而优化权重谱结构。
result: 在LLaMA预训练等任务中，HTMuon相比Muon显著降低了困惑度（最高达0.98），并展现出良好的插件兼容性。
conclusion: HTMuon通过结合重尾自正则化理论提升了优化性能，并被证明是Schatten-q范数约束下的最速下降法。
---

## 摘要
Muon 最近在大语言模型（LLM）训练中展现出了极具前景的结果。在这项工作中，我们研究了如何进一步改进 Muon。我们认为，Muon 的正交化更新规则抑制了重尾权重谱（heavy-tailed weight spectra）的出现，并过度强调了沿噪声主导方向的训练。受重尾自正则化（HT-SR）理论的启发，我们提出了 HTMuon。HTMuon 在保留 Muon 捕捉参数间依赖关系能力的同时，产生了更重尾的更新并诱导了更重尾的权重谱。在 LLM 预训练和图像分类上的实验表明，HTMuon 相比最先进的基准模型持续提升了性能，并且可以作为现有 Muon 变体的插件使用。例如，在 C4 数据集上的 LLaMA 预训练中，HTMuon 相比 Muon 将困惑度（perplexity）降低了高达 0.98。我们进一步从理论上证明了 HTMuon 对应于 Schatten-$q$ 范数约束下的最速下降，并提供了在平滑非凸设置下的收敛性分析。HTMuon 的实现代码可在 https://github.com/TDCSZ327/HTmuon 获取。

## Abstract
Muon has recently shown promising results in LLM training. In this work, we study how to further improve Muon. We argue that Muon's orthogonalized update rule suppresses the emergence of heavy-tailed weight spectra and over-emphasizes the training along noise-dominated directions. Motivated by the Heavy-Tailed Self-Regularization (HT-SR) theory, we propose HTMuon. HTMuon preserves Muon's ability to capture parameter interdependencies while producing heavier-tailed updates and inducing heavier-tailed weight spectra. Experiments on LLM pretraining and image classification show that HTMuon consistently improves performance over state-of-the-art baselines and can also serve as a plug-in on top of existing Muon variants. For example, on LLaMA pretraining on the C4 dataset, HTMuon reduces perplexity by up to $0.98$ compared to Muon. We further theoretically show that HTMuon corresponds to steepest descent under the Schatten-$q$ norm constraint and provide convergence analysis in smooth non-convex settings. The implementation of HTMuon is available at https://github.com/TDCSZ327/HTmuon.

---

## 论文详细总结（自动生成）

这是一份关于论文《HTMuon: Improving Muon via Heavy-Tailed Spectral Correction》的结构化深度总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：Muon 优化器虽然在 LLM 训练中表现优异，但其核心的“正交化更新规则”（将动量矩阵的所有奇异值设为 1）存在两个缺陷：
    1.  **抑制重尾谱**：根据重尾自正则化（HT-SR）理论，训练良好的神经网络权重矩阵应呈现重尾分布，而 Muon 的强制正交化会诱导产生轻尾谱，限制模型泛化能力。
    2.  **噪声敏感**：Muon 对所有奇异值方向赋予相同权重，这会导致优化器过度关注那些通常由噪声主导的小奇异值方向。
*   **研究动机**：通过修正 Muon 的谱分布，使其在保留矩阵优化器捕捉参数间依赖关系优势的同时，诱导产生更符合 HT-SR 理论的重尾权重谱，从而提升训练效果和泛化性能。

### 2. 论文提出的方法论
*   **核心思想**：将 Muon 的“全 1 奇异值”更新改为“基于幂变换的奇异值”更新。
*   **关键技术细节**：
    *   **HTMuon 算法**：对动量矩阵 $M_t$ 进行奇异值分解（SVD），得到 $U_t \Sigma_t V_t^\top$。
    *   **谱修正**：不将奇异值全部设为 1，而是计算 $\Sigma_t^p$，其中 $p \in (0, 1)$。
    *   **参数平衡**：当 $p=0$ 时退化为 Muon（轻尾），当 $p=1$ 时退化为 SGDM（独立更新）。作者推荐 $p=0.125$，这能在捕捉参数耦合与保持重尾特性之间取得平衡。
    *   **加速方案**：
        1.  **间隔更新**：每隔固定步数（如 5 或 10 步）执行一次 HTMuon，其余步数使用 Muon。
        2.  **HTMuon NS**：利用 Newton-Schulz 迭代近似矩阵平方根，避免昂贵的 SVD 计算。
*   **理论支撑**：证明了 HTMuon 等价于在 **Schatten-$q$ 范数**约束下的最速下降法，并提供了非凸平滑设置下的收敛性证明，其样本复杂度与 Muon 和 SGDM 持平（$O(\epsilon^{-4})$）。

### 3. 实验设计
*   **数据集/场景**：
    *   **LLM 预训练**：C4 数据集（训练 LLaMA 60M/135M/350M/1B）、OpenWebText（训练 GPT-2 small）。
    *   **图像分类**：CIFAR-10/100（训练 ResNet18/50）、ImageNet-1K（训练 ViT-tiny）。
    *   **下游任务**：在 7 个常识推理基准（如 ARC, PIQA, Hellaswag）上进行零样本评估。
*   **Benchmark 与对比方法**：
    *   **基础方法**：Adam, AdamW, SGDM。
    *   **Muon 系变体**：Muon, NorMuon, AdaMuon。
    *   **SOTA 优化器**：Cautious, GaLore, Sophia, Mars, SOAP, COSMOS。

### 4. 资源与算力
*   **GPU 型号与数量**：
    *   LLaMA-60M：2 张 NVIDIA L40 或 A6000。
    *   LLaMA-135M：4 张 NVIDIA L40 或 A6000。
    *   LLaMA-350M/1B：4 张 NVIDIA RTX PRO 6000 (Blackwell)。
    *   图像任务：单张 NVIDIA L40 或 RTX PRO 6000。
*   **训练时长**：文中提供了墙钟时间对比。例如，在 LLaMA-60M 上，HTMuon NS (Interval=5) 达到目标困惑度的时间为 0.64 小时，略快于 Muon 的 0.67 小时（尽管单步开销略高，但收敛更快）。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了从 60M 到 1B 参数的模型，跨越了 NLP 和 CV 两个领域，实验组数非常多。
*   **消融实验**：对幂参数 $p$ 的取值（0 到 0.25）、更新间隔（1 到 25 步）、学习率敏感度等进行了详尽的消融研究。
*   **客观性**：所有基准测试均使用了原论文提供的代码库进行复现，并进行了精细的超参数调优（Grid Search），确保了对比的公平性。

### 6. 主要结论与发现
*   **性能领先**：HTMuon 在所有测试任务中均优于 Muon 及其变体。在 LLaMA-135M 预训练中，困惑度比 Muon 降低了 0.98。
*   **重尾验证**：实验观察到 HTMuon 训练出的模型权重矩阵确实具有更小的幂律指数 $\alpha$（即更重的尾部），验证了 HT-SR 理论的有效性。
*   **泛化增强**：在下游常识推理任务中，HTMuon 预训练的模型平均得分比 Muon 高出 1.05 分。
*   **插件特性**：HTMuon 可以作为组件叠加在 NorMuon 等变体上，进一步提升性能。

### 7. 优点
*   **理论与直觉结合**：将深奥的重尾自正则化理论转化为简单的幂变换操作，逻辑清晰。
*   **实用性强**：通过“间隔更新”和“Newton-Schulz 近似”解决了 SVD 计算开销大的痛点，使其具备在大规模模型上落地的潜力。
*   **鲁棒性**：实验证明 $p=0.125$ 是一个非常通用的默认值，减少了调参负担。

### 8. 不足与局限
*   **计算开销**：尽管有加速方案，HTMuon 的原生 SVD 操作依然比 AdamW 或基础 Muon 慢，在大规模分布式训练中的通信/计算重叠可能更复杂。
*   **模型规模上限**：实验最高只做到了 1B 参数模型，尚未在百亿（10B+）或千亿级参数模型上验证其扩展性。
*   **超参数引入**：虽然 $p=0.125$ 表现良好，但相比 AdamW，依然多了一个需要考虑的超参数 $p$。

（完）
