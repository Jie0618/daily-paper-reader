---
title: Heterogeneous Stochastic Momentum ADMM for Distributed Nonconvex Composite Optimization
title_zh: 针对分布式非凸复合优化的异构随机动量 ADMM
authors: "Yangming Zhang, Yongyang Xiong, Jinming Xu, Keyou You, Yang Shi"
date: 2026-03-08
pdf: "https://arxiv.org/pdf/2603.07682v1"
tags: ["query:opt-lm"]
score: 9.0
evidence: 分布式随机非凸复合优化与动量方法
tldr: 针对分布式非凸复合优化中步长受限于全局网络参数的问题，本文提出了一种异构随机动量ADMM算法（HSM-ADMM）。该算法结合STORM动量估计，实现了O(ε⁻¹.⁵)的最优复杂度，并创新性地采用节点自适应步长策略，消除了对全局网络信息的依赖。HSM-ADMM仅需单次通信且支持小批量样本，在保证收敛性的同时显著提升了异构网络下的通信效率和收敛速度。
selection_source: fresh_fetch
motivation: 现有分布式随机优化算法的步长受限于全局网络参数，导致在异构拓扑中性能受限且需要预知全局结构知识。
method: 提出HSM-ADMM算法，结合STORM动量估计与节点特定的自适应步长策略，实现单步通信和单循环结构。
result: 理论证明该算法达到了O(ε⁻¹.⁵)的最优复杂度，且稳定性与全局网络属性解耦，实验验证了其在非凸学习任务中的高效性。
conclusion: HSM-ADMM通过局部自适应机制克服了异构网络瓶颈，在显著降低通信开销的同时实现了非凸优化任务的快速稳健收敛。
---

## 摘要
本文研究了分布式随机非凸且非光滑复合优化问题。现有的随机算法通常依赖于受全局网络参数（如最大节点度或谱半径）严格限制的统一步长。这种依赖性造成了严重的性能瓶颈，特别是在异构网络拓扑中，为了确保稳定性，必须保守地减小步长。为了克服这一限制，我们提出了一种新型的异构随机动量交替方向乘子法（HSM-ADMM）。通过集成递归动量估计器（STORM），HSM-ADMM 利用严格的单循环结构和 $\mathcal{O}(1)$ 的小批量大小，实现了达到 $\epsilon$-驻点所需的 $\mathcal{O}(\epsilon^{-1.5})$ 最优算子复杂度。核心创新在于一种特定于节点的自适应步长策略，该策略根据局部度信息缩放近端项。我们在理论上证明，这种设计使算法稳定性与全局网络属性完全解耦，从而在不需要任何全局结构知识的情况下，在任意连接的拓扑结构上实现稳健且加速的收敛。此外，HSM-ADMM 每次迭代仅需传输单个原始变量，与最先进的梯度跟踪算法相比，显著降低了通信带宽。在分布式非凸学习任务上的广泛数值实验验证了所提出的 HSM-ADMM 算法的卓越效率。

## Abstract
This paper investigates the distributed stochastic nonconvex and nonsmooth composite optimization problem. Existing stochastic typically rely on uniform step size strictly bounded by global network parameters, such as the maximum node degree or spectral radius. This dependency creates a severe performance bottleneck, particularly in heterogeneous network topologies where the step size must be conservatively reduced to ensure stability. To overcome this limitation, we propose a novel Heterogeneous Stochastic Momentum Alternating Direction Method of Multipliers (HSM-ADMM). By integrating a recursive momentum estimator (STORM), HSM-ADMM achieves the optimal oracle complexity of $\mathcal{O}(ε^{-1.5})$ to reach an $ε$-stationary point, utilizing a strictly single-loop structure and an $\mathcal{O}(1)$ mini-batch size. The core innovation lies in a node-specific adaptive step-size strategy, which scales the proximal term according to local degree information. We theoretically demonstrate this design completely decouples the algorithmic stability from global network properties, enabling robust and accelerated convergence across arbitrary connected topologies without requiring any global structural knowledge. Furthermore, HSM-ADMM requires transmitting only a single primal variable per iteration, significantly reducing communication bandwidth compared to state-of-the-art gradient tracking algorithms. Extensive numerical experiments on distributed nonconvex learning tasks validate the superior efficiency of the proposed HSM-ADMM algorithm.

---

## 论文详细总结（自动生成）

这篇论文针对分布式随机非凸复合优化问题，提出了一种名为 **HSM-ADMM** 的创新算法。以下是对该论文的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：如何在分布式网络中高效解决**非凸（Nonconvex）且非光滑（Nonsmooth）**的随机复合优化问题。
*   **研究动机**：
    *   **全局参数限制**：现有分布式算法通常采用统一步长，其上限受限于全局网络参数（如最大节点度或谱半径）。在异构网络中，这会导致“木桶效应”，即步长必须向最差的节点看齐，严重限制了收敛速度。
    *   **通信开销大**：先进的梯度跟踪（Gradient Tracking）算法通常每轮需要传输两个变量（模型参数和梯度跟踪器），带宽压力大。
    *   **计算负担**：许多算法依赖大批量（Large Batch）采样或复杂的双循环结构来降低随机梯度的方差。

### 2. 方法论：HSM-ADMM 算法
*   **核心思想**：将交替方向乘子法（ADMM）与递归动量估计（STORM）相结合，并引入**异构自适应步长策略**。
*   **关键技术细节**：
    *   **STORM 动量估计**：采用递归方式更新梯度估计值 $v_k$，在无需大批量样本的情况下实现方差缩减，达到 $O(\epsilon^{-1.5})$ 的最优算子复杂度。
    *   **异构步长设计**：每个节点 $i$ 根据其**局部度（Local Degree）** $d_i$ 自主决定步长 $\eta_{ki} = c_\eta (d_i + 1) k^{1/3}$。这种设计使算法稳定性与全局拓扑解耦。
    *   **单循环结构**：算法采用严格的单循环更新，每步仅需 $O(1)$ 的小批量样本，无需周期性的全梯度计算。
    *   **ADMM 框架**：通过引入辅助变量和对偶变量，将非光滑正则项（如 $L_1$ 范数）与共识约束解耦，利用近端算子（Proximal Operator）高效求解。

### 3. 实验设计
*   **数据集与场景**：
    *   **a9a 数据集**：使用 2 层多层感知机（MLP）进行分类。
    *   **MNIST 数据集**：使用标准的 LeNet 卷积神经网络架构。
*   **网络拓扑**：分别在 **环形拓扑（Ring）** 和 **随机连接图（Random Graph）** 上进行测试（节点数 $n=8$）。
*   **对比方法（Benchmarks）**：
    *   **SPPDM**：带动量的分布式随机共识优化算法。
    *   **ProxGT-SR-O**：基于梯度跟踪的随机近端梯度算法。
    *   **DEEPSTORMv2**：采用递归动量的非凸分布式优化算法。

### 4. 资源与算力
*   **软件环境**：使用 Python、PyTorch 和 mpi4py 实现。
*   **算力说明**：论文**未明确说明**具体的 GPU 型号、数量以及具体的训练时长。实验主要侧重于收敛曲线（通信轮数 vs. 驻点误差/损失值）的对比。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了两个经典数据集、两种不同的网络拓扑以及三种最先进的基准算法。
*   **充分性评价**：实验设计较为**充分且客观**。通过展示“驻点误差（Stationarity）”、“训练损失”和“测试准确率”随通信轮数的变化，全面验证了算法的有效性。特别是针对异构步长的理论，在不同拓扑下的表现一致性证明了其鲁棒性。

### 6. 主要结论与发现
*   **最优复杂度**：理论证明 HSM-ADMM 达到了 $O(\epsilon^{-1.5})$ 的随机优化理论下界。
*   **拓扑无关性**：异构步长策略成功消除了算法对全局网络信息的依赖，解决了异构网络中的收敛瓶颈。
*   **通信高效性**：相比梯度跟踪类算法，HSM-ADMM 每轮迭代仅需传输一个原始变量，**通信带宽需求减半**。
*   **性能领先**：实验结果显示，在相同的通信轮数下，HSM-ADMM 的收敛速度更快，且最终的分类准确率优于对比算法。

### 7. 优点与亮点
*   **局部自适应**：创新性地利用局部节点度信息设定步长，这是对传统分布式优化“统一步长”范式的重大突破。
*   **架构简洁**：单循环结构结合 $O(1)$ 批量大小，使其在处理大规模流式数据时极具吸引力。
*   **理论完备**：提供了详尽的收敛性证明，且不依赖于常见的“有界数据异构性”假设，适用范围更广。

### 8. 不足与局限
*   **异步性缺失**：目前算法基于同步更新，在实际大规模分布式系统中，节点间的通信延迟可能导致同步等待问题。
*   **算力细节模糊**：缺乏对实际计算耗时（Wall-clock time）的量化分析，难以评估异构步长带来的额外计算开销。
*   **扩展性验证**：实验节点数较少（$n=8$），在成百上千节点的超大规模异构网络中的表现仍有待验证。
*   **压缩技术**：尚未结合梯度压缩或量化技术，在极端带宽受限的环境下仍有优化空间。

（完）
