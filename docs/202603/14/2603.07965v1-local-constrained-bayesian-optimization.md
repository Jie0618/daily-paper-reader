---
title: Local Constrained Bayesian Optimization
title_zh: 局部约束贝叶斯优化
authors: "Jing Jingzhe, Fan Zheyi, Szu Hui Ng, Qingpei Hu"
date: 2026-03-09
pdf: "https://arxiv.org/pdf/2603.07965v1"
tags: ["query:opt-lm"]
score: 10.0
evidence: 约束优化中 KKT 残差的收敛率
tldr: 针对高维受限贝叶斯优化中的维度灾难问题，本文提出了一种名为LCBO的新框架。该方法利用约束惩罚代理模型的可微性，在局部快速下降与不确定性驱动的探索之间交替进行，有效避免了传统信赖域方法在复杂约束下过早收缩的问题。理论证明其KKT残差收敛速度与维度呈多项式关系，实验表明其在高达100维的基准测试中显著优于现有最先进方法。
selection_source: fresh_fetch
motivation: 传统贝叶斯优化在高维受限场景下受维度灾难影响严重，且信赖域方法在面对复杂约束时容易过早收缩。
method: 提出LCBO框架，利用约束惩罚代理模型的梯度信息，交替执行局部快速下降和基于不确定性的探索。
result: 理论上证明了KKT残差的收敛速度与维度呈多项式关系，并在高达100维的基准测试中取得了超越SOTA的效果。
conclusion: LCBO为解决高维受限优化问题提供了一个具有理论收敛保证且性能优异的局部贝叶斯优化新方案。
---

## 摘要
由于维度灾难，针对高维约束问题的贝叶斯优化（BO）仍然是一个重大挑战。我们提出了局部约束贝叶斯优化（LCBO），这是一种专为此类场景量身定制的新型框架。与在面对紧致或复杂约束时容易过早收缩的置信域方法不同，LCBO 利用约束惩罚代理模型的可微景观，在快速局部下降和不确定性驱动的探索之间交替进行。在理论上，我们证明了在温和假设下，对于常见核函数，LCBO 的 Karush-Kuhn-Tucker (KKT) 残差收敛速度与维度 $d$ 呈多项式相关，这为遗憾边界通常呈指数级增长的全局 BO 提供了一个严谨的替代方案。在多达 100 维的高维基准测试上的广泛评估表明，LCBO 始终优于最先进的基准方法。

## Abstract
Bayesian optimization (BO) for high-dimensional constrained problems remains a significant challenge due to the curse of dimensionality. We propose Local Constrained Bayesian Optimization (LCBO), a novel framework tailored for such settings. Unlike trust-region methods that are prone to premature shrinking when confronting tight or complex constraints, LCBO leverages the differentiable landscape of constraint-penalized surrogates to alternate between rapid local descent and uncertainty-driven exploration. Theoretically, we prove that LCBO achieves a convergence rate for the Karush-Kuhn-Tucker (KKT) residual that depends polynomially on the dimension $d$ for common kernels under mild assumptions, offering a rigorous alternative to global BO where regret bounds typically scale exponentially. Extensive evaluations on high-dimensional benchmarks (up to 100D) demonstrate that LCBO consistently outperforms state-of-the-art baselines.

---

## 论文详细总结（自动生成）

以下是对论文《Local Constrained Bayesian Optimization》（局部约束贝叶斯优化，LCBO）的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：在高维空间（$d > 20$）中处理带有未知、昂贵约束的黑盒优化问题。
*   **研究背景**：
    *   **维度灾难**：标准全局贝叶斯优化（BO）的收敛率随维度呈指数级恶化。
    *   **现有方法局限**：目前的局部置信域方法（如 SCBO）在面对复杂或紧致约束时，容易因无法在局部区域内找到可行改进点而导致置信域过早收缩（Premature Shrinking），从而陷入次优解。
*   **研究意义**：提出一种新的局部搜索框架，旨在摆脱刚性置信域的限制，并为高维约束优化提供多项式级别的收敛理论保证。

### 2. 方法论：核心思想与技术细节
LCBO 结合了惩罚函数法和局部梯度估计，核心流程如下：
*   **核心思想**：将约束优化问题转化为惩罚函数 $Q_\rho(x) = f(x) + \frac{\rho}{2}\|c(x)\|^2$ 的无约束优化，通过交替执行“局部探索”和“局部利用”来更新解。
*   **关键技术细节**：
    1.  **单层循环结构**：不同于传统惩罚函数法需要内层循环收敛，LCBO 每轮迭代仅执行一次梯度下降步，通过动态调整步长 $\eta_k$ 和惩罚因子 $\rho_k$ 保证收敛。
    2.  **局部探索（Active Learning）**：通过最小化梯度后验协方差的迹（Trace）来选择观测点，旨在最大限度降低当前点 $x_k$ 处的梯度不确定性。
    3.  **局部利用（Gradient Update）**：利用高斯过程（GP）后验均值计算惩罚函数的近似梯度，并执行投影梯度下降更新 $x_{k+1}$。
    4.  **理论突破**：证明了 LCBO 的 KKT 残差（衡量一阶最优性的指标）对维度 $d$ 的依赖是多项式级别的（如 $\tilde{O}(d^{1/6}n^{-1/6})$），而非指数级别。

### 3. 实验设计
*   **实验场景**：
    *   **高维合成函数**：25维、50维、100维的受限合成任务。
    *   **工程设计基准**：25-Bar 桁架设计（25D）、阶梯悬臂梁设计（50D）。
    *   **强化学习任务**：MuJoCo HalfCheetah 连续控制（102D），涉及能量消耗约束。
*   **对比基准（Baselines）**：
    *   **CEI**：经典的约束期望改进算法（全局 BO）。
    *   **EPBO**：基于精确惩罚函数的 BO。
    *   **SCBO / HD-safeBO**：最先进的高维局部/安全约束 BO 方法。
*   **实验设置**：所有实验重复 10 次，设置 1000 次函数评估预算，观测值带有高斯噪声。

### 4. 资源与算力
*   **硬件环境**：论文提到使用了一台配备双路 **Intel Xeon Platinum 8260 CPU** 和 **376 GB 内存** 的服务器。
*   **算力细节**：文中未明确提及使用 GPU。由于高斯过程（GP）的计算主要涉及矩阵求逆，通常在 CPU 上执行，且 LCBO 采用了局部 GP 模型（仅保留最近的 $N_m$ 个点），显著降低了计算开销。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了从 25D 到 102D 的多个维度梯度，实验组数充足。
*   **消融实验**：在附录 B 中专门针对**批处理调度（Batch Schedule）**进行了消融研究，对比了固定小批量、固定大批量和增长批量对收敛性能的影响，验证了实际应用中固定小批量的优越性。
*   **客观性**：所有算法共享相同的初始训练集，采用中位数和四分位距进行统计展示，评估标准（最佳可行目标值）符合约束优化的通用规范。

### 6. 主要结论与发现
*   **性能领先**：LCBO 在所有高维任务中均显著优于 SOTA 基准，尤其是在 100D 任务中，LCBO 的下降曲线远比其他方法陡峭。
*   **克服收缩问题**：在桁架设计任务中，LCBO 证明了其能够沿着约束边界持续导航，而 SCBO 等方法因置信域收缩过快而陷入停滞。
*   **理论验证**：实验结果支持了理论推导，即 LCBO 能够有效缓解维度灾难，实现高效的样本利用率。

### 7. 优点
*   **理论严谨性**：为局部约束 BO 提供了罕见的多项式级收敛率证明。
*   **灵活性**：摒弃了硬性的置信域几何限制，利用可微惩罚函数的景观进行搜索，适应性更强。
*   **高维扩展性**：在 100 维以上的复杂工程和 RL 任务中表现稳健。

### 8. 不足与局限
*   **局部最优风险**：作为一种局部搜索方法，LCBO 在高度多峰（Multi-modal）的函数景观中可能会陷入局部最优，缺乏全局重启动策略。
*   **超参数敏感性**：惩罚因子的调度（$\rho_k$）和步长选择对收敛速度有较大影响，实际应用中可能需要针对特定问题微调。
*   **约束类型限制**：虽然理论上可以通过松弛变量处理不等式约束，但实验主要聚焦于等式或经过聚合的约束形式。

（完）
