---
title: "Symbolic--KAN: Kolmogorov-Arnold Networks with Discrete Symbolic Structure for Interpretable Learning"
title_zh: Symbolic-KAN：具有离散符号结构的 Kolmogorov-Arnold 网络，用于可解释学习
authors: "Salah A Faroughi, Farinaz Mostajeran, Amirhossein Arzani, Shirko Faroughi"
date: 2026-03-25
pdf: "https://arxiv.org/pdf/2603.23854v1"
tags: ["query:sr-alg"]
score: 10.0
evidence: 在神经网络中使用离散符号结构进行控制方程的符号发现
tldr: 本文提出 Symbolic-KAN，旨在结合符号回归的可解释性与神经网络的可扩展性。该架构通过在 Kolmogorov-Arnold 网络中嵌入离散符号结构，利用分层门控和正则化将连续混合项转化为单热选择，从而直接从数据中学习紧凑的解析表达式。它在回归、动力系统及物理信息学习中表现优异，实现了无需后期拟合的端到端符号发现，为科学发现提供了高效工具。
selection_source: fresh_fetch
motivation: 针对传统符号回归搜索效率低和神经网络黑盒模型不可解释的权衡问题，寻求一种可扩展且具有机械解释性的规律学习方法。
method: 在 KAN 架构中引入解析基元库、分层门控和符号正则化，通过训练将连续权重逐渐离散化为单一基元和投影方向的选择。
result: 实验表明该模型能准确恢复数据背后的解析项和控制方程结构，并能有效处理物理信息驱动的偏微分方程求解与发现。
conclusion: Symbolic-KAN 为科学机器学习提供了一种兼具高性能与高透明度的工具，是通往可扩展且机械化规律发现的重要一步。
---

## 摘要
发现控制方程的符号表达式是科学机器学习的一个长期目标，但在可解释性与可扩展学习之间始终存在根本性的权衡。经典的符号回归方法能产生显式的解析表达式，但依赖于组合搜索；而神经网络在处理大规模数据和高维问题时效率很高，但其表示是不透明的。在这项工作中，我们引入了符号化 Kolmogorov-Arnold 网络（Symbolic-KANs），这是一种通过将离散符号结构直接嵌入可训练深度网络来弥合这一差距的神经架构。Symbolic-KANs 将多元函数表示为应用于学习到的标量投影上的学习单变量基元的组合，并由解析基元库、分层门控和符号正则化引导，将连续混合逐渐细化为 one-hot 选择。在经过门控训练和离散化后，每个激活单元选择一个基元和投影方向，从而产生紧凑的闭式表达式，无需事后符号拟合。Symbolic-KANs 进一步充当可扩展的基元发现机制，识别最相关的解析组件，随后可为稀疏方程学习方法的候选库提供参考。我们证明了 Symbolic-KAN 在数据驱动的回归和逆动力系统任务中，能够可靠地恢复正确的基元项和控制结构。此外，该框架还扩展到偏微分方程的正向和反向物理信息学习，直接从控制约束中产生精确解，同时构建紧凑的符号表示，其选择的基元反映了底层方程的真实解析结构。这些结果使 Symbolic-KAN 成为迈向可扩展、可解释且具有机制基础的控制律学习的重要一步。

## Abstract
Symbolic discovery of governing equations is a long-standing goal in scientific machine learning, yet a fundamental trade-off persists between interpretability and scalable learning. Classical symbolic regression methods yield explicit analytic expressions but rely on combinatorial search, whereas neural networks scale efficiently with data and dimensionality but produce opaque representations. In this work, we introduce Symbolic Kolmogorov-Arnold Networks (Symbolic-KANs), a neural architecture that bridges this gap by embedding discrete symbolic structure directly within a trainable deep network. Symbolic-KANs represent multivariate functions as compositions of learned univariate primitives applied to learned scalar projections, guided by a library of analytic primitives, hierarchical gating, and symbolic regularization that progressively sharpens continuous mixtures into one-hot selections. After gated training and discretization, each active unit selects a single primitive and projection direction, yielding compact closed-form expressions without post-hoc symbolic fitting. Symbolic-KANs further act as scalable primitive discovery mechanisms, identifying the most relevant analytic components that can subsequently inform candidate libraries for sparse equation-learning methods. We demonstrate that Symbolic-KAN reliably recovers correct primitive terms and governing structures in data-driven regression and inverse dynamical systems. Moreover, the framework extends to forward and inverse physics-informed learning of partial differential equations, producing accurate solutions directly from governing constraints while constructing compact symbolic representations whose selected primitives reflect the true analytical structure of the underlying equations. These results position Symbolic-KAN as a step toward scalable, interpretable, and mechanistically grounded learning of governing laws.

---

## 论文详细总结（自动生成）

### 论文总结：Symbolic–KAN —— 具有离散符号结构的 Kolmogorov-Arnold 网络

#### 1. 核心问题与整体含义（研究动机和背景）
在科学机器学习（SciML）领域，长期存在**可解释性**与**可扩展性**的权衡：
*   **传统符号回归（SR）：** 能生成人类可读的解析表达式，但依赖组合搜索，面对高维数据时计算成本极高且易产生“表达式膨胀”。
*   **深度神经网络（如 MLP）：** 具有极强的扩展性和拟合能力，但其内部表示是“黑盒”，难以提取物理机制。
*   **KAN（Kolmogorov-Arnold Networks）：** 虽然通过单变量基函数提高了可解释性，但常用的样条函数（Splines）在深层网络中组合后，最终表达式依然冗长且难以直观理解。

**Symbolic-KAN 的核心动机**是：通过在 KAN 架构中嵌入离散的符号基元库和门控机制，使网络在训练过程中自动“收缩”并选择最合适的数学算子，从而直接输出紧凑、闭式的解析表达式。

#### 2. 方法论：核心思想与关键技术
Symbolic-KAN 将多元函数建模为学习到的单变量基元与标量投影的组合，其核心流程如下：

*   **单元构造（Unit Construction）：**
    每个单元不再使用固定的样条函数，而是从一个**解析基元库**（包含 $x, x^2, \sin, \cos, \exp, \tanh, \log$ 等）中进行选择。每个单元包含多个“边缘”（Edges），每个边缘学习一个线性投影（权重 $w$ 和偏置 $b$），随后应用选定的基元函数。
*   **分层门控机制（Hierarchical Gating）：**
    1.  **基元选择门：** 使用 **Gumbel-Softmax** 对基元库进行连续松弛，允许梯度回传。
    2.  **边缘选择掩码：** 在单元内部选择贡献最大的投影方向。
    3.  **单元激活门：** 通过稀疏正则化决定该神经元是否存活，实现结构剪枝。
*   **训练策略（Gated Training）：**
    *   **阶段 I（软选择）：** 引入温度参数 $\tau$ 进行退火，使基元选择从连续分布逐渐趋向于单热（One-hot）分布。
    *   **阶段 II（硬化与精调）：** 将门控权重强制设为 0 或 1，剔除无效单元，并使用 L-BFGS 优化器对剩余的连续参数（如仿射变换系数）进行高精度微调。
*   **损失函数：** 
    $L = L_{data/phys} + \lambda_{sel} L_{entropy} + \lambda_{nms} L_{NMS} + \lambda_{unit} L_{unit}$。其中熵损失鼓励确定性选择，非极大值抑制（NMS）鼓励基元多样性。

#### 3. 实验设计
论文在三种典型场景下验证了模型性能：
*   **数据驱动回归：** 拟合 1D 多项式和复杂的三角/有理混合函数，验证基元恢复能力。
*   **动力系统识别（逆问题）：** 针对 **Van der Pol 振子**（含非整数幂次项），在不同时间跨度（T=20, 50）下识别系统参数 $a, \mu, c$。
*   **物理信息学习（PI-Symbolic-KAN）：**
    *   **1D 反应扩散方程：** 在不同空间域范围下进行参数反演。
    *   **2D 拉普拉斯方程：** 在正向求解过程中观察网络是否自发选择与解析解一致的基元（如 $\sin, \sinh$）。
*   **对比方法（Benchmark）：** 
    对比了标准 **PINN**（基于 MLP 和 tanh 激活）和 **cPIKAN**（基于切比雪夫多项式的 KAN）。

#### 4. 资源与算力
论文**未明确说明**具体的硬件配置（如 GPU 型号、显存占用）或具体的训练总时长。文中提到使用了 Adam 优化器进行初步训练，随后切换到 L-BFGS 进行精调，这符合此类物理信息学习任务的常规算力消耗水平。

#### 5. 实验数量与充分性
*   **实验覆盖面：** 涵盖了从简单的函数拟合到复杂的非线性 ODE 和 PDE 问题，实验设计较为全面。
*   **消融与敏感性分析：** 针对 Van der Pol 问题，论文详细分析了训练样本数 $N_{tr}$、配置参数、网络深度 $L$ 和宽度 $K$ 对预测误差和参数识别准确性的影响（见图 4）。
*   **客观性：** 通过在不同空间域（$M=2$ vs $M=4$）下对比 PINN 和 cPIKAN，展示了模型在处理大范围域问题时的稳定性优势。实验结果显示其误差通常比基准方法低 1-2 个数量级。

#### 6. 主要结论与发现
*   **符号恢复准确：** Symbolic-KAN 能在训练结束后准确识别出目标函数中的数学项（如 $x^2$ 或 $\sin$），并剔除无关项。
*   **高精度参数估计：** 在逆问题中，对物理参数的估计误差极低（通常 $< 1\%$），且在数据稀疏情况下表现稳健。
*   **物理一致性：** 在求解 PDE 时，网络自动选择的基元类型与该 PDE 的解析解结构高度吻合，证明了其具有“发现”物理规律的能力。
*   **性能优于基准：** 相比于样条或多项式 KAN，Symbolic-KAN 产生的模型更紧凑，且在长程预测和复杂域问题上更不易崩溃。

#### 7. 优点（亮点）
*   **端到端符号化：** 无需像传统方法那样在训练后进行复杂的符号拟合（Post-hoc fitting），网络本身就是公式。
*   **解耦设计：** 将特征投影（线性）与基元形状（非线性仿射变换）分离，提高了优化效率和可解释性。
*   **预选器功能：** 它可以作为 SINDy 等稀疏回归方法的“预选器”，帮助缩小候选函数库的范围。

#### 8. 不足与局限
*   **基元库依赖：** 模型的成功高度依赖于预定义的基元库。如果真实的物理项不在库中（例如复杂的特殊函数），模型可能只能给出近似表示。
*   **外推风险：** 尽管比 MLP 有所改善，但在完全脱离训练分布的数据上，神经网络固有的外推难题依然存在。
*   **高维扩展性：** 实验主要集中在 1D 和 2D 问题，对于极高维（如百维以上）的输入，门控机制的搜索空间和收敛稳定性尚待验证。

（完）
