---
title: "FactorEngine: A Program-level Knowledge-Infused Factor Mining Framework for Quantitative Investment"
title_zh: FactorEngine：一种用于量化投资的程序级知识注入因子挖掘框架
authors: "Qinhong Lin, Ruitao Feng, Yinglun Feng, Zhenxin Huang, Yukun Chen, Zhongliang Yang, Linna Zhou, Binjie Fei, Jiaqi Liu, Yu Li"
date: 2026-03-17
pdf: "https://arxiv.org/pdf/2603.16365v1"
tags: ["query:sr-alg"]
score: 8.0
evidence: 将预测信号自动发现为图灵完备的代码
tldr: FactorEngine是一个程序级量化因子挖掘框架，旨在解决传统符号化方法表达力不足及神经预测器不可解释的问题。它将因子视为图灵完备的代码，通过逻辑修订与参数优化分离、LLM引导搜索与贝叶斯超参搜索分离等机制提升效率。此外，它利用多智能体流水线从财务报告中提取知识并转化为因子，结合经验知识库进行迭代优化，在真实市场数据回测中显著提升了预测稳定性和投资组合表现。
selection_source: fresh_fetch
motivation: 现有的因子挖掘方法在表达能力、可解释性、计算效率以及应对市场环境变化方面存在局限。
method: 提出FactorEngine框架，通过逻辑与参数分离、LLM引导的程序级搜索以及基于财务报告的知识注入模块，实现因子的自动化发现与优化。
result: 在真实OHLCV数据回测中，该方法在IC/ICIR、年化收益率和夏普比率等指标上均显著优于现有基准方法。
conclusion: FactorEngine通过结合大语言模型的程序生成能力与结构化知识注入，为可解释且高效的量化因子挖掘提供了新的SOTA方案。
---

## 摘要
我们研究了 Alpha 因子挖掘，即从噪声大、非平稳的市场数据中自动发现预测信号。在实际应用中，要求挖掘出的因子必须是直接可执行且可审计的，并且发现过程在大规模计算下保持可行。现有的符号化方法受限于表达能力，而神经预测器往往牺牲可解释性以换取性能，且容易受到市场状态切换和过拟合的影响。我们提出了 FactorEngine (FE)，这是一个程序级因子发现框架，它将因子视为图灵完备的代码，并通过三种分离来提高有效性和效率：(i) 逻辑修订与参数优化分离，(ii) LLM 引导的方向性搜索与贝叶斯超参数搜索分离，以及 (iii) LLM 使用与本地计算分离。FE 进一步整合了一个知识注入的引导模块，通过闭环多智能体提取-验证-代码生成流水线将非结构化财务报告转化为可执行的因子程序，并建立了一个支持轨迹感知优化（包括从失败中学习）的经验知识库。在对真实世界 OHLCV 数据的广泛回测中，FE 产生的因子具有显著更强的预测稳定性和投资组合影响力——例如，与基准方法相比，具有更高的 IC/ICIR（以及 Rank IC/ICIR）和更好的 AR/夏普比率，实现了最先进的预测和投资组合表现。

## Abstract
We study alpha factor mining, the automated discovery of predictive signals from noisy, non-stationary market data-under a practical requirement that mined factors be directly executable and auditable, and that the discovery process remain computationally tractable at scale. Existing symbolic approaches are limited by bounded expressiveness, while neural forecasters often trade interpretability for performance and remain vulnerable to regime shifts and overfitting. We introduce FactorEngine (FE), a program-level factor discovery framework that casts factors as Turing-complete code and improves both effectiveness and efficiency via three separations: (i) logic revision vs. parameter optimization, (ii) LLM-guided directional search vs. Bayesian hyperparameter search, and (iii) LLM usage vs. local computation. FE further incorporates a knowledge-infused bootstrapping module that transforms unstructured financial reports into executable factor programs through a closed-loop multi-agent extraction-verification-code-generation pipeline, and an experience knowledge base that supports trajectory-aware refinement (including learning from failures). Across extensive backtests on real-world OHLCV data, FE produces factors with substantially stronger predictive stability and portfolio impact-for example, higher IC/ICIR (and Rank IC/ICIR) and improved AR/Sharpe, than baseline methods, achieving state-of-the-art predictive and portfolio performance.

---

## 论文详细总结（自动生成）

这篇论文介绍了一个名为 **FactorEngine (FE)** 的程序级因子挖掘框架，旨在通过大语言模型（LLM）与贝叶斯优化的结合，实现高效、可解释且具备金融逻辑的量化因子自动发现。

以下是对该论文的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：如何在噪声大、非平稳的金融市场中，自动化地发现既具有强大预测能力，又具备可解释性、可审计性且计算高效的 Alpha 因子。
*   **研究动机**：
    *   **传统符号化方法限制**：如遗传算法（GP）受限于预定义的算子库，表达能力有限，难以捕捉复杂的市场逻辑。
    *   **神经网络方法缺陷**：虽然性能强，但属于“黑箱”模型，缺乏可解释性，且在市场环境变化（Regime Shift）时容易过拟合。
    *   **现有 LLM 方法瓶颈**：现有的基于 LLM 的因子挖掘往往将逻辑推理与参数调优混为一谈，导致计算效率低下，且难以有效整合非结构化的金融研究报告知识。

### 2. 方法论
FactorEngine 的核心思想是将因子视为**图灵完备的 Python 程序**，并通过“三项分离”机制提升效率：
*   **核心架构**：
    1.  **引导模块 (Bootstrapping Module)**：利用多智能体流水线（提取-验证-代码生成），将非结构化金融研究报告（PDF）转化为可执行的 Python 因子代码，作为初始因子池。
    2.  **进化模块 (Evolution Module)**：采用**宏观-微观协同进化**机制。
        *   **宏观层面**：LLM 智能体负责逻辑演化（修改代码逻辑），引入“经验链 (CoE)”让模型从历史失败和成功轨迹中学习。
        *   **微观层面**：将参数优化（如窗口大小、衰减系数）委托给本地的**贝叶斯搜索**（如 TPE 算法），无需 LLM 参与，极大节省了 API 成本并提升了精度。
    3.  **集成模块 (Integration Module)**：通过轻量级阈值筛选精选因子，并使用多因子模型（如 LightGBM）进行组合回测。
*   **关键技术**：
    *   **多岛进化 (Multi-island Evolution)**：并行运行多个进化轨迹，并定期进行优良因子迁移，增加种群多样性。
    *   **计算加速**：使用 `Polars` 库代替 `Pandas` 进行高性能向量化计算，并引入数据缓存机制。

### 3. 实验设计
*   **数据集**：中国 A 股全市场 OHLCV 数据，评估目标为 **CSI300** 和 **CSI500** 指数。
*   **时间跨度**：训练集 (2008-2014)，验证集 (2015-2016)，测试集 (2017-2024)。
*   **基准方法 (Benchmarks)**：
    *   传统模型：LightGBM, LSTM, Transformer, TRA。
    *   符号化/智能体方法：Alpha158 (手工因子), GPlearn (遗传算法), AlphaAgent, RD-Agent-Quant。
*   **对比设置**：分别在 200 次和 400 次迭代预算下进行对比。

### 4. 资源与算力
*   **硬件**：服务器配备 **56 核 CPU**，支持 56 个并行线程。
*   **LLM 模型**：主实验使用 **Gemini-2.5-Pro**，消融实验对比了 GPT-4o 和 Gemini-2.5-Flash-Lite。
*   **效率提升**：通过本地计算与 LLM 分离，数据加载时间从 30 秒降低至 1 秒以内，整体运行速度显著快于同类智能体框架。

### 5. 实验数量与充分性
*   **实验规模**：进行了大规模的回测，涵盖了预测性指标（IC, ICIR, Rank IC）和投资组合指标（年化收益 AR, 夏普比率 SR, 最大回撤 MDD）。
*   **消融实验**：
    *   验证了贝叶斯微观搜索的有效性（显著优于无搜索版本）。
    *   验证了不同 LLM 底座的影响。
    *   验证了“经验链 (CoE)”和“多岛配置”对性能的增益。
*   **客观性**：实验采用了严格的时间序列分割，确保无数据泄露；同时对因子多样性进行了 MDS 空间可视化分析，证明了该方法生成的因子冗余度更低。

### 6. 主要结论与发现
*   **性能领先**：FE 在所有主要指标上均达到 SOTA。在 CSI300 市场上，相比 Alpha158，FE 注入报告知识后 IC 提升了 58%，年化超额收益提升了 126%。
*   **知识注入的价值**：从财务报告中提取的因子（FE-report）表现优于仅从手工因子进化的版本（FE-alpha），说明金融先验知识能有效引导搜索。
*   **抗衰减能力**：FE 生成的因子在长达 8 年的测试期内表现出更强的稳定性，Alpha 衰减速度较慢。

### 7. 优点与亮点
*   **程序级表达**：突破了公式化因子的限制，支持条件分支、循环等复杂逻辑。
*   **高效协同**：巧妙地将“昂贵的逻辑推理（LLM）”与“密集的数值优化（贝叶斯）”分离，解决了 LLM 调参不准且贵的问题。
*   **闭环学习**：通过 CoE 机制让智能体具备了“反思”能力，能从失败的尝试中总结教训。

### 8. 不足与局限
*   **LLM 潜在偏见**：虽然报告日期限制在 2017 年前以防泄露，但 LLM 本身的预训练数据可能包含后期市场信息，存在潜在的隐性过拟合风险。
*   **计算成本**：尽管比同类方法快，但大规模并行回测和多次 LLM 调用对于个人投资者而言仍有较高的算力门槛。
*   **实盘差距**：实验虽考虑了佣金、印花税和滑点，但真实市场的流动性冲击和执行成本可能比模拟环境更复杂。

（完）
