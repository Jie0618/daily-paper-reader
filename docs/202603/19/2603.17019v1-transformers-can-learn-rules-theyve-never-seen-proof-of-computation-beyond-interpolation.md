---
title: "Transformers Can Learn Rules They've Never Seen: Proof of Computation Beyond Interpolation"
title_zh: Transformer 可以学习从未见过的规则：超越插值计算的证明
authors: Andy Gray
date: 2026-03-17
pdf: "https://arxiv.org/pdf/2603.17019v1"
tags: ["query:sr-alg"]
score: 8.0
evidence: Transformer 输出中间符号推导以进行规则推理
tldr: 本研究探讨了Transformer模型是否具备超越训练数据插值、推断未见规则的能力。通过元胞自动机（XOR规则）和符号运算链两个受控实验，研究发现Transformer在排除插值可能性的情况下，仍能通过多步约束传播和中间步骤推导，成功学习并应用未见过的规则。这证明了Transformer在架构上具备发现并表达潜在规则结构的能力，而非仅仅依赖相似性匹配。
selection_source: fresh_fetch
motivation: 旨在验证Transformer是仅能进行基于相似性的插值，还是能够推断并执行训练集中未直接出现的逻辑规则。
method: 采用排除特定输入模式的XOR元胞自动机和需要输出中间推导步骤的符号运算链，对比Transformer与多种插值基准模型的表现。
result: "Transformer在XOR实验中实现了100%的规则恢复，在符号运算实验中显著超越所有插值基准，且性能高度依赖于中间步骤的监督。"
conclusion: 实验结果提供了存在性证明，证实Transformer能够学习并显式表达训练中未见的规则结构，反驳了其仅能进行插值的观点。
---

## 摘要
大语言模型（LLM）辩论中的一个核心问题是，Transformer 是否能够推断出训练中缺失的规则，或者其表现出的泛化能力是否仅能归结为对观察到的示例进行基于相似性的插值。我们在两个受控设置中测试了一个强“仅限插值”假设：一个是通过构造和证明排除了插值，另一个是成功需要输出中间符号推导而非仅最终答案。在实验 1 中，我们使用具有纯 XOR 转移规则的元胞自动机，并从训练中移除特定的局部输入模式；由于 XOR 是线性不可分的，每个留出模式的最近邻都具有相反的标签，因此基于相似性的预测器在留出区域会失败。然而，一个双层 Transformer 恢复了该规则（最高 100%；60 次收敛运行中有 47 次成功），且电路提取识别出了 XOR 计算。性能取决于多步约束传播：在没有展开的情况下，准确率仅与输出偏差一致（63.1%），而软展开达到了 96.7%。在实验 2 中，我们研究了整数上的符号算子链，并留出了一个算子对；模型必须以类似证明的格式输出中间步骤和最终答案。在所有 49 个留出对中，Transformer 均超过了所有插值基准（平均 41.8%，最高 78.6%；平均 KRR 为 4.3%；KNN 和 MLP 在每个对上的得分均为 0%），而移除中间步骤监督会降低性能。结合一项显示标准 Transformer 块可以实现精确局部布尔规则的构造，这些结果提供了一个存在性证明，即 Transformer 可以学习训练中未直接观察到的规则结构并显式地表达它，从而排除了“仅限插值”解释的最强架构形式：即 Transformer 在原则上无法发现和传达未见过的规则，同时保留了这种行为在大规模语言训练中何时出现的问题。

## Abstract
A central question in the LLM debate is whether transformers can infer rules absent from training, or whether apparent generalisation reduces to similarity-based interpolation over observed examples. We test a strong interpolation-only hypothesis in two controlled settings: one where interpolation is ruled out by construction and proof, and one where success requires emitting intermediate symbolic derivations rather than only final answers. In Experiment 1, we use a cellular automaton with a pure XOR transition rule and remove specific local input patterns from training; since XOR is linearly inseparable, each held-out pattern's nearest neighbours have the opposite label, so similarity-based predictors fail on the held-out region. Yet a two-layer transformer recovers the rule (best 100%; 47/60 converged runs), and circuit extraction identifies XOR computation. Performance depends on multi-step constraint propagation: without unrolling, accuracy matches output bias (63.1%), while soft unrolling reaches 96.7%. In Experiment 2, we study symbolic operator chains over integers with one operator pair held out; the model must emit intermediate steps and a final answer in a proof-like format. Across all 49 holdout pairs, the transformer exceeds every interpolation baseline (mean 41.8%, up to 78.6%; mean KRR 4.3%; KNN and MLP score 0% on every pair), while removing intermediate-step supervision degrades performance. Together with a construction showing that a standard transformer block can implement exact local Boolean rules, these results provide an existence proof that transformers can learn rule structure not directly observed in training and express it explicitly, ruling out the strongest architectural form of interpolation-only accounts: that transformers cannot in principle discover and communicate unseen rules, while leaving open when such behaviour arises in large-scale language training.

---

## 论文详细总结（自动生成）

以下是对论文《Transformers Can Learn Rules They've Never Seen: Proof of Computation Beyond Interpolation》的结构化总结：

### 1. 核心问题与整体含义
*   **研究动机**：关于大语言模型（LLM）是具备真正的“计算/推理”能力，还是仅仅是复杂的“随机鹦鹉”（即基于训练数据的相似性插值），学术界一直存在激烈争论。
*   **核心问题**：Transformer 架构是否在原则上能够推断并执行训练集中完全缺失的逻辑规则？
*   **整体含义**：论文通过受控实验提供了一个“存在性证明”，证实 Transformer 能够超越简单的模式匹配，通过约束传播（Constraint Propagation）发现并显式表达未见过的规则。

### 2. 方法论
*   **核心思想**：构建一个插值法在数学上注定失败的任务（如 XOR 逻辑），观察 Transformer 是否能通过间接信号恢复该规则。
*   **关键技术细节**：
    *   **元胞自动机（CA）实验**：使用纯 XOR 转移规则（Rule 150）。在训练中故意移除特定的局部输入模式（留出集），使插值模型（如 KNN）因 XOR 的线性不可分性而预测失败。
    *   **约束传播机制**：模型在 $t+1$ 时刻对隐藏模式的错误预测会导致 $t+2$ 时刻可见位置的误差，从而产生间接梯度信号，迫使模型推断出隐藏的逻辑。
    *   **软展开（Soft Unrolling）**：将 $t+1$ 时刻的概率输出（而非硬分类）反馈回 $t+2$ 时刻，保持梯度的连续传播。
    *   **符号运算链**：要求模型处理整数运算序列 $(a \text{ op}_1 b) \text{ op}_2 c = d$，并强制模型输出中间步骤 $e$ 和算子身份。
*   **理论证明**：附录中给出了数学证明，展示了单个标准 Transformer 块如何通过注意力机制进行局部路由，并利用 ReLU FFN 实现精确的布尔逻辑规则。

### 3. 实验设计
*   **场景与数据集**：
    *   **实验 1**：一维元胞自动机，包含 Rule 150（纯 XOR）及其他复杂规则（Rule 30, 106, D, G）。
    *   **实验 2**：6 位整数（0-63）的符号算子链，涉及 XOR、OR、AND、位移等 7 种算子。
*   **基准测试（Benchmark）**：
    *   **插值基准**：K-最近邻（KNN）、高斯过程（GP）、RBF 核支持向量机（SVM）、随机森林（RF）、核岭回归（KRR）和多层感知机（MLP）。
*   **对比方法**：对比了有无“软展开”、有无中间步骤监督、以及不同邻域宽度下的表现。

### 4. 资源与算力
*   **模型规模**：采用小型 Transformer（2 层，4 头，嵌入维度 64，FFN 维度 128），参数量约 180K。
*   **算力说明**：论文**未明确说明**具体的 GPU 型号、数量或总训练时长。但考虑到模型极小且数据集为合成数据（20K 样本），预计算力需求较低，可在单张消费级 GPU 或笔记本电脑上完成。

### 5. 实验数量与充分性
*   **实验规模**：
    *   CA 实验针对 Rule 150 运行了 60 次独立随机种子实验，其他规则各 10 次。
    *   符号运算实验测试了全部 49 种可能的算子留出组合，每种组合 3 个种子。
*   **充分性与客观性**：实验设计非常严谨。通过数学证明（定理 1-4）预先确定了插值法的准确率下限（0%），确保了对比的公平性。同时进行了大量的消融实验（如层消融、输入干扰测试、Logit Lens 分析），从机制上验证了模型确实学习了 XOR 逻辑而非走捷径。

### 6. 主要结论与发现
*   **超越插值**：在插值法准确率为 0% 的区域，Transformer 最高达到了 100% 的准确率，成功恢复了 XOR 规则。
*   **机制发现**：Transformer 的成功依赖于多步约束传播。没有“展开”时准确率仅为 63.1%（接近随机偏置），而“软展开”使其达到 96.7%。
*   **相变现象**：规则的学习呈现“格罗克”（Grokking）式的相变，即在监督任务准确率达到约 85% 后，留出集的准确率会突然飙升。
*   **符号化表达**：在符号实验中，模型不仅计算正确，还能将发现的规则外部化为学习到的符号，证明了其具备传达未见规则的能力。

### 7. 优点
*   **理论与实证结合**：不仅有实验结果，还提供了关于 Transformer 架构计算能力的数学证明。
*   **因果验证**：通过电路提取和干预实验（Causal Substitution），清晰地展示了模型内部的逻辑门结构。
*   **反驳有力**：直接回应了“随机鹦鹉”理论，证明了架构本身具备超越训练分布的计算潜力。

### 8. 不足与局限
*   **合成环境限制**：实验基于高度结构化的合成数据，尚未在自然语言等噪声大、规则模糊的真实大规模语料上验证。
*   **初始化敏感**：实验结果呈现双峰分布（Bimodal），并非所有随机种子都能收敛到正确规则，显示出训练的某种不稳定性。
*   **规模外推性**：虽然证明了“架构可以”，但并不代表现有的超大规模 LLM 在实际训练中“一定会”以这种方式学习规则。
*   **任务范围**：目前仅限于确定性的布尔逻辑和简单算术，对于随机性或连续值领域的适用性有待研究。

（完）
