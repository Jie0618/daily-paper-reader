---
title: "Adaptive Methods Are Preferable in High Privacy Settings: An SDE Perspective"
title_zh: 高隐私设置下自适应方法更具优势：基于 SDE 的视角
authors: "Enea Monzio Compagnoni, Alessandro Stanghellini, Rustem Islamov, Aurelien Lucchi, Anastasiia Koloskova"
date: 2026-03-03
pdf: "https://arxiv.org/pdf/2603.03226v1"
tags: ["query:opt-lm"]
score: 8.0
evidence: 基于随机微分方程的私有自适应优化器（如DP-SignSGD）分析
tldr: 本研究通过随机微分方程（SDE）视角，深入探讨了差分隐私（DP）优化器中噪声与自适应性的交互。研究对比了 DP-SGD 和 DP-SignSGD，发现在高隐私（小 ε）或大噪声环境下，DP-SignSGD 的收敛速度和效用权衡优于 DP-SGD。更重要的是，自适应方法的最佳学习率与 ε 无关，使其在不同隐私级别下更易于调参和迁移，在实际应用中比非自适应方法更具优势。
selection_source: carryover_cache
motivation: 旨在从理论层面解释并验证自适应优化方法在差分隐私训练的高隐私需求场景下为何表现更优。
method: 首次采用随机微分方程（SDE）框架，对逐样本裁剪下的 DP-SGD 和 DP-SignSGD 的收敛行为进行建模与对比分析。
result: 发现 DP-SignSGD 在高隐私制度下具有更优的收敛速度，且其最优学习率不随隐私预算 ε 变化，表现出极强的鲁棒性。
conclusion: 自适应方法（如 DP-SignSGD 和 DP-Adam）在隐私保护场景中更具实践价值，因为其超参数在不同隐私级别间具有良好的迁移性。
---

## 摘要
随着隐私法规的收紧，差分隐私 (DP) 正成为大规模训练的核心。我们通过随机微分方程 (SDE) 的视角重新审视了优化中 DP 噪声与自适应性之间的相互作用，提供了首个基于 SDE 的隐私优化器分析。针对逐样本裁剪下的 DP-SGD 和 DP-SignSGD，我们展示了在固定超参数下的鲜明对比：DP-SGD 以与 $\varepsilon$ 无关的速度收敛，其隐私-效用权衡为 $\mathcal{O}(1/\varepsilon^2)$；而 DP-SignSGD 的收敛速度与 $\varepsilon$ 成线性关系，权衡为 $\mathcal{O}(1/\varepsilon)$，在强隐私或大批量噪声机制下占据主导地位。相比之下，在最优学习率下，两种方法都能达到相当的理论渐近性能；然而，DP-SGD 的最优学习率随 $\varepsilon$ 线性缩放，而 DP-SignSGD 的最优学习率基本上与 $\varepsilon$ 无关。这使得自适应方法更具实用性，因为它们的超参数可以在不同的隐私级别之间迁移，几乎不需要重新调优。实验结果在训练和测试指标上证实了我们的理论，并从 DP-SignSGD 经验性地扩展到了 DP-Adam。

## Abstract
Differential Privacy (DP) is becoming central to large-scale training as privacy regulations tighten. We revisit how DP noise interacts with adaptivity in optimization through the lens of stochastic differential equations, providing the first SDE-based analysis of private optimizers. Focusing on DP-SGD and DP-SignSGD under per-example clipping, we show a sharp contrast under fixed hyperparameters: DP-SGD converges at a Privacy-Utility Trade-Off of $\mathcal{O}(1/\varepsilon^2)$ with speed independent of $\varepsilon$, while DP-SignSGD converges at a speed linear in $\varepsilon$ with an $\mathcal{O}(1/\varepsilon)$ trade-off, dominating in high-privacy or large batch noise regimes. By contrast, under optimal learning rates, both methods achieve comparable theoretical asymptotic performance; however, the optimal learning rate of DP-SGD scales linearly with $\varepsilon$, while that of DP-SignSGD is essentially $\varepsilon$-independent. This makes adaptive methods far more practical, as their hyperparameters transfer across privacy levels with little or no re-tuning. Empirical results confirm our theory across training and test metrics, and empirically extend from DP-SignSGD to DP-Adam.

---

## 论文详细总结（自动生成）

### 论文总结：高隐私设置下自适应方法更具优势：基于 SDE 的视角

#### 1. 核心问题与整体含义（研究动机和背景）
随着隐私法规（如美国行政命令、欧盟 AI 法案）的收紧，差分隐私（Differential Privacy, DP）已成为大规模机器学习训练的标准。然而，DP 中的**逐样本裁剪（per-example clipping）**和**噪声注入**如何影响优化动力学，尤其是自适应优化器（如 Adam）与非自适应优化器（如 SGD）在不同隐私预算 $\varepsilon$ 下的表现差异，仍缺乏深入的理论解释。本文的研究动机在于通过**随机微分方程（SDE）**这一数学工具，首次对私有优化器进行连续时间建模，旨在揭示自适应方法在强隐私需求（小 $\varepsilon$）场景下表现更优的内在机制。

#### 2. 论文提出的方法论
论文的核心思想是将离散的优化迭代过程近似为连续的 SDE，从而分析其漂移（Drift）和扩散（Diffusion）项。
*   **核心模型**：重点分析了 **DP-SGD** 和 **DP-SignSGD**（作为 DP-Adam 的理论代理）。
*   **关键技术细节**：
    *   **SDE 建模**：推导了在逐样本裁剪下的 SDE 形式。研究发现，DP 噪声对自适应和非自适应方法的影响在结构上是不同的。
    *   **协议 A（固定超参数）**：分析在不重新调优学习率 $\eta$ 的情况下，改变 $\varepsilon$ 对性能的影响。结论显示 DP-SGD 的效用权衡为 $O(1/\varepsilon^2)$，而 DP-SignSGD 为 $O(1/\varepsilon)$。
    *   **协议 B（最优调优）**：分析针对每个 $\varepsilon$ 搜索最优超参数的情况。理论推导证明 DP-SGD 的最优学习率 $\eta^*$ 与 $\varepsilon$ 成线性比例（$\eta^* \propto \varepsilon$），而 DP-SignSGD 的最优学习率基本与 $\varepsilon$ 无关。
*   **噪声假设**：采用了重尾（Student-t 分布）的批处理噪声模型，以更真实地捕捉逐样本裁剪后的梯度分布。

#### 3. 实验设计
*   **数据集/场景**：
    *   **合成实验**：二次凸函数（Quadratic）和四次非凸函数（Quartic），用于验证 SDE 模型的准确性。
    *   **真实任务**：IMDB（情感分析，逻辑回归）、StackOverflow（文本分类，大规模逻辑回归）、MovieLens-100k（推荐系统，非凸矩阵分解）。
*   **Benchmark 与对比方法**：以 **DP-SGD** 为基准，对比了 **DP-SignSGD** 和实际应用中更常用的 **DP-Adam**。
*   **评估指标**：训练损失（Train Loss）、测试损失（Test Loss）以及梯度范数。

#### 4. 资源与算力
论文**未明确说明**具体的硬件型号（如 GPU 数量、型号）或总训练时长。但提到实验基于 Google 发布的官方 GitHub 仓库 `DP2` 进行，且实验涵盖了从简单逻辑回归到拥有 500 万参数的 StackOverflow 任务，显示了一定的计算规模。

#### 5. 实验数量与充分性
*   **实验规模**：论文在 3 个真实数据集和多个合成函数上进行了实验，每组实验通常包含多次重复（如 10 次或 200 次）以获取置信区间。
*   **充分性与公平性**：
    *   实验涵盖了凸与非凸、训练与测试指标，验证了理论的泛化性。
    *   通过“协议 A”和“协议 B”的对比，客观地展示了在“有无调参预算”两种现实情况下的方法优劣。
    *   实验不仅验证了理论中的 DP-SignSGD，还将其结论成功扩展到了实际的 DP-Adam 上，增强了说服力。

#### 6. 主要结论与发现
*   **隐私-效用权衡**：在固定超参数下，DP-SignSGD 在高隐私（小 $\varepsilon$）或大批量噪声机制下优于 DP-SGD。
*   **调参鲁棒性（核心发现）**：自适应方法（DP-SignSGD/Adam）的最优学习率对 $\varepsilon$ 具有**不变性**。这意味着在一种隐私级别下调优的参数可以直接迁移到另一种级别，而 DP-SGD 必须随 $\varepsilon$ 的变化重新寻找学习率。
*   **收敛速度**：DP-SGD 的收敛速度独立于 $\varepsilon$，而 DP-SignSGD 的速度随 $\varepsilon$ 线性缩放。
*   **实践建议**：如果调参预算有限或隐私预算消耗敏感，应优先选择自适应优化器。

#### 7. 优点
*   **理论创新**：首次利用 SDE 视角填补了 DP 优化器理论分析的空白。
*   **实践指导意义强**：解释了为什么在实际 DP 训练中 Adam 往往比 SGD 更容易使用（因为学习率更稳定）。
*   **验证全面**：理论推导与实证结果高度吻合，且从训练集扩展到了测试集。

#### 8. 不足与局限
*   **代理模型的局限**：理论分析主要针对 DP-SignSGD，虽然实验显示其结论适用于 DP-Adam，但两者在二阶矩估计等细节上仍有数学差异。
*   **SDE 近似限制**：SDE 模型在学习率极大或梯度极度不连续的情况下可能失效（尽管在深度学习常见机制下表现良好）。
*   **模型规模**：实验主要集中在逻辑回归和矩阵分解，尚未在大规模 Transformer（如 LLM 预训练）上进行直接验证。

（完）
