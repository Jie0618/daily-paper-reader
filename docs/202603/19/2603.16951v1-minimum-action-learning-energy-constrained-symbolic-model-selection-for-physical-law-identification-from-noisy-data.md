---
title: "Minimum-Action Learning: Energy-Constrained Symbolic Model Selection for Physical Law Identification from Noisy Data"
title_zh: 最小作用量学习：从噪声数据中识别物理定律的能量约束符号模型选择
authors: Martin G. Frasch
date: 2026-03-16
pdf: "https://arxiv.org/pdf/2603.16951v1"
tags: ["query:sr-alg"]
score: 10.0
evidence: 从噪声数据中识别物理定律的符号模型选择
tldr: "本研究提出最小作用量学习（MAL）框架，旨在从高噪声观测数据中识别物理定律。该方法通过最小化结合轨迹重建、架构稀疏性和能量守恒约束的三重作用量泛函，从符号库中筛选力学定律。利用宽模板加速度匹配技术将噪声方差降低万倍，使极低信噪比下的学习成为可能。在开普勒引力和胡克定律基准测试中，MAL不仅实现了100%的定律识别率，还显著降低了计算能耗，为可解释且符合物理约束的模型选择提供了新途径。"
selection_source: fresh_fetch
motivation: 针对从高噪声观测数据中准确识别具有物理一致性和可解释性的符号力学定律这一难题。
method: 提出MAL框架，通过三重作用量泛函结合宽模板加速度匹配预处理技术，在稀疏性与能量守恒约束下进行模型选择。
result: "在开普勒和胡克定律测试中实现了100%的识别准确率，且相比仅基于预测误差的基准方法减少了40%的能耗。"
conclusion: MAL为物理定律识别提供了一个兼具噪声鲁棒性、能量约束和符号可解释性的高效模型选择方案。
---

## 摘要
从噪声观测数据中识别物理定律是科学机器学习中的一个核心挑战。我们提出了最小作用量学习（MAL），这是一个通过最小化结合了轨迹重构、架构稀疏性和能量守恒强制执行的三重作用量泛函，从预定义的基库中选择符号力定律的框架。一种宽模板加速度匹配技术将噪声方差降低了 10,000 倍，将一个棘手的问题（信噪比 SNR ~0.02）转化为可学习的问题（SNR ~1.6）；这种预处理是所有测试方法（包括 SINDy 变体）共享的关键赋能技术。在开普勒引力和胡克定律这两个基准测试中，MAL 以约 0.07 kWh 的能耗（比仅基于预测误差的基准方法降低了 40%）恢复了正确的力定律，其中开普勒指数 p = 3.01 +/- 0.01。开普勒定律的原始正确基率为 40%，胡克定律为 90%；基于能量守恒的标准在所有情况下都能辨别出真实的力定律，从而实现了 100% 的流水线级识别。基库敏感性实验表明，近邻混杂因素会降低选择性能（加入 r^{-2.5} 和 r^{-1.5} 后降至 20%），而远端添加项则无害，且即使在缺少正确基的情况下，守恒诊断仍然具有参考价值。与鲁棒噪声 SINDy 变体、哈密顿神经网络和拉格朗日神经网络的直接比较证实了 MAL 的独特地位：一种结合了符号基识别与动力学展开验证的可解释、能量约束模型选择方法。

## Abstract
Identifying physical laws from noisy observational data is a central challenge in scientific machine learning. We present Minimum-Action Learning (MAL), a framework that selects symbolic force laws from a pre-specified basis library by minimizing a Triple-Action functional combining trajectory reconstruction, architectural sparsity, and energy-conservation enforcement. A wide-stencil acceleration-matching technique reduces noise variance by 10,000x, transforming an intractable problem (SNR ~0.02) into a learnable one (SNR ~1.6); this preprocessing is the critical enabler shared by all methods tested, including SINDy variants. On two benchmarks -- Kepler gravity and Hooke's law -- MAL recovers the correct force law with Kepler exponent p = 3.01 +/- 0.01 at ~0.07 kWh (40% reduction vs. prediction-error-only baselines). The raw correct-basis rate is 40% for Kepler and 90% for Hooke; an energy-conservation-based criterion discriminates the true force law in all cases, yielding 100% pipeline-level identification. Basis library sensitivity experiments show that near-confounders degrade selection (20% with added r^{-2.5} and r^{-1.5}), while distant additions are harmless, and the conservation diagnostic remains informative even when the correct basis is absent. Direct comparison with noise-robust SINDy variants, Hamiltonian Neural Networks, and Lagrangian Neural Networks confirms MAL's distinct niche: interpretable, energy-constrained model selection that combines symbolic basis identification with dynamical rollout validation.

---

## 论文详细总结（自动生成）

### 论文总结：最小作用量学习（Minimum-Action Learning, MAL）

#### 1. 核心问题与整体含义
论文针对**从高噪声观测数据中自动识别物理定律**这一科学机器学习的核心挑战，提出了“最小作用量学习”（MAL）框架。研究动机结合了物理学中的“最小作用量原理”与生物系统中的“能量/代谢约束”：
*   **背景**：传统的符号回归或神经网络方法在面对极高噪声（信噪比 SNR $\approx$ 0.02）时往往失效，且缺乏物理一致性。
*   **核心目标**：通过在神经网络训练中引入能量最小化和对称性约束，实现既具有符号可解释性、又符合物理守恒律、且计算高效的模型选择。

#### 2. 方法论：核心思想与关键技术
MAL 框架通过一个**三重作用量泛函（Triple-Action functional）**来驱动模型训练和架构搜索：
*   **核心公式**：$L = \alpha_I L_{Imax} + \alpha_E L_{Emin} + \alpha_S L_{Symmetry}$
    *   **信息最大化 ($L_{Imax}$)**：结合轨迹重建误差和**宽模板加速度匹配（Wide-stencil acceleration matching）**。后者通过跨步长（stride $s=10$）计算二阶导数，将噪声方差降低了 10,000 倍，是处理极低信噪比数据的关键。
    *   **能量最小化 ($L_{Emin}$)**：通过门控熵和系数稀疏性惩罚，模拟生物代谢约束，驱动网络从“软”的概率分布向“硬”的离散架构（即选定特定的物理基函数）转变。
    *   **对称性强制 ($L_{Symmetry}$)**：基于诺特（Noether）定理，通过最小化能量随时间的变化量来强制执行能量守恒。
*   **双模态胶质-神经优化（BGNO）**：模拟大脑发育过程，分为“热身期”（探索）和“稀疏期”（结晶），使模型最终锁定在单一的物理基函数上。

#### 3. 实验设计
*   **数据集/场景**：
    *   **开普勒定律**：反平方引力场下的行星轨道（2D）。
    *   **胡克定律**：线性回复力下的简谐运动。
*   **基准（Benchmark）**：合成的带噪轨道数据（噪声水平为中位半径的 1%）。
*   **对比方法**：
    *   **SINDy 变体**：Vanilla SINDy, GP-SINDy, Ensemble-SINDy。
    *   **结构化神经网络**：哈密顿神经网络（HNN）、拉格朗日神经网络（LNN）。
    *   **其他**：物理启发神经网络（PINN）、数学大模型（Qwen2-Math 7B）、仅含教师强制（无能量约束）的基准模型。

#### 4. 资源与算力
*   **硬件**：NVIDIA RTX 2080 Ti GPU (11GB), Intel Xeon E5-2670 v3 CPU, 64GB RAM。
*   **训练时长**：单次实验平均耗时 **835 秒**（约 13.9 分钟）。
*   **能耗**：全系统能耗约为 **0.07 kWh**，相比仅基于预测误差的方法降低了 40%。

#### 5. 实验数量与充分性
*   **实验规模**：针对每个基准测试运行了 **10 个独立随机种子**。
*   **消融与干预实验**：
    *   测试了不同噪声水平、不同热身时长、以及物理启发式初始化对成功率的影响。
    *   **基库敏感性测试**：在基库中加入近邻混淆项（如 $r^{-2.5}$）或移除正确项，验证系统的鲁棒性。
*   **充分性评价**：实验设计较为全面，不仅对比了主流的符号识别和物理神经网络方法，还通过长程动力学展开（Rollout）验证了模型的泛化能力，实验结果具有统计学意义。

#### 6. 主要结论与发现
*   **噪声突破**：宽模板技术是成功的先决条件，使 SNR 从 0.02 提升至 1.6。
*   **识别精度**：在开普勒任务中恢复了 $p=3.01$ 的轨道指数（理论值为 3.0）；通过能量守恒判据，实现了 **100% 的流水线级物理定律识别**。
*   **能量效率**：引入 $L_{Emin}$ 约束不仅提高了模型的可解释性（架构稀疏化），还显著降低了训练能耗。
*   **内在时间尺度**：发现架构“结晶”过程具有独立于初始化的内在时间尺度（约 36 个 epoch）。

#### 7. 优点（亮点）
*   **跨学科融合**：巧妙地将生物代谢约束与物理作用量原理结合，为 AI 能源效率提供了新视角。
*   **端到端验证**：不同于 SINDy 仅提供系数，MAL 集成了动力学验证和诺特对称性诊断，能自动剔除数值拟合良好但物理错误的模型。
*   **极高鲁棒性**：在极高噪声环境下依然能保持符号识别的准确性。

#### 8. 不足与局限
*   **基库依赖**：MAL 属于模型选择框架，而非开放式符号发现，其性能受限于预定义的基库是否包含正确项。
*   **直接成功率波动**：在开普勒任务中，原始的直接选择成功率仅为 40%（受基函数近邻简并影响），需依赖后验的能量守恒诊断来达到 100%。
*   **生物类比的严谨性**：文中关于胶质细胞和代谢约束的生物学类比目前更多是结构上的启发，缺乏严格的生物物理等效性证明。

（完）
