---
title: Unsupervised Symbolic Anomaly Detection
title_zh: 无监督符号化异常检测
authors: "Md Maruf Hossain, Tim Katzke, Simon Klüttermann, Emmanuel Müller"
date: 2026-03-18
pdf: "https://arxiv.org/pdf/2603.17575v1"
tags: ["query:sr-alg"]
score: 9.0
evidence: 基于符号回归的无监督异常检测，学习人类可读的方程
tldr: 本研究提出SYRAN，一种基于符号回归的无监督异常检测方法。针对传统模型黑盒化、难以理解的问题，SYRAN通过学习一组描述正常数据不变性的可读方程（符号不变式）来识别异常。该方法在保持与最先进技术相当的检测性能的同时，实现了本质上的可解释性，能够提取出符合科学或医学规律的数学关系，为关键领域的异常监测提供了透明的决策依据。
selection_source: fresh_fetch
motivation: 现有的异常检测模型往往是高维且不透明的黑盒，缺乏内在的可解释性，难以让用户理解检测逻辑。
method: 利用符号回归学习正常数据中近似恒定的符号不变式方程组，通过计算数据对这些数学规律的偏离程度来生成异常评分。
result: 实验证明SYRAN在保持与SOTA方法相当的检测性能的同时，能生成符合已知科学或医学规律的可读方程。
conclusion: 该研究证明了符号回归可以构建出高性能且具有原生可解释性的无监督异常检测系统。
---

## 摘要
我们提出了 SYRAN，一种基于符号回归的无监督异常检测方法。与在不透明的高维模型中编码正常模式不同，我们的方法学习一组描述符号不变量（symbolic invariants）的人类可读方程：即在正常数据上近似为常数的函数。对这些不变量的偏离会产生异常评分，从而使检测逻辑在构建时即具有可解释性，而非通过事后解释获得。实验结果表明，SYRAN 具有高度的可解释性，能够提供与已知科学或医学关系相对应的方程，并保持了与最先进方法相当的强大异常检测性能。

## Abstract
We propose SYRAN, an unsupervised anomaly detection method based on symbolic regression. Instead of encoding normal patterns in an opaque, high-dimensional model, our method learns an ensemble of human-readable equations that describe symbolic invariants: functions that are approximately constant on normal data. Deviations from these invariants yield anomaly scores, so that the detection logic is interpretable by construction, rather than via post-hoc explanation. Experimental results demonstrate that SYRAN is highly interpretable, providing equations that correspond to known scientific or medical relationships, and maintains strong anomaly detection performance comparable to that of state-of-the-art methods.

---

## 论文详细总结（自动生成）

### 论文总结：无监督符号化异常检测 (SYRAN)

#### 1. 论文的核心问题与研究动机
*   **核心问题**：现代异常检测（AD）模型（如深度神经网络、森林集成等）大多是“黑盒”，其内部逻辑由成千上万个参数构成，人类难以理解。在医疗、预测性维护等高风险领域，缺乏透明度限制了模型的可信度。
*   **研究动机**：
    *   **可解释性需求**：需要一种“本质上可解释”而非“事后解释”的方法。
    *   **科学发现**：异常往往源于对某种物理规律或不变性的偏离，提取这些规律有助于深化对数据的理解。
    *   **公平性与验证**：透明的模型有助于识别算法偏见，并允许领域专家对检测逻辑进行验证。

#### 2. 核心方法论：SYRAN 模型
*   **核心思想**：将“正常状态”建模为**符号不变式（Symbolic Invariants）**。即寻找一组数学方程 $f(x)$，使得在正常数据上 $f(x) \approx 1$，而异常数据会显著偏离该常数值。
*   **关键技术细节**：
    *   **损失函数设计**：包含三部分：
        1.  **不变性损失 ($L_1$)**：确保函数在训练集（正常数据）上接近 1。
        2.  **噪声对比项 ($L_{noise}$)**：引入随机生成的背景噪声，强制模型在非数据流形上产生偏离，从而避免产生 $f(x)=1$ 这种无意义的平凡解。
        3.  **复杂度正则化 ($L_c$)**：使用双重对数惩罚项限制方程的复杂度，确保生成的公式简洁、可读。
    *   **优化算法**：采用基于树结构的**进化算法**进行符号回归，在离散的数学算子空间中搜索最优方程结构。
    *   **集成策略**：
        *   **特征装袋（Feature Bagging）**：每个子模型仅随机选取 $K$ 个特征进行建模，以处理高维数据并增加多样性。
        *   **校准与聚合**：对每个方程的偏差进行归一化处理，通过 Sigmoid 函数映射后取平均值作为最终异常评分。

#### 3. 实验设计
*   **数据集与场景**：
    *   **物理案例**：使用太阳系行星轨道数据，验证模型能否重新发现开普勒第三定律（$T^2/a^3 \approx 1$）。
    *   **基准测试**：选取了 ADBench 库中的 19 个真实世界数据集（如医疗诊断 `breastw`、脊柱测量 `vertebral`、化学分析 `wine` 等），特征维度控制在 30 以内。
*   **对比方法（Benchmark）**：对比了 14 种主流算法，包括经典方法（IForest, KNN, LOF, PCA, OCSVM）和现代深度学习方法（DeepSVDD, DEAN, DAGMM）。

#### 4. 资源与算力
*   **算力说明**：文中**未明确指出**具体的 GPU 或 CPU 型号及数量。
*   **训练时长**：提到在进行基准测试时，为每个数据集设置了 **2 小时**的优化时间预算。对于 `fault` 和 `wdbc` 两个数据集，由于无法在 2 小时内可靠完成优化而被排除。
*   **开源资源**：提供了 GitHub 代码仓库以供复现。

#### 5. 实验数量与充分性
*   **实验规模**：
    *   在 19 个标准数据集上进行了性能评估。
    *   进行了**消融实验**，分析了复杂度权重 $\gamma$、特征子集大小 $K$ 和噪声裕度 $\Delta$ 对性能的影响。
    *   提供了定性的方程分析，展示了模型在不同领域生成的具体公式。
*   **充分性评价**：实验设计较为客观，遵循了标准的 Friedman-Wilcoxon 统计检验协议。通过 CD 图（关键差异图）展示了统计显著性，证明了其在小规模特征集上的竞争力和稳健性。

#### 6. 主要结论与发现
*   **性能表现**：SYRAN 的集成平均性能与 SOTA 方法（如 IForest, LOF）相当；而如果由专家从集成中挑选表现最好的单个方程（SYRAN max），其平均排名位居所有方法之首。
*   **可解释性验证**：模型成功在 1 分钟内发现了开普勒第三定律（人类历史上曾耗时 10 年）。在医疗数据上，生成的公式（如细胞大小均匀度）符合医学直觉。
*   **基准测试反思**：发现某些基准数据集（如 `wine`）仅靠一个简单的单变量公式即可完美分类，暗示了现有异常检测基准可能过于简单。

#### 7. 优点与亮点
*   **原生可解释性**：输出的是闭式代数方程，无需任何后验解释工具，可直接嵌入任何编程语言或嵌入式设备。
*   **专家参与**：允许领域专家审查方程，剔除不合理的逻辑，实现“人在回路”的决策。
*   **鲁棒性**：通过噪声对比和复杂度控制，有效解决了符号回归易陷入平凡解和过拟合的问题。

#### 8. 不足与局限
*   **维度灾难**：符号回归的搜索空间随特征数量呈指数级增长，目前仅适用于中低维数据（实验中限制在 30 维以内）。
*   **计算开销**：相比于 IForest 等快速算法，符号回归的进化搜索过程耗时较长（小时级）。
*   **超参数敏感性**：虽然消融实验显示性能相对稳定，但在不同领域可能仍需调整复杂度权重以平衡“准确性”与“可读性”。
*   **数据集覆盖**：未在超大规模数据集或高维图像/序列数据上进行测试。

（完）
