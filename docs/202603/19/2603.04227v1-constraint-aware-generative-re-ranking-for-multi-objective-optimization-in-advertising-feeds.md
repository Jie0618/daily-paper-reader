---
title: Constraint-Aware Generative Re-ranking for Multi-Objective Optimization in Advertising Feeds
title_zh: 广告信息流中多目标优化的约束感知生成式重排序
authors: "Chenfei Li, Hantao Zhao, Weixi Yao, Ruiming Huang, Rongrong Lu, Geng Tian, Dongying Kong"
date: 2026-03-04
pdf: "https://arxiv.org/pdf/2603.04227v1"
tags: ["query:opt-lm"]
score: 8.0
evidence: 将约束优化转化为有界神经解码
tldr: 针对广告信息流重排中平台收益与用户体验的多目标约束优化难题，本文提出一种约束感知生成式重排框架。该框架将约束优化转化为有界神经解码过程，通过统一序列生成与奖励估计，并引入约束感知奖励剪枝技术，在满足严格延迟要求的同时实现了最优序列的高效生成。实验证明该方法在工业级场景下显著提升了收益与用户参与度，为受限环境下的列表级优化提供了高效方案。
selection_source: carryover_cache
motivation: 现有的生成式重排方法在处理广告信息流的多目标约束时，面临推理延迟高和约束处理能力受限的挑战。
method: 提出一种将序列生成与奖励估计统一的框架，并结合约束感知奖励剪枝技术在解码过程中实时满足约束条件。
result: 在大规模工业数据和在线A/B测试中，该方法在满足严格延迟要求的同时，显著提升了平台收入和用户参与度。
conclusion: 该研究证明了通过有界神经解码处理受限组合优化问题的有效性，为工业级重排系统提供了高效的神经解决方案。
---

## 摘要
广告信息流中的重排序优化是一个受约束的组合问题，需要同时实现平台收入最大化和用户体验保持。最近的生成式排序方法通过自回归解码实现了列表级优化，但其部署受到高推理延迟和有限约束处理能力的阻碍。我们提出了一种约束感知的生成式重排序框架，将受约束的优化转化为有界的神经解码。与以往分离生成器和评估器模型的方法不同，我们的框架将序列生成和奖励估计统一到单个网络中。我们进一步引入了约束感知的奖励剪枝，将约束满足直接集成到解码过程中，以高效地生成最优序列。在大规模工业信息流和在线 A/B 测试上的实验表明，我们的方法在满足严格延迟要求的同时，提升了收入和用户参与度，为受约束的列表级优化提供了一种高效的神经解决方案。

## Abstract
Optimizing reranking in advertising feeds is a constrained combinatorial problem, requiring simultaneous maximization of platform revenue and preservation of user experience. Recent generative ranking methods enable listwise optimization via autoregressive decoding, but their deployment is hindered by high inference latency and limited constraint handling.   We propose a constraint-aware generative reranking framework that transforms constrained optimization into bounded neural decoding. Unlike prior approaches that separate generator and evaluator models, our framework unifies sequence generation and reward estimation into a single network.   We further introduce constraint-aware reward pruning, integrating constraint satisfaction directly into decoding to efficiently generate optimal sequences. Experiments on large-scale industrial feeds and online A/B tests show that our method improves revenue and user engagement while meeting strict latency requirements, providing an efficient neural solution for constrained listwise optimization.

---

## 论文详细总结（自动生成）

这是一份关于论文《Constraint-Aware Generative Re-ranking for Multi-Objective Optimization in Advertising Feeds》的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：在广告信息流场景中，重排序（Reranking）是一个复杂的**受约束组合优化问题**。系统需要同时最大化平台收入（广告）和用户参与度（自然内容），同时必须遵守严格的业务约束（如广告密度限制、位置间距要求、广告负载上限等）。
*   **研究动机**：
    *   **现有方法的局限**：传统的“生成器-评估器（GE）”框架将序列生成与效用评估分离，导致推理延迟高且难以高效处理硬约束。
    *   **生成式排序的挑战**：虽然自回归生成模型能捕捉列表级依赖，但其搜索空间随序列长度呈阶乘级增长（$N!$），且通常依赖后处理启发式规则来满足约束，难以保证全局最优。

### 2. 方法论：CGR 框架
核心思想是将受约束的组合优化转化为**有界神经解码（Bounded Neural Decoding）**，在单个网络中统一序列生成、奖励估计与约束处理。

*   **统一多任务架构**：
    *   采用自回归模型，通过注意力机制同时预测曝光（EXP）、点击（CLK）和列表级奖励（Reward）。
    *   **分层注意力机制**：结合全局自注意力、位置偏好交叉注意力和**局部自注意力（LSA）**。LSA 通过固定窗口掩码捕捉用户浏览的局部模式，减少全局注意力的过度平滑。
    *   **多专家融合（PLE）**：利用 Progressive Layered Extraction 结构处理多任务，实现任务间的知识共享与差异化建模。
*   **两阶段有界推理流程**：
    1.  **阶段 I：受限单广告插入**：在自然内容列表中尝试所有符合位置约束的单广告插入，选出最优中间序列。
    2.  **阶段 II：有界生成解码**：基于中间序列，枚举所有符合约束的组合（如双广告、大图广告等）。由于业务规定广告数量 $K$ 极小（通常 $K \le 2$），搜索空间从 $O(N!)$ 降至 $O(K \cdot L)$。
*   **约束感知奖励剪枝**：
    *   在解码过程中直接过滤违反业务规则的候选序列。
    *   利用奖励上界估计进行早期剪枝，剔除劣质路径，确保在严格延迟（SLA）内找到可行域内的全局最优解。

### 3. 实验设计
*   **数据集**：
    *   **公开基准**：Yahoo! LETOR, Microsoft 10K, Avito, ML1M, KR1K。
    *   **工业数据集**：来自 Bilibili 的大规模广告曝光、点击和收入日志。
*   **对比方法（Baselines）**：
    *   传统方法：LambdaMART, PRM（个性化重排）。
    *   生成式方法：自回归生成排序、非自回归生成排序。
    *   生产环境基准：现有的生成器-评估器（GE）框架。
*   **评估指标**：NDCG@10, MAP, HR@10, 以及工业指标 RPM（千次展示收入）、CTR（点击率）和约束违反率。

### 4. 资源与算力
*   **算力说明**：论文**未明确指出**具体的 GPU 型号、数量或训练时长。
*   **推理性能**：文中提到该系统部署在日均请求超过 10 亿次的生产环境，满足 P99 < 40ms 的严格延迟要求。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 5 个公开数据集和 1 个大规模工业数据集，并进行了为期 14 天的在线 A/B 测试。
*   **充分性与客观性**：
    *   实验设计较为充分，不仅对比了学术界主流模型，还对比了工业界实际运行的 GE 框架。
    *   **消融实验**验证了不同注意力模块的作用。
    *   **在线测试**结果显示了统计学显著的提升（p-value < 0.01/0.05），证明了方法的实用性。

### 6. 主要结论与发现
*   **性能提升**：在线 A/B 测试中，CGR 相比生产环境基准提升了 **6.8% 的 RPM** 和 **4.9% 的 CTR**，同时会话时长提升了 3.2%。
*   **约束满足**：在所有测试中，约束合规率达到 **100%**。
*   **效率优化**：通过将阶乘级搜索空间转化为有界空间，推理延迟比传统生成式重排降低了 **85% 以上**。
*   **理论保证**：论文证明了在广告数量受限的结构化假设下，该有界解码算法能找到可行域内的全局最优解。

### 7. 优点
*   **架构统一**：将生成、评估和约束处理整合进单一前向传播，消除了 GE 框架的冗余开销。
*   **利用结构化约束**：巧妙地将业务限制（广告少）转化为算法优势（搜索空间小），实现了效率与效果的平衡。
*   **局部注意力设计**：LSA 模块针对信息流浏览行为进行了优化，比纯全局注意力更符合实际场景。

### 8. 不足与局限
*   **场景依赖性**：该方法的效率高度依赖于“广告数量 $K$ 较小”这一前提。如果是在广告密度极高的场景（如某些纯电商搜索页），搜索空间可能会迅速膨胀。
*   **硬件细节缺失**：缺乏具体的算力消耗和模型参数量说明，不便于其他研究者评估复现成本。
*   **冷启动与泛化**：论文未详细讨论在候选广告集剧烈变动或新广告加入时，模型的实时更新与泛化能力。

（完）
