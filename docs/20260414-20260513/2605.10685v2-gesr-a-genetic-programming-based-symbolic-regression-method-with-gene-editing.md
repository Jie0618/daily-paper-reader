---
title: "GESR: A Genetic Programming-Based Symbolic Regression Method with Gene Editing"
title_zh: GESR：一种基于基因编辑的遗传规划符号回归方法
authors: "Yanjie Li, Weijun Li, Xin Ning"
date: 2026-05-11
pdf: "https://arxiv.org/pdf/2605.10685v2"
tags: ["query:sr-alg"]
score: 10.0
evidence: 基于遗传规划的符号回归用于发现数学定律
tldr: 符号回归旨在从科学数据中自动发现数学规律。针对传统遗传编程（GP）中变异和交叉操作具有盲目性、效率低下的问题，本文提出了GESR方法。该方法引入“基因编辑”理念，利用两个BERT模型分别指导符号的定向变异和交叉点的精准选择。实验证明，GESR显著提升了进化效率，在多项符号回归任务中展现出卓越的性能和计算优势。
selection_source: fresh_fetch
motivation: 传统遗传编程依赖随机变异和交叉，导致进化过程产生大量无效变异，搜索效率受限。
method: 提出基于基因编辑的GESR方法，利用两个BERT模型分别通过掩码语言建模指导符号变异和预测最优交叉点。
result: 实验结果显示，GESR在计算效率上显著优于传统GP算法，并在多个符号回归基准测试中取得了领先的综合表现。
conclusion: 将深度学习的预测能力引入进化算法的基因操作中，能有效提升符号回归的搜索效率和准确性。
---

## 摘要
数学公式是人类与自然沟通的语言。从科学数据中发现数学规律以描述自然现象，是人类几个世纪以来长期追求的目标。在人工智能领域，这一挑战被称为符号回归问题。在现有的符号回归方法中，基于进化算法的遗传规划（GP）仍然是最经典且应用最广泛的方法之一。GP 通过遗传变异和交叉模拟跨代的进化过程。然而，GP 中的变异和交叉完全是随机的。虽然这种随机性有效地模拟了自然进化，但它不可避免地会产生有益和有害的变异。如果存在一个隐喻性的“上帝”，能够预见哪些遗传变异或交叉会产生更好的结果，并据此进行有针对性的基因编辑，进化的效率将得到大幅提升。受此启发，本文提出了一种基于基因编辑的符号回归方法，称为 GESR。在 GESR 中，我们训练了两个“上帝之手”（两个 BERT 模型）。其中，第一个模型利用 BERT 的掩码语言建模能力来指导基因（表达式符号）的变异。另一个 BERT 模型通过预测交叉点来指导个体基因的交叉。实验结果表明，与传统的 GP 算法相比，GESR 显著提高了计算效率，并在多个符号回归任务中取得了强大的综合性能。

## Abstract
Mathematical formulas serve as a language through which humans communicate with nature. Discovering mathematical laws from scientific data to describe natural phenomena has been a long-standing pursuit of humanity for centuries. In the field of artificial intelligence, this challenge is known as the symbolic regression problem. Among existing symbolic regression approaches, Genetic Programming (GP) based on evolutionary algorithms remains one of the most classical and widely adopted methods. GP simulates the evolutionary process across generations through genetic mutation and crossover. However, mutations and crossovers in GP are entirely random. While this randomness effectively mimics natural evolution, it inevitably produces both beneficial and detrimental variations. If there existed a metaphorical `God` capable of foreseeing which genetic mutations or crossovers would yield superior outcomes and performing targeted gene editing accordingly, the efficiency of evolution could be substantially improved. Motivated by this idea, we propose in this paper a symbolic regression approach based on gene editing, termed GESR. In GESR, we trained two "hands of God" (two BERT models). Among them, the first leverages the BERT's masked language modeling capability to guide the mutation of genes (expression symbols). The other BERT model guides the crossover of individual genes by predicting the crossover point. Experimental results demonstrate that GESR significantly improves computational efficiency compared with traditional GP algorithms and achieves strong overall performance across multiple symbolic regression tasks.

---

## 论文详细总结（自动生成）

### 论文总结：GESR——基于基因编辑的遗传规划符号回归方法

#### 1. 核心问题与整体含义
符号回归（Symbolic Regression, SR）旨在从观测数据中自动发现简洁、可解释的数学表达式。传统的遗传规划（GP）是解决该问题的经典方法，但其核心操作——**变异和交叉完全是随机的**。这种“盲目搜索”导致进化效率低下，产生大量无效或有害的变异，减缓了收敛速度。
本文提出了 **GESR** 框架，引入了“**基因编辑**”的概念。其核心动机是利用深度学习模型（BERT）预见并执行有益的基因修改，将无序的随机搜索转化为数据驱动的定向优化，从而大幅提升进化效率和结果的准确性。

#### 2. 方法论
GESR 结合了多模态学习与遗传规划，主要包含以下关键技术：
*   **双 BERT 编辑器（上帝之手）：**
    *   **变异 BERT：** 将表达式树序列化，随机掩码（Mask）部分符号，结合数据集特征，利用掩码语言建模（MLM）预测最能提升拟合度（$R^2$）的符号。
    *   **交叉 BERT：** 输入两个父代序列及数据，预测最适合交换的子树根节点位置，实现定向交叉。
*   **数据编码器（Set Transformer）：** 针对回归数据的排列不变性，使用 Set Transformer 将原始数据 $(X, y)$ 编码为全局特征向量，作为 BERT 编辑的条件输入。
*   **训练数据构建（适应度提升导向）：** 
    *   通过连续松弛化变异函数和 BFGS 算法优化常数，筛选出变异/交叉后性能确实提升的样本作为训练集。
    *   确保模型学习的是“如何改进表达式”，而非简单的符号填充。
*   **算法流程：** 初始化种群 $\rightarrow$ BERT 指导变异 $\rightarrow$ BERT 指导交叉 $\rightarrow$ BFGS 常数优化 $\rightarrow$ 基于适应度筛选精英 $\rightarrow$ 迭代。

#### 3. 实验设计
*   **数据集：** 涵盖了 **SRBench** 中的 13 个标准基准组（如 Nguyen, Keijzer, Feynman, Strogatz 等）以及 **16 个复杂的混沌动力系统**（用于矢量场学习，如 Lorenz, Brusselator 等）。
*   **对比方法（Baselines）：** 
    *   经典/先进 GP 类：PySR, GPlearn, NGGP。
    *   深度学习/强化学习类：SNIP, uDSR, NeSymRes, TPSR, End2End。
*   **评估指标：** 拟合优度（$R^2$）、表达式复杂度（节点数）、推理时间、恢复率（Recovery Rate）、归一化编辑距离（NED）以及动力系统的短期轨迹预测一致性。

#### 4. 资源与算力
论文明确记录了预训练阶段的算力消耗：
*   **硬件：** 8 台 **NVIDIA A100 (80GB)** GPU。
*   **训练规模：** 训练集包含 1000 万至 2000 万个符号表达式样本。
*   **时长：** 预训练耗时约 **56 小时**（10 个 Epoch）。
*   **模型大小：** 每个检查点约 0.5 GB。

#### 5. 实验数量与充分性
实验设计非常详尽且具有高度的客观性：
*   **多维度测试：** 除了标准性能对比，还进行了消融实验（验证变异和交叉模块的各自贡献）、噪声鲁棒性测试（0-0.1 噪声水平）、高维数据扩展性测试（1D-3D）。
*   **公平性保障：** 严格控制种群大小、搜索空间限制（最大长度 80）和常数优化算法（统一使用 BFGS），确保对比在相同规则下进行。
*   **迁移性验证：** 特别将 BERT 模块插入到其他算法（如 NGGP）中，证明了该“基因编辑”模块是**即插即用**的，具有普适价值。

#### 6. 主要结论与发现
*   **效率飞跃：** GESR 在达到相同拟合精度（$R^2 > 0.999$）时，推理速度显著快于传统 GP 和其他神经引导方法。
*   **简洁性：** 相比 PySR 等方法，GESR 发现的公式节点数更少，有效抑制了 GP 常见的“表达式膨胀”问题。
*   **物理发现能力：** 在 16 个混沌系统中，GESR 能够更准确地重建矢量场，其短期轨迹预测（Rollout）的稳定性优于基准方法。
*   **引导作用：** 理论证明与实验均显示，BERT 引导的变异成功率（$\beta$）远高于随机变异（$\alpha$）。

#### 7. 优点
*   **创新性：** 首次将“基因编辑”概念引入进化计算，通过预训练模型解决了 GP 搜索的盲目性。
*   **多模态融合：** 成功将符号序列的语义信息与数值数据的分布特征结合。
*   **模块化：** 训练好的 BERT 编辑器可以作为通用组件增强现有的各种 GP 变体。
*   **鲁棒性：** 在中低水平噪声下表现稳定，且在有限样本（如 Feynman 数据集仅用 100 个点）下仍能保持高恢复率。

#### 8. 不足与局限
*   **预训练依赖：** 算法性能高度依赖于预训练数据的质量和覆盖面，对于完全脱离预训练分布的极端公式可能失效。
*   **维度限制：** 目前模型对输入变量的维度有一定限制（受限于预训练时的设置），在处理超高维（如数百个变量）科学数据时的扩展性尚待验证。
*   **计算开销：** 虽然推理阶段效率高，但前期需要巨大的算力进行大规模预训练。

（完）
